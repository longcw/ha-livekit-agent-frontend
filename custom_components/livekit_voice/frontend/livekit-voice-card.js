var Bf = { exports: {} }, uo = {};
var Gy;
function s_() {
  if (Gy) return uo;
  Gy = 1;
  var a = Symbol.for("react.transitional.element"), e = Symbol.for("react.fragment");
  function t(i, s, c) {
    var u = null;
    if (c !== void 0 && (u = "" + c), s.key !== void 0 && (u = "" + s.key), "key" in s) {
      c = {};
      for (var d in s)
        d !== "key" && (c[d] = s[d]);
    } else c = s;
    return s = c.ref, {
      $$typeof: a,
      type: i,
      key: u,
      ref: s !== void 0 ? s : null,
      props: c
    };
  }
  return uo.Fragment = e, uo.jsx = t, uo.jsxs = t, uo;
}
var Ky;
function o_() {
  return Ky || (Ky = 1, Bf.exports = s_()), Bf.exports;
}
var Se = o_(), zf = { exports: {} }, fo = {}, Ff = { exports: {} }, jf = {};
var Yy;
function c_() {
  return Yy || (Yy = 1, (function(a) {
    function e(q, te) {
      var le = q.length;
      q.push(te);
      e: for (; 0 < le; ) {
        var Me = le - 1 >>> 1, Ae = q[Me];
        if (0 < s(Ae, te))
          q[Me] = te, q[le] = Ae, le = Me;
        else break e;
      }
    }
    function t(q) {
      return q.length === 0 ? null : q[0];
    }
    function i(q) {
      if (q.length === 0) return null;
      var te = q[0], le = q.pop();
      if (le !== te) {
        q[0] = le;
        e: for (var Me = 0, Ae = q.length, w = Ae >>> 1; Me < w; ) {
          var K = 2 * (Me + 1) - 1, se = q[K], ce = K + 1, pe = q[ce];
          if (0 > s(se, le))
            ce < Ae && 0 > s(pe, se) ? (q[Me] = pe, q[ce] = le, Me = ce) : (q[Me] = se, q[K] = le, Me = K);
          else if (ce < Ae && 0 > s(pe, le))
            q[Me] = pe, q[ce] = le, Me = ce;
          else break e;
        }
      }
      return te;
    }
    function s(q, te) {
      var le = q.sortIndex - te.sortIndex;
      return le !== 0 ? le : q.id - te.id;
    }
    if (a.unstable_now = void 0, typeof performance == "object" && typeof performance.now == "function") {
      var c = performance;
      a.unstable_now = function() {
        return c.now();
      };
    } else {
      var u = Date, d = u.now();
      a.unstable_now = function() {
        return u.now() - d;
      };
    }
    var h = [], f = [], p = 1, v = null, y = 3, T = !1, _ = !1, k = !1, x = !1, A = typeof setTimeout == "function" ? setTimeout : null, B = typeof clearTimeout == "function" ? clearTimeout : null, ie = typeof setImmediate < "u" ? setImmediate : null;
    function C(q) {
      for (var te = t(f); te !== null; ) {
        if (te.callback === null) i(f);
        else if (te.startTime <= q)
          i(f), te.sortIndex = te.expirationTime, e(h, te);
        else break;
        te = t(f);
      }
    }
    function E(q) {
      if (k = !1, C(q), !_)
        if (t(h) !== null)
          _ = !0, M || (M = !0, ne());
        else {
          var te = t(f);
          te !== null && ke(E, te.startTime - q);
        }
    }
    var M = !1, N = -1, G = 5, W = -1;
    function Y() {
      return x ? !0 : !(a.unstable_now() - W < G);
    }
    function J() {
      if (x = !1, M) {
        var q = a.unstable_now();
        W = q;
        var te = !0;
        try {
          e: {
            _ = !1, k && (k = !1, B(N), N = -1), T = !0;
            var le = y;
            try {
              t: {
                for (C(q), v = t(h); v !== null && !(v.expirationTime > q && Y()); ) {
                  var Me = v.callback;
                  if (typeof Me == "function") {
                    v.callback = null, y = v.priorityLevel;
                    var Ae = Me(
                      v.expirationTime <= q
                    );
                    if (q = a.unstable_now(), typeof Ae == "function") {
                      v.callback = Ae, C(q), te = !0;
                      break t;
                    }
                    v === t(h) && i(h), C(q);
                  } else i(h);
                  v = t(h);
                }
                if (v !== null) te = !0;
                else {
                  var w = t(f);
                  w !== null && ke(
                    E,
                    w.startTime - q
                  ), te = !1;
                }
              }
              break e;
            } finally {
              v = null, y = le, T = !1;
            }
            te = void 0;
          }
        } finally {
          te ? ne() : M = !1;
        }
      }
    }
    var ne;
    if (typeof ie == "function")
      ne = function() {
        ie(J);
      };
    else if (typeof MessageChannel < "u") {
      var de = new MessageChannel(), fe = de.port2;
      de.port1.onmessage = J, ne = function() {
        fe.postMessage(null);
      };
    } else
      ne = function() {
        A(J, 0);
      };
    function ke(q, te) {
      N = A(function() {
        q(a.unstable_now());
      }, te);
    }
    a.unstable_IdlePriority = 5, a.unstable_ImmediatePriority = 1, a.unstable_LowPriority = 4, a.unstable_NormalPriority = 3, a.unstable_Profiling = null, a.unstable_UserBlockingPriority = 2, a.unstable_cancelCallback = function(q) {
      q.callback = null;
    }, a.unstable_forceFrameRate = function(q) {
      0 > q || 125 < q ? console.error(
        "forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported"
      ) : G = 0 < q ? Math.floor(1e3 / q) : 5;
    }, a.unstable_getCurrentPriorityLevel = function() {
      return y;
    }, a.unstable_next = function(q) {
      switch (y) {
        case 1:
        case 2:
        case 3:
          var te = 3;
          break;
        default:
          te = y;
      }
      var le = y;
      y = te;
      try {
        return q();
      } finally {
        y = le;
      }
    }, a.unstable_requestPaint = function() {
      x = !0;
    }, a.unstable_runWithPriority = function(q, te) {
      switch (q) {
        case 1:
        case 2:
        case 3:
        case 4:
        case 5:
          break;
        default:
          q = 3;
      }
      var le = y;
      y = q;
      try {
        return te();
      } finally {
        y = le;
      }
    }, a.unstable_scheduleCallback = function(q, te, le) {
      var Me = a.unstable_now();
      switch (typeof le == "object" && le !== null ? (le = le.delay, le = typeof le == "number" && 0 < le ? Me + le : Me) : le = Me, q) {
        case 1:
          var Ae = -1;
          break;
        case 2:
          Ae = 250;
          break;
        case 5:
          Ae = 1073741823;
          break;
        case 4:
          Ae = 1e4;
          break;
        default:
          Ae = 5e3;
      }
      return Ae = le + Ae, q = {
        id: p++,
        callback: te,
        priorityLevel: q,
        startTime: le,
        expirationTime: Ae,
        sortIndex: -1
      }, le > Me ? (q.sortIndex = le, e(f, q), t(h) === null && q === t(f) && (k ? (B(N), N = -1) : k = !0, ke(E, le - Me))) : (q.sortIndex = Ae, e(h, q), _ || T || (_ = !0, M || (M = !0, ne()))), q;
    }, a.unstable_shouldYield = Y, a.unstable_wrapCallback = function(q) {
      var te = y;
      return function() {
        var le = y;
        y = te;
        try {
          return q.apply(this, arguments);
        } finally {
          y = le;
        }
      };
    };
  })(jf)), jf;
}
var Jy;
function l_() {
  return Jy || (Jy = 1, Ff.exports = c_()), Ff.exports;
}
var qf = { exports: {} }, Ce = {};
var Qy;
function u_() {
  if (Qy) return Ce;
  Qy = 1;
  var a = Symbol.for("react.transitional.element"), e = Symbol.for("react.portal"), t = Symbol.for("react.fragment"), i = Symbol.for("react.strict_mode"), s = Symbol.for("react.profiler"), c = Symbol.for("react.consumer"), u = Symbol.for("react.context"), d = Symbol.for("react.forward_ref"), h = Symbol.for("react.suspense"), f = Symbol.for("react.memo"), p = Symbol.for("react.lazy"), v = Symbol.for("react.activity"), y = Symbol.iterator;
  function T(w) {
    return w === null || typeof w != "object" ? null : (w = y && w[y] || w["@@iterator"], typeof w == "function" ? w : null);
  }
  var _ = {
    isMounted: function() {
      return !1;
    },
    enqueueForceUpdate: function() {
    },
    enqueueReplaceState: function() {
    },
    enqueueSetState: function() {
    }
  }, k = Object.assign, x = {};
  function A(w, K, se) {
    this.props = w, this.context = K, this.refs = x, this.updater = se || _;
  }
  A.prototype.isReactComponent = {}, A.prototype.setState = function(w, K) {
    if (typeof w != "object" && typeof w != "function" && w != null)
      throw Error(
        "takes an object of state variables to update or a function which returns an object of state variables."
      );
    this.updater.enqueueSetState(this, w, K, "setState");
  }, A.prototype.forceUpdate = function(w) {
    this.updater.enqueueForceUpdate(this, w, "forceUpdate");
  };
  function B() {
  }
  B.prototype = A.prototype;
  function ie(w, K, se) {
    this.props = w, this.context = K, this.refs = x, this.updater = se || _;
  }
  var C = ie.prototype = new B();
  C.constructor = ie, k(C, A.prototype), C.isPureReactComponent = !0;
  var E = Array.isArray;
  function M() {
  }
  var N = { H: null, A: null, T: null, S: null }, G = Object.prototype.hasOwnProperty;
  function W(w, K, se) {
    var ce = se.ref;
    return {
      $$typeof: a,
      type: w,
      key: K,
      ref: ce !== void 0 ? ce : null,
      props: se
    };
  }
  function Y(w, K) {
    return W(w.type, K, w.props);
  }
  function J(w) {
    return typeof w == "object" && w !== null && w.$$typeof === a;
  }
  function ne(w) {
    var K = { "=": "=0", ":": "=2" };
    return "$" + w.replace(/[=:]/g, function(se) {
      return K[se];
    });
  }
  var de = /\/+/g;
  function fe(w, K) {
    return typeof w == "object" && w !== null && w.key != null ? ne("" + w.key) : K.toString(36);
  }
  function ke(w) {
    switch (w.status) {
      case "fulfilled":
        return w.value;
      case "rejected":
        throw w.reason;
      default:
        switch (typeof w.status == "string" ? w.then(M, M) : (w.status = "pending", w.then(
          function(K) {
            w.status === "pending" && (w.status = "fulfilled", w.value = K);
          },
          function(K) {
            w.status === "pending" && (w.status = "rejected", w.reason = K);
          }
        )), w.status) {
          case "fulfilled":
            return w.value;
          case "rejected":
            throw w.reason;
        }
    }
    throw w;
  }
  function q(w, K, se, ce, pe) {
    var oe = typeof w;
    (oe === "undefined" || oe === "boolean") && (w = null);
    var ue = !1;
    if (w === null) ue = !0;
    else
      switch (oe) {
        case "bigint":
        case "string":
        case "number":
          ue = !0;
          break;
        case "object":
          switch (w.$$typeof) {
            case a:
            case e:
              ue = !0;
              break;
            case p:
              return ue = w._init, q(
                ue(w._payload),
                K,
                se,
                ce,
                pe
              );
          }
      }
    if (ue)
      return pe = pe(w), ue = ce === "" ? "." + fe(w, 0) : ce, E(pe) ? (se = "", ue != null && (se = ue.replace(de, "$&/") + "/"), q(pe, K, se, "", function(jt) {
        return jt;
      })) : pe != null && (J(pe) && (pe = Y(
        pe,
        se + (pe.key == null || w && w.key === pe.key ? "" : ("" + pe.key).replace(
          de,
          "$&/"
        ) + "/") + ue
      )), K.push(pe)), 1;
    ue = 0;
    var Be = ce === "" ? "." : ce + ":";
    if (E(w))
      for (var je = 0; je < w.length; je++)
        ce = w[je], oe = Be + fe(ce, je), ue += q(
          ce,
          K,
          se,
          oe,
          pe
        );
    else if (je = T(w), typeof je == "function")
      for (w = je.call(w), je = 0; !(ce = w.next()).done; )
        ce = ce.value, oe = Be + fe(ce, je++), ue += q(
          ce,
          K,
          se,
          oe,
          pe
        );
    else if (oe === "object") {
      if (typeof w.then == "function")
        return q(
          ke(w),
          K,
          se,
          ce,
          pe
        );
      throw K = String(w), Error(
        "Objects are not valid as a React child (found: " + (K === "[object Object]" ? "object with keys {" + Object.keys(w).join(", ") + "}" : K) + "). If you meant to render a collection of children, use an array instead."
      );
    }
    return ue;
  }
  function te(w, K, se) {
    if (w == null) return w;
    var ce = [], pe = 0;
    return q(w, ce, "", "", function(oe) {
      return K.call(se, oe, pe++);
    }), ce;
  }
  function le(w) {
    if (w._status === -1) {
      var K = w._result;
      K = K(), K.then(
        function(se) {
          (w._status === 0 || w._status === -1) && (w._status = 1, w._result = se);
        },
        function(se) {
          (w._status === 0 || w._status === -1) && (w._status = 2, w._result = se);
        }
      ), w._status === -1 && (w._status = 0, w._result = K);
    }
    if (w._status === 1) return w._result.default;
    throw w._result;
  }
  var Me = typeof reportError == "function" ? reportError : function(w) {
    if (typeof window == "object" && typeof window.ErrorEvent == "function") {
      var K = new window.ErrorEvent("error", {
        bubbles: !0,
        cancelable: !0,
        message: typeof w == "object" && w !== null && typeof w.message == "string" ? String(w.message) : String(w),
        error: w
      });
      if (!window.dispatchEvent(K)) return;
    } else if (typeof process == "object" && typeof process.emit == "function") {
      process.emit("uncaughtException", w);
      return;
    }
    console.error(w);
  }, Ae = {
    map: te,
    forEach: function(w, K, se) {
      te(
        w,
        function() {
          K.apply(this, arguments);
        },
        se
      );
    },
    count: function(w) {
      var K = 0;
      return te(w, function() {
        K++;
      }), K;
    },
    toArray: function(w) {
      return te(w, function(K) {
        return K;
      }) || [];
    },
    only: function(w) {
      if (!J(w))
        throw Error(
          "React.Children.only expected to receive a single React element child."
        );
      return w;
    }
  };
  return Ce.Activity = v, Ce.Children = Ae, Ce.Component = A, Ce.Fragment = t, Ce.Profiler = s, Ce.PureComponent = ie, Ce.StrictMode = i, Ce.Suspense = h, Ce.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE = N, Ce.__COMPILER_RUNTIME = {
    __proto__: null,
    c: function(w) {
      return N.H.useMemoCache(w);
    }
  }, Ce.cache = function(w) {
    return function() {
      return w.apply(null, arguments);
    };
  }, Ce.cacheSignal = function() {
    return null;
  }, Ce.cloneElement = function(w, K, se) {
    if (w == null)
      throw Error(
        "The argument must be a React element, but you passed " + w + "."
      );
    var ce = k({}, w.props), pe = w.key;
    if (K != null)
      for (oe in K.key !== void 0 && (pe = "" + K.key), K)
        !G.call(K, oe) || oe === "key" || oe === "__self" || oe === "__source" || oe === "ref" && K.ref === void 0 || (ce[oe] = K[oe]);
    var oe = arguments.length - 2;
    if (oe === 1) ce.children = se;
    else if (1 < oe) {
      for (var ue = Array(oe), Be = 0; Be < oe; Be++)
        ue[Be] = arguments[Be + 2];
      ce.children = ue;
    }
    return W(w.type, pe, ce);
  }, Ce.createContext = function(w) {
    return w = {
      $$typeof: u,
      _currentValue: w,
      _currentValue2: w,
      _threadCount: 0,
      Provider: null,
      Consumer: null
    }, w.Provider = w, w.Consumer = {
      $$typeof: c,
      _context: w
    }, w;
  }, Ce.createElement = function(w, K, se) {
    var ce, pe = {}, oe = null;
    if (K != null)
      for (ce in K.key !== void 0 && (oe = "" + K.key), K)
        G.call(K, ce) && ce !== "key" && ce !== "__self" && ce !== "__source" && (pe[ce] = K[ce]);
    var ue = arguments.length - 2;
    if (ue === 1) pe.children = se;
    else if (1 < ue) {
      for (var Be = Array(ue), je = 0; je < ue; je++)
        Be[je] = arguments[je + 2];
      pe.children = Be;
    }
    if (w && w.defaultProps)
      for (ce in ue = w.defaultProps, ue)
        pe[ce] === void 0 && (pe[ce] = ue[ce]);
    return W(w, oe, pe);
  }, Ce.createRef = function() {
    return { current: null };
  }, Ce.forwardRef = function(w) {
    return { $$typeof: d, render: w };
  }, Ce.isValidElement = J, Ce.lazy = function(w) {
    return {
      $$typeof: p,
      _payload: { _status: -1, _result: w },
      _init: le
    };
  }, Ce.memo = function(w, K) {
    return {
      $$typeof: f,
      type: w,
      compare: K === void 0 ? null : K
    };
  }, Ce.startTransition = function(w) {
    var K = N.T, se = {};
    N.T = se;
    try {
      var ce = w(), pe = N.S;
      pe !== null && pe(se, ce), typeof ce == "object" && ce !== null && typeof ce.then == "function" && ce.then(M, Me);
    } catch (oe) {
      Me(oe);
    } finally {
      K !== null && se.types !== null && (K.types = se.types), N.T = K;
    }
  }, Ce.unstable_useCacheRefresh = function() {
    return N.H.useCacheRefresh();
  }, Ce.use = function(w) {
    return N.H.use(w);
  }, Ce.useActionState = function(w, K, se) {
    return N.H.useActionState(w, K, se);
  }, Ce.useCallback = function(w, K) {
    return N.H.useCallback(w, K);
  }, Ce.useContext = function(w) {
    return N.H.useContext(w);
  }, Ce.useDebugValue = function() {
  }, Ce.useDeferredValue = function(w, K) {
    return N.H.useDeferredValue(w, K);
  }, Ce.useEffect = function(w, K) {
    return N.H.useEffect(w, K);
  }, Ce.useEffectEvent = function(w) {
    return N.H.useEffectEvent(w);
  }, Ce.useId = function() {
    return N.H.useId();
  }, Ce.useImperativeHandle = function(w, K, se) {
    return N.H.useImperativeHandle(w, K, se);
  }, Ce.useInsertionEffect = function(w, K) {
    return N.H.useInsertionEffect(w, K);
  }, Ce.useLayoutEffect = function(w, K) {
    return N.H.useLayoutEffect(w, K);
  }, Ce.useMemo = function(w, K) {
    return N.H.useMemo(w, K);
  }, Ce.useOptimistic = function(w, K) {
    return N.H.useOptimistic(w, K);
  }, Ce.useReducer = function(w, K, se) {
    return N.H.useReducer(w, K, se);
  }, Ce.useRef = function(w) {
    return N.H.useRef(w);
  }, Ce.useState = function(w) {
    return N.H.useState(w);
  }, Ce.useSyncExternalStore = function(w, K, se) {
    return N.H.useSyncExternalStore(
      w,
      K,
      se
    );
  }, Ce.useTransition = function() {
    return N.H.useTransition();
  }, Ce.version = "19.2.7", Ce;
}
var Xy;
function Zh() {
  return Xy || (Xy = 1, qf.exports = u_()), qf.exports;
}
var Hf = { exports: {} }, Ut = {};
var Zy;
function d_() {
  if (Zy) return Ut;
  Zy = 1;
  var a = Zh();
  function e(h) {
    var f = "https://react.dev/errors/" + h;
    if (1 < arguments.length) {
      f += "?args[]=" + encodeURIComponent(arguments[1]);
      for (var p = 2; p < arguments.length; p++)
        f += "&args[]=" + encodeURIComponent(arguments[p]);
    }
    return "Minified React error #" + h + "; visit " + f + " for the full message or use the non-minified dev environment for full errors and additional helpful warnings.";
  }
  function t() {
  }
  var i = {
    d: {
      f: t,
      r: function() {
        throw Error(e(522));
      },
      D: t,
      C: t,
      L: t,
      m: t,
      X: t,
      S: t,
      M: t
    },
    p: 0,
    findDOMNode: null
  }, s = Symbol.for("react.portal");
  function c(h, f, p) {
    var v = 3 < arguments.length && arguments[3] !== void 0 ? arguments[3] : null;
    return {
      $$typeof: s,
      key: v == null ? null : "" + v,
      children: h,
      containerInfo: f,
      implementation: p
    };
  }
  var u = a.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE;
  function d(h, f) {
    if (h === "font") return "";
    if (typeof f == "string")
      return f === "use-credentials" ? f : "";
  }
  return Ut.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE = i, Ut.createPortal = function(h, f) {
    var p = 2 < arguments.length && arguments[2] !== void 0 ? arguments[2] : null;
    if (!f || f.nodeType !== 1 && f.nodeType !== 9 && f.nodeType !== 11)
      throw Error(e(299));
    return c(h, f, null, p);
  }, Ut.flushSync = function(h) {
    var f = u.T, p = i.p;
    try {
      if (u.T = null, i.p = 2, h) return h();
    } finally {
      u.T = f, i.p = p, i.d.f();
    }
  }, Ut.preconnect = function(h, f) {
    typeof h == "string" && (f ? (f = f.crossOrigin, f = typeof f == "string" ? f === "use-credentials" ? f : "" : void 0) : f = null, i.d.C(h, f));
  }, Ut.prefetchDNS = function(h) {
    typeof h == "string" && i.d.D(h);
  }, Ut.preinit = function(h, f) {
    if (typeof h == "string" && f && typeof f.as == "string") {
      var p = f.as, v = d(p, f.crossOrigin), y = typeof f.integrity == "string" ? f.integrity : void 0, T = typeof f.fetchPriority == "string" ? f.fetchPriority : void 0;
      p === "style" ? i.d.S(
        h,
        typeof f.precedence == "string" ? f.precedence : void 0,
        {
          crossOrigin: v,
          integrity: y,
          fetchPriority: T
        }
      ) : p === "script" && i.d.X(h, {
        crossOrigin: v,
        integrity: y,
        fetchPriority: T,
        nonce: typeof f.nonce == "string" ? f.nonce : void 0
      });
    }
  }, Ut.preinitModule = function(h, f) {
    if (typeof h == "string")
      if (typeof f == "object" && f !== null) {
        if (f.as == null || f.as === "script") {
          var p = d(
            f.as,
            f.crossOrigin
          );
          i.d.M(h, {
            crossOrigin: p,
            integrity: typeof f.integrity == "string" ? f.integrity : void 0,
            nonce: typeof f.nonce == "string" ? f.nonce : void 0
          });
        }
      } else f == null && i.d.M(h);
  }, Ut.preload = function(h, f) {
    if (typeof h == "string" && typeof f == "object" && f !== null && typeof f.as == "string") {
      var p = f.as, v = d(p, f.crossOrigin);
      i.d.L(h, p, {
        crossOrigin: v,
        integrity: typeof f.integrity == "string" ? f.integrity : void 0,
        nonce: typeof f.nonce == "string" ? f.nonce : void 0,
        type: typeof f.type == "string" ? f.type : void 0,
        fetchPriority: typeof f.fetchPriority == "string" ? f.fetchPriority : void 0,
        referrerPolicy: typeof f.referrerPolicy == "string" ? f.referrerPolicy : void 0,
        imageSrcSet: typeof f.imageSrcSet == "string" ? f.imageSrcSet : void 0,
        imageSizes: typeof f.imageSizes == "string" ? f.imageSizes : void 0,
        media: typeof f.media == "string" ? f.media : void 0
      });
    }
  }, Ut.preloadModule = function(h, f) {
    if (typeof h == "string")
      if (f) {
        var p = d(f.as, f.crossOrigin);
        i.d.m(h, {
          as: typeof f.as == "string" && f.as !== "script" ? f.as : void 0,
          crossOrigin: p,
          integrity: typeof f.integrity == "string" ? f.integrity : void 0
        });
      } else i.d.m(h);
  }, Ut.requestFormReset = function(h) {
    i.d.r(h);
  }, Ut.unstable_batchedUpdates = function(h, f) {
    return h(f);
  }, Ut.useFormState = function(h, f, p) {
    return u.H.useFormState(h, f, p);
  }, Ut.useFormStatus = function() {
    return u.H.useHostTransitionStatus();
  }, Ut.version = "19.2.7", Ut;
}
var Wy;
function f_() {
  if (Wy) return Hf.exports;
  Wy = 1;
  function a() {
    if (!(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ > "u" || typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE != "function"))
      try {
        __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(a);
      } catch (e) {
        console.error(e);
      }
  }
  return a(), Hf.exports = d_(), Hf.exports;
}
var $y;
function h_() {
  if ($y) return fo;
  $y = 1;
  var a = l_(), e = Zh(), t = f_();
  function i(n) {
    var r = "https://react.dev/errors/" + n;
    if (1 < arguments.length) {
      r += "?args[]=" + encodeURIComponent(arguments[1]);
      for (var o = 2; o < arguments.length; o++)
        r += "&args[]=" + encodeURIComponent(arguments[o]);
    }
    return "Minified React error #" + n + "; visit " + r + " for the full message or use the non-minified dev environment for full errors and additional helpful warnings.";
  }
  function s(n) {
    return !(!n || n.nodeType !== 1 && n.nodeType !== 9 && n.nodeType !== 11);
  }
  function c(n) {
    var r = n, o = n;
    if (n.alternate) for (; r.return; ) r = r.return;
    else {
      n = r;
      do
        r = n, (r.flags & 4098) !== 0 && (o = r.return), n = r.return;
      while (n);
    }
    return r.tag === 3 ? o : null;
  }
  function u(n) {
    if (n.tag === 13) {
      var r = n.memoizedState;
      if (r === null && (n = n.alternate, n !== null && (r = n.memoizedState)), r !== null) return r.dehydrated;
    }
    return null;
  }
  function d(n) {
    if (n.tag === 31) {
      var r = n.memoizedState;
      if (r === null && (n = n.alternate, n !== null && (r = n.memoizedState)), r !== null) return r.dehydrated;
    }
    return null;
  }
  function h(n) {
    if (c(n) !== n)
      throw Error(i(188));
  }
  function f(n) {
    var r = n.alternate;
    if (!r) {
      if (r = c(n), r === null) throw Error(i(188));
      return r !== n ? null : n;
    }
    for (var o = n, l = r; ; ) {
      var m = o.return;
      if (m === null) break;
      var g = m.alternate;
      if (g === null) {
        if (l = m.return, l !== null) {
          o = l;
          continue;
        }
        break;
      }
      if (m.child === g.child) {
        for (g = m.child; g; ) {
          if (g === o) return h(m), n;
          if (g === l) return h(m), r;
          g = g.sibling;
        }
        throw Error(i(188));
      }
      if (o.return !== l.return) o = m, l = g;
      else {
        for (var b = !1, S = m.child; S; ) {
          if (S === o) {
            b = !0, o = m, l = g;
            break;
          }
          if (S === l) {
            b = !0, l = m, o = g;
            break;
          }
          S = S.sibling;
        }
        if (!b) {
          for (S = g.child; S; ) {
            if (S === o) {
              b = !0, o = g, l = m;
              break;
            }
            if (S === l) {
              b = !0, l = g, o = m;
              break;
            }
            S = S.sibling;
          }
          if (!b) throw Error(i(189));
        }
      }
      if (o.alternate !== l) throw Error(i(190));
    }
    if (o.tag !== 3) throw Error(i(188));
    return o.stateNode.current === o ? n : r;
  }
  function p(n) {
    var r = n.tag;
    if (r === 5 || r === 26 || r === 27 || r === 6) return n;
    for (n = n.child; n !== null; ) {
      if (r = p(n), r !== null) return r;
      n = n.sibling;
    }
    return null;
  }
  var v = Object.assign, y = Symbol.for("react.element"), T = Symbol.for("react.transitional.element"), _ = Symbol.for("react.portal"), k = Symbol.for("react.fragment"), x = Symbol.for("react.strict_mode"), A = Symbol.for("react.profiler"), B = Symbol.for("react.consumer"), ie = Symbol.for("react.context"), C = Symbol.for("react.forward_ref"), E = Symbol.for("react.suspense"), M = Symbol.for("react.suspense_list"), N = Symbol.for("react.memo"), G = Symbol.for("react.lazy"), W = Symbol.for("react.activity"), Y = Symbol.for("react.memo_cache_sentinel"), J = Symbol.iterator;
  function ne(n) {
    return n === null || typeof n != "object" ? null : (n = J && n[J] || n["@@iterator"], typeof n == "function" ? n : null);
  }
  var de = Symbol.for("react.client.reference");
  function fe(n) {
    if (n == null) return null;
    if (typeof n == "function")
      return n.$$typeof === de ? null : n.displayName || n.name || null;
    if (typeof n == "string") return n;
    switch (n) {
      case k:
        return "Fragment";
      case A:
        return "Profiler";
      case x:
        return "StrictMode";
      case E:
        return "Suspense";
      case M:
        return "SuspenseList";
      case W:
        return "Activity";
    }
    if (typeof n == "object")
      switch (n.$$typeof) {
        case _:
          return "Portal";
        case ie:
          return n.displayName || "Context";
        case B:
          return (n._context.displayName || "Context") + ".Consumer";
        case C:
          var r = n.render;
          return n = n.displayName, n || (n = r.displayName || r.name || "", n = n !== "" ? "ForwardRef(" + n + ")" : "ForwardRef"), n;
        case N:
          return r = n.displayName || null, r !== null ? r : fe(n.type) || "Memo";
        case G:
          r = n._payload, n = n._init;
          try {
            return fe(n(r));
          } catch {
          }
      }
    return null;
  }
  var ke = Array.isArray, q = e.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, te = t.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE, le = {
    pending: !1,
    data: null,
    method: null,
    action: null
  }, Me = [], Ae = -1;
  function w(n) {
    return { current: n };
  }
  function K(n) {
    0 > Ae || (n.current = Me[Ae], Me[Ae] = null, Ae--);
  }
  function se(n, r) {
    Ae++, Me[Ae] = n.current, n.current = r;
  }
  var ce = w(null), pe = w(null), oe = w(null), ue = w(null);
  function Be(n, r) {
    switch (se(oe, r), se(pe, n), se(ce, null), r.nodeType) {
      case 9:
      case 11:
        n = (n = r.documentElement) && (n = n.namespaceURI) ? my(n) : 0;
        break;
      default:
        if (n = r.tagName, r = r.namespaceURI)
          r = my(r), n = py(r, n);
        else
          switch (n) {
            case "svg":
              n = 1;
              break;
            case "math":
              n = 2;
              break;
            default:
              n = 0;
          }
    }
    K(ce), se(ce, n);
  }
  function je() {
    K(ce), K(pe), K(oe);
  }
  function jt(n) {
    n.memoizedState !== null && se(ue, n);
    var r = ce.current, o = py(r, n.type);
    r !== o && (se(pe, n), se(ce, o));
  }
  function li(n) {
    pe.current === n && (K(ce), K(pe)), ue.current === n && (K(ue), so._currentValue = le);
  }
  var cr, Vo;
  function Wn(n) {
    if (cr === void 0)
      try {
        throw Error();
      } catch (o) {
        var r = o.stack.trim().match(/\n( *(at )?)/);
        cr = r && r[1] || "", Vo = -1 < o.stack.indexOf(`
    at`) ? " (<anonymous>)" : -1 < o.stack.indexOf("@") ? "@unknown:0:0" : "";
      }
    return `
` + cr + n + Vo;
  }
  var Tu = !1;
  function Su(n, r) {
    if (!n || Tu) return "";
    Tu = !0;
    var o = Error.prepareStackTrace;
    Error.prepareStackTrace = void 0;
    try {
      var l = {
        DetermineComponentFrameRoot: function() {
          try {
            if (r) {
              var $ = function() {
                throw Error();
              };
              if (Object.defineProperty($.prototype, "props", {
                set: function() {
                  throw Error();
                }
              }), typeof Reflect == "object" && Reflect.construct) {
                try {
                  Reflect.construct($, []);
                } catch (V) {
                  var F = V;
                }
                Reflect.construct(n, [], $);
              } else {
                try {
                  $.call();
                } catch (V) {
                  F = V;
                }
                n.call($.prototype);
              }
            } else {
              try {
                throw Error();
              } catch (V) {
                F = V;
              }
              ($ = n()) && typeof $.catch == "function" && $.catch(function() {
              });
            }
          } catch (V) {
            if (V && F && typeof V.stack == "string")
              return [V.stack, F.stack];
          }
          return [null, null];
        }
      };
      l.DetermineComponentFrameRoot.displayName = "DetermineComponentFrameRoot";
      var m = Object.getOwnPropertyDescriptor(
        l.DetermineComponentFrameRoot,
        "name"
      );
      m && m.configurable && Object.defineProperty(
        l.DetermineComponentFrameRoot,
        "name",
        { value: "DetermineComponentFrameRoot" }
      );
      var g = l.DetermineComponentFrameRoot(), b = g[0], S = g[1];
      if (b && S) {
        var O = b.split(`
`), z = S.split(`
`);
        for (m = l = 0; l < O.length && !O[l].includes("DetermineComponentFrameRoot"); )
          l++;
        for (; m < z.length && !z[m].includes(
          "DetermineComponentFrameRoot"
        ); )
          m++;
        if (l === O.length || m === z.length)
          for (l = O.length - 1, m = z.length - 1; 1 <= l && 0 <= m && O[l] !== z[m]; )
            m--;
        for (; 1 <= l && 0 <= m; l--, m--)
          if (O[l] !== z[m]) {
            if (l !== 1 || m !== 1)
              do
                if (l--, m--, 0 > m || O[l] !== z[m]) {
                  var Q = `
` + O[l].replace(" at new ", " at ");
                  return n.displayName && Q.includes("<anonymous>") && (Q = Q.replace("<anonymous>", n.displayName)), Q;
                }
              while (1 <= l && 0 <= m);
            break;
          }
      }
    } finally {
      Tu = !1, Error.prepareStackTrace = o;
    }
    return (o = n ? n.displayName || n.name : "") ? Wn(o) : "";
  }
  function Uk(n, r) {
    switch (n.tag) {
      case 26:
      case 27:
      case 5:
        return Wn(n.type);
      case 16:
        return Wn("Lazy");
      case 13:
        return n.child !== r && r !== null ? Wn("Suspense Fallback") : Wn("Suspense");
      case 19:
        return Wn("SuspenseList");
      case 0:
      case 15:
        return Su(n.type, !1);
      case 11:
        return Su(n.type.render, !1);
      case 1:
        return Su(n.type, !0);
      case 31:
        return Wn("Activity");
      default:
        return "";
    }
  }
  function Gm(n) {
    try {
      var r = "", o = null;
      do
        r += Uk(n, o), o = n, n = n.return;
      while (n);
      return r;
    } catch (l) {
      return `
Error generating stack: ` + l.message + `
` + l.stack;
    }
  }
  var ku = Object.prototype.hasOwnProperty, Cu = a.unstable_scheduleCallback, Eu = a.unstable_cancelCallback, Bk = a.unstable_shouldYield, zk = a.unstable_requestPaint, on = a.unstable_now, Fk = a.unstable_getCurrentPriorityLevel, Km = a.unstable_ImmediatePriority, Ym = a.unstable_UserBlockingPriority, Go = a.unstable_NormalPriority, jk = a.unstable_LowPriority, Jm = a.unstable_IdlePriority, qk = a.log, Hk = a.unstable_setDisableYieldValue, ys = null, cn = null;
  function ji(n) {
    if (typeof qk == "function" && Hk(n), cn && typeof cn.setStrictMode == "function")
      try {
        cn.setStrictMode(ys, n);
      } catch {
      }
  }
  var ln = Math.clz32 ? Math.clz32 : Kk, Vk = Math.log, Gk = Math.LN2;
  function Kk(n) {
    return n >>>= 0, n === 0 ? 32 : 31 - (Vk(n) / Gk | 0) | 0;
  }
  var Ko = 256, Yo = 262144, Jo = 4194304;
  function Ma(n) {
    var r = n & 42;
    if (r !== 0) return r;
    switch (n & -n) {
      case 1:
        return 1;
      case 2:
        return 2;
      case 4:
        return 4;
      case 8:
        return 8;
      case 16:
        return 16;
      case 32:
        return 32;
      case 64:
        return 64;
      case 128:
        return 128;
      case 256:
      case 512:
      case 1024:
      case 2048:
      case 4096:
      case 8192:
      case 16384:
      case 32768:
      case 65536:
      case 131072:
        return n & 261888;
      case 262144:
      case 524288:
      case 1048576:
      case 2097152:
        return n & 3932160;
      case 4194304:
      case 8388608:
      case 16777216:
      case 33554432:
        return n & 62914560;
      case 67108864:
        return 67108864;
      case 134217728:
        return 134217728;
      case 268435456:
        return 268435456;
      case 536870912:
        return 536870912;
      case 1073741824:
        return 0;
      default:
        return n;
    }
  }
  function Qo(n, r, o) {
    var l = n.pendingLanes;
    if (l === 0) return 0;
    var m = 0, g = n.suspendedLanes, b = n.pingedLanes;
    n = n.warmLanes;
    var S = l & 134217727;
    return S !== 0 ? (l = S & ~g, l !== 0 ? m = Ma(l) : (b &= S, b !== 0 ? m = Ma(b) : o || (o = S & ~n, o !== 0 && (m = Ma(o))))) : (S = l & ~g, S !== 0 ? m = Ma(S) : b !== 0 ? m = Ma(b) : o || (o = l & ~n, o !== 0 && (m = Ma(o)))), m === 0 ? 0 : r !== 0 && r !== m && (r & g) === 0 && (g = m & -m, o = r & -r, g >= o || g === 32 && (o & 4194048) !== 0) ? r : m;
  }
  function bs(n, r) {
    return (n.pendingLanes & ~(n.suspendedLanes & ~n.pingedLanes) & r) === 0;
  }
  function Yk(n, r) {
    switch (n) {
      case 1:
      case 2:
      case 4:
      case 8:
      case 64:
        return r + 250;
      case 16:
      case 32:
      case 128:
      case 256:
      case 512:
      case 1024:
      case 2048:
      case 4096:
      case 8192:
      case 16384:
      case 32768:
      case 65536:
      case 131072:
      case 262144:
      case 524288:
      case 1048576:
      case 2097152:
        return r + 5e3;
      case 4194304:
      case 8388608:
      case 16777216:
      case 33554432:
        return -1;
      case 67108864:
      case 134217728:
      case 268435456:
      case 536870912:
      case 1073741824:
        return -1;
      default:
        return -1;
    }
  }
  function Qm() {
    var n = Jo;
    return Jo <<= 1, (Jo & 62914560) === 0 && (Jo = 4194304), n;
  }
  function _u(n) {
    for (var r = [], o = 0; 31 > o; o++) r.push(n);
    return r;
  }
  function Ts(n, r) {
    n.pendingLanes |= r, r !== 268435456 && (n.suspendedLanes = 0, n.pingedLanes = 0, n.warmLanes = 0);
  }
  function Jk(n, r, o, l, m, g) {
    var b = n.pendingLanes;
    n.pendingLanes = o, n.suspendedLanes = 0, n.pingedLanes = 0, n.warmLanes = 0, n.expiredLanes &= o, n.entangledLanes &= o, n.errorRecoveryDisabledLanes &= o, n.shellSuspendCounter = 0;
    var S = n.entanglements, O = n.expirationTimes, z = n.hiddenUpdates;
    for (o = b & ~o; 0 < o; ) {
      var Q = 31 - ln(o), $ = 1 << Q;
      S[Q] = 0, O[Q] = -1;
      var F = z[Q];
      if (F !== null)
        for (z[Q] = null, Q = 0; Q < F.length; Q++) {
          var V = F[Q];
          V !== null && (V.lane &= -536870913);
        }
      o &= ~$;
    }
    l !== 0 && Xm(n, l, 0), g !== 0 && m === 0 && n.tag !== 0 && (n.suspendedLanes |= g & ~(b & ~r));
  }
  function Xm(n, r, o) {
    n.pendingLanes |= r, n.suspendedLanes &= ~r;
    var l = 31 - ln(r);
    n.entangledLanes |= r, n.entanglements[l] = n.entanglements[l] | 1073741824 | o & 261930;
  }
  function Zm(n, r) {
    var o = n.entangledLanes |= r;
    for (n = n.entanglements; o; ) {
      var l = 31 - ln(o), m = 1 << l;
      m & r | n[l] & r && (n[l] |= r), o &= ~m;
    }
  }
  function Wm(n, r) {
    var o = r & -r;
    return o = (o & 42) !== 0 ? 1 : Ru(o), (o & (n.suspendedLanes | r)) !== 0 ? 0 : o;
  }
  function Ru(n) {
    switch (n) {
      case 2:
        n = 1;
        break;
      case 8:
        n = 4;
        break;
      case 32:
        n = 16;
        break;
      case 256:
      case 512:
      case 1024:
      case 2048:
      case 4096:
      case 8192:
      case 16384:
      case 32768:
      case 65536:
      case 131072:
      case 262144:
      case 524288:
      case 1048576:
      case 2097152:
      case 4194304:
      case 8388608:
      case 16777216:
      case 33554432:
        n = 128;
        break;
      case 268435456:
        n = 134217728;
        break;
      default:
        n = 0;
    }
    return n;
  }
  function wu(n) {
    return n &= -n, 2 < n ? 8 < n ? (n & 134217727) !== 0 ? 32 : 268435456 : 8 : 2;
  }
  function $m() {
    var n = te.p;
    return n !== 0 ? n : (n = window.event, n === void 0 ? 32 : By(n.type));
  }
  function ep(n, r) {
    var o = te.p;
    try {
      return te.p = n, r();
    } finally {
      te.p = o;
    }
  }
  var qi = Math.random().toString(36).slice(2), At = "__reactFiber$" + qi, Jt = "__reactProps$" + qi, lr = "__reactContainer$" + qi, Mu = "__reactEvents$" + qi, Qk = "__reactListeners$" + qi, Xk = "__reactHandles$" + qi, tp = "__reactResources$" + qi, Ss = "__reactMarker$" + qi;
  function Au(n) {
    delete n[At], delete n[Jt], delete n[Mu], delete n[Qk], delete n[Xk];
  }
  function ur(n) {
    var r = n[At];
    if (r) return r;
    for (var o = n.parentNode; o; ) {
      if (r = o[lr] || o[At]) {
        if (o = r.alternate, r.child !== null || o !== null && o.child !== null)
          for (n = ky(n); n !== null; ) {
            if (o = n[At]) return o;
            n = ky(n);
          }
        return r;
      }
      n = o, o = n.parentNode;
    }
    return null;
  }
  function dr(n) {
    if (n = n[At] || n[lr]) {
      var r = n.tag;
      if (r === 5 || r === 6 || r === 13 || r === 31 || r === 26 || r === 27 || r === 3)
        return n;
    }
    return null;
  }
  function ks(n) {
    var r = n.tag;
    if (r === 5 || r === 26 || r === 27 || r === 6) return n.stateNode;
    throw Error(i(33));
  }
  function fr(n) {
    var r = n[tp];
    return r || (r = n[tp] = { hoistableStyles: /* @__PURE__ */ new Map(), hoistableScripts: /* @__PURE__ */ new Map() }), r;
  }
  function Ct(n) {
    n[Ss] = !0;
  }
  var np = /* @__PURE__ */ new Set(), ip = {};
  function Aa(n, r) {
    hr(n, r), hr(n + "Capture", r);
  }
  function hr(n, r) {
    for (ip[n] = r, n = 0; n < r.length; n++)
      np.add(r[n]);
  }
  var Zk = RegExp(
    "^[:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD][:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$"
  ), ap = {}, rp = {};
  function Wk(n) {
    return ku.call(rp, n) ? !0 : ku.call(ap, n) ? !1 : Zk.test(n) ? rp[n] = !0 : (ap[n] = !0, !1);
  }
  function Xo(n, r, o) {
    if (Wk(r))
      if (o === null) n.removeAttribute(r);
      else {
        switch (typeof o) {
          case "undefined":
          case "function":
          case "symbol":
            n.removeAttribute(r);
            return;
          case "boolean":
            var l = r.toLowerCase().slice(0, 5);
            if (l !== "data-" && l !== "aria-") {
              n.removeAttribute(r);
              return;
            }
        }
        n.setAttribute(r, "" + o);
      }
  }
  function Zo(n, r, o) {
    if (o === null) n.removeAttribute(r);
    else {
      switch (typeof o) {
        case "undefined":
        case "function":
        case "symbol":
        case "boolean":
          n.removeAttribute(r);
          return;
      }
      n.setAttribute(r, "" + o);
    }
  }
  function ui(n, r, o, l) {
    if (l === null) n.removeAttribute(o);
    else {
      switch (typeof l) {
        case "undefined":
        case "function":
        case "symbol":
        case "boolean":
          n.removeAttribute(o);
          return;
      }
      n.setAttributeNS(r, o, "" + l);
    }
  }
  function bn(n) {
    switch (typeof n) {
      case "bigint":
      case "boolean":
      case "number":
      case "string":
      case "undefined":
        return n;
      case "object":
        return n;
      default:
        return "";
    }
  }
  function sp(n) {
    var r = n.type;
    return (n = n.nodeName) && n.toLowerCase() === "input" && (r === "checkbox" || r === "radio");
  }
  function $k(n, r, o) {
    var l = Object.getOwnPropertyDescriptor(
      n.constructor.prototype,
      r
    );
    if (!n.hasOwnProperty(r) && typeof l < "u" && typeof l.get == "function" && typeof l.set == "function") {
      var m = l.get, g = l.set;
      return Object.defineProperty(n, r, {
        configurable: !0,
        get: function() {
          return m.call(this);
        },
        set: function(b) {
          o = "" + b, g.call(this, b);
        }
      }), Object.defineProperty(n, r, {
        enumerable: l.enumerable
      }), {
        getValue: function() {
          return o;
        },
        setValue: function(b) {
          o = "" + b;
        },
        stopTracking: function() {
          n._valueTracker = null, delete n[r];
        }
      };
    }
  }
  function Ou(n) {
    if (!n._valueTracker) {
      var r = sp(n) ? "checked" : "value";
      n._valueTracker = $k(
        n,
        r,
        "" + n[r]
      );
    }
  }
  function op(n) {
    if (!n) return !1;
    var r = n._valueTracker;
    if (!r) return !0;
    var o = r.getValue(), l = "";
    return n && (l = sp(n) ? n.checked ? "true" : "false" : n.value), n = l, n !== o ? (r.setValue(n), !0) : !1;
  }
  function Wo(n) {
    if (n = n || (typeof document < "u" ? document : void 0), typeof n > "u") return null;
    try {
      return n.activeElement || n.body;
    } catch {
      return n.body;
    }
  }
  var eC = /[\n"\\]/g;
  function Tn(n) {
    return n.replace(
      eC,
      function(r) {
        return "\\" + r.charCodeAt(0).toString(16) + " ";
      }
    );
  }
  function Du(n, r, o, l, m, g, b, S) {
    n.name = "", b != null && typeof b != "function" && typeof b != "symbol" && typeof b != "boolean" ? n.type = b : n.removeAttribute("type"), r != null ? b === "number" ? (r === 0 && n.value === "" || n.value != r) && (n.value = "" + bn(r)) : n.value !== "" + bn(r) && (n.value = "" + bn(r)) : b !== "submit" && b !== "reset" || n.removeAttribute("value"), r != null ? Pu(n, b, bn(r)) : o != null ? Pu(n, b, bn(o)) : l != null && n.removeAttribute("value"), m == null && g != null && (n.defaultChecked = !!g), m != null && (n.checked = m && typeof m != "function" && typeof m != "symbol"), S != null && typeof S != "function" && typeof S != "symbol" && typeof S != "boolean" ? n.name = "" + bn(S) : n.removeAttribute("name");
  }
  function cp(n, r, o, l, m, g, b, S) {
    if (g != null && typeof g != "function" && typeof g != "symbol" && typeof g != "boolean" && (n.type = g), r != null || o != null) {
      if (!(g !== "submit" && g !== "reset" || r != null)) {
        Ou(n);
        return;
      }
      o = o != null ? "" + bn(o) : "", r = r != null ? "" + bn(r) : o, S || r === n.value || (n.value = r), n.defaultValue = r;
    }
    l = l ?? m, l = typeof l != "function" && typeof l != "symbol" && !!l, n.checked = S ? n.checked : !!l, n.defaultChecked = !!l, b != null && typeof b != "function" && typeof b != "symbol" && typeof b != "boolean" && (n.name = b), Ou(n);
  }
  function Pu(n, r, o) {
    r === "number" && Wo(n.ownerDocument) === n || n.defaultValue === "" + o || (n.defaultValue = "" + o);
  }
  function mr(n, r, o, l) {
    if (n = n.options, r) {
      r = {};
      for (var m = 0; m < o.length; m++)
        r["$" + o[m]] = !0;
      for (o = 0; o < n.length; o++)
        m = r.hasOwnProperty("$" + n[o].value), n[o].selected !== m && (n[o].selected = m), m && l && (n[o].defaultSelected = !0);
    } else {
      for (o = "" + bn(o), r = null, m = 0; m < n.length; m++) {
        if (n[m].value === o) {
          n[m].selected = !0, l && (n[m].defaultSelected = !0);
          return;
        }
        r !== null || n[m].disabled || (r = n[m]);
      }
      r !== null && (r.selected = !0);
    }
  }
  function lp(n, r, o) {
    if (r != null && (r = "" + bn(r), r !== n.value && (n.value = r), o == null)) {
      n.defaultValue !== r && (n.defaultValue = r);
      return;
    }
    n.defaultValue = o != null ? "" + bn(o) : "";
  }
  function up(n, r, o, l) {
    if (r == null) {
      if (l != null) {
        if (o != null) throw Error(i(92));
        if (ke(l)) {
          if (1 < l.length) throw Error(i(93));
          l = l[0];
        }
        o = l;
      }
      o == null && (o = ""), r = o;
    }
    o = bn(r), n.defaultValue = o, l = n.textContent, l === o && l !== "" && l !== null && (n.value = l), Ou(n);
  }
  function pr(n, r) {
    if (r) {
      var o = n.firstChild;
      if (o && o === n.lastChild && o.nodeType === 3) {
        o.nodeValue = r;
        return;
      }
    }
    n.textContent = r;
  }
  var tC = new Set(
    "animationIterationCount aspectRatio borderImageOutset borderImageSlice borderImageWidth boxFlex boxFlexGroup boxOrdinalGroup columnCount columns flex flexGrow flexPositive flexShrink flexNegative flexOrder gridArea gridRow gridRowEnd gridRowSpan gridRowStart gridColumn gridColumnEnd gridColumnSpan gridColumnStart fontWeight lineClamp lineHeight opacity order orphans scale tabSize widows zIndex zoom fillOpacity floodOpacity stopOpacity strokeDasharray strokeDashoffset strokeMiterlimit strokeOpacity strokeWidth MozAnimationIterationCount MozBoxFlex MozBoxFlexGroup MozLineClamp msAnimationIterationCount msFlex msZoom msFlexGrow msFlexNegative msFlexOrder msFlexPositive msFlexShrink msGridColumn msGridColumnSpan msGridRow msGridRowSpan WebkitAnimationIterationCount WebkitBoxFlex WebKitBoxFlexGroup WebkitBoxOrdinalGroup WebkitColumnCount WebkitColumns WebkitFlex WebkitFlexGrow WebkitFlexPositive WebkitFlexShrink WebkitLineClamp".split(
      " "
    )
  );
  function dp(n, r, o) {
    var l = r.indexOf("--") === 0;
    o == null || typeof o == "boolean" || o === "" ? l ? n.setProperty(r, "") : r === "float" ? n.cssFloat = "" : n[r] = "" : l ? n.setProperty(r, o) : typeof o != "number" || o === 0 || tC.has(r) ? r === "float" ? n.cssFloat = o : n[r] = ("" + o).trim() : n[r] = o + "px";
  }
  function fp(n, r, o) {
    if (r != null && typeof r != "object")
      throw Error(i(62));
    if (n = n.style, o != null) {
      for (var l in o)
        !o.hasOwnProperty(l) || r != null && r.hasOwnProperty(l) || (l.indexOf("--") === 0 ? n.setProperty(l, "") : l === "float" ? n.cssFloat = "" : n[l] = "");
      for (var m in r)
        l = r[m], r.hasOwnProperty(m) && o[m] !== l && dp(n, m, l);
    } else
      for (var g in r)
        r.hasOwnProperty(g) && dp(n, g, r[g]);
  }
  function xu(n) {
    if (n.indexOf("-") === -1) return !1;
    switch (n) {
      case "annotation-xml":
      case "color-profile":
      case "font-face":
      case "font-face-src":
      case "font-face-uri":
      case "font-face-format":
      case "font-face-name":
      case "missing-glyph":
        return !1;
      default:
        return !0;
    }
  }
  var nC = /* @__PURE__ */ new Map([
    ["acceptCharset", "accept-charset"],
    ["htmlFor", "for"],
    ["httpEquiv", "http-equiv"],
    ["crossOrigin", "crossorigin"],
    ["accentHeight", "accent-height"],
    ["alignmentBaseline", "alignment-baseline"],
    ["arabicForm", "arabic-form"],
    ["baselineShift", "baseline-shift"],
    ["capHeight", "cap-height"],
    ["clipPath", "clip-path"],
    ["clipRule", "clip-rule"],
    ["colorInterpolation", "color-interpolation"],
    ["colorInterpolationFilters", "color-interpolation-filters"],
    ["colorProfile", "color-profile"],
    ["colorRendering", "color-rendering"],
    ["dominantBaseline", "dominant-baseline"],
    ["enableBackground", "enable-background"],
    ["fillOpacity", "fill-opacity"],
    ["fillRule", "fill-rule"],
    ["floodColor", "flood-color"],
    ["floodOpacity", "flood-opacity"],
    ["fontFamily", "font-family"],
    ["fontSize", "font-size"],
    ["fontSizeAdjust", "font-size-adjust"],
    ["fontStretch", "font-stretch"],
    ["fontStyle", "font-style"],
    ["fontVariant", "font-variant"],
    ["fontWeight", "font-weight"],
    ["glyphName", "glyph-name"],
    ["glyphOrientationHorizontal", "glyph-orientation-horizontal"],
    ["glyphOrientationVertical", "glyph-orientation-vertical"],
    ["horizAdvX", "horiz-adv-x"],
    ["horizOriginX", "horiz-origin-x"],
    ["imageRendering", "image-rendering"],
    ["letterSpacing", "letter-spacing"],
    ["lightingColor", "lighting-color"],
    ["markerEnd", "marker-end"],
    ["markerMid", "marker-mid"],
    ["markerStart", "marker-start"],
    ["overlinePosition", "overline-position"],
    ["overlineThickness", "overline-thickness"],
    ["paintOrder", "paint-order"],
    ["panose-1", "panose-1"],
    ["pointerEvents", "pointer-events"],
    ["renderingIntent", "rendering-intent"],
    ["shapeRendering", "shape-rendering"],
    ["stopColor", "stop-color"],
    ["stopOpacity", "stop-opacity"],
    ["strikethroughPosition", "strikethrough-position"],
    ["strikethroughThickness", "strikethrough-thickness"],
    ["strokeDasharray", "stroke-dasharray"],
    ["strokeDashoffset", "stroke-dashoffset"],
    ["strokeLinecap", "stroke-linecap"],
    ["strokeLinejoin", "stroke-linejoin"],
    ["strokeMiterlimit", "stroke-miterlimit"],
    ["strokeOpacity", "stroke-opacity"],
    ["strokeWidth", "stroke-width"],
    ["textAnchor", "text-anchor"],
    ["textDecoration", "text-decoration"],
    ["textRendering", "text-rendering"],
    ["transformOrigin", "transform-origin"],
    ["underlinePosition", "underline-position"],
    ["underlineThickness", "underline-thickness"],
    ["unicodeBidi", "unicode-bidi"],
    ["unicodeRange", "unicode-range"],
    ["unitsPerEm", "units-per-em"],
    ["vAlphabetic", "v-alphabetic"],
    ["vHanging", "v-hanging"],
    ["vIdeographic", "v-ideographic"],
    ["vMathematical", "v-mathematical"],
    ["vectorEffect", "vector-effect"],
    ["vertAdvY", "vert-adv-y"],
    ["vertOriginX", "vert-origin-x"],
    ["vertOriginY", "vert-origin-y"],
    ["wordSpacing", "word-spacing"],
    ["writingMode", "writing-mode"],
    ["xmlnsXlink", "xmlns:xlink"],
    ["xHeight", "x-height"]
  ]), iC = /^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*:/i;
  function $o(n) {
    return iC.test("" + n) ? "javascript:throw new Error('React has blocked a javascript: URL as a security precaution.')" : n;
  }
  function di() {
  }
  var Nu = null;
  function Iu(n) {
    return n = n.target || n.srcElement || window, n.correspondingUseElement && (n = n.correspondingUseElement), n.nodeType === 3 ? n.parentNode : n;
  }
  var gr = null, vr = null;
  function hp(n) {
    var r = dr(n);
    if (r && (n = r.stateNode)) {
      var o = n[Jt] || null;
      e: switch (n = r.stateNode, r.type) {
        case "input":
          if (Du(
            n,
            o.value,
            o.defaultValue,
            o.defaultValue,
            o.checked,
            o.defaultChecked,
            o.type,
            o.name
          ), r = o.name, o.type === "radio" && r != null) {
            for (o = n; o.parentNode; ) o = o.parentNode;
            for (o = o.querySelectorAll(
              'input[name="' + Tn(
                "" + r
              ) + '"][type="radio"]'
            ), r = 0; r < o.length; r++) {
              var l = o[r];
              if (l !== n && l.form === n.form) {
                var m = l[Jt] || null;
                if (!m) throw Error(i(90));
                Du(
                  l,
                  m.value,
                  m.defaultValue,
                  m.defaultValue,
                  m.checked,
                  m.defaultChecked,
                  m.type,
                  m.name
                );
              }
            }
            for (r = 0; r < o.length; r++)
              l = o[r], l.form === n.form && op(l);
          }
          break e;
        case "textarea":
          lp(n, o.value, o.defaultValue);
          break e;
        case "select":
          r = o.value, r != null && mr(n, !!o.multiple, r, !1);
      }
    }
  }
  var Lu = !1;
  function mp(n, r, o) {
    if (Lu) return n(r, o);
    Lu = !0;
    try {
      var l = n(r);
      return l;
    } finally {
      if (Lu = !1, (gr !== null || vr !== null) && (Fc(), gr && (r = gr, n = vr, vr = gr = null, hp(r), n)))
        for (r = 0; r < n.length; r++) hp(n[r]);
    }
  }
  function Cs(n, r) {
    var o = n.stateNode;
    if (o === null) return null;
    var l = o[Jt] || null;
    if (l === null) return null;
    o = l[r];
    e: switch (r) {
      case "onClick":
      case "onClickCapture":
      case "onDoubleClick":
      case "onDoubleClickCapture":
      case "onMouseDown":
      case "onMouseDownCapture":
      case "onMouseMove":
      case "onMouseMoveCapture":
      case "onMouseUp":
      case "onMouseUpCapture":
      case "onMouseEnter":
        (l = !l.disabled) || (n = n.type, l = !(n === "button" || n === "input" || n === "select" || n === "textarea")), n = !l;
        break e;
      default:
        n = !1;
    }
    if (n) return null;
    if (o && typeof o != "function")
      throw Error(
        i(231, r, typeof o)
      );
    return o;
  }
  var fi = !(typeof window > "u" || typeof window.document > "u" || typeof window.document.createElement > "u"), Uu = !1;
  if (fi)
    try {
      var Es = {};
      Object.defineProperty(Es, "passive", {
        get: function() {
          Uu = !0;
        }
      }), window.addEventListener("test", Es, Es), window.removeEventListener("test", Es, Es);
    } catch {
      Uu = !1;
    }
  var Hi = null, Bu = null, ec = null;
  function pp() {
    if (ec) return ec;
    var n, r = Bu, o = r.length, l, m = "value" in Hi ? Hi.value : Hi.textContent, g = m.length;
    for (n = 0; n < o && r[n] === m[n]; n++) ;
    var b = o - n;
    for (l = 1; l <= b && r[o - l] === m[g - l]; l++) ;
    return ec = m.slice(n, 1 < l ? 1 - l : void 0);
  }
  function tc(n) {
    var r = n.keyCode;
    return "charCode" in n ? (n = n.charCode, n === 0 && r === 13 && (n = 13)) : n = r, n === 10 && (n = 13), 32 <= n || n === 13 ? n : 0;
  }
  function nc() {
    return !0;
  }
  function gp() {
    return !1;
  }
  function Qt(n) {
    function r(o, l, m, g, b) {
      this._reactName = o, this._targetInst = m, this.type = l, this.nativeEvent = g, this.target = b, this.currentTarget = null;
      for (var S in n)
        n.hasOwnProperty(S) && (o = n[S], this[S] = o ? o(g) : g[S]);
      return this.isDefaultPrevented = (g.defaultPrevented != null ? g.defaultPrevented : g.returnValue === !1) ? nc : gp, this.isPropagationStopped = gp, this;
    }
    return v(r.prototype, {
      preventDefault: function() {
        this.defaultPrevented = !0;
        var o = this.nativeEvent;
        o && (o.preventDefault ? o.preventDefault() : typeof o.returnValue != "unknown" && (o.returnValue = !1), this.isDefaultPrevented = nc);
      },
      stopPropagation: function() {
        var o = this.nativeEvent;
        o && (o.stopPropagation ? o.stopPropagation() : typeof o.cancelBubble != "unknown" && (o.cancelBubble = !0), this.isPropagationStopped = nc);
      },
      persist: function() {
      },
      isPersistent: nc
    }), r;
  }
  var Oa = {
    eventPhase: 0,
    bubbles: 0,
    cancelable: 0,
    timeStamp: function(n) {
      return n.timeStamp || Date.now();
    },
    defaultPrevented: 0,
    isTrusted: 0
  }, ic = Qt(Oa), _s = v({}, Oa, { view: 0, detail: 0 }), aC = Qt(_s), zu, Fu, Rs, ac = v({}, _s, {
    screenX: 0,
    screenY: 0,
    clientX: 0,
    clientY: 0,
    pageX: 0,
    pageY: 0,
    ctrlKey: 0,
    shiftKey: 0,
    altKey: 0,
    metaKey: 0,
    getModifierState: qu,
    button: 0,
    buttons: 0,
    relatedTarget: function(n) {
      return n.relatedTarget === void 0 ? n.fromElement === n.srcElement ? n.toElement : n.fromElement : n.relatedTarget;
    },
    movementX: function(n) {
      return "movementX" in n ? n.movementX : (n !== Rs && (Rs && n.type === "mousemove" ? (zu = n.screenX - Rs.screenX, Fu = n.screenY - Rs.screenY) : Fu = zu = 0, Rs = n), zu);
    },
    movementY: function(n) {
      return "movementY" in n ? n.movementY : Fu;
    }
  }), vp = Qt(ac), rC = v({}, ac, { dataTransfer: 0 }), sC = Qt(rC), oC = v({}, _s, { relatedTarget: 0 }), ju = Qt(oC), cC = v({}, Oa, {
    animationName: 0,
    elapsedTime: 0,
    pseudoElement: 0
  }), lC = Qt(cC), uC = v({}, Oa, {
    clipboardData: function(n) {
      return "clipboardData" in n ? n.clipboardData : window.clipboardData;
    }
  }), dC = Qt(uC), fC = v({}, Oa, { data: 0 }), yp = Qt(fC), hC = {
    Esc: "Escape",
    Spacebar: " ",
    Left: "ArrowLeft",
    Up: "ArrowUp",
    Right: "ArrowRight",
    Down: "ArrowDown",
    Del: "Delete",
    Win: "OS",
    Menu: "ContextMenu",
    Apps: "ContextMenu",
    Scroll: "ScrollLock",
    MozPrintableKey: "Unidentified"
  }, mC = {
    8: "Backspace",
    9: "Tab",
    12: "Clear",
    13: "Enter",
    16: "Shift",
    17: "Control",
    18: "Alt",
    19: "Pause",
    20: "CapsLock",
    27: "Escape",
    32: " ",
    33: "PageUp",
    34: "PageDown",
    35: "End",
    36: "Home",
    37: "ArrowLeft",
    38: "ArrowUp",
    39: "ArrowRight",
    40: "ArrowDown",
    45: "Insert",
    46: "Delete",
    112: "F1",
    113: "F2",
    114: "F3",
    115: "F4",
    116: "F5",
    117: "F6",
    118: "F7",
    119: "F8",
    120: "F9",
    121: "F10",
    122: "F11",
    123: "F12",
    144: "NumLock",
    145: "ScrollLock",
    224: "Meta"
  }, pC = {
    Alt: "altKey",
    Control: "ctrlKey",
    Meta: "metaKey",
    Shift: "shiftKey"
  };
  function gC(n) {
    var r = this.nativeEvent;
    return r.getModifierState ? r.getModifierState(n) : (n = pC[n]) ? !!r[n] : !1;
  }
  function qu() {
    return gC;
  }
  var vC = v({}, _s, {
    key: function(n) {
      if (n.key) {
        var r = hC[n.key] || n.key;
        if (r !== "Unidentified") return r;
      }
      return n.type === "keypress" ? (n = tc(n), n === 13 ? "Enter" : String.fromCharCode(n)) : n.type === "keydown" || n.type === "keyup" ? mC[n.keyCode] || "Unidentified" : "";
    },
    code: 0,
    location: 0,
    ctrlKey: 0,
    shiftKey: 0,
    altKey: 0,
    metaKey: 0,
    repeat: 0,
    locale: 0,
    getModifierState: qu,
    charCode: function(n) {
      return n.type === "keypress" ? tc(n) : 0;
    },
    keyCode: function(n) {
      return n.type === "keydown" || n.type === "keyup" ? n.keyCode : 0;
    },
    which: function(n) {
      return n.type === "keypress" ? tc(n) : n.type === "keydown" || n.type === "keyup" ? n.keyCode : 0;
    }
  }), yC = Qt(vC), bC = v({}, ac, {
    pointerId: 0,
    width: 0,
    height: 0,
    pressure: 0,
    tangentialPressure: 0,
    tiltX: 0,
    tiltY: 0,
    twist: 0,
    pointerType: 0,
    isPrimary: 0
  }), bp = Qt(bC), TC = v({}, _s, {
    touches: 0,
    targetTouches: 0,
    changedTouches: 0,
    altKey: 0,
    metaKey: 0,
    ctrlKey: 0,
    shiftKey: 0,
    getModifierState: qu
  }), SC = Qt(TC), kC = v({}, Oa, {
    propertyName: 0,
    elapsedTime: 0,
    pseudoElement: 0
  }), CC = Qt(kC), EC = v({}, ac, {
    deltaX: function(n) {
      return "deltaX" in n ? n.deltaX : "wheelDeltaX" in n ? -n.wheelDeltaX : 0;
    },
    deltaY: function(n) {
      return "deltaY" in n ? n.deltaY : "wheelDeltaY" in n ? -n.wheelDeltaY : "wheelDelta" in n ? -n.wheelDelta : 0;
    },
    deltaZ: 0,
    deltaMode: 0
  }), _C = Qt(EC), RC = v({}, Oa, {
    newState: 0,
    oldState: 0
  }), wC = Qt(RC), MC = [9, 13, 27, 32], Hu = fi && "CompositionEvent" in window, ws = null;
  fi && "documentMode" in document && (ws = document.documentMode);
  var AC = fi && "TextEvent" in window && !ws, Tp = fi && (!Hu || ws && 8 < ws && 11 >= ws), Sp = " ", kp = !1;
  function Cp(n, r) {
    switch (n) {
      case "keyup":
        return MC.indexOf(r.keyCode) !== -1;
      case "keydown":
        return r.keyCode !== 229;
      case "keypress":
      case "mousedown":
      case "focusout":
        return !0;
      default:
        return !1;
    }
  }
  function Ep(n) {
    return n = n.detail, typeof n == "object" && "data" in n ? n.data : null;
  }
  var yr = !1;
  function OC(n, r) {
    switch (n) {
      case "compositionend":
        return Ep(r);
      case "keypress":
        return r.which !== 32 ? null : (kp = !0, Sp);
      case "textInput":
        return n = r.data, n === Sp && kp ? null : n;
      default:
        return null;
    }
  }
  function DC(n, r) {
    if (yr)
      return n === "compositionend" || !Hu && Cp(n, r) ? (n = pp(), ec = Bu = Hi = null, yr = !1, n) : null;
    switch (n) {
      case "paste":
        return null;
      case "keypress":
        if (!(r.ctrlKey || r.altKey || r.metaKey) || r.ctrlKey && r.altKey) {
          if (r.char && 1 < r.char.length)
            return r.char;
          if (r.which) return String.fromCharCode(r.which);
        }
        return null;
      case "compositionend":
        return Tp && r.locale !== "ko" ? null : r.data;
      default:
        return null;
    }
  }
  var PC = {
    color: !0,
    date: !0,
    datetime: !0,
    "datetime-local": !0,
    email: !0,
    month: !0,
    number: !0,
    password: !0,
    range: !0,
    search: !0,
    tel: !0,
    text: !0,
    time: !0,
    url: !0,
    week: !0
  };
  function _p(n) {
    var r = n && n.nodeName && n.nodeName.toLowerCase();
    return r === "input" ? !!PC[n.type] : r === "textarea";
  }
  function Rp(n, r, o, l) {
    gr ? vr ? vr.push(l) : vr = [l] : gr = l, r = Yc(r, "onChange"), 0 < r.length && (o = new ic(
      "onChange",
      "change",
      null,
      o,
      l
    ), n.push({ event: o, listeners: r }));
  }
  var Ms = null, As = null;
  function xC(n) {
    cy(n, 0);
  }
  function rc(n) {
    var r = ks(n);
    if (op(r)) return n;
  }
  function wp(n, r) {
    if (n === "change") return r;
  }
  var Mp = !1;
  if (fi) {
    var Vu;
    if (fi) {
      var Gu = "oninput" in document;
      if (!Gu) {
        var Ap = document.createElement("div");
        Ap.setAttribute("oninput", "return;"), Gu = typeof Ap.oninput == "function";
      }
      Vu = Gu;
    } else Vu = !1;
    Mp = Vu && (!document.documentMode || 9 < document.documentMode);
  }
  function Op() {
    Ms && (Ms.detachEvent("onpropertychange", Dp), As = Ms = null);
  }
  function Dp(n) {
    if (n.propertyName === "value" && rc(As)) {
      var r = [];
      Rp(
        r,
        As,
        n,
        Iu(n)
      ), mp(xC, r);
    }
  }
  function NC(n, r, o) {
    n === "focusin" ? (Op(), Ms = r, As = o, Ms.attachEvent("onpropertychange", Dp)) : n === "focusout" && Op();
  }
  function IC(n) {
    if (n === "selectionchange" || n === "keyup" || n === "keydown")
      return rc(As);
  }
  function LC(n, r) {
    if (n === "click") return rc(r);
  }
  function UC(n, r) {
    if (n === "input" || n === "change")
      return rc(r);
  }
  function BC(n, r) {
    return n === r && (n !== 0 || 1 / n === 1 / r) || n !== n && r !== r;
  }
  var un = typeof Object.is == "function" ? Object.is : BC;
  function Os(n, r) {
    if (un(n, r)) return !0;
    if (typeof n != "object" || n === null || typeof r != "object" || r === null)
      return !1;
    var o = Object.keys(n), l = Object.keys(r);
    if (o.length !== l.length) return !1;
    for (l = 0; l < o.length; l++) {
      var m = o[l];
      if (!ku.call(r, m) || !un(n[m], r[m]))
        return !1;
    }
    return !0;
  }
  function Pp(n) {
    for (; n && n.firstChild; ) n = n.firstChild;
    return n;
  }
  function xp(n, r) {
    var o = Pp(n);
    n = 0;
    for (var l; o; ) {
      if (o.nodeType === 3) {
        if (l = n + o.textContent.length, n <= r && l >= r)
          return { node: o, offset: r - n };
        n = l;
      }
      e: {
        for (; o; ) {
          if (o.nextSibling) {
            o = o.nextSibling;
            break e;
          }
          o = o.parentNode;
        }
        o = void 0;
      }
      o = Pp(o);
    }
  }
  function Np(n, r) {
    return n && r ? n === r ? !0 : n && n.nodeType === 3 ? !1 : r && r.nodeType === 3 ? Np(n, r.parentNode) : "contains" in n ? n.contains(r) : n.compareDocumentPosition ? !!(n.compareDocumentPosition(r) & 16) : !1 : !1;
  }
  function Ip(n) {
    n = n != null && n.ownerDocument != null && n.ownerDocument.defaultView != null ? n.ownerDocument.defaultView : window;
    for (var r = Wo(n.document); r instanceof n.HTMLIFrameElement; ) {
      try {
        var o = typeof r.contentWindow.location.href == "string";
      } catch {
        o = !1;
      }
      if (o) n = r.contentWindow;
      else break;
      r = Wo(n.document);
    }
    return r;
  }
  function Ku(n) {
    var r = n && n.nodeName && n.nodeName.toLowerCase();
    return r && (r === "input" && (n.type === "text" || n.type === "search" || n.type === "tel" || n.type === "url" || n.type === "password") || r === "textarea" || n.contentEditable === "true");
  }
  var zC = fi && "documentMode" in document && 11 >= document.documentMode, br = null, Yu = null, Ds = null, Ju = !1;
  function Lp(n, r, o) {
    var l = o.window === o ? o.document : o.nodeType === 9 ? o : o.ownerDocument;
    Ju || br == null || br !== Wo(l) || (l = br, "selectionStart" in l && Ku(l) ? l = { start: l.selectionStart, end: l.selectionEnd } : (l = (l.ownerDocument && l.ownerDocument.defaultView || window).getSelection(), l = {
      anchorNode: l.anchorNode,
      anchorOffset: l.anchorOffset,
      focusNode: l.focusNode,
      focusOffset: l.focusOffset
    }), Ds && Os(Ds, l) || (Ds = l, l = Yc(Yu, "onSelect"), 0 < l.length && (r = new ic(
      "onSelect",
      "select",
      null,
      r,
      o
    ), n.push({ event: r, listeners: l }), r.target = br)));
  }
  function Da(n, r) {
    var o = {};
    return o[n.toLowerCase()] = r.toLowerCase(), o["Webkit" + n] = "webkit" + r, o["Moz" + n] = "moz" + r, o;
  }
  var Tr = {
    animationend: Da("Animation", "AnimationEnd"),
    animationiteration: Da("Animation", "AnimationIteration"),
    animationstart: Da("Animation", "AnimationStart"),
    transitionrun: Da("Transition", "TransitionRun"),
    transitionstart: Da("Transition", "TransitionStart"),
    transitioncancel: Da("Transition", "TransitionCancel"),
    transitionend: Da("Transition", "TransitionEnd")
  }, Qu = {}, Up = {};
  fi && (Up = document.createElement("div").style, "AnimationEvent" in window || (delete Tr.animationend.animation, delete Tr.animationiteration.animation, delete Tr.animationstart.animation), "TransitionEvent" in window || delete Tr.transitionend.transition);
  function Pa(n) {
    if (Qu[n]) return Qu[n];
    if (!Tr[n]) return n;
    var r = Tr[n], o;
    for (o in r)
      if (r.hasOwnProperty(o) && o in Up)
        return Qu[n] = r[o];
    return n;
  }
  var Bp = Pa("animationend"), zp = Pa("animationiteration"), Fp = Pa("animationstart"), FC = Pa("transitionrun"), jC = Pa("transitionstart"), qC = Pa("transitioncancel"), jp = Pa("transitionend"), qp = /* @__PURE__ */ new Map(), Xu = "abort auxClick beforeToggle cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel".split(
    " "
  );
  Xu.push("scrollEnd");
  function In(n, r) {
    qp.set(n, r), Aa(r, [n]);
  }
  var sc = typeof reportError == "function" ? reportError : function(n) {
    if (typeof window == "object" && typeof window.ErrorEvent == "function") {
      var r = new window.ErrorEvent("error", {
        bubbles: !0,
        cancelable: !0,
        message: typeof n == "object" && n !== null && typeof n.message == "string" ? String(n.message) : String(n),
        error: n
      });
      if (!window.dispatchEvent(r)) return;
    } else if (typeof process == "object" && typeof process.emit == "function") {
      process.emit("uncaughtException", n);
      return;
    }
    console.error(n);
  }, Sn = [], Sr = 0, Zu = 0;
  function oc() {
    for (var n = Sr, r = Zu = Sr = 0; r < n; ) {
      var o = Sn[r];
      Sn[r++] = null;
      var l = Sn[r];
      Sn[r++] = null;
      var m = Sn[r];
      Sn[r++] = null;
      var g = Sn[r];
      if (Sn[r++] = null, l !== null && m !== null) {
        var b = l.pending;
        b === null ? m.next = m : (m.next = b.next, b.next = m), l.pending = m;
      }
      g !== 0 && Hp(o, m, g);
    }
  }
  function cc(n, r, o, l) {
    Sn[Sr++] = n, Sn[Sr++] = r, Sn[Sr++] = o, Sn[Sr++] = l, Zu |= l, n.lanes |= l, n = n.alternate, n !== null && (n.lanes |= l);
  }
  function Wu(n, r, o, l) {
    return cc(n, r, o, l), lc(n);
  }
  function xa(n, r) {
    return cc(n, null, null, r), lc(n);
  }
  function Hp(n, r, o) {
    n.lanes |= o;
    var l = n.alternate;
    l !== null && (l.lanes |= o);
    for (var m = !1, g = n.return; g !== null; )
      g.childLanes |= o, l = g.alternate, l !== null && (l.childLanes |= o), g.tag === 22 && (n = g.stateNode, n === null || n._visibility & 1 || (m = !0)), n = g, g = g.return;
    return n.tag === 3 ? (g = n.stateNode, m && r !== null && (m = 31 - ln(o), n = g.hiddenUpdates, l = n[m], l === null ? n[m] = [r] : l.push(r), r.lane = o | 536870912), g) : null;
  }
  function lc(n) {
    if (50 < $s)
      throw $s = 0, cf = null, Error(i(185));
    for (var r = n.return; r !== null; )
      n = r, r = n.return;
    return n.tag === 3 ? n.stateNode : null;
  }
  var kr = {};
  function HC(n, r, o, l) {
    this.tag = n, this.key = o, this.sibling = this.child = this.return = this.stateNode = this.type = this.elementType = null, this.index = 0, this.refCleanup = this.ref = null, this.pendingProps = r, this.dependencies = this.memoizedState = this.updateQueue = this.memoizedProps = null, this.mode = l, this.subtreeFlags = this.flags = 0, this.deletions = null, this.childLanes = this.lanes = 0, this.alternate = null;
  }
  function dn(n, r, o, l) {
    return new HC(n, r, o, l);
  }
  function $u(n) {
    return n = n.prototype, !(!n || !n.isReactComponent);
  }
  function hi(n, r) {
    var o = n.alternate;
    return o === null ? (o = dn(
      n.tag,
      r,
      n.key,
      n.mode
    ), o.elementType = n.elementType, o.type = n.type, o.stateNode = n.stateNode, o.alternate = n, n.alternate = o) : (o.pendingProps = r, o.type = n.type, o.flags = 0, o.subtreeFlags = 0, o.deletions = null), o.flags = n.flags & 65011712, o.childLanes = n.childLanes, o.lanes = n.lanes, o.child = n.child, o.memoizedProps = n.memoizedProps, o.memoizedState = n.memoizedState, o.updateQueue = n.updateQueue, r = n.dependencies, o.dependencies = r === null ? null : { lanes: r.lanes, firstContext: r.firstContext }, o.sibling = n.sibling, o.index = n.index, o.ref = n.ref, o.refCleanup = n.refCleanup, o;
  }
  function Vp(n, r) {
    n.flags &= 65011714;
    var o = n.alternate;
    return o === null ? (n.childLanes = 0, n.lanes = r, n.child = null, n.subtreeFlags = 0, n.memoizedProps = null, n.memoizedState = null, n.updateQueue = null, n.dependencies = null, n.stateNode = null) : (n.childLanes = o.childLanes, n.lanes = o.lanes, n.child = o.child, n.subtreeFlags = 0, n.deletions = null, n.memoizedProps = o.memoizedProps, n.memoizedState = o.memoizedState, n.updateQueue = o.updateQueue, n.type = o.type, r = o.dependencies, n.dependencies = r === null ? null : {
      lanes: r.lanes,
      firstContext: r.firstContext
    }), n;
  }
  function uc(n, r, o, l, m, g) {
    var b = 0;
    if (l = n, typeof n == "function") $u(n) && (b = 1);
    else if (typeof n == "string")
      b = JE(
        n,
        o,
        ce.current
      ) ? 26 : n === "html" || n === "head" || n === "body" ? 27 : 5;
    else
      e: switch (n) {
        case W:
          return n = dn(31, o, r, m), n.elementType = W, n.lanes = g, n;
        case k:
          return Na(o.children, m, g, r);
        case x:
          b = 8, m |= 24;
          break;
        case A:
          return n = dn(12, o, r, m | 2), n.elementType = A, n.lanes = g, n;
        case E:
          return n = dn(13, o, r, m), n.elementType = E, n.lanes = g, n;
        case M:
          return n = dn(19, o, r, m), n.elementType = M, n.lanes = g, n;
        default:
          if (typeof n == "object" && n !== null)
            switch (n.$$typeof) {
              case ie:
                b = 10;
                break e;
              case B:
                b = 9;
                break e;
              case C:
                b = 11;
                break e;
              case N:
                b = 14;
                break e;
              case G:
                b = 16, l = null;
                break e;
            }
          b = 29, o = Error(
            i(130, n === null ? "null" : typeof n, "")
          ), l = null;
      }
    return r = dn(b, o, r, m), r.elementType = n, r.type = l, r.lanes = g, r;
  }
  function Na(n, r, o, l) {
    return n = dn(7, n, l, r), n.lanes = o, n;
  }
  function ed(n, r, o) {
    return n = dn(6, n, null, r), n.lanes = o, n;
  }
  function Gp(n) {
    var r = dn(18, null, null, 0);
    return r.stateNode = n, r;
  }
  function td(n, r, o) {
    return r = dn(
      4,
      n.children !== null ? n.children : [],
      n.key,
      r
    ), r.lanes = o, r.stateNode = {
      containerInfo: n.containerInfo,
      pendingChildren: null,
      implementation: n.implementation
    }, r;
  }
  var Kp = /* @__PURE__ */ new WeakMap();
  function kn(n, r) {
    if (typeof n == "object" && n !== null) {
      var o = Kp.get(n);
      return o !== void 0 ? o : (r = {
        value: n,
        source: r,
        stack: Gm(r)
      }, Kp.set(n, r), r);
    }
    return {
      value: n,
      source: r,
      stack: Gm(r)
    };
  }
  var Cr = [], Er = 0, dc = null, Ps = 0, Cn = [], En = 0, Vi = null, $n = 1, ei = "";
  function mi(n, r) {
    Cr[Er++] = Ps, Cr[Er++] = dc, dc = n, Ps = r;
  }
  function Yp(n, r, o) {
    Cn[En++] = $n, Cn[En++] = ei, Cn[En++] = Vi, Vi = n;
    var l = $n;
    n = ei;
    var m = 32 - ln(l) - 1;
    l &= ~(1 << m), o += 1;
    var g = 32 - ln(r) + m;
    if (30 < g) {
      var b = m - m % 5;
      g = (l & (1 << b) - 1).toString(32), l >>= b, m -= b, $n = 1 << 32 - ln(r) + m | o << m | l, ei = g + n;
    } else
      $n = 1 << g | o << m | l, ei = n;
  }
  function nd(n) {
    n.return !== null && (mi(n, 1), Yp(n, 1, 0));
  }
  function id(n) {
    for (; n === dc; )
      dc = Cr[--Er], Cr[Er] = null, Ps = Cr[--Er], Cr[Er] = null;
    for (; n === Vi; )
      Vi = Cn[--En], Cn[En] = null, ei = Cn[--En], Cn[En] = null, $n = Cn[--En], Cn[En] = null;
  }
  function Jp(n, r) {
    Cn[En++] = $n, Cn[En++] = ei, Cn[En++] = Vi, $n = r.id, ei = r.overflow, Vi = n;
  }
  var Ot = null, et = null, Le = !1, Gi = null, _n = !1, ad = Error(i(519));
  function Ki(n) {
    var r = Error(
      i(
        418,
        1 < arguments.length && arguments[1] !== void 0 && arguments[1] ? "text" : "HTML",
        ""
      )
    );
    throw xs(kn(r, n)), ad;
  }
  function Qp(n) {
    var r = n.stateNode, o = n.type, l = n.memoizedProps;
    switch (r[At] = n, r[Jt] = l, o) {
      case "dialog":
        Pe("cancel", r), Pe("close", r);
        break;
      case "iframe":
      case "object":
      case "embed":
        Pe("load", r);
        break;
      case "video":
      case "audio":
        for (o = 0; o < to.length; o++)
          Pe(to[o], r);
        break;
      case "source":
        Pe("error", r);
        break;
      case "img":
      case "image":
      case "link":
        Pe("error", r), Pe("load", r);
        break;
      case "details":
        Pe("toggle", r);
        break;
      case "input":
        Pe("invalid", r), cp(
          r,
          l.value,
          l.defaultValue,
          l.checked,
          l.defaultChecked,
          l.type,
          l.name,
          !0
        );
        break;
      case "select":
        Pe("invalid", r);
        break;
      case "textarea":
        Pe("invalid", r), up(r, l.value, l.defaultValue, l.children);
    }
    o = l.children, typeof o != "string" && typeof o != "number" && typeof o != "bigint" || r.textContent === "" + o || l.suppressHydrationWarning === !0 || fy(r.textContent, o) ? (l.popover != null && (Pe("beforetoggle", r), Pe("toggle", r)), l.onScroll != null && Pe("scroll", r), l.onScrollEnd != null && Pe("scrollend", r), l.onClick != null && (r.onclick = di), r = !0) : r = !1, r || Ki(n, !0);
  }
  function Xp(n) {
    for (Ot = n.return; Ot; )
      switch (Ot.tag) {
        case 5:
        case 31:
        case 13:
          _n = !1;
          return;
        case 27:
        case 3:
          _n = !0;
          return;
        default:
          Ot = Ot.return;
      }
  }
  function _r(n) {
    if (n !== Ot) return !1;
    if (!Le) return Xp(n), Le = !0, !1;
    var r = n.tag, o;
    if ((o = r !== 3 && r !== 27) && ((o = r === 5) && (o = n.type, o = !(o !== "form" && o !== "button") || Cf(n.type, n.memoizedProps)), o = !o), o && et && Ki(n), Xp(n), r === 13) {
      if (n = n.memoizedState, n = n !== null ? n.dehydrated : null, !n) throw Error(i(317));
      et = Sy(n);
    } else if (r === 31) {
      if (n = n.memoizedState, n = n !== null ? n.dehydrated : null, !n) throw Error(i(317));
      et = Sy(n);
    } else
      r === 27 ? (r = et, sa(n.type) ? (n = Mf, Mf = null, et = n) : et = r) : et = Ot ? wn(n.stateNode.nextSibling) : null;
    return !0;
  }
  function Ia() {
    et = Ot = null, Le = !1;
  }
  function rd() {
    var n = Gi;
    return n !== null && ($t === null ? $t = n : $t.push.apply(
      $t,
      n
    ), Gi = null), n;
  }
  function xs(n) {
    Gi === null ? Gi = [n] : Gi.push(n);
  }
  var sd = w(null), La = null, pi = null;
  function Yi(n, r, o) {
    se(sd, r._currentValue), r._currentValue = o;
  }
  function gi(n) {
    n._currentValue = sd.current, K(sd);
  }
  function od(n, r, o) {
    for (; n !== null; ) {
      var l = n.alternate;
      if ((n.childLanes & r) !== r ? (n.childLanes |= r, l !== null && (l.childLanes |= r)) : l !== null && (l.childLanes & r) !== r && (l.childLanes |= r), n === o) break;
      n = n.return;
    }
  }
  function cd(n, r, o, l) {
    var m = n.child;
    for (m !== null && (m.return = n); m !== null; ) {
      var g = m.dependencies;
      if (g !== null) {
        var b = m.child;
        g = g.firstContext;
        e: for (; g !== null; ) {
          var S = g;
          g = m;
          for (var O = 0; O < r.length; O++)
            if (S.context === r[O]) {
              g.lanes |= o, S = g.alternate, S !== null && (S.lanes |= o), od(
                g.return,
                o,
                n
              ), l || (b = null);
              break e;
            }
          g = S.next;
        }
      } else if (m.tag === 18) {
        if (b = m.return, b === null) throw Error(i(341));
        b.lanes |= o, g = b.alternate, g !== null && (g.lanes |= o), od(b, o, n), b = null;
      } else b = m.child;
      if (b !== null) b.return = m;
      else
        for (b = m; b !== null; ) {
          if (b === n) {
            b = null;
            break;
          }
          if (m = b.sibling, m !== null) {
            m.return = b.return, b = m;
            break;
          }
          b = b.return;
        }
      m = b;
    }
  }
  function Rr(n, r, o, l) {
    n = null;
    for (var m = r, g = !1; m !== null; ) {
      if (!g) {
        if ((m.flags & 524288) !== 0) g = !0;
        else if ((m.flags & 262144) !== 0) break;
      }
      if (m.tag === 10) {
        var b = m.alternate;
        if (b === null) throw Error(i(387));
        if (b = b.memoizedProps, b !== null) {
          var S = m.type;
          un(m.pendingProps.value, b.value) || (n !== null ? n.push(S) : n = [S]);
        }
      } else if (m === ue.current) {
        if (b = m.alternate, b === null) throw Error(i(387));
        b.memoizedState.memoizedState !== m.memoizedState.memoizedState && (n !== null ? n.push(so) : n = [so]);
      }
      m = m.return;
    }
    n !== null && cd(
      r,
      n,
      o,
      l
    ), r.flags |= 262144;
  }
  function fc(n) {
    for (n = n.firstContext; n !== null; ) {
      if (!un(
        n.context._currentValue,
        n.memoizedValue
      ))
        return !0;
      n = n.next;
    }
    return !1;
  }
  function Ua(n) {
    La = n, pi = null, n = n.dependencies, n !== null && (n.firstContext = null);
  }
  function Dt(n) {
    return Zp(La, n);
  }
  function hc(n, r) {
    return La === null && Ua(n), Zp(n, r);
  }
  function Zp(n, r) {
    var o = r._currentValue;
    if (r = { context: r, memoizedValue: o, next: null }, pi === null) {
      if (n === null) throw Error(i(308));
      pi = r, n.dependencies = { lanes: 0, firstContext: r }, n.flags |= 524288;
    } else pi = pi.next = r;
    return o;
  }
  var VC = typeof AbortController < "u" ? AbortController : function() {
    var n = [], r = this.signal = {
      aborted: !1,
      addEventListener: function(o, l) {
        n.push(l);
      }
    };
    this.abort = function() {
      r.aborted = !0, n.forEach(function(o) {
        return o();
      });
    };
  }, GC = a.unstable_scheduleCallback, KC = a.unstable_NormalPriority, mt = {
    $$typeof: ie,
    Consumer: null,
    Provider: null,
    _currentValue: null,
    _currentValue2: null,
    _threadCount: 0
  };
  function ld() {
    return {
      controller: new VC(),
      data: /* @__PURE__ */ new Map(),
      refCount: 0
    };
  }
  function Ns(n) {
    n.refCount--, n.refCount === 0 && GC(KC, function() {
      n.controller.abort();
    });
  }
  var Is = null, ud = 0, wr = 0, Mr = null;
  function YC(n, r) {
    if (Is === null) {
      var o = Is = [];
      ud = 0, wr = mf(), Mr = {
        status: "pending",
        value: void 0,
        then: function(l) {
          o.push(l);
        }
      };
    }
    return ud++, r.then(Wp, Wp), r;
  }
  function Wp() {
    if (--ud === 0 && Is !== null) {
      Mr !== null && (Mr.status = "fulfilled");
      var n = Is;
      Is = null, wr = 0, Mr = null;
      for (var r = 0; r < n.length; r++) (0, n[r])();
    }
  }
  function JC(n, r) {
    var o = [], l = {
      status: "pending",
      value: null,
      reason: null,
      then: function(m) {
        o.push(m);
      }
    };
    return n.then(
      function() {
        l.status = "fulfilled", l.value = r;
        for (var m = 0; m < o.length; m++) (0, o[m])(r);
      },
      function(m) {
        for (l.status = "rejected", l.reason = m, m = 0; m < o.length; m++)
          (0, o[m])(void 0);
      }
    ), l;
  }
  var $p = q.S;
  q.S = function(n, r) {
    Lv = on(), typeof r == "object" && r !== null && typeof r.then == "function" && YC(n, r), $p !== null && $p(n, r);
  };
  var Ba = w(null);
  function dd() {
    var n = Ba.current;
    return n !== null ? n : Xe.pooledCache;
  }
  function mc(n, r) {
    r === null ? se(Ba, Ba.current) : se(Ba, r.pool);
  }
  function eg() {
    var n = dd();
    return n === null ? null : { parent: mt._currentValue, pool: n };
  }
  var Ar = Error(i(460)), fd = Error(i(474)), pc = Error(i(542)), gc = { then: function() {
  } };
  function tg(n) {
    return n = n.status, n === "fulfilled" || n === "rejected";
  }
  function ng(n, r, o) {
    switch (o = n[o], o === void 0 ? n.push(r) : o !== r && (r.then(di, di), r = o), r.status) {
      case "fulfilled":
        return r.value;
      case "rejected":
        throw n = r.reason, ag(n), n;
      default:
        if (typeof r.status == "string") r.then(di, di);
        else {
          if (n = Xe, n !== null && 100 < n.shellSuspendCounter)
            throw Error(i(482));
          n = r, n.status = "pending", n.then(
            function(l) {
              if (r.status === "pending") {
                var m = r;
                m.status = "fulfilled", m.value = l;
              }
            },
            function(l) {
              if (r.status === "pending") {
                var m = r;
                m.status = "rejected", m.reason = l;
              }
            }
          );
        }
        switch (r.status) {
          case "fulfilled":
            return r.value;
          case "rejected":
            throw n = r.reason, ag(n), n;
        }
        throw Fa = r, Ar;
    }
  }
  function za(n) {
    try {
      var r = n._init;
      return r(n._payload);
    } catch (o) {
      throw o !== null && typeof o == "object" && typeof o.then == "function" ? (Fa = o, Ar) : o;
    }
  }
  var Fa = null;
  function ig() {
    if (Fa === null) throw Error(i(459));
    var n = Fa;
    return Fa = null, n;
  }
  function ag(n) {
    if (n === Ar || n === pc)
      throw Error(i(483));
  }
  var Or = null, Ls = 0;
  function vc(n) {
    var r = Ls;
    return Ls += 1, Or === null && (Or = []), ng(Or, n, r);
  }
  function Us(n, r) {
    r = r.props.ref, n.ref = r !== void 0 ? r : null;
  }
  function yc(n, r) {
    throw r.$$typeof === y ? Error(i(525)) : (n = Object.prototype.toString.call(r), Error(
      i(
        31,
        n === "[object Object]" ? "object with keys {" + Object.keys(r).join(", ") + "}" : n
      )
    ));
  }
  function rg(n) {
    function r(I, P) {
      if (n) {
        var U = I.deletions;
        U === null ? (I.deletions = [P], I.flags |= 16) : U.push(P);
      }
    }
    function o(I, P) {
      if (!n) return null;
      for (; P !== null; )
        r(I, P), P = P.sibling;
      return null;
    }
    function l(I) {
      for (var P = /* @__PURE__ */ new Map(); I !== null; )
        I.key !== null ? P.set(I.key, I) : P.set(I.index, I), I = I.sibling;
      return P;
    }
    function m(I, P) {
      return I = hi(I, P), I.index = 0, I.sibling = null, I;
    }
    function g(I, P, U) {
      return I.index = U, n ? (U = I.alternate, U !== null ? (U = U.index, U < P ? (I.flags |= 67108866, P) : U) : (I.flags |= 67108866, P)) : (I.flags |= 1048576, P);
    }
    function b(I) {
      return n && I.alternate === null && (I.flags |= 67108866), I;
    }
    function S(I, P, U, Z) {
      return P === null || P.tag !== 6 ? (P = ed(U, I.mode, Z), P.return = I, P) : (P = m(P, U), P.return = I, P);
    }
    function O(I, P, U, Z) {
      var ve = U.type;
      return ve === k ? Q(
        I,
        P,
        U.props.children,
        Z,
        U.key
      ) : P !== null && (P.elementType === ve || typeof ve == "object" && ve !== null && ve.$$typeof === G && za(ve) === P.type) ? (P = m(P, U.props), Us(P, U), P.return = I, P) : (P = uc(
        U.type,
        U.key,
        U.props,
        null,
        I.mode,
        Z
      ), Us(P, U), P.return = I, P);
    }
    function z(I, P, U, Z) {
      return P === null || P.tag !== 4 || P.stateNode.containerInfo !== U.containerInfo || P.stateNode.implementation !== U.implementation ? (P = td(U, I.mode, Z), P.return = I, P) : (P = m(P, U.children || []), P.return = I, P);
    }
    function Q(I, P, U, Z, ve) {
      return P === null || P.tag !== 7 ? (P = Na(
        U,
        I.mode,
        Z,
        ve
      ), P.return = I, P) : (P = m(P, U), P.return = I, P);
    }
    function $(I, P, U) {
      if (typeof P == "string" && P !== "" || typeof P == "number" || typeof P == "bigint")
        return P = ed(
          "" + P,
          I.mode,
          U
        ), P.return = I, P;
      if (typeof P == "object" && P !== null) {
        switch (P.$$typeof) {
          case T:
            return U = uc(
              P.type,
              P.key,
              P.props,
              null,
              I.mode,
              U
            ), Us(U, P), U.return = I, U;
          case _:
            return P = td(
              P,
              I.mode,
              U
            ), P.return = I, P;
          case G:
            return P = za(P), $(I, P, U);
        }
        if (ke(P) || ne(P))
          return P = Na(
            P,
            I.mode,
            U,
            null
          ), P.return = I, P;
        if (typeof P.then == "function")
          return $(I, vc(P), U);
        if (P.$$typeof === ie)
          return $(
            I,
            hc(I, P),
            U
          );
        yc(I, P);
      }
      return null;
    }
    function F(I, P, U, Z) {
      var ve = P !== null ? P.key : null;
      if (typeof U == "string" && U !== "" || typeof U == "number" || typeof U == "bigint")
        return ve !== null ? null : S(I, P, "" + U, Z);
      if (typeof U == "object" && U !== null) {
        switch (U.$$typeof) {
          case T:
            return U.key === ve ? O(I, P, U, Z) : null;
          case _:
            return U.key === ve ? z(I, P, U, Z) : null;
          case G:
            return U = za(U), F(I, P, U, Z);
        }
        if (ke(U) || ne(U))
          return ve !== null ? null : Q(I, P, U, Z, null);
        if (typeof U.then == "function")
          return F(
            I,
            P,
            vc(U),
            Z
          );
        if (U.$$typeof === ie)
          return F(
            I,
            P,
            hc(I, U),
            Z
          );
        yc(I, U);
      }
      return null;
    }
    function V(I, P, U, Z, ve) {
      if (typeof Z == "string" && Z !== "" || typeof Z == "number" || typeof Z == "bigint")
        return I = I.get(U) || null, S(P, I, "" + Z, ve);
      if (typeof Z == "object" && Z !== null) {
        switch (Z.$$typeof) {
          case T:
            return I = I.get(
              Z.key === null ? U : Z.key
            ) || null, O(P, I, Z, ve);
          case _:
            return I = I.get(
              Z.key === null ? U : Z.key
            ) || null, z(P, I, Z, ve);
          case G:
            return Z = za(Z), V(
              I,
              P,
              U,
              Z,
              ve
            );
        }
        if (ke(Z) || ne(Z))
          return I = I.get(U) || null, Q(P, I, Z, ve, null);
        if (typeof Z.then == "function")
          return V(
            I,
            P,
            U,
            vc(Z),
            ve
          );
        if (Z.$$typeof === ie)
          return V(
            I,
            P,
            U,
            hc(P, Z),
            ve
          );
        yc(P, Z);
      }
      return null;
    }
    function he(I, P, U, Z) {
      for (var ve = null, ze = null, me = P, we = P = 0, Ie = null; me !== null && we < U.length; we++) {
        me.index > we ? (Ie = me, me = null) : Ie = me.sibling;
        var Fe = F(
          I,
          me,
          U[we],
          Z
        );
        if (Fe === null) {
          me === null && (me = Ie);
          break;
        }
        n && me && Fe.alternate === null && r(I, me), P = g(Fe, P, we), ze === null ? ve = Fe : ze.sibling = Fe, ze = Fe, me = Ie;
      }
      if (we === U.length)
        return o(I, me), Le && mi(I, we), ve;
      if (me === null) {
        for (; we < U.length; we++)
          me = $(I, U[we], Z), me !== null && (P = g(
            me,
            P,
            we
          ), ze === null ? ve = me : ze.sibling = me, ze = me);
        return Le && mi(I, we), ve;
      }
      for (me = l(me); we < U.length; we++)
        Ie = V(
          me,
          I,
          we,
          U[we],
          Z
        ), Ie !== null && (n && Ie.alternate !== null && me.delete(
          Ie.key === null ? we : Ie.key
        ), P = g(
          Ie,
          P,
          we
        ), ze === null ? ve = Ie : ze.sibling = Ie, ze = Ie);
      return n && me.forEach(function(da) {
        return r(I, da);
      }), Le && mi(I, we), ve;
    }
    function be(I, P, U, Z) {
      if (U == null) throw Error(i(151));
      for (var ve = null, ze = null, me = P, we = P = 0, Ie = null, Fe = U.next(); me !== null && !Fe.done; we++, Fe = U.next()) {
        me.index > we ? (Ie = me, me = null) : Ie = me.sibling;
        var da = F(I, me, Fe.value, Z);
        if (da === null) {
          me === null && (me = Ie);
          break;
        }
        n && me && da.alternate === null && r(I, me), P = g(da, P, we), ze === null ? ve = da : ze.sibling = da, ze = da, me = Ie;
      }
      if (Fe.done)
        return o(I, me), Le && mi(I, we), ve;
      if (me === null) {
        for (; !Fe.done; we++, Fe = U.next())
          Fe = $(I, Fe.value, Z), Fe !== null && (P = g(Fe, P, we), ze === null ? ve = Fe : ze.sibling = Fe, ze = Fe);
        return Le && mi(I, we), ve;
      }
      for (me = l(me); !Fe.done; we++, Fe = U.next())
        Fe = V(me, I, we, Fe.value, Z), Fe !== null && (n && Fe.alternate !== null && me.delete(Fe.key === null ? we : Fe.key), P = g(Fe, P, we), ze === null ? ve = Fe : ze.sibling = Fe, ze = Fe);
      return n && me.forEach(function(r_) {
        return r(I, r_);
      }), Le && mi(I, we), ve;
    }
    function Je(I, P, U, Z) {
      if (typeof U == "object" && U !== null && U.type === k && U.key === null && (U = U.props.children), typeof U == "object" && U !== null) {
        switch (U.$$typeof) {
          case T:
            e: {
              for (var ve = U.key; P !== null; ) {
                if (P.key === ve) {
                  if (ve = U.type, ve === k) {
                    if (P.tag === 7) {
                      o(
                        I,
                        P.sibling
                      ), Z = m(
                        P,
                        U.props.children
                      ), Z.return = I, I = Z;
                      break e;
                    }
                  } else if (P.elementType === ve || typeof ve == "object" && ve !== null && ve.$$typeof === G && za(ve) === P.type) {
                    o(
                      I,
                      P.sibling
                    ), Z = m(P, U.props), Us(Z, U), Z.return = I, I = Z;
                    break e;
                  }
                  o(I, P);
                  break;
                } else r(I, P);
                P = P.sibling;
              }
              U.type === k ? (Z = Na(
                U.props.children,
                I.mode,
                Z,
                U.key
              ), Z.return = I, I = Z) : (Z = uc(
                U.type,
                U.key,
                U.props,
                null,
                I.mode,
                Z
              ), Us(Z, U), Z.return = I, I = Z);
            }
            return b(I);
          case _:
            e: {
              for (ve = U.key; P !== null; ) {
                if (P.key === ve)
                  if (P.tag === 4 && P.stateNode.containerInfo === U.containerInfo && P.stateNode.implementation === U.implementation) {
                    o(
                      I,
                      P.sibling
                    ), Z = m(P, U.children || []), Z.return = I, I = Z;
                    break e;
                  } else {
                    o(I, P);
                    break;
                  }
                else r(I, P);
                P = P.sibling;
              }
              Z = td(U, I.mode, Z), Z.return = I, I = Z;
            }
            return b(I);
          case G:
            return U = za(U), Je(
              I,
              P,
              U,
              Z
            );
        }
        if (ke(U))
          return he(
            I,
            P,
            U,
            Z
          );
        if (ne(U)) {
          if (ve = ne(U), typeof ve != "function") throw Error(i(150));
          return U = ve.call(U), be(
            I,
            P,
            U,
            Z
          );
        }
        if (typeof U.then == "function")
          return Je(
            I,
            P,
            vc(U),
            Z
          );
        if (U.$$typeof === ie)
          return Je(
            I,
            P,
            hc(I, U),
            Z
          );
        yc(I, U);
      }
      return typeof U == "string" && U !== "" || typeof U == "number" || typeof U == "bigint" ? (U = "" + U, P !== null && P.tag === 6 ? (o(I, P.sibling), Z = m(P, U), Z.return = I, I = Z) : (o(I, P), Z = ed(U, I.mode, Z), Z.return = I, I = Z), b(I)) : o(I, P);
    }
    return function(I, P, U, Z) {
      try {
        Ls = 0;
        var ve = Je(
          I,
          P,
          U,
          Z
        );
        return Or = null, ve;
      } catch (me) {
        if (me === Ar || me === pc) throw me;
        var ze = dn(29, me, null, I.mode);
        return ze.lanes = Z, ze.return = I, ze;
      } finally {
      }
    };
  }
  var ja = rg(!0), sg = rg(!1), Ji = !1;
  function hd(n) {
    n.updateQueue = {
      baseState: n.memoizedState,
      firstBaseUpdate: null,
      lastBaseUpdate: null,
      shared: { pending: null, lanes: 0, hiddenCallbacks: null },
      callbacks: null
    };
  }
  function md(n, r) {
    n = n.updateQueue, r.updateQueue === n && (r.updateQueue = {
      baseState: n.baseState,
      firstBaseUpdate: n.firstBaseUpdate,
      lastBaseUpdate: n.lastBaseUpdate,
      shared: n.shared,
      callbacks: null
    });
  }
  function Qi(n) {
    return { lane: n, tag: 0, payload: null, callback: null, next: null };
  }
  function Xi(n, r, o) {
    var l = n.updateQueue;
    if (l === null) return null;
    if (l = l.shared, (qe & 2) !== 0) {
      var m = l.pending;
      return m === null ? r.next = r : (r.next = m.next, m.next = r), l.pending = r, r = lc(n), Hp(n, null, o), r;
    }
    return cc(n, l, r, o), lc(n);
  }
  function Bs(n, r, o) {
    if (r = r.updateQueue, r !== null && (r = r.shared, (o & 4194048) !== 0)) {
      var l = r.lanes;
      l &= n.pendingLanes, o |= l, r.lanes = o, Zm(n, o);
    }
  }
  function pd(n, r) {
    var o = n.updateQueue, l = n.alternate;
    if (l !== null && (l = l.updateQueue, o === l)) {
      var m = null, g = null;
      if (o = o.firstBaseUpdate, o !== null) {
        do {
          var b = {
            lane: o.lane,
            tag: o.tag,
            payload: o.payload,
            callback: null,
            next: null
          };
          g === null ? m = g = b : g = g.next = b, o = o.next;
        } while (o !== null);
        g === null ? m = g = r : g = g.next = r;
      } else m = g = r;
      o = {
        baseState: l.baseState,
        firstBaseUpdate: m,
        lastBaseUpdate: g,
        shared: l.shared,
        callbacks: l.callbacks
      }, n.updateQueue = o;
      return;
    }
    n = o.lastBaseUpdate, n === null ? o.firstBaseUpdate = r : n.next = r, o.lastBaseUpdate = r;
  }
  var gd = !1;
  function zs() {
    if (gd) {
      var n = Mr;
      if (n !== null) throw n;
    }
  }
  function Fs(n, r, o, l) {
    gd = !1;
    var m = n.updateQueue;
    Ji = !1;
    var g = m.firstBaseUpdate, b = m.lastBaseUpdate, S = m.shared.pending;
    if (S !== null) {
      m.shared.pending = null;
      var O = S, z = O.next;
      O.next = null, b === null ? g = z : b.next = z, b = O;
      var Q = n.alternate;
      Q !== null && (Q = Q.updateQueue, S = Q.lastBaseUpdate, S !== b && (S === null ? Q.firstBaseUpdate = z : S.next = z, Q.lastBaseUpdate = O));
    }
    if (g !== null) {
      var $ = m.baseState;
      b = 0, Q = z = O = null, S = g;
      do {
        var F = S.lane & -536870913, V = F !== S.lane;
        if (V ? (Ne & F) === F : (l & F) === F) {
          F !== 0 && F === wr && (gd = !0), Q !== null && (Q = Q.next = {
            lane: 0,
            tag: S.tag,
            payload: S.payload,
            callback: null,
            next: null
          });
          e: {
            var he = n, be = S;
            F = r;
            var Je = o;
            switch (be.tag) {
              case 1:
                if (he = be.payload, typeof he == "function") {
                  $ = he.call(Je, $, F);
                  break e;
                }
                $ = he;
                break e;
              case 3:
                he.flags = he.flags & -65537 | 128;
              case 0:
                if (he = be.payload, F = typeof he == "function" ? he.call(Je, $, F) : he, F == null) break e;
                $ = v({}, $, F);
                break e;
              case 2:
                Ji = !0;
            }
          }
          F = S.callback, F !== null && (n.flags |= 64, V && (n.flags |= 8192), V = m.callbacks, V === null ? m.callbacks = [F] : V.push(F));
        } else
          V = {
            lane: F,
            tag: S.tag,
            payload: S.payload,
            callback: S.callback,
            next: null
          }, Q === null ? (z = Q = V, O = $) : Q = Q.next = V, b |= F;
        if (S = S.next, S === null) {
          if (S = m.shared.pending, S === null)
            break;
          V = S, S = V.next, V.next = null, m.lastBaseUpdate = V, m.shared.pending = null;
        }
      } while (!0);
      Q === null && (O = $), m.baseState = O, m.firstBaseUpdate = z, m.lastBaseUpdate = Q, g === null && (m.shared.lanes = 0), ta |= b, n.lanes = b, n.memoizedState = $;
    }
  }
  function og(n, r) {
    if (typeof n != "function")
      throw Error(i(191, n));
    n.call(r);
  }
  function cg(n, r) {
    var o = n.callbacks;
    if (o !== null)
      for (n.callbacks = null, n = 0; n < o.length; n++)
        og(o[n], r);
  }
  var Dr = w(null), bc = w(0);
  function lg(n, r) {
    n = _i, se(bc, n), se(Dr, r), _i = n | r.baseLanes;
  }
  function vd() {
    se(bc, _i), se(Dr, Dr.current);
  }
  function yd() {
    _i = bc.current, K(Dr), K(bc);
  }
  var fn = w(null), Rn = null;
  function Zi(n) {
    var r = n.alternate;
    se(dt, dt.current & 1), se(fn, n), Rn === null && (r === null || Dr.current !== null || r.memoizedState !== null) && (Rn = n);
  }
  function bd(n) {
    se(dt, dt.current), se(fn, n), Rn === null && (Rn = n);
  }
  function ug(n) {
    n.tag === 22 ? (se(dt, dt.current), se(fn, n), Rn === null && (Rn = n)) : Wi();
  }
  function Wi() {
    se(dt, dt.current), se(fn, fn.current);
  }
  function hn(n) {
    K(fn), Rn === n && (Rn = null), K(dt);
  }
  var dt = w(0);
  function Tc(n) {
    for (var r = n; r !== null; ) {
      if (r.tag === 13) {
        var o = r.memoizedState;
        if (o !== null && (o = o.dehydrated, o === null || Rf(o) || wf(o)))
          return r;
      } else if (r.tag === 19 && (r.memoizedProps.revealOrder === "forwards" || r.memoizedProps.revealOrder === "backwards" || r.memoizedProps.revealOrder === "unstable_legacy-backwards" || r.memoizedProps.revealOrder === "together")) {
        if ((r.flags & 128) !== 0) return r;
      } else if (r.child !== null) {
        r.child.return = r, r = r.child;
        continue;
      }
      if (r === n) break;
      for (; r.sibling === null; ) {
        if (r.return === null || r.return === n) return null;
        r = r.return;
      }
      r.sibling.return = r.return, r = r.sibling;
    }
    return null;
  }
  var vi = 0, Re = null, Ke = null, pt = null, Sc = !1, Pr = !1, qa = !1, kc = 0, js = 0, xr = null, QC = 0;
  function ot() {
    throw Error(i(321));
  }
  function Td(n, r) {
    if (r === null) return !1;
    for (var o = 0; o < r.length && o < n.length; o++)
      if (!un(n[o], r[o])) return !1;
    return !0;
  }
  function Sd(n, r, o, l, m, g) {
    return vi = g, Re = r, r.memoizedState = null, r.updateQueue = null, r.lanes = 0, q.H = n === null || n.memoizedState === null ? Yg : Ld, qa = !1, g = o(l, m), qa = !1, Pr && (g = fg(
      r,
      o,
      l,
      m
    )), dg(n), g;
  }
  function dg(n) {
    q.H = Vs;
    var r = Ke !== null && Ke.next !== null;
    if (vi = 0, pt = Ke = Re = null, Sc = !1, js = 0, xr = null, r) throw Error(i(300));
    n === null || gt || (n = n.dependencies, n !== null && fc(n) && (gt = !0));
  }
  function fg(n, r, o, l) {
    Re = n;
    var m = 0;
    do {
      if (Pr && (xr = null), js = 0, Pr = !1, 25 <= m) throw Error(i(301));
      if (m += 1, pt = Ke = null, n.updateQueue != null) {
        var g = n.updateQueue;
        g.lastEffect = null, g.events = null, g.stores = null, g.memoCache != null && (g.memoCache.index = 0);
      }
      q.H = Jg, g = r(o, l);
    } while (Pr);
    return g;
  }
  function XC() {
    var n = q.H, r = n.useState()[0];
    return r = typeof r.then == "function" ? qs(r) : r, n = n.useState()[0], (Ke !== null ? Ke.memoizedState : null) !== n && (Re.flags |= 1024), r;
  }
  function kd() {
    var n = kc !== 0;
    return kc = 0, n;
  }
  function Cd(n, r, o) {
    r.updateQueue = n.updateQueue, r.flags &= -2053, n.lanes &= ~o;
  }
  function Ed(n) {
    if (Sc) {
      for (n = n.memoizedState; n !== null; ) {
        var r = n.queue;
        r !== null && (r.pending = null), n = n.next;
      }
      Sc = !1;
    }
    vi = 0, pt = Ke = Re = null, Pr = !1, js = kc = 0, xr = null;
  }
  function qt() {
    var n = {
      memoizedState: null,
      baseState: null,
      baseQueue: null,
      queue: null,
      next: null
    };
    return pt === null ? Re.memoizedState = pt = n : pt = pt.next = n, pt;
  }
  function ft() {
    if (Ke === null) {
      var n = Re.alternate;
      n = n !== null ? n.memoizedState : null;
    } else n = Ke.next;
    var r = pt === null ? Re.memoizedState : pt.next;
    if (r !== null)
      pt = r, Ke = n;
    else {
      if (n === null)
        throw Re.alternate === null ? Error(i(467)) : Error(i(310));
      Ke = n, n = {
        memoizedState: Ke.memoizedState,
        baseState: Ke.baseState,
        baseQueue: Ke.baseQueue,
        queue: Ke.queue,
        next: null
      }, pt === null ? Re.memoizedState = pt = n : pt = pt.next = n;
    }
    return pt;
  }
  function Cc() {
    return { lastEffect: null, events: null, stores: null, memoCache: null };
  }
  function qs(n) {
    var r = js;
    return js += 1, xr === null && (xr = []), n = ng(xr, n, r), r = Re, (pt === null ? r.memoizedState : pt.next) === null && (r = r.alternate, q.H = r === null || r.memoizedState === null ? Yg : Ld), n;
  }
  function Ec(n) {
    if (n !== null && typeof n == "object") {
      if (typeof n.then == "function") return qs(n);
      if (n.$$typeof === ie) return Dt(n);
    }
    throw Error(i(438, String(n)));
  }
  function _d(n) {
    var r = null, o = Re.updateQueue;
    if (o !== null && (r = o.memoCache), r == null) {
      var l = Re.alternate;
      l !== null && (l = l.updateQueue, l !== null && (l = l.memoCache, l != null && (r = {
        data: l.data.map(function(m) {
          return m.slice();
        }),
        index: 0
      })));
    }
    if (r == null && (r = { data: [], index: 0 }), o === null && (o = Cc(), Re.updateQueue = o), o.memoCache = r, o = r.data[r.index], o === void 0)
      for (o = r.data[r.index] = Array(n), l = 0; l < n; l++)
        o[l] = Y;
    return r.index++, o;
  }
  function yi(n, r) {
    return typeof r == "function" ? r(n) : r;
  }
  function _c(n) {
    var r = ft();
    return Rd(r, Ke, n);
  }
  function Rd(n, r, o) {
    var l = n.queue;
    if (l === null) throw Error(i(311));
    l.lastRenderedReducer = o;
    var m = n.baseQueue, g = l.pending;
    if (g !== null) {
      if (m !== null) {
        var b = m.next;
        m.next = g.next, g.next = b;
      }
      r.baseQueue = m = g, l.pending = null;
    }
    if (g = n.baseState, m === null) n.memoizedState = g;
    else {
      r = m.next;
      var S = b = null, O = null, z = r, Q = !1;
      do {
        var $ = z.lane & -536870913;
        if ($ !== z.lane ? (Ne & $) === $ : (vi & $) === $) {
          var F = z.revertLane;
          if (F === 0)
            O !== null && (O = O.next = {
              lane: 0,
              revertLane: 0,
              gesture: null,
              action: z.action,
              hasEagerState: z.hasEagerState,
              eagerState: z.eagerState,
              next: null
            }), $ === wr && (Q = !0);
          else if ((vi & F) === F) {
            z = z.next, F === wr && (Q = !0);
            continue;
          } else
            $ = {
              lane: 0,
              revertLane: z.revertLane,
              gesture: null,
              action: z.action,
              hasEagerState: z.hasEagerState,
              eagerState: z.eagerState,
              next: null
            }, O === null ? (S = O = $, b = g) : O = O.next = $, Re.lanes |= F, ta |= F;
          $ = z.action, qa && o(g, $), g = z.hasEagerState ? z.eagerState : o(g, $);
        } else
          F = {
            lane: $,
            revertLane: z.revertLane,
            gesture: z.gesture,
            action: z.action,
            hasEagerState: z.hasEagerState,
            eagerState: z.eagerState,
            next: null
          }, O === null ? (S = O = F, b = g) : O = O.next = F, Re.lanes |= $, ta |= $;
        z = z.next;
      } while (z !== null && z !== r);
      if (O === null ? b = g : O.next = S, !un(g, n.memoizedState) && (gt = !0, Q && (o = Mr, o !== null)))
        throw o;
      n.memoizedState = g, n.baseState = b, n.baseQueue = O, l.lastRenderedState = g;
    }
    return m === null && (l.lanes = 0), [n.memoizedState, l.dispatch];
  }
  function wd(n) {
    var r = ft(), o = r.queue;
    if (o === null) throw Error(i(311));
    o.lastRenderedReducer = n;
    var l = o.dispatch, m = o.pending, g = r.memoizedState;
    if (m !== null) {
      o.pending = null;
      var b = m = m.next;
      do
        g = n(g, b.action), b = b.next;
      while (b !== m);
      un(g, r.memoizedState) || (gt = !0), r.memoizedState = g, r.baseQueue === null && (r.baseState = g), o.lastRenderedState = g;
    }
    return [g, l];
  }
  function hg(n, r, o) {
    var l = Re, m = ft(), g = Le;
    if (g) {
      if (o === void 0) throw Error(i(407));
      o = o();
    } else o = r();
    var b = !un(
      (Ke || m).memoizedState,
      o
    );
    if (b && (m.memoizedState = o, gt = !0), m = m.queue, Od(gg.bind(null, l, m, n), [
      n
    ]), m.getSnapshot !== r || b || pt !== null && pt.memoizedState.tag & 1) {
      if (l.flags |= 2048, Nr(
        9,
        { destroy: void 0 },
        pg.bind(
          null,
          l,
          m,
          o,
          r
        ),
        null
      ), Xe === null) throw Error(i(349));
      g || (vi & 127) !== 0 || mg(l, r, o);
    }
    return o;
  }
  function mg(n, r, o) {
    n.flags |= 16384, n = { getSnapshot: r, value: o }, r = Re.updateQueue, r === null ? (r = Cc(), Re.updateQueue = r, r.stores = [n]) : (o = r.stores, o === null ? r.stores = [n] : o.push(n));
  }
  function pg(n, r, o, l) {
    r.value = o, r.getSnapshot = l, vg(r) && yg(n);
  }
  function gg(n, r, o) {
    return o(function() {
      vg(r) && yg(n);
    });
  }
  function vg(n) {
    var r = n.getSnapshot;
    n = n.value;
    try {
      var o = r();
      return !un(n, o);
    } catch {
      return !0;
    }
  }
  function yg(n) {
    var r = xa(n, 2);
    r !== null && en(r, n, 2);
  }
  function Md(n) {
    var r = qt();
    if (typeof n == "function") {
      var o = n;
      if (n = o(), qa) {
        ji(!0);
        try {
          o();
        } finally {
          ji(!1);
        }
      }
    }
    return r.memoizedState = r.baseState = n, r.queue = {
      pending: null,
      lanes: 0,
      dispatch: null,
      lastRenderedReducer: yi,
      lastRenderedState: n
    }, r;
  }
  function bg(n, r, o, l) {
    return n.baseState = o, Rd(
      n,
      Ke,
      typeof l == "function" ? l : yi
    );
  }
  function ZC(n, r, o, l, m) {
    if (Mc(n)) throw Error(i(485));
    if (n = r.action, n !== null) {
      var g = {
        payload: m,
        action: n,
        next: null,
        isTransition: !0,
        status: "pending",
        value: null,
        reason: null,
        listeners: [],
        then: function(b) {
          g.listeners.push(b);
        }
      };
      q.T !== null ? o(!0) : g.isTransition = !1, l(g), o = r.pending, o === null ? (g.next = r.pending = g, Tg(r, g)) : (g.next = o.next, r.pending = o.next = g);
    }
  }
  function Tg(n, r) {
    var o = r.action, l = r.payload, m = n.state;
    if (r.isTransition) {
      var g = q.T, b = {};
      q.T = b;
      try {
        var S = o(m, l), O = q.S;
        O !== null && O(b, S), Sg(n, r, S);
      } catch (z) {
        Ad(n, r, z);
      } finally {
        g !== null && b.types !== null && (g.types = b.types), q.T = g;
      }
    } else
      try {
        g = o(m, l), Sg(n, r, g);
      } catch (z) {
        Ad(n, r, z);
      }
  }
  function Sg(n, r, o) {
    o !== null && typeof o == "object" && typeof o.then == "function" ? o.then(
      function(l) {
        kg(n, r, l);
      },
      function(l) {
        return Ad(n, r, l);
      }
    ) : kg(n, r, o);
  }
  function kg(n, r, o) {
    r.status = "fulfilled", r.value = o, Cg(r), n.state = o, r = n.pending, r !== null && (o = r.next, o === r ? n.pending = null : (o = o.next, r.next = o, Tg(n, o)));
  }
  function Ad(n, r, o) {
    var l = n.pending;
    if (n.pending = null, l !== null) {
      l = l.next;
      do
        r.status = "rejected", r.reason = o, Cg(r), r = r.next;
      while (r !== l);
    }
    n.action = null;
  }
  function Cg(n) {
    n = n.listeners;
    for (var r = 0; r < n.length; r++) (0, n[r])();
  }
  function Eg(n, r) {
    return r;
  }
  function _g(n, r) {
    if (Le) {
      var o = Xe.formState;
      if (o !== null) {
        e: {
          var l = Re;
          if (Le) {
            if (et) {
              t: {
                for (var m = et, g = _n; m.nodeType !== 8; ) {
                  if (!g) {
                    m = null;
                    break t;
                  }
                  if (m = wn(
                    m.nextSibling
                  ), m === null) {
                    m = null;
                    break t;
                  }
                }
                g = m.data, m = g === "F!" || g === "F" ? m : null;
              }
              if (m) {
                et = wn(
                  m.nextSibling
                ), l = m.data === "F!";
                break e;
              }
            }
            Ki(l);
          }
          l = !1;
        }
        l && (r = o[0]);
      }
    }
    return o = qt(), o.memoizedState = o.baseState = r, l = {
      pending: null,
      lanes: 0,
      dispatch: null,
      lastRenderedReducer: Eg,
      lastRenderedState: r
    }, o.queue = l, o = Vg.bind(
      null,
      Re,
      l
    ), l.dispatch = o, l = Md(!1), g = Id.bind(
      null,
      Re,
      !1,
      l.queue
    ), l = qt(), m = {
      state: r,
      dispatch: null,
      action: n,
      pending: null
    }, l.queue = m, o = ZC.bind(
      null,
      Re,
      m,
      g,
      o
    ), m.dispatch = o, l.memoizedState = n, [r, o, !1];
  }
  function Rg(n) {
    var r = ft();
    return wg(r, Ke, n);
  }
  function wg(n, r, o) {
    if (r = Rd(
      n,
      r,
      Eg
    )[0], n = _c(yi)[0], typeof r == "object" && r !== null && typeof r.then == "function")
      try {
        var l = qs(r);
      } catch (b) {
        throw b === Ar ? pc : b;
      }
    else l = r;
    r = ft();
    var m = r.queue, g = m.dispatch;
    return o !== r.memoizedState && (Re.flags |= 2048, Nr(
      9,
      { destroy: void 0 },
      WC.bind(null, m, o),
      null
    )), [l, g, n];
  }
  function WC(n, r) {
    n.action = r;
  }
  function Mg(n) {
    var r = ft(), o = Ke;
    if (o !== null)
      return wg(r, o, n);
    ft(), r = r.memoizedState, o = ft();
    var l = o.queue.dispatch;
    return o.memoizedState = n, [r, l, !1];
  }
  function Nr(n, r, o, l) {
    return n = { tag: n, create: o, deps: l, inst: r, next: null }, r = Re.updateQueue, r === null && (r = Cc(), Re.updateQueue = r), o = r.lastEffect, o === null ? r.lastEffect = n.next = n : (l = o.next, o.next = n, n.next = l, r.lastEffect = n), n;
  }
  function Ag() {
    return ft().memoizedState;
  }
  function Rc(n, r, o, l) {
    var m = qt();
    Re.flags |= n, m.memoizedState = Nr(
      1 | r,
      { destroy: void 0 },
      o,
      l === void 0 ? null : l
    );
  }
  function wc(n, r, o, l) {
    var m = ft();
    l = l === void 0 ? null : l;
    var g = m.memoizedState.inst;
    Ke !== null && l !== null && Td(l, Ke.memoizedState.deps) ? m.memoizedState = Nr(r, g, o, l) : (Re.flags |= n, m.memoizedState = Nr(
      1 | r,
      g,
      o,
      l
    ));
  }
  function Og(n, r) {
    Rc(8390656, 8, n, r);
  }
  function Od(n, r) {
    wc(2048, 8, n, r);
  }
  function $C(n) {
    Re.flags |= 4;
    var r = Re.updateQueue;
    if (r === null)
      r = Cc(), Re.updateQueue = r, r.events = [n];
    else {
      var o = r.events;
      o === null ? r.events = [n] : o.push(n);
    }
  }
  function Dg(n) {
    var r = ft().memoizedState;
    return $C({ ref: r, nextImpl: n }), function() {
      if ((qe & 2) !== 0) throw Error(i(440));
      return r.impl.apply(void 0, arguments);
    };
  }
  function Pg(n, r) {
    return wc(4, 2, n, r);
  }
  function xg(n, r) {
    return wc(4, 4, n, r);
  }
  function Ng(n, r) {
    if (typeof r == "function") {
      n = n();
      var o = r(n);
      return function() {
        typeof o == "function" ? o() : r(null);
      };
    }
    if (r != null)
      return n = n(), r.current = n, function() {
        r.current = null;
      };
  }
  function Ig(n, r, o) {
    o = o != null ? o.concat([n]) : null, wc(4, 4, Ng.bind(null, r, n), o);
  }
  function Dd() {
  }
  function Lg(n, r) {
    var o = ft();
    r = r === void 0 ? null : r;
    var l = o.memoizedState;
    return r !== null && Td(r, l[1]) ? l[0] : (o.memoizedState = [n, r], n);
  }
  function Ug(n, r) {
    var o = ft();
    r = r === void 0 ? null : r;
    var l = o.memoizedState;
    if (r !== null && Td(r, l[1]))
      return l[0];
    if (l = n(), qa) {
      ji(!0);
      try {
        n();
      } finally {
        ji(!1);
      }
    }
    return o.memoizedState = [l, r], l;
  }
  function Pd(n, r, o) {
    return o === void 0 || (vi & 1073741824) !== 0 && (Ne & 261930) === 0 ? n.memoizedState = r : (n.memoizedState = o, n = Bv(), Re.lanes |= n, ta |= n, o);
  }
  function Bg(n, r, o, l) {
    return un(o, r) ? o : Dr.current !== null ? (n = Pd(n, o, l), un(n, r) || (gt = !0), n) : (vi & 42) === 0 || (vi & 1073741824) !== 0 && (Ne & 261930) === 0 ? (gt = !0, n.memoizedState = o) : (n = Bv(), Re.lanes |= n, ta |= n, r);
  }
  function zg(n, r, o, l, m) {
    var g = te.p;
    te.p = g !== 0 && 8 > g ? g : 8;
    var b = q.T, S = {};
    q.T = S, Id(n, !1, r, o);
    try {
      var O = m(), z = q.S;
      if (z !== null && z(S, O), O !== null && typeof O == "object" && typeof O.then == "function") {
        var Q = JC(
          O,
          l
        );
        Hs(
          n,
          r,
          Q,
          gn(n)
        );
      } else
        Hs(
          n,
          r,
          l,
          gn(n)
        );
    } catch ($) {
      Hs(
        n,
        r,
        { then: function() {
        }, status: "rejected", reason: $ },
        gn()
      );
    } finally {
      te.p = g, b !== null && S.types !== null && (b.types = S.types), q.T = b;
    }
  }
  function eE() {
  }
  function xd(n, r, o, l) {
    if (n.tag !== 5) throw Error(i(476));
    var m = Fg(n).queue;
    zg(
      n,
      m,
      r,
      le,
      o === null ? eE : function() {
        return jg(n), o(l);
      }
    );
  }
  function Fg(n) {
    var r = n.memoizedState;
    if (r !== null) return r;
    r = {
      memoizedState: le,
      baseState: le,
      baseQueue: null,
      queue: {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: yi,
        lastRenderedState: le
      },
      next: null
    };
    var o = {};
    return r.next = {
      memoizedState: o,
      baseState: o,
      baseQueue: null,
      queue: {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: yi,
        lastRenderedState: o
      },
      next: null
    }, n.memoizedState = r, n = n.alternate, n !== null && (n.memoizedState = r), r;
  }
  function jg(n) {
    var r = Fg(n);
    r.next === null && (r = n.alternate.memoizedState), Hs(
      n,
      r.next.queue,
      {},
      gn()
    );
  }
  function Nd() {
    return Dt(so);
  }
  function qg() {
    return ft().memoizedState;
  }
  function Hg() {
    return ft().memoizedState;
  }
  function tE(n) {
    for (var r = n.return; r !== null; ) {
      switch (r.tag) {
        case 24:
        case 3:
          var o = gn();
          n = Qi(o);
          var l = Xi(r, n, o);
          l !== null && (en(l, r, o), Bs(l, r, o)), r = { cache: ld() }, n.payload = r;
          return;
      }
      r = r.return;
    }
  }
  function nE(n, r, o) {
    var l = gn();
    o = {
      lane: l,
      revertLane: 0,
      gesture: null,
      action: o,
      hasEagerState: !1,
      eagerState: null,
      next: null
    }, Mc(n) ? Gg(r, o) : (o = Wu(n, r, o, l), o !== null && (en(o, n, l), Kg(o, r, l)));
  }
  function Vg(n, r, o) {
    var l = gn();
    Hs(n, r, o, l);
  }
  function Hs(n, r, o, l) {
    var m = {
      lane: l,
      revertLane: 0,
      gesture: null,
      action: o,
      hasEagerState: !1,
      eagerState: null,
      next: null
    };
    if (Mc(n)) Gg(r, m);
    else {
      var g = n.alternate;
      if (n.lanes === 0 && (g === null || g.lanes === 0) && (g = r.lastRenderedReducer, g !== null))
        try {
          var b = r.lastRenderedState, S = g(b, o);
          if (m.hasEagerState = !0, m.eagerState = S, un(S, b))
            return cc(n, r, m, 0), Xe === null && oc(), !1;
        } catch {
        } finally {
        }
      if (o = Wu(n, r, m, l), o !== null)
        return en(o, n, l), Kg(o, r, l), !0;
    }
    return !1;
  }
  function Id(n, r, o, l) {
    if (l = {
      lane: 2,
      revertLane: mf(),
      gesture: null,
      action: l,
      hasEagerState: !1,
      eagerState: null,
      next: null
    }, Mc(n)) {
      if (r) throw Error(i(479));
    } else
      r = Wu(
        n,
        o,
        l,
        2
      ), r !== null && en(r, n, 2);
  }
  function Mc(n) {
    var r = n.alternate;
    return n === Re || r !== null && r === Re;
  }
  function Gg(n, r) {
    Pr = Sc = !0;
    var o = n.pending;
    o === null ? r.next = r : (r.next = o.next, o.next = r), n.pending = r;
  }
  function Kg(n, r, o) {
    if ((o & 4194048) !== 0) {
      var l = r.lanes;
      l &= n.pendingLanes, o |= l, r.lanes = o, Zm(n, o);
    }
  }
  var Vs = {
    readContext: Dt,
    use: Ec,
    useCallback: ot,
    useContext: ot,
    useEffect: ot,
    useImperativeHandle: ot,
    useLayoutEffect: ot,
    useInsertionEffect: ot,
    useMemo: ot,
    useReducer: ot,
    useRef: ot,
    useState: ot,
    useDebugValue: ot,
    useDeferredValue: ot,
    useTransition: ot,
    useSyncExternalStore: ot,
    useId: ot,
    useHostTransitionStatus: ot,
    useFormState: ot,
    useActionState: ot,
    useOptimistic: ot,
    useMemoCache: ot,
    useCacheRefresh: ot
  };
  Vs.useEffectEvent = ot;
  var Yg = {
    readContext: Dt,
    use: Ec,
    useCallback: function(n, r) {
      return qt().memoizedState = [
        n,
        r === void 0 ? null : r
      ], n;
    },
    useContext: Dt,
    useEffect: Og,
    useImperativeHandle: function(n, r, o) {
      o = o != null ? o.concat([n]) : null, Rc(
        4194308,
        4,
        Ng.bind(null, r, n),
        o
      );
    },
    useLayoutEffect: function(n, r) {
      return Rc(4194308, 4, n, r);
    },
    useInsertionEffect: function(n, r) {
      Rc(4, 2, n, r);
    },
    useMemo: function(n, r) {
      var o = qt();
      r = r === void 0 ? null : r;
      var l = n();
      if (qa) {
        ji(!0);
        try {
          n();
        } finally {
          ji(!1);
        }
      }
      return o.memoizedState = [l, r], l;
    },
    useReducer: function(n, r, o) {
      var l = qt();
      if (o !== void 0) {
        var m = o(r);
        if (qa) {
          ji(!0);
          try {
            o(r);
          } finally {
            ji(!1);
          }
        }
      } else m = r;
      return l.memoizedState = l.baseState = m, n = {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: n,
        lastRenderedState: m
      }, l.queue = n, n = n.dispatch = nE.bind(
        null,
        Re,
        n
      ), [l.memoizedState, n];
    },
    useRef: function(n) {
      var r = qt();
      return n = { current: n }, r.memoizedState = n;
    },
    useState: function(n) {
      n = Md(n);
      var r = n.queue, o = Vg.bind(null, Re, r);
      return r.dispatch = o, [n.memoizedState, o];
    },
    useDebugValue: Dd,
    useDeferredValue: function(n, r) {
      var o = qt();
      return Pd(o, n, r);
    },
    useTransition: function() {
      var n = Md(!1);
      return n = zg.bind(
        null,
        Re,
        n.queue,
        !0,
        !1
      ), qt().memoizedState = n, [!1, n];
    },
    useSyncExternalStore: function(n, r, o) {
      var l = Re, m = qt();
      if (Le) {
        if (o === void 0)
          throw Error(i(407));
        o = o();
      } else {
        if (o = r(), Xe === null)
          throw Error(i(349));
        (Ne & 127) !== 0 || mg(l, r, o);
      }
      m.memoizedState = o;
      var g = { value: o, getSnapshot: r };
      return m.queue = g, Og(gg.bind(null, l, g, n), [
        n
      ]), l.flags |= 2048, Nr(
        9,
        { destroy: void 0 },
        pg.bind(
          null,
          l,
          g,
          o,
          r
        ),
        null
      ), o;
    },
    useId: function() {
      var n = qt(), r = Xe.identifierPrefix;
      if (Le) {
        var o = ei, l = $n;
        o = (l & ~(1 << 32 - ln(l) - 1)).toString(32) + o, r = "_" + r + "R_" + o, o = kc++, 0 < o && (r += "H" + o.toString(32)), r += "_";
      } else
        o = QC++, r = "_" + r + "r_" + o.toString(32) + "_";
      return n.memoizedState = r;
    },
    useHostTransitionStatus: Nd,
    useFormState: _g,
    useActionState: _g,
    useOptimistic: function(n) {
      var r = qt();
      r.memoizedState = r.baseState = n;
      var o = {
        pending: null,
        lanes: 0,
        dispatch: null,
        lastRenderedReducer: null,
        lastRenderedState: null
      };
      return r.queue = o, r = Id.bind(
        null,
        Re,
        !0,
        o
      ), o.dispatch = r, [n, r];
    },
    useMemoCache: _d,
    useCacheRefresh: function() {
      return qt().memoizedState = tE.bind(
        null,
        Re
      );
    },
    useEffectEvent: function(n) {
      var r = qt(), o = { impl: n };
      return r.memoizedState = o, function() {
        if ((qe & 2) !== 0)
          throw Error(i(440));
        return o.impl.apply(void 0, arguments);
      };
    }
  }, Ld = {
    readContext: Dt,
    use: Ec,
    useCallback: Lg,
    useContext: Dt,
    useEffect: Od,
    useImperativeHandle: Ig,
    useInsertionEffect: Pg,
    useLayoutEffect: xg,
    useMemo: Ug,
    useReducer: _c,
    useRef: Ag,
    useState: function() {
      return _c(yi);
    },
    useDebugValue: Dd,
    useDeferredValue: function(n, r) {
      var o = ft();
      return Bg(
        o,
        Ke.memoizedState,
        n,
        r
      );
    },
    useTransition: function() {
      var n = _c(yi)[0], r = ft().memoizedState;
      return [
        typeof n == "boolean" ? n : qs(n),
        r
      ];
    },
    useSyncExternalStore: hg,
    useId: qg,
    useHostTransitionStatus: Nd,
    useFormState: Rg,
    useActionState: Rg,
    useOptimistic: function(n, r) {
      var o = ft();
      return bg(o, Ke, n, r);
    },
    useMemoCache: _d,
    useCacheRefresh: Hg
  };
  Ld.useEffectEvent = Dg;
  var Jg = {
    readContext: Dt,
    use: Ec,
    useCallback: Lg,
    useContext: Dt,
    useEffect: Od,
    useImperativeHandle: Ig,
    useInsertionEffect: Pg,
    useLayoutEffect: xg,
    useMemo: Ug,
    useReducer: wd,
    useRef: Ag,
    useState: function() {
      return wd(yi);
    },
    useDebugValue: Dd,
    useDeferredValue: function(n, r) {
      var o = ft();
      return Ke === null ? Pd(o, n, r) : Bg(
        o,
        Ke.memoizedState,
        n,
        r
      );
    },
    useTransition: function() {
      var n = wd(yi)[0], r = ft().memoizedState;
      return [
        typeof n == "boolean" ? n : qs(n),
        r
      ];
    },
    useSyncExternalStore: hg,
    useId: qg,
    useHostTransitionStatus: Nd,
    useFormState: Mg,
    useActionState: Mg,
    useOptimistic: function(n, r) {
      var o = ft();
      return Ke !== null ? bg(o, Ke, n, r) : (o.baseState = n, [n, o.queue.dispatch]);
    },
    useMemoCache: _d,
    useCacheRefresh: Hg
  };
  Jg.useEffectEvent = Dg;
  function Ud(n, r, o, l) {
    r = n.memoizedState, o = o(l, r), o = o == null ? r : v({}, r, o), n.memoizedState = o, n.lanes === 0 && (n.updateQueue.baseState = o);
  }
  var Bd = {
    enqueueSetState: function(n, r, o) {
      n = n._reactInternals;
      var l = gn(), m = Qi(l);
      m.payload = r, o != null && (m.callback = o), r = Xi(n, m, l), r !== null && (en(r, n, l), Bs(r, n, l));
    },
    enqueueReplaceState: function(n, r, o) {
      n = n._reactInternals;
      var l = gn(), m = Qi(l);
      m.tag = 1, m.payload = r, o != null && (m.callback = o), r = Xi(n, m, l), r !== null && (en(r, n, l), Bs(r, n, l));
    },
    enqueueForceUpdate: function(n, r) {
      n = n._reactInternals;
      var o = gn(), l = Qi(o);
      l.tag = 2, r != null && (l.callback = r), r = Xi(n, l, o), r !== null && (en(r, n, o), Bs(r, n, o));
    }
  };
  function Qg(n, r, o, l, m, g, b) {
    return n = n.stateNode, typeof n.shouldComponentUpdate == "function" ? n.shouldComponentUpdate(l, g, b) : r.prototype && r.prototype.isPureReactComponent ? !Os(o, l) || !Os(m, g) : !0;
  }
  function Xg(n, r, o, l) {
    n = r.state, typeof r.componentWillReceiveProps == "function" && r.componentWillReceiveProps(o, l), typeof r.UNSAFE_componentWillReceiveProps == "function" && r.UNSAFE_componentWillReceiveProps(o, l), r.state !== n && Bd.enqueueReplaceState(r, r.state, null);
  }
  function Ha(n, r) {
    var o = r;
    if ("ref" in r) {
      o = {};
      for (var l in r)
        l !== "ref" && (o[l] = r[l]);
    }
    if (n = n.defaultProps) {
      o === r && (o = v({}, o));
      for (var m in n)
        o[m] === void 0 && (o[m] = n[m]);
    }
    return o;
  }
  function Zg(n) {
    sc(n);
  }
  function Wg(n) {
    console.error(n);
  }
  function $g(n) {
    sc(n);
  }
  function Ac(n, r) {
    try {
      var o = n.onUncaughtError;
      o(r.value, { componentStack: r.stack });
    } catch (l) {
      setTimeout(function() {
        throw l;
      });
    }
  }
  function ev(n, r, o) {
    try {
      var l = n.onCaughtError;
      l(o.value, {
        componentStack: o.stack,
        errorBoundary: r.tag === 1 ? r.stateNode : null
      });
    } catch (m) {
      setTimeout(function() {
        throw m;
      });
    }
  }
  function zd(n, r, o) {
    return o = Qi(o), o.tag = 3, o.payload = { element: null }, o.callback = function() {
      Ac(n, r);
    }, o;
  }
  function tv(n) {
    return n = Qi(n), n.tag = 3, n;
  }
  function nv(n, r, o, l) {
    var m = o.type.getDerivedStateFromError;
    if (typeof m == "function") {
      var g = l.value;
      n.payload = function() {
        return m(g);
      }, n.callback = function() {
        ev(r, o, l);
      };
    }
    var b = o.stateNode;
    b !== null && typeof b.componentDidCatch == "function" && (n.callback = function() {
      ev(r, o, l), typeof m != "function" && (na === null ? na = /* @__PURE__ */ new Set([this]) : na.add(this));
      var S = l.stack;
      this.componentDidCatch(l.value, {
        componentStack: S !== null ? S : ""
      });
    });
  }
  function iE(n, r, o, l, m) {
    if (o.flags |= 32768, l !== null && typeof l == "object" && typeof l.then == "function") {
      if (r = o.alternate, r !== null && Rr(
        r,
        o,
        m,
        !0
      ), o = fn.current, o !== null) {
        switch (o.tag) {
          case 31:
          case 13:
            return Rn === null ? jc() : o.alternate === null && ct === 0 && (ct = 3), o.flags &= -257, o.flags |= 65536, o.lanes = m, l === gc ? o.flags |= 16384 : (r = o.updateQueue, r === null ? o.updateQueue = /* @__PURE__ */ new Set([l]) : r.add(l), df(n, l, m)), !1;
          case 22:
            return o.flags |= 65536, l === gc ? o.flags |= 16384 : (r = o.updateQueue, r === null ? (r = {
              transitions: null,
              markerInstances: null,
              retryQueue: /* @__PURE__ */ new Set([l])
            }, o.updateQueue = r) : (o = r.retryQueue, o === null ? r.retryQueue = /* @__PURE__ */ new Set([l]) : o.add(l)), df(n, l, m)), !1;
        }
        throw Error(i(435, o.tag));
      }
      return df(n, l, m), jc(), !1;
    }
    if (Le)
      return r = fn.current, r !== null ? ((r.flags & 65536) === 0 && (r.flags |= 256), r.flags |= 65536, r.lanes = m, l !== ad && (n = Error(i(422), { cause: l }), xs(kn(n, o)))) : (l !== ad && (r = Error(i(423), {
        cause: l
      }), xs(
        kn(r, o)
      )), n = n.current.alternate, n.flags |= 65536, m &= -m, n.lanes |= m, l = kn(l, o), m = zd(
        n.stateNode,
        l,
        m
      ), pd(n, m), ct !== 4 && (ct = 2)), !1;
    var g = Error(i(520), { cause: l });
    if (g = kn(g, o), Ws === null ? Ws = [g] : Ws.push(g), ct !== 4 && (ct = 2), r === null) return !0;
    l = kn(l, o), o = r;
    do {
      switch (o.tag) {
        case 3:
          return o.flags |= 65536, n = m & -m, o.lanes |= n, n = zd(o.stateNode, l, n), pd(o, n), !1;
        case 1:
          if (r = o.type, g = o.stateNode, (o.flags & 128) === 0 && (typeof r.getDerivedStateFromError == "function" || g !== null && typeof g.componentDidCatch == "function" && (na === null || !na.has(g))))
            return o.flags |= 65536, m &= -m, o.lanes |= m, m = tv(m), nv(
              m,
              n,
              o,
              l
            ), pd(o, m), !1;
      }
      o = o.return;
    } while (o !== null);
    return !1;
  }
  var Fd = Error(i(461)), gt = !1;
  function Pt(n, r, o, l) {
    r.child = n === null ? sg(r, null, o, l) : ja(
      r,
      n.child,
      o,
      l
    );
  }
  function iv(n, r, o, l, m) {
    o = o.render;
    var g = r.ref;
    if ("ref" in l) {
      var b = {};
      for (var S in l)
        S !== "ref" && (b[S] = l[S]);
    } else b = l;
    return Ua(r), l = Sd(
      n,
      r,
      o,
      b,
      g,
      m
    ), S = kd(), n !== null && !gt ? (Cd(n, r, m), bi(n, r, m)) : (Le && S && nd(r), r.flags |= 1, Pt(n, r, l, m), r.child);
  }
  function av(n, r, o, l, m) {
    if (n === null) {
      var g = o.type;
      return typeof g == "function" && !$u(g) && g.defaultProps === void 0 && o.compare === null ? (r.tag = 15, r.type = g, rv(
        n,
        r,
        g,
        l,
        m
      )) : (n = uc(
        o.type,
        null,
        l,
        r,
        r.mode,
        m
      ), n.ref = r.ref, n.return = r, r.child = n);
    }
    if (g = n.child, !Jd(n, m)) {
      var b = g.memoizedProps;
      if (o = o.compare, o = o !== null ? o : Os, o(b, l) && n.ref === r.ref)
        return bi(n, r, m);
    }
    return r.flags |= 1, n = hi(g, l), n.ref = r.ref, n.return = r, r.child = n;
  }
  function rv(n, r, o, l, m) {
    if (n !== null) {
      var g = n.memoizedProps;
      if (Os(g, l) && n.ref === r.ref)
        if (gt = !1, r.pendingProps = l = g, Jd(n, m))
          (n.flags & 131072) !== 0 && (gt = !0);
        else
          return r.lanes = n.lanes, bi(n, r, m);
    }
    return jd(
      n,
      r,
      o,
      l,
      m
    );
  }
  function sv(n, r, o, l) {
    var m = l.children, g = n !== null ? n.memoizedState : null;
    if (n === null && r.stateNode === null && (r.stateNode = {
      _visibility: 1,
      _pendingMarkers: null,
      _retryCache: null,
      _transitions: null
    }), l.mode === "hidden") {
      if ((r.flags & 128) !== 0) {
        if (g = g !== null ? g.baseLanes | o : o, n !== null) {
          for (l = r.child = n.child, m = 0; l !== null; )
            m = m | l.lanes | l.childLanes, l = l.sibling;
          l = m & ~g;
        } else l = 0, r.child = null;
        return ov(
          n,
          r,
          g,
          o,
          l
        );
      }
      if ((o & 536870912) !== 0)
        r.memoizedState = { baseLanes: 0, cachePool: null }, n !== null && mc(
          r,
          g !== null ? g.cachePool : null
        ), g !== null ? lg(r, g) : vd(), ug(r);
      else
        return l = r.lanes = 536870912, ov(
          n,
          r,
          g !== null ? g.baseLanes | o : o,
          o,
          l
        );
    } else
      g !== null ? (mc(r, g.cachePool), lg(r, g), Wi(), r.memoizedState = null) : (n !== null && mc(r, null), vd(), Wi());
    return Pt(n, r, m, o), r.child;
  }
  function Gs(n, r) {
    return n !== null && n.tag === 22 || r.stateNode !== null || (r.stateNode = {
      _visibility: 1,
      _pendingMarkers: null,
      _retryCache: null,
      _transitions: null
    }), r.sibling;
  }
  function ov(n, r, o, l, m) {
    var g = dd();
    return g = g === null ? null : { parent: mt._currentValue, pool: g }, r.memoizedState = {
      baseLanes: o,
      cachePool: g
    }, n !== null && mc(r, null), vd(), ug(r), n !== null && Rr(n, r, l, !0), r.childLanes = m, null;
  }
  function Oc(n, r) {
    return r = Pc(
      { mode: r.mode, children: r.children },
      n.mode
    ), r.ref = n.ref, n.child = r, r.return = n, r;
  }
  function cv(n, r, o) {
    return ja(r, n.child, null, o), n = Oc(r, r.pendingProps), n.flags |= 2, hn(r), r.memoizedState = null, n;
  }
  function aE(n, r, o) {
    var l = r.pendingProps, m = (r.flags & 128) !== 0;
    if (r.flags &= -129, n === null) {
      if (Le) {
        if (l.mode === "hidden")
          return n = Oc(r, l), r.lanes = 536870912, Gs(null, n);
        if (bd(r), (n = et) ? (n = Ty(
          n,
          _n
        ), n = n !== null && n.data === "&" ? n : null, n !== null && (r.memoizedState = {
          dehydrated: n,
          treeContext: Vi !== null ? { id: $n, overflow: ei } : null,
          retryLane: 536870912,
          hydrationErrors: null
        }, o = Gp(n), o.return = r, r.child = o, Ot = r, et = null)) : n = null, n === null) throw Ki(r);
        return r.lanes = 536870912, null;
      }
      return Oc(r, l);
    }
    var g = n.memoizedState;
    if (g !== null) {
      var b = g.dehydrated;
      if (bd(r), m)
        if (r.flags & 256)
          r.flags &= -257, r = cv(
            n,
            r,
            o
          );
        else if (r.memoizedState !== null)
          r.child = n.child, r.flags |= 128, r = null;
        else throw Error(i(558));
      else if (gt || Rr(n, r, o, !1), m = (o & n.childLanes) !== 0, gt || m) {
        if (l = Xe, l !== null && (b = Wm(l, o), b !== 0 && b !== g.retryLane))
          throw g.retryLane = b, xa(n, b), en(l, n, b), Fd;
        jc(), r = cv(
          n,
          r,
          o
        );
      } else
        n = g.treeContext, et = wn(b.nextSibling), Ot = r, Le = !0, Gi = null, _n = !1, n !== null && Jp(r, n), r = Oc(r, l), r.flags |= 4096;
      return r;
    }
    return n = hi(n.child, {
      mode: l.mode,
      children: l.children
    }), n.ref = r.ref, r.child = n, n.return = r, n;
  }
  function Dc(n, r) {
    var o = r.ref;
    if (o === null)
      n !== null && n.ref !== null && (r.flags |= 4194816);
    else {
      if (typeof o != "function" && typeof o != "object")
        throw Error(i(284));
      (n === null || n.ref !== o) && (r.flags |= 4194816);
    }
  }
  function jd(n, r, o, l, m) {
    return Ua(r), o = Sd(
      n,
      r,
      o,
      l,
      void 0,
      m
    ), l = kd(), n !== null && !gt ? (Cd(n, r, m), bi(n, r, m)) : (Le && l && nd(r), r.flags |= 1, Pt(n, r, o, m), r.child);
  }
  function lv(n, r, o, l, m, g) {
    return Ua(r), r.updateQueue = null, o = fg(
      r,
      l,
      o,
      m
    ), dg(n), l = kd(), n !== null && !gt ? (Cd(n, r, g), bi(n, r, g)) : (Le && l && nd(r), r.flags |= 1, Pt(n, r, o, g), r.child);
  }
  function uv(n, r, o, l, m) {
    if (Ua(r), r.stateNode === null) {
      var g = kr, b = o.contextType;
      typeof b == "object" && b !== null && (g = Dt(b)), g = new o(l, g), r.memoizedState = g.state !== null && g.state !== void 0 ? g.state : null, g.updater = Bd, r.stateNode = g, g._reactInternals = r, g = r.stateNode, g.props = l, g.state = r.memoizedState, g.refs = {}, hd(r), b = o.contextType, g.context = typeof b == "object" && b !== null ? Dt(b) : kr, g.state = r.memoizedState, b = o.getDerivedStateFromProps, typeof b == "function" && (Ud(
        r,
        o,
        b,
        l
      ), g.state = r.memoizedState), typeof o.getDerivedStateFromProps == "function" || typeof g.getSnapshotBeforeUpdate == "function" || typeof g.UNSAFE_componentWillMount != "function" && typeof g.componentWillMount != "function" || (b = g.state, typeof g.componentWillMount == "function" && g.componentWillMount(), typeof g.UNSAFE_componentWillMount == "function" && g.UNSAFE_componentWillMount(), b !== g.state && Bd.enqueueReplaceState(g, g.state, null), Fs(r, l, g, m), zs(), g.state = r.memoizedState), typeof g.componentDidMount == "function" && (r.flags |= 4194308), l = !0;
    } else if (n === null) {
      g = r.stateNode;
      var S = r.memoizedProps, O = Ha(o, S);
      g.props = O;
      var z = g.context, Q = o.contextType;
      b = kr, typeof Q == "object" && Q !== null && (b = Dt(Q));
      var $ = o.getDerivedStateFromProps;
      Q = typeof $ == "function" || typeof g.getSnapshotBeforeUpdate == "function", S = r.pendingProps !== S, Q || typeof g.UNSAFE_componentWillReceiveProps != "function" && typeof g.componentWillReceiveProps != "function" || (S || z !== b) && Xg(
        r,
        g,
        l,
        b
      ), Ji = !1;
      var F = r.memoizedState;
      g.state = F, Fs(r, l, g, m), zs(), z = r.memoizedState, S || F !== z || Ji ? (typeof $ == "function" && (Ud(
        r,
        o,
        $,
        l
      ), z = r.memoizedState), (O = Ji || Qg(
        r,
        o,
        O,
        l,
        F,
        z,
        b
      )) ? (Q || typeof g.UNSAFE_componentWillMount != "function" && typeof g.componentWillMount != "function" || (typeof g.componentWillMount == "function" && g.componentWillMount(), typeof g.UNSAFE_componentWillMount == "function" && g.UNSAFE_componentWillMount()), typeof g.componentDidMount == "function" && (r.flags |= 4194308)) : (typeof g.componentDidMount == "function" && (r.flags |= 4194308), r.memoizedProps = l, r.memoizedState = z), g.props = l, g.state = z, g.context = b, l = O) : (typeof g.componentDidMount == "function" && (r.flags |= 4194308), l = !1);
    } else {
      g = r.stateNode, md(n, r), b = r.memoizedProps, Q = Ha(o, b), g.props = Q, $ = r.pendingProps, F = g.context, z = o.contextType, O = kr, typeof z == "object" && z !== null && (O = Dt(z)), S = o.getDerivedStateFromProps, (z = typeof S == "function" || typeof g.getSnapshotBeforeUpdate == "function") || typeof g.UNSAFE_componentWillReceiveProps != "function" && typeof g.componentWillReceiveProps != "function" || (b !== $ || F !== O) && Xg(
        r,
        g,
        l,
        O
      ), Ji = !1, F = r.memoizedState, g.state = F, Fs(r, l, g, m), zs();
      var V = r.memoizedState;
      b !== $ || F !== V || Ji || n !== null && n.dependencies !== null && fc(n.dependencies) ? (typeof S == "function" && (Ud(
        r,
        o,
        S,
        l
      ), V = r.memoizedState), (Q = Ji || Qg(
        r,
        o,
        Q,
        l,
        F,
        V,
        O
      ) || n !== null && n.dependencies !== null && fc(n.dependencies)) ? (z || typeof g.UNSAFE_componentWillUpdate != "function" && typeof g.componentWillUpdate != "function" || (typeof g.componentWillUpdate == "function" && g.componentWillUpdate(l, V, O), typeof g.UNSAFE_componentWillUpdate == "function" && g.UNSAFE_componentWillUpdate(
        l,
        V,
        O
      )), typeof g.componentDidUpdate == "function" && (r.flags |= 4), typeof g.getSnapshotBeforeUpdate == "function" && (r.flags |= 1024)) : (typeof g.componentDidUpdate != "function" || b === n.memoizedProps && F === n.memoizedState || (r.flags |= 4), typeof g.getSnapshotBeforeUpdate != "function" || b === n.memoizedProps && F === n.memoizedState || (r.flags |= 1024), r.memoizedProps = l, r.memoizedState = V), g.props = l, g.state = V, g.context = O, l = Q) : (typeof g.componentDidUpdate != "function" || b === n.memoizedProps && F === n.memoizedState || (r.flags |= 4), typeof g.getSnapshotBeforeUpdate != "function" || b === n.memoizedProps && F === n.memoizedState || (r.flags |= 1024), l = !1);
    }
    return g = l, Dc(n, r), l = (r.flags & 128) !== 0, g || l ? (g = r.stateNode, o = l && typeof o.getDerivedStateFromError != "function" ? null : g.render(), r.flags |= 1, n !== null && l ? (r.child = ja(
      r,
      n.child,
      null,
      m
    ), r.child = ja(
      r,
      null,
      o,
      m
    )) : Pt(n, r, o, m), r.memoizedState = g.state, n = r.child) : n = bi(
      n,
      r,
      m
    ), n;
  }
  function dv(n, r, o, l) {
    return Ia(), r.flags |= 256, Pt(n, r, o, l), r.child;
  }
  var qd = {
    dehydrated: null,
    treeContext: null,
    retryLane: 0,
    hydrationErrors: null
  };
  function Hd(n) {
    return { baseLanes: n, cachePool: eg() };
  }
  function Vd(n, r, o) {
    return n = n !== null ? n.childLanes & ~o : 0, r && (n |= pn), n;
  }
  function fv(n, r, o) {
    var l = r.pendingProps, m = !1, g = (r.flags & 128) !== 0, b;
    if ((b = g) || (b = n !== null && n.memoizedState === null ? !1 : (dt.current & 2) !== 0), b && (m = !0, r.flags &= -129), b = (r.flags & 32) !== 0, r.flags &= -33, n === null) {
      if (Le) {
        if (m ? Zi(r) : Wi(), (n = et) ? (n = Ty(
          n,
          _n
        ), n = n !== null && n.data !== "&" ? n : null, n !== null && (r.memoizedState = {
          dehydrated: n,
          treeContext: Vi !== null ? { id: $n, overflow: ei } : null,
          retryLane: 536870912,
          hydrationErrors: null
        }, o = Gp(n), o.return = r, r.child = o, Ot = r, et = null)) : n = null, n === null) throw Ki(r);
        return wf(n) ? r.lanes = 32 : r.lanes = 536870912, null;
      }
      var S = l.children;
      return l = l.fallback, m ? (Wi(), m = r.mode, S = Pc(
        { mode: "hidden", children: S },
        m
      ), l = Na(
        l,
        m,
        o,
        null
      ), S.return = r, l.return = r, S.sibling = l, r.child = S, l = r.child, l.memoizedState = Hd(o), l.childLanes = Vd(
        n,
        b,
        o
      ), r.memoizedState = qd, Gs(null, l)) : (Zi(r), Gd(r, S));
    }
    var O = n.memoizedState;
    if (O !== null && (S = O.dehydrated, S !== null)) {
      if (g)
        r.flags & 256 ? (Zi(r), r.flags &= -257, r = Kd(
          n,
          r,
          o
        )) : r.memoizedState !== null ? (Wi(), r.child = n.child, r.flags |= 128, r = null) : (Wi(), S = l.fallback, m = r.mode, l = Pc(
          { mode: "visible", children: l.children },
          m
        ), S = Na(
          S,
          m,
          o,
          null
        ), S.flags |= 2, l.return = r, S.return = r, l.sibling = S, r.child = l, ja(
          r,
          n.child,
          null,
          o
        ), l = r.child, l.memoizedState = Hd(o), l.childLanes = Vd(
          n,
          b,
          o
        ), r.memoizedState = qd, r = Gs(null, l));
      else if (Zi(r), wf(S)) {
        if (b = S.nextSibling && S.nextSibling.dataset, b) var z = b.dgst;
        b = z, l = Error(i(419)), l.stack = "", l.digest = b, xs({ value: l, source: null, stack: null }), r = Kd(
          n,
          r,
          o
        );
      } else if (gt || Rr(n, r, o, !1), b = (o & n.childLanes) !== 0, gt || b) {
        if (b = Xe, b !== null && (l = Wm(b, o), l !== 0 && l !== O.retryLane))
          throw O.retryLane = l, xa(n, l), en(b, n, l), Fd;
        Rf(S) || jc(), r = Kd(
          n,
          r,
          o
        );
      } else
        Rf(S) ? (r.flags |= 192, r.child = n.child, r = null) : (n = O.treeContext, et = wn(
          S.nextSibling
        ), Ot = r, Le = !0, Gi = null, _n = !1, n !== null && Jp(r, n), r = Gd(
          r,
          l.children
        ), r.flags |= 4096);
      return r;
    }
    return m ? (Wi(), S = l.fallback, m = r.mode, O = n.child, z = O.sibling, l = hi(O, {
      mode: "hidden",
      children: l.children
    }), l.subtreeFlags = O.subtreeFlags & 65011712, z !== null ? S = hi(
      z,
      S
    ) : (S = Na(
      S,
      m,
      o,
      null
    ), S.flags |= 2), S.return = r, l.return = r, l.sibling = S, r.child = l, Gs(null, l), l = r.child, S = n.child.memoizedState, S === null ? S = Hd(o) : (m = S.cachePool, m !== null ? (O = mt._currentValue, m = m.parent !== O ? { parent: O, pool: O } : m) : m = eg(), S = {
      baseLanes: S.baseLanes | o,
      cachePool: m
    }), l.memoizedState = S, l.childLanes = Vd(
      n,
      b,
      o
    ), r.memoizedState = qd, Gs(n.child, l)) : (Zi(r), o = n.child, n = o.sibling, o = hi(o, {
      mode: "visible",
      children: l.children
    }), o.return = r, o.sibling = null, n !== null && (b = r.deletions, b === null ? (r.deletions = [n], r.flags |= 16) : b.push(n)), r.child = o, r.memoizedState = null, o);
  }
  function Gd(n, r) {
    return r = Pc(
      { mode: "visible", children: r },
      n.mode
    ), r.return = n, n.child = r;
  }
  function Pc(n, r) {
    return n = dn(22, n, null, r), n.lanes = 0, n;
  }
  function Kd(n, r, o) {
    return ja(r, n.child, null, o), n = Gd(
      r,
      r.pendingProps.children
    ), n.flags |= 2, r.memoizedState = null, n;
  }
  function hv(n, r, o) {
    n.lanes |= r;
    var l = n.alternate;
    l !== null && (l.lanes |= r), od(n.return, r, o);
  }
  function Yd(n, r, o, l, m, g) {
    var b = n.memoizedState;
    b === null ? n.memoizedState = {
      isBackwards: r,
      rendering: null,
      renderingStartTime: 0,
      last: l,
      tail: o,
      tailMode: m,
      treeForkCount: g
    } : (b.isBackwards = r, b.rendering = null, b.renderingStartTime = 0, b.last = l, b.tail = o, b.tailMode = m, b.treeForkCount = g);
  }
  function mv(n, r, o) {
    var l = r.pendingProps, m = l.revealOrder, g = l.tail;
    l = l.children;
    var b = dt.current, S = (b & 2) !== 0;
    if (S ? (b = b & 1 | 2, r.flags |= 128) : b &= 1, se(dt, b), Pt(n, r, l, o), l = Le ? Ps : 0, !S && n !== null && (n.flags & 128) !== 0)
      e: for (n = r.child; n !== null; ) {
        if (n.tag === 13)
          n.memoizedState !== null && hv(n, o, r);
        else if (n.tag === 19)
          hv(n, o, r);
        else if (n.child !== null) {
          n.child.return = n, n = n.child;
          continue;
        }
        if (n === r) break e;
        for (; n.sibling === null; ) {
          if (n.return === null || n.return === r)
            break e;
          n = n.return;
        }
        n.sibling.return = n.return, n = n.sibling;
      }
    switch (m) {
      case "forwards":
        for (o = r.child, m = null; o !== null; )
          n = o.alternate, n !== null && Tc(n) === null && (m = o), o = o.sibling;
        o = m, o === null ? (m = r.child, r.child = null) : (m = o.sibling, o.sibling = null), Yd(
          r,
          !1,
          m,
          o,
          g,
          l
        );
        break;
      case "backwards":
      case "unstable_legacy-backwards":
        for (o = null, m = r.child, r.child = null; m !== null; ) {
          if (n = m.alternate, n !== null && Tc(n) === null) {
            r.child = m;
            break;
          }
          n = m.sibling, m.sibling = o, o = m, m = n;
        }
        Yd(
          r,
          !0,
          o,
          null,
          g,
          l
        );
        break;
      case "together":
        Yd(
          r,
          !1,
          null,
          null,
          void 0,
          l
        );
        break;
      default:
        r.memoizedState = null;
    }
    return r.child;
  }
  function bi(n, r, o) {
    if (n !== null && (r.dependencies = n.dependencies), ta |= r.lanes, (o & r.childLanes) === 0)
      if (n !== null) {
        if (Rr(
          n,
          r,
          o,
          !1
        ), (o & r.childLanes) === 0)
          return null;
      } else return null;
    if (n !== null && r.child !== n.child)
      throw Error(i(153));
    if (r.child !== null) {
      for (n = r.child, o = hi(n, n.pendingProps), r.child = o, o.return = r; n.sibling !== null; )
        n = n.sibling, o = o.sibling = hi(n, n.pendingProps), o.return = r;
      o.sibling = null;
    }
    return r.child;
  }
  function Jd(n, r) {
    return (n.lanes & r) !== 0 ? !0 : (n = n.dependencies, !!(n !== null && fc(n)));
  }
  function rE(n, r, o) {
    switch (r.tag) {
      case 3:
        Be(r, r.stateNode.containerInfo), Yi(r, mt, n.memoizedState.cache), Ia();
        break;
      case 27:
      case 5:
        jt(r);
        break;
      case 4:
        Be(r, r.stateNode.containerInfo);
        break;
      case 10:
        Yi(
          r,
          r.type,
          r.memoizedProps.value
        );
        break;
      case 31:
        if (r.memoizedState !== null)
          return r.flags |= 128, bd(r), null;
        break;
      case 13:
        var l = r.memoizedState;
        if (l !== null)
          return l.dehydrated !== null ? (Zi(r), r.flags |= 128, null) : (o & r.child.childLanes) !== 0 ? fv(n, r, o) : (Zi(r), n = bi(
            n,
            r,
            o
          ), n !== null ? n.sibling : null);
        Zi(r);
        break;
      case 19:
        var m = (n.flags & 128) !== 0;
        if (l = (o & r.childLanes) !== 0, l || (Rr(
          n,
          r,
          o,
          !1
        ), l = (o & r.childLanes) !== 0), m) {
          if (l)
            return mv(
              n,
              r,
              o
            );
          r.flags |= 128;
        }
        if (m = r.memoizedState, m !== null && (m.rendering = null, m.tail = null, m.lastEffect = null), se(dt, dt.current), l) break;
        return null;
      case 22:
        return r.lanes = 0, sv(
          n,
          r,
          o,
          r.pendingProps
        );
      case 24:
        Yi(r, mt, n.memoizedState.cache);
    }
    return bi(n, r, o);
  }
  function pv(n, r, o) {
    if (n !== null)
      if (n.memoizedProps !== r.pendingProps)
        gt = !0;
      else {
        if (!Jd(n, o) && (r.flags & 128) === 0)
          return gt = !1, rE(
            n,
            r,
            o
          );
        gt = (n.flags & 131072) !== 0;
      }
    else
      gt = !1, Le && (r.flags & 1048576) !== 0 && Yp(r, Ps, r.index);
    switch (r.lanes = 0, r.tag) {
      case 16:
        e: {
          var l = r.pendingProps;
          if (n = za(r.elementType), r.type = n, typeof n == "function")
            $u(n) ? (l = Ha(n, l), r.tag = 1, r = uv(
              null,
              r,
              n,
              l,
              o
            )) : (r.tag = 0, r = jd(
              null,
              r,
              n,
              l,
              o
            ));
          else {
            if (n != null) {
              var m = n.$$typeof;
              if (m === C) {
                r.tag = 11, r = iv(
                  null,
                  r,
                  n,
                  l,
                  o
                );
                break e;
              } else if (m === N) {
                r.tag = 14, r = av(
                  null,
                  r,
                  n,
                  l,
                  o
                );
                break e;
              }
            }
            throw r = fe(n) || n, Error(i(306, r, ""));
          }
        }
        return r;
      case 0:
        return jd(
          n,
          r,
          r.type,
          r.pendingProps,
          o
        );
      case 1:
        return l = r.type, m = Ha(
          l,
          r.pendingProps
        ), uv(
          n,
          r,
          l,
          m,
          o
        );
      case 3:
        e: {
          if (Be(
            r,
            r.stateNode.containerInfo
          ), n === null) throw Error(i(387));
          l = r.pendingProps;
          var g = r.memoizedState;
          m = g.element, md(n, r), Fs(r, l, null, o);
          var b = r.memoizedState;
          if (l = b.cache, Yi(r, mt, l), l !== g.cache && cd(
            r,
            [mt],
            o,
            !0
          ), zs(), l = b.element, g.isDehydrated)
            if (g = {
              element: l,
              isDehydrated: !1,
              cache: b.cache
            }, r.updateQueue.baseState = g, r.memoizedState = g, r.flags & 256) {
              r = dv(
                n,
                r,
                l,
                o
              );
              break e;
            } else if (l !== m) {
              m = kn(
                Error(i(424)),
                r
              ), xs(m), r = dv(
                n,
                r,
                l,
                o
              );
              break e;
            } else {
              switch (n = r.stateNode.containerInfo, n.nodeType) {
                case 9:
                  n = n.body;
                  break;
                default:
                  n = n.nodeName === "HTML" ? n.ownerDocument.body : n;
              }
              for (et = wn(n.firstChild), Ot = r, Le = !0, Gi = null, _n = !0, o = sg(
                r,
                null,
                l,
                o
              ), r.child = o; o; )
                o.flags = o.flags & -3 | 4096, o = o.sibling;
            }
          else {
            if (Ia(), l === m) {
              r = bi(
                n,
                r,
                o
              );
              break e;
            }
            Pt(n, r, l, o);
          }
          r = r.child;
        }
        return r;
      case 26:
        return Dc(n, r), n === null ? (o = Ry(
          r.type,
          null,
          r.pendingProps,
          null
        )) ? r.memoizedState = o : Le || (o = r.type, n = r.pendingProps, l = Jc(
          oe.current
        ).createElement(o), l[At] = r, l[Jt] = n, xt(l, o, n), Ct(l), r.stateNode = l) : r.memoizedState = Ry(
          r.type,
          n.memoizedProps,
          r.pendingProps,
          n.memoizedState
        ), null;
      case 27:
        return jt(r), n === null && Le && (l = r.stateNode = Cy(
          r.type,
          r.pendingProps,
          oe.current
        ), Ot = r, _n = !0, m = et, sa(r.type) ? (Mf = m, et = wn(l.firstChild)) : et = m), Pt(
          n,
          r,
          r.pendingProps.children,
          o
        ), Dc(n, r), n === null && (r.flags |= 4194304), r.child;
      case 5:
        return n === null && Le && ((m = l = et) && (l = IE(
          l,
          r.type,
          r.pendingProps,
          _n
        ), l !== null ? (r.stateNode = l, Ot = r, et = wn(l.firstChild), _n = !1, m = !0) : m = !1), m || Ki(r)), jt(r), m = r.type, g = r.pendingProps, b = n !== null ? n.memoizedProps : null, l = g.children, Cf(m, g) ? l = null : b !== null && Cf(m, b) && (r.flags |= 32), r.memoizedState !== null && (m = Sd(
          n,
          r,
          XC,
          null,
          null,
          o
        ), so._currentValue = m), Dc(n, r), Pt(n, r, l, o), r.child;
      case 6:
        return n === null && Le && ((n = o = et) && (o = LE(
          o,
          r.pendingProps,
          _n
        ), o !== null ? (r.stateNode = o, Ot = r, et = null, n = !0) : n = !1), n || Ki(r)), null;
      case 13:
        return fv(n, r, o);
      case 4:
        return Be(
          r,
          r.stateNode.containerInfo
        ), l = r.pendingProps, n === null ? r.child = ja(
          r,
          null,
          l,
          o
        ) : Pt(n, r, l, o), r.child;
      case 11:
        return iv(
          n,
          r,
          r.type,
          r.pendingProps,
          o
        );
      case 7:
        return Pt(
          n,
          r,
          r.pendingProps,
          o
        ), r.child;
      case 8:
        return Pt(
          n,
          r,
          r.pendingProps.children,
          o
        ), r.child;
      case 12:
        return Pt(
          n,
          r,
          r.pendingProps.children,
          o
        ), r.child;
      case 10:
        return l = r.pendingProps, Yi(r, r.type, l.value), Pt(n, r, l.children, o), r.child;
      case 9:
        return m = r.type._context, l = r.pendingProps.children, Ua(r), m = Dt(m), l = l(m), r.flags |= 1, Pt(n, r, l, o), r.child;
      case 14:
        return av(
          n,
          r,
          r.type,
          r.pendingProps,
          o
        );
      case 15:
        return rv(
          n,
          r,
          r.type,
          r.pendingProps,
          o
        );
      case 19:
        return mv(n, r, o);
      case 31:
        return aE(n, r, o);
      case 22:
        return sv(
          n,
          r,
          o,
          r.pendingProps
        );
      case 24:
        return Ua(r), l = Dt(mt), n === null ? (m = dd(), m === null && (m = Xe, g = ld(), m.pooledCache = g, g.refCount++, g !== null && (m.pooledCacheLanes |= o), m = g), r.memoizedState = { parent: l, cache: m }, hd(r), Yi(r, mt, m)) : ((n.lanes & o) !== 0 && (md(n, r), Fs(r, null, null, o), zs()), m = n.memoizedState, g = r.memoizedState, m.parent !== l ? (m = { parent: l, cache: l }, r.memoizedState = m, r.lanes === 0 && (r.memoizedState = r.updateQueue.baseState = m), Yi(r, mt, l)) : (l = g.cache, Yi(r, mt, l), l !== m.cache && cd(
          r,
          [mt],
          o,
          !0
        ))), Pt(
          n,
          r,
          r.pendingProps.children,
          o
        ), r.child;
      case 29:
        throw r.pendingProps;
    }
    throw Error(i(156, r.tag));
  }
  function Ti(n) {
    n.flags |= 4;
  }
  function Qd(n, r, o, l, m) {
    if ((r = (n.mode & 32) !== 0) && (r = !1), r) {
      if (n.flags |= 16777216, (m & 335544128) === m)
        if (n.stateNode.complete) n.flags |= 8192;
        else if (qv()) n.flags |= 8192;
        else
          throw Fa = gc, fd;
    } else n.flags &= -16777217;
  }
  function gv(n, r) {
    if (r.type !== "stylesheet" || (r.state.loading & 4) !== 0)
      n.flags &= -16777217;
    else if (n.flags |= 16777216, !Dy(r))
      if (qv()) n.flags |= 8192;
      else
        throw Fa = gc, fd;
  }
  function xc(n, r) {
    r !== null && (n.flags |= 4), n.flags & 16384 && (r = n.tag !== 22 ? Qm() : 536870912, n.lanes |= r, Br |= r);
  }
  function Ks(n, r) {
    if (!Le)
      switch (n.tailMode) {
        case "hidden":
          r = n.tail;
          for (var o = null; r !== null; )
            r.alternate !== null && (o = r), r = r.sibling;
          o === null ? n.tail = null : o.sibling = null;
          break;
        case "collapsed":
          o = n.tail;
          for (var l = null; o !== null; )
            o.alternate !== null && (l = o), o = o.sibling;
          l === null ? r || n.tail === null ? n.tail = null : n.tail.sibling = null : l.sibling = null;
      }
  }
  function tt(n) {
    var r = n.alternate !== null && n.alternate.child === n.child, o = 0, l = 0;
    if (r)
      for (var m = n.child; m !== null; )
        o |= m.lanes | m.childLanes, l |= m.subtreeFlags & 65011712, l |= m.flags & 65011712, m.return = n, m = m.sibling;
    else
      for (m = n.child; m !== null; )
        o |= m.lanes | m.childLanes, l |= m.subtreeFlags, l |= m.flags, m.return = n, m = m.sibling;
    return n.subtreeFlags |= l, n.childLanes = o, r;
  }
  function sE(n, r, o) {
    var l = r.pendingProps;
    switch (id(r), r.tag) {
      case 16:
      case 15:
      case 0:
      case 11:
      case 7:
      case 8:
      case 12:
      case 9:
      case 14:
        return tt(r), null;
      case 1:
        return tt(r), null;
      case 3:
        return o = r.stateNode, l = null, n !== null && (l = n.memoizedState.cache), r.memoizedState.cache !== l && (r.flags |= 2048), gi(mt), je(), o.pendingContext && (o.context = o.pendingContext, o.pendingContext = null), (n === null || n.child === null) && (_r(r) ? Ti(r) : n === null || n.memoizedState.isDehydrated && (r.flags & 256) === 0 || (r.flags |= 1024, rd())), tt(r), null;
      case 26:
        var m = r.type, g = r.memoizedState;
        return n === null ? (Ti(r), g !== null ? (tt(r), gv(r, g)) : (tt(r), Qd(
          r,
          m,
          null,
          l,
          o
        ))) : g ? g !== n.memoizedState ? (Ti(r), tt(r), gv(r, g)) : (tt(r), r.flags &= -16777217) : (n = n.memoizedProps, n !== l && Ti(r), tt(r), Qd(
          r,
          m,
          n,
          l,
          o
        )), null;
      case 27:
        if (li(r), o = oe.current, m = r.type, n !== null && r.stateNode != null)
          n.memoizedProps !== l && Ti(r);
        else {
          if (!l) {
            if (r.stateNode === null)
              throw Error(i(166));
            return tt(r), null;
          }
          n = ce.current, _r(r) ? Qp(r) : (n = Cy(m, l, o), r.stateNode = n, Ti(r));
        }
        return tt(r), null;
      case 5:
        if (li(r), m = r.type, n !== null && r.stateNode != null)
          n.memoizedProps !== l && Ti(r);
        else {
          if (!l) {
            if (r.stateNode === null)
              throw Error(i(166));
            return tt(r), null;
          }
          if (g = ce.current, _r(r))
            Qp(r);
          else {
            var b = Jc(
              oe.current
            );
            switch (g) {
              case 1:
                g = b.createElementNS(
                  "http://www.w3.org/2000/svg",
                  m
                );
                break;
              case 2:
                g = b.createElementNS(
                  "http://www.w3.org/1998/Math/MathML",
                  m
                );
                break;
              default:
                switch (m) {
                  case "svg":
                    g = b.createElementNS(
                      "http://www.w3.org/2000/svg",
                      m
                    );
                    break;
                  case "math":
                    g = b.createElementNS(
                      "http://www.w3.org/1998/Math/MathML",
                      m
                    );
                    break;
                  case "script":
                    g = b.createElement("div"), g.innerHTML = "<script><\/script>", g = g.removeChild(
                      g.firstChild
                    );
                    break;
                  case "select":
                    g = typeof l.is == "string" ? b.createElement("select", {
                      is: l.is
                    }) : b.createElement("select"), l.multiple ? g.multiple = !0 : l.size && (g.size = l.size);
                    break;
                  default:
                    g = typeof l.is == "string" ? b.createElement(m, { is: l.is }) : b.createElement(m);
                }
            }
            g[At] = r, g[Jt] = l;
            e: for (b = r.child; b !== null; ) {
              if (b.tag === 5 || b.tag === 6)
                g.appendChild(b.stateNode);
              else if (b.tag !== 4 && b.tag !== 27 && b.child !== null) {
                b.child.return = b, b = b.child;
                continue;
              }
              if (b === r) break e;
              for (; b.sibling === null; ) {
                if (b.return === null || b.return === r)
                  break e;
                b = b.return;
              }
              b.sibling.return = b.return, b = b.sibling;
            }
            r.stateNode = g;
            e: switch (xt(g, m, l), m) {
              case "button":
              case "input":
              case "select":
              case "textarea":
                l = !!l.autoFocus;
                break e;
              case "img":
                l = !0;
                break e;
              default:
                l = !1;
            }
            l && Ti(r);
          }
        }
        return tt(r), Qd(
          r,
          r.type,
          n === null ? null : n.memoizedProps,
          r.pendingProps,
          o
        ), null;
      case 6:
        if (n && r.stateNode != null)
          n.memoizedProps !== l && Ti(r);
        else {
          if (typeof l != "string" && r.stateNode === null)
            throw Error(i(166));
          if (n = oe.current, _r(r)) {
            if (n = r.stateNode, o = r.memoizedProps, l = null, m = Ot, m !== null)
              switch (m.tag) {
                case 27:
                case 5:
                  l = m.memoizedProps;
              }
            n[At] = r, n = !!(n.nodeValue === o || l !== null && l.suppressHydrationWarning === !0 || fy(n.nodeValue, o)), n || Ki(r, !0);
          } else
            n = Jc(n).createTextNode(
              l
            ), n[At] = r, r.stateNode = n;
        }
        return tt(r), null;
      case 31:
        if (o = r.memoizedState, n === null || n.memoizedState !== null) {
          if (l = _r(r), o !== null) {
            if (n === null) {
              if (!l) throw Error(i(318));
              if (n = r.memoizedState, n = n !== null ? n.dehydrated : null, !n) throw Error(i(557));
              n[At] = r;
            } else
              Ia(), (r.flags & 128) === 0 && (r.memoizedState = null), r.flags |= 4;
            tt(r), n = !1;
          } else
            o = rd(), n !== null && n.memoizedState !== null && (n.memoizedState.hydrationErrors = o), n = !0;
          if (!n)
            return r.flags & 256 ? (hn(r), r) : (hn(r), null);
          if ((r.flags & 128) !== 0)
            throw Error(i(558));
        }
        return tt(r), null;
      case 13:
        if (l = r.memoizedState, n === null || n.memoizedState !== null && n.memoizedState.dehydrated !== null) {
          if (m = _r(r), l !== null && l.dehydrated !== null) {
            if (n === null) {
              if (!m) throw Error(i(318));
              if (m = r.memoizedState, m = m !== null ? m.dehydrated : null, !m) throw Error(i(317));
              m[At] = r;
            } else
              Ia(), (r.flags & 128) === 0 && (r.memoizedState = null), r.flags |= 4;
            tt(r), m = !1;
          } else
            m = rd(), n !== null && n.memoizedState !== null && (n.memoizedState.hydrationErrors = m), m = !0;
          if (!m)
            return r.flags & 256 ? (hn(r), r) : (hn(r), null);
        }
        return hn(r), (r.flags & 128) !== 0 ? (r.lanes = o, r) : (o = l !== null, n = n !== null && n.memoizedState !== null, o && (l = r.child, m = null, l.alternate !== null && l.alternate.memoizedState !== null && l.alternate.memoizedState.cachePool !== null && (m = l.alternate.memoizedState.cachePool.pool), g = null, l.memoizedState !== null && l.memoizedState.cachePool !== null && (g = l.memoizedState.cachePool.pool), g !== m && (l.flags |= 2048)), o !== n && o && (r.child.flags |= 8192), xc(r, r.updateQueue), tt(r), null);
      case 4:
        return je(), n === null && yf(r.stateNode.containerInfo), tt(r), null;
      case 10:
        return gi(r.type), tt(r), null;
      case 19:
        if (K(dt), l = r.memoizedState, l === null) return tt(r), null;
        if (m = (r.flags & 128) !== 0, g = l.rendering, g === null)
          if (m) Ks(l, !1);
          else {
            if (ct !== 0 || n !== null && (n.flags & 128) !== 0)
              for (n = r.child; n !== null; ) {
                if (g = Tc(n), g !== null) {
                  for (r.flags |= 128, Ks(l, !1), n = g.updateQueue, r.updateQueue = n, xc(r, n), r.subtreeFlags = 0, n = o, o = r.child; o !== null; )
                    Vp(o, n), o = o.sibling;
                  return se(
                    dt,
                    dt.current & 1 | 2
                  ), Le && mi(r, l.treeForkCount), r.child;
                }
                n = n.sibling;
              }
            l.tail !== null && on() > Bc && (r.flags |= 128, m = !0, Ks(l, !1), r.lanes = 4194304);
          }
        else {
          if (!m)
            if (n = Tc(g), n !== null) {
              if (r.flags |= 128, m = !0, n = n.updateQueue, r.updateQueue = n, xc(r, n), Ks(l, !0), l.tail === null && l.tailMode === "hidden" && !g.alternate && !Le)
                return tt(r), null;
            } else
              2 * on() - l.renderingStartTime > Bc && o !== 536870912 && (r.flags |= 128, m = !0, Ks(l, !1), r.lanes = 4194304);
          l.isBackwards ? (g.sibling = r.child, r.child = g) : (n = l.last, n !== null ? n.sibling = g : r.child = g, l.last = g);
        }
        return l.tail !== null ? (n = l.tail, l.rendering = n, l.tail = n.sibling, l.renderingStartTime = on(), n.sibling = null, o = dt.current, se(
          dt,
          m ? o & 1 | 2 : o & 1
        ), Le && mi(r, l.treeForkCount), n) : (tt(r), null);
      case 22:
      case 23:
        return hn(r), yd(), l = r.memoizedState !== null, n !== null ? n.memoizedState !== null !== l && (r.flags |= 8192) : l && (r.flags |= 8192), l ? (o & 536870912) !== 0 && (r.flags & 128) === 0 && (tt(r), r.subtreeFlags & 6 && (r.flags |= 8192)) : tt(r), o = r.updateQueue, o !== null && xc(r, o.retryQueue), o = null, n !== null && n.memoizedState !== null && n.memoizedState.cachePool !== null && (o = n.memoizedState.cachePool.pool), l = null, r.memoizedState !== null && r.memoizedState.cachePool !== null && (l = r.memoizedState.cachePool.pool), l !== o && (r.flags |= 2048), n !== null && K(Ba), null;
      case 24:
        return o = null, n !== null && (o = n.memoizedState.cache), r.memoizedState.cache !== o && (r.flags |= 2048), gi(mt), tt(r), null;
      case 25:
        return null;
      case 30:
        return null;
    }
    throw Error(i(156, r.tag));
  }
  function oE(n, r) {
    switch (id(r), r.tag) {
      case 1:
        return n = r.flags, n & 65536 ? (r.flags = n & -65537 | 128, r) : null;
      case 3:
        return gi(mt), je(), n = r.flags, (n & 65536) !== 0 && (n & 128) === 0 ? (r.flags = n & -65537 | 128, r) : null;
      case 26:
      case 27:
      case 5:
        return li(r), null;
      case 31:
        if (r.memoizedState !== null) {
          if (hn(r), r.alternate === null)
            throw Error(i(340));
          Ia();
        }
        return n = r.flags, n & 65536 ? (r.flags = n & -65537 | 128, r) : null;
      case 13:
        if (hn(r), n = r.memoizedState, n !== null && n.dehydrated !== null) {
          if (r.alternate === null)
            throw Error(i(340));
          Ia();
        }
        return n = r.flags, n & 65536 ? (r.flags = n & -65537 | 128, r) : null;
      case 19:
        return K(dt), null;
      case 4:
        return je(), null;
      case 10:
        return gi(r.type), null;
      case 22:
      case 23:
        return hn(r), yd(), n !== null && K(Ba), n = r.flags, n & 65536 ? (r.flags = n & -65537 | 128, r) : null;
      case 24:
        return gi(mt), null;
      case 25:
        return null;
      default:
        return null;
    }
  }
  function vv(n, r) {
    switch (id(r), r.tag) {
      case 3:
        gi(mt), je();
        break;
      case 26:
      case 27:
      case 5:
        li(r);
        break;
      case 4:
        je();
        break;
      case 31:
        r.memoizedState !== null && hn(r);
        break;
      case 13:
        hn(r);
        break;
      case 19:
        K(dt);
        break;
      case 10:
        gi(r.type);
        break;
      case 22:
      case 23:
        hn(r), yd(), n !== null && K(Ba);
        break;
      case 24:
        gi(mt);
    }
  }
  function Ys(n, r) {
    try {
      var o = r.updateQueue, l = o !== null ? o.lastEffect : null;
      if (l !== null) {
        var m = l.next;
        o = m;
        do {
          if ((o.tag & n) === n) {
            l = void 0;
            var g = o.create, b = o.inst;
            l = g(), b.destroy = l;
          }
          o = o.next;
        } while (o !== m);
      }
    } catch (S) {
      Ve(r, r.return, S);
    }
  }
  function $i(n, r, o) {
    try {
      var l = r.updateQueue, m = l !== null ? l.lastEffect : null;
      if (m !== null) {
        var g = m.next;
        l = g;
        do {
          if ((l.tag & n) === n) {
            var b = l.inst, S = b.destroy;
            if (S !== void 0) {
              b.destroy = void 0, m = r;
              var O = o, z = S;
              try {
                z();
              } catch (Q) {
                Ve(
                  m,
                  O,
                  Q
                );
              }
            }
          }
          l = l.next;
        } while (l !== g);
      }
    } catch (Q) {
      Ve(r, r.return, Q);
    }
  }
  function yv(n) {
    var r = n.updateQueue;
    if (r !== null) {
      var o = n.stateNode;
      try {
        cg(r, o);
      } catch (l) {
        Ve(n, n.return, l);
      }
    }
  }
  function bv(n, r, o) {
    o.props = Ha(
      n.type,
      n.memoizedProps
    ), o.state = n.memoizedState;
    try {
      o.componentWillUnmount();
    } catch (l) {
      Ve(n, r, l);
    }
  }
  function Js(n, r) {
    try {
      var o = n.ref;
      if (o !== null) {
        switch (n.tag) {
          case 26:
          case 27:
          case 5:
            var l = n.stateNode;
            break;
          case 30:
            l = n.stateNode;
            break;
          default:
            l = n.stateNode;
        }
        typeof o == "function" ? n.refCleanup = o(l) : o.current = l;
      }
    } catch (m) {
      Ve(n, r, m);
    }
  }
  function ti(n, r) {
    var o = n.ref, l = n.refCleanup;
    if (o !== null)
      if (typeof l == "function")
        try {
          l();
        } catch (m) {
          Ve(n, r, m);
        } finally {
          n.refCleanup = null, n = n.alternate, n != null && (n.refCleanup = null);
        }
      else if (typeof o == "function")
        try {
          o(null);
        } catch (m) {
          Ve(n, r, m);
        }
      else o.current = null;
  }
  function Tv(n) {
    var r = n.type, o = n.memoizedProps, l = n.stateNode;
    try {
      e: switch (r) {
        case "button":
        case "input":
        case "select":
        case "textarea":
          o.autoFocus && l.focus();
          break e;
        case "img":
          o.src ? l.src = o.src : o.srcSet && (l.srcset = o.srcSet);
      }
    } catch (m) {
      Ve(n, n.return, m);
    }
  }
  function Xd(n, r, o) {
    try {
      var l = n.stateNode;
      AE(l, n.type, o, r), l[Jt] = r;
    } catch (m) {
      Ve(n, n.return, m);
    }
  }
  function Sv(n) {
    return n.tag === 5 || n.tag === 3 || n.tag === 26 || n.tag === 27 && sa(n.type) || n.tag === 4;
  }
  function Zd(n) {
    e: for (; ; ) {
      for (; n.sibling === null; ) {
        if (n.return === null || Sv(n.return)) return null;
        n = n.return;
      }
      for (n.sibling.return = n.return, n = n.sibling; n.tag !== 5 && n.tag !== 6 && n.tag !== 18; ) {
        if (n.tag === 27 && sa(n.type) || n.flags & 2 || n.child === null || n.tag === 4) continue e;
        n.child.return = n, n = n.child;
      }
      if (!(n.flags & 2)) return n.stateNode;
    }
  }
  function Wd(n, r, o) {
    var l = n.tag;
    if (l === 5 || l === 6)
      n = n.stateNode, r ? (o.nodeType === 9 ? o.body : o.nodeName === "HTML" ? o.ownerDocument.body : o).insertBefore(n, r) : (r = o.nodeType === 9 ? o.body : o.nodeName === "HTML" ? o.ownerDocument.body : o, r.appendChild(n), o = o._reactRootContainer, o != null || r.onclick !== null || (r.onclick = di));
    else if (l !== 4 && (l === 27 && sa(n.type) && (o = n.stateNode, r = null), n = n.child, n !== null))
      for (Wd(n, r, o), n = n.sibling; n !== null; )
        Wd(n, r, o), n = n.sibling;
  }
  function Nc(n, r, o) {
    var l = n.tag;
    if (l === 5 || l === 6)
      n = n.stateNode, r ? o.insertBefore(n, r) : o.appendChild(n);
    else if (l !== 4 && (l === 27 && sa(n.type) && (o = n.stateNode), n = n.child, n !== null))
      for (Nc(n, r, o), n = n.sibling; n !== null; )
        Nc(n, r, o), n = n.sibling;
  }
  function kv(n) {
    var r = n.stateNode, o = n.memoizedProps;
    try {
      for (var l = n.type, m = r.attributes; m.length; )
        r.removeAttributeNode(m[0]);
      xt(r, l, o), r[At] = n, r[Jt] = o;
    } catch (g) {
      Ve(n, n.return, g);
    }
  }
  var Si = !1, vt = !1, $d = !1, Cv = typeof WeakSet == "function" ? WeakSet : Set, Et = null;
  function cE(n, r) {
    if (n = n.containerInfo, Sf = tl, n = Ip(n), Ku(n)) {
      if ("selectionStart" in n)
        var o = {
          start: n.selectionStart,
          end: n.selectionEnd
        };
      else
        e: {
          o = (o = n.ownerDocument) && o.defaultView || window;
          var l = o.getSelection && o.getSelection();
          if (l && l.rangeCount !== 0) {
            o = l.anchorNode;
            var m = l.anchorOffset, g = l.focusNode;
            l = l.focusOffset;
            try {
              o.nodeType, g.nodeType;
            } catch {
              o = null;
              break e;
            }
            var b = 0, S = -1, O = -1, z = 0, Q = 0, $ = n, F = null;
            t: for (; ; ) {
              for (var V; $ !== o || m !== 0 && $.nodeType !== 3 || (S = b + m), $ !== g || l !== 0 && $.nodeType !== 3 || (O = b + l), $.nodeType === 3 && (b += $.nodeValue.length), (V = $.firstChild) !== null; )
                F = $, $ = V;
              for (; ; ) {
                if ($ === n) break t;
                if (F === o && ++z === m && (S = b), F === g && ++Q === l && (O = b), (V = $.nextSibling) !== null) break;
                $ = F, F = $.parentNode;
              }
              $ = V;
            }
            o = S === -1 || O === -1 ? null : { start: S, end: O };
          } else o = null;
        }
      o = o || { start: 0, end: 0 };
    } else o = null;
    for (kf = { focusedElem: n, selectionRange: o }, tl = !1, Et = r; Et !== null; )
      if (r = Et, n = r.child, (r.subtreeFlags & 1028) !== 0 && n !== null)
        n.return = r, Et = n;
      else
        for (; Et !== null; ) {
          switch (r = Et, g = r.alternate, n = r.flags, r.tag) {
            case 0:
              if ((n & 4) !== 0 && (n = r.updateQueue, n = n !== null ? n.events : null, n !== null))
                for (o = 0; o < n.length; o++)
                  m = n[o], m.ref.impl = m.nextImpl;
              break;
            case 11:
            case 15:
              break;
            case 1:
              if ((n & 1024) !== 0 && g !== null) {
                n = void 0, o = r, m = g.memoizedProps, g = g.memoizedState, l = o.stateNode;
                try {
                  var he = Ha(
                    o.type,
                    m
                  );
                  n = l.getSnapshotBeforeUpdate(
                    he,
                    g
                  ), l.__reactInternalSnapshotBeforeUpdate = n;
                } catch (be) {
                  Ve(
                    o,
                    o.return,
                    be
                  );
                }
              }
              break;
            case 3:
              if ((n & 1024) !== 0) {
                if (n = r.stateNode.containerInfo, o = n.nodeType, o === 9)
                  _f(n);
                else if (o === 1)
                  switch (n.nodeName) {
                    case "HEAD":
                    case "HTML":
                    case "BODY":
                      _f(n);
                      break;
                    default:
                      n.textContent = "";
                  }
              }
              break;
            case 5:
            case 26:
            case 27:
            case 6:
            case 4:
            case 17:
              break;
            default:
              if ((n & 1024) !== 0) throw Error(i(163));
          }
          if (n = r.sibling, n !== null) {
            n.return = r.return, Et = n;
            break;
          }
          Et = r.return;
        }
  }
  function Ev(n, r, o) {
    var l = o.flags;
    switch (o.tag) {
      case 0:
      case 11:
      case 15:
        Ci(n, o), l & 4 && Ys(5, o);
        break;
      case 1:
        if (Ci(n, o), l & 4)
          if (n = o.stateNode, r === null)
            try {
              n.componentDidMount();
            } catch (b) {
              Ve(o, o.return, b);
            }
          else {
            var m = Ha(
              o.type,
              r.memoizedProps
            );
            r = r.memoizedState;
            try {
              n.componentDidUpdate(
                m,
                r,
                n.__reactInternalSnapshotBeforeUpdate
              );
            } catch (b) {
              Ve(
                o,
                o.return,
                b
              );
            }
          }
        l & 64 && yv(o), l & 512 && Js(o, o.return);
        break;
      case 3:
        if (Ci(n, o), l & 64 && (n = o.updateQueue, n !== null)) {
          if (r = null, o.child !== null)
            switch (o.child.tag) {
              case 27:
              case 5:
                r = o.child.stateNode;
                break;
              case 1:
                r = o.child.stateNode;
            }
          try {
            cg(n, r);
          } catch (b) {
            Ve(o, o.return, b);
          }
        }
        break;
      case 27:
        r === null && l & 4 && kv(o);
      case 26:
      case 5:
        Ci(n, o), r === null && l & 4 && Tv(o), l & 512 && Js(o, o.return);
        break;
      case 12:
        Ci(n, o);
        break;
      case 31:
        Ci(n, o), l & 4 && wv(n, o);
        break;
      case 13:
        Ci(n, o), l & 4 && Mv(n, o), l & 64 && (n = o.memoizedState, n !== null && (n = n.dehydrated, n !== null && (o = vE.bind(
          null,
          o
        ), UE(n, o))));
        break;
      case 22:
        if (l = o.memoizedState !== null || Si, !l) {
          r = r !== null && r.memoizedState !== null || vt, m = Si;
          var g = vt;
          Si = l, (vt = r) && !g ? Ei(
            n,
            o,
            (o.subtreeFlags & 8772) !== 0
          ) : Ci(n, o), Si = m, vt = g;
        }
        break;
      case 30:
        break;
      default:
        Ci(n, o);
    }
  }
  function _v(n) {
    var r = n.alternate;
    r !== null && (n.alternate = null, _v(r)), n.child = null, n.deletions = null, n.sibling = null, n.tag === 5 && (r = n.stateNode, r !== null && Au(r)), n.stateNode = null, n.return = null, n.dependencies = null, n.memoizedProps = null, n.memoizedState = null, n.pendingProps = null, n.stateNode = null, n.updateQueue = null;
  }
  var nt = null, Xt = !1;
  function ki(n, r, o) {
    for (o = o.child; o !== null; )
      Rv(n, r, o), o = o.sibling;
  }
  function Rv(n, r, o) {
    if (cn && typeof cn.onCommitFiberUnmount == "function")
      try {
        cn.onCommitFiberUnmount(ys, o);
      } catch {
      }
    switch (o.tag) {
      case 26:
        vt || ti(o, r), ki(
          n,
          r,
          o
        ), o.memoizedState ? o.memoizedState.count-- : o.stateNode && (o = o.stateNode, o.parentNode.removeChild(o));
        break;
      case 27:
        vt || ti(o, r);
        var l = nt, m = Xt;
        sa(o.type) && (nt = o.stateNode, Xt = !1), ki(
          n,
          r,
          o
        ), io(o.stateNode), nt = l, Xt = m;
        break;
      case 5:
        vt || ti(o, r);
      case 6:
        if (l = nt, m = Xt, nt = null, ki(
          n,
          r,
          o
        ), nt = l, Xt = m, nt !== null)
          if (Xt)
            try {
              (nt.nodeType === 9 ? nt.body : nt.nodeName === "HTML" ? nt.ownerDocument.body : nt).removeChild(o.stateNode);
            } catch (g) {
              Ve(
                o,
                r,
                g
              );
            }
          else
            try {
              nt.removeChild(o.stateNode);
            } catch (g) {
              Ve(
                o,
                r,
                g
              );
            }
        break;
      case 18:
        nt !== null && (Xt ? (n = nt, yy(
          n.nodeType === 9 ? n.body : n.nodeName === "HTML" ? n.ownerDocument.body : n,
          o.stateNode
        ), Kr(n)) : yy(nt, o.stateNode));
        break;
      case 4:
        l = nt, m = Xt, nt = o.stateNode.containerInfo, Xt = !0, ki(
          n,
          r,
          o
        ), nt = l, Xt = m;
        break;
      case 0:
      case 11:
      case 14:
      case 15:
        $i(2, o, r), vt || $i(4, o, r), ki(
          n,
          r,
          o
        );
        break;
      case 1:
        vt || (ti(o, r), l = o.stateNode, typeof l.componentWillUnmount == "function" && bv(
          o,
          r,
          l
        )), ki(
          n,
          r,
          o
        );
        break;
      case 21:
        ki(
          n,
          r,
          o
        );
        break;
      case 22:
        vt = (l = vt) || o.memoizedState !== null, ki(
          n,
          r,
          o
        ), vt = l;
        break;
      default:
        ki(
          n,
          r,
          o
        );
    }
  }
  function wv(n, r) {
    if (r.memoizedState === null && (n = r.alternate, n !== null && (n = n.memoizedState, n !== null))) {
      n = n.dehydrated;
      try {
        Kr(n);
      } catch (o) {
        Ve(r, r.return, o);
      }
    }
  }
  function Mv(n, r) {
    if (r.memoizedState === null && (n = r.alternate, n !== null && (n = n.memoizedState, n !== null && (n = n.dehydrated, n !== null))))
      try {
        Kr(n);
      } catch (o) {
        Ve(r, r.return, o);
      }
  }
  function lE(n) {
    switch (n.tag) {
      case 31:
      case 13:
      case 19:
        var r = n.stateNode;
        return r === null && (r = n.stateNode = new Cv()), r;
      case 22:
        return n = n.stateNode, r = n._retryCache, r === null && (r = n._retryCache = new Cv()), r;
      default:
        throw Error(i(435, n.tag));
    }
  }
  function Ic(n, r) {
    var o = lE(n);
    r.forEach(function(l) {
      if (!o.has(l)) {
        o.add(l);
        var m = yE.bind(null, n, l);
        l.then(m, m);
      }
    });
  }
  function Zt(n, r) {
    var o = r.deletions;
    if (o !== null)
      for (var l = 0; l < o.length; l++) {
        var m = o[l], g = n, b = r, S = b;
        e: for (; S !== null; ) {
          switch (S.tag) {
            case 27:
              if (sa(S.type)) {
                nt = S.stateNode, Xt = !1;
                break e;
              }
              break;
            case 5:
              nt = S.stateNode, Xt = !1;
              break e;
            case 3:
            case 4:
              nt = S.stateNode.containerInfo, Xt = !0;
              break e;
          }
          S = S.return;
        }
        if (nt === null) throw Error(i(160));
        Rv(g, b, m), nt = null, Xt = !1, g = m.alternate, g !== null && (g.return = null), m.return = null;
      }
    if (r.subtreeFlags & 13886)
      for (r = r.child; r !== null; )
        Av(r, n), r = r.sibling;
  }
  var Ln = null;
  function Av(n, r) {
    var o = n.alternate, l = n.flags;
    switch (n.tag) {
      case 0:
      case 11:
      case 14:
      case 15:
        Zt(r, n), Wt(n), l & 4 && ($i(3, n, n.return), Ys(3, n), $i(5, n, n.return));
        break;
      case 1:
        Zt(r, n), Wt(n), l & 512 && (vt || o === null || ti(o, o.return)), l & 64 && Si && (n = n.updateQueue, n !== null && (l = n.callbacks, l !== null && (o = n.shared.hiddenCallbacks, n.shared.hiddenCallbacks = o === null ? l : o.concat(l))));
        break;
      case 26:
        var m = Ln;
        if (Zt(r, n), Wt(n), l & 512 && (vt || o === null || ti(o, o.return)), l & 4) {
          var g = o !== null ? o.memoizedState : null;
          if (l = n.memoizedState, o === null)
            if (l === null)
              if (n.stateNode === null) {
                e: {
                  l = n.type, o = n.memoizedProps, m = m.ownerDocument || m;
                  t: switch (l) {
                    case "title":
                      g = m.getElementsByTagName("title")[0], (!g || g[Ss] || g[At] || g.namespaceURI === "http://www.w3.org/2000/svg" || g.hasAttribute("itemprop")) && (g = m.createElement(l), m.head.insertBefore(
                        g,
                        m.querySelector("head > title")
                      )), xt(g, l, o), g[At] = n, Ct(g), l = g;
                      break e;
                    case "link":
                      var b = Ay(
                        "link",
                        "href",
                        m
                      ).get(l + (o.href || ""));
                      if (b) {
                        for (var S = 0; S < b.length; S++)
                          if (g = b[S], g.getAttribute("href") === (o.href == null || o.href === "" ? null : o.href) && g.getAttribute("rel") === (o.rel == null ? null : o.rel) && g.getAttribute("title") === (o.title == null ? null : o.title) && g.getAttribute("crossorigin") === (o.crossOrigin == null ? null : o.crossOrigin)) {
                            b.splice(S, 1);
                            break t;
                          }
                      }
                      g = m.createElement(l), xt(g, l, o), m.head.appendChild(g);
                      break;
                    case "meta":
                      if (b = Ay(
                        "meta",
                        "content",
                        m
                      ).get(l + (o.content || ""))) {
                        for (S = 0; S < b.length; S++)
                          if (g = b[S], g.getAttribute("content") === (o.content == null ? null : "" + o.content) && g.getAttribute("name") === (o.name == null ? null : o.name) && g.getAttribute("property") === (o.property == null ? null : o.property) && g.getAttribute("http-equiv") === (o.httpEquiv == null ? null : o.httpEquiv) && g.getAttribute("charset") === (o.charSet == null ? null : o.charSet)) {
                            b.splice(S, 1);
                            break t;
                          }
                      }
                      g = m.createElement(l), xt(g, l, o), m.head.appendChild(g);
                      break;
                    default:
                      throw Error(i(468, l));
                  }
                  g[At] = n, Ct(g), l = g;
                }
                n.stateNode = l;
              } else
                Oy(
                  m,
                  n.type,
                  n.stateNode
                );
            else
              n.stateNode = My(
                m,
                l,
                n.memoizedProps
              );
          else
            g !== l ? (g === null ? o.stateNode !== null && (o = o.stateNode, o.parentNode.removeChild(o)) : g.count--, l === null ? Oy(
              m,
              n.type,
              n.stateNode
            ) : My(
              m,
              l,
              n.memoizedProps
            )) : l === null && n.stateNode !== null && Xd(
              n,
              n.memoizedProps,
              o.memoizedProps
            );
        }
        break;
      case 27:
        Zt(r, n), Wt(n), l & 512 && (vt || o === null || ti(o, o.return)), o !== null && l & 4 && Xd(
          n,
          n.memoizedProps,
          o.memoizedProps
        );
        break;
      case 5:
        if (Zt(r, n), Wt(n), l & 512 && (vt || o === null || ti(o, o.return)), n.flags & 32) {
          m = n.stateNode;
          try {
            pr(m, "");
          } catch (he) {
            Ve(n, n.return, he);
          }
        }
        l & 4 && n.stateNode != null && (m = n.memoizedProps, Xd(
          n,
          m,
          o !== null ? o.memoizedProps : m
        )), l & 1024 && ($d = !0);
        break;
      case 6:
        if (Zt(r, n), Wt(n), l & 4) {
          if (n.stateNode === null)
            throw Error(i(162));
          l = n.memoizedProps, o = n.stateNode;
          try {
            o.nodeValue = l;
          } catch (he) {
            Ve(n, n.return, he);
          }
        }
        break;
      case 3:
        if (Zc = null, m = Ln, Ln = Qc(r.containerInfo), Zt(r, n), Ln = m, Wt(n), l & 4 && o !== null && o.memoizedState.isDehydrated)
          try {
            Kr(r.containerInfo);
          } catch (he) {
            Ve(n, n.return, he);
          }
        $d && ($d = !1, Ov(n));
        break;
      case 4:
        l = Ln, Ln = Qc(
          n.stateNode.containerInfo
        ), Zt(r, n), Wt(n), Ln = l;
        break;
      case 12:
        Zt(r, n), Wt(n);
        break;
      case 31:
        Zt(r, n), Wt(n), l & 4 && (l = n.updateQueue, l !== null && (n.updateQueue = null, Ic(n, l)));
        break;
      case 13:
        Zt(r, n), Wt(n), n.child.flags & 8192 && n.memoizedState !== null != (o !== null && o.memoizedState !== null) && (Uc = on()), l & 4 && (l = n.updateQueue, l !== null && (n.updateQueue = null, Ic(n, l)));
        break;
      case 22:
        m = n.memoizedState !== null;
        var O = o !== null && o.memoizedState !== null, z = Si, Q = vt;
        if (Si = z || m, vt = Q || O, Zt(r, n), vt = Q, Si = z, Wt(n), l & 8192)
          e: for (r = n.stateNode, r._visibility = m ? r._visibility & -2 : r._visibility | 1, m && (o === null || O || Si || vt || Va(n)), o = null, r = n; ; ) {
            if (r.tag === 5 || r.tag === 26) {
              if (o === null) {
                O = o = r;
                try {
                  if (g = O.stateNode, m)
                    b = g.style, typeof b.setProperty == "function" ? b.setProperty("display", "none", "important") : b.display = "none";
                  else {
                    S = O.stateNode;
                    var $ = O.memoizedProps.style, F = $ != null && $.hasOwnProperty("display") ? $.display : null;
                    S.style.display = F == null || typeof F == "boolean" ? "" : ("" + F).trim();
                  }
                } catch (he) {
                  Ve(O, O.return, he);
                }
              }
            } else if (r.tag === 6) {
              if (o === null) {
                O = r;
                try {
                  O.stateNode.nodeValue = m ? "" : O.memoizedProps;
                } catch (he) {
                  Ve(O, O.return, he);
                }
              }
            } else if (r.tag === 18) {
              if (o === null) {
                O = r;
                try {
                  var V = O.stateNode;
                  m ? by(V, !0) : by(O.stateNode, !1);
                } catch (he) {
                  Ve(O, O.return, he);
                }
              }
            } else if ((r.tag !== 22 && r.tag !== 23 || r.memoizedState === null || r === n) && r.child !== null) {
              r.child.return = r, r = r.child;
              continue;
            }
            if (r === n) break e;
            for (; r.sibling === null; ) {
              if (r.return === null || r.return === n) break e;
              o === r && (o = null), r = r.return;
            }
            o === r && (o = null), r.sibling.return = r.return, r = r.sibling;
          }
        l & 4 && (l = n.updateQueue, l !== null && (o = l.retryQueue, o !== null && (l.retryQueue = null, Ic(n, o))));
        break;
      case 19:
        Zt(r, n), Wt(n), l & 4 && (l = n.updateQueue, l !== null && (n.updateQueue = null, Ic(n, l)));
        break;
      case 30:
        break;
      case 21:
        break;
      default:
        Zt(r, n), Wt(n);
    }
  }
  function Wt(n) {
    var r = n.flags;
    if (r & 2) {
      try {
        for (var o, l = n.return; l !== null; ) {
          if (Sv(l)) {
            o = l;
            break;
          }
          l = l.return;
        }
        if (o == null) throw Error(i(160));
        switch (o.tag) {
          case 27:
            var m = o.stateNode, g = Zd(n);
            Nc(n, g, m);
            break;
          case 5:
            var b = o.stateNode;
            o.flags & 32 && (pr(b, ""), o.flags &= -33);
            var S = Zd(n);
            Nc(n, S, b);
            break;
          case 3:
          case 4:
            var O = o.stateNode.containerInfo, z = Zd(n);
            Wd(
              n,
              z,
              O
            );
            break;
          default:
            throw Error(i(161));
        }
      } catch (Q) {
        Ve(n, n.return, Q);
      }
      n.flags &= -3;
    }
    r & 4096 && (n.flags &= -4097);
  }
  function Ov(n) {
    if (n.subtreeFlags & 1024)
      for (n = n.child; n !== null; ) {
        var r = n;
        Ov(r), r.tag === 5 && r.flags & 1024 && r.stateNode.reset(), n = n.sibling;
      }
  }
  function Ci(n, r) {
    if (r.subtreeFlags & 8772)
      for (r = r.child; r !== null; )
        Ev(n, r.alternate, r), r = r.sibling;
  }
  function Va(n) {
    for (n = n.child; n !== null; ) {
      var r = n;
      switch (r.tag) {
        case 0:
        case 11:
        case 14:
        case 15:
          $i(4, r, r.return), Va(r);
          break;
        case 1:
          ti(r, r.return);
          var o = r.stateNode;
          typeof o.componentWillUnmount == "function" && bv(
            r,
            r.return,
            o
          ), Va(r);
          break;
        case 27:
          io(r.stateNode);
        case 26:
        case 5:
          ti(r, r.return), Va(r);
          break;
        case 22:
          r.memoizedState === null && Va(r);
          break;
        case 30:
          Va(r);
          break;
        default:
          Va(r);
      }
      n = n.sibling;
    }
  }
  function Ei(n, r, o) {
    for (o = o && (r.subtreeFlags & 8772) !== 0, r = r.child; r !== null; ) {
      var l = r.alternate, m = n, g = r, b = g.flags;
      switch (g.tag) {
        case 0:
        case 11:
        case 15:
          Ei(
            m,
            g,
            o
          ), Ys(4, g);
          break;
        case 1:
          if (Ei(
            m,
            g,
            o
          ), l = g, m = l.stateNode, typeof m.componentDidMount == "function")
            try {
              m.componentDidMount();
            } catch (z) {
              Ve(l, l.return, z);
            }
          if (l = g, m = l.updateQueue, m !== null) {
            var S = l.stateNode;
            try {
              var O = m.shared.hiddenCallbacks;
              if (O !== null)
                for (m.shared.hiddenCallbacks = null, m = 0; m < O.length; m++)
                  og(O[m], S);
            } catch (z) {
              Ve(l, l.return, z);
            }
          }
          o && b & 64 && yv(g), Js(g, g.return);
          break;
        case 27:
          kv(g);
        case 26:
        case 5:
          Ei(
            m,
            g,
            o
          ), o && l === null && b & 4 && Tv(g), Js(g, g.return);
          break;
        case 12:
          Ei(
            m,
            g,
            o
          );
          break;
        case 31:
          Ei(
            m,
            g,
            o
          ), o && b & 4 && wv(m, g);
          break;
        case 13:
          Ei(
            m,
            g,
            o
          ), o && b & 4 && Mv(m, g);
          break;
        case 22:
          g.memoizedState === null && Ei(
            m,
            g,
            o
          ), Js(g, g.return);
          break;
        case 30:
          break;
        default:
          Ei(
            m,
            g,
            o
          );
      }
      r = r.sibling;
    }
  }
  function ef(n, r) {
    var o = null;
    n !== null && n.memoizedState !== null && n.memoizedState.cachePool !== null && (o = n.memoizedState.cachePool.pool), n = null, r.memoizedState !== null && r.memoizedState.cachePool !== null && (n = r.memoizedState.cachePool.pool), n !== o && (n != null && n.refCount++, o != null && Ns(o));
  }
  function tf(n, r) {
    n = null, r.alternate !== null && (n = r.alternate.memoizedState.cache), r = r.memoizedState.cache, r !== n && (r.refCount++, n != null && Ns(n));
  }
  function Un(n, r, o, l) {
    if (r.subtreeFlags & 10256)
      for (r = r.child; r !== null; )
        Dv(
          n,
          r,
          o,
          l
        ), r = r.sibling;
  }
  function Dv(n, r, o, l) {
    var m = r.flags;
    switch (r.tag) {
      case 0:
      case 11:
      case 15:
        Un(
          n,
          r,
          o,
          l
        ), m & 2048 && Ys(9, r);
        break;
      case 1:
        Un(
          n,
          r,
          o,
          l
        );
        break;
      case 3:
        Un(
          n,
          r,
          o,
          l
        ), m & 2048 && (n = null, r.alternate !== null && (n = r.alternate.memoizedState.cache), r = r.memoizedState.cache, r !== n && (r.refCount++, n != null && Ns(n)));
        break;
      case 12:
        if (m & 2048) {
          Un(
            n,
            r,
            o,
            l
          ), n = r.stateNode;
          try {
            var g = r.memoizedProps, b = g.id, S = g.onPostCommit;
            typeof S == "function" && S(
              b,
              r.alternate === null ? "mount" : "update",
              n.passiveEffectDuration,
              -0
            );
          } catch (O) {
            Ve(r, r.return, O);
          }
        } else
          Un(
            n,
            r,
            o,
            l
          );
        break;
      case 31:
        Un(
          n,
          r,
          o,
          l
        );
        break;
      case 13:
        Un(
          n,
          r,
          o,
          l
        );
        break;
      case 23:
        break;
      case 22:
        g = r.stateNode, b = r.alternate, r.memoizedState !== null ? g._visibility & 2 ? Un(
          n,
          r,
          o,
          l
        ) : Qs(n, r) : g._visibility & 2 ? Un(
          n,
          r,
          o,
          l
        ) : (g._visibility |= 2, Ir(
          n,
          r,
          o,
          l,
          (r.subtreeFlags & 10256) !== 0 || !1
        )), m & 2048 && ef(b, r);
        break;
      case 24:
        Un(
          n,
          r,
          o,
          l
        ), m & 2048 && tf(r.alternate, r);
        break;
      default:
        Un(
          n,
          r,
          o,
          l
        );
    }
  }
  function Ir(n, r, o, l, m) {
    for (m = m && ((r.subtreeFlags & 10256) !== 0 || !1), r = r.child; r !== null; ) {
      var g = n, b = r, S = o, O = l, z = b.flags;
      switch (b.tag) {
        case 0:
        case 11:
        case 15:
          Ir(
            g,
            b,
            S,
            O,
            m
          ), Ys(8, b);
          break;
        case 23:
          break;
        case 22:
          var Q = b.stateNode;
          b.memoizedState !== null ? Q._visibility & 2 ? Ir(
            g,
            b,
            S,
            O,
            m
          ) : Qs(
            g,
            b
          ) : (Q._visibility |= 2, Ir(
            g,
            b,
            S,
            O,
            m
          )), m && z & 2048 && ef(
            b.alternate,
            b
          );
          break;
        case 24:
          Ir(
            g,
            b,
            S,
            O,
            m
          ), m && z & 2048 && tf(b.alternate, b);
          break;
        default:
          Ir(
            g,
            b,
            S,
            O,
            m
          );
      }
      r = r.sibling;
    }
  }
  function Qs(n, r) {
    if (r.subtreeFlags & 10256)
      for (r = r.child; r !== null; ) {
        var o = n, l = r, m = l.flags;
        switch (l.tag) {
          case 22:
            Qs(o, l), m & 2048 && ef(
              l.alternate,
              l
            );
            break;
          case 24:
            Qs(o, l), m & 2048 && tf(l.alternate, l);
            break;
          default:
            Qs(o, l);
        }
        r = r.sibling;
      }
  }
  var Xs = 8192;
  function Lr(n, r, o) {
    if (n.subtreeFlags & Xs)
      for (n = n.child; n !== null; )
        Pv(
          n,
          r,
          o
        ), n = n.sibling;
  }
  function Pv(n, r, o) {
    switch (n.tag) {
      case 26:
        Lr(
          n,
          r,
          o
        ), n.flags & Xs && n.memoizedState !== null && QE(
          o,
          Ln,
          n.memoizedState,
          n.memoizedProps
        );
        break;
      case 5:
        Lr(
          n,
          r,
          o
        );
        break;
      case 3:
      case 4:
        var l = Ln;
        Ln = Qc(n.stateNode.containerInfo), Lr(
          n,
          r,
          o
        ), Ln = l;
        break;
      case 22:
        n.memoizedState === null && (l = n.alternate, l !== null && l.memoizedState !== null ? (l = Xs, Xs = 16777216, Lr(
          n,
          r,
          o
        ), Xs = l) : Lr(
          n,
          r,
          o
        ));
        break;
      default:
        Lr(
          n,
          r,
          o
        );
    }
  }
  function xv(n) {
    var r = n.alternate;
    if (r !== null && (n = r.child, n !== null)) {
      r.child = null;
      do
        r = n.sibling, n.sibling = null, n = r;
      while (n !== null);
    }
  }
  function Zs(n) {
    var r = n.deletions;
    if ((n.flags & 16) !== 0) {
      if (r !== null)
        for (var o = 0; o < r.length; o++) {
          var l = r[o];
          Et = l, Iv(
            l,
            n
          );
        }
      xv(n);
    }
    if (n.subtreeFlags & 10256)
      for (n = n.child; n !== null; )
        Nv(n), n = n.sibling;
  }
  function Nv(n) {
    switch (n.tag) {
      case 0:
      case 11:
      case 15:
        Zs(n), n.flags & 2048 && $i(9, n, n.return);
        break;
      case 3:
        Zs(n);
        break;
      case 12:
        Zs(n);
        break;
      case 22:
        var r = n.stateNode;
        n.memoizedState !== null && r._visibility & 2 && (n.return === null || n.return.tag !== 13) ? (r._visibility &= -3, Lc(n)) : Zs(n);
        break;
      default:
        Zs(n);
    }
  }
  function Lc(n) {
    var r = n.deletions;
    if ((n.flags & 16) !== 0) {
      if (r !== null)
        for (var o = 0; o < r.length; o++) {
          var l = r[o];
          Et = l, Iv(
            l,
            n
          );
        }
      xv(n);
    }
    for (n = n.child; n !== null; ) {
      switch (r = n, r.tag) {
        case 0:
        case 11:
        case 15:
          $i(8, r, r.return), Lc(r);
          break;
        case 22:
          o = r.stateNode, o._visibility & 2 && (o._visibility &= -3, Lc(r));
          break;
        default:
          Lc(r);
      }
      n = n.sibling;
    }
  }
  function Iv(n, r) {
    for (; Et !== null; ) {
      var o = Et;
      switch (o.tag) {
        case 0:
        case 11:
        case 15:
          $i(8, o, r);
          break;
        case 23:
        case 22:
          if (o.memoizedState !== null && o.memoizedState.cachePool !== null) {
            var l = o.memoizedState.cachePool.pool;
            l != null && l.refCount++;
          }
          break;
        case 24:
          Ns(o.memoizedState.cache);
      }
      if (l = o.child, l !== null) l.return = o, Et = l;
      else
        e: for (o = n; Et !== null; ) {
          l = Et;
          var m = l.sibling, g = l.return;
          if (_v(l), l === o) {
            Et = null;
            break e;
          }
          if (m !== null) {
            m.return = g, Et = m;
            break e;
          }
          Et = g;
        }
    }
  }
  var uE = {
    getCacheForType: function(n) {
      var r = Dt(mt), o = r.data.get(n);
      return o === void 0 && (o = n(), r.data.set(n, o)), o;
    },
    cacheSignal: function() {
      return Dt(mt).controller.signal;
    }
  }, dE = typeof WeakMap == "function" ? WeakMap : Map, qe = 0, Xe = null, De = null, Ne = 0, He = 0, mn = null, ea = !1, Ur = !1, nf = !1, _i = 0, ct = 0, ta = 0, Ga = 0, af = 0, pn = 0, Br = 0, Ws = null, $t = null, rf = !1, Uc = 0, Lv = 0, Bc = 1 / 0, zc = null, na = null, St = 0, ia = null, zr = null, Ri = 0, sf = 0, of = null, Uv = null, $s = 0, cf = null;
  function gn() {
    return (qe & 2) !== 0 && Ne !== 0 ? Ne & -Ne : q.T !== null ? mf() : $m();
  }
  function Bv() {
    if (pn === 0)
      if ((Ne & 536870912) === 0 || Le) {
        var n = Yo;
        Yo <<= 1, (Yo & 3932160) === 0 && (Yo = 262144), pn = n;
      } else pn = 536870912;
    return n = fn.current, n !== null && (n.flags |= 32), pn;
  }
  function en(n, r, o) {
    (n === Xe && (He === 2 || He === 9) || n.cancelPendingCommit !== null) && (Fr(n, 0), aa(
      n,
      Ne,
      pn,
      !1
    )), Ts(n, o), ((qe & 2) === 0 || n !== Xe) && (n === Xe && ((qe & 2) === 0 && (Ga |= o), ct === 4 && aa(
      n,
      Ne,
      pn,
      !1
    )), ni(n));
  }
  function zv(n, r, o) {
    if ((qe & 6) !== 0) throw Error(i(327));
    var l = !o && (r & 127) === 0 && (r & n.expiredLanes) === 0 || bs(n, r), m = l ? mE(n, r) : uf(n, r, !0), g = l;
    do {
      if (m === 0) {
        Ur && !l && aa(n, r, 0, !1);
        break;
      } else {
        if (o = n.current.alternate, g && !fE(o)) {
          m = uf(n, r, !1), g = !1;
          continue;
        }
        if (m === 2) {
          if (g = r, n.errorRecoveryDisabledLanes & g)
            var b = 0;
          else
            b = n.pendingLanes & -536870913, b = b !== 0 ? b : b & 536870912 ? 536870912 : 0;
          if (b !== 0) {
            r = b;
            e: {
              var S = n;
              m = Ws;
              var O = S.current.memoizedState.isDehydrated;
              if (O && (Fr(S, b).flags |= 256), b = uf(
                S,
                b,
                !1
              ), b !== 2) {
                if (nf && !O) {
                  S.errorRecoveryDisabledLanes |= g, Ga |= g, m = 4;
                  break e;
                }
                g = $t, $t = m, g !== null && ($t === null ? $t = g : $t.push.apply(
                  $t,
                  g
                ));
              }
              m = b;
            }
            if (g = !1, m !== 2) continue;
          }
        }
        if (m === 1) {
          Fr(n, 0), aa(n, r, 0, !0);
          break;
        }
        e: {
          switch (l = n, g = m, g) {
            case 0:
            case 1:
              throw Error(i(345));
            case 4:
              if ((r & 4194048) !== r) break;
            case 6:
              aa(
                l,
                r,
                pn,
                !ea
              );
              break e;
            case 2:
              $t = null;
              break;
            case 3:
            case 5:
              break;
            default:
              throw Error(i(329));
          }
          if ((r & 62914560) === r && (m = Uc + 300 - on(), 10 < m)) {
            if (aa(
              l,
              r,
              pn,
              !ea
            ), Qo(l, 0, !0) !== 0) break e;
            Ri = r, l.timeoutHandle = gy(
              Fv.bind(
                null,
                l,
                o,
                $t,
                zc,
                rf,
                r,
                pn,
                Ga,
                Br,
                ea,
                g,
                "Throttled",
                -0,
                0
              ),
              m
            );
            break e;
          }
          Fv(
            l,
            o,
            $t,
            zc,
            rf,
            r,
            pn,
            Ga,
            Br,
            ea,
            g,
            null,
            -0,
            0
          );
        }
      }
      break;
    } while (!0);
    ni(n);
  }
  function Fv(n, r, o, l, m, g, b, S, O, z, Q, $, F, V) {
    if (n.timeoutHandle = -1, $ = r.subtreeFlags, $ & 8192 || ($ & 16785408) === 16785408) {
      $ = {
        stylesheets: null,
        count: 0,
        imgCount: 0,
        imgBytes: 0,
        suspenseyImages: [],
        waitingForImages: !0,
        waitingForViewTransition: !1,
        unsuspend: di
      }, Pv(
        r,
        g,
        $
      );
      var he = (g & 62914560) === g ? Uc - on() : (g & 4194048) === g ? Lv - on() : 0;
      if (he = XE(
        $,
        he
      ), he !== null) {
        Ri = g, n.cancelPendingCommit = he(
          Jv.bind(
            null,
            n,
            r,
            g,
            o,
            l,
            m,
            b,
            S,
            O,
            Q,
            $,
            null,
            F,
            V
          )
        ), aa(n, g, b, !z);
        return;
      }
    }
    Jv(
      n,
      r,
      g,
      o,
      l,
      m,
      b,
      S,
      O
    );
  }
  function fE(n) {
    for (var r = n; ; ) {
      var o = r.tag;
      if ((o === 0 || o === 11 || o === 15) && r.flags & 16384 && (o = r.updateQueue, o !== null && (o = o.stores, o !== null)))
        for (var l = 0; l < o.length; l++) {
          var m = o[l], g = m.getSnapshot;
          m = m.value;
          try {
            if (!un(g(), m)) return !1;
          } catch {
            return !1;
          }
        }
      if (o = r.child, r.subtreeFlags & 16384 && o !== null)
        o.return = r, r = o;
      else {
        if (r === n) break;
        for (; r.sibling === null; ) {
          if (r.return === null || r.return === n) return !0;
          r = r.return;
        }
        r.sibling.return = r.return, r = r.sibling;
      }
    }
    return !0;
  }
  function aa(n, r, o, l) {
    r &= ~af, r &= ~Ga, n.suspendedLanes |= r, n.pingedLanes &= ~r, l && (n.warmLanes |= r), l = n.expirationTimes;
    for (var m = r; 0 < m; ) {
      var g = 31 - ln(m), b = 1 << g;
      l[g] = -1, m &= ~b;
    }
    o !== 0 && Xm(n, o, r);
  }
  function Fc() {
    return (qe & 6) === 0 ? (eo(0), !1) : !0;
  }
  function lf() {
    if (De !== null) {
      if (He === 0)
        var n = De.return;
      else
        n = De, pi = La = null, Ed(n), Or = null, Ls = 0, n = De;
      for (; n !== null; )
        vv(n.alternate, n), n = n.return;
      De = null;
    }
  }
  function Fr(n, r) {
    var o = n.timeoutHandle;
    o !== -1 && (n.timeoutHandle = -1, PE(o)), o = n.cancelPendingCommit, o !== null && (n.cancelPendingCommit = null, o()), Ri = 0, lf(), Xe = n, De = o = hi(n.current, null), Ne = r, He = 0, mn = null, ea = !1, Ur = bs(n, r), nf = !1, Br = pn = af = Ga = ta = ct = 0, $t = Ws = null, rf = !1, (r & 8) !== 0 && (r |= r & 32);
    var l = n.entangledLanes;
    if (l !== 0)
      for (n = n.entanglements, l &= r; 0 < l; ) {
        var m = 31 - ln(l), g = 1 << m;
        r |= n[m], l &= ~g;
      }
    return _i = r, oc(), o;
  }
  function jv(n, r) {
    Re = null, q.H = Vs, r === Ar || r === pc ? (r = ig(), He = 3) : r === fd ? (r = ig(), He = 4) : He = r === Fd ? 8 : r !== null && typeof r == "object" && typeof r.then == "function" ? 6 : 1, mn = r, De === null && (ct = 1, Ac(
      n,
      kn(r, n.current)
    ));
  }
  function qv() {
    var n = fn.current;
    return n === null ? !0 : (Ne & 4194048) === Ne ? Rn === null : (Ne & 62914560) === Ne || (Ne & 536870912) !== 0 ? n === Rn : !1;
  }
  function Hv() {
    var n = q.H;
    return q.H = Vs, n === null ? Vs : n;
  }
  function Vv() {
    var n = q.A;
    return q.A = uE, n;
  }
  function jc() {
    ct = 4, ea || (Ne & 4194048) !== Ne && fn.current !== null || (Ur = !0), (ta & 134217727) === 0 && (Ga & 134217727) === 0 || Xe === null || aa(
      Xe,
      Ne,
      pn,
      !1
    );
  }
  function uf(n, r, o) {
    var l = qe;
    qe |= 2;
    var m = Hv(), g = Vv();
    (Xe !== n || Ne !== r) && (zc = null, Fr(n, r)), r = !1;
    var b = ct;
    e: do
      try {
        if (He !== 0 && De !== null) {
          var S = De, O = mn;
          switch (He) {
            case 8:
              lf(), b = 6;
              break e;
            case 3:
            case 2:
            case 9:
            case 6:
              fn.current === null && (r = !0);
              var z = He;
              if (He = 0, mn = null, jr(n, S, O, z), o && Ur) {
                b = 0;
                break e;
              }
              break;
            default:
              z = He, He = 0, mn = null, jr(n, S, O, z);
          }
        }
        hE(), b = ct;
        break;
      } catch (Q) {
        jv(n, Q);
      }
    while (!0);
    return r && n.shellSuspendCounter++, pi = La = null, qe = l, q.H = m, q.A = g, De === null && (Xe = null, Ne = 0, oc()), b;
  }
  function hE() {
    for (; De !== null; ) Gv(De);
  }
  function mE(n, r) {
    var o = qe;
    qe |= 2;
    var l = Hv(), m = Vv();
    Xe !== n || Ne !== r ? (zc = null, Bc = on() + 500, Fr(n, r)) : Ur = bs(
      n,
      r
    );
    e: do
      try {
        if (He !== 0 && De !== null) {
          r = De;
          var g = mn;
          t: switch (He) {
            case 1:
              He = 0, mn = null, jr(n, r, g, 1);
              break;
            case 2:
            case 9:
              if (tg(g)) {
                He = 0, mn = null, Kv(r);
                break;
              }
              r = function() {
                He !== 2 && He !== 9 || Xe !== n || (He = 7), ni(n);
              }, g.then(r, r);
              break e;
            case 3:
              He = 7;
              break e;
            case 4:
              He = 5;
              break e;
            case 7:
              tg(g) ? (He = 0, mn = null, Kv(r)) : (He = 0, mn = null, jr(n, r, g, 7));
              break;
            case 5:
              var b = null;
              switch (De.tag) {
                case 26:
                  b = De.memoizedState;
                case 5:
                case 27:
                  var S = De;
                  if (b ? Dy(b) : S.stateNode.complete) {
                    He = 0, mn = null;
                    var O = S.sibling;
                    if (O !== null) De = O;
                    else {
                      var z = S.return;
                      z !== null ? (De = z, qc(z)) : De = null;
                    }
                    break t;
                  }
              }
              He = 0, mn = null, jr(n, r, g, 5);
              break;
            case 6:
              He = 0, mn = null, jr(n, r, g, 6);
              break;
            case 8:
              lf(), ct = 6;
              break e;
            default:
              throw Error(i(462));
          }
        }
        pE();
        break;
      } catch (Q) {
        jv(n, Q);
      }
    while (!0);
    return pi = La = null, q.H = l, q.A = m, qe = o, De !== null ? 0 : (Xe = null, Ne = 0, oc(), ct);
  }
  function pE() {
    for (; De !== null && !Bk(); )
      Gv(De);
  }
  function Gv(n) {
    var r = pv(n.alternate, n, _i);
    n.memoizedProps = n.pendingProps, r === null ? qc(n) : De = r;
  }
  function Kv(n) {
    var r = n, o = r.alternate;
    switch (r.tag) {
      case 15:
      case 0:
        r = lv(
          o,
          r,
          r.pendingProps,
          r.type,
          void 0,
          Ne
        );
        break;
      case 11:
        r = lv(
          o,
          r,
          r.pendingProps,
          r.type.render,
          r.ref,
          Ne
        );
        break;
      case 5:
        Ed(r);
      default:
        vv(o, r), r = De = Vp(r, _i), r = pv(o, r, _i);
    }
    n.memoizedProps = n.pendingProps, r === null ? qc(n) : De = r;
  }
  function jr(n, r, o, l) {
    pi = La = null, Ed(r), Or = null, Ls = 0;
    var m = r.return;
    try {
      if (iE(
        n,
        m,
        r,
        o,
        Ne
      )) {
        ct = 1, Ac(
          n,
          kn(o, n.current)
        ), De = null;
        return;
      }
    } catch (g) {
      if (m !== null) throw De = m, g;
      ct = 1, Ac(
        n,
        kn(o, n.current)
      ), De = null;
      return;
    }
    r.flags & 32768 ? (Le || l === 1 ? n = !0 : Ur || (Ne & 536870912) !== 0 ? n = !1 : (ea = n = !0, (l === 2 || l === 9 || l === 3 || l === 6) && (l = fn.current, l !== null && l.tag === 13 && (l.flags |= 16384))), Yv(r, n)) : qc(r);
  }
  function qc(n) {
    var r = n;
    do {
      if ((r.flags & 32768) !== 0) {
        Yv(
          r,
          ea
        );
        return;
      }
      n = r.return;
      var o = sE(
        r.alternate,
        r,
        _i
      );
      if (o !== null) {
        De = o;
        return;
      }
      if (r = r.sibling, r !== null) {
        De = r;
        return;
      }
      De = r = n;
    } while (r !== null);
    ct === 0 && (ct = 5);
  }
  function Yv(n, r) {
    do {
      var o = oE(n.alternate, n);
      if (o !== null) {
        o.flags &= 32767, De = o;
        return;
      }
      if (o = n.return, o !== null && (o.flags |= 32768, o.subtreeFlags = 0, o.deletions = null), !r && (n = n.sibling, n !== null)) {
        De = n;
        return;
      }
      De = n = o;
    } while (n !== null);
    ct = 6, De = null;
  }
  function Jv(n, r, o, l, m, g, b, S, O) {
    n.cancelPendingCommit = null;
    do
      Hc();
    while (St !== 0);
    if ((qe & 6) !== 0) throw Error(i(327));
    if (r !== null) {
      if (r === n.current) throw Error(i(177));
      if (g = r.lanes | r.childLanes, g |= Zu, Jk(
        n,
        o,
        g,
        b,
        S,
        O
      ), n === Xe && (De = Xe = null, Ne = 0), zr = r, ia = n, Ri = o, sf = g, of = m, Uv = l, (r.subtreeFlags & 10256) !== 0 || (r.flags & 10256) !== 0 ? (n.callbackNode = null, n.callbackPriority = 0, bE(Go, function() {
        return $v(), null;
      })) : (n.callbackNode = null, n.callbackPriority = 0), l = (r.flags & 13878) !== 0, (r.subtreeFlags & 13878) !== 0 || l) {
        l = q.T, q.T = null, m = te.p, te.p = 2, b = qe, qe |= 4;
        try {
          cE(n, r, o);
        } finally {
          qe = b, te.p = m, q.T = l;
        }
      }
      St = 1, Qv(), Xv(), Zv();
    }
  }
  function Qv() {
    if (St === 1) {
      St = 0;
      var n = ia, r = zr, o = (r.flags & 13878) !== 0;
      if ((r.subtreeFlags & 13878) !== 0 || o) {
        o = q.T, q.T = null;
        var l = te.p;
        te.p = 2;
        var m = qe;
        qe |= 4;
        try {
          Av(r, n);
          var g = kf, b = Ip(n.containerInfo), S = g.focusedElem, O = g.selectionRange;
          if (b !== S && S && S.ownerDocument && Np(
            S.ownerDocument.documentElement,
            S
          )) {
            if (O !== null && Ku(S)) {
              var z = O.start, Q = O.end;
              if (Q === void 0 && (Q = z), "selectionStart" in S)
                S.selectionStart = z, S.selectionEnd = Math.min(
                  Q,
                  S.value.length
                );
              else {
                var $ = S.ownerDocument || document, F = $ && $.defaultView || window;
                if (F.getSelection) {
                  var V = F.getSelection(), he = S.textContent.length, be = Math.min(O.start, he), Je = O.end === void 0 ? be : Math.min(O.end, he);
                  !V.extend && be > Je && (b = Je, Je = be, be = b);
                  var I = xp(
                    S,
                    be
                  ), P = xp(
                    S,
                    Je
                  );
                  if (I && P && (V.rangeCount !== 1 || V.anchorNode !== I.node || V.anchorOffset !== I.offset || V.focusNode !== P.node || V.focusOffset !== P.offset)) {
                    var U = $.createRange();
                    U.setStart(I.node, I.offset), V.removeAllRanges(), be > Je ? (V.addRange(U), V.extend(P.node, P.offset)) : (U.setEnd(P.node, P.offset), V.addRange(U));
                  }
                }
              }
            }
            for ($ = [], V = S; V = V.parentNode; )
              V.nodeType === 1 && $.push({
                element: V,
                left: V.scrollLeft,
                top: V.scrollTop
              });
            for (typeof S.focus == "function" && S.focus(), S = 0; S < $.length; S++) {
              var Z = $[S];
              Z.element.scrollLeft = Z.left, Z.element.scrollTop = Z.top;
            }
          }
          tl = !!Sf, kf = Sf = null;
        } finally {
          qe = m, te.p = l, q.T = o;
        }
      }
      n.current = r, St = 2;
    }
  }
  function Xv() {
    if (St === 2) {
      St = 0;
      var n = ia, r = zr, o = (r.flags & 8772) !== 0;
      if ((r.subtreeFlags & 8772) !== 0 || o) {
        o = q.T, q.T = null;
        var l = te.p;
        te.p = 2;
        var m = qe;
        qe |= 4;
        try {
          Ev(n, r.alternate, r);
        } finally {
          qe = m, te.p = l, q.T = o;
        }
      }
      St = 3;
    }
  }
  function Zv() {
    if (St === 4 || St === 3) {
      St = 0, zk();
      var n = ia, r = zr, o = Ri, l = Uv;
      (r.subtreeFlags & 10256) !== 0 || (r.flags & 10256) !== 0 ? St = 5 : (St = 0, zr = ia = null, Wv(n, n.pendingLanes));
      var m = n.pendingLanes;
      if (m === 0 && (na = null), wu(o), r = r.stateNode, cn && typeof cn.onCommitFiberRoot == "function")
        try {
          cn.onCommitFiberRoot(
            ys,
            r,
            void 0,
            (r.current.flags & 128) === 128
          );
        } catch {
        }
      if (l !== null) {
        r = q.T, m = te.p, te.p = 2, q.T = null;
        try {
          for (var g = n.onRecoverableError, b = 0; b < l.length; b++) {
            var S = l[b];
            g(S.value, {
              componentStack: S.stack
            });
          }
        } finally {
          q.T = r, te.p = m;
        }
      }
      (Ri & 3) !== 0 && Hc(), ni(n), m = n.pendingLanes, (o & 261930) !== 0 && (m & 42) !== 0 ? n === cf ? $s++ : ($s = 0, cf = n) : $s = 0, eo(0);
    }
  }
  function Wv(n, r) {
    (n.pooledCacheLanes &= r) === 0 && (r = n.pooledCache, r != null && (n.pooledCache = null, Ns(r)));
  }
  function Hc() {
    return Qv(), Xv(), Zv(), $v();
  }
  function $v() {
    if (St !== 5) return !1;
    var n = ia, r = sf;
    sf = 0;
    var o = wu(Ri), l = q.T, m = te.p;
    try {
      te.p = 32 > o ? 32 : o, q.T = null, o = of, of = null;
      var g = ia, b = Ri;
      if (St = 0, zr = ia = null, Ri = 0, (qe & 6) !== 0) throw Error(i(331));
      var S = qe;
      if (qe |= 4, Nv(g.current), Dv(
        g,
        g.current,
        b,
        o
      ), qe = S, eo(0, !1), cn && typeof cn.onPostCommitFiberRoot == "function")
        try {
          cn.onPostCommitFiberRoot(ys, g);
        } catch {
        }
      return !0;
    } finally {
      te.p = m, q.T = l, Wv(n, r);
    }
  }
  function ey(n, r, o) {
    r = kn(o, r), r = zd(n.stateNode, r, 2), n = Xi(n, r, 2), n !== null && (Ts(n, 2), ni(n));
  }
  function Ve(n, r, o) {
    if (n.tag === 3)
      ey(n, n, o);
    else
      for (; r !== null; ) {
        if (r.tag === 3) {
          ey(
            r,
            n,
            o
          );
          break;
        } else if (r.tag === 1) {
          var l = r.stateNode;
          if (typeof r.type.getDerivedStateFromError == "function" || typeof l.componentDidCatch == "function" && (na === null || !na.has(l))) {
            n = kn(o, n), o = tv(2), l = Xi(r, o, 2), l !== null && (nv(
              o,
              l,
              r,
              n
            ), Ts(l, 2), ni(l));
            break;
          }
        }
        r = r.return;
      }
  }
  function df(n, r, o) {
    var l = n.pingCache;
    if (l === null) {
      l = n.pingCache = new dE();
      var m = /* @__PURE__ */ new Set();
      l.set(r, m);
    } else
      m = l.get(r), m === void 0 && (m = /* @__PURE__ */ new Set(), l.set(r, m));
    m.has(o) || (nf = !0, m.add(o), n = gE.bind(null, n, r, o), r.then(n, n));
  }
  function gE(n, r, o) {
    var l = n.pingCache;
    l !== null && l.delete(r), n.pingedLanes |= n.suspendedLanes & o, n.warmLanes &= ~o, Xe === n && (Ne & o) === o && (ct === 4 || ct === 3 && (Ne & 62914560) === Ne && 300 > on() - Uc ? (qe & 2) === 0 && Fr(n, 0) : af |= o, Br === Ne && (Br = 0)), ni(n);
  }
  function ty(n, r) {
    r === 0 && (r = Qm()), n = xa(n, r), n !== null && (Ts(n, r), ni(n));
  }
  function vE(n) {
    var r = n.memoizedState, o = 0;
    r !== null && (o = r.retryLane), ty(n, o);
  }
  function yE(n, r) {
    var o = 0;
    switch (n.tag) {
      case 31:
      case 13:
        var l = n.stateNode, m = n.memoizedState;
        m !== null && (o = m.retryLane);
        break;
      case 19:
        l = n.stateNode;
        break;
      case 22:
        l = n.stateNode._retryCache;
        break;
      default:
        throw Error(i(314));
    }
    l !== null && l.delete(r), ty(n, o);
  }
  function bE(n, r) {
    return Cu(n, r);
  }
  var Vc = null, qr = null, ff = !1, Gc = !1, hf = !1, ra = 0;
  function ni(n) {
    n !== qr && n.next === null && (qr === null ? Vc = qr = n : qr = qr.next = n), Gc = !0, ff || (ff = !0, SE());
  }
  function eo(n, r) {
    if (!hf && Gc) {
      hf = !0;
      do
        for (var o = !1, l = Vc; l !== null; ) {
          if (n !== 0) {
            var m = l.pendingLanes;
            if (m === 0) var g = 0;
            else {
              var b = l.suspendedLanes, S = l.pingedLanes;
              g = (1 << 31 - ln(42 | n) + 1) - 1, g &= m & ~(b & ~S), g = g & 201326741 ? g & 201326741 | 1 : g ? g | 2 : 0;
            }
            g !== 0 && (o = !0, ry(l, g));
          } else
            g = Ne, g = Qo(
              l,
              l === Xe ? g : 0,
              l.cancelPendingCommit !== null || l.timeoutHandle !== -1
            ), (g & 3) === 0 || bs(l, g) || (o = !0, ry(l, g));
          l = l.next;
        }
      while (o);
      hf = !1;
    }
  }
  function TE() {
    ny();
  }
  function ny() {
    Gc = ff = !1;
    var n = 0;
    ra !== 0 && DE() && (n = ra);
    for (var r = on(), o = null, l = Vc; l !== null; ) {
      var m = l.next, g = iy(l, r);
      g === 0 ? (l.next = null, o === null ? Vc = m : o.next = m, m === null && (qr = o)) : (o = l, (n !== 0 || (g & 3) !== 0) && (Gc = !0)), l = m;
    }
    St !== 0 && St !== 5 || eo(n), ra !== 0 && (ra = 0);
  }
  function iy(n, r) {
    for (var o = n.suspendedLanes, l = n.pingedLanes, m = n.expirationTimes, g = n.pendingLanes & -62914561; 0 < g; ) {
      var b = 31 - ln(g), S = 1 << b, O = m[b];
      O === -1 ? ((S & o) === 0 || (S & l) !== 0) && (m[b] = Yk(S, r)) : O <= r && (n.expiredLanes |= S), g &= ~S;
    }
    if (r = Xe, o = Ne, o = Qo(
      n,
      n === r ? o : 0,
      n.cancelPendingCommit !== null || n.timeoutHandle !== -1
    ), l = n.callbackNode, o === 0 || n === r && (He === 2 || He === 9) || n.cancelPendingCommit !== null)
      return l !== null && l !== null && Eu(l), n.callbackNode = null, n.callbackPriority = 0;
    if ((o & 3) === 0 || bs(n, o)) {
      if (r = o & -o, r === n.callbackPriority) return r;
      switch (l !== null && Eu(l), wu(o)) {
        case 2:
        case 8:
          o = Ym;
          break;
        case 32:
          o = Go;
          break;
        case 268435456:
          o = Jm;
          break;
        default:
          o = Go;
      }
      return l = ay.bind(null, n), o = Cu(o, l), n.callbackPriority = r, n.callbackNode = o, r;
    }
    return l !== null && l !== null && Eu(l), n.callbackPriority = 2, n.callbackNode = null, 2;
  }
  function ay(n, r) {
    if (St !== 0 && St !== 5)
      return n.callbackNode = null, n.callbackPriority = 0, null;
    var o = n.callbackNode;
    if (Hc() && n.callbackNode !== o)
      return null;
    var l = Ne;
    return l = Qo(
      n,
      n === Xe ? l : 0,
      n.cancelPendingCommit !== null || n.timeoutHandle !== -1
    ), l === 0 ? null : (zv(n, l, r), iy(n, on()), n.callbackNode != null && n.callbackNode === o ? ay.bind(null, n) : null);
  }
  function ry(n, r) {
    if (Hc()) return null;
    zv(n, r, !0);
  }
  function SE() {
    xE(function() {
      (qe & 6) !== 0 ? Cu(
        Km,
        TE
      ) : ny();
    });
  }
  function mf() {
    if (ra === 0) {
      var n = wr;
      n === 0 && (n = Ko, Ko <<= 1, (Ko & 261888) === 0 && (Ko = 256)), ra = n;
    }
    return ra;
  }
  function sy(n) {
    return n == null || typeof n == "symbol" || typeof n == "boolean" ? null : typeof n == "function" ? n : $o("" + n);
  }
  function oy(n, r) {
    var o = r.ownerDocument.createElement("input");
    return o.name = r.name, o.value = r.value, n.id && o.setAttribute("form", n.id), r.parentNode.insertBefore(o, r), n = new FormData(n), o.parentNode.removeChild(o), n;
  }
  function kE(n, r, o, l, m) {
    if (r === "submit" && o && o.stateNode === m) {
      var g = sy(
        (m[Jt] || null).action
      ), b = l.submitter;
      b && (r = (r = b[Jt] || null) ? sy(r.formAction) : b.getAttribute("formAction"), r !== null && (g = r, b = null));
      var S = new ic(
        "action",
        "action",
        null,
        l,
        m
      );
      n.push({
        event: S,
        listeners: [
          {
            instance: null,
            listener: function() {
              if (l.defaultPrevented) {
                if (ra !== 0) {
                  var O = b ? oy(m, b) : new FormData(m);
                  xd(
                    o,
                    {
                      pending: !0,
                      data: O,
                      method: m.method,
                      action: g
                    },
                    null,
                    O
                  );
                }
              } else
                typeof g == "function" && (S.preventDefault(), O = b ? oy(m, b) : new FormData(m), xd(
                  o,
                  {
                    pending: !0,
                    data: O,
                    method: m.method,
                    action: g
                  },
                  g,
                  O
                ));
            },
            currentTarget: m
          }
        ]
      });
    }
  }
  for (var pf = 0; pf < Xu.length; pf++) {
    var gf = Xu[pf], CE = gf.toLowerCase(), EE = gf[0].toUpperCase() + gf.slice(1);
    In(
      CE,
      "on" + EE
    );
  }
  In(Bp, "onAnimationEnd"), In(zp, "onAnimationIteration"), In(Fp, "onAnimationStart"), In("dblclick", "onDoubleClick"), In("focusin", "onFocus"), In("focusout", "onBlur"), In(FC, "onTransitionRun"), In(jC, "onTransitionStart"), In(qC, "onTransitionCancel"), In(jp, "onTransitionEnd"), hr("onMouseEnter", ["mouseout", "mouseover"]), hr("onMouseLeave", ["mouseout", "mouseover"]), hr("onPointerEnter", ["pointerout", "pointerover"]), hr("onPointerLeave", ["pointerout", "pointerover"]), Aa(
    "onChange",
    "change click focusin focusout input keydown keyup selectionchange".split(" ")
  ), Aa(
    "onSelect",
    "focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange".split(
      " "
    )
  ), Aa("onBeforeInput", [
    "compositionend",
    "keypress",
    "textInput",
    "paste"
  ]), Aa(
    "onCompositionEnd",
    "compositionend focusout keydown keypress keyup mousedown".split(" ")
  ), Aa(
    "onCompositionStart",
    "compositionstart focusout keydown keypress keyup mousedown".split(" ")
  ), Aa(
    "onCompositionUpdate",
    "compositionupdate focusout keydown keypress keyup mousedown".split(" ")
  );
  var to = "abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting".split(
    " "
  ), _E = new Set(
    "beforetoggle cancel close invalid load scroll scrollend toggle".split(" ").concat(to)
  );
  function cy(n, r) {
    r = (r & 4) !== 0;
    for (var o = 0; o < n.length; o++) {
      var l = n[o], m = l.event;
      l = l.listeners;
      e: {
        var g = void 0;
        if (r)
          for (var b = l.length - 1; 0 <= b; b--) {
            var S = l[b], O = S.instance, z = S.currentTarget;
            if (S = S.listener, O !== g && m.isPropagationStopped())
              break e;
            g = S, m.currentTarget = z;
            try {
              g(m);
            } catch (Q) {
              sc(Q);
            }
            m.currentTarget = null, g = O;
          }
        else
          for (b = 0; b < l.length; b++) {
            if (S = l[b], O = S.instance, z = S.currentTarget, S = S.listener, O !== g && m.isPropagationStopped())
              break e;
            g = S, m.currentTarget = z;
            try {
              g(m);
            } catch (Q) {
              sc(Q);
            }
            m.currentTarget = null, g = O;
          }
      }
    }
  }
  function Pe(n, r) {
    var o = r[Mu];
    o === void 0 && (o = r[Mu] = /* @__PURE__ */ new Set());
    var l = n + "__bubble";
    o.has(l) || (ly(r, n, 2, !1), o.add(l));
  }
  function vf(n, r, o) {
    var l = 0;
    r && (l |= 4), ly(
      o,
      n,
      l,
      r
    );
  }
  var Kc = "_reactListening" + Math.random().toString(36).slice(2);
  function yf(n) {
    if (!n[Kc]) {
      n[Kc] = !0, np.forEach(function(o) {
        o !== "selectionchange" && (_E.has(o) || vf(o, !1, n), vf(o, !0, n));
      });
      var r = n.nodeType === 9 ? n : n.ownerDocument;
      r === null || r[Kc] || (r[Kc] = !0, vf("selectionchange", !1, r));
    }
  }
  function ly(n, r, o, l) {
    switch (By(r)) {
      case 2:
        var m = $E;
        break;
      case 8:
        m = e_;
        break;
      default:
        m = xf;
    }
    o = m.bind(
      null,
      r,
      o,
      n
    ), m = void 0, !Uu || r !== "touchstart" && r !== "touchmove" && r !== "wheel" || (m = !0), l ? m !== void 0 ? n.addEventListener(r, o, {
      capture: !0,
      passive: m
    }) : n.addEventListener(r, o, !0) : m !== void 0 ? n.addEventListener(r, o, {
      passive: m
    }) : n.addEventListener(r, o, !1);
  }
  function bf(n, r, o, l, m) {
    var g = l;
    if ((r & 1) === 0 && (r & 2) === 0 && l !== null)
      e: for (; ; ) {
        if (l === null) return;
        var b = l.tag;
        if (b === 3 || b === 4) {
          var S = l.stateNode.containerInfo;
          if (S === m) break;
          if (b === 4)
            for (b = l.return; b !== null; ) {
              var O = b.tag;
              if ((O === 3 || O === 4) && b.stateNode.containerInfo === m)
                return;
              b = b.return;
            }
          for (; S !== null; ) {
            if (b = ur(S), b === null) return;
            if (O = b.tag, O === 5 || O === 6 || O === 26 || O === 27) {
              l = g = b;
              continue e;
            }
            S = S.parentNode;
          }
        }
        l = l.return;
      }
    mp(function() {
      var z = g, Q = Iu(o), $ = [];
      e: {
        var F = qp.get(n);
        if (F !== void 0) {
          var V = ic, he = n;
          switch (n) {
            case "keypress":
              if (tc(o) === 0) break e;
            case "keydown":
            case "keyup":
              V = yC;
              break;
            case "focusin":
              he = "focus", V = ju;
              break;
            case "focusout":
              he = "blur", V = ju;
              break;
            case "beforeblur":
            case "afterblur":
              V = ju;
              break;
            case "click":
              if (o.button === 2) break e;
            case "auxclick":
            case "dblclick":
            case "mousedown":
            case "mousemove":
            case "mouseup":
            case "mouseout":
            case "mouseover":
            case "contextmenu":
              V = vp;
              break;
            case "drag":
            case "dragend":
            case "dragenter":
            case "dragexit":
            case "dragleave":
            case "dragover":
            case "dragstart":
            case "drop":
              V = sC;
              break;
            case "touchcancel":
            case "touchend":
            case "touchmove":
            case "touchstart":
              V = SC;
              break;
            case Bp:
            case zp:
            case Fp:
              V = lC;
              break;
            case jp:
              V = CC;
              break;
            case "scroll":
            case "scrollend":
              V = aC;
              break;
            case "wheel":
              V = _C;
              break;
            case "copy":
            case "cut":
            case "paste":
              V = dC;
              break;
            case "gotpointercapture":
            case "lostpointercapture":
            case "pointercancel":
            case "pointerdown":
            case "pointermove":
            case "pointerout":
            case "pointerover":
            case "pointerup":
              V = bp;
              break;
            case "toggle":
            case "beforetoggle":
              V = wC;
          }
          var be = (r & 4) !== 0, Je = !be && (n === "scroll" || n === "scrollend"), I = be ? F !== null ? F + "Capture" : null : F;
          be = [];
          for (var P = z, U; P !== null; ) {
            var Z = P;
            if (U = Z.stateNode, Z = Z.tag, Z !== 5 && Z !== 26 && Z !== 27 || U === null || I === null || (Z = Cs(P, I), Z != null && be.push(
              no(P, Z, U)
            )), Je) break;
            P = P.return;
          }
          0 < be.length && (F = new V(
            F,
            he,
            null,
            o,
            Q
          ), $.push({ event: F, listeners: be }));
        }
      }
      if ((r & 7) === 0) {
        e: {
          if (F = n === "mouseover" || n === "pointerover", V = n === "mouseout" || n === "pointerout", F && o !== Nu && (he = o.relatedTarget || o.fromElement) && (ur(he) || he[lr]))
            break e;
          if ((V || F) && (F = Q.window === Q ? Q : (F = Q.ownerDocument) ? F.defaultView || F.parentWindow : window, V ? (he = o.relatedTarget || o.toElement, V = z, he = he ? ur(he) : null, he !== null && (Je = c(he), be = he.tag, he !== Je || be !== 5 && be !== 27 && be !== 6) && (he = null)) : (V = null, he = z), V !== he)) {
            if (be = vp, Z = "onMouseLeave", I = "onMouseEnter", P = "mouse", (n === "pointerout" || n === "pointerover") && (be = bp, Z = "onPointerLeave", I = "onPointerEnter", P = "pointer"), Je = V == null ? F : ks(V), U = he == null ? F : ks(he), F = new be(
              Z,
              P + "leave",
              V,
              o,
              Q
            ), F.target = Je, F.relatedTarget = U, Z = null, ur(Q) === z && (be = new be(
              I,
              P + "enter",
              he,
              o,
              Q
            ), be.target = U, be.relatedTarget = Je, Z = be), Je = Z, V && he)
              t: {
                for (be = RE, I = V, P = he, U = 0, Z = I; Z; Z = be(Z))
                  U++;
                Z = 0;
                for (var ve = P; ve; ve = be(ve))
                  Z++;
                for (; 0 < U - Z; )
                  I = be(I), U--;
                for (; 0 < Z - U; )
                  P = be(P), Z--;
                for (; U--; ) {
                  if (I === P || P !== null && I === P.alternate) {
                    be = I;
                    break t;
                  }
                  I = be(I), P = be(P);
                }
                be = null;
              }
            else be = null;
            V !== null && uy(
              $,
              F,
              V,
              be,
              !1
            ), he !== null && Je !== null && uy(
              $,
              Je,
              he,
              be,
              !0
            );
          }
        }
        e: {
          if (F = z ? ks(z) : window, V = F.nodeName && F.nodeName.toLowerCase(), V === "select" || V === "input" && F.type === "file")
            var ze = wp;
          else if (_p(F))
            if (Mp)
              ze = UC;
            else {
              ze = IC;
              var me = NC;
            }
          else
            V = F.nodeName, !V || V.toLowerCase() !== "input" || F.type !== "checkbox" && F.type !== "radio" ? z && xu(z.elementType) && (ze = wp) : ze = LC;
          if (ze && (ze = ze(n, z))) {
            Rp(
              $,
              ze,
              o,
              Q
            );
            break e;
          }
          me && me(n, F, z), n === "focusout" && z && F.type === "number" && z.memoizedProps.value != null && Pu(F, "number", F.value);
        }
        switch (me = z ? ks(z) : window, n) {
          case "focusin":
            (_p(me) || me.contentEditable === "true") && (br = me, Yu = z, Ds = null);
            break;
          case "focusout":
            Ds = Yu = br = null;
            break;
          case "mousedown":
            Ju = !0;
            break;
          case "contextmenu":
          case "mouseup":
          case "dragend":
            Ju = !1, Lp($, o, Q);
            break;
          case "selectionchange":
            if (zC) break;
          case "keydown":
          case "keyup":
            Lp($, o, Q);
        }
        var we;
        if (Hu)
          e: {
            switch (n) {
              case "compositionstart":
                var Ie = "onCompositionStart";
                break e;
              case "compositionend":
                Ie = "onCompositionEnd";
                break e;
              case "compositionupdate":
                Ie = "onCompositionUpdate";
                break e;
            }
            Ie = void 0;
          }
        else
          yr ? Cp(n, o) && (Ie = "onCompositionEnd") : n === "keydown" && o.keyCode === 229 && (Ie = "onCompositionStart");
        Ie && (Tp && o.locale !== "ko" && (yr || Ie !== "onCompositionStart" ? Ie === "onCompositionEnd" && yr && (we = pp()) : (Hi = Q, Bu = "value" in Hi ? Hi.value : Hi.textContent, yr = !0)), me = Yc(z, Ie), 0 < me.length && (Ie = new yp(
          Ie,
          n,
          null,
          o,
          Q
        ), $.push({ event: Ie, listeners: me }), we ? Ie.data = we : (we = Ep(o), we !== null && (Ie.data = we)))), (we = AC ? OC(n, o) : DC(n, o)) && (Ie = Yc(z, "onBeforeInput"), 0 < Ie.length && (me = new yp(
          "onBeforeInput",
          "beforeinput",
          null,
          o,
          Q
        ), $.push({
          event: me,
          listeners: Ie
        }), me.data = we)), kE(
          $,
          n,
          z,
          o,
          Q
        );
      }
      cy($, r);
    });
  }
  function no(n, r, o) {
    return {
      instance: n,
      listener: r,
      currentTarget: o
    };
  }
  function Yc(n, r) {
    for (var o = r + "Capture", l = []; n !== null; ) {
      var m = n, g = m.stateNode;
      if (m = m.tag, m !== 5 && m !== 26 && m !== 27 || g === null || (m = Cs(n, o), m != null && l.unshift(
        no(n, m, g)
      ), m = Cs(n, r), m != null && l.push(
        no(n, m, g)
      )), n.tag === 3) return l;
      n = n.return;
    }
    return [];
  }
  function RE(n) {
    if (n === null) return null;
    do
      n = n.return;
    while (n && n.tag !== 5 && n.tag !== 27);
    return n || null;
  }
  function uy(n, r, o, l, m) {
    for (var g = r._reactName, b = []; o !== null && o !== l; ) {
      var S = o, O = S.alternate, z = S.stateNode;
      if (S = S.tag, O !== null && O === l) break;
      S !== 5 && S !== 26 && S !== 27 || z === null || (O = z, m ? (z = Cs(o, g), z != null && b.unshift(
        no(o, z, O)
      )) : m || (z = Cs(o, g), z != null && b.push(
        no(o, z, O)
      ))), o = o.return;
    }
    b.length !== 0 && n.push({ event: r, listeners: b });
  }
  var wE = /\r\n?/g, ME = /\u0000|\uFFFD/g;
  function dy(n) {
    return (typeof n == "string" ? n : "" + n).replace(wE, `
`).replace(ME, "");
  }
  function fy(n, r) {
    return r = dy(r), dy(n) === r;
  }
  function Ye(n, r, o, l, m, g) {
    switch (o) {
      case "children":
        typeof l == "string" ? r === "body" || r === "textarea" && l === "" || pr(n, l) : (typeof l == "number" || typeof l == "bigint") && r !== "body" && pr(n, "" + l);
        break;
      case "className":
        Zo(n, "class", l);
        break;
      case "tabIndex":
        Zo(n, "tabindex", l);
        break;
      case "dir":
      case "role":
      case "viewBox":
      case "width":
      case "height":
        Zo(n, o, l);
        break;
      case "style":
        fp(n, l, g);
        break;
      case "data":
        if (r !== "object") {
          Zo(n, "data", l);
          break;
        }
      case "src":
      case "href":
        if (l === "" && (r !== "a" || o !== "href")) {
          n.removeAttribute(o);
          break;
        }
        if (l == null || typeof l == "function" || typeof l == "symbol" || typeof l == "boolean") {
          n.removeAttribute(o);
          break;
        }
        l = $o("" + l), n.setAttribute(o, l);
        break;
      case "action":
      case "formAction":
        if (typeof l == "function") {
          n.setAttribute(
            o,
            "javascript:throw new Error('A React form was unexpectedly submitted. If you called form.submit() manually, consider using form.requestSubmit() instead. If you\\'re trying to use event.stopPropagation() in a submit event handler, consider also calling event.preventDefault().')"
          );
          break;
        } else
          typeof g == "function" && (o === "formAction" ? (r !== "input" && Ye(n, r, "name", m.name, m, null), Ye(
            n,
            r,
            "formEncType",
            m.formEncType,
            m,
            null
          ), Ye(
            n,
            r,
            "formMethod",
            m.formMethod,
            m,
            null
          ), Ye(
            n,
            r,
            "formTarget",
            m.formTarget,
            m,
            null
          )) : (Ye(n, r, "encType", m.encType, m, null), Ye(n, r, "method", m.method, m, null), Ye(n, r, "target", m.target, m, null)));
        if (l == null || typeof l == "symbol" || typeof l == "boolean") {
          n.removeAttribute(o);
          break;
        }
        l = $o("" + l), n.setAttribute(o, l);
        break;
      case "onClick":
        l != null && (n.onclick = di);
        break;
      case "onScroll":
        l != null && Pe("scroll", n);
        break;
      case "onScrollEnd":
        l != null && Pe("scrollend", n);
        break;
      case "dangerouslySetInnerHTML":
        if (l != null) {
          if (typeof l != "object" || !("__html" in l))
            throw Error(i(61));
          if (o = l.__html, o != null) {
            if (m.children != null) throw Error(i(60));
            n.innerHTML = o;
          }
        }
        break;
      case "multiple":
        n.multiple = l && typeof l != "function" && typeof l != "symbol";
        break;
      case "muted":
        n.muted = l && typeof l != "function" && typeof l != "symbol";
        break;
      case "suppressContentEditableWarning":
      case "suppressHydrationWarning":
      case "defaultValue":
      case "defaultChecked":
      case "innerHTML":
      case "ref":
        break;
      case "autoFocus":
        break;
      case "xlinkHref":
        if (l == null || typeof l == "function" || typeof l == "boolean" || typeof l == "symbol") {
          n.removeAttribute("xlink:href");
          break;
        }
        o = $o("" + l), n.setAttributeNS(
          "http://www.w3.org/1999/xlink",
          "xlink:href",
          o
        );
        break;
      case "contentEditable":
      case "spellCheck":
      case "draggable":
      case "value":
      case "autoReverse":
      case "externalResourcesRequired":
      case "focusable":
      case "preserveAlpha":
        l != null && typeof l != "function" && typeof l != "symbol" ? n.setAttribute(o, "" + l) : n.removeAttribute(o);
        break;
      case "inert":
      case "allowFullScreen":
      case "async":
      case "autoPlay":
      case "controls":
      case "default":
      case "defer":
      case "disabled":
      case "disablePictureInPicture":
      case "disableRemotePlayback":
      case "formNoValidate":
      case "hidden":
      case "loop":
      case "noModule":
      case "noValidate":
      case "open":
      case "playsInline":
      case "readOnly":
      case "required":
      case "reversed":
      case "scoped":
      case "seamless":
      case "itemScope":
        l && typeof l != "function" && typeof l != "symbol" ? n.setAttribute(o, "") : n.removeAttribute(o);
        break;
      case "capture":
      case "download":
        l === !0 ? n.setAttribute(o, "") : l !== !1 && l != null && typeof l != "function" && typeof l != "symbol" ? n.setAttribute(o, l) : n.removeAttribute(o);
        break;
      case "cols":
      case "rows":
      case "size":
      case "span":
        l != null && typeof l != "function" && typeof l != "symbol" && !isNaN(l) && 1 <= l ? n.setAttribute(o, l) : n.removeAttribute(o);
        break;
      case "rowSpan":
      case "start":
        l == null || typeof l == "function" || typeof l == "symbol" || isNaN(l) ? n.removeAttribute(o) : n.setAttribute(o, l);
        break;
      case "popover":
        Pe("beforetoggle", n), Pe("toggle", n), Xo(n, "popover", l);
        break;
      case "xlinkActuate":
        ui(
          n,
          "http://www.w3.org/1999/xlink",
          "xlink:actuate",
          l
        );
        break;
      case "xlinkArcrole":
        ui(
          n,
          "http://www.w3.org/1999/xlink",
          "xlink:arcrole",
          l
        );
        break;
      case "xlinkRole":
        ui(
          n,
          "http://www.w3.org/1999/xlink",
          "xlink:role",
          l
        );
        break;
      case "xlinkShow":
        ui(
          n,
          "http://www.w3.org/1999/xlink",
          "xlink:show",
          l
        );
        break;
      case "xlinkTitle":
        ui(
          n,
          "http://www.w3.org/1999/xlink",
          "xlink:title",
          l
        );
        break;
      case "xlinkType":
        ui(
          n,
          "http://www.w3.org/1999/xlink",
          "xlink:type",
          l
        );
        break;
      case "xmlBase":
        ui(
          n,
          "http://www.w3.org/XML/1998/namespace",
          "xml:base",
          l
        );
        break;
      case "xmlLang":
        ui(
          n,
          "http://www.w3.org/XML/1998/namespace",
          "xml:lang",
          l
        );
        break;
      case "xmlSpace":
        ui(
          n,
          "http://www.w3.org/XML/1998/namespace",
          "xml:space",
          l
        );
        break;
      case "is":
        Xo(n, "is", l);
        break;
      case "innerText":
      case "textContent":
        break;
      default:
        (!(2 < o.length) || o[0] !== "o" && o[0] !== "O" || o[1] !== "n" && o[1] !== "N") && (o = nC.get(o) || o, Xo(n, o, l));
    }
  }
  function Tf(n, r, o, l, m, g) {
    switch (o) {
      case "style":
        fp(n, l, g);
        break;
      case "dangerouslySetInnerHTML":
        if (l != null) {
          if (typeof l != "object" || !("__html" in l))
            throw Error(i(61));
          if (o = l.__html, o != null) {
            if (m.children != null) throw Error(i(60));
            n.innerHTML = o;
          }
        }
        break;
      case "children":
        typeof l == "string" ? pr(n, l) : (typeof l == "number" || typeof l == "bigint") && pr(n, "" + l);
        break;
      case "onScroll":
        l != null && Pe("scroll", n);
        break;
      case "onScrollEnd":
        l != null && Pe("scrollend", n);
        break;
      case "onClick":
        l != null && (n.onclick = di);
        break;
      case "suppressContentEditableWarning":
      case "suppressHydrationWarning":
      case "innerHTML":
      case "ref":
        break;
      case "innerText":
      case "textContent":
        break;
      default:
        if (!ip.hasOwnProperty(o))
          e: {
            if (o[0] === "o" && o[1] === "n" && (m = o.endsWith("Capture"), r = o.slice(2, m ? o.length - 7 : void 0), g = n[Jt] || null, g = g != null ? g[o] : null, typeof g == "function" && n.removeEventListener(r, g, m), typeof l == "function")) {
              typeof g != "function" && g !== null && (o in n ? n[o] = null : n.hasAttribute(o) && n.removeAttribute(o)), n.addEventListener(r, l, m);
              break e;
            }
            o in n ? n[o] = l : l === !0 ? n.setAttribute(o, "") : Xo(n, o, l);
          }
    }
  }
  function xt(n, r, o) {
    switch (r) {
      case "div":
      case "span":
      case "svg":
      case "path":
      case "a":
      case "g":
      case "p":
      case "li":
        break;
      case "img":
        Pe("error", n), Pe("load", n);
        var l = !1, m = !1, g;
        for (g in o)
          if (o.hasOwnProperty(g)) {
            var b = o[g];
            if (b != null)
              switch (g) {
                case "src":
                  l = !0;
                  break;
                case "srcSet":
                  m = !0;
                  break;
                case "children":
                case "dangerouslySetInnerHTML":
                  throw Error(i(137, r));
                default:
                  Ye(n, r, g, b, o, null);
              }
          }
        m && Ye(n, r, "srcSet", o.srcSet, o, null), l && Ye(n, r, "src", o.src, o, null);
        return;
      case "input":
        Pe("invalid", n);
        var S = g = b = m = null, O = null, z = null;
        for (l in o)
          if (o.hasOwnProperty(l)) {
            var Q = o[l];
            if (Q != null)
              switch (l) {
                case "name":
                  m = Q;
                  break;
                case "type":
                  b = Q;
                  break;
                case "checked":
                  O = Q;
                  break;
                case "defaultChecked":
                  z = Q;
                  break;
                case "value":
                  g = Q;
                  break;
                case "defaultValue":
                  S = Q;
                  break;
                case "children":
                case "dangerouslySetInnerHTML":
                  if (Q != null)
                    throw Error(i(137, r));
                  break;
                default:
                  Ye(n, r, l, Q, o, null);
              }
          }
        cp(
          n,
          g,
          S,
          O,
          z,
          b,
          m,
          !1
        );
        return;
      case "select":
        Pe("invalid", n), l = b = g = null;
        for (m in o)
          if (o.hasOwnProperty(m) && (S = o[m], S != null))
            switch (m) {
              case "value":
                g = S;
                break;
              case "defaultValue":
                b = S;
                break;
              case "multiple":
                l = S;
              default:
                Ye(n, r, m, S, o, null);
            }
        r = g, o = b, n.multiple = !!l, r != null ? mr(n, !!l, r, !1) : o != null && mr(n, !!l, o, !0);
        return;
      case "textarea":
        Pe("invalid", n), g = m = l = null;
        for (b in o)
          if (o.hasOwnProperty(b) && (S = o[b], S != null))
            switch (b) {
              case "value":
                l = S;
                break;
              case "defaultValue":
                m = S;
                break;
              case "children":
                g = S;
                break;
              case "dangerouslySetInnerHTML":
                if (S != null) throw Error(i(91));
                break;
              default:
                Ye(n, r, b, S, o, null);
            }
        up(n, l, m, g);
        return;
      case "option":
        for (O in o)
          if (o.hasOwnProperty(O) && (l = o[O], l != null))
            switch (O) {
              case "selected":
                n.selected = l && typeof l != "function" && typeof l != "symbol";
                break;
              default:
                Ye(n, r, O, l, o, null);
            }
        return;
      case "dialog":
        Pe("beforetoggle", n), Pe("toggle", n), Pe("cancel", n), Pe("close", n);
        break;
      case "iframe":
      case "object":
        Pe("load", n);
        break;
      case "video":
      case "audio":
        for (l = 0; l < to.length; l++)
          Pe(to[l], n);
        break;
      case "image":
        Pe("error", n), Pe("load", n);
        break;
      case "details":
        Pe("toggle", n);
        break;
      case "embed":
      case "source":
      case "link":
        Pe("error", n), Pe("load", n);
      case "area":
      case "base":
      case "br":
      case "col":
      case "hr":
      case "keygen":
      case "meta":
      case "param":
      case "track":
      case "wbr":
      case "menuitem":
        for (z in o)
          if (o.hasOwnProperty(z) && (l = o[z], l != null))
            switch (z) {
              case "children":
              case "dangerouslySetInnerHTML":
                throw Error(i(137, r));
              default:
                Ye(n, r, z, l, o, null);
            }
        return;
      default:
        if (xu(r)) {
          for (Q in o)
            o.hasOwnProperty(Q) && (l = o[Q], l !== void 0 && Tf(
              n,
              r,
              Q,
              l,
              o,
              void 0
            ));
          return;
        }
    }
    for (S in o)
      o.hasOwnProperty(S) && (l = o[S], l != null && Ye(n, r, S, l, o, null));
  }
  function AE(n, r, o, l) {
    switch (r) {
      case "div":
      case "span":
      case "svg":
      case "path":
      case "a":
      case "g":
      case "p":
      case "li":
        break;
      case "input":
        var m = null, g = null, b = null, S = null, O = null, z = null, Q = null;
        for (V in o) {
          var $ = o[V];
          if (o.hasOwnProperty(V) && $ != null)
            switch (V) {
              case "checked":
                break;
              case "value":
                break;
              case "defaultValue":
                O = $;
              default:
                l.hasOwnProperty(V) || Ye(n, r, V, null, l, $);
            }
        }
        for (var F in l) {
          var V = l[F];
          if ($ = o[F], l.hasOwnProperty(F) && (V != null || $ != null))
            switch (F) {
              case "type":
                g = V;
                break;
              case "name":
                m = V;
                break;
              case "checked":
                z = V;
                break;
              case "defaultChecked":
                Q = V;
                break;
              case "value":
                b = V;
                break;
              case "defaultValue":
                S = V;
                break;
              case "children":
              case "dangerouslySetInnerHTML":
                if (V != null)
                  throw Error(i(137, r));
                break;
              default:
                V !== $ && Ye(
                  n,
                  r,
                  F,
                  V,
                  l,
                  $
                );
            }
        }
        Du(
          n,
          b,
          S,
          O,
          z,
          Q,
          g,
          m
        );
        return;
      case "select":
        V = b = S = F = null;
        for (g in o)
          if (O = o[g], o.hasOwnProperty(g) && O != null)
            switch (g) {
              case "value":
                break;
              case "multiple":
                V = O;
              default:
                l.hasOwnProperty(g) || Ye(
                  n,
                  r,
                  g,
                  null,
                  l,
                  O
                );
            }
        for (m in l)
          if (g = l[m], O = o[m], l.hasOwnProperty(m) && (g != null || O != null))
            switch (m) {
              case "value":
                F = g;
                break;
              case "defaultValue":
                S = g;
                break;
              case "multiple":
                b = g;
              default:
                g !== O && Ye(
                  n,
                  r,
                  m,
                  g,
                  l,
                  O
                );
            }
        r = S, o = b, l = V, F != null ? mr(n, !!o, F, !1) : !!l != !!o && (r != null ? mr(n, !!o, r, !0) : mr(n, !!o, o ? [] : "", !1));
        return;
      case "textarea":
        V = F = null;
        for (S in o)
          if (m = o[S], o.hasOwnProperty(S) && m != null && !l.hasOwnProperty(S))
            switch (S) {
              case "value":
                break;
              case "children":
                break;
              default:
                Ye(n, r, S, null, l, m);
            }
        for (b in l)
          if (m = l[b], g = o[b], l.hasOwnProperty(b) && (m != null || g != null))
            switch (b) {
              case "value":
                F = m;
                break;
              case "defaultValue":
                V = m;
                break;
              case "children":
                break;
              case "dangerouslySetInnerHTML":
                if (m != null) throw Error(i(91));
                break;
              default:
                m !== g && Ye(n, r, b, m, l, g);
            }
        lp(n, F, V);
        return;
      case "option":
        for (var he in o)
          if (F = o[he], o.hasOwnProperty(he) && F != null && !l.hasOwnProperty(he))
            switch (he) {
              case "selected":
                n.selected = !1;
                break;
              default:
                Ye(
                  n,
                  r,
                  he,
                  null,
                  l,
                  F
                );
            }
        for (O in l)
          if (F = l[O], V = o[O], l.hasOwnProperty(O) && F !== V && (F != null || V != null))
            switch (O) {
              case "selected":
                n.selected = F && typeof F != "function" && typeof F != "symbol";
                break;
              default:
                Ye(
                  n,
                  r,
                  O,
                  F,
                  l,
                  V
                );
            }
        return;
      case "img":
      case "link":
      case "area":
      case "base":
      case "br":
      case "col":
      case "embed":
      case "hr":
      case "keygen":
      case "meta":
      case "param":
      case "source":
      case "track":
      case "wbr":
      case "menuitem":
        for (var be in o)
          F = o[be], o.hasOwnProperty(be) && F != null && !l.hasOwnProperty(be) && Ye(n, r, be, null, l, F);
        for (z in l)
          if (F = l[z], V = o[z], l.hasOwnProperty(z) && F !== V && (F != null || V != null))
            switch (z) {
              case "children":
              case "dangerouslySetInnerHTML":
                if (F != null)
                  throw Error(i(137, r));
                break;
              default:
                Ye(
                  n,
                  r,
                  z,
                  F,
                  l,
                  V
                );
            }
        return;
      default:
        if (xu(r)) {
          for (var Je in o)
            F = o[Je], o.hasOwnProperty(Je) && F !== void 0 && !l.hasOwnProperty(Je) && Tf(
              n,
              r,
              Je,
              void 0,
              l,
              F
            );
          for (Q in l)
            F = l[Q], V = o[Q], !l.hasOwnProperty(Q) || F === V || F === void 0 && V === void 0 || Tf(
              n,
              r,
              Q,
              F,
              l,
              V
            );
          return;
        }
    }
    for (var I in o)
      F = o[I], o.hasOwnProperty(I) && F != null && !l.hasOwnProperty(I) && Ye(n, r, I, null, l, F);
    for ($ in l)
      F = l[$], V = o[$], !l.hasOwnProperty($) || F === V || F == null && V == null || Ye(n, r, $, F, l, V);
  }
  function hy(n) {
    switch (n) {
      case "css":
      case "script":
      case "font":
      case "img":
      case "image":
      case "input":
      case "link":
        return !0;
      default:
        return !1;
    }
  }
  function OE() {
    if (typeof performance.getEntriesByType == "function") {
      for (var n = 0, r = 0, o = performance.getEntriesByType("resource"), l = 0; l < o.length; l++) {
        var m = o[l], g = m.transferSize, b = m.initiatorType, S = m.duration;
        if (g && S && hy(b)) {
          for (b = 0, S = m.responseEnd, l += 1; l < o.length; l++) {
            var O = o[l], z = O.startTime;
            if (z > S) break;
            var Q = O.transferSize, $ = O.initiatorType;
            Q && hy($) && (O = O.responseEnd, b += Q * (O < S ? 1 : (S - z) / (O - z)));
          }
          if (--l, r += 8 * (g + b) / (m.duration / 1e3), n++, 10 < n) break;
        }
      }
      if (0 < n) return r / n / 1e6;
    }
    return navigator.connection && (n = navigator.connection.downlink, typeof n == "number") ? n : 5;
  }
  var Sf = null, kf = null;
  function Jc(n) {
    return n.nodeType === 9 ? n : n.ownerDocument;
  }
  function my(n) {
    switch (n) {
      case "http://www.w3.org/2000/svg":
        return 1;
      case "http://www.w3.org/1998/Math/MathML":
        return 2;
      default:
        return 0;
    }
  }
  function py(n, r) {
    if (n === 0)
      switch (r) {
        case "svg":
          return 1;
        case "math":
          return 2;
        default:
          return 0;
      }
    return n === 1 && r === "foreignObject" ? 0 : n;
  }
  function Cf(n, r) {
    return n === "textarea" || n === "noscript" || typeof r.children == "string" || typeof r.children == "number" || typeof r.children == "bigint" || typeof r.dangerouslySetInnerHTML == "object" && r.dangerouslySetInnerHTML !== null && r.dangerouslySetInnerHTML.__html != null;
  }
  var Ef = null;
  function DE() {
    var n = window.event;
    return n && n.type === "popstate" ? n === Ef ? !1 : (Ef = n, !0) : (Ef = null, !1);
  }
  var gy = typeof setTimeout == "function" ? setTimeout : void 0, PE = typeof clearTimeout == "function" ? clearTimeout : void 0, vy = typeof Promise == "function" ? Promise : void 0, xE = typeof queueMicrotask == "function" ? queueMicrotask : typeof vy < "u" ? function(n) {
    return vy.resolve(null).then(n).catch(NE);
  } : gy;
  function NE(n) {
    setTimeout(function() {
      throw n;
    });
  }
  function sa(n) {
    return n === "head";
  }
  function yy(n, r) {
    var o = r, l = 0;
    do {
      var m = o.nextSibling;
      if (n.removeChild(o), m && m.nodeType === 8)
        if (o = m.data, o === "/$" || o === "/&") {
          if (l === 0) {
            n.removeChild(m), Kr(r);
            return;
          }
          l--;
        } else if (o === "$" || o === "$?" || o === "$~" || o === "$!" || o === "&")
          l++;
        else if (o === "html")
          io(n.ownerDocument.documentElement);
        else if (o === "head") {
          o = n.ownerDocument.head, io(o);
          for (var g = o.firstChild; g; ) {
            var b = g.nextSibling, S = g.nodeName;
            g[Ss] || S === "SCRIPT" || S === "STYLE" || S === "LINK" && g.rel.toLowerCase() === "stylesheet" || o.removeChild(g), g = b;
          }
        } else
          o === "body" && io(n.ownerDocument.body);
      o = m;
    } while (o);
    Kr(r);
  }
  function by(n, r) {
    var o = n;
    n = 0;
    do {
      var l = o.nextSibling;
      if (o.nodeType === 1 ? r ? (o._stashedDisplay = o.style.display, o.style.display = "none") : (o.style.display = o._stashedDisplay || "", o.getAttribute("style") === "" && o.removeAttribute("style")) : o.nodeType === 3 && (r ? (o._stashedText = o.nodeValue, o.nodeValue = "") : o.nodeValue = o._stashedText || ""), l && l.nodeType === 8)
        if (o = l.data, o === "/$") {
          if (n === 0) break;
          n--;
        } else
          o !== "$" && o !== "$?" && o !== "$~" && o !== "$!" || n++;
      o = l;
    } while (o);
  }
  function _f(n) {
    var r = n.firstChild;
    for (r && r.nodeType === 10 && (r = r.nextSibling); r; ) {
      var o = r;
      switch (r = r.nextSibling, o.nodeName) {
        case "HTML":
        case "HEAD":
        case "BODY":
          _f(o), Au(o);
          continue;
        case "SCRIPT":
        case "STYLE":
          continue;
        case "LINK":
          if (o.rel.toLowerCase() === "stylesheet") continue;
      }
      n.removeChild(o);
    }
  }
  function IE(n, r, o, l) {
    for (; n.nodeType === 1; ) {
      var m = o;
      if (n.nodeName.toLowerCase() !== r.toLowerCase()) {
        if (!l && (n.nodeName !== "INPUT" || n.type !== "hidden"))
          break;
      } else if (l) {
        if (!n[Ss])
          switch (r) {
            case "meta":
              if (!n.hasAttribute("itemprop")) break;
              return n;
            case "link":
              if (g = n.getAttribute("rel"), g === "stylesheet" && n.hasAttribute("data-precedence"))
                break;
              if (g !== m.rel || n.getAttribute("href") !== (m.href == null || m.href === "" ? null : m.href) || n.getAttribute("crossorigin") !== (m.crossOrigin == null ? null : m.crossOrigin) || n.getAttribute("title") !== (m.title == null ? null : m.title))
                break;
              return n;
            case "style":
              if (n.hasAttribute("data-precedence")) break;
              return n;
            case "script":
              if (g = n.getAttribute("src"), (g !== (m.src == null ? null : m.src) || n.getAttribute("type") !== (m.type == null ? null : m.type) || n.getAttribute("crossorigin") !== (m.crossOrigin == null ? null : m.crossOrigin)) && g && n.hasAttribute("async") && !n.hasAttribute("itemprop"))
                break;
              return n;
            default:
              return n;
          }
      } else if (r === "input" && n.type === "hidden") {
        var g = m.name == null ? null : "" + m.name;
        if (m.type === "hidden" && n.getAttribute("name") === g)
          return n;
      } else return n;
      if (n = wn(n.nextSibling), n === null) break;
    }
    return null;
  }
  function LE(n, r, o) {
    if (r === "") return null;
    for (; n.nodeType !== 3; )
      if ((n.nodeType !== 1 || n.nodeName !== "INPUT" || n.type !== "hidden") && !o || (n = wn(n.nextSibling), n === null)) return null;
    return n;
  }
  function Ty(n, r) {
    for (; n.nodeType !== 8; )
      if ((n.nodeType !== 1 || n.nodeName !== "INPUT" || n.type !== "hidden") && !r || (n = wn(n.nextSibling), n === null)) return null;
    return n;
  }
  function Rf(n) {
    return n.data === "$?" || n.data === "$~";
  }
  function wf(n) {
    return n.data === "$!" || n.data === "$?" && n.ownerDocument.readyState !== "loading";
  }
  function UE(n, r) {
    var o = n.ownerDocument;
    if (n.data === "$~") n._reactRetry = r;
    else if (n.data !== "$?" || o.readyState !== "loading")
      r();
    else {
      var l = function() {
        r(), o.removeEventListener("DOMContentLoaded", l);
      };
      o.addEventListener("DOMContentLoaded", l), n._reactRetry = l;
    }
  }
  function wn(n) {
    for (; n != null; n = n.nextSibling) {
      var r = n.nodeType;
      if (r === 1 || r === 3) break;
      if (r === 8) {
        if (r = n.data, r === "$" || r === "$!" || r === "$?" || r === "$~" || r === "&" || r === "F!" || r === "F")
          break;
        if (r === "/$" || r === "/&") return null;
      }
    }
    return n;
  }
  var Mf = null;
  function Sy(n) {
    n = n.nextSibling;
    for (var r = 0; n; ) {
      if (n.nodeType === 8) {
        var o = n.data;
        if (o === "/$" || o === "/&") {
          if (r === 0)
            return wn(n.nextSibling);
          r--;
        } else
          o !== "$" && o !== "$!" && o !== "$?" && o !== "$~" && o !== "&" || r++;
      }
      n = n.nextSibling;
    }
    return null;
  }
  function ky(n) {
    n = n.previousSibling;
    for (var r = 0; n; ) {
      if (n.nodeType === 8) {
        var o = n.data;
        if (o === "$" || o === "$!" || o === "$?" || o === "$~" || o === "&") {
          if (r === 0) return n;
          r--;
        } else o !== "/$" && o !== "/&" || r++;
      }
      n = n.previousSibling;
    }
    return null;
  }
  function Cy(n, r, o) {
    switch (r = Jc(o), n) {
      case "html":
        if (n = r.documentElement, !n) throw Error(i(452));
        return n;
      case "head":
        if (n = r.head, !n) throw Error(i(453));
        return n;
      case "body":
        if (n = r.body, !n) throw Error(i(454));
        return n;
      default:
        throw Error(i(451));
    }
  }
  function io(n) {
    for (var r = n.attributes; r.length; )
      n.removeAttributeNode(r[0]);
    Au(n);
  }
  var Mn = /* @__PURE__ */ new Map(), Ey = /* @__PURE__ */ new Set();
  function Qc(n) {
    return typeof n.getRootNode == "function" ? n.getRootNode() : n.nodeType === 9 ? n : n.ownerDocument;
  }
  var wi = te.d;
  te.d = {
    f: BE,
    r: zE,
    D: FE,
    C: jE,
    L: qE,
    m: HE,
    X: GE,
    S: VE,
    M: KE
  };
  function BE() {
    var n = wi.f(), r = Fc();
    return n || r;
  }
  function zE(n) {
    var r = dr(n);
    r !== null && r.tag === 5 && r.type === "form" ? jg(r) : wi.r(n);
  }
  var Hr = typeof document > "u" ? null : document;
  function _y(n, r, o) {
    var l = Hr;
    if (l && typeof r == "string" && r) {
      var m = Tn(r);
      m = 'link[rel="' + n + '"][href="' + m + '"]', typeof o == "string" && (m += '[crossorigin="' + o + '"]'), Ey.has(m) || (Ey.add(m), n = { rel: n, crossOrigin: o, href: r }, l.querySelector(m) === null && (r = l.createElement("link"), xt(r, "link", n), Ct(r), l.head.appendChild(r)));
    }
  }
  function FE(n) {
    wi.D(n), _y("dns-prefetch", n, null);
  }
  function jE(n, r) {
    wi.C(n, r), _y("preconnect", n, r);
  }
  function qE(n, r, o) {
    wi.L(n, r, o);
    var l = Hr;
    if (l && n && r) {
      var m = 'link[rel="preload"][as="' + Tn(r) + '"]';
      r === "image" && o && o.imageSrcSet ? (m += '[imagesrcset="' + Tn(
        o.imageSrcSet
      ) + '"]', typeof o.imageSizes == "string" && (m += '[imagesizes="' + Tn(
        o.imageSizes
      ) + '"]')) : m += '[href="' + Tn(n) + '"]';
      var g = m;
      switch (r) {
        case "style":
          g = Vr(n);
          break;
        case "script":
          g = Gr(n);
      }
      Mn.has(g) || (n = v(
        {
          rel: "preload",
          href: r === "image" && o && o.imageSrcSet ? void 0 : n,
          as: r
        },
        o
      ), Mn.set(g, n), l.querySelector(m) !== null || r === "style" && l.querySelector(ao(g)) || r === "script" && l.querySelector(ro(g)) || (r = l.createElement("link"), xt(r, "link", n), Ct(r), l.head.appendChild(r)));
    }
  }
  function HE(n, r) {
    wi.m(n, r);
    var o = Hr;
    if (o && n) {
      var l = r && typeof r.as == "string" ? r.as : "script", m = 'link[rel="modulepreload"][as="' + Tn(l) + '"][href="' + Tn(n) + '"]', g = m;
      switch (l) {
        case "audioworklet":
        case "paintworklet":
        case "serviceworker":
        case "sharedworker":
        case "worker":
        case "script":
          g = Gr(n);
      }
      if (!Mn.has(g) && (n = v({ rel: "modulepreload", href: n }, r), Mn.set(g, n), o.querySelector(m) === null)) {
        switch (l) {
          case "audioworklet":
          case "paintworklet":
          case "serviceworker":
          case "sharedworker":
          case "worker":
          case "script":
            if (o.querySelector(ro(g)))
              return;
        }
        l = o.createElement("link"), xt(l, "link", n), Ct(l), o.head.appendChild(l);
      }
    }
  }
  function VE(n, r, o) {
    wi.S(n, r, o);
    var l = Hr;
    if (l && n) {
      var m = fr(l).hoistableStyles, g = Vr(n);
      r = r || "default";
      var b = m.get(g);
      if (!b) {
        var S = { loading: 0, preload: null };
        if (b = l.querySelector(
          ao(g)
        ))
          S.loading = 5;
        else {
          n = v(
            { rel: "stylesheet", href: n, "data-precedence": r },
            o
          ), (o = Mn.get(g)) && Af(n, o);
          var O = b = l.createElement("link");
          Ct(O), xt(O, "link", n), O._p = new Promise(function(z, Q) {
            O.onload = z, O.onerror = Q;
          }), O.addEventListener("load", function() {
            S.loading |= 1;
          }), O.addEventListener("error", function() {
            S.loading |= 2;
          }), S.loading |= 4, Xc(b, r, l);
        }
        b = {
          type: "stylesheet",
          instance: b,
          count: 1,
          state: S
        }, m.set(g, b);
      }
    }
  }
  function GE(n, r) {
    wi.X(n, r);
    var o = Hr;
    if (o && n) {
      var l = fr(o).hoistableScripts, m = Gr(n), g = l.get(m);
      g || (g = o.querySelector(ro(m)), g || (n = v({ src: n, async: !0 }, r), (r = Mn.get(m)) && Of(n, r), g = o.createElement("script"), Ct(g), xt(g, "link", n), o.head.appendChild(g)), g = {
        type: "script",
        instance: g,
        count: 1,
        state: null
      }, l.set(m, g));
    }
  }
  function KE(n, r) {
    wi.M(n, r);
    var o = Hr;
    if (o && n) {
      var l = fr(o).hoistableScripts, m = Gr(n), g = l.get(m);
      g || (g = o.querySelector(ro(m)), g || (n = v({ src: n, async: !0, type: "module" }, r), (r = Mn.get(m)) && Of(n, r), g = o.createElement("script"), Ct(g), xt(g, "link", n), o.head.appendChild(g)), g = {
        type: "script",
        instance: g,
        count: 1,
        state: null
      }, l.set(m, g));
    }
  }
  function Ry(n, r, o, l) {
    var m = (m = oe.current) ? Qc(m) : null;
    if (!m) throw Error(i(446));
    switch (n) {
      case "meta":
      case "title":
        return null;
      case "style":
        return typeof o.precedence == "string" && typeof o.href == "string" ? (r = Vr(o.href), o = fr(
          m
        ).hoistableStyles, l = o.get(r), l || (l = {
          type: "style",
          instance: null,
          count: 0,
          state: null
        }, o.set(r, l)), l) : { type: "void", instance: null, count: 0, state: null };
      case "link":
        if (o.rel === "stylesheet" && typeof o.href == "string" && typeof o.precedence == "string") {
          n = Vr(o.href);
          var g = fr(
            m
          ).hoistableStyles, b = g.get(n);
          if (b || (m = m.ownerDocument || m, b = {
            type: "stylesheet",
            instance: null,
            count: 0,
            state: { loading: 0, preload: null }
          }, g.set(n, b), (g = m.querySelector(
            ao(n)
          )) && !g._p && (b.instance = g, b.state.loading = 5), Mn.has(n) || (o = {
            rel: "preload",
            as: "style",
            href: o.href,
            crossOrigin: o.crossOrigin,
            integrity: o.integrity,
            media: o.media,
            hrefLang: o.hrefLang,
            referrerPolicy: o.referrerPolicy
          }, Mn.set(n, o), g || YE(
            m,
            n,
            o,
            b.state
          ))), r && l === null)
            throw Error(i(528, ""));
          return b;
        }
        if (r && l !== null)
          throw Error(i(529, ""));
        return null;
      case "script":
        return r = o.async, o = o.src, typeof o == "string" && r && typeof r != "function" && typeof r != "symbol" ? (r = Gr(o), o = fr(
          m
        ).hoistableScripts, l = o.get(r), l || (l = {
          type: "script",
          instance: null,
          count: 0,
          state: null
        }, o.set(r, l)), l) : { type: "void", instance: null, count: 0, state: null };
      default:
        throw Error(i(444, n));
    }
  }
  function Vr(n) {
    return 'href="' + Tn(n) + '"';
  }
  function ao(n) {
    return 'link[rel="stylesheet"][' + n + "]";
  }
  function wy(n) {
    return v({}, n, {
      "data-precedence": n.precedence,
      precedence: null
    });
  }
  function YE(n, r, o, l) {
    n.querySelector('link[rel="preload"][as="style"][' + r + "]") ? l.loading = 1 : (r = n.createElement("link"), l.preload = r, r.addEventListener("load", function() {
      return l.loading |= 1;
    }), r.addEventListener("error", function() {
      return l.loading |= 2;
    }), xt(r, "link", o), Ct(r), n.head.appendChild(r));
  }
  function Gr(n) {
    return '[src="' + Tn(n) + '"]';
  }
  function ro(n) {
    return "script[async]" + n;
  }
  function My(n, r, o) {
    if (r.count++, r.instance === null)
      switch (r.type) {
        case "style":
          var l = n.querySelector(
            'style[data-href~="' + Tn(o.href) + '"]'
          );
          if (l)
            return r.instance = l, Ct(l), l;
          var m = v({}, o, {
            "data-href": o.href,
            "data-precedence": o.precedence,
            href: null,
            precedence: null
          });
          return l = (n.ownerDocument || n).createElement(
            "style"
          ), Ct(l), xt(l, "style", m), Xc(l, o.precedence, n), r.instance = l;
        case "stylesheet":
          m = Vr(o.href);
          var g = n.querySelector(
            ao(m)
          );
          if (g)
            return r.state.loading |= 4, r.instance = g, Ct(g), g;
          l = wy(o), (m = Mn.get(m)) && Af(l, m), g = (n.ownerDocument || n).createElement("link"), Ct(g);
          var b = g;
          return b._p = new Promise(function(S, O) {
            b.onload = S, b.onerror = O;
          }), xt(g, "link", l), r.state.loading |= 4, Xc(g, o.precedence, n), r.instance = g;
        case "script":
          return g = Gr(o.src), (m = n.querySelector(
            ro(g)
          )) ? (r.instance = m, Ct(m), m) : (l = o, (m = Mn.get(g)) && (l = v({}, o), Of(l, m)), n = n.ownerDocument || n, m = n.createElement("script"), Ct(m), xt(m, "link", l), n.head.appendChild(m), r.instance = m);
        case "void":
          return null;
        default:
          throw Error(i(443, r.type));
      }
    else
      r.type === "stylesheet" && (r.state.loading & 4) === 0 && (l = r.instance, r.state.loading |= 4, Xc(l, o.precedence, n));
    return r.instance;
  }
  function Xc(n, r, o) {
    for (var l = o.querySelectorAll(
      'link[rel="stylesheet"][data-precedence],style[data-precedence]'
    ), m = l.length ? l[l.length - 1] : null, g = m, b = 0; b < l.length; b++) {
      var S = l[b];
      if (S.dataset.precedence === r) g = S;
      else if (g !== m) break;
    }
    g ? g.parentNode.insertBefore(n, g.nextSibling) : (r = o.nodeType === 9 ? o.head : o, r.insertBefore(n, r.firstChild));
  }
  function Af(n, r) {
    n.crossOrigin == null && (n.crossOrigin = r.crossOrigin), n.referrerPolicy == null && (n.referrerPolicy = r.referrerPolicy), n.title == null && (n.title = r.title);
  }
  function Of(n, r) {
    n.crossOrigin == null && (n.crossOrigin = r.crossOrigin), n.referrerPolicy == null && (n.referrerPolicy = r.referrerPolicy), n.integrity == null && (n.integrity = r.integrity);
  }
  var Zc = null;
  function Ay(n, r, o) {
    if (Zc === null) {
      var l = /* @__PURE__ */ new Map(), m = Zc = /* @__PURE__ */ new Map();
      m.set(o, l);
    } else
      m = Zc, l = m.get(o), l || (l = /* @__PURE__ */ new Map(), m.set(o, l));
    if (l.has(n)) return l;
    for (l.set(n, null), o = o.getElementsByTagName(n), m = 0; m < o.length; m++) {
      var g = o[m];
      if (!(g[Ss] || g[At] || n === "link" && g.getAttribute("rel") === "stylesheet") && g.namespaceURI !== "http://www.w3.org/2000/svg") {
        var b = g.getAttribute(r) || "";
        b = n + b;
        var S = l.get(b);
        S ? S.push(g) : l.set(b, [g]);
      }
    }
    return l;
  }
  function Oy(n, r, o) {
    n = n.ownerDocument || n, n.head.insertBefore(
      o,
      r === "title" ? n.querySelector("head > title") : null
    );
  }
  function JE(n, r, o) {
    if (o === 1 || r.itemProp != null) return !1;
    switch (n) {
      case "meta":
      case "title":
        return !0;
      case "style":
        if (typeof r.precedence != "string" || typeof r.href != "string" || r.href === "")
          break;
        return !0;
      case "link":
        if (typeof r.rel != "string" || typeof r.href != "string" || r.href === "" || r.onLoad || r.onError)
          break;
        switch (r.rel) {
          case "stylesheet":
            return n = r.disabled, typeof r.precedence == "string" && n == null;
          default:
            return !0;
        }
      case "script":
        if (r.async && typeof r.async != "function" && typeof r.async != "symbol" && !r.onLoad && !r.onError && r.src && typeof r.src == "string")
          return !0;
    }
    return !1;
  }
  function Dy(n) {
    return !(n.type === "stylesheet" && (n.state.loading & 3) === 0);
  }
  function QE(n, r, o, l) {
    if (o.type === "stylesheet" && (typeof l.media != "string" || matchMedia(l.media).matches !== !1) && (o.state.loading & 4) === 0) {
      if (o.instance === null) {
        var m = Vr(l.href), g = r.querySelector(
          ao(m)
        );
        if (g) {
          r = g._p, r !== null && typeof r == "object" && typeof r.then == "function" && (n.count++, n = Wc.bind(n), r.then(n, n)), o.state.loading |= 4, o.instance = g, Ct(g);
          return;
        }
        g = r.ownerDocument || r, l = wy(l), (m = Mn.get(m)) && Af(l, m), g = g.createElement("link"), Ct(g);
        var b = g;
        b._p = new Promise(function(S, O) {
          b.onload = S, b.onerror = O;
        }), xt(g, "link", l), o.instance = g;
      }
      n.stylesheets === null && (n.stylesheets = /* @__PURE__ */ new Map()), n.stylesheets.set(o, r), (r = o.state.preload) && (o.state.loading & 3) === 0 && (n.count++, o = Wc.bind(n), r.addEventListener("load", o), r.addEventListener("error", o));
    }
  }
  var Df = 0;
  function XE(n, r) {
    return n.stylesheets && n.count === 0 && el(n, n.stylesheets), 0 < n.count || 0 < n.imgCount ? function(o) {
      var l = setTimeout(function() {
        if (n.stylesheets && el(n, n.stylesheets), n.unsuspend) {
          var g = n.unsuspend;
          n.unsuspend = null, g();
        }
      }, 6e4 + r);
      0 < n.imgBytes && Df === 0 && (Df = 62500 * OE());
      var m = setTimeout(
        function() {
          if (n.waitingForImages = !1, n.count === 0 && (n.stylesheets && el(n, n.stylesheets), n.unsuspend)) {
            var g = n.unsuspend;
            n.unsuspend = null, g();
          }
        },
        (n.imgBytes > Df ? 50 : 800) + r
      );
      return n.unsuspend = o, function() {
        n.unsuspend = null, clearTimeout(l), clearTimeout(m);
      };
    } : null;
  }
  function Wc() {
    if (this.count--, this.count === 0 && (this.imgCount === 0 || !this.waitingForImages)) {
      if (this.stylesheets) el(this, this.stylesheets);
      else if (this.unsuspend) {
        var n = this.unsuspend;
        this.unsuspend = null, n();
      }
    }
  }
  var $c = null;
  function el(n, r) {
    n.stylesheets = null, n.unsuspend !== null && (n.count++, $c = /* @__PURE__ */ new Map(), r.forEach(ZE, n), $c = null, Wc.call(n));
  }
  function ZE(n, r) {
    if (!(r.state.loading & 4)) {
      var o = $c.get(n);
      if (o) var l = o.get(null);
      else {
        o = /* @__PURE__ */ new Map(), $c.set(n, o);
        for (var m = n.querySelectorAll(
          "link[data-precedence],style[data-precedence]"
        ), g = 0; g < m.length; g++) {
          var b = m[g];
          (b.nodeName === "LINK" || b.getAttribute("media") !== "not all") && (o.set(b.dataset.precedence, b), l = b);
        }
        l && o.set(null, l);
      }
      m = r.instance, b = m.getAttribute("data-precedence"), g = o.get(b) || l, g === l && o.set(null, m), o.set(b, m), this.count++, l = Wc.bind(this), m.addEventListener("load", l), m.addEventListener("error", l), g ? g.parentNode.insertBefore(m, g.nextSibling) : (n = n.nodeType === 9 ? n.head : n, n.insertBefore(m, n.firstChild)), r.state.loading |= 4;
    }
  }
  var so = {
    $$typeof: ie,
    Provider: null,
    Consumer: null,
    _currentValue: le,
    _currentValue2: le,
    _threadCount: 0
  };
  function WE(n, r, o, l, m, g, b, S, O) {
    this.tag = 1, this.containerInfo = n, this.pingCache = this.current = this.pendingChildren = null, this.timeoutHandle = -1, this.callbackNode = this.next = this.pendingContext = this.context = this.cancelPendingCommit = null, this.callbackPriority = 0, this.expirationTimes = _u(-1), this.entangledLanes = this.shellSuspendCounter = this.errorRecoveryDisabledLanes = this.expiredLanes = this.warmLanes = this.pingedLanes = this.suspendedLanes = this.pendingLanes = 0, this.entanglements = _u(0), this.hiddenUpdates = _u(null), this.identifierPrefix = l, this.onUncaughtError = m, this.onCaughtError = g, this.onRecoverableError = b, this.pooledCache = null, this.pooledCacheLanes = 0, this.formState = O, this.incompleteTransitions = /* @__PURE__ */ new Map();
  }
  function Py(n, r, o, l, m, g, b, S, O, z, Q, $) {
    return n = new WE(
      n,
      r,
      o,
      b,
      O,
      z,
      Q,
      $,
      S
    ), r = 1, g === !0 && (r |= 24), g = dn(3, null, null, r), n.current = g, g.stateNode = n, r = ld(), r.refCount++, n.pooledCache = r, r.refCount++, g.memoizedState = {
      element: l,
      isDehydrated: o,
      cache: r
    }, hd(g), n;
  }
  function xy(n) {
    return n ? (n = kr, n) : kr;
  }
  function Ny(n, r, o, l, m, g) {
    m = xy(m), l.context === null ? l.context = m : l.pendingContext = m, l = Qi(r), l.payload = { element: o }, g = g === void 0 ? null : g, g !== null && (l.callback = g), o = Xi(n, l, r), o !== null && (en(o, n, r), Bs(o, n, r));
  }
  function Iy(n, r) {
    if (n = n.memoizedState, n !== null && n.dehydrated !== null) {
      var o = n.retryLane;
      n.retryLane = o !== 0 && o < r ? o : r;
    }
  }
  function Pf(n, r) {
    Iy(n, r), (n = n.alternate) && Iy(n, r);
  }
  function Ly(n) {
    if (n.tag === 13 || n.tag === 31) {
      var r = xa(n, 67108864);
      r !== null && en(r, n, 67108864), Pf(n, 67108864);
    }
  }
  function Uy(n) {
    if (n.tag === 13 || n.tag === 31) {
      var r = gn();
      r = Ru(r);
      var o = xa(n, r);
      o !== null && en(o, n, r), Pf(n, r);
    }
  }
  var tl = !0;
  function $E(n, r, o, l) {
    var m = q.T;
    q.T = null;
    var g = te.p;
    try {
      te.p = 2, xf(n, r, o, l);
    } finally {
      te.p = g, q.T = m;
    }
  }
  function e_(n, r, o, l) {
    var m = q.T;
    q.T = null;
    var g = te.p;
    try {
      te.p = 8, xf(n, r, o, l);
    } finally {
      te.p = g, q.T = m;
    }
  }
  function xf(n, r, o, l) {
    if (tl) {
      var m = Nf(l);
      if (m === null)
        bf(
          n,
          r,
          l,
          nl,
          o
        ), zy(n, l);
      else if (n_(
        m,
        n,
        r,
        o,
        l
      ))
        l.stopPropagation();
      else if (zy(n, l), r & 4 && -1 < t_.indexOf(n)) {
        for (; m !== null; ) {
          var g = dr(m);
          if (g !== null)
            switch (g.tag) {
              case 3:
                if (g = g.stateNode, g.current.memoizedState.isDehydrated) {
                  var b = Ma(g.pendingLanes);
                  if (b !== 0) {
                    var S = g;
                    for (S.pendingLanes |= 2, S.entangledLanes |= 2; b; ) {
                      var O = 1 << 31 - ln(b);
                      S.entanglements[1] |= O, b &= ~O;
                    }
                    ni(g), (qe & 6) === 0 && (Bc = on() + 500, eo(0));
                  }
                }
                break;
              case 31:
              case 13:
                S = xa(g, 2), S !== null && en(S, g, 2), Fc(), Pf(g, 2);
            }
          if (g = Nf(l), g === null && bf(
            n,
            r,
            l,
            nl,
            o
          ), g === m) break;
          m = g;
        }
        m !== null && l.stopPropagation();
      } else
        bf(
          n,
          r,
          l,
          null,
          o
        );
    }
  }
  function Nf(n) {
    return n = Iu(n), If(n);
  }
  var nl = null;
  function If(n) {
    if (nl = null, n = ur(n), n !== null) {
      var r = c(n);
      if (r === null) n = null;
      else {
        var o = r.tag;
        if (o === 13) {
          if (n = u(r), n !== null) return n;
          n = null;
        } else if (o === 31) {
          if (n = d(r), n !== null) return n;
          n = null;
        } else if (o === 3) {
          if (r.stateNode.current.memoizedState.isDehydrated)
            return r.tag === 3 ? r.stateNode.containerInfo : null;
          n = null;
        } else r !== n && (n = null);
      }
    }
    return nl = n, null;
  }
  function By(n) {
    switch (n) {
      case "beforetoggle":
      case "cancel":
      case "click":
      case "close":
      case "contextmenu":
      case "copy":
      case "cut":
      case "auxclick":
      case "dblclick":
      case "dragend":
      case "dragstart":
      case "drop":
      case "focusin":
      case "focusout":
      case "input":
      case "invalid":
      case "keydown":
      case "keypress":
      case "keyup":
      case "mousedown":
      case "mouseup":
      case "paste":
      case "pause":
      case "play":
      case "pointercancel":
      case "pointerdown":
      case "pointerup":
      case "ratechange":
      case "reset":
      case "resize":
      case "seeked":
      case "submit":
      case "toggle":
      case "touchcancel":
      case "touchend":
      case "touchstart":
      case "volumechange":
      case "change":
      case "selectionchange":
      case "textInput":
      case "compositionstart":
      case "compositionend":
      case "compositionupdate":
      case "beforeblur":
      case "afterblur":
      case "beforeinput":
      case "blur":
      case "fullscreenchange":
      case "focus":
      case "hashchange":
      case "popstate":
      case "select":
      case "selectstart":
        return 2;
      case "drag":
      case "dragenter":
      case "dragexit":
      case "dragleave":
      case "dragover":
      case "mousemove":
      case "mouseout":
      case "mouseover":
      case "pointermove":
      case "pointerout":
      case "pointerover":
      case "scroll":
      case "touchmove":
      case "wheel":
      case "mouseenter":
      case "mouseleave":
      case "pointerenter":
      case "pointerleave":
        return 8;
      case "message":
        switch (Fk()) {
          case Km:
            return 2;
          case Ym:
            return 8;
          case Go:
          case jk:
            return 32;
          case Jm:
            return 268435456;
          default:
            return 32;
        }
      default:
        return 32;
    }
  }
  var Lf = !1, oa = null, ca = null, la = null, oo = /* @__PURE__ */ new Map(), co = /* @__PURE__ */ new Map(), ua = [], t_ = "mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset".split(
    " "
  );
  function zy(n, r) {
    switch (n) {
      case "focusin":
      case "focusout":
        oa = null;
        break;
      case "dragenter":
      case "dragleave":
        ca = null;
        break;
      case "mouseover":
      case "mouseout":
        la = null;
        break;
      case "pointerover":
      case "pointerout":
        oo.delete(r.pointerId);
        break;
      case "gotpointercapture":
      case "lostpointercapture":
        co.delete(r.pointerId);
    }
  }
  function lo(n, r, o, l, m, g) {
    return n === null || n.nativeEvent !== g ? (n = {
      blockedOn: r,
      domEventName: o,
      eventSystemFlags: l,
      nativeEvent: g,
      targetContainers: [m]
    }, r !== null && (r = dr(r), r !== null && Ly(r)), n) : (n.eventSystemFlags |= l, r = n.targetContainers, m !== null && r.indexOf(m) === -1 && r.push(m), n);
  }
  function n_(n, r, o, l, m) {
    switch (r) {
      case "focusin":
        return oa = lo(
          oa,
          n,
          r,
          o,
          l,
          m
        ), !0;
      case "dragenter":
        return ca = lo(
          ca,
          n,
          r,
          o,
          l,
          m
        ), !0;
      case "mouseover":
        return la = lo(
          la,
          n,
          r,
          o,
          l,
          m
        ), !0;
      case "pointerover":
        var g = m.pointerId;
        return oo.set(
          g,
          lo(
            oo.get(g) || null,
            n,
            r,
            o,
            l,
            m
          )
        ), !0;
      case "gotpointercapture":
        return g = m.pointerId, co.set(
          g,
          lo(
            co.get(g) || null,
            n,
            r,
            o,
            l,
            m
          )
        ), !0;
    }
    return !1;
  }
  function Fy(n) {
    var r = ur(n.target);
    if (r !== null) {
      var o = c(r);
      if (o !== null) {
        if (r = o.tag, r === 13) {
          if (r = u(o), r !== null) {
            n.blockedOn = r, ep(n.priority, function() {
              Uy(o);
            });
            return;
          }
        } else if (r === 31) {
          if (r = d(o), r !== null) {
            n.blockedOn = r, ep(n.priority, function() {
              Uy(o);
            });
            return;
          }
        } else if (r === 3 && o.stateNode.current.memoizedState.isDehydrated) {
          n.blockedOn = o.tag === 3 ? o.stateNode.containerInfo : null;
          return;
        }
      }
    }
    n.blockedOn = null;
  }
  function il(n) {
    if (n.blockedOn !== null) return !1;
    for (var r = n.targetContainers; 0 < r.length; ) {
      var o = Nf(n.nativeEvent);
      if (o === null) {
        o = n.nativeEvent;
        var l = new o.constructor(
          o.type,
          o
        );
        Nu = l, o.target.dispatchEvent(l), Nu = null;
      } else
        return r = dr(o), r !== null && Ly(r), n.blockedOn = o, !1;
      r.shift();
    }
    return !0;
  }
  function jy(n, r, o) {
    il(n) && o.delete(r);
  }
  function i_() {
    Lf = !1, oa !== null && il(oa) && (oa = null), ca !== null && il(ca) && (ca = null), la !== null && il(la) && (la = null), oo.forEach(jy), co.forEach(jy);
  }
  function al(n, r) {
    n.blockedOn === r && (n.blockedOn = null, Lf || (Lf = !0, a.unstable_scheduleCallback(
      a.unstable_NormalPriority,
      i_
    )));
  }
  var rl = null;
  function qy(n) {
    rl !== n && (rl = n, a.unstable_scheduleCallback(
      a.unstable_NormalPriority,
      function() {
        rl === n && (rl = null);
        for (var r = 0; r < n.length; r += 3) {
          var o = n[r], l = n[r + 1], m = n[r + 2];
          if (typeof l != "function") {
            if (If(l || o) === null)
              continue;
            break;
          }
          var g = dr(o);
          g !== null && (n.splice(r, 3), r -= 3, xd(
            g,
            {
              pending: !0,
              data: m,
              method: o.method,
              action: l
            },
            l,
            m
          ));
        }
      }
    ));
  }
  function Kr(n) {
    function r(O) {
      return al(O, n);
    }
    oa !== null && al(oa, n), ca !== null && al(ca, n), la !== null && al(la, n), oo.forEach(r), co.forEach(r);
    for (var o = 0; o < ua.length; o++) {
      var l = ua[o];
      l.blockedOn === n && (l.blockedOn = null);
    }
    for (; 0 < ua.length && (o = ua[0], o.blockedOn === null); )
      Fy(o), o.blockedOn === null && ua.shift();
    if (o = (n.ownerDocument || n).$$reactFormReplay, o != null)
      for (l = 0; l < o.length; l += 3) {
        var m = o[l], g = o[l + 1], b = m[Jt] || null;
        if (typeof g == "function")
          b || qy(o);
        else if (b) {
          var S = null;
          if (g && g.hasAttribute("formAction")) {
            if (m = g, b = g[Jt] || null)
              S = b.formAction;
            else if (If(m) !== null) continue;
          } else S = b.action;
          typeof S == "function" ? o[l + 1] = S : (o.splice(l, 3), l -= 3), qy(o);
        }
      }
  }
  function Hy() {
    function n(g) {
      g.canIntercept && g.info === "react-transition" && g.intercept({
        handler: function() {
          return new Promise(function(b) {
            return m = b;
          });
        },
        focusReset: "manual",
        scroll: "manual"
      });
    }
    function r() {
      m !== null && (m(), m = null), l || setTimeout(o, 20);
    }
    function o() {
      if (!l && !navigation.transition) {
        var g = navigation.currentEntry;
        g && g.url != null && navigation.navigate(g.url, {
          state: g.getState(),
          info: "react-transition",
          history: "replace"
        });
      }
    }
    if (typeof navigation == "object") {
      var l = !1, m = null;
      return navigation.addEventListener("navigate", n), navigation.addEventListener("navigatesuccess", r), navigation.addEventListener("navigateerror", r), setTimeout(o, 100), function() {
        l = !0, navigation.removeEventListener("navigate", n), navigation.removeEventListener("navigatesuccess", r), navigation.removeEventListener("navigateerror", r), m !== null && (m(), m = null);
      };
    }
  }
  function Uf(n) {
    this._internalRoot = n;
  }
  sl.prototype.render = Uf.prototype.render = function(n) {
    var r = this._internalRoot;
    if (r === null) throw Error(i(409));
    var o = r.current, l = gn();
    Ny(o, l, n, r, null, null);
  }, sl.prototype.unmount = Uf.prototype.unmount = function() {
    var n = this._internalRoot;
    if (n !== null) {
      this._internalRoot = null;
      var r = n.containerInfo;
      Ny(n.current, 2, null, n, null, null), Fc(), r[lr] = null;
    }
  };
  function sl(n) {
    this._internalRoot = n;
  }
  sl.prototype.unstable_scheduleHydration = function(n) {
    if (n) {
      var r = $m();
      n = { blockedOn: null, target: n, priority: r };
      for (var o = 0; o < ua.length && r !== 0 && r < ua[o].priority; o++) ;
      ua.splice(o, 0, n), o === 0 && Fy(n);
    }
  };
  var Vy = e.version;
  if (Vy !== "19.2.7")
    throw Error(
      i(
        527,
        Vy,
        "19.2.7"
      )
    );
  te.findDOMNode = function(n) {
    var r = n._reactInternals;
    if (r === void 0)
      throw typeof n.render == "function" ? Error(i(188)) : (n = Object.keys(n).join(","), Error(i(268, n)));
    return n = f(r), n = n !== null ? p(n) : null, n = n === null ? null : n.stateNode, n;
  };
  var a_ = {
    bundleType: 0,
    version: "19.2.7",
    rendererPackageName: "react-dom",
    currentDispatcherRef: q,
    reconcilerVersion: "19.2.7"
  };
  if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ < "u") {
    var ol = __REACT_DEVTOOLS_GLOBAL_HOOK__;
    if (!ol.isDisabled && ol.supportsFiber)
      try {
        ys = ol.inject(
          a_
        ), cn = ol;
      } catch {
      }
  }
  return fo.createRoot = function(n, r) {
    if (!s(n)) throw Error(i(299));
    var o = !1, l = "", m = Zg, g = Wg, b = $g;
    return r != null && (r.unstable_strictMode === !0 && (o = !0), r.identifierPrefix !== void 0 && (l = r.identifierPrefix), r.onUncaughtError !== void 0 && (m = r.onUncaughtError), r.onCaughtError !== void 0 && (g = r.onCaughtError), r.onRecoverableError !== void 0 && (b = r.onRecoverableError)), r = Py(
      n,
      1,
      !1,
      null,
      null,
      o,
      l,
      null,
      m,
      g,
      b,
      Hy
    ), n[lr] = r.current, yf(n), new Uf(r);
  }, fo.hydrateRoot = function(n, r, o) {
    if (!s(n)) throw Error(i(299));
    var l = !1, m = "", g = Zg, b = Wg, S = $g, O = null;
    return o != null && (o.unstable_strictMode === !0 && (l = !0), o.identifierPrefix !== void 0 && (m = o.identifierPrefix), o.onUncaughtError !== void 0 && (g = o.onUncaughtError), o.onCaughtError !== void 0 && (b = o.onCaughtError), o.onRecoverableError !== void 0 && (S = o.onRecoverableError), o.formState !== void 0 && (O = o.formState)), r = Py(
      n,
      1,
      !0,
      r,
      o ?? null,
      l,
      m,
      O,
      g,
      b,
      S,
      Hy
    ), r.context = xy(null), o = r.current, l = gn(), l = Ru(l), m = Qi(l), m.callback = null, Xi(o, m, l), o = l, r.current.lanes = o, Ts(r, o), ni(r), n[lr] = r.current, yf(n), new sl(r);
  }, fo.version = "19.2.7", fo;
}
var eb;
function m_() {
  if (eb) return zf.exports;
  eb = 1;
  function a() {
    if (!(typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ > "u" || typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE != "function"))
      try {
        __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(a);
      } catch (e) {
        console.error(e);
      }
  }
  return a(), zf.exports = h_(), zf.exports;
}
var p_ = m_(), L = Zh();
function g_(a, e) {
  return e.forEach(function(t) {
    t && typeof t != "string" && !Array.isArray(t) && Object.keys(t).forEach(function(i) {
      if (i !== "default" && !(i in a)) {
        var s = Object.getOwnPropertyDescriptor(t, i);
        Object.defineProperty(a, i, s.get ? s : { enumerable: !0, get: function() {
          return t[i];
        } });
      }
    });
  }), Object.freeze(a);
}
var v_ = Object.defineProperty, y_ = (a, e, t) => e in a ? v_(a, e, {
  enumerable: !0,
  configurable: !0,
  writable: !0,
  value: t
}) : a[e] = t, tb = (a, e, t) => y_(a, typeof e != "symbol" ? e + "" : e, t);
let Lt = class {
  constructor() {
    tb(this, "_locking"), tb(this, "_locks"), this._locking = Promise.resolve(), this._locks = 0;
  }
  isLocked() {
    return this._locks > 0;
  }
  lock() {
    this._locks += 1;
    let e;
    const t = new Promise((s) => e = () => {
      this._locks -= 1, s();
    }), i = this._locking.then(() => e);
    return this._locking = this._locking.then(() => t), i;
  }
};
function lt(a, e) {
  if (!a)
    throw new Error(e);
}
const b_ = 34028234663852886e22, T_ = -34028234663852886e22, S_ = 4294967295, k_ = 2147483647, C_ = -2147483648;
function vl(a) {
  if (typeof a != "number") throw new Error("invalid int 32: " + typeof a);
  if (!Number.isInteger(a) || a > k_ || a < C_) throw new Error("invalid int 32: " + a);
}
function gh(a) {
  if (typeof a != "number") throw new Error("invalid uint 32: " + typeof a);
  if (!Number.isInteger(a) || a > S_ || a < 0) throw new Error("invalid uint 32: " + a);
}
function q0(a) {
  if (typeof a != "number") throw new Error("invalid float 32: " + typeof a);
  if (Number.isFinite(a) && (a > b_ || a < T_))
    throw new Error("invalid float 32: " + a);
}
const H0 = Symbol("@bufbuild/protobuf/enum-type");
function E_(a) {
  const e = a[H0];
  return lt(e, "missing enum type on enum object"), e;
}
function V0(a, e, t, i) {
  a[H0] = G0(e, t.map((s) => ({
    no: s.no,
    name: s.name,
    localName: a[s.no]
  })));
}
function G0(a, e, t) {
  const i = /* @__PURE__ */ Object.create(null), s = /* @__PURE__ */ Object.create(null), c = [];
  for (const u of e) {
    const d = K0(u);
    c.push(d), i[u.name] = d, s[u.no] = d;
  }
  return {
    typeName: a,
    values: c,
    // We do not surface options at this time
    // options: opt?.options ?? Object.create(null),
    findName(u) {
      return i[u];
    },
    findNumber(u) {
      return s[u];
    }
  };
}
function __(a, e, t) {
  const i = {};
  for (const s of e) {
    const c = K0(s);
    i[c.localName] = c.no, i[c.no] = c.localName;
  }
  return V0(i, a, e), i;
}
function K0(a) {
  return "localName" in a ? a : Object.assign(Object.assign({}, a), {
    localName: a.name
  });
}
class Wh {
  /**
   * Compare with a message of the same type.
   * Note that this function disregards extensions and unknown fields.
   */
  equals(e) {
    return this.getType().runtime.util.equals(this.getType(), this, e);
  }
  /**
   * Create a deep copy.
   */
  clone() {
    return this.getType().runtime.util.clone(this);
  }
  /**
   * Parse from binary data, merging fields.
   *
   * Repeated fields are appended. Map entries are added, overwriting
   * existing keys.
   *
   * If a message field is already present, it will be merged with the
   * new data.
   */
  fromBinary(e, t) {
    const i = this.getType(), s = i.runtime.bin, c = s.makeReadOptions(t);
    return s.readMessage(this, c.readerFactory(e), e.byteLength, c), this;
  }
  /**
   * Parse a message from a JSON value.
   */
  fromJson(e, t) {
    const i = this.getType(), s = i.runtime.json, c = s.makeReadOptions(t);
    return s.readMessage(i, e, c, this), this;
  }
  /**
   * Parse a message from a JSON string.
   */
  fromJsonString(e, t) {
    let i;
    try {
      i = JSON.parse(e);
    } catch (s) {
      throw new Error("cannot decode ".concat(this.getType().typeName, " from JSON: ").concat(s instanceof Error ? s.message : String(s)));
    }
    return this.fromJson(i, t);
  }
  /**
   * Serialize the message to binary data.
   */
  toBinary(e) {
    const t = this.getType(), i = t.runtime.bin, s = i.makeWriteOptions(e), c = s.writerFactory();
    return i.writeMessage(this, c, s), c.finish();
  }
  /**
   * Serialize the message to a JSON value, a JavaScript value that can be
   * passed to JSON.stringify().
   */
  toJson(e) {
    const t = this.getType(), i = t.runtime.json, s = i.makeWriteOptions(e);
    return i.writeMessage(this, s);
  }
  /**
   * Serialize the message to a JSON string.
   */
  toJsonString(e) {
    var t;
    const i = this.toJson(e);
    return JSON.stringify(i, null, (t = e?.prettySpaces) !== null && t !== void 0 ? t : 0);
  }
  /**
   * Override for serialization behavior. This will be invoked when calling
   * JSON.stringify on this message (i.e. JSON.stringify(msg)).
   *
   * Note that this will not serialize google.protobuf.Any with a packed
   * message because the protobuf JSON format specifies that it needs to be
   * unpacked, and this is only possible with a type registry to look up the
   * message type.  As a result, attempting to serialize a message with this
   * type will throw an Error.
   *
   * This method is protected because you should not need to invoke it
   * directly -- instead use JSON.stringify or toJsonString for
   * stringified JSON.  Alternatively, if actual JSON is desired, you should
   * use toJson.
   */
  toJSON() {
    return this.toJson({
      emitDefaultValues: !0
    });
  }
  /**
   * Retrieve the MessageType of this message - a singleton that represents
   * the protobuf message declaration and provides metadata for reflection-
   * based operations.
   */
  getType() {
    return Object.getPrototypeOf(this).constructor;
  }
}
function R_(a, e, t, i) {
  var s;
  const c = (s = i?.localName) !== null && s !== void 0 ? s : e.substring(e.lastIndexOf(".") + 1), u = {
    [c]: function(d) {
      a.util.initFields(this), a.util.initPartial(d, this);
    }
  }[c];
  return Object.setPrototypeOf(u.prototype, new Wh()), Object.assign(u, {
    runtime: a,
    typeName: e,
    fields: a.util.newFieldList(t),
    fromBinary(d, h) {
      return new u().fromBinary(d, h);
    },
    fromJson(d, h) {
      return new u().fromJson(d, h);
    },
    fromJsonString(d, h) {
      return new u().fromJsonString(d, h);
    },
    equals(d, h) {
      return a.util.equals(u, d, h);
    }
  }), u;
}
function w_() {
  let a = 0, e = 0;
  for (let i = 0; i < 28; i += 7) {
    let s = this.buf[this.pos++];
    if (a |= (s & 127) << i, (s & 128) == 0)
      return this.assertBounds(), [a, e];
  }
  let t = this.buf[this.pos++];
  if (a |= (t & 15) << 28, e = (t & 112) >> 4, (t & 128) == 0)
    return this.assertBounds(), [a, e];
  for (let i = 3; i <= 31; i += 7) {
    let s = this.buf[this.pos++];
    if (e |= (s & 127) << i, (s & 128) == 0)
      return this.assertBounds(), [a, e];
  }
  throw new Error("invalid varint");
}
function Vf(a, e, t) {
  for (let c = 0; c < 28; c = c + 7) {
    const u = a >>> c, d = !(!(u >>> 7) && e == 0), h = (d ? u | 128 : u) & 255;
    if (t.push(h), !d)
      return;
  }
  const i = a >>> 28 & 15 | (e & 7) << 4, s = e >> 3 != 0;
  if (t.push((s ? i | 128 : i) & 255), !!s) {
    for (let c = 3; c < 31; c = c + 7) {
      const u = e >>> c, d = !!(u >>> 7), h = (d ? u | 128 : u) & 255;
      if (t.push(h), !d)
        return;
    }
    t.push(e >>> 31 & 1);
  }
}
const yl = 4294967296;
function nb(a) {
  const e = a[0] === "-";
  e && (a = a.slice(1));
  const t = 1e6;
  let i = 0, s = 0;
  function c(u, d) {
    const h = Number(a.slice(u, d));
    s *= t, i = i * t + h, i >= yl && (s = s + (i / yl | 0), i = i % yl);
  }
  return c(-24, -18), c(-18, -12), c(-12, -6), c(-6), e ? J0(i, s) : $h(i, s);
}
function M_(a, e) {
  let t = $h(a, e);
  const i = t.hi & 2147483648;
  i && (t = J0(t.lo, t.hi));
  const s = Y0(t.lo, t.hi);
  return i ? "-" + s : s;
}
function Y0(a, e) {
  var t = A_(a, e);
  if (a = t.lo, e = t.hi, e <= 2097151)
    return String(yl * e + a);
  const i = a & 16777215, s = (a >>> 24 | e << 8) & 16777215, c = e >> 16 & 65535;
  let u = i + s * 6777216 + c * 6710656, d = s + c * 8147497, h = c * 2;
  const f = 1e7;
  return u >= f && (d += Math.floor(u / f), u %= f), d >= f && (h += Math.floor(d / f), d %= f), h.toString() + ib(d) + ib(u);
}
function A_(a, e) {
  return {
    lo: a >>> 0,
    hi: e >>> 0
  };
}
function $h(a, e) {
  return {
    lo: a | 0,
    hi: e | 0
  };
}
function J0(a, e) {
  return e = ~e, a ? a = ~a + 1 : e += 1, $h(a, e);
}
const ib = (a) => {
  const e = String(a);
  return "0000000".slice(e.length) + e;
};
function ab(a, e) {
  if (a >= 0) {
    for (; a > 127; )
      e.push(a & 127 | 128), a = a >>> 7;
    e.push(a);
  } else {
    for (let t = 0; t < 9; t++)
      e.push(a & 127 | 128), a = a >> 7;
    e.push(1);
  }
}
function O_() {
  let a = this.buf[this.pos++], e = a & 127;
  if ((a & 128) == 0)
    return this.assertBounds(), e;
  if (a = this.buf[this.pos++], e |= (a & 127) << 7, (a & 128) == 0)
    return this.assertBounds(), e;
  if (a = this.buf[this.pos++], e |= (a & 127) << 14, (a & 128) == 0)
    return this.assertBounds(), e;
  if (a = this.buf[this.pos++], e |= (a & 127) << 21, (a & 128) == 0)
    return this.assertBounds(), e;
  a = this.buf[this.pos++], e |= (a & 15) << 28;
  for (let t = 5; (a & 128) !== 0 && t < 10; t++) a = this.buf[this.pos++];
  if ((a & 128) != 0) throw new Error("invalid varint");
  return this.assertBounds(), e >>> 0;
}
function D_() {
  const a = new DataView(new ArrayBuffer(8));
  if (typeof BigInt == "function" && typeof a.getBigInt64 == "function" && typeof a.getBigUint64 == "function" && typeof a.setBigInt64 == "function" && typeof a.setBigUint64 == "function" && (typeof process != "object" || typeof process.env != "object" || process.env.BUF_BIGINT_DISABLE !== "1")) {
    const s = BigInt("-9223372036854775808"), c = BigInt("9223372036854775807"), u = BigInt("0"), d = BigInt("18446744073709551615");
    return {
      zero: BigInt(0),
      supported: !0,
      parse(h) {
        const f = typeof h == "bigint" ? h : BigInt(h);
        if (f > c || f < s)
          throw new Error("int64 invalid: ".concat(h));
        return f;
      },
      uParse(h) {
        const f = typeof h == "bigint" ? h : BigInt(h);
        if (f > d || f < u)
          throw new Error("uint64 invalid: ".concat(h));
        return f;
      },
      enc(h) {
        return a.setBigInt64(0, this.parse(h), !0), {
          lo: a.getInt32(0, !0),
          hi: a.getInt32(4, !0)
        };
      },
      uEnc(h) {
        return a.setBigInt64(0, this.uParse(h), !0), {
          lo: a.getInt32(0, !0),
          hi: a.getInt32(4, !0)
        };
      },
      dec(h, f) {
        return a.setInt32(0, h, !0), a.setInt32(4, f, !0), a.getBigInt64(0, !0);
      },
      uDec(h, f) {
        return a.setInt32(0, h, !0), a.setInt32(4, f, !0), a.getBigUint64(0, !0);
      }
    };
  }
  const t = (s) => lt(/^-?[0-9]+$/.test(s), "int64 invalid: ".concat(s)), i = (s) => lt(/^[0-9]+$/.test(s), "uint64 invalid: ".concat(s));
  return {
    zero: "0",
    supported: !1,
    parse(s) {
      return typeof s != "string" && (s = s.toString()), t(s), s;
    },
    uParse(s) {
      return typeof s != "string" && (s = s.toString()), i(s), s;
    },
    enc(s) {
      return typeof s != "string" && (s = s.toString()), t(s), nb(s);
    },
    uEnc(s) {
      return typeof s != "string" && (s = s.toString()), i(s), nb(s);
    },
    dec(s, c) {
      return M_(s, c);
    },
    uDec(s, c) {
      return Y0(s, c);
    }
  };
}
const at = D_();
var ee;
(function(a) {
  a[a.DOUBLE = 1] = "DOUBLE", a[a.FLOAT = 2] = "FLOAT", a[a.INT64 = 3] = "INT64", a[a.UINT64 = 4] = "UINT64", a[a.INT32 = 5] = "INT32", a[a.FIXED64 = 6] = "FIXED64", a[a.FIXED32 = 7] = "FIXED32", a[a.BOOL = 8] = "BOOL", a[a.STRING = 9] = "STRING", a[a.BYTES = 12] = "BYTES", a[a.UINT32 = 13] = "UINT32", a[a.SFIXED32 = 15] = "SFIXED32", a[a.SFIXED64 = 16] = "SFIXED64", a[a.SINT32 = 17] = "SINT32", a[a.SINT64 = 18] = "SINT64";
})(ee || (ee = {}));
var Sa;
(function(a) {
  a[a.BIGINT = 0] = "BIGINT", a[a.STRING = 1] = "STRING";
})(Sa || (Sa = {}));
function fa(a, e, t) {
  if (e === t)
    return !0;
  if (a == ee.BYTES) {
    if (!(e instanceof Uint8Array) || !(t instanceof Uint8Array) || e.length !== t.length)
      return !1;
    for (let i = 0; i < e.length; i++)
      if (e[i] !== t[i])
        return !1;
    return !0;
  }
  switch (a) {
    case ee.UINT64:
    case ee.FIXED64:
    case ee.INT64:
    case ee.SFIXED64:
    case ee.SINT64:
      return e == t;
  }
  return !1;
}
function ls(a, e) {
  switch (a) {
    case ee.BOOL:
      return !1;
    case ee.UINT64:
    case ee.FIXED64:
    case ee.INT64:
    case ee.SFIXED64:
    case ee.SINT64:
      return e == 0 ? at.zero : "0";
    case ee.DOUBLE:
    case ee.FLOAT:
      return 0;
    case ee.BYTES:
      return new Uint8Array(0);
    case ee.STRING:
      return "";
    default:
      return 0;
  }
}
function Q0(a, e) {
  switch (a) {
    case ee.BOOL:
      return e === !1;
    case ee.STRING:
      return e === "";
    case ee.BYTES:
      return e instanceof Uint8Array && !e.byteLength;
    default:
      return e == 0;
  }
}
function rb(a, e) {
  (e == null || e > a.length) && (e = a.length);
  for (var t = 0, i = Array(e); t < e; t++) i[t] = a[t];
  return i;
}
function P_(a) {
  if (Array.isArray(a)) return a;
}
function xe(a, e, t) {
  return (e = L_(e)) in a ? Object.defineProperty(a, e, {
    value: t,
    enumerable: !0,
    configurable: !0,
    writable: !0
  }) : a[e] = t, a;
}
function x_(a, e) {
  var t = a == null ? null : typeof Symbol < "u" && a[Symbol.iterator] || a["@@iterator"];
  if (t != null) {
    var i, s, c, u, d = [], h = !0, f = !1;
    try {
      if (c = (t = t.call(a)).next, e === 0) {
        if (Object(t) !== t) return;
        h = !1;
      } else for (; !(h = (i = c.call(t)).done) && (d.push(i.value), d.length !== e); h = !0) ;
    } catch (p) {
      f = !0, s = p;
    } finally {
      try {
        if (!h && t.return != null && (u = t.return(), Object(u) !== u)) return;
      } finally {
        if (f) throw s;
      }
    }
    return d;
  }
}
function N_() {
  throw new TypeError(`Invalid attempt to destructure non-iterable instance.
In order to be iterable, non-array objects must have a [Symbol.iterator]() method.`);
}
function ge(a, e) {
  return P_(a) || x_(a, e) || U_(a, e) || N_();
}
function I_(a, e) {
  if (typeof a != "object" || !a) return a;
  var t = a[Symbol.toPrimitive];
  if (t !== void 0) {
    var i = t.call(a, e);
    if (typeof i != "object") return i;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return (e === "string" ? String : Number)(a);
}
function L_(a) {
  var e = I_(a, "string");
  return typeof e == "symbol" ? e : e + "";
}
function U_(a, e) {
  if (a) {
    if (typeof a == "string") return rb(a, e);
    var t = {}.toString.call(a).slice(8, -1);
    return t === "Object" && a.constructor && (t = a.constructor.name), t === "Map" || t === "Set" ? Array.from(a) : t === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? rb(a, e) : void 0;
  }
}
var ht;
(function(a) {
  a[a.Varint = 0] = "Varint", a[a.Bit64 = 1] = "Bit64", a[a.LengthDelimited = 2] = "LengthDelimited", a[a.StartGroup = 3] = "StartGroup", a[a.EndGroup = 4] = "EndGroup", a[a.Bit32 = 5] = "Bit32";
})(ht || (ht = {}));
class B_ {
  constructor(e) {
    this.stack = [], this.textEncoder = e ?? new TextEncoder(), this.chunks = [], this.buf = [];
  }
  /**
   * Return all bytes written and reset this writer.
   */
  finish() {
    this.chunks.push(new Uint8Array(this.buf));
    let e = 0;
    for (let s = 0; s < this.chunks.length; s++) e += this.chunks[s].length;
    let t = new Uint8Array(e), i = 0;
    for (let s = 0; s < this.chunks.length; s++)
      t.set(this.chunks[s], i), i += this.chunks[s].length;
    return this.chunks = [], t;
  }
  /**
   * Start a new fork for length-delimited data like a message
   * or a packed repeated field.
   *
   * Must be joined later with `join()`.
   */
  fork() {
    return this.stack.push({
      chunks: this.chunks,
      buf: this.buf
    }), this.chunks = [], this.buf = [], this;
  }
  /**
   * Join the last fork. Write its length and bytes, then
   * return to the previous state.
   */
  join() {
    let e = this.finish(), t = this.stack.pop();
    if (!t) throw new Error("invalid state, fork stack empty");
    return this.chunks = t.chunks, this.buf = t.buf, this.uint32(e.byteLength), this.raw(e);
  }
  /**
   * Writes a tag (field number and wire type).
   *
   * Equivalent to `uint32( (fieldNo << 3 | type) >>> 0 )`.
   *
   * Generated code should compute the tag ahead of time and call `uint32()`.
   */
  tag(e, t) {
    return this.uint32((e << 3 | t) >>> 0);
  }
  /**
   * Write a chunk of raw bytes.
   */
  raw(e) {
    return this.buf.length && (this.chunks.push(new Uint8Array(this.buf)), this.buf = []), this.chunks.push(e), this;
  }
  /**
   * Write a `uint32` value, an unsigned 32 bit varint.
   */
  uint32(e) {
    for (gh(e); e > 127; )
      this.buf.push(e & 127 | 128), e = e >>> 7;
    return this.buf.push(e), this;
  }
  /**
   * Write a `int32` value, a signed 32 bit varint.
   */
  int32(e) {
    return vl(e), ab(e, this.buf), this;
  }
  /**
   * Write a `bool` value, a variant.
   */
  bool(e) {
    return this.buf.push(e ? 1 : 0), this;
  }
  /**
   * Write a `bytes` value, length-delimited arbitrary data.
   */
  bytes(e) {
    return this.uint32(e.byteLength), this.raw(e);
  }
  /**
   * Write a `string` value, length-delimited data converted to UTF-8 text.
   */
  string(e) {
    let t = this.textEncoder.encode(e);
    return this.uint32(t.byteLength), this.raw(t);
  }
  /**
   * Write a `float` value, 32-bit floating point number.
   */
  float(e) {
    q0(e);
    let t = new Uint8Array(4);
    return new DataView(t.buffer).setFloat32(0, e, !0), this.raw(t);
  }
  /**
   * Write a `double` value, a 64-bit floating point number.
   */
  double(e) {
    let t = new Uint8Array(8);
    return new DataView(t.buffer).setFloat64(0, e, !0), this.raw(t);
  }
  /**
   * Write a `fixed32` value, an unsigned, fixed-length 32-bit integer.
   */
  fixed32(e) {
    gh(e);
    let t = new Uint8Array(4);
    return new DataView(t.buffer).setUint32(0, e, !0), this.raw(t);
  }
  /**
   * Write a `sfixed32` value, a signed, fixed-length 32-bit integer.
   */
  sfixed32(e) {
    vl(e);
    let t = new Uint8Array(4);
    return new DataView(t.buffer).setInt32(0, e, !0), this.raw(t);
  }
  /**
   * Write a `sint32` value, a signed, zigzag-encoded 32-bit varint.
   */
  sint32(e) {
    return vl(e), e = (e << 1 ^ e >> 31) >>> 0, ab(e, this.buf), this;
  }
  /**
   * Write a `fixed64` value, a signed, fixed-length 64-bit integer.
   */
  sfixed64(e) {
    let t = new Uint8Array(8), i = new DataView(t.buffer), s = at.enc(e);
    return i.setInt32(0, s.lo, !0), i.setInt32(4, s.hi, !0), this.raw(t);
  }
  /**
   * Write a `fixed64` value, an unsigned, fixed-length 64 bit integer.
   */
  fixed64(e) {
    let t = new Uint8Array(8), i = new DataView(t.buffer), s = at.uEnc(e);
    return i.setInt32(0, s.lo, !0), i.setInt32(4, s.hi, !0), this.raw(t);
  }
  /**
   * Write a `int64` value, a signed 64-bit varint.
   */
  int64(e) {
    let t = at.enc(e);
    return Vf(t.lo, t.hi, this.buf), this;
  }
  /**
   * Write a `sint64` value, a signed, zig-zag-encoded 64-bit varint.
   */
  sint64(e) {
    let t = at.enc(e), i = t.hi >> 31, s = t.lo << 1 ^ i, c = (t.hi << 1 | t.lo >>> 31) ^ i;
    return Vf(s, c, this.buf), this;
  }
  /**
   * Write a `uint64` value, an unsigned 64-bit varint.
   */
  uint64(e) {
    let t = at.uEnc(e);
    return Vf(t.lo, t.hi, this.buf), this;
  }
}
class z_ {
  constructor(e, t) {
    this.varint64 = w_, this.uint32 = O_, this.buf = e, this.len = e.length, this.pos = 0, this.view = new DataView(e.buffer, e.byteOffset, e.byteLength), this.textDecoder = t ?? new TextDecoder();
  }
  /**
   * Reads a tag - field number and wire type.
   */
  tag() {
    let e = this.uint32(), t = e >>> 3, i = e & 7;
    if (t <= 0 || i < 0 || i > 5) throw new Error("illegal tag: field no " + t + " wire type " + i);
    return [t, i];
  }
  /**
   * Skip one element and return the skipped data.
   *
   * When skipping StartGroup, provide the tags field number to check for
   * matching field number in the EndGroup tag.
   */
  skip(e, t) {
    let i = this.pos;
    switch (e) {
      case ht.Varint:
        for (; this.buf[this.pos++] & 128; )
          ;
        break;
      // eslint-disable-next-line
      // @ts-ignore TS7029: Fallthrough case in switch
      case ht.Bit64:
        this.pos += 4;
      // eslint-disable-next-line
      // @ts-ignore TS7029: Fallthrough case in switch
      case ht.Bit32:
        this.pos += 4;
        break;
      case ht.LengthDelimited:
        let s = this.uint32();
        this.pos += s;
        break;
      case ht.StartGroup:
        for (; ; ) {
          const c = this.tag(), u = ge(c, 2), d = u[0], h = u[1];
          if (h === ht.EndGroup) {
            if (t !== void 0 && d !== t)
              throw new Error("invalid end group tag");
            break;
          }
          this.skip(h, d);
        }
        break;
      default:
        throw new Error("cant skip wire type " + e);
    }
    return this.assertBounds(), this.buf.subarray(i, this.pos);
  }
  /**
   * Throws error if position in byte array is out of range.
   */
  assertBounds() {
    if (this.pos > this.len) throw new RangeError("premature EOF");
  }
  /**
   * Read a `int32` field, a signed 32 bit varint.
   */
  int32() {
    return this.uint32() | 0;
  }
  /**
   * Read a `sint32` field, a signed, zigzag-encoded 32-bit varint.
   */
  sint32() {
    let e = this.uint32();
    return e >>> 1 ^ -(e & 1);
  }
  /**
   * Read a `int64` field, a signed 64-bit varint.
   */
  int64() {
    return at.dec(...this.varint64());
  }
  /**
   * Read a `uint64` field, an unsigned 64-bit varint.
   */
  uint64() {
    return at.uDec(...this.varint64());
  }
  /**
   * Read a `sint64` field, a signed, zig-zag-encoded 64-bit varint.
   */
  sint64() {
    let e = this.varint64(), t = ge(e, 2), i = t[0], s = t[1], c = -(i & 1);
    return i = (i >>> 1 | (s & 1) << 31) ^ c, s = s >>> 1 ^ c, at.dec(i, s);
  }
  /**
   * Read a `bool` field, a variant.
   */
  bool() {
    let e = this.varint64(), t = ge(e, 2), i = t[0], s = t[1];
    return i !== 0 || s !== 0;
  }
  /**
   * Read a `fixed32` field, an unsigned, fixed-length 32-bit integer.
   */
  fixed32() {
    return this.view.getUint32((this.pos += 4) - 4, !0);
  }
  /**
   * Read a `sfixed32` field, a signed, fixed-length 32-bit integer.
   */
  sfixed32() {
    return this.view.getInt32((this.pos += 4) - 4, !0);
  }
  /**
   * Read a `fixed64` field, an unsigned, fixed-length 64 bit integer.
   */
  fixed64() {
    return at.uDec(this.sfixed32(), this.sfixed32());
  }
  /**
   * Read a `fixed64` field, a signed, fixed-length 64-bit integer.
   */
  sfixed64() {
    return at.dec(this.sfixed32(), this.sfixed32());
  }
  /**
   * Read a `float` field, 32-bit floating point number.
   */
  float() {
    return this.view.getFloat32((this.pos += 4) - 4, !0);
  }
  /**
   * Read a `double` field, a 64-bit floating point number.
   */
  double() {
    return this.view.getFloat64((this.pos += 8) - 8, !0);
  }
  /**
   * Read a `bytes` field, length-delimited arbitrary data.
   */
  bytes() {
    let e = this.uint32(), t = this.pos;
    return this.pos += e, this.assertBounds(), this.buf.subarray(t, t + e);
  }
  /**
   * Read a `string` field, length-delimited data converted to UTF-8 text.
   */
  string() {
    return this.textDecoder.decode(this.bytes());
  }
}
function F_(a, e, t, i) {
  let s;
  return {
    typeName: e,
    extendee: t,
    get field() {
      if (!s) {
        const c = typeof i == "function" ? i() : i;
        c.name = e.split(".").pop(), c.jsonName = "[".concat(e, "]"), s = a.util.newFieldList([c]).list()[0];
      }
      return s;
    },
    runtime: a
  };
}
function X0(a) {
  const e = a.field.localName, t = /* @__PURE__ */ Object.create(null);
  return t[e] = j_(a), [t, () => t[e]];
}
function j_(a) {
  const e = a.field;
  if (e.repeated)
    return [];
  if (e.default !== void 0)
    return e.default;
  switch (e.kind) {
    case "enum":
      return e.T.values[0].no;
    case "scalar":
      return ls(e.T, e.L);
    case "message":
      const t = e.T, i = new t();
      return t.fieldWrapper ? t.fieldWrapper.unwrapField(i) : i;
    case "map":
      throw "map fields are not allowed to be extensions";
  }
}
function q_(a, e) {
  if (!e.repeated && (e.kind == "enum" || e.kind == "scalar")) {
    for (let t = a.length - 1; t >= 0; --t)
      if (a[t].no == e.no)
        return [a[t]];
    return [];
  }
  return a.filter((t) => t.no === e.no);
}
let Pi = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/".split(""), Wl = [];
for (let a = 0; a < Pi.length; a++) Wl[Pi[a].charCodeAt(0)] = a;
Wl[45] = Pi.indexOf("+");
Wl[95] = Pi.indexOf("/");
const Z0 = {
  /**
   * Decodes a base64 string to a byte array.
   *
   * - ignores white-space, including line breaks and tabs
   * - allows inner padding (can decode concatenated base64 strings)
   * - does not require padding
   * - understands base64url encoding:
   *   "-" instead of "+",
   *   "_" instead of "/",
   *   no padding
   */
  dec(a) {
    let e = a.length * 3 / 4;
    a[a.length - 2] == "=" ? e -= 2 : a[a.length - 1] == "=" && (e -= 1);
    let t = new Uint8Array(e), i = 0, s = 0, c, u = 0;
    for (let d = 0; d < a.length; d++) {
      if (c = Wl[a.charCodeAt(d)], c === void 0)
        switch (a[d]) {
          // @ts-ignore TS7029: Fallthrough case in switch
          case "=":
            s = 0;
          // reset state when padding found
          // @ts-ignore TS7029: Fallthrough case in switch
          case `
`:
          case "\r":
          case "	":
          case " ":
            continue;
          // skip white-space, and padding
          default:
            throw Error("invalid base64 string.");
        }
      switch (s) {
        case 0:
          u = c, s = 1;
          break;
        case 1:
          t[i++] = u << 2 | (c & 48) >> 4, u = c, s = 2;
          break;
        case 2:
          t[i++] = (u & 15) << 4 | (c & 60) >> 2, u = c, s = 3;
          break;
        case 3:
          t[i++] = (u & 3) << 6 | c, s = 0;
          break;
      }
    }
    if (s == 1) throw Error("invalid base64 string.");
    return t.subarray(0, i);
  },
  /**
   * Encode a byte array to a base64 string.
   */
  enc(a) {
    let e = "", t = 0, i, s = 0;
    for (let c = 0; c < a.length; c++)
      switch (i = a[c], t) {
        case 0:
          e += Pi[i >> 2], s = (i & 3) << 4, t = 1;
          break;
        case 1:
          e += Pi[s | i >> 4], s = (i & 15) << 2, t = 2;
          break;
        case 2:
          e += Pi[s | i >> 6], e += Pi[i & 63], t = 0;
          break;
      }
    return t && (e += Pi[s], e += "=", t == 1 && (e += "=")), e;
  }
};
function H_(a, e, t) {
  $0(e, a);
  const i = e.runtime.bin.makeReadOptions(t), s = q_(a.getType().runtime.bin.listUnknownFields(a), e.field), c = X0(e), u = ge(c, 2), d = u[0], h = u[1];
  for (const f of s)
    e.runtime.bin.readField(d, i.readerFactory(f.data), e.field, f.wireType, i);
  return h();
}
function V_(a, e, t, i) {
  $0(e, a);
  const s = e.runtime.bin.makeReadOptions(i), c = e.runtime.bin.makeWriteOptions(i);
  if (W0(a, e)) {
    const f = a.getType().runtime.bin.listUnknownFields(a).filter((p) => p.no != e.field.no);
    a.getType().runtime.bin.discardUnknownFields(a);
    for (const p of f)
      a.getType().runtime.bin.onUnknownField(a, p.no, p.wireType, p.data);
  }
  const u = c.writerFactory();
  let d = e.field;
  !d.opt && !d.repeated && (d.kind == "enum" || d.kind == "scalar") && (d = Object.assign(Object.assign({}, e.field), {
    opt: !0
  })), e.runtime.bin.writeField(d, t, u, c);
  const h = s.readerFactory(u.finish());
  for (; h.pos < h.len; ) {
    const f = h.tag(), p = ge(f, 2), v = p[0], y = p[1], T = h.skip(y, v);
    a.getType().runtime.bin.onUnknownField(a, v, y, T);
  }
}
function W0(a, e) {
  const t = a.getType();
  return e.extendee.typeName === t.typeName && !!t.runtime.bin.listUnknownFields(a).find((i) => i.no == e.field.no);
}
function $0(a, e) {
  lt(a.extendee.typeName == e.getType().typeName, "extension ".concat(a.typeName, " can only be applied to message ").concat(a.extendee.typeName));
}
function eT(a, e) {
  const t = a.localName;
  if (a.repeated)
    return e[t].length > 0;
  if (a.oneof)
    return e[a.oneof.localName].case === t;
  switch (a.kind) {
    case "enum":
    case "scalar":
      return a.opt || a.req ? e[t] !== void 0 : a.kind == "enum" ? e[t] !== a.T.values[0].no : !Q0(a.T, e[t]);
    case "message":
      return e[t] !== void 0;
    case "map":
      return Object.keys(e[t]).length > 0;
  }
}
function sb(a, e) {
  const t = a.localName, i = !a.opt && !a.req;
  if (a.repeated)
    e[t] = [];
  else if (a.oneof)
    e[a.oneof.localName] = {
      case: void 0
    };
  else
    switch (a.kind) {
      case "map":
        e[t] = {};
        break;
      case "enum":
        e[t] = i ? a.T.values[0].no : void 0;
        break;
      case "scalar":
        e[t] = i ? ls(a.T, a.L) : void 0;
        break;
      case "message":
        e[t] = void 0;
        break;
    }
}
function xi(a, e) {
  if (a === null || typeof a != "object" || !Object.getOwnPropertyNames(Wh.prototype).every((i) => i in a && typeof a[i] == "function"))
    return !1;
  const t = a.getType();
  return t === null || typeof t != "function" || !("typeName" in t) || typeof t.typeName != "string" ? !1 : e === void 0 ? !0 : t.typeName == e.typeName;
}
function tT(a, e) {
  return xi(e) || !a.fieldWrapper ? e : a.fieldWrapper.wrapField(e);
}
ee.DOUBLE, ee.FLOAT, ee.INT64, ee.UINT64, ee.INT32, ee.UINT32, ee.BOOL, ee.STRING, ee.BYTES;
const ob = {
  ignoreUnknownFields: !1
}, cb = {
  emitDefaultValues: !1,
  enumAsInteger: !1,
  useProtoFieldName: !1,
  prettySpaces: 0
};
function G_(a) {
  return a ? Object.assign(Object.assign({}, ob), a) : ob;
}
function K_(a) {
  return a ? Object.assign(Object.assign({}, cb), a) : cb;
}
const Pl = Symbol(), bl = Symbol();
function Y_() {
  return {
    makeReadOptions: G_,
    makeWriteOptions: K_,
    readMessage(a, e, t, i) {
      if (e == null || Array.isArray(e) || typeof e != "object")
        throw new Error("cannot decode message ".concat(a.typeName, " from JSON: ").concat(si(e)));
      i = i ?? new a();
      const s = /* @__PURE__ */ new Map(), c = t.typeRegistry;
      for (const d of Object.entries(e)) {
        var u = ge(d, 2);
        const h = u[0], f = u[1], p = a.fields.findJsonName(h);
        if (p) {
          if (p.oneof) {
            if (f === null && p.kind == "scalar")
              continue;
            const v = s.get(p.oneof);
            if (v !== void 0)
              throw new Error("cannot decode message ".concat(a.typeName, ' from JSON: multiple keys for oneof "').concat(p.oneof.name, '" present: "').concat(v, '", "').concat(h, '"'));
            s.set(p.oneof, h);
          }
          lb(i, f, p, t, a);
        } else {
          let v = !1;
          if (c?.findExtension && h.startsWith("[") && h.endsWith("]")) {
            const y = c.findExtension(h.substring(1, h.length - 1));
            if (y && y.extendee.typeName == a.typeName) {
              v = !0;
              const T = X0(y), _ = ge(T, 2), k = _[0], x = _[1];
              lb(k, f, y.field, t, y), V_(i, y, x(), t);
            }
          }
          if (!v && !t.ignoreUnknownFields)
            throw new Error("cannot decode message ".concat(a.typeName, ' from JSON: key "').concat(h, '" is unknown'));
        }
      }
      return i;
    },
    writeMessage(a, e) {
      const t = a.getType(), i = {};
      let s;
      try {
        for (s of t.fields.byNumber()) {
          if (!eT(s, a)) {
            if (s.req)
              throw "required field not set";
            if (!e.emitDefaultValues || !Q_(s))
              continue;
          }
          const u = s.oneof ? a[s.oneof.localName].value : a[s.localName], d = ub(s, u, e);
          d !== void 0 && (i[e.useProtoFieldName ? s.name : s.jsonName] = d);
        }
        const c = e.typeRegistry;
        if (c?.findExtensionFor)
          for (const u of t.runtime.bin.listUnknownFields(a)) {
            const d = c.findExtensionFor(t.typeName, u.no);
            if (d && W0(a, d)) {
              const h = H_(a, d, e), f = ub(d.field, h, e);
              f !== void 0 && (i[d.field.jsonName] = f);
            }
          }
      } catch (c) {
        const u = s ? "cannot encode field ".concat(t.typeName, ".").concat(s.name, " to JSON") : "cannot encode message ".concat(t.typeName, " to JSON"), d = c instanceof Error ? c.message : String(c);
        throw new Error(u + (d.length > 0 ? ": ".concat(d) : ""));
      }
      return i;
    },
    readScalar(a, e, t) {
      return So(a, e, t ?? Sa.BIGINT, !0);
    },
    writeScalar(a, e, t) {
      if (e !== void 0 && (t || Q0(a, e)))
        return Tl(a, e);
    },
    debug: si
  };
}
function si(a) {
  if (a === null)
    return "null";
  switch (typeof a) {
    case "object":
      return Array.isArray(a) ? "array" : "object";
    case "string":
      return a.length > 100 ? "string" : '"'.concat(a.split('"').join('\\"'), '"');
    default:
      return String(a);
  }
}
function lb(a, e, t, i, s) {
  let c = t.localName;
  if (t.repeated) {
    if (lt(t.kind != "map"), e === null)
      return;
    if (!Array.isArray(e))
      throw new Error("cannot decode field ".concat(s.typeName, ".").concat(t.name, " from JSON: ").concat(si(e)));
    const d = a[c];
    for (const h of e) {
      if (h === null)
        throw new Error("cannot decode field ".concat(s.typeName, ".").concat(t.name, " from JSON: ").concat(si(h)));
      switch (t.kind) {
        case "message":
          d.push(t.T.fromJson(h, i));
          break;
        case "enum":
          const f = Gf(t.T, h, i.ignoreUnknownFields, !0);
          f !== bl && d.push(f);
          break;
        case "scalar":
          try {
            d.push(So(t.T, h, t.L, !0));
          } catch (p) {
            let v = "cannot decode field ".concat(s.typeName, ".").concat(t.name, " from JSON: ").concat(si(h));
            throw p instanceof Error && p.message.length > 0 && (v += ": ".concat(p.message)), new Error(v);
          }
          break;
      }
    }
  } else if (t.kind == "map") {
    if (e === null)
      return;
    if (typeof e != "object" || Array.isArray(e))
      throw new Error("cannot decode field ".concat(s.typeName, ".").concat(t.name, " from JSON: ").concat(si(e)));
    const d = a[c];
    for (const h of Object.entries(e)) {
      var u = ge(h, 2);
      const f = u[0], p = u[1];
      if (p === null)
        throw new Error("cannot decode field ".concat(s.typeName, ".").concat(t.name, " from JSON: map value null"));
      let v;
      try {
        v = J_(t.K, f);
      } catch (y) {
        let T = "cannot decode map key for field ".concat(s.typeName, ".").concat(t.name, " from JSON: ").concat(si(e));
        throw y instanceof Error && y.message.length > 0 && (T += ": ".concat(y.message)), new Error(T);
      }
      switch (t.V.kind) {
        case "message":
          d[v] = t.V.T.fromJson(p, i);
          break;
        case "enum":
          const y = Gf(t.V.T, p, i.ignoreUnknownFields, !0);
          y !== bl && (d[v] = y);
          break;
        case "scalar":
          try {
            d[v] = So(t.V.T, p, Sa.BIGINT, !0);
          } catch (T) {
            let _ = "cannot decode map value for field ".concat(s.typeName, ".").concat(t.name, " from JSON: ").concat(si(e));
            throw T instanceof Error && T.message.length > 0 && (_ += ": ".concat(T.message)), new Error(_);
          }
          break;
      }
    }
  } else
    switch (t.oneof && (a = a[t.oneof.localName] = {
      case: c
    }, c = "value"), t.kind) {
      case "message":
        const d = t.T;
        if (e === null && d.typeName != "google.protobuf.Value")
          return;
        let h = a[c];
        xi(h) ? h.fromJson(e, i) : (a[c] = h = d.fromJson(e, i), d.fieldWrapper && !t.oneof && (a[c] = d.fieldWrapper.unwrapField(h)));
        break;
      case "enum":
        const f = Gf(t.T, e, i.ignoreUnknownFields, !1);
        switch (f) {
          case Pl:
            sb(t, a);
            break;
          case bl:
            break;
          default:
            a[c] = f;
            break;
        }
        break;
      case "scalar":
        try {
          const p = So(t.T, e, t.L, !1);
          switch (p) {
            case Pl:
              sb(t, a);
              break;
            default:
              a[c] = p;
              break;
          }
        } catch (p) {
          let v = "cannot decode field ".concat(s.typeName, ".").concat(t.name, " from JSON: ").concat(si(e));
          throw p instanceof Error && p.message.length > 0 && (v += ": ".concat(p.message)), new Error(v);
        }
        break;
    }
}
function J_(a, e) {
  if (a === ee.BOOL)
    switch (e) {
      case "true":
        e = !0;
        break;
      case "false":
        e = !1;
        break;
    }
  return So(a, e, Sa.BIGINT, !0).toString();
}
function So(a, e, t, i) {
  if (e === null)
    return i ? ls(a, t) : Pl;
  switch (a) {
    // float, double: JSON value will be a number or one of the special string values "NaN", "Infinity", and "-Infinity".
    // Either numbers or strings are accepted. Exponent notation is also accepted.
    case ee.DOUBLE:
    case ee.FLOAT:
      if (e === "NaN") return Number.NaN;
      if (e === "Infinity") return Number.POSITIVE_INFINITY;
      if (e === "-Infinity") return Number.NEGATIVE_INFINITY;
      if (e === "" || typeof e == "string" && e.trim().length !== e.length || typeof e != "string" && typeof e != "number")
        break;
      const s = Number(e);
      if (Number.isNaN(s) || !Number.isFinite(s))
        break;
      return a == ee.FLOAT && q0(s), s;
    // int32, fixed32, uint32: JSON value will be a decimal number. Either numbers or strings are accepted.
    case ee.INT32:
    case ee.FIXED32:
    case ee.SFIXED32:
    case ee.SINT32:
    case ee.UINT32:
      let c;
      if (typeof e == "number" ? c = e : typeof e == "string" && e.length > 0 && e.trim().length === e.length && (c = Number(e)), c === void 0) break;
      return a == ee.UINT32 || a == ee.FIXED32 ? gh(c) : vl(c), c;
    // int64, fixed64, uint64: JSON value will be a decimal string. Either numbers or strings are accepted.
    case ee.INT64:
    case ee.SFIXED64:
    case ee.SINT64:
      if (typeof e != "number" && typeof e != "string") break;
      const u = at.parse(e);
      return t ? u.toString() : u;
    case ee.FIXED64:
    case ee.UINT64:
      if (typeof e != "number" && typeof e != "string") break;
      const d = at.uParse(e);
      return t ? d.toString() : d;
    // bool:
    case ee.BOOL:
      if (typeof e != "boolean") break;
      return e;
    // string:
    case ee.STRING:
      if (typeof e != "string")
        break;
      try {
        encodeURIComponent(e);
      } catch {
        throw new Error("invalid UTF8");
      }
      return e;
    // bytes: JSON value will be the data encoded as a string using standard base64 encoding with paddings.
    // Either standard or URL-safe base64 encoding with/without paddings are accepted.
    case ee.BYTES:
      if (e === "") return new Uint8Array(0);
      if (typeof e != "string") break;
      return Z0.dec(e);
  }
  throw new Error();
}
function Gf(a, e, t, i) {
  if (e === null)
    return a.typeName == "google.protobuf.NullValue" ? 0 : i ? a.values[0].no : Pl;
  switch (typeof e) {
    case "number":
      if (Number.isInteger(e))
        return e;
      break;
    case "string":
      const s = a.findName(e);
      if (s !== void 0)
        return s.no;
      if (t)
        return bl;
      break;
  }
  throw new Error("cannot decode enum ".concat(a.typeName, " from JSON: ").concat(si(e)));
}
function Q_(a) {
  return a.repeated || a.kind == "map" ? !0 : !(a.oneof || a.kind == "message" || a.opt || a.req);
}
function ub(a, e, t) {
  if (a.kind == "map") {
    lt(typeof e == "object" && e != null);
    const u = {}, d = Object.entries(e);
    switch (a.V.kind) {
      case "scalar":
        for (const f of d) {
          var i = ge(f, 2);
          const p = i[0], v = i[1];
          u[p.toString()] = Tl(a.V.T, v);
        }
        break;
      case "message":
        for (const f of d) {
          var s = ge(f, 2);
          const p = s[0], v = s[1];
          u[p.toString()] = v.toJson(t);
        }
        break;
      case "enum":
        const h = a.V.T;
        for (const f of d) {
          var c = ge(f, 2);
          const p = c[0], v = c[1];
          u[p.toString()] = Kf(h, v, t.enumAsInteger);
        }
        break;
    }
    return t.emitDefaultValues || d.length > 0 ? u : void 0;
  }
  if (a.repeated) {
    lt(Array.isArray(e));
    const u = [];
    switch (a.kind) {
      case "scalar":
        for (let d = 0; d < e.length; d++)
          u.push(Tl(a.T, e[d]));
        break;
      case "enum":
        for (let d = 0; d < e.length; d++)
          u.push(Kf(a.T, e[d], t.enumAsInteger));
        break;
      case "message":
        for (let d = 0; d < e.length; d++)
          u.push(e[d].toJson(t));
        break;
    }
    return t.emitDefaultValues || u.length > 0 ? u : void 0;
  }
  switch (a.kind) {
    case "scalar":
      return Tl(a.T, e);
    case "enum":
      return Kf(a.T, e, t.enumAsInteger);
    case "message":
      return tT(a.T, e).toJson(t);
  }
}
function Kf(a, e, t) {
  var i;
  if (lt(typeof e == "number"), a.typeName == "google.protobuf.NullValue")
    return null;
  if (t)
    return e;
  const s = a.findNumber(e);
  return (i = s?.name) !== null && i !== void 0 ? i : e;
}
function Tl(a, e) {
  switch (a) {
    // int32, fixed32, uint32: JSON value will be a decimal number. Either numbers or strings are accepted.
    case ee.INT32:
    case ee.SFIXED32:
    case ee.SINT32:
    case ee.FIXED32:
    case ee.UINT32:
      return lt(typeof e == "number"), e;
    // float, double: JSON value will be a number or one of the special string values "NaN", "Infinity", and "-Infinity".
    // Either numbers or strings are accepted. Exponent notation is also accepted.
    case ee.FLOAT:
    // assertFloat32(value);
    case ee.DOUBLE:
      return lt(typeof e == "number"), Number.isNaN(e) ? "NaN" : e === Number.POSITIVE_INFINITY ? "Infinity" : e === Number.NEGATIVE_INFINITY ? "-Infinity" : e;
    // string:
    case ee.STRING:
      return lt(typeof e == "string"), e;
    // bool:
    case ee.BOOL:
      return lt(typeof e == "boolean"), e;
    // JSON value will be a decimal string. Either numbers or strings are accepted.
    case ee.UINT64:
    case ee.FIXED64:
    case ee.INT64:
    case ee.SFIXED64:
    case ee.SINT64:
      return lt(typeof e == "bigint" || typeof e == "string" || typeof e == "number"), e.toString();
    // bytes: JSON value will be the data encoded as a string using standard base64 encoding with paddings.
    // Either standard or URL-safe base64 encoding with/without paddings are accepted.
    case ee.BYTES:
      return lt(e instanceof Uint8Array), Z0.enc(e);
  }
}
const Yr = Symbol("@bufbuild/protobuf/unknown-fields"), db = {
  readUnknownFields: !0,
  readerFactory: (a) => new z_(a)
}, fb = {
  writeUnknownFields: !0,
  writerFactory: () => new B_()
};
function X_(a) {
  return a ? Object.assign(Object.assign({}, db), a) : db;
}
function Z_(a) {
  return a ? Object.assign(Object.assign({}, fb), a) : fb;
}
function W_() {
  return {
    makeReadOptions: X_,
    makeWriteOptions: Z_,
    listUnknownFields(a) {
      var e;
      return (e = a[Yr]) !== null && e !== void 0 ? e : [];
    },
    discardUnknownFields(a) {
      delete a[Yr];
    },
    writeUnknownFields(a, e) {
      const i = a[Yr];
      if (i)
        for (const s of i)
          e.tag(s.no, s.wireType).raw(s.data);
    },
    onUnknownField(a, e, t, i) {
      const s = a;
      Array.isArray(s[Yr]) || (s[Yr] = []), s[Yr].push({
        no: e,
        wireType: t,
        data: i
      });
    },
    readMessage(a, e, t, i, s) {
      const c = a.getType(), u = s ? e.len : e.pos + t;
      let d, h;
      for (; e.pos < u; ) {
        var f = e.tag(), p = ge(f, 2);
        if (d = p[0], h = p[1], s === !0 && h == ht.EndGroup)
          break;
        const v = c.fields.find(d);
        if (!v) {
          const y = e.skip(h, d);
          i.readUnknownFields && this.onUnknownField(a, d, h, y);
          continue;
        }
        hb(a, e, v, h, i);
      }
      if (s && // eslint-disable-line @typescript-eslint/strict-boolean-expressions
      (h != ht.EndGroup || d !== t))
        throw new Error("invalid end group tag");
    },
    readField: hb,
    writeMessage(a, e, t) {
      const i = a.getType();
      for (const s of i.fields.byNumber()) {
        if (!eT(s, a)) {
          if (s.req)
            throw new Error("cannot encode field ".concat(i.typeName, ".").concat(s.name, " to binary: required field not set"));
          continue;
        }
        const c = s.oneof ? a[s.oneof.localName].value : a[s.localName];
        mb(s, c, e, t);
      }
      return t.writeUnknownFields && this.writeUnknownFields(a, e), e;
    },
    writeField(a, e, t, i) {
      e !== void 0 && mb(a, e, t, i);
    }
  };
}
function hb(a, e, t, i, s) {
  let c = t.repeated, u = t.localName;
  switch (t.oneof && (a = a[t.oneof.localName], a.case != u && delete a.value, a.case = u, u = "value"), t.kind) {
    case "scalar":
    case "enum":
      const d = t.kind == "enum" ? ee.INT32 : t.T;
      let h = xl;
      if (t.kind == "scalar" && t.L > 0 && (h = eR), c) {
        let _ = a[u];
        if (i == ht.LengthDelimited && d != ee.STRING && d != ee.BYTES) {
          let x = e.uint32() + e.pos;
          for (; e.pos < x; )
            _.push(h(e, d));
        } else
          _.push(h(e, d));
      } else
        a[u] = h(e, d);
      break;
    case "message":
      const f = t.T;
      c ? a[u].push(Sl(e, new f(), s, t)) : xi(a[u]) ? Sl(e, a[u], s, t) : (a[u] = Sl(e, new f(), s, t), f.fieldWrapper && !t.oneof && !t.repeated && (a[u] = f.fieldWrapper.unwrapField(a[u])));
      break;
    case "map":
      let p = $_(t, e, s), v = ge(p, 2), y = v[0], T = v[1];
      a[u][y] = T;
      break;
  }
}
function Sl(a, e, t, i) {
  const s = e.getType().runtime.bin, c = i?.delimited;
  return s.readMessage(
    e,
    a,
    c ? i.no : a.uint32(),
    // eslint-disable-line @typescript-eslint/strict-boolean-expressions
    t,
    c
  ), e;
}
function $_(a, e, t) {
  const i = e.uint32(), s = e.pos + i;
  let c, u;
  for (; e.pos < s; ) {
    const d = e.tag();
    switch (ge(d, 1)[0]) {
      case 1:
        c = xl(e, a.K);
        break;
      case 2:
        switch (a.V.kind) {
          case "scalar":
            u = xl(e, a.V.T);
            break;
          case "enum":
            u = e.int32();
            break;
          case "message":
            u = Sl(e, new a.V.T(), t, void 0);
            break;
        }
        break;
    }
  }
  if (c === void 0 && (c = ls(a.K, Sa.BIGINT)), typeof c != "string" && typeof c != "number" && (c = c.toString()), u === void 0)
    switch (a.V.kind) {
      case "scalar":
        u = ls(a.V.T, Sa.BIGINT);
        break;
      case "enum":
        u = a.V.T.values[0].no;
        break;
      case "message":
        u = new a.V.T();
        break;
    }
  return [c, u];
}
function eR(a, e) {
  const t = xl(a, e);
  return typeof t == "bigint" ? t.toString() : t;
}
function xl(a, e) {
  switch (e) {
    case ee.STRING:
      return a.string();
    case ee.BOOL:
      return a.bool();
    case ee.DOUBLE:
      return a.double();
    case ee.FLOAT:
      return a.float();
    case ee.INT32:
      return a.int32();
    case ee.INT64:
      return a.int64();
    case ee.UINT64:
      return a.uint64();
    case ee.FIXED64:
      return a.fixed64();
    case ee.BYTES:
      return a.bytes();
    case ee.FIXED32:
      return a.fixed32();
    case ee.SFIXED32:
      return a.sfixed32();
    case ee.SFIXED64:
      return a.sfixed64();
    case ee.SINT64:
      return a.sint64();
    case ee.UINT32:
      return a.uint32();
    case ee.SINT32:
      return a.sint32();
  }
}
function mb(a, e, t, i) {
  lt(e !== void 0);
  const s = a.repeated;
  switch (a.kind) {
    case "scalar":
    case "enum":
      let u = a.kind == "enum" ? ee.INT32 : a.T;
      if (s)
        if (lt(Array.isArray(e)), a.packed)
          nR(t, u, a.no, e);
        else
          for (const d of e)
            ko(t, u, a.no, d);
      else
        ko(t, u, a.no, e);
      break;
    case "message":
      if (s) {
        lt(Array.isArray(e));
        for (const d of e)
          pb(t, i, a, d);
      } else
        pb(t, i, a, e);
      break;
    case "map":
      lt(typeof e == "object" && e != null);
      for (const d of Object.entries(e)) {
        var c = ge(d, 2);
        const h = c[0], f = c[1];
        tR(t, i, a, h, f);
      }
      break;
  }
}
function tR(a, e, t, i, s) {
  a.tag(t.no, ht.LengthDelimited), a.fork();
  let c = i;
  switch (t.K) {
    case ee.INT32:
    case ee.FIXED32:
    case ee.UINT32:
    case ee.SFIXED32:
    case ee.SINT32:
      c = Number.parseInt(i);
      break;
    case ee.BOOL:
      lt(i == "true" || i == "false"), c = i == "true";
      break;
  }
  switch (ko(a, t.K, 1, c), t.V.kind) {
    case "scalar":
      ko(a, t.V.T, 2, s);
      break;
    case "enum":
      ko(a, ee.INT32, 2, s);
      break;
    case "message":
      lt(s !== void 0), a.tag(2, ht.LengthDelimited).bytes(s.toBinary(e));
      break;
  }
  a.join();
}
function pb(a, e, t, i) {
  const s = tT(t.T, i);
  t.delimited ? a.tag(t.no, ht.StartGroup).raw(s.toBinary(e)).tag(t.no, ht.EndGroup) : a.tag(t.no, ht.LengthDelimited).bytes(s.toBinary(e));
}
function ko(a, e, t, i) {
  lt(i !== void 0);
  let s = nT(e), c = ge(s, 2), u = c[0], d = c[1];
  a.tag(t, u)[d](i);
}
function nR(a, e, t, i) {
  if (!i.length)
    return;
  a.tag(t, ht.LengthDelimited).fork();
  let s = nT(e), c = ge(s, 2), u = c[1];
  for (let d = 0; d < i.length; d++)
    a[u](i[d]);
  a.join();
}
function nT(a) {
  let e = ht.Varint;
  switch (a) {
    case ee.BYTES:
    case ee.STRING:
      e = ht.LengthDelimited;
      break;
    case ee.DOUBLE:
    case ee.FIXED64:
    case ee.SFIXED64:
      e = ht.Bit64;
      break;
    case ee.FIXED32:
    case ee.SFIXED32:
    case ee.FLOAT:
      e = ht.Bit32;
      break;
  }
  const t = ee[a].toLowerCase();
  return [e, t];
}
function iR() {
  return {
    setEnumType: V0,
    initPartial(a, e) {
      if (a === void 0)
        return;
      const t = e.getType();
      for (const s of t.fields.byMember()) {
        const c = s.localName, u = e, d = a;
        if (d[c] != null)
          switch (s.kind) {
            case "oneof":
              const h = d[c].case;
              if (h === void 0)
                continue;
              const f = s.findField(h);
              let p = d[c].value;
              f && f.kind == "message" && !xi(p, f.T) ? p = new f.T(p) : f && f.kind === "scalar" && f.T === ee.BYTES && (p = ho(p)), u[c] = {
                case: h,
                value: p
              };
              break;
            case "scalar":
            case "enum":
              let v = d[c];
              s.T === ee.BYTES && (v = s.repeated ? v.map(ho) : ho(v)), u[c] = v;
              break;
            case "map":
              switch (s.V.kind) {
                case "scalar":
                case "enum":
                  if (s.V.T === ee.BYTES)
                    for (const _ of Object.entries(d[c])) {
                      var i = ge(_, 2);
                      const k = i[0], x = i[1];
                      u[c][k] = ho(x);
                    }
                  else
                    Object.assign(u[c], d[c]);
                  break;
                case "message":
                  const T = s.V.T;
                  for (const _ of Object.keys(d[c])) {
                    let k = d[c][_];
                    T.fieldWrapper || (k = new T(k)), u[c][_] = k;
                  }
                  break;
              }
              break;
            case "message":
              const y = s.T;
              if (s.repeated)
                u[c] = d[c].map((T) => xi(T, y) ? T : new y(T));
              else {
                const T = d[c];
                y.fieldWrapper ? /* We can't use BytesValue.typeName as that will create a circular import */ y.typeName === "google.protobuf.BytesValue" ? u[c] = ho(T) : u[c] = T : u[c] = xi(T, y) ? T : new y(T);
              }
              break;
          }
      }
    },
    // TODO use isFieldSet() here to support future field presence
    equals(a, e, t) {
      return e === t ? !0 : !e || !t ? !1 : a.fields.byMember().every((i) => {
        const s = e[i.localName], c = t[i.localName];
        if (i.repeated) {
          if (s.length !== c.length)
            return !1;
          switch (i.kind) {
            case "message":
              return s.every((u, d) => i.T.equals(u, c[d]));
            case "scalar":
              return s.every((u, d) => fa(i.T, u, c[d]));
            case "enum":
              return s.every((u, d) => fa(ee.INT32, u, c[d]));
          }
          throw new Error("repeated cannot contain ".concat(i.kind));
        }
        switch (i.kind) {
          case "message":
            let u = s, d = c;
            return i.T.fieldWrapper && (u !== void 0 && !xi(u) && (u = i.T.fieldWrapper.wrapField(u)), d !== void 0 && !xi(d) && (d = i.T.fieldWrapper.wrapField(d))), i.T.equals(u, d);
          case "enum":
            return fa(ee.INT32, s, c);
          case "scalar":
            return fa(i.T, s, c);
          case "oneof":
            if (s.case !== c.case)
              return !1;
            const h = i.findField(s.case);
            if (h === void 0)
              return !0;
            switch (h.kind) {
              case "message":
                return h.T.equals(s.value, c.value);
              case "enum":
                return fa(ee.INT32, s.value, c.value);
              case "scalar":
                return fa(h.T, s.value, c.value);
            }
            throw new Error("oneof cannot contain ".concat(h.kind));
          case "map":
            const f = Object.keys(s).concat(Object.keys(c));
            switch (i.V.kind) {
              case "message":
                const p = i.V.T;
                return f.every((y) => p.equals(s[y], c[y]));
              case "enum":
                return f.every((y) => fa(ee.INT32, s[y], c[y]));
              case "scalar":
                const v = i.V.T;
                return f.every((y) => fa(v, s[y], c[y]));
            }
            break;
        }
      });
    },
    // TODO use isFieldSet() here to support future field presence
    clone(a) {
      const e = a.getType(), t = new e(), i = t;
      for (const c of e.fields.byMember()) {
        const u = a[c.localName];
        let d;
        if (c.repeated)
          d = u.map(cl);
        else if (c.kind == "map") {
          d = i[c.localName];
          for (const h of Object.entries(u)) {
            var s = ge(h, 2);
            const f = s[0], p = s[1];
            d[f] = cl(p);
          }
        } else c.kind == "oneof" ? d = c.findField(u.case) ? {
          case: u.case,
          value: cl(u.value)
        } : {
          case: void 0
        } : d = cl(u);
        i[c.localName] = d;
      }
      for (const c of e.runtime.bin.listUnknownFields(a))
        e.runtime.bin.onUnknownField(i, c.no, c.wireType, c.data);
      return t;
    }
  };
}
function cl(a) {
  if (a === void 0)
    return a;
  if (xi(a))
    return a.clone();
  if (a instanceof Uint8Array) {
    const e = new Uint8Array(a.byteLength);
    return e.set(a), e;
  }
  return a;
}
function ho(a) {
  return a instanceof Uint8Array ? a : new Uint8Array(a);
}
function aR(a, e, t) {
  return {
    syntax: a,
    json: Y_(),
    bin: W_(),
    util: Object.assign(Object.assign({}, iR()), {
      newFieldList: e,
      initFields: t
    }),
    makeMessageType(i, s, c) {
      return R_(this, i, s, c);
    },
    makeEnum: __,
    makeEnumType: G0,
    getEnumType: E_,
    makeExtension(i, s, c) {
      return F_(this, i, s, c);
    }
  };
}
class rR {
  constructor(e, t) {
    this._fields = e, this._normalizer = t;
  }
  findJsonName(e) {
    if (!this.jsonNames) {
      const t = {};
      for (const i of this.list())
        t[i.jsonName] = t[i.name] = i;
      this.jsonNames = t;
    }
    return this.jsonNames[e];
  }
  find(e) {
    if (!this.numbers) {
      const t = {};
      for (const i of this.list())
        t[i.no] = i;
      this.numbers = t;
    }
    return this.numbers[e];
  }
  list() {
    return this.all || (this.all = this._normalizer(this._fields)), this.all;
  }
  byNumber() {
    return this.numbersAsc || (this.numbersAsc = this.list().concat().sort((e, t) => e.no - t.no)), this.numbersAsc;
  }
  byMember() {
    if (!this.members) {
      this.members = [];
      const e = this.members;
      let t;
      for (const i of this.list())
        i.oneof ? i.oneof !== t && (t = i.oneof, e.push(t)) : e.push(i);
    }
    return this.members;
  }
}
function iT(a, e) {
  const t = aT(a);
  return e ? t : dR(uR(t));
}
function sR(a) {
  return iT(a, !1);
}
const oR = aT;
function aT(a) {
  let e = !1;
  const t = [];
  for (let i = 0; i < a.length; i++) {
    let s = a.charAt(i);
    switch (s) {
      case "_":
        e = !0;
        break;
      case "0":
      case "1":
      case "2":
      case "3":
      case "4":
      case "5":
      case "6":
      case "7":
      case "8":
      case "9":
        t.push(s), e = !1;
        break;
      default:
        e && (e = !1, s = s.toUpperCase()), t.push(s);
        break;
    }
  }
  return t.join("");
}
const cR = /* @__PURE__ */ new Set([
  // names reserved by JavaScript
  "constructor",
  "toString",
  "toJSON",
  "valueOf"
]), lR = /* @__PURE__ */ new Set([
  // names reserved by the runtime
  "getType",
  "clone",
  "equals",
  "fromBinary",
  "fromJson",
  "fromJsonString",
  "toBinary",
  "toJson",
  "toJsonString",
  // names reserved by the runtime for the future
  "toObject"
]), rT = (a) => "".concat(a, "$"), uR = (a) => lR.has(a) ? rT(a) : a, dR = (a) => cR.has(a) ? rT(a) : a;
class fR {
  constructor(e) {
    this.kind = "oneof", this.repeated = !1, this.packed = !1, this.opt = !1, this.req = !1, this.default = void 0, this.fields = [], this.name = e, this.localName = sR(e);
  }
  addField(e) {
    lt(e.oneof === this, "field ".concat(e.name, " not one of ").concat(this.name)), this.fields.push(e);
  }
  findField(e) {
    if (!this._lookup) {
      this._lookup = /* @__PURE__ */ Object.create(null);
      for (let t = 0; t < this.fields.length; t++)
        this._lookup[this.fields[t].localName] = this.fields[t];
    }
    return this._lookup[e];
  }
}
function hR(a, e) {
  var t, i, s, c, u, d;
  const h = [];
  let f;
  for (const p of typeof a == "function" ? a() : a) {
    const v = p;
    if (v.localName = iT(p.name, p.oneof !== void 0), v.jsonName = (t = p.jsonName) !== null && t !== void 0 ? t : oR(p.name), v.repeated = (i = p.repeated) !== null && i !== void 0 ? i : !1, p.kind == "scalar" && (v.L = (s = p.L) !== null && s !== void 0 ? s : Sa.BIGINT), v.delimited = (c = p.delimited) !== null && c !== void 0 ? c : !1, v.req = (u = p.req) !== null && u !== void 0 ? u : !1, v.opt = (d = p.opt) !== null && d !== void 0 ? d : !1, p.packed === void 0 && (v.packed = p.kind == "enum" || p.kind == "scalar" && p.T != ee.BYTES && p.T != ee.STRING), p.oneof !== void 0) {
      const y = typeof p.oneof == "string" ? p.oneof : p.oneof.name;
      (!f || f.name != y) && (f = new fR(y)), v.oneof = f, f.addField(v);
    }
    h.push(v);
  }
  return h;
}
const D = aR(
  "proto3",
  (a) => new rR(a, (e) => hR(e)),
  // TODO merge with proto2 and initExtensionField, also see initPartial, equals, clone
  (a) => {
    for (const e of a.getType().fields.byMember()) {
      if (e.opt)
        continue;
      const t = e.localName, i = a;
      if (e.repeated) {
        i[t] = [];
        continue;
      }
      switch (e.kind) {
        case "oneof":
          i[t] = {
            case: void 0
          };
          break;
        case "enum":
          i[t] = 0;
          break;
        case "map":
          i[t] = {};
          break;
        case "scalar":
          i[t] = ls(e.T, e.L);
          break;
      }
    }
  }
);
class nn extends Wh {
  constructor(e) {
    super(), this.seconds = at.zero, this.nanos = 0, D.util.initPartial(e, this);
  }
  fromJson(e, t) {
    if (typeof e != "string")
      throw new Error("cannot decode google.protobuf.Timestamp from JSON: ".concat(D.json.debug(e)));
    const i = e.match(/^([0-9]{4})-([0-9]{2})-([0-9]{2})T([0-9]{2}):([0-9]{2}):([0-9]{2})(?:Z|\.([0-9]{3,9})Z|([+-][0-9][0-9]:[0-9][0-9]))$/);
    if (!i)
      throw new Error("cannot decode google.protobuf.Timestamp from JSON: invalid RFC 3339 string");
    const s = Date.parse(i[1] + "-" + i[2] + "-" + i[3] + "T" + i[4] + ":" + i[5] + ":" + i[6] + (i[8] ? i[8] : "Z"));
    if (Number.isNaN(s))
      throw new Error("cannot decode google.protobuf.Timestamp from JSON: invalid RFC 3339 string");
    if (s < Date.parse("0001-01-01T00:00:00Z") || s > Date.parse("9999-12-31T23:59:59Z"))
      throw new Error("cannot decode message google.protobuf.Timestamp from JSON: must be from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59Z inclusive");
    return this.seconds = at.parse(s / 1e3), this.nanos = 0, i[7] && (this.nanos = parseInt("1" + i[7] + "0".repeat(9 - i[7].length)) - 1e9), this;
  }
  toJson(e) {
    const t = Number(this.seconds) * 1e3;
    if (t < Date.parse("0001-01-01T00:00:00Z") || t > Date.parse("9999-12-31T23:59:59Z"))
      throw new Error("cannot encode google.protobuf.Timestamp to JSON: must be from 0001-01-01T00:00:00Z to 9999-12-31T23:59:59Z inclusive");
    if (this.nanos < 0)
      throw new Error("cannot encode google.protobuf.Timestamp to JSON: nanos must not be negative");
    let i = "Z";
    if (this.nanos > 0) {
      const s = (this.nanos + 1e9).toString().substring(1);
      s.substring(3) === "000000" ? i = "." + s.substring(0, 3) + "Z" : s.substring(6) === "000" ? i = "." + s.substring(0, 6) + "Z" : i = "." + s + "Z";
    }
    return new Date(t).toISOString().replace(".000Z", i);
  }
  toDate() {
    return new Date(Number(this.seconds) * 1e3 + Math.ceil(this.nanos / 1e6));
  }
  static now() {
    return nn.fromDate(/* @__PURE__ */ new Date());
  }
  static fromDate(e) {
    const t = e.getTime();
    return new nn({
      seconds: at.parse(Math.floor(t / 1e3)),
      nanos: t % 1e3 * 1e6
    });
  }
  static fromBinary(e, t) {
    return new nn().fromBinary(e, t);
  }
  static fromJson(e, t) {
    return new nn().fromJson(e, t);
  }
  static fromJsonString(e, t) {
    return new nn().fromJsonString(e, t);
  }
  static equals(e, t) {
    return D.util.equals(nn, e, t);
  }
}
nn.runtime = D;
nn.typeName = "google.protobuf.Timestamp";
nn.fields = D.util.newFieldList(() => [{
  no: 1,
  name: "seconds",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 2,
  name: "nanos",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}]);
const mR = /* @__PURE__ */ D.makeMessageType("livekit.MetricsBatch", () => [{
  no: 1,
  name: "timestamp_ms",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 2,
  name: "normalized_timestamp",
  kind: "message",
  T: nn
}, {
  no: 3,
  name: "str_data",
  kind: "scalar",
  T: 9,
  repeated: !0
}, {
  no: 4,
  name: "time_series",
  kind: "message",
  T: pR,
  repeated: !0
}, {
  no: 5,
  name: "events",
  kind: "message",
  T: vR,
  repeated: !0
}]), pR = /* @__PURE__ */ D.makeMessageType("livekit.TimeSeriesMetric", () => [{
  no: 1,
  name: "label",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "participant_identity",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 3,
  name: "track_sid",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "samples",
  kind: "message",
  T: gR,
  repeated: !0
}, {
  no: 5,
  name: "rid",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]), gR = /* @__PURE__ */ D.makeMessageType("livekit.MetricSample", () => [{
  no: 1,
  name: "timestamp_ms",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 2,
  name: "normalized_timestamp",
  kind: "message",
  T: nn
}, {
  no: 3,
  name: "value",
  kind: "scalar",
  T: 2
  /* ScalarType.FLOAT */
}]), vR = /* @__PURE__ */ D.makeMessageType("livekit.EventMetric", () => [{
  no: 1,
  name: "label",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "participant_identity",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 3,
  name: "track_sid",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "start_timestamp_ms",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 5,
  name: "end_timestamp_ms",
  kind: "scalar",
  T: 3,
  opt: !0
}, {
  no: 6,
  name: "normalized_start_timestamp",
  kind: "message",
  T: nn
}, {
  no: 7,
  name: "normalized_end_timestamp",
  kind: "message",
  T: nn,
  opt: !0
}, {
  no: 8,
  name: "metadata",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 9,
  name: "rid",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]), yR = /* @__PURE__ */ D.makeEnum("livekit.AudioCodec", [{
  no: 0,
  name: "DEFAULT_AC"
}, {
  no: 1,
  name: "OPUS"
}, {
  no: 2,
  name: "AAC"
}, {
  no: 3,
  name: "AC_MP3"
}]), bR = /* @__PURE__ */ D.makeEnum("livekit.VideoCodec", [{
  no: 0,
  name: "DEFAULT_VC"
}, {
  no: 1,
  name: "H264_BASELINE"
}, {
  no: 2,
  name: "H264_MAIN"
}, {
  no: 3,
  name: "H264_HIGH"
}, {
  no: 4,
  name: "VP8"
}]), TR = /* @__PURE__ */ D.makeEnum("livekit.ImageCodec", [{
  no: 0,
  name: "IC_DEFAULT"
}, {
  no: 1,
  name: "IC_JPEG"
}]), sT = /* @__PURE__ */ D.makeEnum("livekit.BackupCodecPolicy", [{
  no: 0,
  name: "PREFER_REGRESSION"
}, {
  no: 1,
  name: "SIMULCAST"
}, {
  no: 2,
  name: "REGRESSION"
}]), jn = /* @__PURE__ */ D.makeEnum("livekit.TrackType", [{
  no: 0,
  name: "AUDIO"
}, {
  no: 1,
  name: "VIDEO"
}, {
  no: 2,
  name: "DATA"
}]), yt = /* @__PURE__ */ D.makeEnum("livekit.TrackSource", [{
  no: 0,
  name: "UNKNOWN"
}, {
  no: 1,
  name: "CAMERA"
}, {
  no: 2,
  name: "MICROPHONE"
}, {
  no: 3,
  name: "SCREEN_SHARE"
}, {
  no: 4,
  name: "SCREEN_SHARE_AUDIO"
}]), em = /* @__PURE__ */ D.makeEnum("livekit.VideoQuality", [{
  no: 0,
  name: "LOW"
}, {
  no: 1,
  name: "MEDIUM"
}, {
  no: 2,
  name: "HIGH"
}, {
  no: 3,
  name: "OFF"
}]), bo = /* @__PURE__ */ D.makeEnum("livekit.ConnectionQuality", [{
  no: 0,
  name: "POOR"
}, {
  no: 1,
  name: "GOOD"
}, {
  no: 2,
  name: "EXCELLENT"
}, {
  no: 3,
  name: "LOST"
}]), Ao = /* @__PURE__ */ D.makeEnum("livekit.ClientConfigSetting", [{
  no: 0,
  name: "UNSET"
}, {
  no: 1,
  name: "DISABLED"
}, {
  no: 2,
  name: "ENABLED"
}]), Yn = /* @__PURE__ */ D.makeEnum("livekit.DisconnectReason", [{
  no: 0,
  name: "UNKNOWN_REASON"
}, {
  no: 1,
  name: "CLIENT_INITIATED"
}, {
  no: 2,
  name: "DUPLICATE_IDENTITY"
}, {
  no: 3,
  name: "SERVER_SHUTDOWN"
}, {
  no: 4,
  name: "PARTICIPANT_REMOVED"
}, {
  no: 5,
  name: "ROOM_DELETED"
}, {
  no: 6,
  name: "STATE_MISMATCH"
}, {
  no: 7,
  name: "JOIN_FAILURE"
}, {
  no: 8,
  name: "MIGRATION"
}, {
  no: 9,
  name: "SIGNAL_CLOSE"
}, {
  no: 10,
  name: "ROOM_CLOSED"
}, {
  no: 11,
  name: "USER_UNAVAILABLE"
}, {
  no: 12,
  name: "USER_REJECTED"
}, {
  no: 13,
  name: "SIP_TRUNK_FAILURE"
}, {
  no: 14,
  name: "CONNECTION_TIMEOUT"
}, {
  no: 15,
  name: "MEDIA_FAILURE"
}, {
  no: 16,
  name: "AGENT_ERROR"
}]), Ka = /* @__PURE__ */ D.makeEnum("livekit.ReconnectReason", [{
  no: 0,
  name: "RR_UNKNOWN"
}, {
  no: 1,
  name: "RR_SIGNAL_DISCONNECTED"
}, {
  no: 2,
  name: "RR_PUBLISHER_FAILED"
}, {
  no: 3,
  name: "RR_SUBSCRIBER_FAILED"
}, {
  no: 4,
  name: "RR_SWITCH_CANDIDATE"
}]), SR = /* @__PURE__ */ D.makeEnum("livekit.SubscriptionError", [{
  no: 0,
  name: "SE_UNKNOWN"
}, {
  no: 1,
  name: "SE_CODEC_UNSUPPORTED"
}, {
  no: 2,
  name: "SE_TRACK_NOTFOUND"
}]), _t = /* @__PURE__ */ D.makeEnum("livekit.AudioTrackFeature", [{
  no: 0,
  name: "TF_STEREO"
}, {
  no: 1,
  name: "TF_NO_DTX"
}, {
  no: 2,
  name: "TF_AUTO_GAIN_CONTROL"
}, {
  no: 3,
  name: "TF_ECHO_CANCELLATION"
}, {
  no: 4,
  name: "TF_NOISE_SUPPRESSION"
}, {
  no: 5,
  name: "TF_ENHANCED_NOISE_CANCELLATION"
}, {
  no: 6,
  name: "TF_PRECONNECT_BUFFER"
}]), us = /* @__PURE__ */ D.makeEnum("livekit.PacketTrailerFeature", [{
  no: 0,
  name: "PTF_USER_TIMESTAMP"
}, {
  no: 1,
  name: "PTF_FRAME_ID"
}]), $l = /* @__PURE__ */ D.makeMessageType("livekit.Room", () => [{
  no: 1,
  name: "sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "empty_timeout",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 14,
  name: "departure_timeout",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "max_participants",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 5,
  name: "creation_time",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 15,
  name: "creation_time_ms",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 6,
  name: "turn_password",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 7,
  name: "enabled_codecs",
  kind: "message",
  T: Nl,
  repeated: !0
}, {
  no: 8,
  name: "metadata",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 9,
  name: "num_participants",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 11,
  name: "num_publishers",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 10,
  name: "active_recording",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 13,
  name: "version",
  kind: "message",
  T: TT
}]), Nl = /* @__PURE__ */ D.makeMessageType("livekit.Codec", () => [{
  no: 1,
  name: "mime",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "fmtp_line",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]), kR = /* @__PURE__ */ D.makeMessageType("livekit.ParticipantPermission", () => [{
  no: 1,
  name: "can_subscribe",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 2,
  name: "can_publish",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 3,
  name: "can_publish_data",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 9,
  name: "can_publish_sources",
  kind: "enum",
  T: D.getEnumType(yt),
  repeated: !0
}, {
  no: 7,
  name: "hidden",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 8,
  name: "recorder",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 10,
  name: "can_update_metadata",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 11,
  name: "agent",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 12,
  name: "can_subscribe_metrics",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 13,
  name: "can_manage_agent_session",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]), ar = /* @__PURE__ */ D.makeMessageType("livekit.ParticipantInfo", () => [{
  no: 1,
  name: "sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "identity",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "state",
  kind: "enum",
  T: D.getEnumType($r)
}, {
  no: 4,
  name: "tracks",
  kind: "message",
  T: Xr,
  repeated: !0
}, {
  no: 5,
  name: "metadata",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 6,
  name: "joined_at",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 17,
  name: "joined_at_ms",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 9,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 10,
  name: "version",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 11,
  name: "permission",
  kind: "message",
  T: kR
}, {
  no: 12,
  name: "region",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 13,
  name: "is_publisher",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 14,
  name: "kind",
  kind: "enum",
  T: D.getEnumType(ka)
}, {
  no: 15,
  name: "attributes",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}, {
  no: 16,
  name: "disconnect_reason",
  kind: "enum",
  T: D.getEnumType(Yn)
}, {
  no: 18,
  name: "kind_details",
  kind: "enum",
  T: D.getEnumType(CR),
  repeated: !0
}, {
  no: 19,
  name: "data_tracks",
  kind: "message",
  T: eu,
  repeated: !0
}, {
  no: 20,
  name: "client_protocol",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}]), $r = /* @__PURE__ */ D.makeEnum("livekit.ParticipantInfo.State", [{
  no: 0,
  name: "JOINING"
}, {
  no: 1,
  name: "JOINED"
}, {
  no: 2,
  name: "ACTIVE"
}, {
  no: 3,
  name: "DISCONNECTED"
}]), ka = /* @__PURE__ */ D.makeEnum("livekit.ParticipantInfo.Kind", [{
  no: 0,
  name: "STANDARD"
}, {
  no: 1,
  name: "INGRESS"
}, {
  no: 2,
  name: "EGRESS"
}, {
  no: 3,
  name: "SIP"
}, {
  no: 4,
  name: "AGENT"
}, {
  no: 7,
  name: "CONNECTOR"
}, {
  no: 8,
  name: "BRIDGE"
}]), CR = /* @__PURE__ */ D.makeEnum("livekit.ParticipantInfo.KindDetail", [{
  no: 0,
  name: "CLOUD_AGENT"
}, {
  no: 1,
  name: "FORWARDED"
}, {
  no: 2,
  name: "CONNECTOR_WHATSAPP"
}, {
  no: 3,
  name: "CONNECTOR_TWILIO"
}, {
  no: 4,
  name: "BRIDGE_RTSP"
}]), $e = /* @__PURE__ */ D.makeEnum("livekit.Encryption.Type", [{
  no: 0,
  name: "NONE"
}, {
  no: 1,
  name: "GCM"
}, {
  no: 2,
  name: "CUSTOM"
}]), ER = /* @__PURE__ */ D.makeMessageType("livekit.SimulcastCodecInfo", () => [{
  no: 1,
  name: "mime_type",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "mid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "cid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "layers",
  kind: "message",
  T: ya,
  repeated: !0
}, {
  no: 5,
  name: "video_layer_mode",
  kind: "enum",
  T: D.getEnumType(oT)
}, {
  no: 6,
  name: "sdp_cid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]), Xr = /* @__PURE__ */ D.makeMessageType("livekit.TrackInfo", () => [{
  no: 1,
  name: "sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "type",
  kind: "enum",
  T: D.getEnumType(jn)
}, {
  no: 3,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "muted",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 5,
  name: "width",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 6,
  name: "height",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 7,
  name: "simulcast",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 8,
  name: "disable_dtx",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 9,
  name: "source",
  kind: "enum",
  T: D.getEnumType(yt)
}, {
  no: 10,
  name: "layers",
  kind: "message",
  T: ya,
  repeated: !0
}, {
  no: 11,
  name: "mime_type",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 12,
  name: "mid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 13,
  name: "codecs",
  kind: "message",
  T: ER,
  repeated: !0
}, {
  no: 14,
  name: "stereo",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 15,
  name: "disable_red",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 16,
  name: "encryption",
  kind: "enum",
  T: D.getEnumType($e)
}, {
  no: 17,
  name: "stream",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 18,
  name: "version",
  kind: "message",
  T: TT
}, {
  no: 19,
  name: "audio_features",
  kind: "enum",
  T: D.getEnumType(_t),
  repeated: !0
}, {
  no: 20,
  name: "backup_codec_policy",
  kind: "enum",
  T: D.getEnumType(sT)
}, {
  no: 21,
  name: "packet_trailer_features",
  kind: "enum",
  T: D.getEnumType(us),
  repeated: !0
}]), eu = /* @__PURE__ */ D.makeMessageType("livekit.DataTrackInfo", () => [{
  no: 1,
  name: "pub_handle",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "encryption",
  kind: "enum",
  T: D.getEnumType($e)
}]), _R = /* @__PURE__ */ D.makeMessageType("livekit.DataTrackSubscriptionOptions", () => [{
  no: 1,
  name: "target_fps",
  kind: "scalar",
  T: 13,
  opt: !0
}]), ya = /* @__PURE__ */ D.makeMessageType("livekit.VideoLayer", () => [{
  no: 1,
  name: "quality",
  kind: "enum",
  T: D.getEnumType(em)
}, {
  no: 2,
  name: "width",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 3,
  name: "height",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "bitrate",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 5,
  name: "ssrc",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 6,
  name: "spatial_layer",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 7,
  name: "rid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 8,
  name: "repair_ssrc",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]), oT = /* @__PURE__ */ D.makeEnum("livekit.VideoLayer.Mode", [{
  no: 0,
  name: "MODE_UNUSED"
}, {
  no: 1,
  name: "ONE_SPATIAL_LAYER_PER_STREAM"
}, {
  no: 2,
  name: "MULTIPLE_SPATIAL_LAYERS_PER_STREAM"
}, {
  no: 3,
  name: "ONE_SPATIAL_LAYER_PER_STREAM_INCOMPLETE_RTCP_SR"
}]), Bt = /* @__PURE__ */ D.makeMessageType("livekit.DataPacket", () => [{
  no: 1,
  name: "kind",
  kind: "enum",
  T: D.getEnumType(ba)
}, {
  no: 4,
  name: "participant_identity",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "destination_identities",
  kind: "scalar",
  T: 9,
  repeated: !0
}, {
  no: 2,
  name: "user",
  kind: "message",
  T: tm,
  oneof: "value"
}, {
  no: 3,
  name: "speaker",
  kind: "message",
  T: RR,
  oneof: "value"
}, {
  no: 6,
  name: "sip_dtmf",
  kind: "message",
  T: dT,
  oneof: "value"
}, {
  no: 7,
  name: "transcription",
  kind: "message",
  T: wR,
  oneof: "value"
}, {
  no: 8,
  name: "metrics",
  kind: "message",
  T: mR,
  oneof: "value"
}, {
  no: 9,
  name: "chat_message",
  kind: "message",
  T: Il,
  oneof: "value"
}, {
  no: 10,
  name: "rpc_request",
  kind: "message",
  T: nm,
  oneof: "value"
}, {
  no: 11,
  name: "rpc_ack",
  kind: "message",
  T: tu,
  oneof: "value"
}, {
  no: 12,
  name: "rpc_response",
  kind: "message",
  T: im,
  oneof: "value"
}, {
  no: 13,
  name: "stream_header",
  kind: "message",
  T: Ll,
  oneof: "value"
}, {
  no: 14,
  name: "stream_chunk",
  kind: "message",
  T: Ul,
  oneof: "value"
}, {
  no: 15,
  name: "stream_trailer",
  kind: "message",
  T: Bl,
  oneof: "value"
}, {
  no: 18,
  name: "encrypted_packet",
  kind: "message",
  T: cT,
  oneof: "value"
}, {
  no: 16,
  name: "sequence",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 17,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]), ba = /* @__PURE__ */ D.makeEnum("livekit.DataPacket.Kind", [{
  no: 0,
  name: "RELIABLE"
}, {
  no: 1,
  name: "LOSSY"
}]), cT = /* @__PURE__ */ D.makeMessageType("livekit.EncryptedPacket", () => [{
  no: 1,
  name: "encryption_type",
  kind: "enum",
  T: D.getEnumType($e)
}, {
  no: 2,
  name: "iv",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}, {
  no: 3,
  name: "key_index",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "encrypted_value",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}]), lT = /* @__PURE__ */ D.makeMessageType("livekit.EncryptedPacketPayload", () => [{
  no: 1,
  name: "user",
  kind: "message",
  T: tm,
  oneof: "value"
}, {
  no: 3,
  name: "chat_message",
  kind: "message",
  T: Il,
  oneof: "value"
}, {
  no: 4,
  name: "rpc_request",
  kind: "message",
  T: nm,
  oneof: "value"
}, {
  no: 5,
  name: "rpc_ack",
  kind: "message",
  T: tu,
  oneof: "value"
}, {
  no: 6,
  name: "rpc_response",
  kind: "message",
  T: im,
  oneof: "value"
}, {
  no: 7,
  name: "stream_header",
  kind: "message",
  T: Ll,
  oneof: "value"
}, {
  no: 8,
  name: "stream_chunk",
  kind: "message",
  T: Ul,
  oneof: "value"
}, {
  no: 9,
  name: "stream_trailer",
  kind: "message",
  T: Bl,
  oneof: "value"
}]), RR = /* @__PURE__ */ D.makeMessageType("livekit.ActiveSpeakerUpdate", () => [{
  no: 1,
  name: "speakers",
  kind: "message",
  T: uT,
  repeated: !0
}]), uT = /* @__PURE__ */ D.makeMessageType("livekit.SpeakerInfo", () => [{
  no: 1,
  name: "sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "level",
  kind: "scalar",
  T: 2
  /* ScalarType.FLOAT */
}, {
  no: 3,
  name: "active",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]), tm = /* @__PURE__ */ D.makeMessageType("livekit.UserPacket", () => [{
  no: 1,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "participant_identity",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "payload",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}, {
  no: 3,
  name: "destination_sids",
  kind: "scalar",
  T: 9,
  repeated: !0
}, {
  no: 6,
  name: "destination_identities",
  kind: "scalar",
  T: 9,
  repeated: !0
}, {
  no: 4,
  name: "topic",
  kind: "scalar",
  T: 9,
  opt: !0
}, {
  no: 8,
  name: "id",
  kind: "scalar",
  T: 9,
  opt: !0
}, {
  no: 9,
  name: "start_time",
  kind: "scalar",
  T: 4,
  opt: !0
}, {
  no: 10,
  name: "end_time",
  kind: "scalar",
  T: 4,
  opt: !0
}, {
  no: 11,
  name: "nonce",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}]), dT = /* @__PURE__ */ D.makeMessageType("livekit.SipDTMF", () => [{
  no: 3,
  name: "code",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "digit",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]), wR = /* @__PURE__ */ D.makeMessageType("livekit.Transcription", () => [{
  no: 2,
  name: "transcribed_participant_identity",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "track_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "segments",
  kind: "message",
  T: MR,
  repeated: !0
}]), MR = /* @__PURE__ */ D.makeMessageType("livekit.TranscriptionSegment", () => [{
  no: 1,
  name: "id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "text",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "start_time",
  kind: "scalar",
  T: 4
  /* ScalarType.UINT64 */
}, {
  no: 4,
  name: "end_time",
  kind: "scalar",
  T: 4
  /* ScalarType.UINT64 */
}, {
  no: 5,
  name: "final",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 6,
  name: "language",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]), Il = /* @__PURE__ */ D.makeMessageType("livekit.ChatMessage", () => [{
  no: 1,
  name: "id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "timestamp",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 3,
  name: "edit_timestamp",
  kind: "scalar",
  T: 3,
  opt: !0
}, {
  no: 4,
  name: "message",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "deleted",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 6,
  name: "generated",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]), nm = /* @__PURE__ */ D.makeMessageType("livekit.RpcRequest", () => [{
  no: 1,
  name: "id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "method",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "payload",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "response_timeout_ms",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 5,
  name: "version",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 6,
  name: "compressed_payload",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}]), tu = /* @__PURE__ */ D.makeMessageType("livekit.RpcAck", () => [{
  no: 1,
  name: "request_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]), im = /* @__PURE__ */ D.makeMessageType("livekit.RpcResponse", () => [{
  no: 1,
  name: "request_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "payload",
  kind: "scalar",
  T: 9,
  oneof: "value"
}, {
  no: 3,
  name: "error",
  kind: "message",
  T: fT,
  oneof: "value"
}, {
  no: 4,
  name: "compressed_payload",
  kind: "scalar",
  T: 12,
  oneof: "value"
}]), fT = /* @__PURE__ */ D.makeMessageType("livekit.RpcError", () => [{
  no: 1,
  name: "code",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "message",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "data",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]), hT = /* @__PURE__ */ D.makeMessageType("livekit.ParticipantTracks", () => [{
  no: 1,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "track_sids",
  kind: "scalar",
  T: 9,
  repeated: !0
}]), mT = /* @__PURE__ */ D.makeMessageType("livekit.ServerInfo", () => [{
  no: 1,
  name: "edition",
  kind: "enum",
  T: D.getEnumType(pT)
}, {
  no: 2,
  name: "version",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "protocol",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 4,
  name: "region",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "node_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 6,
  name: "debug_info",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 7,
  name: "agent_protocol",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}]), pT = /* @__PURE__ */ D.makeEnum("livekit.ServerInfo.Edition", [{
  no: 0,
  name: "Standard"
}, {
  no: 1,
  name: "Cloud"
}]), gT = /* @__PURE__ */ D.makeMessageType("livekit.ClientInfo", () => [{
  no: 1,
  name: "sdk",
  kind: "enum",
  T: D.getEnumType(vT)
}, {
  no: 2,
  name: "version",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "protocol",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 4,
  name: "os",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "os_version",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 6,
  name: "device_model",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 7,
  name: "browser",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 8,
  name: "browser_version",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 9,
  name: "address",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 10,
  name: "network",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 11,
  name: "other_sdks",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 12,
  name: "client_protocol",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 13,
  name: "capabilities",
  kind: "enum",
  T: D.getEnumType(yT),
  repeated: !0
}]), vT = /* @__PURE__ */ D.makeEnum("livekit.ClientInfo.SDK", [{
  no: 0,
  name: "UNKNOWN"
}, {
  no: 1,
  name: "JS"
}, {
  no: 2,
  name: "SWIFT"
}, {
  no: 3,
  name: "ANDROID"
}, {
  no: 4,
  name: "FLUTTER"
}, {
  no: 5,
  name: "GO"
}, {
  no: 6,
  name: "UNITY"
}, {
  no: 7,
  name: "REACT_NATIVE"
}, {
  no: 8,
  name: "RUST"
}, {
  no: 9,
  name: "PYTHON"
}, {
  no: 10,
  name: "CPP"
}, {
  no: 11,
  name: "UNITY_WEB"
}, {
  no: 12,
  name: "NODE"
}, {
  no: 13,
  name: "UNREAL"
}, {
  no: 14,
  name: "ESP32"
}]), yT = /* @__PURE__ */ D.makeEnum("livekit.ClientInfo.Capability", [{
  no: 0,
  name: "CAP_UNUSED"
}, {
  no: 1,
  name: "CAP_PACKET_TRAILER"
}]), bT = /* @__PURE__ */ D.makeMessageType("livekit.ClientConfiguration", () => [{
  no: 1,
  name: "video",
  kind: "message",
  T: gb
}, {
  no: 2,
  name: "screen",
  kind: "message",
  T: gb
}, {
  no: 3,
  name: "resume_connection",
  kind: "enum",
  T: D.getEnumType(Ao)
}, {
  no: 4,
  name: "disabled_codecs",
  kind: "message",
  T: AR
}, {
  no: 5,
  name: "force_relay",
  kind: "enum",
  T: D.getEnumType(Ao)
}]), gb = /* @__PURE__ */ D.makeMessageType("livekit.VideoConfiguration", () => [{
  no: 1,
  name: "hardware_encoder",
  kind: "enum",
  T: D.getEnumType(Ao)
}]), AR = /* @__PURE__ */ D.makeMessageType("livekit.DisabledCodecs", () => [{
  no: 1,
  name: "codecs",
  kind: "message",
  T: Nl,
  repeated: !0
}, {
  no: 2,
  name: "publish",
  kind: "message",
  T: Nl,
  repeated: !0
}]), TT = /* @__PURE__ */ D.makeMessageType("livekit.TimedVersion", () => [{
  no: 1,
  name: "unix_micro",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 2,
  name: "ticks",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}]), vh = /* @__PURE__ */ D.makeEnum("livekit.DataStream.OperationType", [{
  no: 0,
  name: "CREATE"
}, {
  no: 1,
  name: "UPDATE"
}, {
  no: 2,
  name: "DELETE"
}, {
  no: 3,
  name: "REACTION"
}]), ST = /* @__PURE__ */ D.makeMessageType("livekit.DataStream.TextHeader", () => [{
  no: 1,
  name: "operation_type",
  kind: "enum",
  T: D.getEnumType(vh)
}, {
  no: 2,
  name: "version",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 3,
  name: "reply_to_stream_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "attached_stream_ids",
  kind: "scalar",
  T: 9,
  repeated: !0
}, {
  no: 5,
  name: "generated",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}], {
  localName: "DataStream_TextHeader"
}), kT = /* @__PURE__ */ D.makeMessageType("livekit.DataStream.ByteHeader", () => [{
  no: 1,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}], {
  localName: "DataStream_ByteHeader"
}), Ll = /* @__PURE__ */ D.makeMessageType("livekit.DataStream.Header", () => [{
  no: 1,
  name: "stream_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "timestamp",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 3,
  name: "topic",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "mime_type",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "total_length",
  kind: "scalar",
  T: 4,
  opt: !0
}, {
  no: 7,
  name: "encryption_type",
  kind: "enum",
  T: D.getEnumType($e)
}, {
  no: 8,
  name: "attributes",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}, {
  no: 9,
  name: "text_header",
  kind: "message",
  T: ST,
  oneof: "content_header"
}, {
  no: 10,
  name: "byte_header",
  kind: "message",
  T: kT,
  oneof: "content_header"
}], {
  localName: "DataStream_Header"
}), Ul = /* @__PURE__ */ D.makeMessageType("livekit.DataStream.Chunk", () => [{
  no: 1,
  name: "stream_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "chunk_index",
  kind: "scalar",
  T: 4
  /* ScalarType.UINT64 */
}, {
  no: 3,
  name: "content",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}, {
  no: 4,
  name: "version",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 5,
  name: "iv",
  kind: "scalar",
  T: 12,
  opt: !0
}], {
  localName: "DataStream_Chunk"
}), Bl = /* @__PURE__ */ D.makeMessageType("livekit.DataStream.Trailer", () => [{
  no: 1,
  name: "stream_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "reason",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "attributes",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}], {
  localName: "DataStream_Trailer"
}), OR = /* @__PURE__ */ D.makeMessageType("livekit.FilterParams", () => [{
  no: 1,
  name: "include_events",
  kind: "scalar",
  T: 9,
  repeated: !0
}, {
  no: 2,
  name: "exclude_events",
  kind: "scalar",
  T: 9,
  repeated: !0
}]), DR = /* @__PURE__ */ D.makeMessageType("livekit.WebhookConfig", () => [{
  no: 1,
  name: "url",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "signing_key",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "filter_params",
  kind: "message",
  T: OR
}]), PR = /* @__PURE__ */ D.makeMessageType("livekit.SubscribedAudioCodec", () => [{
  no: 1,
  name: "codec",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "enabled",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]), xR = /* @__PURE__ */ D.makeEnum("livekit.JobRestartPolicy", [{
  no: 0,
  name: "JRP_ON_FAILURE"
}, {
  no: 1,
  name: "JRP_NEVER"
}]), NR = /* @__PURE__ */ D.makeMessageType("livekit.RoomAgentDispatch", () => [{
  no: 1,
  name: "agent_name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "metadata",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "restart_policy",
  kind: "enum",
  T: D.getEnumType(xR)
}, {
  no: 4,
  name: "deployment",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]), CT = /* @__PURE__ */ D.makeEnum("livekit.EncodingOptionsPreset", [{
  no: 0,
  name: "H264_720P_30"
}, {
  no: 1,
  name: "H264_720P_60"
}, {
  no: 2,
  name: "H264_1080P_30"
}, {
  no: 3,
  name: "H264_1080P_60"
}, {
  no: 4,
  name: "PORTRAIT_H264_720P_30"
}, {
  no: 5,
  name: "PORTRAIT_H264_720P_60"
}, {
  no: 6,
  name: "PORTRAIT_H264_1080P_30"
}, {
  no: 7,
  name: "PORTRAIT_H264_1080P_60"
}]), IR = /* @__PURE__ */ D.makeEnum("livekit.EncodedFileType", [{
  no: 0,
  name: "DEFAULT_FILETYPE"
}, {
  no: 1,
  name: "MP4"
}, {
  no: 2,
  name: "OGG"
}, {
  no: 3,
  name: "MP3"
}]), LR = /* @__PURE__ */ D.makeEnum("livekit.StreamProtocol", [{
  no: 0,
  name: "DEFAULT_PROTOCOL"
}, {
  no: 1,
  name: "RTMP"
}, {
  no: 2,
  name: "SRT"
}, {
  no: 3,
  name: "WEBSOCKET"
}]), UR = /* @__PURE__ */ D.makeEnum("livekit.SegmentedFileProtocol", [{
  no: 0,
  name: "DEFAULT_SEGMENTED_FILE_PROTOCOL"
}, {
  no: 1,
  name: "HLS_PROTOCOL"
}]), BR = /* @__PURE__ */ D.makeEnum("livekit.SegmentedFileSuffix", [{
  no: 0,
  name: "INDEX"
}, {
  no: 1,
  name: "TIMESTAMP"
}]), zR = /* @__PURE__ */ D.makeEnum("livekit.ImageFileSuffix", [{
  no: 0,
  name: "IMAGE_SUFFIX_INDEX"
}, {
  no: 1,
  name: "IMAGE_SUFFIX_TIMESTAMP"
}, {
  no: 2,
  name: "IMAGE_SUFFIX_NONE_OVERWRITE"
}]), FR = /* @__PURE__ */ D.makeEnum("livekit.AudioMixing", [{
  no: 0,
  name: "DEFAULT_MIXING"
}, {
  no: 1,
  name: "DUAL_CHANNEL_AGENT"
}, {
  no: 2,
  name: "DUAL_CHANNEL_ALTERNATE"
}]), ET = /* @__PURE__ */ D.makeMessageType("livekit.EncodingOptions", () => [{
  no: 1,
  name: "width",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 2,
  name: "height",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 3,
  name: "depth",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 4,
  name: "framerate",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 5,
  name: "audio_codec",
  kind: "enum",
  T: D.getEnumType(yR)
}, {
  no: 6,
  name: "audio_bitrate",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 7,
  name: "audio_frequency",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 8,
  name: "video_codec",
  kind: "enum",
  T: D.getEnumType(bR)
}, {
  no: 9,
  name: "video_bitrate",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 10,
  name: "key_frame_interval",
  kind: "scalar",
  T: 1
  /* ScalarType.DOUBLE */
}, {
  no: 11,
  name: "audio_quality",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 12,
  name: "video_quality",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}]), vb = /* @__PURE__ */ D.makeMessageType("livekit.StreamOutput", () => [{
  no: 1,
  name: "protocol",
  kind: "enum",
  T: D.getEnumType(LR)
}, {
  no: 2,
  name: "urls",
  kind: "scalar",
  T: 9,
  repeated: !0
}]), yh = /* @__PURE__ */ D.makeMessageType("livekit.SegmentedFileOutput", () => [{
  no: 1,
  name: "protocol",
  kind: "enum",
  T: D.getEnumType(UR)
}, {
  no: 2,
  name: "filename_prefix",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "playlist_name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 11,
  name: "live_playlist_name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "segment_duration",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 10,
  name: "filename_suffix",
  kind: "enum",
  T: D.getEnumType(BR)
}, {
  no: 8,
  name: "disable_manifest",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 5,
  name: "s3",
  kind: "message",
  T: nu,
  oneof: "output"
}, {
  no: 6,
  name: "gcp",
  kind: "message",
  T: iu,
  oneof: "output"
}, {
  no: 7,
  name: "azure",
  kind: "message",
  T: au,
  oneof: "output"
}, {
  no: 9,
  name: "aliOSS",
  kind: "message",
  T: ru,
  oneof: "output"
}]), jR = /* @__PURE__ */ D.makeMessageType("livekit.ImageOutput", () => [{
  no: 1,
  name: "capture_interval",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "width",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 3,
  name: "height",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 4,
  name: "filename_prefix",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "filename_suffix",
  kind: "enum",
  T: D.getEnumType(zR)
}, {
  no: 6,
  name: "image_codec",
  kind: "enum",
  T: D.getEnumType(TR)
}, {
  no: 7,
  name: "disable_manifest",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 8,
  name: "s3",
  kind: "message",
  T: nu,
  oneof: "output"
}, {
  no: 9,
  name: "gcp",
  kind: "message",
  T: iu,
  oneof: "output"
}, {
  no: 10,
  name: "azure",
  kind: "message",
  T: au,
  oneof: "output"
}, {
  no: 11,
  name: "aliOSS",
  kind: "message",
  T: ru,
  oneof: "output"
}]), nu = /* @__PURE__ */ D.makeMessageType("livekit.S3Upload", () => [{
  no: 1,
  name: "access_key",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "secret",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 11,
  name: "session_token",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 12,
  name: "assume_role_arn",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 13,
  name: "assume_role_external_id",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "region",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "endpoint",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "bucket",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 6,
  name: "force_path_style",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 7,
  name: "metadata",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}, {
  no: 8,
  name: "tagging",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 9,
  name: "content_disposition",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 10,
  name: "proxy",
  kind: "message",
  T: _T
}]), iu = /* @__PURE__ */ D.makeMessageType("livekit.GCPUpload", () => [{
  no: 1,
  name: "credentials",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "bucket",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "proxy",
  kind: "message",
  T: _T
}]), au = /* @__PURE__ */ D.makeMessageType("livekit.AzureBlobUpload", () => [{
  no: 1,
  name: "account_name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "account_key",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "container_name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]), ru = /* @__PURE__ */ D.makeMessageType("livekit.AliOSSUpload", () => [{
  no: 1,
  name: "access_key",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "secret",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "region",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "endpoint",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "bucket",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]), _T = /* @__PURE__ */ D.makeMessageType("livekit.ProxyConfig", () => [{
  no: 1,
  name: "url",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "username",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "password",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]), qR = /* @__PURE__ */ D.makeMessageType("livekit.AutoParticipantEgress", () => [{
  no: 1,
  name: "preset",
  kind: "enum",
  T: D.getEnumType(CT),
  oneof: "options"
}, {
  no: 2,
  name: "advanced",
  kind: "message",
  T: ET,
  oneof: "options"
}, {
  no: 3,
  name: "file_outputs",
  kind: "message",
  T: bh,
  repeated: !0
}, {
  no: 4,
  name: "segment_outputs",
  kind: "message",
  T: yh,
  repeated: !0
}]), HR = /* @__PURE__ */ D.makeMessageType("livekit.AutoTrackEgress", () => [{
  no: 1,
  name: "filepath",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "disable_manifest",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 2,
  name: "s3",
  kind: "message",
  T: nu,
  oneof: "output"
}, {
  no: 3,
  name: "gcp",
  kind: "message",
  T: iu,
  oneof: "output"
}, {
  no: 4,
  name: "azure",
  kind: "message",
  T: au,
  oneof: "output"
}, {
  no: 6,
  name: "aliOSS",
  kind: "message",
  T: ru,
  oneof: "output"
}]), VR = /* @__PURE__ */ D.makeMessageType("livekit.RoomCompositeEgressRequest", () => [{
  no: 1,
  name: "room_name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "layout",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "audio_only",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 15,
  name: "audio_mixing",
  kind: "enum",
  T: D.getEnumType(FR)
}, {
  no: 4,
  name: "video_only",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 5,
  name: "custom_base_url",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 6,
  name: "file",
  kind: "message",
  T: bh,
  oneof: "output"
}, {
  no: 7,
  name: "stream",
  kind: "message",
  T: vb,
  oneof: "output"
}, {
  no: 10,
  name: "segments",
  kind: "message",
  T: yh,
  oneof: "output"
}, {
  no: 8,
  name: "preset",
  kind: "enum",
  T: D.getEnumType(CT),
  oneof: "options"
}, {
  no: 9,
  name: "advanced",
  kind: "message",
  T: ET,
  oneof: "options"
}, {
  no: 11,
  name: "file_outputs",
  kind: "message",
  T: bh,
  repeated: !0
}, {
  no: 12,
  name: "stream_outputs",
  kind: "message",
  T: vb,
  repeated: !0
}, {
  no: 13,
  name: "segment_outputs",
  kind: "message",
  T: yh,
  repeated: !0
}, {
  no: 14,
  name: "image_outputs",
  kind: "message",
  T: jR,
  repeated: !0
}, {
  no: 16,
  name: "webhooks",
  kind: "message",
  T: DR,
  repeated: !0
}]), bh = /* @__PURE__ */ D.makeMessageType("livekit.EncodedFileOutput", () => [{
  no: 1,
  name: "file_type",
  kind: "enum",
  T: D.getEnumType(IR)
}, {
  no: 2,
  name: "filepath",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 6,
  name: "disable_manifest",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 3,
  name: "s3",
  kind: "message",
  T: nu,
  oneof: "output"
}, {
  no: 4,
  name: "gcp",
  kind: "message",
  T: iu,
  oneof: "output"
}, {
  no: 5,
  name: "azure",
  kind: "message",
  T: au,
  oneof: "output"
}, {
  no: 7,
  name: "aliOSS",
  kind: "message",
  T: ru,
  oneof: "output"
}]), GR = /* @__PURE__ */ D.makeMessageType("livekit.RoomEgress", () => [{
  no: 1,
  name: "room",
  kind: "message",
  T: VR
}, {
  no: 3,
  name: "participant",
  kind: "message",
  T: qR
}, {
  no: 2,
  name: "tracks",
  kind: "message",
  T: HR
}]), KR = /* @__PURE__ */ D.makeMessageType("livekit.RoomConfiguration", () => [{
  no: 1,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "empty_timeout",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 3,
  name: "departure_timeout",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "max_participants",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 11,
  name: "metadata",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "egress",
  kind: "message",
  T: GR
}, {
  no: 7,
  name: "min_playout_delay",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 8,
  name: "max_playout_delay",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 9,
  name: "sync_streams",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 10,
  name: "agents",
  kind: "message",
  T: NR,
  repeated: !0
}, {
  no: 12,
  name: "tags",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}]), qn = /* @__PURE__ */ D.makeEnum("livekit.SignalTarget", [{
  no: 0,
  name: "PUBLISHER"
}, {
  no: 1,
  name: "SUBSCRIBER"
}]), Th = /* @__PURE__ */ D.makeEnum("livekit.StreamState", [{
  no: 0,
  name: "ACTIVE"
}, {
  no: 1,
  name: "PAUSED"
}]), YR = /* @__PURE__ */ D.makeEnum("livekit.CandidateProtocol", [{
  no: 0,
  name: "UDP"
}, {
  no: 1,
  name: "TCP"
}, {
  no: 2,
  name: "TLS"
}]), JR = /* @__PURE__ */ D.makeMessageType("livekit.SignalRequest", () => [{
  no: 1,
  name: "offer",
  kind: "message",
  T: Ca,
  oneof: "message"
}, {
  no: 2,
  name: "answer",
  kind: "message",
  T: Ca,
  oneof: "message"
}, {
  no: 3,
  name: "trickle",
  kind: "message",
  T: su,
  oneof: "message"
}, {
  no: 4,
  name: "add_track",
  kind: "message",
  T: Oo,
  oneof: "message"
}, {
  no: 5,
  name: "mute",
  kind: "message",
  T: ou,
  oneof: "message"
}, {
  no: 6,
  name: "subscription",
  kind: "message",
  T: cu,
  oneof: "message"
}, {
  no: 7,
  name: "track_setting",
  kind: "message",
  T: MT,
  oneof: "message"
}, {
  no: 8,
  name: "leave",
  kind: "message",
  T: lu,
  oneof: "message"
}, {
  no: 10,
  name: "update_layers",
  kind: "message",
  T: OT,
  oneof: "message"
}, {
  no: 11,
  name: "subscription_permission",
  kind: "message",
  T: xT,
  oneof: "message"
}, {
  no: 12,
  name: "sync_state",
  kind: "message",
  T: dm,
  oneof: "message"
}, {
  no: 13,
  name: "simulate",
  kind: "message",
  T: ri,
  oneof: "message"
}, {
  no: 14,
  name: "ping",
  kind: "scalar",
  T: 3,
  oneof: "message"
}, {
  no: 15,
  name: "update_metadata",
  kind: "message",
  T: lm,
  oneof: "message"
}, {
  no: 16,
  name: "ping_req",
  kind: "message",
  T: LT,
  oneof: "message"
}, {
  no: 17,
  name: "update_audio_track",
  kind: "message",
  T: cm,
  oneof: "message"
}, {
  no: 18,
  name: "update_video_track",
  kind: "message",
  T: AT,
  oneof: "message"
}, {
  no: 19,
  name: "publish_data_track_request",
  kind: "message",
  T: am,
  oneof: "message"
}, {
  no: 20,
  name: "unpublish_data_track_request",
  kind: "message",
  T: sm,
  oneof: "message"
}, {
  no: 21,
  name: "update_data_subscription",
  kind: "message",
  T: RT,
  oneof: "message"
}]), yb = /* @__PURE__ */ D.makeMessageType("livekit.SignalResponse", () => [{
  no: 1,
  name: "join",
  kind: "message",
  T: WR,
  oneof: "message"
}, {
  no: 2,
  name: "answer",
  kind: "message",
  T: Ca,
  oneof: "message"
}, {
  no: 3,
  name: "offer",
  kind: "message",
  T: Ca,
  oneof: "message"
}, {
  no: 4,
  name: "trickle",
  kind: "message",
  T: su,
  oneof: "message"
}, {
  no: 5,
  name: "update",
  kind: "message",
  T: t1,
  oneof: "message"
}, {
  no: 6,
  name: "track_published",
  kind: "message",
  T: om,
  oneof: "message"
}, {
  no: 8,
  name: "leave",
  kind: "message",
  T: lu,
  oneof: "message"
}, {
  no: 9,
  name: "mute",
  kind: "message",
  T: ou,
  oneof: "message"
}, {
  no: 10,
  name: "speakers_changed",
  kind: "message",
  T: n1,
  oneof: "message"
}, {
  no: 11,
  name: "room_update",
  kind: "message",
  T: i1,
  oneof: "message"
}, {
  no: 12,
  name: "connection_quality",
  kind: "message",
  T: r1,
  oneof: "message"
}, {
  no: 13,
  name: "stream_state_update",
  kind: "message",
  T: o1,
  oneof: "message"
}, {
  no: 14,
  name: "subscribed_quality_update",
  kind: "message",
  T: l1,
  oneof: "message"
}, {
  no: 15,
  name: "subscription_permission_update",
  kind: "message",
  T: d1,
  oneof: "message"
}, {
  no: 16,
  name: "refresh_token",
  kind: "scalar",
  T: 9,
  oneof: "message"
}, {
  no: 17,
  name: "track_unpublished",
  kind: "message",
  T: e1,
  oneof: "message"
}, {
  no: 18,
  name: "pong",
  kind: "scalar",
  T: 3,
  oneof: "message"
}, {
  no: 19,
  name: "reconnect",
  kind: "message",
  T: $R,
  oneof: "message"
}, {
  no: 20,
  name: "pong_resp",
  kind: "message",
  T: h1,
  oneof: "message"
}, {
  no: 21,
  name: "subscription_response",
  kind: "message",
  T: g1,
  oneof: "message"
}, {
  no: 22,
  name: "request_response",
  kind: "message",
  T: v1,
  oneof: "message"
}, {
  no: 23,
  name: "track_subscribed",
  kind: "message",
  T: y1,
  oneof: "message"
}, {
  no: 24,
  name: "room_moved",
  kind: "message",
  T: f1,
  oneof: "message"
}, {
  no: 25,
  name: "media_sections_requirement",
  kind: "message",
  T: S1,
  oneof: "message"
}, {
  no: 26,
  name: "subscribed_audio_codec_update",
  kind: "message",
  T: u1,
  oneof: "message"
}, {
  no: 27,
  name: "publish_data_track_response",
  kind: "message",
  T: rm,
  oneof: "message"
}, {
  no: 28,
  name: "unpublish_data_track_response",
  kind: "message",
  T: QR,
  oneof: "message"
}, {
  no: 29,
  name: "data_track_subscriber_handles",
  kind: "message",
  T: XR,
  oneof: "message"
}]), Sh = /* @__PURE__ */ D.makeMessageType("livekit.SimulcastCodec", () => [{
  no: 1,
  name: "codec",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "cid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "layers",
  kind: "message",
  T: ya,
  repeated: !0
}, {
  no: 5,
  name: "video_layer_mode",
  kind: "enum",
  T: D.getEnumType(oT)
}]), Oo = /* @__PURE__ */ D.makeMessageType("livekit.AddTrackRequest", () => [{
  no: 1,
  name: "cid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "type",
  kind: "enum",
  T: D.getEnumType(jn)
}, {
  no: 4,
  name: "width",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 5,
  name: "height",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 6,
  name: "muted",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 7,
  name: "disable_dtx",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 8,
  name: "source",
  kind: "enum",
  T: D.getEnumType(yt)
}, {
  no: 9,
  name: "layers",
  kind: "message",
  T: ya,
  repeated: !0
}, {
  no: 10,
  name: "simulcast_codecs",
  kind: "message",
  T: Sh,
  repeated: !0
}, {
  no: 11,
  name: "sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 12,
  name: "stereo",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 13,
  name: "disable_red",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 14,
  name: "encryption",
  kind: "enum",
  T: D.getEnumType($e)
}, {
  no: 15,
  name: "stream",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 16,
  name: "backup_codec_policy",
  kind: "enum",
  T: D.getEnumType(sT)
}, {
  no: 17,
  name: "audio_features",
  kind: "enum",
  T: D.getEnumType(_t),
  repeated: !0
}, {
  no: 18,
  name: "packet_trailer_features",
  kind: "enum",
  T: D.getEnumType(us),
  repeated: !0
}]), am = /* @__PURE__ */ D.makeMessageType("livekit.PublishDataTrackRequest", () => [{
  no: 1,
  name: "pub_handle",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "encryption",
  kind: "enum",
  T: D.getEnumType($e)
}]), rm = /* @__PURE__ */ D.makeMessageType("livekit.PublishDataTrackResponse", () => [{
  no: 1,
  name: "info",
  kind: "message",
  T: eu
}]), sm = /* @__PURE__ */ D.makeMessageType("livekit.UnpublishDataTrackRequest", () => [{
  no: 1,
  name: "pub_handle",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]), QR = /* @__PURE__ */ D.makeMessageType("livekit.UnpublishDataTrackResponse", () => [{
  no: 1,
  name: "info",
  kind: "message",
  T: eu
}]), XR = /* @__PURE__ */ D.makeMessageType("livekit.DataTrackSubscriberHandles", () => [{
  no: 1,
  name: "sub_handles",
  kind: "map",
  K: 13,
  V: {
    kind: "message",
    T: ZR
  }
}]), ZR = /* @__PURE__ */ D.makeMessageType("livekit.DataTrackSubscriberHandles.PublishedDataTrack", () => [{
  no: 1,
  name: "publisher_identity",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "publisher_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}], {
  localName: "DataTrackSubscriberHandles_PublishedDataTrack"
}), su = /* @__PURE__ */ D.makeMessageType("livekit.TrickleRequest", () => [{
  no: 1,
  name: "candidateInit",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "target",
  kind: "enum",
  T: D.getEnumType(qn)
}, {
  no: 3,
  name: "final",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]), ou = /* @__PURE__ */ D.makeMessageType("livekit.MuteTrackRequest", () => [{
  no: 1,
  name: "sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "muted",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]), WR = /* @__PURE__ */ D.makeMessageType("livekit.JoinResponse", () => [{
  no: 1,
  name: "room",
  kind: "message",
  T: $l
}, {
  no: 2,
  name: "participant",
  kind: "message",
  T: ar
}, {
  no: 3,
  name: "other_participants",
  kind: "message",
  T: ar,
  repeated: !0
}, {
  no: 4,
  name: "server_version",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 5,
  name: "ice_servers",
  kind: "message",
  T: DT,
  repeated: !0
}, {
  no: 6,
  name: "subscriber_primary",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 7,
  name: "alternative_url",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 8,
  name: "client_configuration",
  kind: "message",
  T: bT
}, {
  no: 9,
  name: "server_region",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 10,
  name: "ping_timeout",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 11,
  name: "ping_interval",
  kind: "scalar",
  T: 5
  /* ScalarType.INT32 */
}, {
  no: 12,
  name: "server_info",
  kind: "message",
  T: mT
}, {
  no: 13,
  name: "sif_trailer",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}, {
  no: 14,
  name: "enabled_publish_codecs",
  kind: "message",
  T: Nl,
  repeated: !0
}, {
  no: 15,
  name: "fast_publish",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]), $R = /* @__PURE__ */ D.makeMessageType("livekit.ReconnectResponse", () => [{
  no: 1,
  name: "ice_servers",
  kind: "message",
  T: DT,
  repeated: !0
}, {
  no: 2,
  name: "client_configuration",
  kind: "message",
  T: bT
}, {
  no: 3,
  name: "server_info",
  kind: "message",
  T: mT
}, {
  no: 4,
  name: "last_message_seq",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]), om = /* @__PURE__ */ D.makeMessageType("livekit.TrackPublishedResponse", () => [{
  no: 1,
  name: "cid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "track",
  kind: "message",
  T: Xr
}]), e1 = /* @__PURE__ */ D.makeMessageType("livekit.TrackUnpublishedResponse", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]), Ca = /* @__PURE__ */ D.makeMessageType("livekit.SessionDescription", () => [{
  no: 1,
  name: "type",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "sdp",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "id",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 4,
  name: "mid_to_track_id",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}]), t1 = /* @__PURE__ */ D.makeMessageType("livekit.ParticipantUpdate", () => [{
  no: 1,
  name: "participants",
  kind: "message",
  T: ar,
  repeated: !0
}]), cu = /* @__PURE__ */ D.makeMessageType("livekit.UpdateSubscription", () => [{
  no: 1,
  name: "track_sids",
  kind: "scalar",
  T: 9,
  repeated: !0
}, {
  no: 2,
  name: "subscribe",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 3,
  name: "participant_tracks",
  kind: "message",
  T: hT,
  repeated: !0
}]), RT = /* @__PURE__ */ D.makeMessageType("livekit.UpdateDataSubscription", () => [{
  no: 1,
  name: "updates",
  kind: "message",
  T: wT,
  repeated: !0
}]), wT = /* @__PURE__ */ D.makeMessageType("livekit.UpdateDataSubscription.Update", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "subscribe",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 3,
  name: "options",
  kind: "message",
  T: _R
}], {
  localName: "UpdateDataSubscription_Update"
}), MT = /* @__PURE__ */ D.makeMessageType("livekit.UpdateTrackSettings", () => [{
  no: 1,
  name: "track_sids",
  kind: "scalar",
  T: 9,
  repeated: !0
}, {
  no: 3,
  name: "disabled",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 4,
  name: "quality",
  kind: "enum",
  T: D.getEnumType(em)
}, {
  no: 5,
  name: "width",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 6,
  name: "height",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 7,
  name: "fps",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 8,
  name: "priority",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]), cm = /* @__PURE__ */ D.makeMessageType("livekit.UpdateLocalAudioTrack", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "features",
  kind: "enum",
  T: D.getEnumType(_t),
  repeated: !0
}]), AT = /* @__PURE__ */ D.makeMessageType("livekit.UpdateLocalVideoTrack", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "width",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 3,
  name: "height",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]), lu = /* @__PURE__ */ D.makeMessageType("livekit.LeaveRequest", () => [{
  no: 1,
  name: "can_reconnect",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 2,
  name: "reason",
  kind: "enum",
  T: D.getEnumType(Yn)
}, {
  no: 3,
  name: "action",
  kind: "enum",
  T: D.getEnumType(es)
}, {
  no: 4,
  name: "regions",
  kind: "message",
  T: m1
}]), es = /* @__PURE__ */ D.makeEnum("livekit.LeaveRequest.Action", [{
  no: 0,
  name: "DISCONNECT"
}, {
  no: 1,
  name: "RESUME"
}, {
  no: 2,
  name: "RECONNECT"
}]), OT = /* @__PURE__ */ D.makeMessageType("livekit.UpdateVideoLayers", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "layers",
  kind: "message",
  T: ya,
  repeated: !0
}]), lm = /* @__PURE__ */ D.makeMessageType("livekit.UpdateParticipantMetadata", () => [{
  no: 1,
  name: "metadata",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "name",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "attributes",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}, {
  no: 4,
  name: "request_id",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]), DT = /* @__PURE__ */ D.makeMessageType("livekit.ICEServer", () => [{
  no: 1,
  name: "urls",
  kind: "scalar",
  T: 9,
  repeated: !0
}, {
  no: 2,
  name: "username",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "credential",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]), n1 = /* @__PURE__ */ D.makeMessageType("livekit.SpeakersChanged", () => [{
  no: 1,
  name: "speakers",
  kind: "message",
  T: uT,
  repeated: !0
}]), i1 = /* @__PURE__ */ D.makeMessageType("livekit.RoomUpdate", () => [{
  no: 1,
  name: "room",
  kind: "message",
  T: $l
}]), a1 = /* @__PURE__ */ D.makeMessageType("livekit.ConnectionQualityInfo", () => [{
  no: 1,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "quality",
  kind: "enum",
  T: D.getEnumType(bo)
}, {
  no: 3,
  name: "score",
  kind: "scalar",
  T: 2
  /* ScalarType.FLOAT */
}]), r1 = /* @__PURE__ */ D.makeMessageType("livekit.ConnectionQualityUpdate", () => [{
  no: 1,
  name: "updates",
  kind: "message",
  T: a1,
  repeated: !0
}]), s1 = /* @__PURE__ */ D.makeMessageType("livekit.StreamStateInfo", () => [{
  no: 1,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "state",
  kind: "enum",
  T: D.getEnumType(Th)
}]), o1 = /* @__PURE__ */ D.makeMessageType("livekit.StreamStateUpdate", () => [{
  no: 1,
  name: "stream_states",
  kind: "message",
  T: s1,
  repeated: !0
}]), um = /* @__PURE__ */ D.makeMessageType("livekit.SubscribedQuality", () => [{
  no: 1,
  name: "quality",
  kind: "enum",
  T: D.getEnumType(em)
}, {
  no: 2,
  name: "enabled",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]), c1 = /* @__PURE__ */ D.makeMessageType("livekit.SubscribedCodec", () => [{
  no: 1,
  name: "codec",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "qualities",
  kind: "message",
  T: um,
  repeated: !0
}]), l1 = /* @__PURE__ */ D.makeMessageType("livekit.SubscribedQualityUpdate", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "subscribed_qualities",
  kind: "message",
  T: um,
  repeated: !0
}, {
  no: 3,
  name: "subscribed_codecs",
  kind: "message",
  T: c1,
  repeated: !0
}]), u1 = /* @__PURE__ */ D.makeMessageType("livekit.SubscribedAudioCodecUpdate", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "subscribed_audio_codecs",
  kind: "message",
  T: PR,
  repeated: !0
}]), PT = /* @__PURE__ */ D.makeMessageType("livekit.TrackPermission", () => [{
  no: 1,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "all_tracks",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 3,
  name: "track_sids",
  kind: "scalar",
  T: 9,
  repeated: !0
}, {
  no: 4,
  name: "participant_identity",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]), xT = /* @__PURE__ */ D.makeMessageType("livekit.SubscriptionPermission", () => [{
  no: 1,
  name: "all_participants",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 2,
  name: "track_permissions",
  kind: "message",
  T: PT,
  repeated: !0
}]), d1 = /* @__PURE__ */ D.makeMessageType("livekit.SubscriptionPermissionUpdate", () => [{
  no: 1,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "allowed",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}]), f1 = /* @__PURE__ */ D.makeMessageType("livekit.RoomMovedResponse", () => [{
  no: 1,
  name: "room",
  kind: "message",
  T: $l
}, {
  no: 2,
  name: "token",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "participant",
  kind: "message",
  T: ar
}, {
  no: 4,
  name: "other_participants",
  kind: "message",
  T: ar,
  repeated: !0
}]), dm = /* @__PURE__ */ D.makeMessageType("livekit.SyncState", () => [{
  no: 1,
  name: "answer",
  kind: "message",
  T: Ca
}, {
  no: 2,
  name: "subscription",
  kind: "message",
  T: cu
}, {
  no: 3,
  name: "publish_tracks",
  kind: "message",
  T: om,
  repeated: !0
}, {
  no: 4,
  name: "data_channels",
  kind: "message",
  T: IT,
  repeated: !0
}, {
  no: 5,
  name: "offer",
  kind: "message",
  T: Ca
}, {
  no: 6,
  name: "track_sids_disabled",
  kind: "scalar",
  T: 9,
  repeated: !0
}, {
  no: 7,
  name: "datachannel_receive_states",
  kind: "message",
  T: NT,
  repeated: !0
}, {
  no: 8,
  name: "publish_data_tracks",
  kind: "message",
  T: rm,
  repeated: !0
}]), NT = /* @__PURE__ */ D.makeMessageType("livekit.DataChannelReceiveState", () => [{
  no: 1,
  name: "publisher_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "last_seq",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]), IT = /* @__PURE__ */ D.makeMessageType("livekit.DataChannelInfo", () => [{
  no: 1,
  name: "label",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "id",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 3,
  name: "target",
  kind: "enum",
  T: D.getEnumType(qn)
}]), ri = /* @__PURE__ */ D.makeMessageType("livekit.SimulateScenario", () => [{
  no: 1,
  name: "speaker_update",
  kind: "scalar",
  T: 5,
  oneof: "scenario"
}, {
  no: 2,
  name: "node_failure",
  kind: "scalar",
  T: 8,
  oneof: "scenario"
}, {
  no: 3,
  name: "migration",
  kind: "scalar",
  T: 8,
  oneof: "scenario"
}, {
  no: 4,
  name: "server_leave",
  kind: "scalar",
  T: 8,
  oneof: "scenario"
}, {
  no: 5,
  name: "switch_candidate_protocol",
  kind: "enum",
  T: D.getEnumType(YR),
  oneof: "scenario"
}, {
  no: 6,
  name: "subscriber_bandwidth",
  kind: "scalar",
  T: 3,
  oneof: "scenario"
}, {
  no: 7,
  name: "disconnect_signal_on_resume",
  kind: "scalar",
  T: 8,
  oneof: "scenario"
}, {
  no: 8,
  name: "disconnect_signal_on_resume_no_messages",
  kind: "scalar",
  T: 8,
  oneof: "scenario"
}, {
  no: 9,
  name: "leave_request_full_reconnect",
  kind: "scalar",
  T: 8,
  oneof: "scenario"
}]), LT = /* @__PURE__ */ D.makeMessageType("livekit.Ping", () => [{
  no: 1,
  name: "timestamp",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 2,
  name: "rtt",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}]), h1 = /* @__PURE__ */ D.makeMessageType("livekit.Pong", () => [{
  no: 1,
  name: "last_ping_timestamp",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}, {
  no: 2,
  name: "timestamp",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}]), m1 = /* @__PURE__ */ D.makeMessageType("livekit.RegionSettings", () => [{
  no: 1,
  name: "regions",
  kind: "message",
  T: p1,
  repeated: !0
}]), p1 = /* @__PURE__ */ D.makeMessageType("livekit.RegionInfo", () => [{
  no: 1,
  name: "region",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "url",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 3,
  name: "distance",
  kind: "scalar",
  T: 3
  /* ScalarType.INT64 */
}]), g1 = /* @__PURE__ */ D.makeMessageType("livekit.SubscriptionResponse", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 2,
  name: "err",
  kind: "enum",
  T: D.getEnumType(SR)
}]), v1 = /* @__PURE__ */ D.makeMessageType("livekit.RequestResponse", () => [{
  no: 1,
  name: "request_id",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "reason",
  kind: "enum",
  T: D.getEnumType(Ja)
}, {
  no: 3,
  name: "message",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "trickle",
  kind: "message",
  T: su,
  oneof: "request"
}, {
  no: 5,
  name: "add_track",
  kind: "message",
  T: Oo,
  oneof: "request"
}, {
  no: 6,
  name: "mute",
  kind: "message",
  T: ou,
  oneof: "request"
}, {
  no: 7,
  name: "update_metadata",
  kind: "message",
  T: lm,
  oneof: "request"
}, {
  no: 8,
  name: "update_audio_track",
  kind: "message",
  T: cm,
  oneof: "request"
}, {
  no: 9,
  name: "update_video_track",
  kind: "message",
  T: AT,
  oneof: "request"
}, {
  no: 10,
  name: "publish_data_track",
  kind: "message",
  T: am,
  oneof: "request"
}, {
  no: 11,
  name: "unpublish_data_track",
  kind: "message",
  T: sm,
  oneof: "request"
}]), Ja = /* @__PURE__ */ D.makeEnum("livekit.RequestResponse.Reason", [{
  no: 0,
  name: "OK"
}, {
  no: 1,
  name: "NOT_FOUND"
}, {
  no: 2,
  name: "NOT_ALLOWED"
}, {
  no: 3,
  name: "LIMIT_EXCEEDED"
}, {
  no: 4,
  name: "QUEUED"
}, {
  no: 5,
  name: "UNSUPPORTED_TYPE"
}, {
  no: 6,
  name: "UNCLASSIFIED_ERROR"
}, {
  no: 7,
  name: "INVALID_HANDLE"
}, {
  no: 8,
  name: "INVALID_NAME"
}, {
  no: 9,
  name: "DUPLICATE_HANDLE"
}, {
  no: 10,
  name: "DUPLICATE_NAME"
}]), y1 = /* @__PURE__ */ D.makeMessageType("livekit.TrackSubscribed", () => [{
  no: 1,
  name: "track_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}]), UT = /* @__PURE__ */ D.makeMessageType("livekit.ConnectionSettings", () => [{
  no: 1,
  name: "auto_subscribe",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 2,
  name: "adaptive_stream",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 3,
  name: "subscriber_allow_pause",
  kind: "scalar",
  T: 8,
  opt: !0
}, {
  no: 4,
  name: "disable_ice_lite",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 5,
  name: "auto_subscribe_data_track",
  kind: "scalar",
  T: 8,
  opt: !0
}]), b1 = /* @__PURE__ */ D.makeMessageType("livekit.JoinRequest", () => [{
  no: 1,
  name: "client_info",
  kind: "message",
  T: gT
}, {
  no: 2,
  name: "connection_settings",
  kind: "message",
  T: UT
}, {
  no: 3,
  name: "metadata",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 4,
  name: "participant_attributes",
  kind: "map",
  K: 9,
  V: {
    kind: "scalar",
    T: 9
    /* ScalarType.STRING */
  }
}, {
  no: 5,
  name: "add_track_requests",
  kind: "message",
  T: Oo,
  repeated: !0
}, {
  no: 6,
  name: "publisher_offer",
  kind: "message",
  T: Ca
}, {
  no: 7,
  name: "reconnect",
  kind: "scalar",
  T: 8
  /* ScalarType.BOOL */
}, {
  no: 8,
  name: "reconnect_reason",
  kind: "enum",
  T: D.getEnumType(Ka)
}, {
  no: 9,
  name: "participant_sid",
  kind: "scalar",
  T: 9
  /* ScalarType.STRING */
}, {
  no: 10,
  name: "sync_state",
  kind: "message",
  T: dm
}]), T1 = /* @__PURE__ */ D.makeMessageType("livekit.WrappedJoinRequest", () => [{
  no: 1,
  name: "compression",
  kind: "enum",
  T: D.getEnumType(kh)
}, {
  no: 2,
  name: "join_request",
  kind: "scalar",
  T: 12
  /* ScalarType.BYTES */
}]), kh = /* @__PURE__ */ D.makeEnum("livekit.WrappedJoinRequest.Compression", [{
  no: 0,
  name: "NONE"
}, {
  no: 1,
  name: "GZIP"
}]), S1 = /* @__PURE__ */ D.makeMessageType("livekit.MediaSectionsRequirement", () => [{
  no: 1,
  name: "num_audios",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}, {
  no: 2,
  name: "num_videos",
  kind: "scalar",
  T: 13
  /* ScalarType.UINT32 */
}]);
function k1(a) {
  return a && a.__esModule && Object.prototype.hasOwnProperty.call(a, "default") ? a.default : a;
}
var kl = { exports: {} }, C1 = kl.exports, bb;
function E1() {
  return bb || (bb = 1, (function(a) {
    (function(e, t) {
      a.exports ? a.exports = t() : e.log = t();
    })(C1, function() {
      var e = function() {
      }, t = "undefined", i = typeof window !== t && typeof window.navigator !== t && /Trident\/|MSIE /.test(window.navigator.userAgent), s = ["trace", "debug", "info", "warn", "error"], c = {}, u = null;
      function d(k, x) {
        var A = k[x];
        if (typeof A.bind == "function")
          return A.bind(k);
        try {
          return Function.prototype.bind.call(A, k);
        } catch {
          return function() {
            return Function.prototype.apply.apply(A, [k, arguments]);
          };
        }
      }
      function h() {
        console.log && (console.log.apply ? console.log.apply(console, arguments) : Function.prototype.apply.apply(console.log, [console, arguments])), console.trace && console.trace();
      }
      function f(k) {
        return k === "debug" && (k = "log"), typeof console === t ? !1 : k === "trace" && i ? h : console[k] !== void 0 ? d(console, k) : console.log !== void 0 ? d(console, "log") : e;
      }
      function p() {
        for (var k = this.getLevel(), x = 0; x < s.length; x++) {
          var A = s[x];
          this[A] = x < k ? e : this.methodFactory(A, k, this.name);
        }
        if (this.log = this.debug, typeof console === t && k < this.levels.SILENT)
          return "No console available for logging";
      }
      function v(k) {
        return function() {
          typeof console !== t && (p.call(this), this[k].apply(this, arguments));
        };
      }
      function y(k, x, A) {
        return f(k) || v.apply(this, arguments);
      }
      function T(k, x) {
        var A = this, B, ie, C, E = "loglevel";
        typeof k == "string" ? E += ":" + k : typeof k == "symbol" && (E = void 0);
        function M(J) {
          var ne = (s[J] || "silent").toUpperCase();
          if (!(typeof window === t || !E)) {
            try {
              window.localStorage[E] = ne;
              return;
            } catch {
            }
            try {
              window.document.cookie = encodeURIComponent(E) + "=" + ne + ";";
            } catch {
            }
          }
        }
        function N() {
          var J;
          if (!(typeof window === t || !E)) {
            try {
              J = window.localStorage[E];
            } catch {
            }
            if (typeof J === t)
              try {
                var ne = window.document.cookie, de = encodeURIComponent(E), fe = ne.indexOf(de + "=");
                fe !== -1 && (J = /^([^;]+)/.exec(ne.slice(fe + de.length + 1))[1]);
              } catch {
              }
            return A.levels[J] === void 0 && (J = void 0), J;
          }
        }
        function G() {
          if (!(typeof window === t || !E)) {
            try {
              window.localStorage.removeItem(E);
            } catch {
            }
            try {
              window.document.cookie = encodeURIComponent(E) + "=; expires=Thu, 01 Jan 1970 00:00:00 UTC";
            } catch {
            }
          }
        }
        function W(J) {
          var ne = J;
          if (typeof ne == "string" && A.levels[ne.toUpperCase()] !== void 0 && (ne = A.levels[ne.toUpperCase()]), typeof ne == "number" && ne >= 0 && ne <= A.levels.SILENT)
            return ne;
          throw new TypeError("log.setLevel() called with invalid level: " + J);
        }
        A.name = k, A.levels = {
          TRACE: 0,
          DEBUG: 1,
          INFO: 2,
          WARN: 3,
          ERROR: 4,
          SILENT: 5
        }, A.methodFactory = x || y, A.getLevel = function() {
          return C ?? ie ?? B;
        }, A.setLevel = function(J, ne) {
          return C = W(J), ne !== !1 && M(C), p.call(A);
        }, A.setDefaultLevel = function(J) {
          ie = W(J), N() || A.setLevel(J, !1);
        }, A.resetLevel = function() {
          C = null, G(), p.call(A);
        }, A.enableAll = function(J) {
          A.setLevel(A.levels.TRACE, J);
        }, A.disableAll = function(J) {
          A.setLevel(A.levels.SILENT, J);
        }, A.rebuild = function() {
          if (u !== A && (B = W(u.getLevel())), p.call(A), u === A)
            for (var J in c)
              c[J].rebuild();
        }, B = W(u ? u.getLevel() : "WARN");
        var Y = N();
        Y != null && (C = W(Y)), p.call(A);
      }
      u = new T(), u.getLogger = function(x) {
        if (typeof x != "symbol" && typeof x != "string" || x === "")
          throw new TypeError("You must supply a name when creating a logger.");
        var A = c[x];
        return A || (A = c[x] = new T(x, u.methodFactory)), A;
      };
      var _ = typeof window !== t ? window.log : void 0;
      return u.noConflict = function() {
        return typeof window !== t && window.log === u && (window.log = _), u;
      }, u.getLoggers = function() {
        return c;
      }, u.default = u, u;
    });
  })(kl)), kl.exports;
}
var uu = E1(), Ch;
(function(a) {
  a[a.trace = 0] = "trace", a[a.debug = 1] = "debug", a[a.info = 2] = "info", a[a.warn = 3] = "warn", a[a.error = 4] = "error", a[a.silent = 5] = "silent";
})(Ch || (Ch = {}));
var Mt;
(function(a) {
  a.Default = "livekit", a.Room = "livekit-room", a.TokenSource = "livekit-token-source", a.Participant = "livekit-participant", a.Track = "livekit-track", a.Publication = "livekit-track-publication", a.Engine = "livekit-engine", a.Signal = "livekit-signal", a.PCManager = "livekit-pc-manager", a.PCTransport = "livekit-pc-transport", a.E2EE = "lk-e2ee", a.DataTracks = "livekit-data-tracks";
})(Mt || (Mt = {}));
let Ee = uu.getLogger(Mt.Default);
Object.values(Mt).map((a) => uu.getLogger(a));
Ee.setDefaultLevel(Ch.info);
function rn(a, e) {
  const t = uu.getLogger(a);
  return t.setDefaultLevel(Ee.getLevel()), e ? _1(t, e) : t;
}
function _1(a, e) {
  const t = (s) => (c, u) => {
    const d = e(), h = d || u ? Object.assign(Object.assign({}, d), u) : void 0;
    a[s](c, h);
  }, i = Object.create(a);
  return i.trace = t("trace"), i.debug = t("debug"), i.info = t("info"), i.warn = t("warn"), i.error = t("error"), i;
}
const R1 = uu.getLogger(Mt.E2EE), mo = 7e3, w1 = [0, 300, 4 * 300, 9 * 300, 16 * 300, mo, mo, mo, mo, mo];
class M1 {
  constructor(e) {
    this._retryDelays = e !== void 0 ? [...e] : w1;
  }
  nextRetryDelayInMs(e) {
    if (e.retryCount >= this._retryDelays.length) return null;
    const t = this._retryDelays[e.retryCount];
    return e.retryCount <= 1 ? t : t + Math.random() * 1e3;
  }
}
function BT(a, e) {
  var t = {};
  for (var i in a) Object.prototype.hasOwnProperty.call(a, i) && e.indexOf(i) < 0 && (t[i] = a[i]);
  if (a != null && typeof Object.getOwnPropertySymbols == "function")
    for (var s = 0, i = Object.getOwnPropertySymbols(a); s < i.length; s++)
      e.indexOf(i[s]) < 0 && Object.prototype.propertyIsEnumerable.call(a, i[s]) && (t[i[s]] = a[i[s]]);
  return t;
}
function R(a, e, t, i) {
  function s(c) {
    return c instanceof t ? c : new t(function(u) {
      u(c);
    });
  }
  return new (t || (t = Promise))(function(c, u) {
    function d(p) {
      try {
        f(i.next(p));
      } catch (v) {
        u(v);
      }
    }
    function h(p) {
      try {
        f(i.throw(p));
      } catch (v) {
        u(v);
      }
    }
    function f(p) {
      p.done ? c(p.value) : s(p.value).then(d, h);
    }
    f((i = i.apply(a, e || [])).next());
  });
}
function Tb(a) {
  var e = typeof Symbol == "function" && Symbol.iterator, t = e && a[e], i = 0;
  if (t) return t.call(a);
  if (a && typeof a.length == "number") return {
    next: function() {
      return a && i >= a.length && (a = void 0), { value: a && a[i++], done: !a };
    }
  };
  throw new TypeError(e ? "Object is not iterable." : "Symbol.iterator is not defined.");
}
function ds(a) {
  return this instanceof ds ? (this.v = a, this) : new ds(a);
}
function A1(a, e, t) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var i = t.apply(a, e || []), s, c = [];
  return s = Object.create((typeof AsyncIterator == "function" ? AsyncIterator : Object).prototype), d("next"), d("throw"), d("return", u), s[Symbol.asyncIterator] = function() {
    return this;
  }, s;
  function u(T) {
    return function(_) {
      return Promise.resolve(_).then(T, v);
    };
  }
  function d(T, _) {
    i[T] && (s[T] = function(k) {
      return new Promise(function(x, A) {
        c.push([T, k, x, A]) > 1 || h(T, k);
      });
    }, _ && (s[T] = _(s[T])));
  }
  function h(T, _) {
    try {
      f(i[T](_));
    } catch (k) {
      y(c[0][3], k);
    }
  }
  function f(T) {
    T.value instanceof ds ? Promise.resolve(T.value.v).then(p, v) : y(c[0][2], T);
  }
  function p(T) {
    h("next", T);
  }
  function v(T) {
    h("throw", T);
  }
  function y(T, _) {
    T(_), c.shift(), c.length && h(c[0][0], c[0][1]);
  }
}
function O1(a) {
  var e, t;
  return e = {}, i("next"), i("throw", function(s) {
    throw s;
  }), i("return"), e[Symbol.iterator] = function() {
    return this;
  }, e;
  function i(s, c) {
    e[s] = a[s] ? function(u) {
      return (t = !t) ? { value: ds(a[s](u)), done: !1 } : c ? c(u) : u;
    } : c;
  }
}
function Hn(a) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var e = a[Symbol.asyncIterator], t;
  return e ? e.call(a) : (a = typeof Tb == "function" ? Tb(a) : a[Symbol.iterator](), t = {}, i("next"), i("throw"), i("return"), t[Symbol.asyncIterator] = function() {
    return this;
  }, t);
  function i(c) {
    t[c] = a[c] && function(u) {
      return new Promise(function(d, h) {
        u = a[c](u), s(d, h, u.done, u.value);
      });
    };
  }
  function s(c, u, d, h) {
    Promise.resolve(h).then(function(f) {
      c({ value: f, done: d });
    }, u);
  }
}
var ll = { exports: {} }, Sb;
function D1() {
  if (Sb) return ll.exports;
  Sb = 1;
  var a = typeof Reflect == "object" ? Reflect : null, e = a && typeof a.apply == "function" ? a.apply : function(E, M, N) {
    return Function.prototype.apply.call(E, M, N);
  }, t;
  a && typeof a.ownKeys == "function" ? t = a.ownKeys : Object.getOwnPropertySymbols ? t = function(E) {
    return Object.getOwnPropertyNames(E).concat(Object.getOwnPropertySymbols(E));
  } : t = function(E) {
    return Object.getOwnPropertyNames(E);
  };
  function i(C) {
    console && console.warn && console.warn(C);
  }
  var s = Number.isNaN || function(E) {
    return E !== E;
  };
  function c() {
    c.init.call(this);
  }
  ll.exports = c, ll.exports.once = A, c.EventEmitter = c, c.prototype._events = void 0, c.prototype._eventsCount = 0, c.prototype._maxListeners = void 0;
  var u = 10;
  function d(C) {
    if (typeof C != "function")
      throw new TypeError('The "listener" argument must be of type Function. Received type ' + typeof C);
  }
  Object.defineProperty(c, "defaultMaxListeners", {
    enumerable: !0,
    get: function() {
      return u;
    },
    set: function(C) {
      if (typeof C != "number" || C < 0 || s(C))
        throw new RangeError('The value of "defaultMaxListeners" is out of range. It must be a non-negative number. Received ' + C + ".");
      u = C;
    }
  }), c.init = function() {
    (this._events === void 0 || this._events === Object.getPrototypeOf(this)._events) && (this._events = /* @__PURE__ */ Object.create(null), this._eventsCount = 0), this._maxListeners = this._maxListeners || void 0;
  }, c.prototype.setMaxListeners = function(E) {
    if (typeof E != "number" || E < 0 || s(E))
      throw new RangeError('The value of "n" is out of range. It must be a non-negative number. Received ' + E + ".");
    return this._maxListeners = E, this;
  };
  function h(C) {
    return C._maxListeners === void 0 ? c.defaultMaxListeners : C._maxListeners;
  }
  c.prototype.getMaxListeners = function() {
    return h(this);
  }, c.prototype.emit = function(E) {
    for (var M = [], N = 1; N < arguments.length; N++) M.push(arguments[N]);
    var G = E === "error", W = this._events;
    if (W !== void 0) G = G && W.error === void 0;
    else if (!G) return !1;
    if (G) {
      var Y;
      if (M.length > 0 && (Y = M[0]), Y instanceof Error)
        throw Y;
      var J = new Error("Unhandled error." + (Y ? " (" + Y.message + ")" : ""));
      throw J.context = Y, J;
    }
    var ne = W[E];
    if (ne === void 0) return !1;
    if (typeof ne == "function")
      e(ne, this, M);
    else
      for (var de = ne.length, fe = _(ne, de), N = 0; N < de; ++N) e(fe[N], this, M);
    return !0;
  };
  function f(C, E, M, N) {
    var G, W, Y;
    if (d(M), W = C._events, W === void 0 ? (W = C._events = /* @__PURE__ */ Object.create(null), C._eventsCount = 0) : (W.newListener !== void 0 && (C.emit("newListener", E, M.listener ? M.listener : M), W = C._events), Y = W[E]), Y === void 0)
      Y = W[E] = M, ++C._eventsCount;
    else if (typeof Y == "function" ? Y = W[E] = N ? [M, Y] : [Y, M] : N ? Y.unshift(M) : Y.push(M), G = h(C), G > 0 && Y.length > G && !Y.warned) {
      Y.warned = !0;
      var J = new Error("Possible EventEmitter memory leak detected. " + Y.length + " " + String(E) + " listeners added. Use emitter.setMaxListeners() to increase limit");
      J.name = "MaxListenersExceededWarning", J.emitter = C, J.type = E, J.count = Y.length, i(J);
    }
    return C;
  }
  c.prototype.addListener = function(E, M) {
    return f(this, E, M, !1);
  }, c.prototype.on = c.prototype.addListener, c.prototype.prependListener = function(E, M) {
    return f(this, E, M, !0);
  };
  function p() {
    if (!this.fired)
      return this.target.removeListener(this.type, this.wrapFn), this.fired = !0, arguments.length === 0 ? this.listener.call(this.target) : this.listener.apply(this.target, arguments);
  }
  function v(C, E, M) {
    var N = {
      fired: !1,
      wrapFn: void 0,
      target: C,
      type: E,
      listener: M
    }, G = p.bind(N);
    return G.listener = M, N.wrapFn = G, G;
  }
  c.prototype.once = function(E, M) {
    return d(M), this.on(E, v(this, E, M)), this;
  }, c.prototype.prependOnceListener = function(E, M) {
    return d(M), this.prependListener(E, v(this, E, M)), this;
  }, c.prototype.removeListener = function(E, M) {
    var N, G, W, Y, J;
    if (d(M), G = this._events, G === void 0) return this;
    if (N = G[E], N === void 0) return this;
    if (N === M || N.listener === M)
      --this._eventsCount === 0 ? this._events = /* @__PURE__ */ Object.create(null) : (delete G[E], G.removeListener && this.emit("removeListener", E, N.listener || M));
    else if (typeof N != "function") {
      for (W = -1, Y = N.length - 1; Y >= 0; Y--)
        if (N[Y] === M || N[Y].listener === M) {
          J = N[Y].listener, W = Y;
          break;
        }
      if (W < 0) return this;
      W === 0 ? N.shift() : k(N, W), N.length === 1 && (G[E] = N[0]), G.removeListener !== void 0 && this.emit("removeListener", E, J || M);
    }
    return this;
  }, c.prototype.off = c.prototype.removeListener, c.prototype.removeAllListeners = function(E) {
    var M, N, G;
    if (N = this._events, N === void 0) return this;
    if (N.removeListener === void 0)
      return arguments.length === 0 ? (this._events = /* @__PURE__ */ Object.create(null), this._eventsCount = 0) : N[E] !== void 0 && (--this._eventsCount === 0 ? this._events = /* @__PURE__ */ Object.create(null) : delete N[E]), this;
    if (arguments.length === 0) {
      var W = Object.keys(N), Y;
      for (G = 0; G < W.length; ++G)
        Y = W[G], Y !== "removeListener" && this.removeAllListeners(Y);
      return this.removeAllListeners("removeListener"), this._events = /* @__PURE__ */ Object.create(null), this._eventsCount = 0, this;
    }
    if (M = N[E], typeof M == "function")
      this.removeListener(E, M);
    else if (M !== void 0)
      for (G = M.length - 1; G >= 0; G--)
        this.removeListener(E, M[G]);
    return this;
  };
  function y(C, E, M) {
    var N = C._events;
    if (N === void 0) return [];
    var G = N[E];
    return G === void 0 ? [] : typeof G == "function" ? M ? [G.listener || G] : [G] : M ? x(G) : _(G, G.length);
  }
  c.prototype.listeners = function(E) {
    return y(this, E, !0);
  }, c.prototype.rawListeners = function(E) {
    return y(this, E, !1);
  }, c.listenerCount = function(C, E) {
    return typeof C.listenerCount == "function" ? C.listenerCount(E) : T.call(C, E);
  }, c.prototype.listenerCount = T;
  function T(C) {
    var E = this._events;
    if (E !== void 0) {
      var M = E[C];
      if (typeof M == "function")
        return 1;
      if (M !== void 0)
        return M.length;
    }
    return 0;
  }
  c.prototype.eventNames = function() {
    return this._eventsCount > 0 ? t(this._events) : [];
  };
  function _(C, E) {
    for (var M = new Array(E), N = 0; N < E; ++N) M[N] = C[N];
    return M;
  }
  function k(C, E) {
    for (; E + 1 < C.length; E++) C[E] = C[E + 1];
    C.pop();
  }
  function x(C) {
    for (var E = new Array(C.length), M = 0; M < E.length; ++M)
      E[M] = C[M].listener || C[M];
    return E;
  }
  function A(C, E) {
    return new Promise(function(M, N) {
      function G(Y) {
        C.removeListener(E, W), N(Y);
      }
      function W() {
        typeof C.removeListener == "function" && C.removeListener("error", G), M([].slice.call(arguments));
      }
      ie(C, E, W, {
        once: !0
      }), E !== "error" && B(C, G, {
        once: !0
      });
    });
  }
  function B(C, E, M) {
    typeof C.on == "function" && ie(C, "error", E, M);
  }
  function ie(C, E, M, N) {
    if (typeof C.on == "function")
      N.once ? C.once(E, M) : C.on(E, M);
    else if (typeof C.addEventListener == "function")
      C.addEventListener(E, function G(W) {
        N.once && C.removeEventListener(E, G), M(W);
      });
    else
      throw new TypeError('The "emitter" argument must be of type EventEmitter. Received type ' + typeof C);
  }
  return ll.exports;
}
var sn = D1();
let zT = !0, FT = !0;
function To(a, e, t) {
  const i = a.match(e);
  return i && i.length >= t && parseFloat(i[t], 10);
}
function or(a, e, t) {
  if (!a.RTCPeerConnection)
    return;
  if (!Object.getOwnPropertyDescriptor(EventTarget.prototype, "addEventListener").writable) {
    fm("Unable to polyfill events");
    return;
  }
  const s = a.RTCPeerConnection.prototype, c = s.addEventListener;
  s.addEventListener = function(d, h) {
    if (d !== e)
      return c.apply(this, arguments);
    const f = (p) => {
      const v = t(p);
      v && (h.handleEvent ? h.handleEvent(v) : h(v));
    };
    return this._eventMap = this._eventMap || {}, this._eventMap[e] || (this._eventMap[e] = /* @__PURE__ */ new Map()), this._eventMap[e].set(h, f), c.apply(this, [d, f]);
  };
  const u = s.removeEventListener;
  s.removeEventListener = function(d, h) {
    if (d !== e || !this._eventMap || !this._eventMap[e])
      return u.apply(this, arguments);
    if (!this._eventMap[e].has(h))
      return u.apply(this, arguments);
    const f = this._eventMap[e].get(h);
    return this._eventMap[e].delete(h), this._eventMap[e].size === 0 && delete this._eventMap[e], Object.keys(this._eventMap).length === 0 && delete this._eventMap, u.apply(this, [d, f]);
  }, Object.defineProperty(s, "on" + e, {
    get() {
      return this["_on" + e];
    },
    set(d) {
      this["_on" + e] && (this.removeEventListener(e, this["_on" + e]), delete this["_on" + e]), d && this.addEventListener(e, this["_on" + e] = d);
    },
    enumerable: !0,
    configurable: !0
  });
}
function P1(a) {
  return typeof a != "boolean" ? new Error("Argument type: " + typeof a + ". Please use a boolean.") : (zT = a, a ? "adapter.js logging disabled" : "adapter.js logging enabled");
}
function x1(a) {
  return typeof a != "boolean" ? new Error("Argument type: " + typeof a + ". Please use a boolean.") : (FT = !a, "adapter.js deprecation warnings " + (a ? "disabled" : "enabled"));
}
function fm() {
  if (typeof window == "object") {
    if (zT)
      return;
    typeof console < "u" && typeof console.log == "function" && console.log.apply(console, arguments);
  }
}
function hm(a, e) {
  FT && console.warn(a + " is deprecated, please use " + e + " instead.");
}
function N1(a) {
  const e = {
    browser: null,
    version: null
  };
  if (typeof a > "u" || !a.navigator || !a.navigator.userAgent)
    return e.browser = "Not a browser.", e;
  const t = a.navigator;
  if (t.userAgentData && t.userAgentData.brands) {
    const i = t.userAgentData.brands.find((s) => s.brand === "Chromium");
    if (i) {
      const s = parseInt(i.version, 10);
      if (s >= 90)
        return {
          browser: "chrome",
          version: s
        };
    }
  }
  if (t.mozGetUserMedia)
    e.browser = "firefox", e.version = parseInt(To(t.userAgent, /Firefox\/(\d+)\./, 1));
  else if (t.webkitGetUserMedia || a.isSecureContext === !1 && a.webkitRTCPeerConnection)
    e.browser = "chrome", e.version = parseInt(To(t.userAgent, /Chrom(e|ium)\/(\d+)\./, 2)) || null;
  else if (a.RTCPeerConnection && t.userAgent.match(/AppleWebKit\/(\d+)\./))
    e.browser = "safari", e.version = parseInt(To(t.userAgent, /AppleWebKit\/(\d+)\./, 1)), e.supportsUnifiedPlan = a.RTCRtpTransceiver && "currentDirection" in a.RTCRtpTransceiver.prototype, e._safariVersion = To(t.userAgent, /Version\/(\d+(\.?\d+))/, 1);
  else
    return e.browser = "Not a supported browser.", e;
  return e;
}
function kb(a) {
  return Object.prototype.toString.call(a) === "[object Object]";
}
function jT(a) {
  return kb(a) ? Object.keys(a).reduce(function(e, t) {
    const i = kb(a[t]), s = i ? jT(a[t]) : a[t], c = i && !Object.keys(s).length;
    return s === void 0 || c ? e : Object.assign(e, {
      [t]: s
    });
  }, {}) : a;
}
function Eh(a, e, t) {
  !e || t.has(e.id) || (t.set(e.id, e), Object.keys(e).forEach((i) => {
    i.endsWith("Id") ? Eh(a, a.get(e[i]), t) : i.endsWith("Ids") && e[i].forEach((s) => {
      Eh(a, a.get(s), t);
    });
  }));
}
function Cb(a, e, t) {
  const i = t ? "outbound-rtp" : "inbound-rtp", s = /* @__PURE__ */ new Map();
  if (e === null)
    return s;
  const c = [];
  return a.forEach((u) => {
    u.type === "track" && u.trackIdentifier === e.id && c.push(u);
  }), c.forEach((u) => {
    a.forEach((d) => {
      d.type === i && d.trackId === u.id && Eh(a, d, s);
    });
  }), s;
}
const Eb = fm;
function qT(a, e) {
  if (e.version >= 64)
    return;
  const t = a && a.navigator;
  if (!t.mediaDevices)
    return;
  const i = function(d) {
    if (typeof d != "object" || d.mandatory || d.optional)
      return d;
    const h = {};
    return Object.keys(d).forEach((f) => {
      if (f === "require" || f === "advanced" || f === "mediaSource")
        return;
      const p = typeof d[f] == "object" ? d[f] : {
        ideal: d[f]
      };
      p.exact !== void 0 && typeof p.exact == "number" && (p.min = p.max = p.exact);
      const v = function(y, T) {
        return y ? y + T.charAt(0).toUpperCase() + T.slice(1) : T === "deviceId" ? "sourceId" : T;
      };
      if (p.ideal !== void 0) {
        h.optional = h.optional || [];
        let y = {};
        typeof p.ideal == "number" ? (y[v("min", f)] = p.ideal, h.optional.push(y), y = {}, y[v("max", f)] = p.ideal, h.optional.push(y)) : (y[v("", f)] = p.ideal, h.optional.push(y));
      }
      p.exact !== void 0 && typeof p.exact != "number" ? (h.mandatory = h.mandatory || {}, h.mandatory[v("", f)] = p.exact) : ["min", "max"].forEach((y) => {
        p[y] !== void 0 && (h.mandatory = h.mandatory || {}, h.mandatory[v(y, f)] = p[y]);
      });
    }), d.advanced && (h.optional = (h.optional || []).concat(d.advanced)), h;
  }, s = function(d, h) {
    if (e.version >= 61)
      return h(d);
    if (d = JSON.parse(JSON.stringify(d)), d && typeof d.audio == "object") {
      const f = function(p, v, y) {
        v in p && !(y in p) && (p[y] = p[v], delete p[v]);
      };
      d = JSON.parse(JSON.stringify(d)), f(d.audio, "autoGainControl", "googAutoGainControl"), f(d.audio, "noiseSuppression", "googNoiseSuppression"), d.audio = i(d.audio);
    }
    if (d && typeof d.video == "object") {
      let f = d.video.facingMode;
      f = f && (typeof f == "object" ? f : {
        ideal: f
      });
      const p = e.version < 66;
      if (f && (f.exact === "user" || f.exact === "environment" || f.ideal === "user" || f.ideal === "environment") && !(t.mediaDevices.getSupportedConstraints && t.mediaDevices.getSupportedConstraints().facingMode && !p)) {
        delete d.video.facingMode;
        let v;
        if (f.exact === "environment" || f.ideal === "environment" ? v = ["back", "rear"] : (f.exact === "user" || f.ideal === "user") && (v = ["front"]), v)
          return t.mediaDevices.enumerateDevices().then((y) => {
            y = y.filter((_) => _.kind === "videoinput");
            let T = y.find((_) => v.some((k) => _.label.toLowerCase().includes(k)));
            return !T && y.length && v.includes("back") && (T = y[y.length - 1]), T && (d.video.deviceId = f.exact ? {
              exact: T.deviceId
            } : {
              ideal: T.deviceId
            }), d.video = i(d.video), Eb("chrome: " + JSON.stringify(d)), h(d);
          });
      }
      d.video = i(d.video);
    }
    return Eb("chrome: " + JSON.stringify(d)), h(d);
  }, c = function(d) {
    return e.version >= 64 ? d : {
      name: {
        PermissionDeniedError: "NotAllowedError",
        PermissionDismissedError: "NotAllowedError",
        InvalidStateError: "NotAllowedError",
        DevicesNotFoundError: "NotFoundError",
        ConstraintNotSatisfiedError: "OverconstrainedError",
        TrackStartError: "NotReadableError",
        MediaDeviceFailedDueToShutdown: "NotAllowedError",
        MediaDeviceKillSwitchOn: "NotAllowedError",
        TabCaptureError: "AbortError",
        ScreenCaptureError: "AbortError",
        DeviceCaptureError: "AbortError"
      }[d.name] || d.name,
      message: d.message,
      constraint: d.constraint || d.constraintName,
      toString() {
        return this.name + (this.message && ": ") + this.message;
      }
    };
  }, u = function(d, h, f) {
    s(d, (p) => {
      t.webkitGetUserMedia(p, h, (v) => {
        f && f(c(v));
      });
    });
  };
  if (t.getUserMedia = u.bind(t), t.mediaDevices.getUserMedia) {
    const d = t.mediaDevices.getUserMedia.bind(t.mediaDevices);
    t.mediaDevices.getUserMedia = function(h) {
      return s(h, (f) => d(f).then((p) => {
        if (f.audio && !p.getAudioTracks().length || f.video && !p.getVideoTracks().length)
          throw p.getTracks().forEach((v) => {
            v.stop();
          }), new DOMException("", "NotFoundError");
        return p;
      }, (p) => Promise.reject(c(p))));
    };
  }
}
function HT(a) {
  a.MediaStream = a.MediaStream || a.webkitMediaStream;
}
function VT(a, e) {
  if (!(e.version > 102))
    if (typeof a == "object" && a.RTCPeerConnection && !("ontrack" in a.RTCPeerConnection.prototype)) {
      Object.defineProperty(a.RTCPeerConnection.prototype, "ontrack", {
        get() {
          return this._ontrack;
        },
        set(i) {
          this._ontrack && this.removeEventListener("track", this._ontrack), this.addEventListener("track", this._ontrack = i);
        },
        enumerable: !0,
        configurable: !0
      });
      const t = a.RTCPeerConnection.prototype.setRemoteDescription;
      a.RTCPeerConnection.prototype.setRemoteDescription = function() {
        return this._ontrackpoly || (this._ontrackpoly = (s) => {
          s.stream.addEventListener("addtrack", (c) => {
            let u;
            a.RTCPeerConnection.prototype.getReceivers ? u = this.getReceivers().find((h) => h.track && h.track.id === c.track.id) : u = {
              track: c.track
            };
            const d = new Event("track");
            d.track = c.track, d.receiver = u, d.transceiver = {
              receiver: u
            }, d.streams = [s.stream], this.dispatchEvent(d);
          }), s.stream.getTracks().forEach((c) => {
            let u;
            a.RTCPeerConnection.prototype.getReceivers ? u = this.getReceivers().find((h) => h.track && h.track.id === c.id) : u = {
              track: c
            };
            const d = new Event("track");
            d.track = c, d.receiver = u, d.transceiver = {
              receiver: u
            }, d.streams = [s.stream], this.dispatchEvent(d);
          });
        }, this.addEventListener("addstream", this._ontrackpoly)), t.apply(this, arguments);
      };
    } else
      or(a, "track", (t) => (t.transceiver || Object.defineProperty(t, "transceiver", {
        value: {
          receiver: t.receiver
        }
      }), t));
}
function GT(a) {
  if (typeof a == "object" && a.RTCPeerConnection && !("getSenders" in a.RTCPeerConnection.prototype) && "createDTMFSender" in a.RTCPeerConnection.prototype) {
    const e = function(s, c) {
      return {
        track: c,
        get dtmf() {
          return this._dtmf === void 0 && (c.kind === "audio" ? this._dtmf = s.createDTMFSender(c) : this._dtmf = null), this._dtmf;
        },
        _pc: s
      };
    };
    if (!a.RTCPeerConnection.prototype.getSenders) {
      a.RTCPeerConnection.prototype.getSenders = function() {
        return this._senders = this._senders || [], this._senders.slice();
      };
      const s = a.RTCPeerConnection.prototype.addTrack;
      a.RTCPeerConnection.prototype.addTrack = function(d, h) {
        let f = s.apply(this, arguments);
        return f || (f = e(this, d), this._senders.push(f)), f;
      };
      const c = a.RTCPeerConnection.prototype.removeTrack;
      a.RTCPeerConnection.prototype.removeTrack = function(d) {
        c.apply(this, arguments);
        const h = this._senders.indexOf(d);
        h !== -1 && this._senders.splice(h, 1);
      };
    }
    const t = a.RTCPeerConnection.prototype.addStream;
    a.RTCPeerConnection.prototype.addStream = function(c) {
      this._senders = this._senders || [], t.apply(this, [c]), c.getTracks().forEach((u) => {
        this._senders.push(e(this, u));
      });
    };
    const i = a.RTCPeerConnection.prototype.removeStream;
    a.RTCPeerConnection.prototype.removeStream = function(c) {
      this._senders = this._senders || [], i.apply(this, [c]), c.getTracks().forEach((u) => {
        const d = this._senders.find((h) => h.track === u);
        d && this._senders.splice(this._senders.indexOf(d), 1);
      });
    };
  } else if (typeof a == "object" && a.RTCPeerConnection && "getSenders" in a.RTCPeerConnection.prototype && "createDTMFSender" in a.RTCPeerConnection.prototype && a.RTCRtpSender && !("dtmf" in a.RTCRtpSender.prototype)) {
    const e = a.RTCPeerConnection.prototype.getSenders;
    a.RTCPeerConnection.prototype.getSenders = function() {
      const i = e.apply(this, []);
      return i.forEach((s) => s._pc = this), i;
    }, Object.defineProperty(a.RTCRtpSender.prototype, "dtmf", {
      get() {
        return this._dtmf === void 0 && (this.track.kind === "audio" ? this._dtmf = this._pc.createDTMFSender(this.track) : this._dtmf = null), this._dtmf;
      }
    });
  }
}
function KT(a, e) {
  if (e.version >= 67 || !(typeof a == "object" && a.RTCPeerConnection && a.RTCRtpSender && a.RTCRtpReceiver))
    return;
  if (!("getStats" in a.RTCRtpSender.prototype)) {
    const i = a.RTCPeerConnection.prototype.getSenders;
    i && (a.RTCPeerConnection.prototype.getSenders = function() {
      const u = i.apply(this, []);
      return u.forEach((d) => d._pc = this), u;
    });
    const s = a.RTCPeerConnection.prototype.addTrack;
    s && (a.RTCPeerConnection.prototype.addTrack = function() {
      const u = s.apply(this, arguments);
      return u._pc = this, u;
    }), a.RTCRtpSender.prototype.getStats = function() {
      const u = this;
      return this._pc.getStats().then((d) => (
        /* Note: this will include stats of all senders that
         *   send a track with the same id as sender.track as
         *   it is not possible to identify the RTCRtpSender.
         */
        Cb(d, u.track, !0)
      ));
    };
  }
  if (!("getStats" in a.RTCRtpReceiver.prototype)) {
    const i = a.RTCPeerConnection.prototype.getReceivers;
    i && (a.RTCPeerConnection.prototype.getReceivers = function() {
      const c = i.apply(this, []);
      return c.forEach((u) => u._pc = this), c;
    }), or(a, "track", (s) => (s.receiver._pc = s.srcElement, s)), a.RTCRtpReceiver.prototype.getStats = function() {
      const c = this;
      return this._pc.getStats().then((u) => Cb(u, c.track, !1));
    };
  }
  if (!("getStats" in a.RTCRtpSender.prototype && "getStats" in a.RTCRtpReceiver.prototype))
    return;
  const t = a.RTCPeerConnection.prototype.getStats;
  a.RTCPeerConnection.prototype.getStats = function() {
    if (arguments.length > 0 && arguments[0] instanceof a.MediaStreamTrack) {
      const s = arguments[0];
      let c, u, d;
      return this.getSenders().forEach((h) => {
        h.track === s && (c ? d = !0 : c = h);
      }), this.getReceivers().forEach((h) => (h.track === s && (u ? d = !0 : u = h), h.track === s)), d || c && u ? Promise.reject(new DOMException("There are more than one sender or receiver for the track.", "InvalidAccessError")) : c ? c.getStats() : u ? u.getStats() : Promise.reject(new DOMException("There is no sender or receiver for the track.", "InvalidAccessError"));
    }
    return t.apply(this, arguments);
  };
}
function YT(a) {
  a.RTCPeerConnection.prototype.getLocalStreams = function() {
    return this._shimmedLocalStreams = this._shimmedLocalStreams || {}, Object.keys(this._shimmedLocalStreams).map((u) => this._shimmedLocalStreams[u][0]);
  };
  const e = a.RTCPeerConnection.prototype.addTrack;
  a.RTCPeerConnection.prototype.addTrack = function(u, d) {
    if (!d)
      return e.apply(this, arguments);
    this._shimmedLocalStreams = this._shimmedLocalStreams || {};
    const h = e.apply(this, arguments);
    return this._shimmedLocalStreams[d.id] ? this._shimmedLocalStreams[d.id].indexOf(h) === -1 && this._shimmedLocalStreams[d.id].push(h) : this._shimmedLocalStreams[d.id] = [d, h], h;
  };
  const t = a.RTCPeerConnection.prototype.addStream;
  a.RTCPeerConnection.prototype.addStream = function(u) {
    this._shimmedLocalStreams = this._shimmedLocalStreams || {}, u.getTracks().forEach((f) => {
      if (this.getSenders().find((v) => v.track === f))
        throw new DOMException("Track already exists.", "InvalidAccessError");
    });
    const d = this.getSenders();
    t.apply(this, arguments);
    const h = this.getSenders().filter((f) => d.indexOf(f) === -1);
    this._shimmedLocalStreams[u.id] = [u].concat(h);
  };
  const i = a.RTCPeerConnection.prototype.removeStream;
  a.RTCPeerConnection.prototype.removeStream = function(u) {
    return this._shimmedLocalStreams = this._shimmedLocalStreams || {}, delete this._shimmedLocalStreams[u.id], i.apply(this, arguments);
  };
  const s = a.RTCPeerConnection.prototype.removeTrack;
  a.RTCPeerConnection.prototype.removeTrack = function(u) {
    return this._shimmedLocalStreams = this._shimmedLocalStreams || {}, u && Object.keys(this._shimmedLocalStreams).forEach((d) => {
      const h = this._shimmedLocalStreams[d].indexOf(u);
      h !== -1 && this._shimmedLocalStreams[d].splice(h, 1), this._shimmedLocalStreams[d].length === 1 && delete this._shimmedLocalStreams[d];
    }), s.apply(this, arguments);
  };
}
function JT(a, e) {
  if (!a.RTCPeerConnection)
    return;
  if (a.RTCPeerConnection.prototype.addTrack && e.version >= 65)
    return YT(a);
  const t = a.RTCPeerConnection.prototype.getLocalStreams;
  a.RTCPeerConnection.prototype.getLocalStreams = function() {
    const p = t.apply(this);
    return this._reverseStreams = this._reverseStreams || {}, p.map((v) => this._reverseStreams[v.id]);
  };
  const i = a.RTCPeerConnection.prototype.addStream;
  a.RTCPeerConnection.prototype.addStream = function(p) {
    if (this._streams = this._streams || {}, this._reverseStreams = this._reverseStreams || {}, p.getTracks().forEach((v) => {
      if (this.getSenders().find((T) => T.track === v))
        throw new DOMException("Track already exists.", "InvalidAccessError");
    }), !this._reverseStreams[p.id]) {
      const v = new a.MediaStream(p.getTracks());
      this._streams[p.id] = v, this._reverseStreams[v.id] = p, p = v;
    }
    i.apply(this, [p]);
  };
  const s = a.RTCPeerConnection.prototype.removeStream;
  a.RTCPeerConnection.prototype.removeStream = function(p) {
    this._streams = this._streams || {}, this._reverseStreams = this._reverseStreams || {}, s.apply(this, [this._streams[p.id] || p]), delete this._reverseStreams[this._streams[p.id] ? this._streams[p.id].id : p.id], delete this._streams[p.id];
  }, a.RTCPeerConnection.prototype.addTrack = function(p, v) {
    if (this.signalingState === "closed")
      throw new DOMException("The RTCPeerConnection's signalingState is 'closed'.", "InvalidStateError");
    const y = [].slice.call(arguments, 1);
    if (y.length !== 1 || !y[0].getTracks().find((k) => k === p))
      throw new DOMException("The adapter.js addTrack polyfill only supports a single  stream which is associated with the specified track.", "NotSupportedError");
    if (this.getSenders().find((k) => k.track === p))
      throw new DOMException("Track already exists.", "InvalidAccessError");
    this._streams = this._streams || {}, this._reverseStreams = this._reverseStreams || {};
    const _ = this._streams[v.id];
    if (_)
      _.addTrack(p), Promise.resolve().then(() => {
        this.dispatchEvent(new Event("negotiationneeded"));
      });
    else {
      const k = new a.MediaStream([p]);
      this._streams[v.id] = k, this._reverseStreams[k.id] = v, this.addStream(k);
    }
    return this.getSenders().find((k) => k.track === p);
  };
  function c(f, p) {
    let v = p.sdp;
    return Object.keys(f._reverseStreams || []).forEach((y) => {
      const T = f._reverseStreams[y], _ = f._streams[T.id];
      v = v.replace(new RegExp(_.id, "g"), T.id);
    }), new RTCSessionDescription({
      type: p.type,
      sdp: v
    });
  }
  function u(f, p) {
    let v = p.sdp;
    return Object.keys(f._reverseStreams || []).forEach((y) => {
      const T = f._reverseStreams[y], _ = f._streams[T.id];
      v = v.replace(new RegExp(T.id, "g"), _.id);
    }), new RTCSessionDescription({
      type: p.type,
      sdp: v
    });
  }
  ["createOffer", "createAnswer"].forEach(function(f) {
    const p = a.RTCPeerConnection.prototype[f], v = {
      [f]() {
        const y = arguments;
        return arguments.length && typeof arguments[0] == "function" ? p.apply(this, [(_) => {
          const k = c(this, _);
          y[0].apply(null, [k]);
        }, (_) => {
          y[1] && y[1].apply(null, _);
        }, arguments[2]]) : p.apply(this, arguments).then((_) => c(this, _));
      }
    };
    a.RTCPeerConnection.prototype[f] = v[f];
  });
  const d = a.RTCPeerConnection.prototype.setLocalDescription;
  a.RTCPeerConnection.prototype.setLocalDescription = function() {
    return !arguments.length || !arguments[0].type ? d.apply(this, arguments) : (arguments[0] = u(this, arguments[0]), d.apply(this, arguments));
  };
  const h = Object.getOwnPropertyDescriptor(a.RTCPeerConnection.prototype, "localDescription");
  Object.defineProperty(a.RTCPeerConnection.prototype, "localDescription", {
    get() {
      const f = h.get.apply(this);
      return f.type === "" ? f : c(this, f);
    }
  }), a.RTCPeerConnection.prototype.removeTrack = function(p) {
    if (this.signalingState === "closed")
      throw new DOMException("The RTCPeerConnection's signalingState is 'closed'.", "InvalidStateError");
    if (!p._pc)
      throw new DOMException("Argument 1 of RTCPeerConnection.removeTrack does not implement interface RTCRtpSender.", "TypeError");
    if (!(p._pc === this))
      throw new DOMException("Sender was not created by this connection.", "InvalidAccessError");
    this._streams = this._streams || {};
    let y;
    Object.keys(this._streams).forEach((T) => {
      this._streams[T].getTracks().find((k) => p.track === k) && (y = this._streams[T]);
    }), y && (y.getTracks().length === 1 ? this.removeStream(this._reverseStreams[y.id]) : y.removeTrack(p.track), this.dispatchEvent(new Event("negotiationneeded")));
  };
}
function _h(a, e) {
  !a.RTCPeerConnection && a.webkitRTCPeerConnection && (a.RTCPeerConnection = a.webkitRTCPeerConnection), a.RTCPeerConnection && e.version < 53 && ["setLocalDescription", "setRemoteDescription", "addIceCandidate"].forEach(function(t) {
    const i = a.RTCPeerConnection.prototype[t], s = {
      [t]() {
        return arguments[0] = new (t === "addIceCandidate" ? a.RTCIceCandidate : a.RTCSessionDescription)(arguments[0]), i.apply(this, arguments);
      }
    };
    a.RTCPeerConnection.prototype[t] = s[t];
  });
}
function QT(a, e) {
  e.version > 102 || or(a, "negotiationneeded", (t) => {
    const i = t.target;
    if (!((e.version < 72 || i.getConfiguration && i.getConfiguration().sdpSemantics === "plan-b") && i.signalingState !== "stable"))
      return t;
  });
}
var _b = /* @__PURE__ */ Object.freeze({ __proto__: null, fixNegotiationNeeded: QT, shimAddTrackRemoveTrack: JT, shimAddTrackRemoveTrackWithNative: YT, shimGetSendersWithDtmf: GT, shimGetUserMedia: qT, shimMediaStream: HT, shimOnTrack: VT, shimPeerConnection: _h, shimSenderReceiverGetStats: KT });
function XT(a, e) {
  const t = a && a.navigator;
  if (!t.mediaDevices)
    return;
  const i = a && a.MediaStreamTrack;
  if (t.getUserMedia = function(s, c, u) {
    hm("navigator.getUserMedia", "navigator.mediaDevices.getUserMedia"), t.mediaDevices.getUserMedia(s).then(c, u);
  }, !(e.version > 55 && "autoGainControl" in t.mediaDevices.getSupportedConstraints())) {
    const s = function(u, d, h) {
      d in u && !(h in u) && (u[h] = u[d], delete u[d]);
    }, c = t.mediaDevices.getUserMedia.bind(t.mediaDevices);
    if (t.mediaDevices.getUserMedia = function(u) {
      return typeof u == "object" && typeof u.audio == "object" && (u = JSON.parse(JSON.stringify(u)), s(u.audio, "autoGainControl", "mozAutoGainControl"), s(u.audio, "noiseSuppression", "mozNoiseSuppression")), c(u);
    }, i && i.prototype.getSettings) {
      const u = i.prototype.getSettings;
      i.prototype.getSettings = function() {
        const d = u.apply(this, arguments);
        return s(d, "mozAutoGainControl", "autoGainControl"), s(d, "mozNoiseSuppression", "noiseSuppression"), d;
      };
    }
    if (i && i.prototype.applyConstraints) {
      const u = i.prototype.applyConstraints;
      i.prototype.applyConstraints = function(d) {
        return this.kind === "audio" && typeof d == "object" && (d = JSON.parse(JSON.stringify(d)), s(d, "autoGainControl", "mozAutoGainControl"), s(d, "noiseSuppression", "mozNoiseSuppression")), u.apply(this, [d]);
      };
    }
  }
}
function I1(a, e) {
  a.navigator.mediaDevices && (a.navigator.mediaDevices && "getDisplayMedia" in a.navigator.mediaDevices || (a.navigator.mediaDevices.getDisplayMedia = function(i) {
    if (!(i && i.video)) {
      const s = new DOMException("getDisplayMedia without video constraints is undefined");
      return s.name = "NotFoundError", s.code = 8, Promise.reject(s);
    }
    return i.video === !0 ? i.video = {
      mediaSource: e
    } : i.video.mediaSource = e, a.navigator.mediaDevices.getUserMedia(i);
  }));
}
function ZT(a) {
  typeof a == "object" && a.RTCTrackEvent && "receiver" in a.RTCTrackEvent.prototype && !("transceiver" in a.RTCTrackEvent.prototype) && Object.defineProperty(a.RTCTrackEvent.prototype, "transceiver", {
    get() {
      return {
        receiver: this.receiver
      };
    }
  });
}
function Rh(a, e) {
  typeof a != "object" || !(a.RTCPeerConnection || a.mozRTCPeerConnection) || (!a.RTCPeerConnection && a.mozRTCPeerConnection && (a.RTCPeerConnection = a.mozRTCPeerConnection), e.version < 53 && ["setLocalDescription", "setRemoteDescription", "addIceCandidate"].forEach(function(t) {
    const i = a.RTCPeerConnection.prototype[t], s = {
      [t]() {
        return arguments[0] = new (t === "addIceCandidate" ? a.RTCIceCandidate : a.RTCSessionDescription)(arguments[0]), i.apply(this, arguments);
      }
    };
    a.RTCPeerConnection.prototype[t] = s[t];
  }));
}
function WT(a, e) {
  if (typeof a != "object" || !(a.RTCPeerConnection || a.mozRTCPeerConnection) || e.version >= 151)
    return;
  const t = {
    inboundrtp: "inbound-rtp",
    outboundrtp: "outbound-rtp",
    candidatepair: "candidate-pair",
    localcandidate: "local-candidate",
    remotecandidate: "remote-candidate"
  }, i = a.RTCPeerConnection.prototype.getStats;
  a.RTCPeerConnection.prototype.getStats = function() {
    const c = Array.prototype.slice.call(arguments), u = c[0], d = c[1], h = c[2];
    return this.signalingState === "closed" ? Promise.resolve(/* @__PURE__ */ new Map()) : i.apply(this, [u || null]).then((f) => {
      if (e.version < 53 && !d)
        try {
          f.forEach((p) => {
            p.type = t[p.type] || p.type;
          });
        } catch (p) {
          if (p.name !== "TypeError")
            throw p;
          f.forEach((v, y) => {
            f.set(y, Object.assign({}, v, {
              type: t[v.type] || v.type
            }));
          });
        }
      return f;
    }).then(d, h);
  };
}
function $T(a) {
  if (!(typeof a == "object" && a.RTCPeerConnection && a.RTCRtpSender) || a.RTCRtpSender && "getStats" in a.RTCRtpSender.prototype)
    return;
  const e = a.RTCPeerConnection.prototype.getSenders;
  e && (a.RTCPeerConnection.prototype.getSenders = function() {
    const s = e.apply(this, []);
    return s.forEach((c) => c._pc = this), s;
  });
  const t = a.RTCPeerConnection.prototype.addTrack;
  t && (a.RTCPeerConnection.prototype.addTrack = function() {
    const s = t.apply(this, arguments);
    return s._pc = this, s;
  }), a.RTCRtpSender.prototype.getStats = function() {
    return this.track ? this._pc.getStats(this.track) : Promise.resolve(/* @__PURE__ */ new Map());
  };
}
function eS(a) {
  if (!(typeof a == "object" && a.RTCPeerConnection && a.RTCRtpSender) || a.RTCRtpSender && "getStats" in a.RTCRtpReceiver.prototype)
    return;
  const e = a.RTCPeerConnection.prototype.getReceivers;
  e && (a.RTCPeerConnection.prototype.getReceivers = function() {
    const i = e.apply(this, []);
    return i.forEach((s) => s._pc = this), i;
  }), or(a, "track", (t) => (t.receiver._pc = t.srcElement, t)), a.RTCRtpReceiver.prototype.getStats = function() {
    return this._pc.getStats(this.track);
  };
}
function tS(a) {
  !a.RTCPeerConnection || "removeStream" in a.RTCPeerConnection.prototype || (a.RTCPeerConnection.prototype.removeStream = function(t) {
    hm("removeStream", "removeTrack"), this.getSenders().forEach((i) => {
      i.track && t.getTracks().includes(i.track) && this.removeTrack(i);
    });
  });
}
function nS(a) {
  a.DataChannel && !a.RTCDataChannel && (a.RTCDataChannel = a.DataChannel);
}
function iS(a, e) {
  if (!(typeof a == "object" && a.RTCPeerConnection) || e.version >= 110)
    return;
  const t = a.RTCPeerConnection.prototype.addTransceiver;
  t && (a.RTCPeerConnection.prototype.addTransceiver = function() {
    this.setParametersPromises = [];
    let s = arguments[1] && arguments[1].sendEncodings;
    s === void 0 && (s = []), s = [...s];
    const c = s.length > 0;
    c && s.forEach((d) => {
      if ("rid" in d && !/^[a-z0-9]{0,16}$/i.test(d.rid))
        throw new TypeError("Invalid RID value provided.");
      if ("scaleResolutionDownBy" in d && !(parseFloat(d.scaleResolutionDownBy) >= 1))
        throw new RangeError("scale_resolution_down_by must be >= 1.0");
      if ("maxFramerate" in d && !(parseFloat(d.maxFramerate) >= 0))
        throw new RangeError("max_framerate must be >= 0.0");
    });
    const u = t.apply(this, arguments);
    if (c) {
      const d = u.sender, h = d.getParameters();
      (!("encodings" in h) || // Avoid being fooled by patched getParameters() below.
      h.encodings.length === 1 && Object.keys(h.encodings[0]).length === 0) && (h.encodings = s, d.sendEncodings = s, this.setParametersPromises.push(d.setParameters(h).then(() => {
        delete d.sendEncodings;
      }).catch(() => {
        delete d.sendEncodings;
      })));
    }
    return u;
  });
}
function aS(a, e) {
  if (!(typeof a == "object" && a.RTCRtpSender) || e.version >= 110)
    return;
  const t = a.RTCRtpSender.prototype.getParameters;
  t && (a.RTCRtpSender.prototype.getParameters = function() {
    const s = t.apply(this, arguments);
    return "encodings" in s || (s.encodings = [].concat(this.sendEncodings || [{}])), s;
  });
}
function rS(a, e) {
  if (!(typeof a == "object" && a.RTCPeerConnection) || e.version >= 110)
    return;
  const t = a.RTCPeerConnection.prototype.createOffer;
  a.RTCPeerConnection.prototype.createOffer = function() {
    return this.setParametersPromises && this.setParametersPromises.length ? Promise.all(this.setParametersPromises).then(() => t.apply(this, arguments)).finally(() => {
      this.setParametersPromises = [];
    }) : t.apply(this, arguments);
  };
}
function sS(a, e) {
  if (!(typeof a == "object" && a.RTCPeerConnection) || e.version >= 110)
    return;
  const t = a.RTCPeerConnection.prototype.createAnswer;
  a.RTCPeerConnection.prototype.createAnswer = function() {
    return this.setParametersPromises && this.setParametersPromises.length ? Promise.all(this.setParametersPromises).then(() => t.apply(this, arguments)).finally(() => {
      this.setParametersPromises = [];
    }) : t.apply(this, arguments);
  };
}
var Rb = /* @__PURE__ */ Object.freeze({ __proto__: null, shimAddTransceiver: iS, shimCreateAnswer: sS, shimCreateOffer: rS, shimGetDisplayMedia: I1, shimGetParameters: aS, shimGetStats: WT, shimGetUserMedia: XT, shimOnTrack: ZT, shimPeerConnection: Rh, shimRTCDataChannel: nS, shimReceiverGetStats: eS, shimRemoveStream: tS, shimSenderGetStats: $T });
function oS(a) {
  if (!(typeof a != "object" || !a.RTCPeerConnection)) {
    if ("getLocalStreams" in a.RTCPeerConnection.prototype || (a.RTCPeerConnection.prototype.getLocalStreams = function() {
      return this._localStreams || (this._localStreams = []), this._localStreams;
    }), !("addStream" in a.RTCPeerConnection.prototype)) {
      const e = a.RTCPeerConnection.prototype.addTrack;
      a.RTCPeerConnection.prototype.addStream = function(i) {
        this._localStreams || (this._localStreams = []), this._localStreams.includes(i) || this._localStreams.push(i), i.getAudioTracks().forEach((s) => e.call(this, s, i)), i.getVideoTracks().forEach((s) => e.call(this, s, i));
      }, a.RTCPeerConnection.prototype.addTrack = function(i) {
        for (var s = arguments.length, c = new Array(s > 1 ? s - 1 : 0), u = 1; u < s; u++)
          c[u - 1] = arguments[u];
        return c && c.forEach((d) => {
          this._localStreams ? this._localStreams.includes(d) || this._localStreams.push(d) : this._localStreams = [d];
        }), e.apply(this, arguments);
      };
    }
    "removeStream" in a.RTCPeerConnection.prototype || (a.RTCPeerConnection.prototype.removeStream = function(t) {
      this._localStreams || (this._localStreams = []);
      const i = this._localStreams.indexOf(t);
      if (i === -1)
        return;
      this._localStreams.splice(i, 1);
      const s = t.getTracks();
      this.getSenders().forEach((c) => {
        s.includes(c.track) && this.removeTrack(c);
      });
    });
  }
}
function cS(a) {
  if (!(typeof a != "object" || !a.RTCPeerConnection) && ("getRemoteStreams" in a.RTCPeerConnection.prototype || (a.RTCPeerConnection.prototype.getRemoteStreams = function() {
    return this._remoteStreams ? this._remoteStreams : [];
  }), !("onaddstream" in a.RTCPeerConnection.prototype))) {
    Object.defineProperty(a.RTCPeerConnection.prototype, "onaddstream", {
      get() {
        return this._onaddstream;
      },
      set(t) {
        this._onaddstream && (this.removeEventListener("addstream", this._onaddstream), this.removeEventListener("track", this._onaddstreampoly)), this.addEventListener("addstream", this._onaddstream = t), this.addEventListener("track", this._onaddstreampoly = (i) => {
          i.streams.forEach((s) => {
            if (this._remoteStreams || (this._remoteStreams = []), this._remoteStreams.includes(s))
              return;
            this._remoteStreams.push(s);
            const c = new Event("addstream");
            c.stream = s, this.dispatchEvent(c);
          });
        });
      }
    });
    const e = a.RTCPeerConnection.prototype.setRemoteDescription;
    a.RTCPeerConnection.prototype.setRemoteDescription = function() {
      const i = this;
      return this._onaddstreampoly || this.addEventListener("track", this._onaddstreampoly = function(s) {
        s.streams.forEach((c) => {
          if (i._remoteStreams || (i._remoteStreams = []), i._remoteStreams.indexOf(c) >= 0)
            return;
          i._remoteStreams.push(c);
          const u = new Event("addstream");
          u.stream = c, i.dispatchEvent(u);
        });
      }), e.apply(i, arguments);
    };
  }
}
function lS(a) {
  if (typeof a != "object" || !a.RTCPeerConnection)
    return;
  const e = a.RTCPeerConnection.prototype, t = e.createOffer, i = e.createAnswer, s = e.setLocalDescription, c = e.setRemoteDescription, u = e.addIceCandidate;
  e.createOffer = function(f, p) {
    const v = arguments.length >= 2 ? arguments[2] : arguments[0], y = t.apply(this, [v]);
    return p ? (y.then(f, p), Promise.resolve()) : y;
  }, e.createAnswer = function(f, p) {
    const v = arguments.length >= 2 ? arguments[2] : arguments[0], y = i.apply(this, [v]);
    return p ? (y.then(f, p), Promise.resolve()) : y;
  };
  let d = function(h, f, p) {
    const v = s.apply(this, [h]);
    return p ? (v.then(f, p), Promise.resolve()) : v;
  };
  e.setLocalDescription = d, d = function(h, f, p) {
    const v = c.apply(this, [h]);
    return p ? (v.then(f, p), Promise.resolve()) : v;
  }, e.setRemoteDescription = d, d = function(h, f, p) {
    const v = u.apply(this, [h]);
    return p ? (v.then(f, p), Promise.resolve()) : v;
  }, e.addIceCandidate = d;
}
function uS(a) {
  const e = a && a.navigator;
  if (e.mediaDevices && e.mediaDevices.getUserMedia) {
    const t = e.mediaDevices, i = t.getUserMedia.bind(t);
    e.mediaDevices.getUserMedia = (s) => i(dS(s));
  }
  !e.getUserMedia && e.mediaDevices && e.mediaDevices.getUserMedia && (e.getUserMedia = function(i, s, c) {
    e.mediaDevices.getUserMedia(i).then(s, c);
  }.bind(e));
}
function dS(a) {
  return a && a.video !== void 0 ? Object.assign({}, a, {
    video: jT(a.video)
  }) : a;
}
function fS(a) {
  if (!a.RTCPeerConnection)
    return;
  const e = a.RTCPeerConnection;
  a.RTCPeerConnection = function(i, s) {
    if (i && i.iceServers) {
      const c = [];
      for (let u = 0; u < i.iceServers.length; u++) {
        let d = i.iceServers[u];
        d.urls === void 0 && d.url ? (hm("RTCIceServer.url", "RTCIceServer.urls"), d = JSON.parse(JSON.stringify(d)), d.urls = d.url, delete d.url, c.push(d)) : c.push(i.iceServers[u]);
      }
      i.iceServers = c;
    }
    return new e(i, s);
  }, a.RTCPeerConnection.prototype = e.prototype, "generateCertificate" in e && Object.defineProperty(a.RTCPeerConnection, "generateCertificate", {
    get() {
      return e.generateCertificate;
    }
  });
}
function hS(a) {
  typeof a == "object" && a.RTCTrackEvent && "receiver" in a.RTCTrackEvent.prototype && !("transceiver" in a.RTCTrackEvent.prototype) && Object.defineProperty(a.RTCTrackEvent.prototype, "transceiver", {
    get() {
      return {
        receiver: this.receiver
      };
    }
  });
}
function mS(a) {
  const e = a.RTCPeerConnection.prototype.createOffer;
  a.RTCPeerConnection.prototype.createOffer = function(i) {
    if (i) {
      typeof i.offerToReceiveAudio < "u" && (i.offerToReceiveAudio = !!i.offerToReceiveAudio);
      const s = this.getTransceivers().find((u) => u.receiver.track.kind === "audio");
      i.offerToReceiveAudio === !1 && s ? s.direction === "sendrecv" ? s.setDirection ? s.setDirection("sendonly") : s.direction = "sendonly" : s.direction === "recvonly" && (s.setDirection ? s.setDirection("inactive") : s.direction = "inactive") : i.offerToReceiveAudio === !0 && !s && this.addTransceiver("audio", {
        direction: "recvonly"
      }), typeof i.offerToReceiveVideo < "u" && (i.offerToReceiveVideo = !!i.offerToReceiveVideo);
      const c = this.getTransceivers().find((u) => u.receiver.track.kind === "video");
      i.offerToReceiveVideo === !1 && c ? c.direction === "sendrecv" ? c.setDirection ? c.setDirection("sendonly") : c.direction = "sendonly" : c.direction === "recvonly" && (c.setDirection ? c.setDirection("inactive") : c.direction = "inactive") : i.offerToReceiveVideo === !0 && !c && this.addTransceiver("video", {
        direction: "recvonly"
      });
    }
    return e.apply(this, arguments);
  };
}
function pS(a) {
  typeof a != "object" || a.AudioContext || (a.AudioContext = a.webkitAudioContext);
}
var wb = /* @__PURE__ */ Object.freeze({ __proto__: null, shimAudioContext: pS, shimCallbacksAPI: lS, shimConstraints: dS, shimCreateOfferLegacy: mS, shimGetUserMedia: uS, shimLocalStreamsAPI: oS, shimRTCIceServerUrls: fS, shimRemoteStreamsAPI: cS, shimTrackEventTransceiver: hS }), Yf = { exports: {} }, Mb;
function L1() {
  return Mb || (Mb = 1, (function(a) {
    const e = {};
    e.generateIdentifier = function() {
      return Math.random().toString(36).substring(2, 12);
    }, e.localCName = e.generateIdentifier(), e.splitLines = function(t) {
      return t.trim().split(`
`).map((i) => i.trim());
    }, e.splitSections = function(t) {
      return t.split(`
m=`).map((s, c) => (c > 0 ? "m=" + s : s).trim() + `\r
`);
    }, e.getDescription = function(t) {
      const i = e.splitSections(t);
      return i && i[0];
    }, e.getMediaSections = function(t) {
      const i = e.splitSections(t);
      return i.shift(), i;
    }, e.matchPrefix = function(t, i) {
      return e.splitLines(t).filter((s) => s.indexOf(i) === 0);
    }, e.parseCandidate = function(t) {
      let i;
      t.indexOf("a=candidate:") === 0 ? i = t.substring(12).split(" ") : i = t.substring(10).split(" ");
      const s = {
        foundation: i[0],
        component: {
          1: "rtp",
          2: "rtcp"
        }[i[1]] || i[1],
        protocol: i[2].toLowerCase(),
        priority: parseInt(i[3], 10),
        ip: i[4],
        address: i[4],
        // address is an alias for ip.
        port: parseInt(i[5], 10),
        // skip parts[6] == 'typ'
        type: i[7]
      };
      for (let c = 8; c < i.length; c += 2)
        switch (i[c]) {
          case "raddr":
            s.relatedAddress = i[c + 1];
            break;
          case "rport":
            s.relatedPort = parseInt(i[c + 1], 10);
            break;
          case "tcptype":
            s.tcpType = i[c + 1];
            break;
          case "ufrag":
            s.ufrag = i[c + 1], s.usernameFragment = i[c + 1];
            break;
          default:
            s[i[c]] === void 0 && (s[i[c]] = i[c + 1]);
            break;
        }
      return s;
    }, e.writeCandidate = function(t) {
      const i = [];
      i.push(t.foundation);
      const s = t.component;
      s === "rtp" ? i.push(1) : s === "rtcp" ? i.push(2) : i.push(s), i.push(t.protocol.toUpperCase()), i.push(t.priority), i.push(t.address || t.ip), i.push(t.port);
      const c = t.type;
      return i.push("typ"), i.push(c), c !== "host" && t.relatedAddress && t.relatedPort !== void 0 && (i.push("raddr"), i.push(t.relatedAddress), i.push("rport"), i.push(t.relatedPort)), t.tcpType && t.protocol.toLowerCase() === "tcp" && (i.push("tcptype"), i.push(t.tcpType)), (t.usernameFragment || t.ufrag) && (i.push("ufrag"), i.push(t.usernameFragment || t.ufrag)), "candidate:" + i.join(" ");
    }, e.parseIceOptions = function(t) {
      return t.substring(14).split(" ");
    }, e.parseRtpMap = function(t) {
      let i = t.substring(9).split(" ");
      const s = {
        payloadType: parseInt(i.shift(), 10)
        // was: id
      };
      return i = i[0].split("/"), s.name = i[0], s.clockRate = parseInt(i[1], 10), s.channels = i.length === 3 ? parseInt(i[2], 10) : 1, s.numChannels = s.channels, s;
    }, e.writeRtpMap = function(t) {
      let i = t.payloadType;
      t.preferredPayloadType !== void 0 && (i = t.preferredPayloadType);
      const s = t.channels || t.numChannels || 1;
      return "a=rtpmap:" + i + " " + t.name + "/" + t.clockRate + (s !== 1 ? "/" + s : "") + `\r
`;
    }, e.parseExtmap = function(t) {
      const i = t.substring(9).split(" ");
      return {
        id: parseInt(i[0], 10),
        direction: i[0].indexOf("/") > 0 ? i[0].split("/")[1] : "sendrecv",
        uri: i[1],
        attributes: i.slice(2).join(" ")
      };
    }, e.writeExtmap = function(t) {
      return "a=extmap:" + (t.id || t.preferredId) + (t.direction && t.direction !== "sendrecv" ? "/" + t.direction : "") + " " + t.uri + (t.attributes ? " " + t.attributes : "") + `\r
`;
    }, e.parseFmtp = function(t) {
      const i = {};
      let s;
      const c = t.substring(t.indexOf(" ") + 1).split(";");
      for (let u = 0; u < c.length; u++)
        s = c[u].trim().split("="), i[s[0].trim()] = s[1];
      return i;
    }, e.writeFmtp = function(t) {
      let i = "", s = t.payloadType;
      if (t.preferredPayloadType !== void 0 && (s = t.preferredPayloadType), t.parameters && Object.keys(t.parameters).length) {
        const c = [];
        Object.keys(t.parameters).forEach((u) => {
          t.parameters[u] !== void 0 ? c.push(u + "=" + t.parameters[u]) : c.push(u);
        }), i += "a=fmtp:" + s + " " + c.join(";") + `\r
`;
      }
      return i;
    }, e.parseRtcpFb = function(t) {
      const i = t.substring(t.indexOf(" ") + 1).split(" ");
      return {
        type: i.shift(),
        parameter: i.join(" ")
      };
    }, e.writeRtcpFb = function(t) {
      let i = "", s = t.payloadType;
      return t.preferredPayloadType !== void 0 && (s = t.preferredPayloadType), t.rtcpFeedback && t.rtcpFeedback.length && t.rtcpFeedback.forEach((c) => {
        i += "a=rtcp-fb:" + s + " " + c.type + (c.parameter && c.parameter.length ? " " + c.parameter : "") + `\r
`;
      }), i;
    }, e.parseSsrcMedia = function(t) {
      const i = t.indexOf(" "), s = {
        ssrc: parseInt(t.substring(7, i), 10)
      }, c = t.indexOf(":", i);
      return c > -1 ? (s.attribute = t.substring(i + 1, c), s.value = t.substring(c + 1)) : s.attribute = t.substring(i + 1), s;
    }, e.parseSsrcGroup = function(t) {
      const i = t.substring(13).split(" ");
      return {
        semantics: i.shift(),
        ssrcs: i.map((s) => parseInt(s, 10))
      };
    }, e.getMid = function(t) {
      const i = e.matchPrefix(t, "a=mid:")[0];
      if (i)
        return i.substring(6);
    }, e.parseFingerprint = function(t) {
      const i = t.substring(14).split(" ");
      return {
        algorithm: i[0].toLowerCase(),
        // algorithm is case-sensitive in Edge.
        value: i[1].toUpperCase()
        // the definition is upper-case in RFC 4572.
      };
    }, e.getDtlsParameters = function(t, i) {
      return {
        role: "auto",
        fingerprints: e.matchPrefix(t + i, "a=fingerprint:").map(e.parseFingerprint)
      };
    }, e.writeDtlsParameters = function(t, i) {
      let s = "a=setup:" + i + `\r
`;
      return t.fingerprints.forEach((c) => {
        s += "a=fingerprint:" + c.algorithm + " " + c.value + `\r
`;
      }), s;
    }, e.parseCryptoLine = function(t) {
      const i = t.substring(9).split(" ");
      return {
        tag: parseInt(i[0], 10),
        cryptoSuite: i[1],
        keyParams: i[2],
        sessionParams: i.slice(3)
      };
    }, e.writeCryptoLine = function(t) {
      return "a=crypto:" + t.tag + " " + t.cryptoSuite + " " + (typeof t.keyParams == "object" ? e.writeCryptoKeyParams(t.keyParams) : t.keyParams) + (t.sessionParams ? " " + t.sessionParams.join(" ") : "") + `\r
`;
    }, e.parseCryptoKeyParams = function(t) {
      if (t.indexOf("inline:") !== 0)
        return null;
      const i = t.substring(7).split("|");
      return {
        keyMethod: "inline",
        keySalt: i[0],
        lifeTime: i[1],
        mkiValue: i[2] ? i[2].split(":")[0] : void 0,
        mkiLength: i[2] ? i[2].split(":")[1] : void 0
      };
    }, e.writeCryptoKeyParams = function(t) {
      return t.keyMethod + ":" + t.keySalt + (t.lifeTime ? "|" + t.lifeTime : "") + (t.mkiValue && t.mkiLength ? "|" + t.mkiValue + ":" + t.mkiLength : "");
    }, e.getCryptoParameters = function(t, i) {
      return e.matchPrefix(t + i, "a=crypto:").map(e.parseCryptoLine);
    }, e.getIceParameters = function(t, i) {
      const s = e.matchPrefix(t + i, "a=ice-ufrag:")[0], c = e.matchPrefix(t + i, "a=ice-pwd:")[0];
      return s && c ? {
        usernameFragment: s.substring(12),
        password: c.substring(10)
      } : null;
    }, e.writeIceParameters = function(t) {
      let i = "a=ice-ufrag:" + t.usernameFragment + `\r
a=ice-pwd:` + t.password + `\r
`;
      return t.iceLite && (i += `a=ice-lite\r
`), i;
    }, e.parseRtpParameters = function(t) {
      const i = {
        codecs: [],
        headerExtensions: [],
        fecMechanisms: [],
        rtcp: []
      }, c = e.splitLines(t)[0].split(" ");
      i.profile = c[2];
      for (let d = 3; d < c.length; d++) {
        const h = c[d], f = e.matchPrefix(t, "a=rtpmap:" + h + " ")[0];
        if (f) {
          const p = e.parseRtpMap(f), v = e.matchPrefix(t, "a=fmtp:" + h + " ");
          switch (p.parameters = v.length ? e.parseFmtp(v[0]) : {}, p.rtcpFeedback = e.matchPrefix(t, "a=rtcp-fb:" + h + " ").map(e.parseRtcpFb), i.codecs.push(p), p.name.toUpperCase()) {
            case "RED":
            case "ULPFEC":
              i.fecMechanisms.push(p.name.toUpperCase());
              break;
          }
        }
      }
      e.matchPrefix(t, "a=extmap:").forEach((d) => {
        i.headerExtensions.push(e.parseExtmap(d));
      });
      const u = e.matchPrefix(t, "a=rtcp-fb:* ").map(e.parseRtcpFb);
      return i.codecs.forEach((d) => {
        u.forEach((h) => {
          d.rtcpFeedback.find((p) => p.type === h.type && p.parameter === h.parameter) || d.rtcpFeedback.push(h);
        });
      }), i;
    }, e.writeRtpDescription = function(t, i) {
      let s = "";
      s += "m=" + t + " ", s += i.codecs.length > 0 ? "9" : "0", s += " " + (i.profile || "UDP/TLS/RTP/SAVPF") + " ", s += i.codecs.map((u) => u.preferredPayloadType !== void 0 ? u.preferredPayloadType : u.payloadType).join(" ") + `\r
`, s += `c=IN IP4 0.0.0.0\r
`, s += `a=rtcp:9 IN IP4 0.0.0.0\r
`, i.codecs.forEach((u) => {
        s += e.writeRtpMap(u), s += e.writeFmtp(u), s += e.writeRtcpFb(u);
      });
      let c = 0;
      return i.codecs.forEach((u) => {
        u.maxptime > c && (c = u.maxptime);
      }), c > 0 && (s += "a=maxptime:" + c + `\r
`), i.headerExtensions && i.headerExtensions.forEach((u) => {
        s += e.writeExtmap(u);
      }), s;
    }, e.parseRtpEncodingParameters = function(t) {
      const i = [], s = e.parseRtpParameters(t), c = s.fecMechanisms.indexOf("RED") !== -1, u = s.fecMechanisms.indexOf("ULPFEC") !== -1, d = e.matchPrefix(t, "a=ssrc:").map((y) => e.parseSsrcMedia(y)).filter((y) => y.attribute === "cname"), h = d.length > 0 && d[0].ssrc;
      let f;
      const p = e.matchPrefix(t, "a=ssrc-group:FID").map((y) => y.substring(17).split(" ").map((_) => parseInt(_, 10)));
      p.length > 0 && p[0].length > 1 && p[0][0] === h && (f = p[0][1]), s.codecs.forEach((y) => {
        if (y.name.toUpperCase() === "RTX" && y.parameters.apt) {
          let T = {
            ssrc: h,
            codecPayloadType: parseInt(y.parameters.apt, 10)
          };
          h && f && (T.rtx = {
            ssrc: f
          }), i.push(T), c && (T = JSON.parse(JSON.stringify(T)), T.fec = {
            ssrc: h,
            mechanism: u ? "red+ulpfec" : "red"
          }, i.push(T));
        }
      }), i.length === 0 && h && i.push({
        ssrc: h
      });
      let v = e.matchPrefix(t, "b=");
      return v.length && (v[0].indexOf("b=TIAS:") === 0 ? v = parseInt(v[0].substring(7), 10) : v[0].indexOf("b=AS:") === 0 ? v = parseInt(v[0].substring(5), 10) * 1e3 * 0.95 - 2e3 * 8 : v = void 0, i.forEach((y) => {
        y.maxBitrate = v;
      })), i;
    }, e.parseRtcpParameters = function(t) {
      const i = {}, s = e.matchPrefix(t, "a=ssrc:").map((d) => e.parseSsrcMedia(d)).filter((d) => d.attribute === "cname")[0];
      s && (i.cname = s.value, i.ssrc = s.ssrc);
      const c = e.matchPrefix(t, "a=rtcp-rsize");
      i.reducedSize = c.length > 0, i.compound = c.length === 0;
      const u = e.matchPrefix(t, "a=rtcp-mux");
      return i.mux = u.length > 0, i;
    }, e.writeRtcpParameters = function(t) {
      let i = "";
      return t.reducedSize && (i += `a=rtcp-rsize\r
`), t.mux && (i += `a=rtcp-mux\r
`), t.ssrc !== void 0 && t.cname && (i += "a=ssrc:" + t.ssrc + " cname:" + t.cname + `\r
`), i;
    }, e.parseMsid = function(t) {
      let i;
      const s = e.matchPrefix(t, "a=msid:");
      if (s.length === 1)
        return i = s[0].substring(7).split(" "), {
          stream: i[0],
          track: i[1]
        };
      const c = e.matchPrefix(t, "a=ssrc:").map((u) => e.parseSsrcMedia(u)).filter((u) => u.attribute === "msid");
      if (c.length > 0)
        return i = c[0].value.split(" "), {
          stream: i[0],
          track: i[1]
        };
    }, e.parseSctpDescription = function(t) {
      const i = e.parseMLine(t), s = e.matchPrefix(t, "a=max-message-size:");
      let c;
      s.length > 0 && (c = parseInt(s[0].substring(19), 10)), isNaN(c) && (c = 65536);
      const u = e.matchPrefix(t, "a=sctp-port:");
      if (u.length > 0)
        return {
          port: parseInt(u[0].substring(12), 10),
          protocol: i.fmt,
          maxMessageSize: c
        };
      const d = e.matchPrefix(t, "a=sctpmap:");
      if (d.length > 0) {
        const h = d[0].substring(10).split(" ");
        return {
          port: parseInt(h[0], 10),
          protocol: h[1],
          maxMessageSize: c
        };
      }
    }, e.writeSctpDescription = function(t, i) {
      let s = [];
      return t.protocol !== "DTLS/SCTP" ? s = ["m=" + t.kind + " 9 " + t.protocol + " " + i.protocol + `\r
`, `c=IN IP4 0.0.0.0\r
`, "a=sctp-port:" + i.port + `\r
`] : s = ["m=" + t.kind + " 9 " + t.protocol + " " + i.port + `\r
`, `c=IN IP4 0.0.0.0\r
`, "a=sctpmap:" + i.port + " " + i.protocol + ` 65535\r
`], i.maxMessageSize !== void 0 && s.push("a=max-message-size:" + i.maxMessageSize + `\r
`), s.join("");
    }, e.generateSessionId = function() {
      return Math.random().toString().substr(2, 22);
    }, e.writeSessionBoilerplate = function(t, i, s) {
      let c;
      const u = i !== void 0 ? i : 2;
      return t ? c = t : c = e.generateSessionId(), `v=0\r
o=` + (s || "thisisadapterortc") + " " + c + " " + u + ` IN IP4 127.0.0.1\r
s=-\r
t=0 0\r
`;
    }, e.getDirection = function(t, i) {
      const s = e.splitLines(t);
      for (let c = 0; c < s.length; c++)
        switch (s[c]) {
          case "a=sendrecv":
          case "a=sendonly":
          case "a=recvonly":
          case "a=inactive":
            return s[c].substring(2);
        }
      return i ? e.getDirection(i) : "sendrecv";
    }, e.getKind = function(t) {
      return e.splitLines(t)[0].split(" ")[0].substring(2);
    }, e.isRejected = function(t) {
      return t.split(" ", 2)[1] === "0";
    }, e.parseMLine = function(t) {
      const s = e.splitLines(t)[0].substring(2).split(" ");
      return {
        kind: s[0],
        port: parseInt(s[1], 10),
        protocol: s[2],
        fmt: s.slice(3).join(" ")
      };
    }, e.parseOLine = function(t) {
      const s = e.matchPrefix(t, "o=")[0].substring(2).split(" ");
      return {
        username: s[0],
        sessionId: s[1],
        sessionVersion: parseInt(s[2], 10),
        netType: s[3],
        addressType: s[4],
        address: s[5]
      };
    }, e.isValidSDP = function(t) {
      if (typeof t != "string" || t.length === 0)
        return !1;
      const i = e.splitLines(t);
      for (let s = 0; s < i.length; s++)
        if (i[s].length < 2 || i[s].charAt(1) !== "=")
          return !1;
      return !0;
    }, a.exports = e;
  })(Yf)), Yf.exports;
}
var gS = L1(), ts = /* @__PURE__ */ k1(gS), U1 = /* @__PURE__ */ g_({ __proto__: null, default: ts }, [gS]);
function Cl(a) {
  if (!a.RTCIceCandidate || a.RTCIceCandidate && "foundation" in a.RTCIceCandidate.prototype)
    return;
  const e = a.RTCIceCandidate;
  a.RTCIceCandidate = function(i) {
    if (typeof i == "object" && i.candidate && i.candidate.indexOf("a=") === 0 && (i = JSON.parse(JSON.stringify(i)), i.candidate = i.candidate.substring(2)), i.candidate && i.candidate.length) {
      const s = new e(i), c = ts.parseCandidate(i.candidate);
      for (const u in c)
        u in s || Object.defineProperty(s, u, {
          value: c[u]
        });
      return s.toJSON = function() {
        return {
          candidate: s.candidate,
          sdpMid: s.sdpMid,
          sdpMLineIndex: s.sdpMLineIndex,
          usernameFragment: s.usernameFragment
        };
      }, s;
    }
    return new e(i);
  }, a.RTCIceCandidate.prototype = e.prototype, or(a, "icecandidate", (t) => (t.candidate && Object.defineProperty(t, "candidate", {
    value: new a.RTCIceCandidate(t.candidate),
    writable: "false"
  }), t));
}
function wh(a) {
  !a.RTCIceCandidate || a.RTCIceCandidate && "relayProtocol" in a.RTCIceCandidate.prototype || or(a, "icecandidate", (e) => {
    if (e.candidate) {
      const t = ts.parseCandidate(e.candidate.candidate);
      t.type === "relay" && (e.candidate.relayProtocol = {
        0: "tls",
        1: "tcp",
        2: "udp"
      }[t.priority >> 24]);
    }
    return e;
  });
}
function El(a, e) {
  if (!a.RTCPeerConnection || e.browser === "chrome" && e.version > 102 || e.browser === "firefox" && e.version >= 113)
    return;
  "sctp" in a.RTCPeerConnection.prototype || Object.defineProperty(a.RTCPeerConnection.prototype, "sctp", {
    get() {
      return typeof this._sctp > "u" ? null : this._sctp;
    }
  });
  const t = function(d) {
    if (!d || !d.sdp)
      return !1;
    const h = ts.splitSections(d.sdp);
    return h.shift(), h.some((f) => {
      const p = ts.parseMLine(f);
      return p && p.kind === "application" && p.protocol.indexOf("SCTP") !== -1;
    });
  }, i = function(d) {
    const h = d.sdp.match(/mozilla...THIS_IS_SDPARTA-(\d+)/);
    if (h === null || h.length < 2)
      return -1;
    const f = parseInt(h[1], 10);
    return f !== f ? -1 : f;
  }, s = function(d) {
    let h = 65536;
    return e.browser === "firefox" && (e.version < 57 ? d === -1 ? h = 16384 : h = 2147483637 : e.version < 60 ? h = e.version === 57 ? 65535 : 65536 : h = 2147483637), h;
  }, c = function(d, h) {
    let f = 65536;
    e.browser === "firefox" && e.version === 57 && (f = 65535);
    const p = ts.matchPrefix(d.sdp, "a=max-message-size:");
    return p.length > 0 ? f = parseInt(p[0].substring(19), 10) : e.browser === "firefox" && h !== -1 && (f = 2147483637), f;
  }, u = a.RTCPeerConnection.prototype.setRemoteDescription;
  a.RTCPeerConnection.prototype.setRemoteDescription = function() {
    if (this._sctp = null, e.browser === "chrome" && e.version >= 76 && this.getConfiguration().sdpSemantics === "plan-b" && Object.defineProperty(this, "sctp", {
      get() {
        return typeof this._sctp > "u" ? null : this._sctp;
      },
      enumerable: !0,
      configurable: !0
    }), t(arguments[0])) {
      const h = i(arguments[0]), f = s(h), p = c(arguments[0], h);
      let v;
      f === 0 && p === 0 ? v = Number.POSITIVE_INFINITY : f === 0 || p === 0 ? v = Math.max(f, p) : v = Math.min(f, p);
      const y = {};
      Object.defineProperty(y, "maxMessageSize", {
        get() {
          return v;
        }
      }), this._sctp = y;
    }
    return u.apply(this, arguments);
  };
}
function _l(a, e) {
  if (!(a.RTCPeerConnection && "createDataChannel" in a.RTCPeerConnection.prototype) || e.browser === "chrome" && e.version >= 149 || e.browser === "firefox" && e.version > 60)
    return;
  function t(s, c) {
    const u = s.send;
    s.send = function() {
      const h = arguments[0], f = h.length || h.size || h.byteLength;
      if (s.readyState === "open" && c.sctp && f > c.sctp.maxMessageSize)
        throw new TypeError("Message too large (can send a maximum of " + c.sctp.maxMessageSize + " bytes)");
      return u.apply(s, arguments);
    };
  }
  const i = a.RTCPeerConnection.prototype.createDataChannel;
  a.RTCPeerConnection.prototype.createDataChannel = function() {
    const c = i.apply(this, arguments);
    return t(c, this), c;
  }, or(a, "datachannel", (s) => (t(s.channel, s.target), s));
}
function Mh(a) {
  if (!a.RTCPeerConnection || "connectionState" in a.RTCPeerConnection.prototype)
    return;
  const e = a.RTCPeerConnection.prototype;
  Object.defineProperty(e, "connectionState", {
    get() {
      return {
        completed: "connected",
        checking: "connecting"
      }[this.iceConnectionState] || this.iceConnectionState;
    },
    enumerable: !0,
    configurable: !0
  }), Object.defineProperty(e, "onconnectionstatechange", {
    get() {
      return this._onconnectionstatechange || null;
    },
    set(t) {
      this._onconnectionstatechange && (this.removeEventListener("connectionstatechange", this._onconnectionstatechange), delete this._onconnectionstatechange), t && this.addEventListener("connectionstatechange", this._onconnectionstatechange = t);
    },
    enumerable: !0,
    configurable: !0
  }), ["setLocalDescription", "setRemoteDescription"].forEach((t) => {
    const i = e[t];
    e[t] = function() {
      return this._connectionstatechangepoly || (this._connectionstatechangepoly = (s) => {
        const c = s.target;
        if (c._lastConnectionState !== c.connectionState) {
          c._lastConnectionState = c.connectionState;
          const u = new Event("connectionstatechange", s);
          c.dispatchEvent(u);
        }
        return s;
      }, this.addEventListener("iceconnectionstatechange", this._connectionstatechangepoly)), i.apply(this, arguments);
    };
  });
}
function Ah(a, e) {
  if (!a.RTCPeerConnection || e.browser === "chrome" && e.version >= 71 || e.browser === "safari" && e._safariVersion >= 13.1)
    return;
  const t = a.RTCPeerConnection.prototype.setRemoteDescription;
  a.RTCPeerConnection.prototype.setRemoteDescription = function(s) {
    if (s && s.sdp && s.sdp.indexOf(`
a=extmap-allow-mixed`) !== -1) {
      const c = s.sdp.split(`
`).filter((u) => u.trim() !== "a=extmap-allow-mixed").join(`
`);
      a.RTCSessionDescription && s instanceof a.RTCSessionDescription ? arguments[0] = new a.RTCSessionDescription({
        type: s.type,
        sdp: c
      }) : s.sdp = c;
    }
    return t.apply(this, arguments);
  };
}
function Rl(a, e) {
  if (!(a.RTCPeerConnection && a.RTCPeerConnection.prototype))
    return;
  const t = a.RTCPeerConnection.prototype.addIceCandidate;
  !t || t.length === 0 || (a.RTCPeerConnection.prototype.addIceCandidate = function() {
    return arguments[0] ? (e.browser === "chrome" && e.version < 78 || e.browser === "firefox" && e.version < 68 || e.browser === "safari") && arguments[0] && arguments[0].candidate === "" ? Promise.resolve() : t.apply(this, arguments) : (arguments[1] && arguments[1].apply(null), Promise.resolve());
  });
}
function wl(a, e) {
  if (!(a.RTCPeerConnection && a.RTCPeerConnection.prototype))
    return;
  const t = a.RTCPeerConnection.prototype.setLocalDescription;
  !t || t.length === 0 || (a.RTCPeerConnection.prototype.setLocalDescription = function() {
    let s = arguments[0] || {};
    if (typeof s != "object" || s.type && s.sdp)
      return t.apply(this, arguments);
    if (s = {
      type: s.type,
      sdp: s.sdp
    }, !s.type)
      switch (this.signalingState) {
        case "stable":
        case "have-local-offer":
        case "have-remote-pranswer":
          s.type = "offer";
          break;
        default:
          s.type = "answer";
          break;
      }
    return s.sdp || s.type !== "offer" && s.type !== "answer" ? t.apply(this, [s]) : (s.type === "offer" ? this.createOffer : this.createAnswer).apply(this).then((u) => t.apply(this, [u]));
  });
}
var B1 = /* @__PURE__ */ Object.freeze({ __proto__: null, removeExtmapAllowMixed: Ah, shimAddIceCandidateNullOrEmpty: Rl, shimConnectionState: Mh, shimMaxMessageSize: El, shimParameterlessSetLocalDescription: wl, shimRTCIceCandidate: Cl, shimRTCIceCandidateRelayProtocol: wh, shimSendThrowTypeError: _l });
function z1() {
  let a = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, e = a.window, t = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {
    shimChrome: !0,
    shimFirefox: !0,
    shimSafari: !0
  };
  const i = fm, s = N1(e), c = {
    browserDetails: s,
    commonShim: B1,
    extractVersion: To,
    disableLog: P1,
    disableWarnings: x1,
    // Expose sdp as a convenience. For production apps include directly.
    sdp: U1
  };
  switch (s.browser) {
    case "chrome":
      if (!_b || !_h || !t.shimChrome)
        return i("Chrome shim is not included in this adapter release."), c;
      if (s.version === null)
        return i("Chrome shim can not determine version, not shimming."), c;
      i("adapter.js shimming chrome."), c.browserShim = _b, Rl(e, s), wl(e), qT(e, s), HT(e), _h(e, s), VT(e, s), JT(e, s), GT(e), KT(e, s), QT(e, s), Cl(e), wh(e), Mh(e), El(e, s), _l(e, s), Ah(e, s);
      break;
    case "firefox":
      if (!Rb || !Rh || !t.shimFirefox)
        return i("Firefox shim is not included in this adapter release."), c;
      i("adapter.js shimming firefox."), c.browserShim = Rb, Rl(e, s), wl(e), XT(e, s), Rh(e, s), WT(e, s), ZT(e), tS(e), $T(e), eS(e), nS(e), iS(e, s), aS(e, s), rS(e, s), sS(e, s), Cl(e), Mh(e), El(e, s), _l(e, s);
      break;
    case "safari":
      if (!wb || !t.shimSafari)
        return i("Safari shim is not included in this adapter release."), c;
      i("adapter.js shimming safari."), c.browserShim = wb, Rl(e, s), wl(e), fS(e), mS(e), lS(e), oS(e), cS(e), hS(e), uS(e), pS(e), Cl(e), wh(e), El(e, s), _l(e, s), Ah(e, s);
      break;
    default:
      i("Unsupported browser!");
      break;
  }
  return c;
}
z1({
  window: typeof window > "u" ? void 0 : window
});
var Oh, vS;
class Gt extends (vS = Promise) {
  // eslint-disable-next-line @typescript-eslint/no-useless-constructor
  constructor(e) {
    super(e);
  }
  catch(e) {
    return super.catch(e);
  }
  static reject(e) {
    return super.reject(e);
  }
  static all(e) {
    return super.all(e);
  }
  static race(e) {
    return super.race(e);
  }
}
Oh = Gt;
Gt.resolve = (a) => Reflect.get(vS, "resolve", Oh).call(Oh, a);
const F1 = /version\/(\d+(\.?_?\d+)+)/i;
let Jf;
function Kt(a) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0;
  if (typeof navigator > "u")
    return;
  const t = navigator.userAgent.toLowerCase();
  if (Jf === void 0 || e) {
    const i = j1.find((s) => s.test.test(t));
    Jf = i?.describe(t);
  }
  return Jf;
}
const j1 = [
  {
    test: /firefox|iceweasel|fxios/i,
    describe(a) {
      return {
        name: "Firefox",
        version: Ml(/(?:firefox|iceweasel|fxios)[\s/](\d+(\.?_?\d+)+)/i, a),
        os: a.toLowerCase().includes("fxios") ? "iOS" : void 0,
        osVersion: Qf(a)
      };
    }
  },
  {
    test: /chrom|crios|crmo/i,
    describe(a) {
      return {
        name: "Chrome",
        version: Ml(/(?:chrome|chromium|crios|crmo)\/(\d+(\.?_?\d+)+)/i, a),
        os: a.toLowerCase().includes("crios") ? "iOS" : void 0,
        osVersion: Qf(a)
      };
    }
  },
  /* Safari */
  {
    test: /safari|applewebkit/i,
    describe(a) {
      return {
        name: "Safari",
        version: Ml(F1, a),
        os: a.includes("mobile/") ? "iOS" : "macOS",
        osVersion: Qf(a)
      };
    }
  }
];
function Ml(a, e) {
  let t = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 1;
  const i = e.match(a);
  return i && i.length >= t && i[t] || "";
}
function Qf(a) {
  return a.includes("mac os") ? Ml(/\(.+?(\d+_\d+(:?_\d+)?)/, a, 1).replace(/_/g, ".") : void 0;
}
var q1 = "2.20.1";
const H1 = q1, V1 = 17, yS = 0, zl = 1, G1 = zl;
class Bi extends Error {
  constructor(e, t, i) {
    super(t || "an error has occurred"), this.name = "LiveKitError", this.code = e, typeof i?.cause < "u" && (this.cause = i?.cause);
  }
}
class Nn extends Bi {
}
var We;
(function(a) {
  a[a.NotAllowed = 0] = "NotAllowed", a[a.ServerUnreachable = 1] = "ServerUnreachable", a[a.InternalError = 2] = "InternalError", a[a.Cancelled = 3] = "Cancelled", a[a.LeaveRequest = 4] = "LeaveRequest", a[a.Timeout = 5] = "Timeout", a[a.WebSocket = 6] = "WebSocket", a[a.ServiceNotFound = 7] = "ServiceNotFound";
})(We || (We = {}));
class ye extends Nn {
  constructor(e, t, i, s) {
    super(1, e), this.name = "ConnectionError", this.status = i, this.reason = t, this.context = s, this.reasonName = We[t];
  }
  static notAllowed(e, t, i) {
    return new ye(e, We.NotAllowed, t, i);
  }
  static timeout(e) {
    return new ye(e, We.Timeout);
  }
  static leaveRequest(e, t) {
    return new ye(e, We.LeaveRequest, void 0, t);
  }
  static internal(e, t) {
    return new ye(e, We.InternalError, void 0, t);
  }
  static cancelled(e) {
    return new ye(e, We.Cancelled);
  }
  static serverUnreachable(e, t) {
    return new ye(e, We.ServerUnreachable, t);
  }
  static websocket(e, t, i) {
    return new ye(e, We.WebSocket, t, i);
  }
  static serviceNotFound(e, t) {
    return new ye(e, We.ServiceNotFound, void 0, t);
  }
}
class mm extends Bi {
  constructor(e) {
    super(21, e ?? "device is unsupported"), this.name = "DeviceUnsupportedError";
  }
}
class Ni extends Bi {
  constructor(e) {
    super(20, e ?? "track is invalid"), this.name = "TrackInvalidError";
  }
}
class K1 extends Bi {
  constructor(e) {
    super(10, e ?? "unsupported server"), this.name = "UnsupportedServer";
  }
}
class rt extends Bi {
  constructor(e) {
    super(12, e ?? "unexpected connection state"), this.name = "UnexpectedConnectionState";
  }
}
class er extends Bi {
  constructor(e) {
    super(13, e ?? "unable to negotiate"), this.name = "NegotiationError";
  }
}
class Y1 extends Bi {
  constructor(e) {
    super(14, e ?? "unable to publish data"), this.name = "PublishDataError";
  }
}
class Ab extends Bi {
  constructor(e, t) {
    super(15, e), this.name = "PublishTrackError", this.status = t;
  }
}
class Ob extends Nn {
  constructor(e, t) {
    super(15, e), this.name = "SignalRequestError", this.reason = t, this.reasonName = typeof t == "string" ? t : Ja[t];
  }
}
var Ht;
(function(a) {
  a[a.AlreadyOpened = 0] = "AlreadyOpened", a[a.AbnormalEnd = 1] = "AbnormalEnd", a[a.DecodeFailed = 2] = "DecodeFailed", a[a.LengthExceeded = 3] = "LengthExceeded", a[a.Incomplete = 4] = "Incomplete", a[a.HandlerAlreadyRegistered = 7] = "HandlerAlreadyRegistered", a[a.EncryptionTypeMismatch = 8] = "EncryptionTypeMismatch";
})(Ht || (Ht = {}));
class Dn extends Nn {
  constructor(e, t) {
    super(16, e), this.name = "DataStreamError", this.reason = t, this.reasonName = Ht[t];
  }
}
class Jr extends Bi {
  constructor(e) {
    super(18, e), this.name = "SignalReconnectError";
  }
}
var Fl;
(function(a) {
  a.PermissionDenied = "PermissionDenied", a.NotFound = "NotFound", a.DeviceInUse = "DeviceInUse", a.Other = "Other";
})(Fl || (Fl = {}));
(function(a) {
  function e(t) {
    if (t && "name" in t)
      return t.name === "NotFoundError" || t.name === "DevicesNotFoundError" ? a.NotFound : t.name === "NotAllowedError" || t.name === "PermissionDeniedError" ? a.PermissionDenied : t.name === "NotReadableError" || t.name === "TrackStartError" ? a.DeviceInUse : a.Other;
  }
  a.getFailure = e;
})(Fl || (Fl = {}));
class bt {
}
bt.setTimeout = function() {
  return setTimeout(...arguments);
};
bt.setInterval = // eslint-disable-next-line @typescript-eslint/no-implied-eval
function() {
  return setInterval(...arguments);
};
bt.clearTimeout = function() {
  return clearTimeout(...arguments);
};
bt.clearInterval = function() {
  return clearInterval(...arguments);
};
var j;
(function(a) {
  a.Connected = "connected", a.Reconnecting = "reconnecting", a.SignalReconnecting = "signalReconnecting", a.Reconnected = "reconnected", a.Disconnected = "disconnected", a.ConnectionStateChanged = "connectionStateChanged", a.Moved = "moved", a.MediaDevicesChanged = "mediaDevicesChanged", a.ParticipantConnected = "participantConnected", a.ParticipantDisconnected = "participantDisconnected", a.TrackPublished = "trackPublished", a.TrackSubscribed = "trackSubscribed", a.TrackSubscriptionFailed = "trackSubscriptionFailed", a.TrackUnpublished = "trackUnpublished", a.TrackUnsubscribed = "trackUnsubscribed", a.TrackMuted = "trackMuted", a.TrackUnmuted = "trackUnmuted", a.LocalTrackPublished = "localTrackPublished", a.LocalTrackUnpublished = "localTrackUnpublished", a.LocalAudioSilenceDetected = "localAudioSilenceDetected", a.ActiveSpeakersChanged = "activeSpeakersChanged", a.ParticipantMetadataChanged = "participantMetadataChanged", a.ParticipantNameChanged = "participantNameChanged", a.ParticipantAttributesChanged = "participantAttributesChanged", a.ParticipantActive = "participantActive", a.RoomMetadataChanged = "roomMetadataChanged", a.DataReceived = "dataReceived", a.SipDTMFReceived = "sipDTMFReceived", a.TranscriptionReceived = "transcriptionReceived", a.ConnectionQualityChanged = "connectionQualityChanged", a.TrackStreamStateChanged = "trackStreamStateChanged", a.TrackSubscriptionPermissionChanged = "trackSubscriptionPermissionChanged", a.TrackSubscriptionStatusChanged = "trackSubscriptionStatusChanged", a.AudioPlaybackStatusChanged = "audioPlaybackChanged", a.VideoPlaybackStatusChanged = "videoPlaybackChanged", a.MediaDevicesError = "mediaDevicesError", a.ParticipantPermissionsChanged = "participantPermissionsChanged", a.SignalConnected = "signalConnected", a.RecordingStatusChanged = "recordingStatusChanged", a.ParticipantEncryptionStatusChanged = "participantEncryptionStatusChanged", a.EncryptionError = "encryptionError", a.DCBufferStatusChanged = "dcBufferStatusChanged", a.ActiveDeviceChanged = "activeDeviceChanged", a.ChatMessage = "chatMessage", a.LocalTrackSubscribed = "localTrackSubscribed", a.MetricsReceived = "metricsReceived", a.DataTrackPublished = "dataTrackPublished", a.DataTrackUnpublished = "dataTrackUnpublished", a.LocalDataTrackPublished = "localDataTrackPublished", a.LocalDataTrackUnpublished = "localDataTrackUnpublished";
})(j || (j = {}));
var X;
(function(a) {
  a.TrackPublished = "trackPublished", a.TrackSubscribed = "trackSubscribed", a.TrackSubscriptionFailed = "trackSubscriptionFailed", a.TrackUnpublished = "trackUnpublished", a.TrackUnsubscribed = "trackUnsubscribed", a.TrackMuted = "trackMuted", a.TrackUnmuted = "trackUnmuted", a.LocalTrackPublished = "localTrackPublished", a.LocalTrackUnpublished = "localTrackUnpublished", a.LocalTrackCpuConstrained = "localTrackCpuConstrained", a.LocalSenderCreated = "localSenderCreated", a.ParticipantMetadataChanged = "participantMetadataChanged", a.ParticipantNameChanged = "participantNameChanged", a.DataReceived = "dataReceived", a.SipDTMFReceived = "sipDTMFReceived", a.TranscriptionReceived = "transcriptionReceived", a.IsSpeakingChanged = "isSpeakingChanged", a.ConnectionQualityChanged = "connectionQualityChanged", a.TrackStreamStateChanged = "trackStreamStateChanged", a.TrackSubscriptionPermissionChanged = "trackSubscriptionPermissionChanged", a.TrackSubscriptionStatusChanged = "trackSubscriptionStatusChanged", a.TrackCpuConstrained = "trackCpuConstrained", a.MediaDevicesError = "mediaDevicesError", a.AudioStreamAcquired = "audioStreamAcquired", a.ParticipantPermissionsChanged = "participantPermissionsChanged", a.PCTrackAdded = "pcTrackAdded", a.AttributesChanged = "attributesChanged", a.LocalTrackSubscribed = "localTrackSubscribed", a.ChatMessage = "chatMessage", a.Active = "active";
})(X || (X = {}));
var ae;
(function(a) {
  a.TransportsCreated = "transportsCreated", a.Connected = "connected", a.Disconnected = "disconnected", a.Resuming = "resuming", a.Resumed = "resumed", a.Restarting = "restarting", a.Restarted = "restarted", a.SignalResumed = "signalResumed", a.SignalRestarted = "signalRestarted", a.Closing = "closing", a.MediaTrackAdded = "mediaTrackAdded", a.ActiveSpeakersUpdate = "activeSpeakersUpdate", a.DataPacketReceived = "dataPacketReceived", a.RTPVideoMapUpdate = "rtpVideoMapUpdate", a.DCBufferStatusChanged = "dcBufferStatusChanged", a.ParticipantUpdate = "participantUpdate", a.RoomUpdate = "roomUpdate", a.SpeakersChanged = "speakersChanged", a.StreamStateChanged = "streamStateChanged", a.ConnectionQualityUpdate = "connectionQualityUpdate", a.SubscriptionError = "subscriptionError", a.SubscriptionPermissionUpdate = "subscriptionPermissionUpdate", a.RemoteMute = "remoteMute", a.SubscribedQualityUpdate = "subscribedQualityUpdate", a.LocalTrackUnpublished = "localTrackUnpublished", a.LocalTrackSubscribed = "localTrackSubscribed", a.Offline = "offline", a.SignalRequestResponse = "signalRequestResponse", a.SignalConnected = "signalConnected", a.RoomMoved = "roomMoved", a.PublishDataTrackResponse = "publishDataTrackResponse", a.UnPublishDataTrackResponse = "unPublishDataTrackResponse", a.DataTrackSubscriberHandles = "dataTrackSubscriberHandles", a.DataTrackPacketReceived = "dataTrackPacketReceived", a.Joined = "joined", a.TokenRefreshed = "tokenRefreshed", a.ServerRegionsReported = "serverRegionsReported";
})(ae || (ae = {}));
var re;
(function(a) {
  a.Message = "message", a.Muted = "muted", a.Unmuted = "unmuted", a.Restarted = "restarted", a.Ended = "ended", a.Subscribed = "subscribed", a.Unsubscribed = "unsubscribed", a.CpuConstrained = "cpuConstrained", a.UpdateSettings = "updateSettings", a.UpdateSubscription = "updateSubscription", a.AudioPlaybackStarted = "audioPlaybackStarted", a.AudioPlaybackFailed = "audioPlaybackFailed", a.AudioSilenceDetected = "audioSilenceDetected", a.VisibilityChanged = "visibilityChanged", a.VideoDimensionsChanged = "videoDimensionsChanged", a.VideoPlaybackStarted = "videoPlaybackStarted", a.VideoPlaybackFailed = "videoPlaybackFailed", a.ElementAttached = "elementAttached", a.ElementDetached = "elementDetached", a.UpstreamPaused = "upstreamPaused", a.UpstreamResumed = "upstreamResumed", a.SubscriptionPermissionChanged = "subscriptionPermissionChanged", a.SubscriptionStatusChanged = "subscriptionStatusChanged", a.SubscriptionFailed = "subscriptionFailed", a.TrackProcessorUpdate = "trackProcessorUpdate", a.AudioTrackFeatureUpdate = "audioTrackFeatureUpdate", a.TranscriptionReceived = "transcriptionReceived", a.TimeSyncUpdate = "timeSyncUpdate", a.PreConnectBufferFlushed = "preConnectBufferFlushed";
})(re || (re = {}));
function J1(a) {
  return typeof a > "u" ? a : typeof structuredClone == "function" ? typeof a == "object" && a !== null ? structuredClone(Object.assign({}, a)) : structuredClone(a) : JSON.parse(JSON.stringify(a));
}
class Qe {
  constructor(e, t, i, s, c) {
    if (typeof e == "object")
      this.width = e.width, this.height = e.height, this.aspectRatio = e.aspectRatio, this.encoding = {
        maxBitrate: e.maxBitrate,
        maxFramerate: e.maxFramerate,
        priority: e.priority
      };
    else if (t !== void 0 && i !== void 0)
      this.width = e, this.height = t, this.aspectRatio = e / t, this.encoding = {
        maxBitrate: i,
        maxFramerate: s,
        priority: c
      };
    else
      throw new TypeError("Unsupported options: provide at least width, height and maxBitrate");
  }
  get resolution() {
    return {
      width: this.width,
      height: this.height,
      frameRate: this.encoding.maxFramerate,
      aspectRatio: this.aspectRatio
    };
  }
}
const Q1 = ["vp8", "h264"], X1 = ["vp8", "h264", "vp9", "av1", "h265"];
function bS(a) {
  return !!Q1.find((e) => e === a);
}
const Z1 = bS;
var Db;
(function(a) {
  a[a.PREFER_REGRESSION = 0] = "PREFER_REGRESSION", a[a.SIMULCAST = 1] = "SIMULCAST", a[a.REGRESSION = 2] = "REGRESSION";
})(Db || (Db = {}));
var Dh;
(function(a) {
  a.telephone = {
    maxBitrate: 12e3
  }, a.speech = {
    maxBitrate: 24e3
  }, a.music = {
    maxBitrate: 48e3
  }, a.musicStereo = {
    maxBitrate: 64e3
  }, a.musicHighQuality = {
    maxBitrate: 96e3
  }, a.musicHighQualityStereo = {
    maxBitrate: 128e3
  };
})(Dh || (Dh = {}));
const Do = {
  h90: new Qe(160, 90, 9e4, 20),
  h180: new Qe(320, 180, 16e4, 20),
  h216: new Qe(384, 216, 18e4, 20),
  h360: new Qe(640, 360, 45e4, 20),
  h540: new Qe(960, 540, 8e5, 25),
  h720: new Qe(1280, 720, 17e5, 30),
  h1080: new Qe(1920, 1080, 3e6, 30),
  h1440: new Qe(2560, 1440, 5e6, 30),
  h2160: new Qe(3840, 2160, 8e6, 30)
}, Ph = {
  h120: new Qe(160, 120, 7e4, 20),
  h180: new Qe(240, 180, 125e3, 20),
  h240: new Qe(320, 240, 14e4, 20),
  h360: new Qe(480, 360, 33e4, 20),
  h480: new Qe(640, 480, 5e5, 20),
  h540: new Qe(720, 540, 6e5, 25),
  h720: new Qe(960, 720, 13e5, 30),
  h1080: new Qe(1440, 1080, 23e5, 30),
  h1440: new Qe(1920, 1440, 38e5, 30)
}, pm = {
  h360fps3: new Qe(640, 360, 2e5, 3, "medium"),
  h360fps15: new Qe(640, 360, 4e5, 15, "medium"),
  h720fps5: new Qe(1280, 720, 8e5, 5, "medium"),
  h720fps15: new Qe(1280, 720, 15e5, 15, "medium"),
  h720fps30: new Qe(1280, 720, 2e6, 30, "medium"),
  h1080fps15: new Qe(1920, 1080, 25e5, 15, "medium"),
  h1080fps30: new Qe(1920, 1080, 5e6, 30, "medium"),
  // original resolution, without resizing
  original: new Qe(0, 0, 7e6, 30, "medium")
};
function TS(a, e, t) {
  var i, s, c, u;
  const d = CS(a ?? {}), h = d.optionsWithoutProcessor, f = d.audioProcessor, p = d.videoProcessor, v = e?.processor, y = t?.processor, T = h ?? {};
  return T.audio === !0 && (T.audio = {}), T.video === !0 && (T.video = {}), T.audio && (xh(T.audio, e), (i = (c = T.audio).deviceId) !== null && i !== void 0 || (c.deviceId = {
    ideal: "default"
  }), (f || v) && (T.audio.processor = f ?? v)), T.video && (xh(T.video, t), (s = (u = T.video).deviceId) !== null && s !== void 0 || (u.deviceId = {
    ideal: "default"
  }), (p || y) && (T.video.processor = p ?? y)), T;
}
function xh(a, e) {
  return Object.keys(e).forEach((t) => {
    a[t] === void 0 && (a[t] = e[t]);
  }), a;
}
function gm(a) {
  var e, t, i, s;
  const c = {};
  if (a.video)
    if (typeof a.video == "object") {
      const u = {}, d = u, h = a.video;
      Object.keys(h).forEach((f) => {
        switch (f) {
          case "resolution":
            xh(d, h.resolution);
            break;
          default:
            d[f] = h[f];
        }
      }), c.video = u, (e = (i = c.video).deviceId) !== null && e !== void 0 || (i.deviceId = {
        ideal: "default"
      });
    } else
      c.video = a.video ? {
        deviceId: {
          ideal: "default"
        }
      } : !1;
  else
    c.video = !1;
  return a.audio ? typeof a.audio == "object" ? (c.audio = a.audio, (t = (s = c.audio).deviceId) !== null && t !== void 0 || (s.deviceId = {
    ideal: "default"
  })) : c.audio = {
    deviceId: {
      ideal: "default"
    }
  } : c.audio = !1, c;
}
function SS(a) {
  return R(this, arguments, void 0, function(e) {
    let t = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 200;
    return (function* () {
      const i = kS();
      if (i) {
        const s = i.createAnalyser();
        s.fftSize = 2048;
        const c = s.frequencyBinCount, u = new Uint8Array(c);
        i.createMediaStreamSource(new MediaStream([e.mediaStreamTrack])).connect(s), yield zt(t), s.getByteTimeDomainData(u);
        const h = u.some((f) => f !== 128 && f !== 0);
        return i.close(), !h;
      }
      return !1;
    })();
  });
}
function kS() {
  var a;
  const e = (
    // @ts-ignore
    typeof window < "u" && (window.AudioContext || window.webkitAudioContext)
  );
  if (e) {
    const t = new e({
      latencyHint: "interactive"
    });
    if (t.state === "suspended" && typeof window < "u" && (!((a = window.document) === null || a === void 0) && a.body)) {
      const i = () => R(this, void 0, void 0, function* () {
        var s;
        try {
          t.state === "suspended" && (yield t.resume());
        } catch (c) {
          console.warn("Error trying to auto-resume audio context", c);
        } finally {
          (s = window.document.body) === null || s === void 0 || s.removeEventListener("click", i);
        }
      });
      t.addEventListener("statechange", () => {
        var s;
        t.state === "closed" && ((s = window.document.body) === null || s === void 0 || s.removeEventListener("click", i));
      }), window.document.body.addEventListener("click", i);
    }
    return t;
  }
}
function W1(a) {
  return a === "audioinput" ? H.Source.Microphone : a === "videoinput" ? H.Source.Camera : H.Source.Unknown;
}
function Nh(a) {
  return a === H.Source.Microphone ? "audioinput" : a === H.Source.Camera ? "videoinput" : void 0;
}
function $1(a) {
  var e, t;
  let i = (e = a.video) !== null && e !== void 0 ? e : !0;
  return a.resolution && a.resolution.width > 0 && a.resolution.height > 0 && (i = typeof i == "boolean" ? {} : i, sr() ? i = Object.assign(Object.assign({}, i), {
    width: {
      max: a.resolution.width
    },
    height: {
      max: a.resolution.height
    },
    frameRate: a.resolution.frameRate
  }) : i = Object.assign(Object.assign({}, i), {
    width: {
      ideal: a.resolution.width
    },
    height: {
      ideal: a.resolution.height
    },
    frameRate: a.resolution.frameRate
  })), {
    audio: (t = a.audio) !== null && t !== void 0 ? t : !1,
    video: i,
    // @ts-expect-error support for experimental display media features
    controller: a.controller,
    selfBrowserSurface: a.selfBrowserSurface,
    surfaceSwitching: a.surfaceSwitching,
    systemAudio: a.systemAudio,
    preferCurrentTab: a.preferCurrentTab
  };
}
function Co(a) {
  return a.split("/")[1].toLowerCase();
}
function ew(a) {
  const e = [];
  return a.forEach((t) => {
    t.track !== void 0 && e.push(new om({
      cid: t.track.mediaStreamID,
      track: t.trackInfo
    }));
  }), e;
}
function Oe(a) {
  return "mediaStreamTrack" in a ? {
    trackID: a.sid,
    source: a.source,
    muted: a.isMuted,
    enabled: a.mediaStreamTrack.enabled,
    kind: a.kind,
    streamID: a.mediaStreamID,
    streamTrackID: a.mediaStreamTrack.id
  } : {
    trackID: a.trackSid,
    enabled: a.isEnabled,
    muted: a.isMuted,
    trackInfo: Object.assign({
      mimeType: a.mimeType,
      name: a.trackName,
      encrypted: a.isEncrypted,
      kind: a.kind,
      source: a.source
    }, a.track ? Oe(a.track) : {})
  };
}
function tw() {
  return typeof RTCRtpReceiver < "u" && typeof RTCRtpReceiver.prototype.getSynchronizationSources == "function";
}
function nw(a, e) {
  var t;
  a === void 0 && (a = {}), e === void 0 && (e = {});
  const i = [...Object.keys(e), ...Object.keys(a)], s = {};
  for (const c of i)
    a[c] !== e[c] && (s[c] = (t = e[c]) !== null && t !== void 0 ? t : "");
  return s;
}
function CS(a) {
  const e = Object.assign({}, a);
  let t, i;
  return typeof e.audio == "object" && e.audio.processor && (t = e.audio.processor, e.audio = Object.assign(Object.assign({}, e.audio), {
    processor: void 0
  })), typeof e.video == "object" && e.video.processor && (i = e.video.processor, e.video = Object.assign(Object.assign({}, e.video), {
    processor: void 0
  })), {
    audioProcessor: t,
    videoProcessor: i,
    optionsWithoutProcessor: J1(e)
  };
}
function iw(a) {
  switch (a) {
    case yt.CAMERA:
      return H.Source.Camera;
    case yt.MICROPHONE:
      return H.Source.Microphone;
    case yt.SCREEN_SHARE:
      return H.Source.ScreenShare;
    case yt.SCREEN_SHARE_AUDIO:
      return H.Source.ScreenShareAudio;
    default:
      return H.Source.Unknown;
  }
}
function Pb(a, e) {
  return a.width * a.height < e.width * e.height;
}
function aw(a, e) {
  var t;
  return (t = a.layers) === null || t === void 0 ? void 0 : t.find((i) => i.quality === e);
}
const rw = 5e3, po = [];
var yn;
(function(a) {
  a[a.LOW = 0] = "LOW", a[a.MEDIUM = 1] = "MEDIUM", a[a.HIGH = 2] = "HIGH";
})(yn || (yn = {}));
class H extends sn.EventEmitter {
  /**
   * indicates current state of stream, it'll indicate `paused` if the track
   * has been paused by congestion controller
   */
  get streamState() {
    return this._streamState;
  }
  /** @internal */
  setStreamState(e) {
    this._streamState = e;
  }
  constructor(e, t) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
    var s;
    super(), this.attachedElements = [], this.isMuted = !1, this._streamState = H.StreamState.Active, this.isInBackground = !1, this._currentBitrate = 0, this.log = Ee, this.appVisibilityChangedListener = () => {
      this.backgroundTimeout && clearTimeout(this.backgroundTimeout), document.visibilityState === "hidden" ? this.backgroundTimeout = setTimeout(() => this.handleAppVisibilityChanged(), rw) : this.handleAppVisibilityChanged();
    }, this.log = rn((s = i.loggerName) !== null && s !== void 0 ? s : Mt.Track), this.loggerContextCb = i.loggerContextCb, this.setMaxListeners(100), this.kind = t, this._mediaStreamTrack = e, this._mediaStreamID = e.id, this.source = H.Source.Unknown;
  }
  get logContext() {
    var e;
    return Object.assign(Object.assign({}, (e = this.loggerContextCb) === null || e === void 0 ? void 0 : e.call(this)), Oe(this));
  }
  /** current receive bits per second */
  get currentBitrate() {
    return this._currentBitrate;
  }
  get mediaStreamTrack() {
    return this._mediaStreamTrack;
  }
  /**
   * @internal
   * used for keep mediaStream's first id, since it's id might change
   * if we disable/enable a track
   */
  get mediaStreamID() {
    return this._mediaStreamID;
  }
  attach(e) {
    let t = "audio";
    this.kind === H.Kind.Video && (t = "video"), this.attachedElements.length === 0 && this.kind === H.Kind.Video && this.addAppVisibilityListener(), e || (t === "audio" && (po.forEach((c) => {
      c.parentElement === null && !e && (e = c);
    }), e && po.splice(po.indexOf(e), 1)), e || (e = document.createElement(t))), this.attachedElements.includes(e) || this.attachedElements.push(e), Zr(this.mediaStreamTrack, e);
    const i = e.srcObject.getTracks(), s = i.some((c) => c.kind === "audio");
    return e.play().then(() => {
      this.emit(s ? re.AudioPlaybackStarted : re.VideoPlaybackStarted);
    }).catch((c) => {
      c.name === "NotAllowedError" ? this.emit(s ? re.AudioPlaybackFailed : re.VideoPlaybackFailed, c) : c.name === "AbortError" ? Ee.debug("".concat(s ? "audio" : "video", " playback aborted, likely due to new play request")) : Ee.warn("could not playback ".concat(s ? "audio" : "video"), c), s && e && i.some((u) => u.kind === "video") && c.name === "NotAllowedError" && (e.muted = !0, e.play().catch(() => {
      }));
    }), this.emit(re.ElementAttached, e), e;
  }
  detach(e) {
    try {
      if (e) {
        ns(this.mediaStreamTrack, e);
        const i = this.attachedElements.indexOf(e);
        return i >= 0 && (this.attachedElements.splice(i, 1), this.recycleElement(e), this.emit(re.ElementDetached, e)), e;
      }
      const t = [];
      return this.attachedElements.forEach((i) => {
        ns(this.mediaStreamTrack, i), t.push(i), this.recycleElement(i), this.emit(re.ElementDetached, i);
      }), this.attachedElements = [], t;
    } finally {
      this.attachedElements.length === 0 && this.removeAppVisibilityListener();
    }
  }
  stop() {
    this.stopMonitor(), this._mediaStreamTrack.stop();
  }
  enable() {
    this._mediaStreamTrack.enabled = !0;
  }
  disable() {
    this._mediaStreamTrack.enabled = !1;
  }
  /* @internal */
  stopMonitor() {
    this.monitorInterval && clearInterval(this.monitorInterval), this.timeSyncHandle && cancelAnimationFrame(this.timeSyncHandle);
  }
  /** @internal */
  updateLoggerOptions(e) {
    e.loggerName && (this.log = rn(e.loggerName)), e.loggerContextCb && (this.loggerContextCb = e.loggerContextCb);
  }
  recycleElement(e) {
    if (e instanceof HTMLAudioElement) {
      let t = !0;
      e.pause(), po.forEach((i) => {
        i.parentElement || (t = !1);
      }), t && po.push(e);
    }
  }
  handleAppVisibilityChanged() {
    return R(this, void 0, void 0, function* () {
      this.isInBackground = document.visibilityState === "hidden", !this.isInBackground && this.kind === H.Kind.Video && setTimeout(() => this.attachedElements.forEach((e) => e.play().catch(() => {
      })), 0);
    });
  }
  addAppVisibilityListener() {
    an() ? (this.isInBackground = document.visibilityState === "hidden", document.addEventListener("visibilitychange", this.appVisibilityChangedListener)) : this.isInBackground = !1;
  }
  removeAppVisibilityListener() {
    an() && document.removeEventListener("visibilitychange", this.appVisibilityChangedListener);
  }
}
function Zr(a, e) {
  let t;
  e.srcObject instanceof MediaStream ? t = e.srcObject : t = new MediaStream();
  let i;
  a.kind === "audio" ? i = t.getAudioTracks() : i = t.getVideoTracks(), i.includes(a) || (i.forEach((s) => {
    t.removeTrack(s);
  }), t.addTrack(a)), (!sr() || !(e instanceof HTMLVideoElement)) && (e.autoplay = !0), e.muted = t.getAudioTracks().length === 0, e instanceof HTMLVideoElement && (e.playsInline = !0), e.srcObject !== t && (e.srcObject = t, (sr() || rr()) && e instanceof HTMLVideoElement && setTimeout(() => {
    e.srcObject = t, e.play().catch(() => {
    });
  }, 0));
}
function ns(a, e) {
  if (e.srcObject instanceof MediaStream) {
    const t = e.srcObject;
    t.removeTrack(a), t.getTracks().length > 0 ? e.srcObject = t : e.srcObject = null;
  }
}
(function(a) {
  let e;
  (function(f) {
    f.Audio = "audio", f.Video = "video", f.Unknown = "unknown";
  })(e = a.Kind || (a.Kind = {}));
  let t;
  (function(f) {
    f.Camera = "camera", f.Microphone = "microphone", f.ScreenShare = "screen_share", f.ScreenShareAudio = "screen_share_audio", f.Unknown = "unknown";
  })(t = a.Source || (a.Source = {}));
  let i;
  (function(f) {
    f.Active = "active", f.Paused = "paused", f.Unknown = "unknown";
  })(i = a.StreamState || (a.StreamState = {}));
  function s(f) {
    switch (f) {
      case e.Audio:
        return jn.AUDIO;
      case e.Video:
        return jn.VIDEO;
      default:
        return jn.DATA;
    }
  }
  a.kindToProto = s;
  function c(f) {
    switch (f) {
      case jn.AUDIO:
        return e.Audio;
      case jn.VIDEO:
        return e.Video;
      default:
        return e.Unknown;
    }
  }
  a.kindFromProto = c;
  function u(f) {
    switch (f) {
      case t.Camera:
        return yt.CAMERA;
      case t.Microphone:
        return yt.MICROPHONE;
      case t.ScreenShare:
        return yt.SCREEN_SHARE;
      case t.ScreenShareAudio:
        return yt.SCREEN_SHARE_AUDIO;
      default:
        return yt.UNKNOWN;
    }
  }
  a.sourceToProto = u;
  function d(f) {
    switch (f) {
      case yt.CAMERA:
        return t.Camera;
      case yt.MICROPHONE:
        return t.Microphone;
      case yt.SCREEN_SHARE:
        return t.ScreenShare;
      case yt.SCREEN_SHARE_AUDIO:
        return t.ScreenShareAudio;
      default:
        return t.Unknown;
    }
  }
  a.sourceFromProto = d;
  function h(f) {
    switch (f) {
      case Th.ACTIVE:
        return i.Active;
      case Th.PAUSED:
        return i.Paused;
      default:
        return i.Unknown;
    }
  }
  a.streamStateFromProto = h;
})(H || (H = {}));
const sw = "|", xb = "https://aomediacodec.github.io/av1-rtp-spec/#dependency-descriptor-rtp-header-extension";
function ow(a) {
  const e = a.split(sw);
  return e.length > 1 ? [e[0], a.substr(e[0].length + 1)] : [a, ""];
}
function zt(a) {
  return new Gt((e) => bt.setTimeout(e, a));
}
function Ih() {
  return "addTransceiver" in RTCPeerConnection.prototype;
}
function Lh() {
  return "addTrack" in RTCPeerConnection.prototype;
}
function cw() {
  if (!("getCapabilities" in RTCRtpSender) || sr() || rr())
    return !1;
  const a = RTCRtpSender.getCapabilities("video");
  let e = !1;
  if (a) {
    for (const t of a.codecs)
      if (t.mimeType.toLowerCase() === "video/av1") {
        e = !0;
        break;
      }
  }
  return e;
}
function lw() {
  if (!("getCapabilities" in RTCRtpSender) || rr())
    return !1;
  if (sr()) {
    const t = Kt();
    if (t?.version && Xn(t.version, "16") < 0 || t?.os === "iOS" && t?.osVersion && Xn(t.osVersion, "16") < 0)
      return !1;
  }
  const a = RTCRtpSender.getCapabilities("video");
  let e = !1;
  if (a) {
    for (const t of a.codecs)
      if (t.mimeType.toLowerCase() === "video/vp9") {
        e = !0;
        break;
      }
  }
  return e;
}
function Vn(a) {
  return a === "av1" || a === "vp9";
}
function Uh(a) {
  return !document || Po() ? !1 : (a || (a = document.createElement("audio")), "setSinkId" in a);
}
function uw() {
  return typeof RTCPeerConnection > "u" ? !1 : Ih() || Lh();
}
function rr() {
  var a;
  return ((a = Kt()) === null || a === void 0 ? void 0 : a.name) === "Firefox";
}
function dw() {
  const a = Kt();
  return !!a && a.name === "Chrome" && a.os !== "iOS";
}
function Bh() {
  return typeof window < "u" && // @ts-ignore
  typeof window.RTCRtpScriptTransform < "u" && !dw();
}
function sr() {
  var a;
  return ((a = Kt()) === null || a === void 0 ? void 0 : a.name) === "Safari";
}
function Po() {
  const a = Kt();
  return a?.name === "Safari" || a?.os === "iOS";
}
function fw() {
  const a = Kt();
  return a?.name === "Safari" && a.version.startsWith("17.") || a?.os === "iOS" && !!a?.osVersion && Xn(a.osVersion, "17") >= 0;
}
function hw(a) {
  return a || (a = Kt()), a?.name === "Safari" && Xn(a.version, "18.3") > 0 || a?.os === "iOS" && !!a?.osVersion && Xn(a.osVersion, "18.3") > 0;
}
function ES() {
  var a, e;
  return an() ? (
    // @ts-expect-error `userAgentData` is not yet part of typescript
    (e = (a = navigator.userAgentData) === null || a === void 0 ? void 0 : a.mobile) !== null && e !== void 0 ? e : /Tablet|iPad|Mobile|Android|BlackBerry/.test(navigator.userAgent)
  ) : !1;
}
function mw() {
  const a = Kt(), e = "17.2";
  if (a)
    return a.name !== "Safari" && a.os !== "iOS" || a.os === "iOS" && a.osVersion && Xn(a.osVersion, e) >= 0 ? !0 : a.name === "Safari" && Xn(a.version, e) >= 0;
}
function an() {
  return typeof document < "u";
}
function Qn() {
  return navigator.product == "ReactNative";
}
function fs(a) {
  return a.hostname.endsWith(".livekit.cloud") || a.hostname.endsWith(".livekit.run");
}
function Xf(a) {
  return fs(a) ? a.hostname.split(".")[0] : null;
}
function _S() {
  if (global && global.LiveKitReactNativeGlobal)
    return global.LiveKitReactNativeGlobal;
}
function RS() {
  if (!Qn())
    return;
  let a = _S();
  if (a)
    return a.platform;
}
function Nb() {
  if (an())
    return window.devicePixelRatio;
  if (Qn()) {
    let a = _S();
    if (a)
      return a.devicePixelRatio;
  }
  return 1;
}
function Xn(a, e) {
  const t = a.split("."), i = e.split("."), s = Math.min(t.length, i.length);
  for (let c = 0; c < s; ++c) {
    const u = parseInt(t[c], 10), d = parseInt(i[c], 10);
    if (u > d) return 1;
    if (u < d) return -1;
    if (c === s - 1 && u === d) return 0;
  }
  return a === "" && e !== "" ? -1 : e === "" ? 1 : t.length == i.length ? 0 : t.length < i.length ? -1 : 1;
}
function pw(a) {
  for (const e of a)
    e.target.handleResize(e);
}
function gw(a) {
  for (const e of a)
    e.target.handleVisibilityChanged(e);
}
let Zf = null;
const Ib = () => (Zf || (Zf = new ResizeObserver(pw)), Zf);
let Wf = null;
const Lb = () => (Wf || (Wf = new IntersectionObserver(gw, {
  root: null,
  rootMargin: "0px"
})), Wf);
function vw(a) {
  var e;
  const t = new gT({
    capabilities: a,
    sdk: vT.JS,
    protocol: V1,
    clientProtocol: G1,
    version: H1
  });
  return Qn() && (t.os = (e = RS()) !== null && e !== void 0 ? e : ""), t;
}
function Ub() {
  let a = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 16, e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 16, t = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1, i = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : !1;
  const s = document.createElement("canvas");
  s.width = a, s.height = e;
  const c = s.getContext("2d");
  c?.fillRect(0, 0, s.width, s.height), i && c && (c.beginPath(), c.arc(a / 2, e / 2, 50, 0, Math.PI * 2, !0), c.closePath(), c.fillStyle = "grey", c.fill());
  const d = s.captureStream().getTracks(), h = ge(d, 1), f = h[0];
  if (!f)
    throw Error("Could not get empty media stream video track");
  return f.enabled = t, f;
}
let go;
function $f() {
  if (!go) {
    const t = new AudioContext(), i = t.createOscillator(), s = t.createGain();
    s.gain.setValueAtTime(0, 0);
    const c = t.createMediaStreamDestination();
    i.connect(s), s.connect(c), i.start();
    var a = c.stream.getAudioTracks(), e = ge(a, 1);
    if (go = e[0], !go)
      throw Error("Could not get empty media stream audio track");
    go.enabled = !1;
  }
  return go.clone();
}
class wt {
  get isResolved() {
    return this._isResolved;
  }
  constructor(e, t) {
    this._isResolved = !1, this.onFinally = t, this.promise = new Promise((i, s) => R(this, void 0, void 0, function* () {
      this.resolve = i, this.reject = s, e && (yield e(i, s));
    })).finally(() => {
      var i;
      this._isResolved = !0, (i = this.onFinally) === null || i === void 0 || i.call(this);
    });
  }
}
function yw(a) {
  return X1.includes(a);
}
function tr(a) {
  if (typeof a == "string" || typeof a == "number")
    return a;
  if (Array.isArray(a))
    return a[0];
  if (a.exact !== void 0)
    return Array.isArray(a.exact) ? a.exact[0] : a.exact;
  if (a.ideal !== void 0)
    return Array.isArray(a.ideal) ? a.ideal[0] : a.ideal;
  throw Error("could not unwrap constraint");
}
function bw(a) {
  return a.startsWith("http") ? a.replace(/^(http)/, "ws") : a;
}
function xo(a) {
  return a.startsWith("ws") ? a.replace(/^(ws)/, "http") : a;
}
function Tw(a, e) {
  return a.segments.map((t) => {
    let i = t.id, s = t.text, c = t.language, u = t.startTime, d = t.endTime, h = t.final;
    var f;
    const p = (f = e.get(i)) !== null && f !== void 0 ? f : Date.now(), v = Date.now();
    return h ? e.delete(i) : e.set(i, p), {
      id: i,
      text: s,
      startTime: Number.parseInt(u.toString()),
      endTime: Number.parseInt(d.toString()),
      final: h,
      language: c,
      firstReceivedTime: p,
      lastReceivedTime: v
    };
  });
}
function Sw(a) {
  const e = a.id, t = a.timestamp, i = a.message, s = a.editTimestamp;
  return {
    id: e,
    timestamp: Number.parseInt(t.toString()),
    editTimestamp: s ? Number.parseInt(s.toString()) : void 0,
    message: i
  };
}
function Bb(a) {
  switch (a.reason) {
    case We.LeaveRequest:
      return a.context;
    case We.Cancelled:
      return Yn.CLIENT_INITIATED;
    case We.NotAllowed:
      return Yn.USER_REJECTED;
    case We.ServerUnreachable:
      return Yn.JOIN_FAILURE;
    default:
      return Yn.UNKNOWN_REASON;
  }
}
function Al(a) {
  return a !== void 0 ? Number(a) : void 0;
}
function Ya(a) {
  return a !== void 0 ? BigInt(a) : void 0;
}
function is(a) {
  return !!a && !(a instanceof MediaStreamTrack) && a.isLocal;
}
function ci(a) {
  return !!a && a.kind == H.Kind.Audio;
}
function Li(a) {
  return !!a && a.kind == H.Kind.Video;
}
function ii(a) {
  return is(a) && Li(a);
}
function Ai(a) {
  return is(a) && ci(a);
}
function zh(a) {
  return !!a && !a.isLocal;
}
function kw(a) {
  return !!a && !a.isLocal;
}
function eh(a) {
  return zh(a) && Li(a);
}
function Cw(a) {
  return a.isLocal;
}
function Ew(a, e) {
  const t = [];
  let i = new TextEncoder().encode(a);
  for (; i.length > e; ) {
    let s = e;
    for (; s > 0; ) {
      const c = i[s];
      if (c !== void 0 && (c & 192) !== 128)
        break;
      s--;
    }
    t.push(i.slice(0, s)), i = i.slice(s);
  }
  return i.length > 0 && t.push(i), t;
}
function _w(a) {
  var e;
  const t = a.get("Cache-Control");
  if (t) {
    const i = (e = t.match(/(?:^|[,\s])max-age=(\d+)/)) === null || e === void 0 ? void 0 : e[1];
    if (i)
      return parseInt(i, 10);
  }
}
function wS() {
  return typeof CompressionStream < "u";
}
function Rw() {
  return wS() && !rr();
}
function ww(a, e) {
  let t = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1;
  const i = Mw(a, e);
  return t ? i : vm(i, "v1");
}
function Mw(a, e) {
  const t = new URL(bw(a));
  return e.forEach((i, s) => {
    t.searchParams.set(s, i);
  }), vm(t, "rtc");
}
function Aw(a) {
  const e = new URL(xo(a));
  return vm(e, "validate");
}
function MS(a) {
  return a.endsWith("/") ? a : "".concat(a, "/");
}
function vm(a, e) {
  return a.pathname = "".concat(MS(a.pathname)).concat(e), a;
}
function zb(a) {
  if (typeof a == "string")
    return yb.fromJson(JSON.parse(a), {
      ignoreUnknownFields: !0
    });
  if (a instanceof ArrayBuffer)
    return yb.fromBinary(new Uint8Array(a));
  throw new Error("could not decode websocket message: ".concat(typeof a));
}
function Ow(a) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "Unknown reason";
  if (!(a instanceof AbortSignal))
    return e;
  const t = a.reason;
  switch (typeof t) {
    case "string":
      return t;
    case "object":
      return t instanceof Error ? t.message : e;
    default:
      return "toString" in t ? t.toString() : e;
  }
}
const Dw = 10, vo = "lk_e2ee", Pw = "LKFrameEncryptionKey", xw = {
  sharedKey: !1,
  ratchetSalt: Pw,
  ratchetWindowSize: 8,
  failureTolerance: Dw,
  keyringSize: 16,
  keySize: 128
};
var Ta;
(function(a) {
  a.SetKey = "setKey", a.RatchetRequest = "ratchetRequest", a.KeyRatcheted = "keyRatcheted";
})(Ta || (Ta = {}));
var Fb;
(function(a) {
  a.KeyRatcheted = "keyRatcheted";
})(Fb || (Fb = {}));
var pa;
(function(a) {
  a.ParticipantEncryptionStatusChanged = "participantEncryptionStatusChanged", a.EncryptionError = "encryptionError";
})(pa || (pa = {}));
var jb;
(function(a) {
  a.Error = "cryptorError";
})(jb || (jb = {}));
function Nw() {
  return ym() || Iw();
}
function Iw() {
  return typeof window < "u" && typeof window.RTCRtpScriptTransform < "u";
}
function ym() {
  return typeof window < "u" && typeof window.RTCRtpSender < "u" && // @ts-ignore
  typeof window.RTCRtpSender.prototype.createEncodedStreams < "u";
}
function Lw(a) {
  return R(this, void 0, void 0, function* () {
    let e = new TextEncoder();
    return yield crypto.subtle.importKey("raw", e.encode(a), {
      name: "PBKDF2"
    }, !1, ["deriveBits", "deriveKey"]);
  });
}
function Uw(a) {
  return R(this, void 0, void 0, function* () {
    return yield crypto.subtle.importKey("raw", a, "HKDF", !1, ["deriveBits", "deriveKey"]);
  });
}
function Bw(a) {
  var e, t, i, s, c;
  if (((e = a.value) === null || e === void 0 ? void 0 : e.case) !== "sipDtmf" && ((t = a.value) === null || t === void 0 ? void 0 : t.case) !== "metrics" && ((i = a.value) === null || i === void 0 ? void 0 : i.case) !== "speaker" && ((s = a.value) === null || s === void 0 ? void 0 : s.case) !== "transcription" && ((c = a.value) === null || c === void 0 ? void 0 : c.case) !== "encryptedPacket")
    return new lT({
      value: a.value
    });
}
class zw extends sn.EventEmitter {
  constructor() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    super(), this.latestManuallySetKeyIndex = 0, this.onKeyRatcheted = (t, i, s) => {
      Ee.debug("key ratcheted event received", {
        ratchetResult: t,
        participantId: i,
        keyIndex: s
      });
    }, this.keyInfoMap = /* @__PURE__ */ new Map(), this.options = Object.assign(Object.assign({}, xw), e), this.on(Ta.KeyRatcheted, this.onKeyRatcheted);
  }
  /**
   * callback to invoke once a key has been set for a participant
   * @param key
   * @param participantIdentity
   * @param keyIndex
   */
  onSetEncryptionKey(e, t, i) {
    const s = {
      key: e,
      participantIdentity: t,
      keyIndex: i
    };
    if (!this.options.sharedKey && !t)
      throw new Error("participant identity needs to be passed for encryption key if sharedKey option is false");
    this.keyInfoMap.set("".concat(t ?? "shared", "-").concat(i ?? 0), s), i !== void 0 && (this.latestManuallySetKeyIndex = i), this.emit(Ta.SetKey, s, i !== void 0);
  }
  getKeys() {
    return Array.from(this.keyInfoMap.values());
  }
  getLatestManuallySetKeyIndex() {
    return this.latestManuallySetKeyIndex;
  }
  getOptions() {
    return this.options;
  }
  ratchetKey(e, t) {
    this.emit(Ta.RatchetRequest, e, t);
  }
}
class Fw extends zw {
  constructor() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    const t = Object.assign(Object.assign({}, e), {
      sharedKey: !0,
      // for a shared key provider failing to decrypt for a specific participant
      // should not mark the key as invalid, so we accept wrong keys forever
      // and won't try to auto-ratchet
      ratchetWindowSize: 0,
      failureTolerance: -1
    });
    super(t);
  }
  /**
   * Accepts a passphrase that's used to create the crypto keys.
   * When passing in a string, PBKDF2 is used. (recommended for maximum compatibility across SDKs)
   * When passing in an ArrayBuffer of cryptographically random numbers, HKDF is used.
   *
   * Note: Not all client SDKS support HKDF.
   * @param key
   */
  setKey(e) {
    return R(this, void 0, void 0, function* () {
      const t = typeof e == "string" ? yield Lw(e) : yield Uw(e);
      this.onSetEncryptionKey(t);
    });
  }
}
var qb;
(function(a) {
  a[a.InvalidKey = 0] = "InvalidKey", a[a.MissingKey = 1] = "MissingKey", a[a.InternalError = 2] = "InternalError";
})(qb || (qb = {}));
function No() {
  return Bh();
}
function Io(a) {
  return !!a?.worker && (ym() || No());
}
function jl(a) {
  return !!(a?.timestamp || a?.frameId);
}
function jw(a) {
  const e = [];
  return a?.timestamp && e.push(us.PTF_USER_TIMESTAMP), a?.frameId && e.push(us.PTF_FRAME_ID), e;
}
function qw(a) {
  if (!a || a.length === 0)
    return;
  const e = {};
  return a.includes(us.PTF_USER_TIMESTAMP) && (e.timestamp = !0), a.includes(us.PTF_FRAME_ID) && (e.frameId = !0), e.timestamp || e.frameId ? e : void 0;
}
function bm(a) {
  let e = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 50, t = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
  var i, s;
  let c;
  const u = (i = t.isImmediate) !== null && i !== void 0 ? i : !1, d = (s = t.callback) !== null && s !== void 0 ? s : !1, h = t.maxWait;
  let f = Date.now(), p = [];
  function v() {
    if (h !== void 0) {
      const T = Date.now() - f;
      if (T + e >= h)
        return h - T;
    }
    return e;
  }
  const y = function() {
    for (var T = arguments.length, _ = new Array(T), k = 0; k < T; k++)
      _[k] = arguments[k];
    const x = this;
    return new Promise((A, B) => {
      const ie = function() {
        if (c = void 0, f = Date.now(), !u) {
          const E = a.apply(x, _);
          d && d(E), p.forEach((M) => {
            let N = M.resolve;
            return N(E);
          }), p = [];
        }
      }, C = u && c === void 0;
      if (c !== void 0 && bt.clearTimeout(c), c = bt.setTimeout(ie, v()), C) {
        const E = a.apply(x, _);
        return d && d(E), A(E);
      }
      p.push({
        resolve: A,
        reject: B
      });
    });
  };
  return y.cancel = function(T) {
    c !== void 0 && bt.clearTimeout(c), p.forEach((_) => {
      let k = _.reject;
      return k(T);
    }), p = [];
  }, y;
}
const Tm = 2e3;
function du(a, e) {
  if (!e)
    return 0;
  let t, i;
  return "bytesReceived" in a ? (t = a.bytesReceived, i = e.bytesReceived) : "bytesSent" in a && (t = a.bytesSent, i = e.bytesSent), t === void 0 || i === void 0 || a.timestamp === void 0 || e.timestamp === void 0 ? 0 : (t - i) * 8 * 1e3 / (a.timestamp - e.timestamp);
}
class AS extends H {
  constructor(e, t, i, s, c) {
    super(e, i, c), this.sid = t, this.receiver = s;
  }
  get isLocal() {
    return !1;
  }
  /** @internal */
  setMuted(e) {
    this.isMuted !== e && (this.isMuted = e, this._mediaStreamTrack.enabled = !e, this.emit(e ? re.Muted : re.Unmuted, this));
  }
  /** @internal */
  setMediaStream(e) {
    this.mediaStream = e;
    const t = (i) => {
      i.track === this._mediaStreamTrack && (e.removeEventListener("removetrack", t), this.receiver && "playoutDelayHint" in this.receiver && (this.receiver.playoutDelayHint = void 0), this.receiver = void 0, this._currentBitrate = 0, this.emit(re.Ended, this));
    };
    e.addEventListener("removetrack", t);
  }
  start() {
    this.startMonitor(), super.enable();
  }
  stop() {
    this.stopMonitor(), super.disable();
  }
  /**
   * Gets the RTCStatsReport for the RemoteTrack's underlying RTCRtpReceiver
   * See https://developer.mozilla.org/en-US/docs/Web/API/RTCStatsReport
   *
   * @returns Promise<RTCStatsReport> | undefined
   */
  getRTCStatsReport() {
    return R(this, void 0, void 0, function* () {
      var e;
      return !((e = this.receiver) === null || e === void 0) && e.getStats ? yield this.receiver.getStats() : void 0;
    });
  }
  /**
   * Allows to set a playout delay (in seconds) for this track.
   * A higher value allows for more buffering of the track in the browser
   * and will result in a delay of media being played back of `delayInSeconds`
   */
  setPlayoutDelay(e) {
    this.receiver ? "playoutDelayHint" in this.receiver ? this.receiver.playoutDelayHint = e : this.log.warn("Playout delay not supported in this browser") : this.log.warn("Cannot set playout delay, track already ended");
  }
  /**
   * Returns the current playout delay (in seconds) of this track.
   */
  getPlayoutDelay() {
    if (this.receiver) {
      if ("playoutDelayHint" in this.receiver)
        return this.receiver.playoutDelayHint;
      this.log.warn("Playout delay not supported in this browser");
    } else
      this.log.warn("Cannot get playout delay, track already ended");
    return 0;
  }
  /* @internal */
  startMonitor() {
    this.monitorInterval || (this.monitorInterval = setInterval(() => this.monitorReceiver(), Tm)), tw() && this.registerTimeSyncUpdate();
  }
  registerTimeSyncUpdate() {
    const e = () => {
      var t;
      this.timeSyncHandle = requestAnimationFrame(() => e());
      const i = (t = this.receiver) === null || t === void 0 ? void 0 : t.getSynchronizationSources()[0];
      if (i) {
        const s = i.timestamp, c = i.rtpTimestamp;
        c && this.rtpTimestamp !== c && (this.emit(re.TimeSyncUpdate, {
          timestamp: s,
          rtpTimestamp: c
        }), this.rtpTimestamp = c);
      }
    };
    e();
  }
}
const th = 100;
class Sm extends AS {
  constructor(e, t, i, s, c) {
    super(e, t, H.Kind.Video, i, c), this.elementInfos = [], this.monitorReceiver = () => R(this, void 0, void 0, function* () {
      if (!this.receiver) {
        this._currentBitrate = 0;
        return;
      }
      const u = yield this.getReceiverStats();
      u && this.prevStats && this.receiver && (this._currentBitrate = du(u, this.prevStats)), this.prevStats = u;
    }), this.debouncedHandleResize = bm(() => {
      this.updateDimensions();
    }, th), this.adaptiveStreamSettings = s;
  }
  get isAdaptiveStream() {
    return this.adaptiveStreamSettings !== void 0;
  }
  /**
   * Look up frame-level metadata for a given RTP timestamp.
   * Use with the `TrackEvent.TimeSyncUpdate` event to correlate displayed frames
   * with their capture-time metadata.
   *
   * Requires the room to be configured with the `frameMetadata` worker option
   * and the publishing track to have frame metadata features enabled.
   *
   */
  lookupFrameMetadata(e) {
    let t = e.rtpTimestamp;
    var i;
    return (i = this.frameMetadataExtractor) === null || i === void 0 ? void 0 : i.lookupMetadata(t);
  }
  setStreamState(e) {
    super.setStreamState(e), this.log.debug("setStreamState", e), this.isAdaptiveStream && e === H.StreamState.Active && this.updateVisibility();
  }
  /**
   * Note: When using adaptiveStream, you need to use remoteVideoTrack.attach() to add the track to a HTMLVideoElement, otherwise your video tracks might never start
   */
  get mediaStreamTrack() {
    return this._mediaStreamTrack;
  }
  /** @internal */
  setMuted(e) {
    super.setMuted(e), this.attachedElements.forEach((t) => {
      e ? ns(this._mediaStreamTrack, t) : Zr(this._mediaStreamTrack, t);
    });
  }
  attach(e) {
    if (e ? super.attach(e) : e = super.attach(), this.adaptiveStreamSettings && this.elementInfos.find((t) => t.element === e) === void 0) {
      const t = new Hw(e);
      this.observeElementInfo(t);
    }
    return e;
  }
  /**
   * Observe an ElementInfo for changes when adaptive streaming.
   * @param elementInfo
   * @internal
   */
  observeElementInfo(e) {
    this.adaptiveStreamSettings && this.elementInfos.find((t) => t === e) === void 0 ? (e.handleResize = () => {
      this.debouncedHandleResize();
    }, e.handleVisibilityChanged = () => {
      this.updateVisibility();
    }, this.elementInfos.push(e), e.observe(), this.debouncedHandleResize(), this.updateVisibility()) : this.log.warn("visibility resize observer not triggered", this.logContext);
  }
  /**
   * Stop observing an ElementInfo for changes.
   * @param elementInfo
   * @internal
   */
  stopObservingElementInfo(e) {
    if (!this.isAdaptiveStream) {
      this.log.warn("stopObservingElementInfo ignored", this.logContext);
      return;
    }
    const t = this.elementInfos.filter((i) => i === e);
    for (const i of t)
      i.stopObserving();
    this.elementInfos = this.elementInfos.filter((i) => i !== e), this.updateVisibility(), this.debouncedHandleResize();
  }
  detach(e) {
    if (e)
      return this.stopObservingElement(e), super.detach(e);
    const t = super.detach();
    for (const i of t)
      this.stopObservingElement(i);
    return t;
  }
  /** @internal */
  getDecoderImplementation() {
    var e;
    return (e = this.prevStats) === null || e === void 0 ? void 0 : e.decoderImplementation;
  }
  getReceiverStats() {
    return R(this, void 0, void 0, function* () {
      if (!this.receiver || !this.receiver.getStats)
        return;
      const e = yield this.receiver.getStats();
      let t, i = "", s = /* @__PURE__ */ new Map();
      return e.forEach((c) => {
        c.type === "inbound-rtp" ? (i = c.codecId, t = {
          type: "video",
          streamId: c.id,
          framesDecoded: c.framesDecoded,
          framesDropped: c.framesDropped,
          framesReceived: c.framesReceived,
          packetsReceived: c.packetsReceived,
          packetsLost: c.packetsLost,
          frameWidth: c.frameWidth,
          frameHeight: c.frameHeight,
          pliCount: c.pliCount,
          firCount: c.firCount,
          nackCount: c.nackCount,
          jitter: c.jitter,
          timestamp: c.timestamp,
          bytesReceived: c.bytesReceived,
          decoderImplementation: c.decoderImplementation
        }) : c.type === "codec" && s.set(c.id, c);
      }), t && i !== "" && s.get(i) && (t.mimeType = s.get(i).mimeType), t;
    });
  }
  stopObservingElement(e) {
    const t = this.elementInfos.filter((i) => i.element === e);
    for (const i of t)
      this.stopObservingElementInfo(i);
  }
  handleAppVisibilityChanged() {
    const e = Object.create(null, {
      handleAppVisibilityChanged: {
        get: () => super.handleAppVisibilityChanged
      }
    });
    return R(this, void 0, void 0, function* () {
      yield e.handleAppVisibilityChanged.call(this), this.isAdaptiveStream && this.updateVisibility();
    });
  }
  updateVisibility(e) {
    var t, i;
    const s = this.elementInfos.reduce((h, f) => Math.max(h, f.visibilityChangedAt || 0), 0), c = !((i = (t = this.adaptiveStreamSettings) === null || t === void 0 ? void 0 : t.pauseVideoInBackground) !== null && i !== void 0) || i ? this.isInBackground : !1, u = this.elementInfos.some((h) => h.pictureInPicture), d = this.elementInfos.some((h) => h.visible) && !c || u;
    if (!(this.lastVisible === d && !e)) {
      if (!d && Date.now() - s < th) {
        bt.setTimeout(() => {
          this.updateVisibility();
        }, th);
        return;
      }
      this.lastVisible = d, this.emit(re.VisibilityChanged, d, this);
    }
  }
  updateDimensions() {
    var e, t;
    let i = 0, s = 0;
    const c = this.getPixelDensity();
    for (const u of this.elementInfos) {
      const d = u.width() * c, h = u.height() * c;
      d + h > i + s && (i = d, s = h);
    }
    ((e = this.lastDimensions) === null || e === void 0 ? void 0 : e.width) === i && ((t = this.lastDimensions) === null || t === void 0 ? void 0 : t.height) === s || (this.lastDimensions = {
      width: i,
      height: s
    }, this.emit(re.VideoDimensionsChanged, this.lastDimensions, this));
  }
  getPixelDensity() {
    var e;
    const t = (e = this.adaptiveStreamSettings) === null || e === void 0 ? void 0 : e.pixelDensity;
    return t === "screen" ? Nb() : t || (Nb() > 2 ? 2 : 1);
  }
}
class Hw {
  get visible() {
    return this.isPiP || this.isIntersecting;
  }
  get pictureInPicture() {
    return this.isPiP;
  }
  constructor(e, t) {
    this.onVisibilityChanged = (i) => {
      var s;
      const c = i.target, u = i.isIntersecting;
      c === this.element && (this.isIntersecting = u, this.isPiP = yo(this.element), this.visibilityChangedAt = Date.now(), (s = this.handleVisibilityChanged) === null || s === void 0 || s.call(this));
    }, this.onEnterPiP = () => {
      var i, s;
      (s = (i = window.documentPictureInPicture) === null || i === void 0 ? void 0 : i.window) === null || s === void 0 || s.addEventListener("pagehide", this.onLeavePiP), queueMicrotask(() => {
        requestAnimationFrame(() => {
          var c;
          this.isPiP = yo(this.element), (c = this.handleVisibilityChanged) === null || c === void 0 || c.call(this);
        });
      });
    }, this.onLeavePiP = () => {
      var i;
      this.isPiP = yo(this.element), (i = this.handleVisibilityChanged) === null || i === void 0 || i.call(this);
    }, this.element = e, this.isIntersecting = t ?? Fh(e), this.isPiP = an() && yo(e), this.visibilityChangedAt = 0;
  }
  width() {
    return this.element.clientWidth;
  }
  height() {
    return this.element.clientHeight;
  }
  observe() {
    var e, t, i;
    this.isIntersecting = Fh(this.element), this.isPiP = yo(this.element), this.element.handleResize = () => {
      var s;
      (s = this.handleResize) === null || s === void 0 || s.call(this);
    }, this.element.handleVisibilityChanged = this.onVisibilityChanged, Lb().observe(this.element), Ib().observe(this.element), this.element.addEventListener("enterpictureinpicture", this.onEnterPiP), this.element.addEventListener("leavepictureinpicture", this.onLeavePiP), (e = window.documentPictureInPicture) === null || e === void 0 || e.addEventListener("enter", this.onEnterPiP), (i = (t = window.documentPictureInPicture) === null || t === void 0 ? void 0 : t.window) === null || i === void 0 || i.addEventListener("pagehide", this.onLeavePiP);
  }
  stopObserving() {
    var e, t, i, s, c;
    (e = Lb()) === null || e === void 0 || e.unobserve(this.element), (t = Ib()) === null || t === void 0 || t.unobserve(this.element), this.element.removeEventListener("enterpictureinpicture", this.onEnterPiP), this.element.removeEventListener("leavepictureinpicture", this.onLeavePiP), (i = window.documentPictureInPicture) === null || i === void 0 || i.removeEventListener("enter", this.onEnterPiP), (c = (s = window.documentPictureInPicture) === null || s === void 0 ? void 0 : s.window) === null || c === void 0 || c.removeEventListener("pagehide", this.onLeavePiP);
  }
}
function yo(a) {
  var e, t;
  return document.pictureInPictureElement === a ? !0 : !((e = window.documentPictureInPicture) === null || e === void 0) && e.window ? Fh(a, (t = window.documentPictureInPicture) === null || t === void 0 ? void 0 : t.window) : !1;
}
function Fh(a, e) {
  const t = e || window;
  let i = a.offsetTop, s = a.offsetLeft;
  const c = a.offsetWidth, u = a.offsetHeight, d = a, h = d.hidden, f = getComputedStyle(a), p = f.display;
  for (; a.offsetParent; )
    a = a.offsetParent, i += a.offsetTop, s += a.offsetLeft;
  return i < t.pageYOffset + t.innerHeight && s < t.pageXOffset + t.innerWidth && i + u > t.pageYOffset && s + c > t.pageXOffset && !h && p !== "none";
}
class Vw extends sn.EventEmitter {
  constructor(e, t) {
    super(), this.decryptDataRequests = /* @__PURE__ */ new Map(), this.encryptDataRequests = /* @__PURE__ */ new Map(), this.onWorkerMessage = (i) => {
      var s, c;
      const u = i.data, d = u.kind, h = u.data;
      switch (d) {
        case "error":
          if (Ee.error(h.error.message), h.uuid) {
            const v = this.decryptDataRequests.get(h.uuid);
            if (v?.reject) {
              v.reject(h.error);
              break;
            }
            const y = this.encryptDataRequests.get(h.uuid);
            if (y?.reject) {
              y.reject(h.error);
              break;
            }
          }
          this.emit(pa.EncryptionError, h.error, h.participantIdentity);
          break;
        case "initAck":
          h.enabled && this.keyProvider.getKeys().forEach((v) => {
            this.postKey(v, !1);
          });
          break;
        case "enable":
          if (h.enabled && this.keyProvider.getKeys().forEach((v) => {
            this.postKey(v, !1);
          }), this.encryptionEnabled !== h.enabled && h.participantIdentity === ((s = this.room) === null || s === void 0 ? void 0 : s.localParticipant.identity))
            this.emit(pa.ParticipantEncryptionStatusChanged, h.enabled, this.room.localParticipant), this.encryptionEnabled = h.enabled;
          else if (h.participantIdentity) {
            const v = (c = this.room) === null || c === void 0 ? void 0 : c.getParticipantByIdentity(h.participantIdentity);
            if (!v)
              throw TypeError("couldn't set encryption status, participant not found".concat(h.participantIdentity));
            this.emit(pa.ParticipantEncryptionStatusChanged, h.enabled, v);
          }
          break;
        case "ratchetKey":
          this.keyProvider.emit(Ta.KeyRatcheted, h.ratchetResult, h.participantIdentity, h.keyIndex);
          break;
        case "decryptDataResponse":
          const f = this.decryptDataRequests.get(h.uuid);
          f?.resolve && f.resolve(h);
          break;
        case "encryptDataResponse":
          const p = this.encryptDataRequests.get(h.uuid);
          p?.resolve && p.resolve(h);
          break;
        case "packetTrailerMetadata":
          this.handleFrameMetadata(h.trackId, h.rtpTimestamp, h.ssrc, h.metadata);
          break;
      }
    }, this.onWorkerError = (i) => {
      Ee.error("e2ee worker encountered an error:", {
        error: i.error
      }), this.emit(pa.EncryptionError, i.error, void 0);
    }, this.keyProvider = e.keyProvider, this.worker = e.worker, this.encryptionEnabled = !1, this.dataChannelEncryptionEnabled = t;
  }
  get isEnabled() {
    return this.encryptionEnabled;
  }
  get isDataChannelEncryptionEnabled() {
    return this.isEnabled && this.dataChannelEncryptionEnabled;
  }
  /**
   * @internal
   */
  setup(e) {
    if (!Nw())
      throw new mm("tried to setup end-to-end encryption on an unsupported browser");
    if (Ee.info("setting up e2ee"), e !== this.room) {
      this.room = e, this.setupEventListeners(e, this.keyProvider);
      const t = {
        kind: "init",
        data: {
          keyProviderOptions: this.keyProvider.getOptions(),
          loglevel: R1.getLevel()
        }
      };
      this.worker && (Ee.info("initializing worker", {
        worker: this.worker
      }), this.worker.onmessage = this.onWorkerMessage, this.worker.onerror = this.onWorkerError, this.worker.postMessage(t));
    }
  }
  /**
   * @internal
   */
  setParticipantCryptorEnabled(e, t) {
    Ee.debug("set e2ee to ".concat(e, " for participant ").concat(t)), this.postEnable(e, t);
  }
  /**
   * @internal
   */
  setSifTrailer(e) {
    !e || e.length === 0 ? Ee.warn("ignoring server sent trailer as it's empty") : this.postSifTrailer(e);
  }
  handleFrameMetadata(e, t, i, s) {
    if (this.room) {
      for (const c of [this.room.localParticipant, ...this.room.remoteParticipants.values()])
        for (const u of c.trackPublications.values())
          if (u.track && u.track.mediaStreamID === e && u.track instanceof Sm && u.track.frameMetadataExtractor) {
            u.track.frameMetadataExtractor.storeMetadata(t, i, s);
            return;
          }
    }
  }
  setupEngine(e) {
    e.on(ae.RTPVideoMapUpdate, (t) => {
      this.postRTPMap(t);
    });
  }
  setupEventListeners(e, t) {
    e.on(j.TrackPublished, (i, s) => this.setParticipantCryptorEnabled(i.trackInfo.encryption !== $e.NONE, s.identity)), e.on(j.ConnectionStateChanged, (i) => {
      i === Te.Connected && e.remoteParticipants.forEach((s) => {
        s.trackPublications.forEach((c) => {
          this.setParticipantCryptorEnabled(c.trackInfo.encryption !== $e.NONE, s.identity);
        });
      });
    }).on(j.TrackUnsubscribed, (i, s, c) => {
      var u;
      const d = {
        kind: "removeTransform",
        data: {
          participantIdentity: c.identity,
          trackId: i.mediaStreamID
        }
      };
      (u = this.worker) === null || u === void 0 || u.postMessage(d);
    }).on(j.TrackSubscribed, (i, s, c) => {
      this.setupE2EEReceiver(i, c.identity, s.trackInfo);
    }).on(j.SignalConnected, () => {
      if (!this.room)
        throw new TypeError("expected room to be present on signal connect");
      const i = t.getLatestManuallySetKeyIndex();
      t.getKeys().forEach((s) => {
        var c;
        this.postKey(s, i === ((c = s.keyIndex) !== null && c !== void 0 ? c : 0));
      }), this.setParticipantCryptorEnabled(this.room.localParticipant.isE2EEEnabled, this.room.localParticipant.identity);
    }), e.localParticipant.on(X.LocalSenderCreated, (i, s) => R(this, void 0, void 0, function* () {
      this.setupE2EESender(s, i);
    })), e.localParticipant.on(X.LocalTrackPublished, (i) => {
      if (!Li(i.track) || !Po())
        return;
      const s = {
        kind: "updateCodec",
        data: {
          trackId: i.track.mediaStreamID,
          codec: Co(i.trackInfo.codecs[0].mimeType),
          participantIdentity: this.room.localParticipant.identity,
          hasPacketTrailer: !1
        }
      };
      this.worker.postMessage(s);
    }), t.on(Ta.SetKey, (i, s) => this.postKey(i, s ?? !0)).on(Ta.RatchetRequest, (i, s) => this.postRatchetRequest(i, s));
  }
  encryptData(e) {
    return R(this, void 0, void 0, function* () {
      if (!this.worker)
        throw Error("could not encrypt data, worker is missing");
      const t = crypto.randomUUID(), i = {
        kind: "encryptDataRequest",
        data: {
          uuid: t,
          payload: e,
          participantIdentity: this.room.localParticipant.identity
        }
      }, s = new wt();
      return s.onFinally = () => {
        this.encryptDataRequests.delete(t);
      }, this.encryptDataRequests.set(t, s), this.worker.postMessage(i), s.promise;
    });
  }
  handleEncryptedData(e, t, i, s) {
    if (!this.worker)
      throw Error("could not handle encrypted data, worker is missing");
    const c = crypto.randomUUID(), u = {
      kind: "decryptDataRequest",
      data: {
        uuid: c,
        payload: e,
        iv: t,
        participantIdentity: i,
        keyIndex: s
      }
    }, d = new wt();
    return d.onFinally = () => {
      this.decryptDataRequests.delete(c);
    }, this.decryptDataRequests.set(c, d), this.worker.postMessage(u), d.promise;
  }
  postRatchetRequest(e, t) {
    if (!this.worker)
      throw Error("could not ratchet key, worker is missing");
    const i = {
      kind: "ratchetRequest",
      data: {
        participantIdentity: e,
        keyIndex: t
      }
    };
    this.worker.postMessage(i);
  }
  postKey(e, t) {
    let i = e.key, s = e.participantIdentity, c = e.keyIndex;
    var u;
    if (!this.worker)
      throw Error("could not set key, worker is missing");
    const d = {
      kind: "setKey",
      data: {
        participantIdentity: s,
        isPublisher: s === ((u = this.room) === null || u === void 0 ? void 0 : u.localParticipant.identity),
        key: i,
        keyIndex: c,
        updateCurrentKeyIndex: t
      }
    };
    this.worker.postMessage(d);
  }
  postEnable(e, t) {
    if (this.worker) {
      const i = {
        kind: "enable",
        data: {
          enabled: e,
          participantIdentity: t
        }
      };
      this.worker.postMessage(i);
    } else
      throw new ReferenceError("failed to enable e2ee, worker is not ready");
  }
  postRTPMap(e) {
    var t;
    if (!this.worker)
      throw TypeError("could not post rtp map, worker is missing");
    if (!(!((t = this.room) === null || t === void 0) && t.localParticipant.identity))
      throw TypeError("could not post rtp map, local participant identity is missing");
    const i = {
      kind: "setRTPMap",
      data: {
        map: e,
        participantIdentity: this.room.localParticipant.identity
      }
    };
    this.worker.postMessage(i);
  }
  postSifTrailer(e) {
    if (!this.worker)
      throw Error("could not post SIF trailer, worker is missing");
    const t = {
      kind: "setSifTrailer",
      data: {
        trailer: e
      }
    };
    this.worker.postMessage(t);
  }
  setupE2EEReceiver(e, t, i) {
    if (!e.receiver)
      return;
    if (!i?.mimeType || i.mimeType === "")
      throw new TypeError("MimeType missing from trackInfo, cannot set up E2EE cryptor");
    const s = e.kind === "video" && !!i.packetTrailerFeatures && i.packetTrailerFeatures.length > 0;
    this.handleReceiver(e.receiver, e.mediaStreamID, t, e.kind === "video" ? Co(i.mimeType) : void 0, s);
  }
  setupE2EESender(e, t) {
    var i, s, c;
    if (!is(e) || !t) {
      t || Ee.warn("early return because sender is not ready");
      return;
    }
    this.handleSender(t, e.mediaStreamID, void 0, Li(e) ? (s = (i = e.publishOptions) === null || i === void 0 ? void 0 : i.frameMetadata) !== null && s !== void 0 ? s : (c = e.publishOptions) === null || c === void 0 ? void 0 : c.packetTrailer : void 0);
  }
  /**
   * Handles the given {@code RTCRtpReceiver} by creating a {@code TransformStream} which will inject
   * a frame decoder.
   *
   */
  handleReceiver(e, t, i, s, c) {
    return R(this, void 0, void 0, function* () {
      if (this.worker) {
        if (Bh()) {
          const u = {
            kind: "decode",
            participantIdentity: i,
            trackId: t,
            codec: s,
            hasPacketTrailer: c
          };
          e.transform = new RTCRtpScriptTransform(this.worker, u);
        } else {
          if (vo in e && s) {
            const f = {
              kind: "updateCodec",
              data: {
                trackId: t,
                codec: s,
                participantIdentity: i,
                hasPacketTrailer: c
              }
            };
            this.worker.postMessage(f);
            return;
          }
          let u = e.writableStream, d = e.readableStream;
          if (!u || !d) {
            const f = e.createEncodedStreams();
            e.writableStream = f.writable, u = f.writable, e.readableStream = f.readable, d = f.readable;
          }
          const h = {
            kind: "decode",
            data: {
              readableStream: d,
              writableStream: u,
              trackId: t,
              codec: s,
              participantIdentity: i,
              isReuse: vo in e,
              hasPacketTrailer: c
            }
          };
          this.worker.postMessage(h, [d, u]);
        }
        e[vo] = !0;
      }
    });
  }
  /**
   * Handles the given {@code RTCRtpSender} by creating a {@code TransformStream} which will inject
   * a frame encoder.
   *
   */
  handleSender(e, t, i, s) {
    var c;
    if (!(vo in e || !this.worker)) {
      if (!(!((c = this.room) === null || c === void 0) && c.localParticipant.identity) || this.room.localParticipant.identity === "")
        throw TypeError("local identity needs to be known in order to set up encrypted sender");
      if (Bh()) {
        Ee.info("initialize script transform");
        const u = {
          kind: "encode",
          participantIdentity: this.room.localParticipant.identity,
          trackId: t,
          codec: i,
          hasPacketTrailer: jl(s),
          packetTrailer: s
        };
        e.transform = new RTCRtpScriptTransform(this.worker, u);
      } else {
        Ee.info("initialize encoded streams");
        const u = e.createEncodedStreams(), d = {
          kind: "encode",
          data: {
            readableStream: u.readable,
            writableStream: u.writable,
            codec: i,
            trackId: t,
            participantIdentity: this.room.localParticipant.identity,
            isReuse: !1,
            hasPacketTrailer: jl(s),
            packetTrailer: s
          }
        };
        this.worker.postMessage(d, [u.readable, u.writable]);
      }
      e[vo] = !0;
    }
  }
}
const Gw = 300;
class Kw {
  constructor() {
    this.metadataMap = /* @__PURE__ */ new Map(), this.activeSsrc = 0;
  }
  storeMetadata(e, t, i) {
    for (this.activeSsrc !== 0 && this.activeSsrc !== t && this.metadataMap.clear(), this.activeSsrc = t; this.metadataMap.size >= Gw; ) {
      const s = this.metadataMap.keys().next().value;
      this.metadataMap.delete(s);
    }
    this.metadataMap.set(e, i);
  }
  lookupMetadata(e) {
    return this.metadataMap.get(e);
  }
  dispose() {
    this.metadataMap.clear(), this.activeSsrc = 0;
  }
}
class Yw {
  constructor(e) {
    this.extractors = /* @__PURE__ */ new Map(), this.workerPipelines = /* @__PURE__ */ new Map(), this.onWorkerMessage = (t) => {
      const i = t.data;
      if (i.kind === "metadata") {
        const s = this.extractors.get(i.data.trackId);
        s && s.storeMetadata(i.data.rtpTimestamp, i.data.ssrc, i.data.metadata);
      }
    }, this.onWorkerError = (t) => {
      Ee.error("frame metadata worker encountered an error:", {
        error: t.error
      });
    }, this.worker = e?.worker;
  }
  /** @internal */
  setup(e) {
    e !== this.room && (this.room = e, this.worker && (this.worker.onmessage = this.onWorkerMessage, this.worker.onerror = this.onWorkerError, this.worker.postMessage({
      kind: "init"
    })), e.on(j.TrackSubscribed, (t, i, s) => {
      t.kind === "video" && this.setupReceiver(t, i.trackInfo);
    }).on(j.TrackUnsubscribed, (t) => {
      this.teardownTrack(t);
    }).on(j.Disconnected, () => {
      this.cleanup();
    }));
  }
  setupReceiver(e, t) {
    var i, s, c;
    const u = e.receiver;
    if (!u)
      return;
    if (!(!!t?.packetTrailerFeatures && t.packetTrailerFeatures.length > 0)) {
      !((i = this.room) === null || i === void 0) && i.hasE2EESetup || this.setupPassthroughReceiver(u, e.mediaStreamID);
      return;
    }
    if (!Io(this.worker ? {
      worker: this.worker
    } : void 0) && !(!((s = this.room) === null || s === void 0) && s.hasE2EESetup)) {
      Ee.warn("frame metadata transform not supported; skipping extraction");
      return;
    }
    const h = new Kw(), f = e.mediaStreamID;
    this.extractors.set(f, h), e.frameMetadataExtractor = h, !(!((c = this.room) === null || c === void 0) && c.hasE2EESetup) && this.setupWorkerReceiver(u, f, !0);
  }
  setupPassthroughReceiver(e, t) {
    if (No()) {
      "transform" in e && (e.transform = null);
      return;
    }
    if (this.worker && Io({
      worker: this.worker
    }) && !this.workerPipelines.has(e)) {
      this.setupWorkerReceiver(e, t, !1);
      return;
    }
    this.worker && this.workerPipelines.has(e) && this.setupWorkerReceiver(e, t, !1);
  }
  setupWorkerReceiver(e, t) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0;
    const s = this.worker;
    if (!s)
      return;
    if (No()) {
      e.transform = new RTCRtpScriptTransform(s, {
        kind: "decode",
        trackId: t
      });
      return;
    }
    const c = this.workerPipelines.get(e);
    if (c) {
      const h = {
        kind: "updateTrackId",
        data: {
          oldTrackId: c,
          newTrackId: t,
          hasPacketTrailer: i
        }
      };
      s.postMessage(h), this.workerPipelines.set(e, t);
      return;
    }
    if (!("createEncodedStreams" in e)) {
      Ee.warn("createEncodedStreams not supported");
      return;
    }
    let u;
    try {
      u = e.createEncodedStreams();
    } catch (h) {
      Ee.warn("failed to create encoded streams", {
        error: h
      });
      return;
    }
    const d = {
      kind: "decode",
      data: {
        readableStream: u.readable,
        writableStream: u.writable,
        trackId: t,
        hasPacketTrailer: i
      }
    };
    s.postMessage(d, [u.readable, u.writable]), this.workerPipelines.set(e, t);
  }
  teardownTrack(e) {
    const t = e.mediaStreamID, i = this.extractors.get(t);
    i && (i.dispose(), this.extractors.delete(t)), e instanceof Sm && (e.frameMetadataExtractor = void 0);
  }
  cleanup() {
    var e;
    for (const t of this.extractors.values())
      t.dispose();
    this.extractors.clear(), this.workerPipelines.clear(), (e = this.worker) === null || e === void 0 || e.terminate();
  }
}
const Jw = 500, Qw = 15e3;
class as {
  // eslint-disable-next-line @typescript-eslint/no-empty-function
  constructor() {
    this.failedConnectionAttempts = /* @__PURE__ */ new Map(), this.backOffPromises = /* @__PURE__ */ new Map();
  }
  static getInstance() {
    return this._instance || (this._instance = new as()), this._instance;
  }
  addFailedConnectionAttempt(e) {
    var t;
    const i = new URL(e), s = Xf(i);
    if (!s)
      return;
    let c = (t = this.failedConnectionAttempts.get(s)) !== null && t !== void 0 ? t : 0;
    this.failedConnectionAttempts.set(s, c + 1), this.backOffPromises.set(s, zt(Math.min(Jw * Math.pow(2, c), Qw)));
  }
  getBackOffPromise(e) {
    const t = new URL(e), i = t && Xf(t);
    return i && this.backOffPromises.get(i) || Promise.resolve();
  }
  resetFailedConnectionAttempts(e) {
    const t = new URL(e), i = t && Xf(t);
    i && (this.failedConnectionAttempts.set(i, 0), this.backOffPromises.set(i, Promise.resolve()));
  }
  resetAll() {
    this.backOffPromises.clear(), this.failedConnectionAttempts.clear();
  }
}
as._instance = null;
const nh = "default";
class Rt {
  constructor() {
    this._previousDevices = [];
  }
  static getInstance() {
    return this.instance === void 0 && (this.instance = new Rt()), this.instance;
  }
  get previousDevices() {
    return this._previousDevices;
  }
  getDevices(e) {
    return R(this, arguments, void 0, function(t) {
      var i = this;
      let s = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0;
      return (function* () {
        var c;
        if (((c = Rt.userMediaPromiseMap) === null || c === void 0 ? void 0 : c.size) > 0) {
          Ee.debug("awaiting getUserMedia promise");
          try {
            t ? yield Rt.userMediaPromiseMap.get(t) : yield Promise.all(Rt.userMediaPromiseMap.values());
          } catch {
            Ee.warn("error waiting for media permissons");
          }
        }
        let u = yield navigator.mediaDevices.enumerateDevices();
        if (s && // for safari we need to skip this check, as otherwise it will re-acquire user media and fail on iOS https://bugs.webkit.org/show_bug.cgi?id=179363
        !(sr() && i.hasDeviceInUse(t)) && (u.filter((h) => h.kind === t).length === 0 || u.some((h) => {
          const f = h.label === "", p = t ? h.kind === t : !0;
          return f && p;
        }))) {
          const h = {
            video: t !== "audioinput" && t !== "audiooutput",
            audio: t !== "videoinput" && {
              deviceId: {
                ideal: "default"
              }
            }
          }, f = yield navigator.mediaDevices.getUserMedia(h);
          u = yield navigator.mediaDevices.enumerateDevices(), f.getTracks().forEach((p) => {
            p.stop();
          });
        }
        return i._previousDevices = u, t && (u = u.filter((d) => d.kind === t)), u;
      })();
    });
  }
  normalizeDeviceId(e, t, i) {
    return R(this, void 0, void 0, function* () {
      if (t !== nh)
        return t;
      const s = yield this.getDevices(e), c = s.find((d) => d.deviceId === nh);
      if (!c) {
        Ee.warn("could not reliably determine default device");
        return;
      }
      const u = s.find((d) => d.deviceId !== nh && d.groupId === (i ?? c.groupId));
      if (!u) {
        Ee.warn("could not reliably determine default device");
        return;
      }
      return u?.deviceId;
    });
  }
  hasDeviceInUse(e) {
    return e ? Rt.userMediaPromiseMap.has(e) : Rt.userMediaPromiseMap.size > 0;
  }
}
Rt.mediaDeviceKinds = ["audioinput", "audiooutput", "videoinput"];
Rt.userMediaPromiseMap = /* @__PURE__ */ new Map();
const km = 65535, OS = 4294967295;
class Jn {
  static u16(e) {
    return new Jn(e, km);
  }
  static u32(e) {
    return new Jn(e, OS);
  }
  constructor(e, t) {
    if (this.value = e, e < 0)
      throw new Error("WrapAroundUnsignedInt: cannot faithfully represent an integer smaller than 0");
    if (t > Number.MAX_SAFE_INTEGER)
      throw new Error("WrapAroundUnsignedInt: cannot faithfully represent an integer bigger than MAX_SAFE_INTEGER.");
    this.maxSize = t, this.clamp();
  }
  /** Manually clamp the given containing value according to the wrap around max size bounds. Use
   * this after out of bounds modification to the contained value by external code. */
  clamp() {
    for (; this.value > this.maxSize; )
      this.value -= this.maxSize + 1;
    for (; this.value < 0; )
      this.value += this.maxSize + 1;
  }
  clone() {
    return new Jn(this.value, this.maxSize);
  }
  /** When called, maps the containing value to a new containing value. After mapping, the wrap
   * around external max size bounds are applied. Note that this is a mutative operation. */
  update(e) {
    this.value = e(this.value), this.clamp();
  }
  /** Increments the given `n` to the inner value. Note that this is a mutative operation. */
  increment() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 1;
    this.update((t) => t + e);
  }
  /** Decrements the given `n` from the inner value. Note that this is a mutative operation. */
  decrement() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 1;
    this.update((t) => t - e);
  }
  getThenIncrement() {
    const e = this.value;
    return this.increment(), new Jn(e, this.maxSize);
  }
  /** Returns true if {@link this} is before the passed other {@link WrapAroundUnsignedInt}. */
  isBefore(e) {
    const t = this.value >>> 0, s = (e.value >>> 0) - t >>> 0;
    return s !== 0 && s < this.maxSize + 1;
  }
}
class rs {
  static fromRtpTicks(e) {
    return new rs(e, 9e4);
  }
  /** Generates a timestamp initialized to a non cryptographically secure random value, so that
   * different streams are more difficult to correlate in packet capture. */
  static rtpRandom() {
    const e = Math.round(Math.random() * OS);
    return rs.fromRtpTicks(e);
  }
  constructor(e, t) {
    this.timestamp = Jn.u32(e), this.rateInHz = t;
  }
  asTicks() {
    return this.timestamp.value;
  }
  clone() {
    return new rs(this.timestamp.value, this.rateInHz);
  }
  wrappingAdd(e) {
    this.timestamp.increment(e);
  }
  /** Returns true if {@link this} is before the passed other {@link DataTrackTimestamp}. */
  isBefore(e) {
    return this.timestamp.isBefore(e.timestamp);
  }
}
class Qa {
  constructor(e, t, i) {
    this.epoch = t, this.base = i, this.previous = i.clone(), this.rateInHz = e;
  }
  static startingNow(e, t) {
    return new Qa(t, /* @__PURE__ */ new Date(), e);
  }
  static startingAtTime(e, t, i) {
    return new Qa(i, e, t);
  }
  static rtpStartingNow(e) {
    return Qa.startingNow(e, 9e4);
  }
  static rtpStartingAtTime(e, t) {
    return Qa.startingAtTime(e, t, 9e4);
  }
  now() {
    return this.at(/* @__PURE__ */ new Date());
  }
  at(e) {
    let t = e.getTime() - this.epoch.getTime(), i = Qa.durationInMsToTicks(t, this.rateInHz), s = this.base.clone();
    return s.wrappingAdd(i), s.isBefore(this.previous) && (s = this.previous), this.previous = s.clone(), s.clone();
  }
  /** Convert a duration since the epoch into clock ticks. */
  static durationInMsToTicks(e, t) {
    let s = (e * 1e6 * t + 5e8) / 1e9;
    return Math.round(s);
  }
}
function Cm(a) {
  if (a instanceof DataView)
    return a;
  if (a instanceof ArrayBuffer)
    return new DataView(a);
  if (a instanceof Uint8Array)
    return new DataView(a.buffer, a.byteOffset, a.byteLength);
  throw new Error("Error coercing ".concat(a, " to DataView - input was not DataView, ArrayBuffer, or Uint8Array."));
}
var nr;
(function(a) {
  a[a.Reserved = 0] = "Reserved", a[a.TooLarge = 1] = "TooLarge";
})(nr || (nr = {}));
class hs extends Nn {
  constructor(e, t) {
    super(19, e), this.name = "DataTrackHandleError", this.reason = t, this.reasonName = nr[t];
  }
  isReason(e) {
    return this.reason === e;
  }
  static tooLarge() {
    return new hs("Value too large to be a valid track handle", nr.TooLarge);
  }
  static reserved(e) {
    return new hs("0x".concat(e.toString(16), " is a reserved value."), nr.Reserved);
  }
}
const Xw = {
  fromNumber(a) {
    if (a === 0)
      throw hs.reserved(a);
    if (a > km)
      throw hs.tooLarge();
    return a;
  }
};
class Zw {
  constructor() {
    this.value = 0;
  }
  /** Returns a unique track handle for the next publication, if one can be obtained. */
  get() {
    return this.value += 1, this.value > km ? null : this.value;
  }
  reset() {
    this.value = 0;
  }
}
const ql = {
  from(a) {
    return {
      sid: a.sid,
      pubHandle: a.pubHandle,
      name: a.name,
      usesE2ee: a.encryption !== $e.NONE
    };
  },
  toProtobuf(a) {
    return new eu({
      sid: a.sid,
      pubHandle: a.pubHandle,
      name: a.name,
      encryption: a.usesE2ee ? $e.GCM : $e.NONE
    });
  }
};
var Eo;
(function(a) {
  a[a.WAITING = 0] = "WAITING", a[a.RUNNING = 1] = "RUNNING", a[a.COMPLETED = 2] = "COMPLETED";
})(Eo || (Eo = {}));
class Ww {
  constructor() {
    this.pendingTasks = /* @__PURE__ */ new Map(), this.taskMutex = new Lt(), this.nextTaskIndex = 0;
  }
  run(e) {
    return R(this, void 0, void 0, function* () {
      const t = {
        id: this.nextTaskIndex++,
        enqueuedAt: Date.now(),
        status: Eo.WAITING
      };
      this.pendingTasks.set(t.id, t);
      const i = yield this.taskMutex.lock();
      try {
        return t.executedAt = Date.now(), t.status = Eo.RUNNING, yield e();
      } finally {
        t.status = Eo.COMPLETED, this.pendingTasks.delete(t.id), i();
      }
    });
  }
  flush() {
    return R(this, void 0, void 0, function* () {
      return this.run(() => R(this, void 0, void 0, function* () {
      }));
    });
  }
  snapshot() {
    return Array.from(this.pendingTasks.values());
  }
}
class $w {
  get readyState() {
    return this.ws.readyState;
  }
  constructor(e) {
    let t = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    var i, s;
    if (!((i = t.signal) === null || i === void 0) && i.aborted)
      throw new DOMException("This operation was aborted", "AbortError");
    this.url = e;
    const c = new WebSocket(e, (s = t.protocols) !== null && s !== void 0 ? s : []);
    c.binaryType = "arraybuffer", this.ws = c;
    const u = function() {
      let d = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {}, h = d.closeCode, f = d.reason;
      return c.close(h, f);
    };
    this.opened = new Gt((d, h) => {
      const f = () => {
        h(ye.websocket("Encountered websocket error during connection establishment"));
      };
      c.onopen = () => {
        d({
          readable: new ReadableStream({
            start(p) {
              c.onmessage = (v) => {
                let y = v.data;
                return p.enqueue(y);
              }, c.onerror = (v) => p.error(v);
            },
            cancel: u
          }),
          writable: new WritableStream({
            write(p) {
              c.send(p);
            },
            abort() {
              c.close();
            },
            close: u
          }),
          protocol: c.protocol,
          extensions: c.extensions
        }), c.removeEventListener("error", f);
      }, c.addEventListener("error", f);
    }), this.closed = new Gt((d, h) => {
      const f = () => R(this, void 0, void 0, function* () {
        const p = new Gt((y) => {
          c.readyState !== WebSocket.CLOSED && c.addEventListener("close", (T) => {
            y(T);
          }, {
            once: !0
          });
        }), v = yield Gt.race([zt(250), p]);
        v ? d(v) : h(ye.websocket("Encountered unspecified websocket error without a timely close event"));
      });
      c.onclose = (p) => {
        let v = p.code, y = p.reason;
        d({
          closeCode: v,
          reason: y
        }), c.removeEventListener("error", f);
      }, c.addEventListener("error", f);
    }), t.signal && (t.signal.onabort = () => c.close()), this.close = u;
  }
}
const eM = ["syncState", "trickle", "offer", "answer", "simulate", "leave"];
function tM(a) {
  const e = eM.indexOf(a.case) >= 0;
  return Ee.trace("request allowed to bypass queue:", {
    canPass: e,
    req: a
  }), e;
}
var Ze;
(function(a) {
  a[a.CONNECTING = 0] = "CONNECTING", a[a.CONNECTED = 1] = "CONNECTED", a[a.RECONNECTING = 2] = "RECONNECTING", a[a.DISCONNECTING = 3] = "DISCONNECTING", a[a.DISCONNECTED = 4] = "DISCONNECTED";
})(Ze || (Ze = {}));
const nM = 250;
class Em {
  get currentState() {
    return this.state;
  }
  get isDisconnected() {
    return this.state === Ze.DISCONNECTING || this.state === Ze.DISCONNECTED;
  }
  get isEstablishingConnection() {
    return this.state === Ze.CONNECTING || this.state === Ze.RECONNECTING;
  }
  getNextRequestId() {
    return this._requestId += 1, this._requestId;
  }
  constructor() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1, t = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    var i;
    this.rtt = 0, this.state = Ze.DISCONNECTED, this.log = Ee, this._requestId = 0, this.useV0SignalPath = !1, this.resetCallbacks = () => {
      this.onAnswer = void 0, this.onLeave = void 0, this.onLocalTrackPublished = void 0, this.onLocalTrackUnpublished = void 0, this.onNegotiateRequested = void 0, this.onOffer = void 0, this.onRemoteMuteChanged = void 0, this.onSubscribedQualityUpdate = void 0, this.onTokenRefresh = void 0, this.onTrickle = void 0, this.onClose = void 0, this.onMediaSectionsRequirement = void 0;
    }, this.loggerContextCb = t.loggerContextCb, this.log = rn((i = t.loggerName) !== null && i !== void 0 ? i : Mt.Signal, () => this.logContext), this.useJSON = e, this.requestQueue = new Ww(), this.queuedRequests = [], this.closingLock = new Lt(), this.connectionLock = new Lt(), this.state = Ze.DISCONNECTED;
  }
  get logContext() {
    var e, t;
    return (t = (e = this.loggerContextCb) === null || e === void 0 ? void 0 : e.call(this)) !== null && t !== void 0 ? t : {};
  }
  join(e, t, i, s) {
    return R(this, arguments, void 0, function(c, u, d, h) {
      var f = this;
      let p = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : !1, v = arguments.length > 5 ? arguments[5] : void 0;
      return (function* () {
        return f.state = Ze.CONNECTING, f.options = d, yield f.connect(c, u, d, h, p, v);
      })();
    });
  }
  reconnect(e, t, i, s) {
    return R(this, void 0, void 0, function* () {
      if (!this.options) {
        this.log.warn("attempted to reconnect without signal options being set, ignoring");
        return;
      }
      return this.state = Ze.RECONNECTING, this.clearPingInterval(), yield this.connect(e, t, Object.assign(Object.assign({}, this.options), {
        reconnect: !0,
        sid: i,
        reconnectReason: s
      }), void 0, this.useV0SignalPath);
    });
  }
  connect(e, t, i, s) {
    return R(this, arguments, void 0, function(c, u, d, h) {
      var f = this;
      let p = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : !1, v = arguments.length > 5 ? arguments[5] : void 0;
      return (function* () {
        const y = yield f.connectionLock.lock();
        f.connectOptions = d, f.useV0SignalPath = p;
        const T = vw(d.clientInfoCapabilities), _ = p ? iM(u, T, d) : yield aM(u, T, d, v), k = ww(c, _, p).toString(), x = Aw(k).toString();
        return new Promise((A, B) => R(f, void 0, void 0, function* () {
          var ie, C;
          try {
            let E = !1;
            const M = (J) => R(this, void 0, void 0, function* () {
              if (E)
                return;
              E = !0;
              const ne = J instanceof Event ? J.currentTarget : J, de = Ow(ne, "Abort handler called");
              this.streamWriter && !this.isDisconnected ? this.sendLeave().then(() => this.close(de)).catch((fe) => {
                this.log.error(fe), this.close();
              }) : this.close(), N(), B(ye.cancelled(de));
            });
            h?.addEventListener("abort", M);
            const N = () => {
              clearTimeout(G), h?.removeEventListener("abort", M);
            }, G = setTimeout(() => {
              M(ye.timeout("room connection has timed out (signal)"));
            }, d.websocketTimeout), W = (J, ne) => {
              this.handleSignalConnected(J, G, ne);
            }, Y = new URL(k);
            if (Y.searchParams.has("access_token") && Y.searchParams.set("access_token", "<redacted>"), this.ws) {
              const J = performance.now();
              yield this.close(!1), this.log.debug("closed previous ws connection in ".concat(performance.now() - J, "ms"));
            }
            this.log.info("signal connecting to ".concat(Y), {
              reconnect: d.reconnect,
              reconnectReason: d.reconnectReason
            }), this.ws = new $w(k);
            try {
              this.ws.closed.then((te) => {
                var le;
                this.isEstablishingConnection && B(ye.internal("Websocket got closed during a (re)connection attempt: ".concat(te.reason))), te.closeCode !== 1e3 && (this.log.warn("websocket closed", {
                  reason: te.reason,
                  code: te.closeCode,
                  wasClean: te.closeCode === 1e3,
                  state: this.state
                }), this.state === Ze.CONNECTED && this.handleOnClose((le = te.reason) !== null && le !== void 0 ? le : "Unexpected WS error"));
              }).catch((te) => {
                this.isEstablishingConnection && B(ye.internal("Websocket error during a (re)connection attempt: ".concat(te)));
              });
              const J = yield this.ws.opened.catch((te) => R(this, void 0, void 0, function* () {
                if (this.state !== Ze.CONNECTED) {
                  this.state = Ze.DISCONNECTED, clearTimeout(G);
                  const le = yield this.handleConnectionError(te, x);
                  B(le);
                  return;
                }
                this.handleWSError(te), B(te);
              }));
              if (clearTimeout(G), !J)
                return;
              const ne = J.readable.getReader();
              this.streamWriter = J.writable.getWriter();
              const de = yield ne.read();
              if (ne.releaseLock(), !de.value)
                throw ye.internal("no message received as first message");
              const fe = zb(de.value), ke = this.validateFirstMessage(fe, (ie = d.reconnect) !== null && ie !== void 0 ? ie : !1);
              if (!ke.isValid) {
                B(ke.error);
                return;
              }
              ((C = fe.message) === null || C === void 0 ? void 0 : C.case) === "join" && (this.pingTimeoutDuration = fe.message.value.pingTimeout, this.pingIntervalDuration = fe.message.value.pingInterval, this.pingTimeoutDuration && this.pingTimeoutDuration > 0 && this.log.debug("ping config", {
                timeout: this.pingTimeoutDuration,
                interval: this.pingIntervalDuration
              }), this.onJoined && this.onJoined(fe.message.value));
              const q = ke.shouldProcessFirstMessage ? fe : void 0;
              W(J, q), A(ke.response);
            } catch (J) {
              B(J);
            } finally {
              N();
            }
          } finally {
            y();
          }
        }));
      })();
    });
  }
  startReadingLoop(e, t) {
    return R(this, void 0, void 0, function* () {
      for (t && this.handleSignalResponse(t); ; ) {
        this.signalLatency && (yield zt(this.signalLatency));
        const i = yield e.read(), s = i.done, c = i.value;
        if (s)
          break;
        const u = zb(c);
        this.handleSignalResponse(u);
      }
    });
  }
  close() {
    return R(this, arguments, void 0, function() {
      var e = this;
      let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0, i = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "Close method called on signal client";
      return (function* () {
        if ([Ze.DISCONNECTING || Ze.DISCONNECTED].includes(e.state)) {
          e.log.debug("ignoring signal close as it's already in disconnecting state");
          return;
        }
        const s = yield e.closingLock.lock();
        try {
          if (e.clearPingInterval(), t && (e.state = Ze.DISCONNECTING), e.ws) {
            e.ws.close({
              closeCode: 1e3,
              reason: i
            });
            const c = e.ws.closed;
            e.ws = void 0, e.streamWriter = void 0, yield Promise.race([c, zt(nM)]);
          }
        } catch (c) {
          e.log.debug("websocket error while closing", {
            error: c
          });
        } finally {
          t && (e.state = Ze.DISCONNECTED), s();
        }
      })();
    });
  }
  // initial offer after joining
  sendOffer(e, t) {
    this.log.debug("sending offer", {
      offerSdp: e.sdp
    }), this.sendRequest({
      case: "offer",
      value: Xa(e, t)
    });
  }
  // answer a server-initiated offer
  sendAnswer(e, t) {
    return this.log.debug("sending answer", {
      answerSdp: e.sdp
    }), this.sendRequest({
      case: "answer",
      value: Xa(e, t)
    });
  }
  sendIceCandidate(e, t) {
    return this.log.debug("sending ice candidate", {
      candidate: e
    }), this.sendRequest({
      case: "trickle",
      value: new su({
        candidateInit: JSON.stringify(e),
        target: t
      })
    });
  }
  sendMuteTrack(e, t) {
    return this.sendRequest({
      case: "mute",
      value: new ou({
        sid: e,
        muted: t
      })
    });
  }
  sendAddTrack(e) {
    return this.sendRequest({
      case: "addTrack",
      value: e
    });
  }
  sendUpdateLocalMetadata(e, t) {
    return R(this, arguments, void 0, function(i, s) {
      var c = this;
      let u = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
      return (function* () {
        const d = c.getNextRequestId();
        return yield c.sendRequest({
          case: "updateMetadata",
          value: new lm({
            requestId: d,
            metadata: i,
            name: s,
            attributes: u
          })
        }), d;
      })();
    });
  }
  sendUpdateTrackSettings(e) {
    this.sendRequest({
      case: "trackSetting",
      value: e
    });
  }
  sendUpdateSubscription(e) {
    return this.sendRequest({
      case: "subscription",
      value: e
    });
  }
  sendSyncState(e) {
    return this.sendRequest({
      case: "syncState",
      value: e
    });
  }
  sendUpdateVideoLayers(e, t) {
    return this.sendRequest({
      case: "updateLayers",
      value: new OT({
        trackSid: e,
        layers: t
      })
    });
  }
  sendUpdateSubscriptionPermissions(e, t) {
    return this.sendRequest({
      case: "subscriptionPermission",
      value: new xT({
        allParticipants: e,
        trackPermissions: t
      })
    });
  }
  sendSimulateScenario(e) {
    return this.sendRequest({
      case: "simulate",
      value: e
    });
  }
  sendPing() {
    return Promise.all([this.sendRequest({
      case: "ping",
      value: at.parse(Date.now())
    }), this.sendRequest({
      case: "pingReq",
      value: new LT({
        timestamp: at.parse(Date.now()),
        rtt: at.parse(this.rtt)
      })
    })]);
  }
  sendUpdateLocalAudioTrack(e, t) {
    return this.sendRequest({
      case: "updateAudioTrack",
      value: new cm({
        trackSid: e,
        features: t
      })
    });
  }
  sendLeave() {
    return this.sendRequest({
      case: "leave",
      value: new lu({
        reason: Yn.CLIENT_INITIATED,
        // server doesn't process this field, keeping it here to indicate the intent of a full disconnect
        action: es.DISCONNECT
      })
    });
  }
  sendPublishDataTrackRequest(e, t, i) {
    return this.sendRequest({
      case: "publishDataTrackRequest",
      value: new am({
        pubHandle: e,
        name: t,
        encryption: i ? $e.GCM : $e.NONE
      })
    });
  }
  sendUnPublishDataTrackRequest(e) {
    return this.sendRequest({
      case: "unpublishDataTrackRequest",
      value: new sm({
        pubHandle: e
      })
    });
  }
  sendUpdateDataSubscription(e, t) {
    return this.sendRequest({
      case: "updateDataSubscription",
      value: new RT({
        // FIXME: consider refactoring to allow caller to pass an array of events through
        updates: [new wT({
          trackSid: e,
          subscribe: t
        })]
      })
    });
  }
  sendRequest(e) {
    return R(this, arguments, void 0, function(t) {
      var i = this;
      let s = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !1;
      return (function* () {
        if (!s && !tM(t) && i.state === Ze.RECONNECTING) {
          i.queuedRequests.push(() => R(i, void 0, void 0, function* () {
            yield this.sendRequest(t, !0);
          }));
          return;
        }
        if (s || (yield i.requestQueue.flush()), i.signalLatency && (yield zt(i.signalLatency)), i.isDisconnected) {
          i.log.debug("skipping signal request (type: ".concat(t.case, ") - SignalClient disconnected"));
          return;
        }
        if (!i.streamWriter) {
          i.log.error("cannot send signal request before connected, type: ".concat(t?.case));
          return;
        }
        const u = new JR({
          message: t
        });
        try {
          i.useJSON ? yield i.streamWriter.write(u.toJsonString()) : yield i.streamWriter.write(u.toBinary().buffer);
        } catch (d) {
          i.log.error("error sending signal message", {
            error: d
          });
        }
      })();
    });
  }
  handleSignalResponse(e) {
    var t, i;
    const s = e.message;
    if (s == null) {
      this.log.debug("received unsupported message");
      return;
    }
    let c = !1;
    if (s.case === "answer") {
      const u = Hb(s.value);
      this.onAnswer && this.onAnswer(u, s.value.id, s.value.midToTrackId);
    } else if (s.case === "offer") {
      const u = Hb(s.value);
      this.onOffer && this.onOffer(u, s.value.id, s.value.midToTrackId);
    } else if (s.case === "trickle") {
      const u = JSON.parse(s.value.candidateInit);
      this.onTrickle && this.onTrickle(u, s.value.target);
    } else s.case === "update" ? this.onParticipantUpdate && this.onParticipantUpdate((t = s.value.participants) !== null && t !== void 0 ? t : []) : s.case === "trackPublished" ? this.onLocalTrackPublished && this.onLocalTrackPublished(s.value) : s.case === "speakersChanged" ? this.onSpeakersChanged && this.onSpeakersChanged((i = s.value.speakers) !== null && i !== void 0 ? i : []) : s.case === "leave" ? this.onLeave && this.onLeave(s.value) : s.case === "mute" ? this.onRemoteMuteChanged && this.onRemoteMuteChanged(s.value.sid, s.value.muted) : s.case === "roomUpdate" ? this.onRoomUpdate && s.value.room && this.onRoomUpdate(s.value.room) : s.case === "connectionQuality" ? this.onConnectionQuality && this.onConnectionQuality(s.value) : s.case === "streamStateUpdate" ? this.onStreamStateUpdate && this.onStreamStateUpdate(s.value) : s.case === "subscribedQualityUpdate" ? this.onSubscribedQualityUpdate && this.onSubscribedQualityUpdate(s.value) : s.case === "subscriptionPermissionUpdate" ? this.onSubscriptionPermissionUpdate && this.onSubscriptionPermissionUpdate(s.value) : s.case === "refreshToken" ? this.onTokenRefresh && this.onTokenRefresh(s.value) : s.case === "trackUnpublished" ? this.onLocalTrackUnpublished && this.onLocalTrackUnpublished(s.value) : s.case === "subscriptionResponse" ? this.onSubscriptionError && this.onSubscriptionError(s.value) : s.case === "pong" || (s.case === "pongResp" ? (this.rtt = Date.now() - Number.parseInt(s.value.lastPingTimestamp.toString()), this.resetPingTimeout(), c = !0) : s.case === "requestResponse" ? this.onRequestResponse && this.onRequestResponse(s.value) : s.case === "trackSubscribed" ? this.onLocalTrackSubscribed && this.onLocalTrackSubscribed(s.value.trackSid) : s.case === "roomMoved" ? (this.onTokenRefresh && this.onTokenRefresh(s.value.token), this.onRoomMoved && this.onRoomMoved(s.value)) : s.case === "mediaSectionsRequirement" ? this.onMediaSectionsRequirement && this.onMediaSectionsRequirement(s.value) : s.case === "publishDataTrackResponse" ? this.onPublishDataTrackResponse && this.onPublishDataTrackResponse(s.value) : s.case === "unpublishDataTrackResponse" ? this.onUnPublishDataTrackResponse && this.onUnPublishDataTrackResponse(s.value) : s.case === "dataTrackSubscriberHandles" ? this.onDataTrackSubscriberHandles && this.onDataTrackSubscriberHandles(s.value) : this.log.debug("unsupported message", {
      msgCase: s.case
    }));
    c || this.resetPingTimeout();
  }
  setReconnected() {
    for (; this.queuedRequests.length > 0; ) {
      const e = this.queuedRequests.shift();
      e && this.requestQueue.run(e);
    }
  }
  handleOnClose(e) {
    return R(this, void 0, void 0, function* () {
      if (this.state === Ze.DISCONNECTED) return;
      const t = this.onClose;
      yield this.close(void 0, e), this.log.info("websocket connection closed: ".concat(e), {
        reason: e
      }), t && t(e);
    });
  }
  handleWSError(e) {
    this.log.error("websocket error", {
      error: e
    });
  }
  /**
   * Resets the ping timeout and starts a new timeout.
   * Call this after receiving a pong message
   */
  resetPingTimeout() {
    if (this.clearPingTimeout(), !this.pingTimeoutDuration) {
      this.log.warn("ping timeout duration not set");
      return;
    }
    this.pingTimeout = bt.setTimeout(() => {
      this.log.warn("ping timeout triggered. last pong received at: ".concat(new Date(Date.now() - this.pingTimeoutDuration * 1e3).toUTCString())), this.handleOnClose("ping timeout");
    }, this.pingTimeoutDuration * 1e3);
  }
  /**
   * Clears ping timeout (does not start a new timeout)
   */
  clearPingTimeout() {
    this.pingTimeout && bt.clearTimeout(this.pingTimeout);
  }
  startPingInterval() {
    if (this.clearPingInterval(), this.resetPingTimeout(), !this.pingIntervalDuration) {
      this.log.warn("ping interval duration not set");
      return;
    }
    this.log.debug("start ping interval"), this.pingInterval = bt.setInterval(() => {
      this.sendPing();
    }, this.pingIntervalDuration * 1e3);
  }
  clearPingInterval() {
    this.log.debug("clearing ping interval"), this.clearPingTimeout(), this.pingInterval && bt.clearInterval(this.pingInterval);
  }
  /**
   * Handles the successful connection to the signal server
   * @param connection The WebSocket connection
   * @param timeoutHandle The timeout handle to clear
   * @param firstMessage Optional first message to process
   * @internal
   */
  handleSignalConnected(e, t, i) {
    this.state = Ze.CONNECTED, this.log.info("signal connected"), clearTimeout(t), this.startPingInterval(), this.startReadingLoop(e.readable.getReader(), i);
  }
  /**
   * Validates the first message received from the signal server
   * @param firstSignalResponse The first signal response received
   * @param isReconnect Whether this is a reconnection attempt
   * @returns Validation result with response or error
   * @internal
   */
  validateFirstMessage(e, t) {
    var i, s, c, u, d;
    return ((i = e.message) === null || i === void 0 ? void 0 : i.case) === "join" ? {
      isValid: !0,
      response: e.message.value
    } : this.state === Ze.RECONNECTING && ((s = e.message) === null || s === void 0 ? void 0 : s.case) !== "leave" ? ((c = e.message) === null || c === void 0 ? void 0 : c.case) === "reconnect" ? {
      isValid: !0,
      response: e.message.value
    } : (this.log.debug("declaring signal reconnected without reconnect response received"), {
      isValid: !0,
      response: void 0,
      shouldProcessFirstMessage: !0
    }) : this.isEstablishingConnection && ((u = e.message) === null || u === void 0 ? void 0 : u.case) === "leave" ? {
      isValid: !1,
      error: ye.leaveRequest("Received leave request while trying to (re)connect", e.message.value.reason)
    } : t ? {
      isValid: !1,
      error: ye.internal("Unexpected first message")
    } : {
      isValid: !1,
      error: ye.internal("did not receive join response, got ".concat((d = e.message) === null || d === void 0 ? void 0 : d.case, " instead"))
    };
  }
  /**
   * Handles WebSocket connection errors by validating with the server
   * @param reason The error that occurred
   * @param validateUrl The URL to validate the connection with
   * @returns A ConnectionError with appropriate reason and status
   * @internal
   */
  handleConnectionError(e, t) {
    return R(this, void 0, void 0, function* () {
      try {
        const i = yield fetch(t);
        switch (i.status) {
          case 404:
            const s = yield i.text();
            return s.includes("requested room does not exist") ? ye.notAllowed(s, i.status) : ye.serviceNotFound("v1 RTC path not found. Consider upgrading your LiveKit server version", "v0-rtc");
          case 401:
          case 403:
            const c = yield i.text();
            return ye.notAllowed(c, i.status);
          default:
            break;
        }
        return e instanceof ye ? e : ye.internal("Encountered unknown websocket error during connection: ".concat(e), {
          status: i.status,
          statusText: i.statusText
        });
      } catch (i) {
        return i instanceof ye ? i : ye.serverUnreachable(i instanceof Error ? i.message : "server was not reachable");
      }
    });
  }
}
function Hb(a) {
  const e = {
    type: "offer",
    sdp: a.sdp
  };
  switch (a.type) {
    case "answer":
    case "offer":
    case "pranswer":
    case "rollback":
      e.type = a.type;
      break;
  }
  return e;
}
function Xa(a, e) {
  return new Ca({
    sdp: a.sdp,
    type: a.type,
    id: e
  });
}
function iM(a, e, t) {
  var i;
  const s = new URLSearchParams();
  return s.set("access_token", a), t.reconnect && (s.set("reconnect", "1"), t.sid && s.set("sid", t.sid)), s.set("auto_subscribe", t.autoSubscribe ? "1" : "0"), s.set("sdk", Qn() ? "reactnative" : "js"), s.set("version", e.version), s.set("protocol", e.protocol.toString()), s.set("client_protocol", e.clientProtocol.toString()), e.deviceModel && s.set("device_model", e.deviceModel), e.os && s.set("os", e.os), e.osVersion && s.set("os_version", e.osVersion), e.browser && s.set("browser", e.browser), e.browserVersion && s.set("browser_version", e.browserVersion), t.adaptiveStream && s.set("adaptive_stream", "1"), t.reconnectReason && s.set("reconnect_reason", t.reconnectReason.toString()), !((i = navigator.connection) === null || i === void 0) && i.type && s.set("network", navigator.connection.type), s;
}
function aM(a, e, t, i) {
  return R(this, void 0, void 0, function* () {
    const s = new URLSearchParams();
    s.set("access_token", a);
    const c = new b1({
      clientInfo: e,
      connectionSettings: new UT({
        autoSubscribe: !!t.autoSubscribe,
        adaptiveStream: !!t.adaptiveStream
      }),
      reconnect: !!t.reconnect,
      participantSid: t.sid ? t.sid : void 0,
      publisherOffer: i
    });
    t.reconnectReason && (c.reconnectReason = t.reconnectReason);
    const u = c.toBinary();
    let d, h;
    if (wS()) {
      const y = new CompressionStream("gzip"), T = y.writable.getWriter();
      T.write(new Uint8Array(u)), T.close();
      const _ = [], k = y.readable.getReader();
      for (; ; ) {
        const ie = yield k.read(), C = ie.done, E = ie.value;
        if (C) break;
        _.push(E);
      }
      const x = _.reduce((ie, C) => ie + C.length, 0), A = new Uint8Array(x);
      let B = 0;
      for (const ie of _)
        A.set(ie, B), B += ie.length;
      d = A, h = kh.GZIP;
    } else
      d = u, h = kh.NONE;
    const p = new T1({
      joinRequest: d,
      compression: h
    }).toBinary(), v = (y) => {
      const T = Array.from(y, (_) => String.fromCodePoint(_)).join("");
      return btoa(T);
    };
    return s.set("join_request", v(p).replace(/\+/g, "-").replace(/\//g, "_")), s;
  });
}
class Vb {
  constructor() {
    this.buffer = [], this._totalSize = 0;
  }
  push(e) {
    this.buffer.push(e), this._totalSize += e.data.byteLength;
  }
  pop() {
    const e = this.buffer.shift();
    return e && (this._totalSize -= e.data.byteLength), e;
  }
  getAll() {
    return this.buffer.slice();
  }
  popToSequence(e) {
    for (; this.buffer.length > 0 && this.buffer[0].sequence <= e; )
      this.pop();
  }
  alignBufferedAmount(e) {
    for (; this.buffer.length > 0; ) {
      const t = this.buffer[0];
      if (this._totalSize - t.data.byteLength <= e)
        break;
      this.pop();
    }
  }
  get length() {
    return this.buffer.length;
  }
}
class rM {
  /**
   * @param ttl ttl of the key (ms)
   */
  constructor(e) {
    this._map = /* @__PURE__ */ new Map(), this._lastCleanup = 0, this.ttl = e;
  }
  set(e, t) {
    const i = Date.now();
    i - this._lastCleanup > this.ttl / 2 && this.cleanup();
    const s = i + this.ttl;
    return this._map.set(e, {
      value: t,
      expiresAt: s
    }), this;
  }
  get(e) {
    const t = this._map.get(e);
    if (t) {
      if (t.expiresAt < Date.now()) {
        this._map.delete(e);
        return;
      }
      return t.value;
    }
  }
  has(e) {
    const t = this._map.get(e);
    return t ? t.expiresAt < Date.now() ? (this._map.delete(e), !1) : !0 : !1;
  }
  delete(e) {
    return this._map.delete(e);
  }
  clear() {
    this._map.clear();
  }
  cleanup() {
    const e = Date.now();
    for (const i of this._map.entries()) {
      var t = ge(i, 2);
      const s = t[0];
      t[1].expiresAt < e && this._map.delete(s);
    }
    this._lastCleanup = e;
  }
  get size() {
    return this.cleanup(), this._map.size;
  }
  forEach(e) {
    this.cleanup();
    for (const i of this._map.entries()) {
      var t = ge(i, 2);
      const s = t[0], c = t[1];
      c.expiresAt >= Date.now() && e(c.value, s, this.asValueMap());
    }
  }
  map(e) {
    this.cleanup();
    const t = [], i = this.asValueMap();
    for (const c of i.entries()) {
      var s = ge(c, 2);
      const u = s[0], d = s[1];
      t.push(e(d, u, i));
    }
    return t;
  }
  asValueMap() {
    const e = /* @__PURE__ */ new Map();
    for (const i of this._map.entries()) {
      var t = ge(i, 2);
      const s = t[0], c = t[1];
      c.expiresAt >= Date.now() && e.set(s, c.value);
    }
    return e;
  }
}
var Bn = {}, ih = {}, ah = { exports: {} }, Gb;
function _m() {
  if (Gb) return ah.exports;
  Gb = 1;
  var a = ah.exports = {
    v: [{
      name: "version",
      reg: /^(\d*)$/
    }],
    o: [{
      // o=- 20518 0 IN IP4 203.0.113.1
      // NB: sessionId will be a String in most cases because it is huge
      name: "origin",
      reg: /^(\S*) (\d*) (\d*) (\S*) IP(\d) (\S*)/,
      names: ["username", "sessionId", "sessionVersion", "netType", "ipVer", "address"],
      format: "%s %s %d %s IP%d %s"
    }],
    // default parsing of these only (though some of these feel outdated)
    s: [{
      name: "name"
    }],
    i: [{
      name: "description"
    }],
    u: [{
      name: "uri"
    }],
    e: [{
      name: "email"
    }],
    p: [{
      name: "phone"
    }],
    z: [{
      name: "timezones"
    }],
    // TODO: this one can actually be parsed properly...
    r: [{
      name: "repeats"
    }],
    // TODO: this one can also be parsed properly
    // k: [{}], // outdated thing ignored
    t: [{
      // t=0 0
      name: "timing",
      reg: /^(\d*) (\d*)/,
      names: ["start", "stop"],
      format: "%d %d"
    }],
    c: [{
      // c=IN IP4 10.47.197.26
      name: "connection",
      reg: /^IN IP(\d) (\S*)/,
      names: ["version", "ip"],
      format: "IN IP%d %s"
    }],
    b: [{
      // b=AS:4000
      push: "bandwidth",
      reg: /^(TIAS|AS|CT|RR|RS):(\d*)/,
      names: ["type", "limit"],
      format: "%s:%s"
    }],
    m: [{
      // m=video 51744 RTP/AVP 126 97 98 34 31
      // NB: special - pushes to session
      // TODO: rtp/fmtp should be filtered by the payloads found here?
      reg: /^(\w*) (\d*) ([\w/]*)(?: (.*))?/,
      names: ["type", "port", "protocol", "payloads"],
      format: "%s %d %s %s"
    }],
    a: [
      {
        // a=rtpmap:110 opus/48000/2
        push: "rtp",
        reg: /^rtpmap:(\d*) ([\w\-.]*)(?:\s*\/(\d*)(?:\s*\/(\S*))?)?/,
        names: ["payload", "codec", "rate", "encoding"],
        format: function(e) {
          return e.encoding ? "rtpmap:%d %s/%s/%s" : e.rate ? "rtpmap:%d %s/%s" : "rtpmap:%d %s";
        }
      },
      {
        // a=fmtp:108 profile-level-id=24;object=23;bitrate=64000
        // a=fmtp:111 minptime=10; useinbandfec=1
        push: "fmtp",
        reg: /^fmtp:(\d*) ([\S| ]*)/,
        names: ["payload", "config"],
        format: "fmtp:%d %s"
      },
      {
        // a=control:streamid=0
        name: "control",
        reg: /^control:(.*)/,
        format: "control:%s"
      },
      {
        // a=rtcp:65179 IN IP4 193.84.77.194
        name: "rtcp",
        reg: /^rtcp:(\d*)(?: (\S*) IP(\d) (\S*))?/,
        names: ["port", "netType", "ipVer", "address"],
        format: function(e) {
          return e.address != null ? "rtcp:%d %s IP%d %s" : "rtcp:%d";
        }
      },
      {
        // a=rtcp-fb:98 trr-int 100
        push: "rtcpFbTrrInt",
        reg: /^rtcp-fb:(\*|\d*) trr-int (\d*)/,
        names: ["payload", "value"],
        format: "rtcp-fb:%s trr-int %d"
      },
      {
        // a=rtcp-fb:98 nack rpsi
        push: "rtcpFb",
        reg: /^rtcp-fb:(\*|\d*) ([\w-_]*)(?: ([\w-_]*))?/,
        names: ["payload", "type", "subtype"],
        format: function(e) {
          return e.subtype != null ? "rtcp-fb:%s %s %s" : "rtcp-fb:%s %s";
        }
      },
      {
        // a=extmap:2 urn:ietf:params:rtp-hdrext:toffset
        // a=extmap:1/recvonly URI-gps-string
        // a=extmap:3 urn:ietf:params:rtp-hdrext:encrypt urn:ietf:params:rtp-hdrext:smpte-tc 25@600/24
        push: "ext",
        reg: /^extmap:(\d+)(?:\/(\w+))?(?: (urn:ietf:params:rtp-hdrext:encrypt))? (\S*)(?: (\S*))?/,
        names: ["value", "direction", "encrypt-uri", "uri", "config"],
        format: function(e) {
          return "extmap:%d" + (e.direction ? "/%s" : "%v") + (e["encrypt-uri"] ? " %s" : "%v") + " %s" + (e.config ? " %s" : "");
        }
      },
      {
        // a=extmap-allow-mixed
        name: "extmapAllowMixed",
        reg: /^(extmap-allow-mixed)/
      },
      {
        // a=crypto:1 AES_CM_128_HMAC_SHA1_80 inline:PS1uQCVeeCFCanVmcjkpPywjNWhcYD0mXXtxaVBR|2^20|1:32
        push: "crypto",
        reg: /^crypto:(\d*) ([\w_]*) (\S*)(?: (\S*))?/,
        names: ["id", "suite", "config", "sessionConfig"],
        format: function(e) {
          return e.sessionConfig != null ? "crypto:%d %s %s %s" : "crypto:%d %s %s";
        }
      },
      {
        // a=setup:actpass
        name: "setup",
        reg: /^setup:(\w*)/,
        format: "setup:%s"
      },
      {
        // a=connection:new
        name: "connectionType",
        reg: /^connection:(new|existing)/,
        format: "connection:%s"
      },
      {
        // a=mid:1
        name: "mid",
        reg: /^mid:([^\s]*)/,
        format: "mid:%s"
      },
      {
        // a=msid:0c8b064d-d807-43b4-b434-f92a889d8587 98178685-d409-46e0-8e16-7ef0db0db64a
        name: "msid",
        reg: /^msid:(.*)/,
        format: "msid:%s"
      },
      {
        // a=ptime:20
        name: "ptime",
        reg: /^ptime:(\d*(?:\.\d*)*)/,
        format: "ptime:%d"
      },
      {
        // a=maxptime:60
        name: "maxptime",
        reg: /^maxptime:(\d*(?:\.\d*)*)/,
        format: "maxptime:%d"
      },
      {
        // a=sendrecv
        name: "direction",
        reg: /^(sendrecv|recvonly|sendonly|inactive)/
      },
      {
        // a=ice-lite
        name: "icelite",
        reg: /^(ice-lite)/
      },
      {
        // a=ice-ufrag:F7gI
        name: "iceUfrag",
        reg: /^ice-ufrag:(\S*)/,
        format: "ice-ufrag:%s"
      },
      {
        // a=ice-pwd:x9cml/YzichV2+XlhiMu8g
        name: "icePwd",
        reg: /^ice-pwd:(\S*)/,
        format: "ice-pwd:%s"
      },
      {
        // a=fingerprint:SHA-1 00:11:22:33:44:55:66:77:88:99:AA:BB:CC:DD:EE:FF:00:11:22:33
        name: "fingerprint",
        reg: /^fingerprint:(\S*) (\S*)/,
        names: ["type", "hash"],
        format: "fingerprint:%s %s"
      },
      {
        // a=candidate:0 1 UDP 2113667327 203.0.113.1 54400 typ host
        // a=candidate:1162875081 1 udp 2113937151 192.168.34.75 60017 typ host generation 0 network-id 3 network-cost 10
        // a=candidate:3289912957 2 udp 1845501695 193.84.77.194 60017 typ srflx raddr 192.168.34.75 rport 60017 generation 0 network-id 3 network-cost 10
        // a=candidate:229815620 1 tcp 1518280447 192.168.150.19 60017 typ host tcptype active generation 0 network-id 3 network-cost 10
        // a=candidate:3289912957 2 tcp 1845501695 193.84.77.194 60017 typ srflx raddr 192.168.34.75 rport 60017 tcptype passive generation 0 network-id 3 network-cost 10
        push: "candidates",
        reg: /^candidate:(\S*) (\d*) (\S*) (\d*) (\S*) (\d*) typ (\S*)(?: raddr (\S*) rport (\d*))?(?: tcptype (\S*))?(?: generation (\d*))?(?: network-id (\d*))?(?: network-cost (\d*))?/,
        names: ["foundation", "component", "transport", "priority", "ip", "port", "type", "raddr", "rport", "tcptype", "generation", "network-id", "network-cost"],
        format: function(e) {
          var t = "candidate:%s %d %s %d %s %d typ %s";
          return t += e.raddr != null ? " raddr %s rport %d" : "%v%v", t += e.tcptype != null ? " tcptype %s" : "%v", e.generation != null && (t += " generation %d"), t += e["network-id"] != null ? " network-id %d" : "%v", t += e["network-cost"] != null ? " network-cost %d" : "%v", t;
        }
      },
      {
        // a=end-of-candidates (keep after the candidates line for readability)
        name: "endOfCandidates",
        reg: /^(end-of-candidates)/
      },
      {
        // a=remote-candidates:1 203.0.113.1 54400 2 203.0.113.1 54401 ...
        name: "remoteCandidates",
        reg: /^remote-candidates:(.*)/,
        format: "remote-candidates:%s"
      },
      {
        // a=ice-options:google-ice
        name: "iceOptions",
        reg: /^ice-options:(\S*)/,
        format: "ice-options:%s"
      },
      {
        // a=ssrc:2566107569 cname:t9YU8M1UxTF8Y1A1
        push: "ssrcs",
        reg: /^ssrc:(\d*) ([\w_-]*)(?::(.*))?/,
        names: ["id", "attribute", "value"],
        format: function(e) {
          var t = "ssrc:%d";
          return e.attribute != null && (t += " %s", e.value != null && (t += ":%s")), t;
        }
      },
      {
        // a=ssrc-group:FEC 1 2
        // a=ssrc-group:FEC-FR 3004364195 1080772241
        push: "ssrcGroups",
        // token-char = %x21 / %x23-27 / %x2A-2B / %x2D-2E / %x30-39 / %x41-5A / %x5E-7E
        reg: /^ssrc-group:([\x21\x23\x24\x25\x26\x27\x2A\x2B\x2D\x2E\w]*) (.*)/,
        names: ["semantics", "ssrcs"],
        format: "ssrc-group:%s %s"
      },
      {
        // a=msid-semantic: WMS Jvlam5X3SX1OP6pn20zWogvaKJz5Hjf9OnlV
        name: "msidSemantic",
        reg: /^msid-semantic:\s?(\w*) (\S*)/,
        names: ["semantic", "token"],
        format: "msid-semantic: %s %s"
        // space after ':' is not accidental
      },
      {
        // a=group:BUNDLE audio video
        push: "groups",
        reg: /^group:(\w*) (.*)/,
        names: ["type", "mids"],
        format: "group:%s %s"
      },
      {
        // a=rtcp-mux
        name: "rtcpMux",
        reg: /^(rtcp-mux)/
      },
      {
        // a=rtcp-rsize
        name: "rtcpRsize",
        reg: /^(rtcp-rsize)/
      },
      {
        // a=sctpmap:5000 webrtc-datachannel 1024
        name: "sctpmap",
        reg: /^sctpmap:([\w_/]*) (\S*)(?: (\S*))?/,
        names: ["sctpmapNumber", "app", "maxMessageSize"],
        format: function(e) {
          return e.maxMessageSize != null ? "sctpmap:%s %s %s" : "sctpmap:%s %s";
        }
      },
      {
        // a=x-google-flag:conference
        name: "xGoogleFlag",
        reg: /^x-google-flag:([^\s]*)/,
        format: "x-google-flag:%s"
      },
      {
        // a=rid:1 send max-width=1280;max-height=720;max-fps=30;depend=0
        push: "rids",
        reg: /^rid:([\d\w]+) (\w+)(?: ([\S| ]*))?/,
        names: ["id", "direction", "params"],
        format: function(e) {
          return e.params ? "rid:%s %s %s" : "rid:%s %s";
        }
      },
      {
        // a=imageattr:97 send [x=800,y=640,sar=1.1,q=0.6] [x=480,y=320] recv [x=330,y=250]
        // a=imageattr:* send [x=800,y=640] recv *
        // a=imageattr:100 recv [x=320,y=240]
        push: "imageattrs",
        reg: new RegExp(
          // a=imageattr:97
          "^imageattr:(\\d+|\\*)[\\s\\t]+(send|recv)[\\s\\t]+(\\*|\\[\\S+\\](?:[\\s\\t]+\\[\\S+\\])*)(?:[\\s\\t]+(recv|send)[\\s\\t]+(\\*|\\[\\S+\\](?:[\\s\\t]+\\[\\S+\\])*))?"
        ),
        names: ["pt", "dir1", "attrs1", "dir2", "attrs2"],
        format: function(e) {
          return "imageattr:%s %s %s" + (e.dir2 ? " %s %s" : "");
        }
      },
      {
        // a=simulcast:send 1,2,3;~4,~5 recv 6;~7,~8
        // a=simulcast:recv 1;4,5 send 6;7
        name: "simulcast",
        reg: new RegExp(
          // a=simulcast:
          "^simulcast:(send|recv) ([a-zA-Z0-9\\-_~;,]+)(?:\\s?(send|recv) ([a-zA-Z0-9\\-_~;,]+))?$"
        ),
        names: ["dir1", "list1", "dir2", "list2"],
        format: function(e) {
          return "simulcast:%s %s" + (e.dir2 ? " %s %s" : "");
        }
      },
      {
        // old simulcast draft 03 (implemented by Firefox)
        //   https://tools.ietf.org/html/draft-ietf-mmusic-sdp-simulcast-03
        // a=simulcast: recv pt=97;98 send pt=97
        // a=simulcast: send rid=5;6;7 paused=6,7
        name: "simulcast_03",
        reg: /^simulcast:[\s\t]+([\S+\s\t]+)$/,
        names: ["value"],
        format: "simulcast: %s"
      },
      {
        // a=framerate:25
        // a=framerate:29.97
        name: "framerate",
        reg: /^framerate:(\d+(?:$|\.\d+))/,
        format: "framerate:%s"
      },
      {
        // RFC4570
        // a=source-filter: incl IN IP4 239.5.2.31 10.1.15.5
        name: "sourceFilter",
        reg: /^source-filter: *(excl|incl) (\S*) (IP4|IP6|\*) (\S*) (.*)/,
        names: ["filterMode", "netType", "addressTypes", "destAddress", "srcList"],
        format: "source-filter: %s %s %s %s %s"
      },
      {
        // a=bundle-only
        name: "bundleOnly",
        reg: /^(bundle-only)/
      },
      {
        // a=label:1
        name: "label",
        reg: /^label:(.+)/,
        format: "label:%s"
      },
      {
        // RFC version 26 for SCTP over DTLS
        // https://tools.ietf.org/html/draft-ietf-mmusic-sctp-sdp-26#section-5
        name: "sctpPort",
        reg: /^sctp-port:(\d+)$/,
        format: "sctp-port:%s"
      },
      {
        // RFC version 26 for SCTP over DTLS
        // https://tools.ietf.org/html/draft-ietf-mmusic-sctp-sdp-26#section-6
        name: "maxMessageSize",
        reg: /^max-message-size:(\d+)$/,
        format: "max-message-size:%s"
      },
      {
        // RFC7273
        // a=ts-refclk:ptp=IEEE1588-2008:39-A7-94-FF-FE-07-CB-D0:37
        push: "tsRefClocks",
        reg: /^ts-refclk:([^\s=]*)(?:=(\S*))?/,
        names: ["clksrc", "clksrcExt"],
        format: function(e) {
          return "ts-refclk:%s" + (e.clksrcExt != null ? "=%s" : "");
        }
      },
      {
        // RFC7273
        // a=mediaclk:direct=963214424
        name: "mediaClk",
        reg: /^mediaclk:(?:id=(\S*))? *([^\s=]*)(?:=(\S*))?(?: *rate=(\d+)\/(\d+))?/,
        names: ["id", "mediaClockName", "mediaClockValue", "rateNumerator", "rateDenominator"],
        format: function(e) {
          var t = "mediaclk:";
          return t += e.id != null ? "id=%s %s" : "%v%s", t += e.mediaClockValue != null ? "=%s" : "", t += e.rateNumerator != null ? " rate=%s" : "", t += e.rateDenominator != null ? "/%s" : "", t;
        }
      },
      {
        // a=keywds:keywords
        name: "keywords",
        reg: /^keywds:(.+)$/,
        format: "keywds:%s"
      },
      {
        // a=content:main
        name: "content",
        reg: /^content:(.+)/,
        format: "content:%s"
      },
      // BFCP https://tools.ietf.org/html/rfc4583
      {
        // a=floorctrl:c-s
        name: "bfcpFloorCtrl",
        reg: /^floorctrl:(c-only|s-only|c-s)/,
        format: "floorctrl:%s"
      },
      {
        // a=confid:1
        name: "bfcpConfId",
        reg: /^confid:(\d+)/,
        format: "confid:%s"
      },
      {
        // a=userid:1
        name: "bfcpUserId",
        reg: /^userid:(\d+)/,
        format: "userid:%s"
      },
      {
        // a=floorid:1
        name: "bfcpFloorId",
        reg: /^floorid:(.+) (?:m-stream|mstrm):(.+)/,
        names: ["id", "mStream"],
        format: "floorid:%s mstrm:%s"
      },
      {
        // any a= that we don't understand is kept verbatim on media.invalid
        push: "invalid",
        names: ["value"]
      }
    ]
  };
  return Object.keys(a).forEach(function(e) {
    var t = a[e];
    t.forEach(function(i) {
      i.reg || (i.reg = /(.*)/), i.format || (i.format = "%s");
    });
  }), ah.exports;
}
var Kb;
function sM() {
  return Kb || (Kb = 1, (function(a) {
    var e = function(d) {
      return String(Number(d)) === d ? Number(d) : d;
    }, t = function(d, h, f, p) {
      if (p && !f)
        h[p] = e(d[1]);
      else
        for (var v = 0; v < f.length; v += 1)
          d[v + 1] != null && (h[f[v]] = e(d[v + 1]));
    }, i = function(d, h, f) {
      var p = d.name && d.names;
      d.push && !h[d.push] ? h[d.push] = [] : p && !h[d.name] && (h[d.name] = {});
      var v = d.push ? {} : (
        // blank object that will be pushed
        p ? h[d.name] : h
      );
      t(f.match(d.reg), v, d.names, d.name), d.push && h[d.push].push(v);
    }, s = _m(), c = RegExp.prototype.test.bind(/^([a-z])=(.*)/);
    a.parse = function(d) {
      var h = {}, f = [], p = h;
      return d.split(/(\r\n|\r|\n)/).filter(c).forEach(function(v) {
        var y = v[0], T = v.slice(2);
        y === "m" && (f.push({
          rtp: [],
          fmtp: []
        }), p = f[f.length - 1]);
        for (var _ = 0; _ < (s[y] || []).length; _ += 1) {
          var k = s[y][_];
          if (k.reg.test(T))
            return i(k, p, T);
        }
      }), h.media = f, h;
    };
    var u = function(d, h) {
      var f = h.split(/=(.+)/, 2);
      return f.length === 2 ? d[f[0]] = e(f[1]) : f.length === 1 && h.length > 1 && (d[f[0]] = void 0), d;
    };
    a.parseParams = function(d) {
      return d.split(/;\s?/).reduce(u, {});
    }, a.parseFmtpConfig = a.parseParams, a.parsePayloads = function(d) {
      return d.toString().split(" ").map(Number);
    }, a.parseRemoteCandidates = function(d) {
      for (var h = [], f = d.split(" ").map(e), p = 0; p < f.length; p += 3)
        h.push({
          component: f[p],
          ip: f[p + 1],
          port: f[p + 2]
        });
      return h;
    }, a.parseImageAttributes = function(d) {
      return d.split(" ").map(function(h) {
        return h.substring(1, h.length - 1).split(",").reduce(u, {});
      });
    }, a.parseSimulcastStreamList = function(d) {
      return d.split(";").map(function(h) {
        return h.split(",").map(function(f) {
          var p, v = !1;
          return f[0] !== "~" ? p = e(f) : (p = e(f.substring(1, f.length)), v = !0), {
            scid: p,
            paused: v
          };
        });
      });
    };
  })(ih)), ih;
}
var rh, Yb;
function oM() {
  if (Yb) return rh;
  Yb = 1;
  var a = _m(), e = /%[sdv%]/g, t = function(u) {
    var d = 1, h = arguments, f = h.length;
    return u.replace(e, function(p) {
      if (d >= f)
        return p;
      var v = h[d];
      switch (d += 1, p) {
        case "%%":
          return "%";
        case "%s":
          return String(v);
        case "%d":
          return Number(v);
        case "%v":
          return "";
      }
    });
  }, i = function(u, d, h) {
    var f = d.format instanceof Function ? d.format(d.push ? h : h[d.name]) : d.format, p = [u + "=" + f];
    if (d.names)
      for (var v = 0; v < d.names.length; v += 1) {
        var y = d.names[v];
        d.name ? p.push(h[d.name][y]) : p.push(h[d.names[v]]);
      }
    else
      p.push(h[d.name]);
    return t.apply(null, p);
  }, s = ["v", "o", "s", "i", "u", "e", "p", "c", "b", "t", "r", "z", "a"], c = ["i", "c", "b", "a"];
  return rh = function(u, d) {
    d = d || {}, u.version == null && (u.version = 0), u.name == null && (u.name = " "), u.media.forEach(function(v) {
      v.payloads == null && (v.payloads = "");
    });
    var h = d.outerOrder || s, f = d.innerOrder || c, p = [];
    return h.forEach(function(v) {
      a[v].forEach(function(y) {
        y.name in u && u[y.name] != null ? p.push(i(v, y, u)) : y.push in u && u[y.push] != null && u[y.push].forEach(function(T) {
          p.push(i(v, y, T));
        });
      });
    }), u.media.forEach(function(v) {
      p.push(i("m", a.m[0], v)), f.forEach(function(y) {
        a[y].forEach(function(T) {
          T.name in v && v[T.name] != null ? p.push(i(y, T, v)) : T.push in v && v[T.push] != null && v[T.push].forEach(function(_) {
            p.push(i(y, T, _));
          });
        });
      });
    }), p.join(`\r
`) + `\r
`;
  }, rh;
}
var Jb;
function cM() {
  if (Jb) return Bn;
  Jb = 1;
  var a = sM(), e = oM(), t = _m();
  return Bn.grammar = t, Bn.write = e, Bn.parse = a.parse, Bn.parseParams = a.parseParams, Bn.parseFmtpConfig = a.parseFmtpConfig, Bn.parsePayloads = a.parsePayloads, Bn.parseRemoteCandidates = a.parseRemoteCandidates, Bn.parseImageAttributes = a.parseImageAttributes, Bn.parseSimulcastStreamList = a.parseSimulcastStreamList, Bn;
}
var An = cM();
const lM = 0.7, uM = 20, ga = {
  NegotiationStarted: "negotiationStarted",
  NegotiationComplete: "negotiationComplete",
  // Fired with the offerId for every successful publisher answer application,
  // including answers that immediately recurse into another offer via
  // `renegotiate`. Use this rather than NegotiationComplete to know that a
  // specific offer has been negotiated end-to-end.
  OfferAnswered: "offerAnswered",
  RTPVideoPayloadTypes: "rtpVideoPayloadTypes"
};
class Qb extends sn.EventEmitter {
  get pc() {
    return this._pc || (this._pc = this.createPC()), this._pc;
  }
  constructor(e) {
    let t = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    var i;
    super(), this.log = Ee, this.ddExtID = 0, this.latestOfferId = 0, this.latestAcknowledgedOfferId = 0, this.pendingCandidates = [], this.restartingIce = !1, this.renegotiate = !1, this.trackBitrates = [], this.remoteStereoMids = [], this.remoteNackMids = [], this.negotiate = bm((s) => R(this, void 0, void 0, function* () {
      this.emit(ga.NegotiationStarted);
      try {
        yield this.createAndSendOffer();
      } catch (c) {
        if (s)
          s(c);
        else
          throw c;
      }
    }), uM), this.close = () => {
      this._pc && (this.pendingInitialOffer = void 0, this._pc.close(), this._pc.onconnectionstatechange = null, this._pc.oniceconnectionstatechange = null, this._pc.onicegatheringstatechange = null, this._pc.ondatachannel = null, this._pc.onnegotiationneeded = null, this._pc.onsignalingstatechange = null, this._pc.onicecandidate = null, this._pc.ondatachannel = null, this._pc.ontrack = null, this._pc.onconnectionstatechange = null, this._pc.oniceconnectionstatechange = null, this._pc = null);
    }, this.log = rn((i = t.loggerName) !== null && i !== void 0 ? i : Mt.PCTransport), this.loggerOptions = t, this.config = e, this._pc = this.createPC(), this.offerLock = new Lt();
  }
  createPC() {
    const e = new RTCPeerConnection(this.config);
    return e.onicecandidate = (t) => {
      var i;
      t.candidate && ((i = this.onIceCandidate) === null || i === void 0 || i.call(this, t.candidate));
    }, e.onicecandidateerror = (t) => {
      var i;
      (i = this.onIceCandidateError) === null || i === void 0 || i.call(this, t);
    }, e.oniceconnectionstatechange = () => {
      var t;
      (t = this.onIceConnectionStateChange) === null || t === void 0 || t.call(this, e.iceConnectionState);
    }, e.onsignalingstatechange = () => {
      var t;
      (t = this.onSignalingStatechange) === null || t === void 0 || t.call(this, e.signalingState);
    }, e.onconnectionstatechange = () => {
      var t;
      (t = this.onConnectionStateChange) === null || t === void 0 || t.call(this, e.connectionState);
    }, e.ondatachannel = (t) => {
      var i;
      (i = this.onDataChannel) === null || i === void 0 || i.call(this, t);
    }, e.ontrack = (t) => {
      var i;
      (i = this.onTrack) === null || i === void 0 || i.call(this, t);
    }, e;
  }
  get logContext() {
    var e, t;
    return Object.assign({}, (t = (e = this.loggerOptions).loggerContextCb) === null || t === void 0 ? void 0 : t.call(e));
  }
  get isICEConnected() {
    return this._pc !== null && (this.pc.iceConnectionState === "connected" || this.pc.iceConnectionState === "completed");
  }
  addIceCandidate(e) {
    return R(this, void 0, void 0, function* () {
      if (this.pc.remoteDescription && !this.restartingIce)
        return this.pc.addIceCandidate(e);
      this.pendingCandidates.push(e);
    });
  }
  setRemoteDescription(e, t) {
    return R(this, void 0, void 0, function* () {
      var i, s;
      if (e.type === "answer" && this.latestOfferId > 0 && t > 0 && t !== this.latestOfferId)
        return this.log.warn("ignoring answer for old offer", Object.assign(Object.assign({}, this.logContext), {
          offerId: t,
          latestOfferId: this.latestOfferId
        })), !1;
      let c;
      if (e.type === "offer") {
        let u = fM(e), d = u.stereoMids, h = u.nackMids;
        this.remoteStereoMids = d, this.remoteNackMids = h;
      } else if (e.type === "answer") {
        if (this.pendingInitialOffer && this._pc) {
          const h = this.pendingInitialOffer;
          this.pendingInitialOffer = void 0;
          const f = An.parse((i = h.sdp) !== null && i !== void 0 ? i : "");
          f.media.forEach((p) => {
            ul(p);
          }), this.log.debug("setting pending initial offer before processing answer", this.logContext), yield this.setMungedSDP(h, An.write(f));
        }
        const u = An.parse((s = e.sdp) !== null && s !== void 0 ? s : "");
        u.media.forEach((h) => {
          const f = _o(h.mid);
          h.type === "audio" && this.trackBitrates.some((p) => {
            if (!p.transceiver || f != p.transceiver.mid)
              return !1;
            let v = 0;
            if (h.rtp.some((T) => T.codec.toUpperCase() === p.codec.toUpperCase() ? (v = T.payload, !0) : !1), v === 0)
              return !0;
            let y = !1;
            for (const T of h.fmtp)
              if (T.payload === v) {
                T.config = T.config.split(";").filter((_) => !_.includes("maxaveragebitrate")).join(";"), p.maxbr > 0 && (T.config += ";maxaveragebitrate=".concat(p.maxbr * 1e3)), y = !0;
                break;
              }
            return y || p.maxbr > 0 && h.fmtp.push({
              payload: v,
              config: "maxaveragebitrate=".concat(p.maxbr * 1e3)
            }), !0;
          });
        });
        const d = this.getPlaceholderMids();
        d.size > 0 && Zb(u.media, (h) => d.has(_o(h.mid))), c = An.write(u);
      }
      return yield this.setMungedSDP(e, c, !0), this.pendingCandidates.forEach((u) => {
        this.pc.addIceCandidate(u);
      }), this.pendingCandidates = [], this.restartingIce = !1, e.type === "answer" && (this.latestAcknowledgedOfferId = t, this.emit(ga.OfferAnswered, t)), this.renegotiate ? (this.renegotiate = !1, yield this.createAndSendOffer()) : e.type === "answer" && (this.emit(ga.NegotiationComplete), e.sdp && An.parse(e.sdp).media.forEach((d) => {
        d.type === "video" && this.emit(ga.RTPVideoPayloadTypes, d.rtp);
      })), !0;
    });
  }
  createInitialOffer() {
    return R(this, void 0, void 0, function* () {
      var e;
      const t = yield this.offerLock.lock();
      try {
        if (this.pc.signalingState !== "stable") {
          this.log.warn("signaling state is not stable, cannot create initial offer", this.logContext);
          return;
        }
        const i = this.latestOfferId + 1;
        this.latestOfferId = i;
        const s = yield this.pc.createOffer();
        this.pendingInitialOffer = {
          sdp: s.sdp,
          type: s.type
        };
        const c = An.parse((e = s.sdp) !== null && e !== void 0 ? e : "");
        return c.media.forEach((u) => {
          ul(u);
        }), s.sdp = An.write(c), {
          offer: s,
          offerId: i
        };
      } finally {
        t();
      }
    });
  }
  createAndSendOffer(e) {
    return R(this, void 0, void 0, function* () {
      var t;
      const i = yield this.offerLock.lock();
      try {
        if (this.onOffer === void 0)
          return;
        if (e?.iceRestart && (this.log.debug("restarting ICE", this.logContext), this.restartingIce = !0), this._pc && (this._pc.signalingState === "have-local-offer" || this.pendingInitialOffer)) {
          const h = this._pc.remoteDescription;
          if (e?.iceRestart && h)
            yield this._pc.setRemoteDescription(h);
          else {
            this.renegotiate = !0, this.log.debug("requesting renegotiation", Object.assign({}, this.logContext));
            return;
          }
        } else if (!this._pc || this._pc.signalingState === "closed") {
          this.log.warn("could not createOffer with closed peer connection", this.logContext);
          return;
        }
        this.log.debug("starting to negotiate", this.logContext);
        const s = this.latestOfferId + 1;
        this.latestOfferId = s;
        const c = yield this.pc.createOffer(e);
        this.log.debug("original offer", Object.assign({
          sdp: c.sdp
        }, this.logContext));
        const u = An.parse((t = c.sdp) !== null && t !== void 0 ? t : "");
        u.media.forEach((h) => {
          ul(h), h.type === "audio" ? Xb(h, ["all"], []) : h.type === "video" && this.trackBitrates.some((f) => {
            if (!h.msid || !f.cid || !h.msid.includes(f.cid))
              return !1;
            let p = 0;
            if (h.rtp.some((y) => y.codec.toUpperCase() === f.codec.toUpperCase() ? (p = y.payload, !0) : !1), p === 0 || (Vn(f.codec) && !sr() && this.ensureVideoDDExtensionForSVC(h, u), !Vn(f.codec)))
              return !0;
            const v = Math.round(f.maxbr * lM);
            for (const y of h.fmtp)
              if (y.payload === p) {
                y.config.includes("x-google-start-bitrate") || (y.config += ";x-google-start-bitrate=".concat(v));
                break;
              }
            return !0;
          });
        });
        const d = this.getPlaceholderMids();
        if (d.size > 0 && Zb(u.media, (h) => d.has(_o(h.mid))), this.latestOfferId > s) {
          this.log.warn("latestOfferId mismatch", Object.assign(Object.assign({}, this.logContext), {
            latestOfferId: this.latestOfferId,
            offerId: s
          }));
          return;
        }
        yield this.setMungedSDP(c, An.write(u)), this.onOffer(c, this.latestOfferId);
      } finally {
        i();
      }
    });
  }
  createAndSetAnswer() {
    return R(this, void 0, void 0, function* () {
      var e;
      const t = yield this.pc.createAnswer(), i = An.parse((e = t.sdp) !== null && e !== void 0 ? e : "");
      return i.media.forEach((s) => {
        ul(s), s.type === "audio" && Xb(s, this.remoteStereoMids, this.remoteNackMids);
      }), yield this.setMungedSDP(t, An.write(i)), t;
    });
  }
  /**
   * Returns the mids of transceivers that carry no outgoing track on this
   * (publisher) connection: the pre-populated placeholders added by
   * `RTCEngine.applyInitialPublisherLayout`, plus any transceiver that was used
   * for a track and reverted on unpublish. Their codec fmtp is conformed to the
   * published tracks so a shared payload type stays consistent across the bundle.
   */
  getPlaceholderMids() {
    var e, t;
    return dM((t = (e = this._pc) === null || e === void 0 ? void 0 : e.getTransceivers()) !== null && t !== void 0 ? t : []);
  }
  createDataChannel(e, t) {
    return this.pc.createDataChannel(e, t);
  }
  addTransceiver(e, t) {
    return this.pc.addTransceiver(e, t);
  }
  addTransceiverOfKind(e, t) {
    return this.pc.addTransceiver(e, t);
  }
  addTrack(e) {
    if (!this._pc)
      throw new rt("PC closed, cannot add track");
    return this._pc.addTrack(e);
  }
  setTrackCodecBitrate(e) {
    this.trackBitrates.push(e);
  }
  setConfiguration(e) {
    var t;
    if (!this._pc)
      throw new rt("PC closed, cannot configure");
    return (t = this._pc) === null || t === void 0 ? void 0 : t.setConfiguration(e);
  }
  canRemoveTrack() {
    var e;
    return !!(!((e = this._pc) === null || e === void 0) && e.removeTrack);
  }
  removeTrack(e) {
    var t;
    return (t = this._pc) === null || t === void 0 ? void 0 : t.removeTrack(e);
  }
  getConnectionState() {
    var e, t;
    return (t = (e = this._pc) === null || e === void 0 ? void 0 : e.connectionState) !== null && t !== void 0 ? t : "closed";
  }
  getICEConnectionState() {
    var e, t;
    return (t = (e = this._pc) === null || e === void 0 ? void 0 : e.iceConnectionState) !== null && t !== void 0 ? t : "closed";
  }
  getSignallingState() {
    var e, t;
    return (t = (e = this._pc) === null || e === void 0 ? void 0 : e.signalingState) !== null && t !== void 0 ? t : "closed";
  }
  getTransceivers() {
    var e, t;
    return (t = (e = this._pc) === null || e === void 0 ? void 0 : e.getTransceivers()) !== null && t !== void 0 ? t : [];
  }
  getSenders() {
    var e, t;
    return (t = (e = this._pc) === null || e === void 0 ? void 0 : e.getSenders()) !== null && t !== void 0 ? t : [];
  }
  getLocalDescription() {
    var e;
    return (e = this._pc) === null || e === void 0 ? void 0 : e.localDescription;
  }
  getRemoteDescription() {
    var e;
    return (e = this.pc) === null || e === void 0 ? void 0 : e.remoteDescription;
  }
  getStats() {
    return this.pc.getStats();
  }
  getMaxMessageSize() {
    var e, t;
    return (t = (e = this._pc) === null || e === void 0 ? void 0 : e.sctp) === null || t === void 0 ? void 0 : t.maxMessageSize;
  }
  getConnectedAddress() {
    return R(this, void 0, void 0, function* () {
      var e;
      if (!this._pc)
        return;
      let t = "";
      const i = /* @__PURE__ */ new Map(), s = /* @__PURE__ */ new Map();
      if ((yield this._pc.getStats()).forEach((d) => {
        switch (d.type) {
          case "transport":
            t = d.selectedCandidatePairId;
            break;
          case "candidate-pair":
            t === "" && d.selected && (t = d.id), i.set(d.id, d);
            break;
          case "remote-candidate":
            s.set(d.id, "".concat(d.address, ":").concat(d.port));
            break;
        }
      }), t === "")
        return;
      const u = (e = i.get(t)) === null || e === void 0 ? void 0 : e.remoteCandidateId;
      if (u !== void 0)
        return s.get(u);
    });
  }
  setMungedSDP(e, t, i) {
    return R(this, void 0, void 0, function* () {
      var s, c;
      const u = e.sdp;
      if (t) {
        e.sdp = t;
        try {
          this.log.debug("setting munged ".concat(i ? "remote" : "local", " description"), this.logContext), i ? yield this.pc.setRemoteDescription(e) : yield this.pc.setLocalDescription(e);
          return;
        } catch (d) {
          this.log.warn("not able to set ".concat(e.type, ", falling back to unmodified sdp"), Object.assign(Object.assign({}, this.logContext), {
            error: d,
            mungedSdp: t,
            originalSdp: u
          })), e.sdp = u;
        }
      }
      try {
        i ? yield (s = this._pc) === null || s === void 0 ? void 0 : s.setRemoteDescription(e) : yield (c = this._pc) === null || c === void 0 ? void 0 : c.setLocalDescription(e);
      } catch (d) {
        let h = "unknown error";
        d instanceof Error ? h = d.message : typeof d == "string" && (h = d);
        const f = {
          error: h,
          sdp: e.sdp
        };
        throw t && t !== u && (f.mungedSdp = t), !i && this.pc.remoteDescription && (f.remoteSdp = this.pc.remoteDescription), this.log.error("unable to set ".concat(e.type), Object.assign(Object.assign({}, this.logContext), {
          fields: f
        })), new er(h);
      }
    });
  }
  ensureVideoDDExtensionForSVC(e, t) {
    var i, s;
    if (!((i = e.ext) === null || i === void 0 ? void 0 : i.some((u) => u.uri === xb))) {
      if (this.ddExtID === 0) {
        let u = 0;
        t.media.forEach((d) => {
          var h;
          (h = d.ext) === null || h === void 0 || h.forEach((f) => {
            f.value > u && (u = f.value);
          });
        }), this.ddExtID = u + 1;
      }
      (s = e.ext) === null || s === void 0 || s.push({
        value: this.ddExtID,
        uri: xb
      });
    }
  }
}
function Xb(a, e, t) {
  const i = _o(a.mid);
  let s = 0;
  a.rtp.some((c) => c.codec === "opus" ? (s = c.payload, !0) : !1), s > 0 && (a.rtcpFb || (a.rtcpFb = []), t.includes(i) && !a.rtcpFb.some((c) => c.payload === s && c.type === "nack") && a.rtcpFb.push({
    payload: s,
    type: "nack"
  }), (e.includes(i) || e.length === 1 && e[0] === "all") && a.fmtp.some((c) => c.payload === s ? (c.config.includes("stereo=1") || (c.config += ";stereo=1"), !0) : !1));
}
function dM(a) {
  const e = /* @__PURE__ */ new Set();
  for (const t of a)
    t.mid && !t.sender.track && e.add(t.mid);
  return e;
}
function Zb(a, e) {
  var t, i;
  const s = /* @__PURE__ */ new Map(), c = /* @__PURE__ */ new Set();
  for (const u of a) {
    const d = e(u);
    for (const h of (t = u.fmtp) !== null && t !== void 0 ? t : [])
      d ? s.has(h.payload) || s.set(h.payload, h.config) : (s.set(h.payload, h.config), c.add(h.payload));
  }
  if (s.size !== 0) {
    for (const u of a)
      if (e(u))
        for (const d of (i = u.fmtp) !== null && i !== void 0 ? i : []) {
          const h = s.get(d.payload);
          h !== void 0 && d.config !== h && (d.config = h);
        }
  }
}
function fM(a) {
  var e;
  const t = [], i = [], s = An.parse((e = a.sdp) !== null && e !== void 0 ? e : "");
  let c = 0;
  return s.media.forEach((u) => {
    var d;
    const h = _o(u.mid);
    u.type === "audio" && (u.rtp.some((f) => f.codec === "opus" ? (c = f.payload, !0) : !1), !((d = u.rtcpFb) === null || d === void 0) && d.some((f) => f.payload === c && f.type === "nack") && i.push(h), u.fmtp.some((f) => f.payload === c ? (f.config.includes("sprop-stereo=1") && t.push(h), !0) : !1));
  }), {
    stereoMids: t,
    nackMids: i
  };
}
function ul(a) {
  if (a.connection) {
    const e = a.connection.ip.indexOf(":") >= 0;
    (a.connection.version === 4 && e || a.connection.version === 6 && !e) && (a.connection.ip = "0.0.0.0", a.connection.version = 4);
  }
}
function _o(a) {
  return typeof a == "number" ? a.toFixed(0) : a;
}
const jh = "vp8", hM = {
  audioPreset: Dh.music,
  dtx: !0,
  red: !0,
  forceStereo: !1,
  simulcast: !0,
  screenShareEncoding: pm.h1080fps15.encoding,
  stopMicTrackOnMute: !1,
  videoCodec: jh,
  backupCodec: !0,
  preConnectBuffer: !1
}, DS = {
  deviceId: {
    ideal: "default"
  },
  autoGainControl: !0,
  echoCancellation: !0,
  noiseSuppression: !0,
  voiceIsolation: !0
}, PS = {
  deviceId: {
    ideal: "default"
  },
  resolution: Do.h720.resolution
}, mM = {
  adaptiveStream: !1,
  dynacast: !1,
  stopLocalTrackOnUnpublish: !0,
  reconnectPolicy: new M1(),
  disconnectOnPageLeave: !0,
  webAudioMix: !1,
  singlePeerConnection: !0
}, Rm = {
  autoSubscribe: !0,
  maxRetries: 1,
  peerConnectionTimeout: 15e3,
  websocketTimeout: 15e3
};
var st;
(function(a) {
  a[a.NEW = 0] = "NEW", a[a.CONNECTING = 1] = "CONNECTING", a[a.CONNECTED = 2] = "CONNECTED", a[a.FAILED = 3] = "FAILED", a[a.CLOSING = 4] = "CLOSING", a[a.CLOSED = 5] = "CLOSED";
})(st || (st = {}));
class Wb {
  get needsPublisher() {
    return this.isPublisherConnectionRequired;
  }
  get needsSubscriber() {
    return this.isSubscriberConnectionRequired;
  }
  get currentState() {
    return this.state;
  }
  get mode() {
    return this._mode;
  }
  constructor(e, t, i) {
    var s;
    this.peerConnectionTimeout = Rm.peerConnectionTimeout, this.log = Ee, this.updateState = () => {
      var c, u;
      const d = this.state, h = this.requiredTransports.map((f) => f.getConnectionState());
      h.every((f) => f === "connected") ? this.state = st.CONNECTED : h.some((f) => f === "failed") ? this.state = st.FAILED : h.some((f) => f === "connecting") ? this.state = st.CONNECTING : h.every((f) => f === "closed") ? this.state = st.CLOSED : h.some((f) => f === "closed") ? this.state = st.CLOSING : h.every((f) => f === "new") && (this.state = st.NEW), d !== this.state && (this.log.debug("pc state change: from ".concat(st[d], " to ").concat(st[this.state]), this.logContext), (c = this.onStateChange) === null || c === void 0 || c.call(this, this.state, this.publisher.getConnectionState(), (u = this.subscriber) === null || u === void 0 ? void 0 : u.getConnectionState()));
    }, this.log = rn((s = t.loggerName) !== null && s !== void 0 ? s : Mt.PCManager), this.loggerOptions = t, this.isPublisherConnectionRequired = e !== "subscriber-primary", this.isSubscriberConnectionRequired = e === "subscriber-primary", this.publisher = new Qb(i, t), this._mode = e, e !== "publisher-only" && (this.subscriber = new Qb(i, t), this.subscriber.onConnectionStateChange = this.updateState, this.subscriber.onIceConnectionStateChange = this.updateState, this.subscriber.onSignalingStatechange = this.updateState, this.subscriber.onIceCandidate = (c) => {
      var u;
      (u = this.onIceCandidate) === null || u === void 0 || u.call(this, c, qn.SUBSCRIBER);
    }, this.subscriber.onDataChannel = (c) => {
      var u;
      (u = this.onDataChannel) === null || u === void 0 || u.call(this, c);
    }, this.subscriber.onTrack = (c) => {
      var u;
      (u = this.onTrack) === null || u === void 0 || u.call(this, c);
    }), this.publisher.onConnectionStateChange = this.updateState, this.publisher.onIceConnectionStateChange = this.updateState, this.publisher.onSignalingStatechange = this.updateState, this.publisher.onIceCandidate = (c) => {
      var u;
      (u = this.onIceCandidate) === null || u === void 0 || u.call(this, c, qn.PUBLISHER);
    }, this.publisher.onTrack = (c) => {
      var u;
      (u = this.onTrack) === null || u === void 0 || u.call(this, c);
    }, this.publisher.onOffer = (c, u) => {
      var d;
      (d = this.onPublisherOffer) === null || d === void 0 || d.call(this, c, u);
    }, this.state = st.NEW, this.connectionLock = new Lt(), this.remoteOfferLock = new Lt();
  }
  get logContext() {
    var e, t;
    return Object.assign({}, (t = (e = this.loggerOptions).loggerContextCb) === null || t === void 0 ? void 0 : t.call(e));
  }
  requirePublisher() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0;
    this.isPublisherConnectionRequired = e, this.updateState();
  }
  createAndSendPublisherOffer(e) {
    return this.publisher.createAndSendOffer(e);
  }
  setPublisherAnswer(e, t) {
    return this.publisher.setRemoteDescription(e, t);
  }
  removeTrack(e) {
    return this.publisher.removeTrack(e);
  }
  close() {
    return R(this, void 0, void 0, function* () {
      var e;
      if (this.publisher && this.publisher.getSignallingState() !== "closed") {
        const t = this.publisher;
        for (const i of t.getSenders())
          try {
            t.canRemoveTrack() && t.removeTrack(i);
          } catch (s) {
            this.log.warn("could not removeTrack", Object.assign(Object.assign({}, this.logContext), {
              error: s
            }));
          }
      }
      yield Promise.all([this.publisher.close(), (e = this.subscriber) === null || e === void 0 ? void 0 : e.close()]), this.updateState();
    });
  }
  triggerIceRestart() {
    return R(this, void 0, void 0, function* () {
      this.subscriber && (this.subscriber.restartingIce = !0), this.needsPublisher && (yield this.createAndSendPublisherOffer({
        iceRestart: !0
      }));
    });
  }
  addIceCandidate(e, t) {
    return R(this, void 0, void 0, function* () {
      var i;
      t === qn.PUBLISHER ? yield this.publisher.addIceCandidate(e) : yield (i = this.subscriber) === null || i === void 0 ? void 0 : i.addIceCandidate(e);
    });
  }
  createSubscriberAnswerFromOffer(e, t) {
    return R(this, void 0, void 0, function* () {
      var i, s, c;
      this.log.debug("received server offer", Object.assign(Object.assign({}, this.logContext), {
        RTCSdpType: e.type,
        sdp: e.sdp,
        signalingState: (i = this.subscriber) === null || i === void 0 ? void 0 : i.getSignallingState().toString()
      }));
      const u = yield this.remoteOfferLock.lock();
      try {
        return (yield (s = this.subscriber) === null || s === void 0 ? void 0 : s.setRemoteDescription(e, t)) ? yield (c = this.subscriber) === null || c === void 0 ? void 0 : c.createAndSetAnswer() : void 0;
      } finally {
        u();
      }
    });
  }
  updateConfiguration(e, t) {
    var i;
    this.publisher.setConfiguration(e), (i = this.subscriber) === null || i === void 0 || i.setConfiguration(e), t && this.triggerIceRestart();
  }
  ensurePCTransportConnection(e, t) {
    return R(this, void 0, void 0, function* () {
      var i;
      const s = yield this.connectionLock.lock();
      try {
        this.isPublisherConnectionRequired && this.publisher.getConnectionState() !== "connected" && this.publisher.getConnectionState() !== "connecting" && (this.log.debug("negotiation required, start negotiating", this.logContext), this.publisher.negotiate()), yield Promise.all((i = this.requiredTransports) === null || i === void 0 ? void 0 : i.map((c) => this.ensureTransportConnected(c, e, t)));
      } finally {
        s();
      }
    });
  }
  negotiate(e) {
    return R(this, void 0, void 0, function* () {
      return new Gt((t, i) => {
        const s = this.publisher.latestOfferId;
        if (this.publisher.latestAcknowledgedOfferId > s) {
          this.log.debug("negotiation already handled in more recent acknowledged offer", this.logContext), t();
          return;
        }
        let c = !1;
        const u = () => {
          c || (c = !0, clearTimeout(f), this.publisher.off(ga.OfferAnswered, d), e.signal.removeEventListener("abort", h));
        }, d = (p) => {
          p > s && (u(), t());
        }, h = () => {
          u(), i(new er("negotiation aborted"));
        }, f = setTimeout(() => {
          u(), i(new er("negotiation timed out"));
        }, this.peerConnectionTimeout);
        e.signal.addEventListener("abort", h), this.publisher.on(ga.OfferAnswered, d), this.publisher.negotiate((p) => {
          u(), p instanceof Error ? i(p) : i(new Error(String(p)));
        });
      });
    });
  }
  addPublisherTransceiver(e, t) {
    return this.publisher.addTransceiver(e, t);
  }
  addPublisherTransceiverOfKind(e, t) {
    return this.publisher.addTransceiverOfKind(e, t);
  }
  getMidForReceiver(e) {
    const i = (this.subscriber ? this.subscriber.getTransceivers() : this.publisher.getTransceivers()).find((s) => s.receiver === e);
    return i?.mid;
  }
  getMaxPublisherMessageSize() {
    return this.publisher.getMaxMessageSize();
  }
  addPublisherTrack(e) {
    return this.publisher.addTrack(e);
  }
  createPublisherDataChannel(e, t) {
    return this.publisher.createDataChannel(e, t);
  }
  /**
   * Returns the first required transport's address if no explicit target is specified
   */
  getConnectedAddress(e) {
    return e === qn.PUBLISHER ? this.publisher.getConnectedAddress() : e === qn.SUBSCRIBER ? this.publisher.getConnectedAddress() : this.requiredTransports[0].getConnectedAddress();
  }
  get requiredTransports() {
    const e = [];
    return this.isPublisherConnectionRequired && e.push(this.publisher), this.isSubscriberConnectionRequired && this.subscriber && e.push(this.subscriber), e;
  }
  ensureTransportConnected(e, t) {
    return R(this, arguments, void 0, function(i, s) {
      var c = this;
      let u = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : this.peerConnectionTimeout;
      return (function* () {
        if (i.getConnectionState() !== "connected")
          return new Promise((h, f) => R(c, void 0, void 0, function* () {
            const p = () => {
              this.log.warn("abort transport connection", this.logContext), bt.clearTimeout(v), f(ye.cancelled("room connection has been cancelled"));
            };
            s?.signal.aborted && p(), s?.signal.addEventListener("abort", p);
            const v = bt.setTimeout(() => {
              s?.signal.removeEventListener("abort", p), f(ye.internal("could not establish pc connection"));
            }, u);
            for (; this.state !== st.CONNECTED; )
              if (yield zt(50), s?.signal.aborted) {
                f(ye.cancelled("room connection has been cancelled"));
                return;
              }
            bt.clearTimeout(v), s?.signal.removeEventListener("abort", p), h();
          }));
      })();
    });
  }
}
const wm = typeof MediaRecorder < "u";
class pM {
  constructor() {
    throw new Error("MediaRecorder is not available in this environment");
  }
}
const gM = wm ? MediaRecorder : pM;
class vM extends gM {
  constructor(e, t) {
    if (!wm)
      throw new Error("MediaRecorder is not available in this environment");
    super(new MediaStream([e.mediaStreamTrack]), t);
    let i, s;
    const c = () => s === void 0, u = () => {
      this.removeEventListener("dataavailable", i), this.removeEventListener("stop", u), this.removeEventListener("error", d), s?.close(), s = void 0;
    }, d = (h) => {
      s?.error(h), this.removeEventListener("dataavailable", i), this.removeEventListener("stop", u), this.removeEventListener("error", d), s = void 0;
    };
    this.byteStream = new ReadableStream({
      start: (h) => {
        s = h, i = (f) => R(this, void 0, void 0, function* () {
          let p;
          if (f.data.arrayBuffer) {
            const v = yield f.data.arrayBuffer();
            p = new Uint8Array(v);
          } else if (f.data.byteArray)
            p = f.data.byteArray;
          else
            throw new Error("no data available!");
          c() || h.enqueue(p);
        }), this.addEventListener("dataavailable", i);
      },
      cancel: () => {
        u();
      }
    }), this.addEventListener("stop", u), this.addEventListener("error", d);
  }
}
function yM() {
  return wm;
}
const bM = 1e3, TM = 1e4;
class xS extends H {
  /** @internal */
  get sender() {
    return this._sender;
  }
  /** @internal */
  set sender(e) {
    this._sender = e;
  }
  get constraints() {
    return this._constraints;
  }
  get hasPreConnectBuffer() {
    return !!this.localTrackRecorder;
  }
  /**
   *
   * @param mediaTrack
   * @param kind
   * @param constraints MediaTrackConstraints that are being used when restarting or reacquiring tracks
   * @param userProvidedTrack Signals to the SDK whether or not the mediaTrack should be managed (i.e. released and reacquired) internally by the SDK
   */
  constructor(e, t, i) {
    let s = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : !1, c = arguments.length > 4 ? arguments[4] : void 0;
    super(e, t, c), this.manuallyStopped = !1, this.pendingDeviceChange = !1, this._isUpstreamPaused = !1, this.handleTrackMuteEvent = () => this.debouncedTrackMuteHandler().catch(() => this.log.debug("track mute bounce got cancelled by an unmute event", this.logContext)), this.debouncedTrackMuteHandler = bm(() => R(this, void 0, void 0, function* () {
      yield this.pauseUpstream();
    }), 5e3), this.handleTrackUnmuteEvent = () => R(this, void 0, void 0, function* () {
      this.debouncedTrackMuteHandler.cancel("unmute"), yield this.resumeUpstream();
    }), this.handleEnded = () => {
      this.isInBackground && (this.reacquireTrack = !0), this._mediaStreamTrack.removeEventListener("mute", this.handleTrackMuteEvent), this._mediaStreamTrack.removeEventListener("unmute", this.handleTrackUnmuteEvent), this.emit(re.Ended, this);
    }, this.reacquireTrack = !1, this.providedByUser = s, this.muteLock = new Lt(), this.pauseUpstreamLock = new Lt(), this.trackChangeLock = new Lt(), this.trackChangeLock.lock().then((u) => R(this, void 0, void 0, function* () {
      try {
        yield this.setMediaStreamTrack(e, !0);
      } finally {
        u();
      }
    })), this._constraints = e.getConstraints(), i && (this._constraints = i);
  }
  get id() {
    return this._mediaStreamTrack.id;
  }
  get dimensions() {
    if (this.kind !== H.Kind.Video)
      return;
    const e = this._mediaStreamTrack.getSettings(), t = e.width, i = e.height;
    if (t && i)
      return {
        width: t,
        height: i
      };
  }
  get isUpstreamPaused() {
    return this._isUpstreamPaused;
  }
  get isUserProvided() {
    return this.providedByUser;
  }
  get mediaStreamTrack() {
    var e, t;
    return (t = (e = this.processor) === null || e === void 0 ? void 0 : e.processedTrack) !== null && t !== void 0 ? t : this._mediaStreamTrack;
  }
  get isLocal() {
    return !0;
  }
  /**
   * @internal
   * returns mediaStreamTrack settings of the capturing mediastreamtrack source - ignoring processors
   */
  getSourceTrackSettings() {
    return this._mediaStreamTrack.getSettings();
  }
  setMediaStreamTrack(e, t, i) {
    return R(this, void 0, void 0, function* () {
      var s;
      if (e === this._mediaStreamTrack && !t)
        return;
      this._mediaStreamTrack && (this.attachedElements.forEach((u) => {
        ns(this._mediaStreamTrack, u);
      }), this.debouncedTrackMuteHandler.cancel("new-track"), this._mediaStreamTrack.removeEventListener("ended", this.handleEnded), this._mediaStreamTrack.removeEventListener("mute", this.handleTrackMuteEvent), this._mediaStreamTrack.removeEventListener("unmute", this.handleTrackUnmuteEvent)), this.mediaStream = new MediaStream([e]), e && (e.addEventListener("ended", this.handleEnded), e.addEventListener("mute", this.handleTrackMuteEvent), e.addEventListener("unmute", this.handleTrackUnmuteEvent), this._constraints = e.getConstraints());
      let c;
      if (this.processor && e) {
        if (this.log.debug("restarting processor", this.logContext), this.kind === "unknown")
          throw TypeError("cannot set processor on track of unknown kind");
        this.processorElement && (Zr(e, this.processorElement), this.processorElement.muted = !0), yield this.processor.restart({
          track: e,
          kind: this.kind,
          element: this.processorElement,
          localTrack: this
        }), c = this.processor.processedTrack;
      }
      this.sender && ((s = this.sender.transport) === null || s === void 0 ? void 0 : s.state) !== "closed" && (yield this.sender.replaceTrack(c ?? e)), !this.providedByUser && this._mediaStreamTrack !== e && this._mediaStreamTrack.stop(), this._mediaStreamTrack = e, e && (this._mediaStreamTrack.enabled = i ? !0 : !this.isMuted, yield this.resumeUpstream(), this.attachedElements.forEach((u) => {
        Zr(c ?? e, u);
      }));
    });
  }
  waitForDimensions() {
    return R(this, arguments, void 0, function() {
      var e = this;
      let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : bM;
      return (function* () {
        var i;
        if (e.kind === H.Kind.Audio)
          throw new Error("cannot get dimensions for audio tracks");
        ((i = Kt()) === null || i === void 0 ? void 0 : i.os) === "iOS" && (yield zt(10));
        const s = Date.now();
        for (; Date.now() - s < t; ) {
          const c = e.dimensions;
          if (c)
            return c;
          yield zt(50);
        }
        throw new Ni("unable to get track dimensions after timeout");
      })();
    });
  }
  setDeviceId(e) {
    return R(this, void 0, void 0, function* () {
      return this._constraints.deviceId === e && this._mediaStreamTrack.getSettings().deviceId === tr(e) ? !0 : (this._constraints.deviceId = e, this.isMuted ? (this.pendingDeviceChange = !0, !0) : (yield this.restartTrack(), tr(e) === this._mediaStreamTrack.getSettings().deviceId));
    });
  }
  /**
   * @returns DeviceID of the device that is currently being used for this track
   */
  getDeviceId() {
    return R(this, arguments, void 0, function() {
      var e = this;
      let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0;
      return (function* () {
        if (e.source === H.Source.ScreenShare)
          return;
        const i = e._mediaStreamTrack.getSettings(), s = i.deviceId, c = i.groupId, u = e.kind === H.Kind.Audio ? "audioinput" : "videoinput";
        return t ? Rt.getInstance().normalizeDeviceId(u, s, c) : s;
      })();
    });
  }
  mute() {
    return R(this, void 0, void 0, function* () {
      return this.setTrackMuted(!0), this;
    });
  }
  unmute() {
    return R(this, void 0, void 0, function* () {
      return this.setTrackMuted(!1), this;
    });
  }
  replaceTrack(e, t) {
    return R(this, void 0, void 0, function* () {
      const i = yield this.trackChangeLock.lock();
      try {
        if (!this.sender)
          throw new Ni("unable to replace an unpublished track");
        let s, c;
        typeof t == "boolean" ? s = t : t !== void 0 && (s = t.userProvidedTrack, c = t.stopProcessor), this.providedByUser = s ?? !0, this.log.debug("replace MediaStreamTrack", this.logContext), yield this.setMediaStreamTrack(e), c && this.processor && (yield this.internalStopProcessor());
      } finally {
        i();
      }
      return yield this.onSenderTrackSwapped(), this;
    });
  }
  /**
   * Hook invoked after the MediaStreamTrack on the sender has been swapped
   * (via replaceTrack, setProcessor, or stopProcessor). Fires outside the
   * trackChangeLock so subclasses can do asynchronous work such as polling
   * for new dimensions without blocking other track operations.
   */
  onSenderTrackSwapped() {
    return R(this, void 0, void 0, function* () {
    });
  }
  restart(e, t) {
    return R(this, void 0, void 0, function* () {
      this.manuallyStopped = !1;
      const i = yield this.trackChangeLock.lock();
      try {
        e || (e = this._constraints);
        const s = e, c = s.deviceId, u = s.facingMode, d = BT(e, ["deviceId", "facingMode"]);
        this.log.debug("restarting track with constraints", Object.assign(Object.assign({}, this.logContext), {
          constraints: e
        }));
        const h = {
          audio: !1,
          video: !1
        };
        this.kind === H.Kind.Video ? h.video = c || u ? {
          deviceId: c,
          facingMode: u
        } : !0 : h.audio = c ? Object.assign({
          deviceId: c
        }, d) : !0, this.attachedElements.forEach((v) => {
          ns(this.mediaStreamTrack, v);
        }), this._mediaStreamTrack.removeEventListener("ended", this.handleEnded), this._mediaStreamTrack.stop();
        const p = (yield navigator.mediaDevices.getUserMedia(h)).getTracks()[0];
        return this.kind === H.Kind.Video && (yield p.applyConstraints(d)), p.addEventListener("ended", this.handleEnded), this.log.debug("re-acquired MediaStreamTrack", this.logContext), yield this.setMediaStreamTrack(p, !1, t), this._constraints = e, this.pendingDeviceChange = !1, this.emit(re.Restarted, this), this.manuallyStopped && (this.log.warn("track was stopped during a restart, stopping restarted track", this.logContext), this.stop()), this;
      } finally {
        i();
      }
    });
  }
  setTrackMuted(e) {
    this.log.debug("setting ".concat(this.kind, " track ").concat(e ? "muted" : "unmuted"), this.logContext), !(this.isMuted === e && this._mediaStreamTrack.enabled !== e) && (this.isMuted = e, this._mediaStreamTrack.enabled = !e, this.emit(e ? re.Muted : re.Unmuted, this));
  }
  get needsReAcquisition() {
    return this._mediaStreamTrack.readyState !== "live" || this._mediaStreamTrack.muted || !this._mediaStreamTrack.enabled || this.reacquireTrack;
  }
  handleAppVisibilityChanged() {
    const e = Object.create(null, {
      handleAppVisibilityChanged: {
        get: () => super.handleAppVisibilityChanged
      }
    });
    return R(this, void 0, void 0, function* () {
      yield e.handleAppVisibilityChanged.call(this), ES() && (this.log.debug("visibility changed, is in Background: ".concat(this.isInBackground), this.logContext), !this.isInBackground && this.needsReAcquisition && !this.isUserProvided && !this.isMuted && (this.log.debug("track needs to be reacquired, restarting ".concat(this.source), this.logContext), yield this.restart(), this.reacquireTrack = !1));
    });
  }
  stop() {
    var e;
    this.manuallyStopped = !0, super.stop(), this._mediaStreamTrack.removeEventListener("ended", this.handleEnded), this._mediaStreamTrack.removeEventListener("mute", this.handleTrackMuteEvent), this._mediaStreamTrack.removeEventListener("unmute", this.handleTrackUnmuteEvent), (e = this.processor) === null || e === void 0 || e.destroy(), this.processor = void 0;
  }
  /**
   * pauses publishing to the server without disabling the local MediaStreamTrack
   * this is used to display a user's own video locally while pausing publishing to
   * the server.
   * this API is unsupported on Safari < 12 due to a bug
   **/
  pauseUpstream() {
    return R(this, void 0, void 0, function* () {
      var e;
      const t = yield this.pauseUpstreamLock.lock();
      try {
        if (this._isUpstreamPaused === !0)
          return;
        if (!this.sender) {
          this.log.warn("unable to pause upstream for an unpublished track", this.logContext);
          return;
        }
        this._isUpstreamPaused = !0, this.emit(re.UpstreamPaused, this);
        const i = Kt();
        if (i?.name === "Safari" && Xn(i.version, "12.0") < 0)
          throw new mm("pauseUpstream is not supported on Safari < 12.");
        ((e = this.sender.transport) === null || e === void 0 ? void 0 : e.state) !== "closed" && (yield this.sender.replaceTrack(null));
      } finally {
        t();
      }
    });
  }
  resumeUpstream() {
    return R(this, void 0, void 0, function* () {
      var e;
      const t = yield this.pauseUpstreamLock.lock();
      try {
        if (this._isUpstreamPaused === !1)
          return;
        if (!this.sender) {
          this.log.warn("unable to resume upstream for an unpublished track", this.logContext);
          return;
        }
        this._isUpstreamPaused = !1, this.emit(re.UpstreamResumed, this), ((e = this.sender.transport) === null || e === void 0 ? void 0 : e.state) !== "closed" && (yield this.sender.replaceTrack(this.mediaStreamTrack));
      } finally {
        t();
      }
    });
  }
  /**
   * Gets the RTCStatsReport for the LocalTrack's underlying RTCRtpSender
   * See https://developer.mozilla.org/en-US/docs/Web/API/RTCStatsReport
   *
   * @returns Promise<RTCStatsReport> | undefined
   */
  getRTCStatsReport() {
    return R(this, void 0, void 0, function* () {
      var e;
      return !((e = this.sender) === null || e === void 0) && e.getStats ? yield this.sender.getStats() : void 0;
    });
  }
  /**
   * Sets a processor on this track.
   * See https://github.com/livekit/track-processors-js for example usage
   *
   * @param processor
   * @param showProcessedStreamLocally
   * @returns
   */
  setProcessor(e) {
    return R(this, arguments, void 0, function(t) {
      var i = this;
      let s = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0;
      return (function* () {
        var c;
        const u = yield i.trackChangeLock.lock();
        try {
          i.log.debug("setting up processor", i.logContext);
          const d = document.createElement(i.kind), h = {
            kind: i.kind,
            track: i._mediaStreamTrack,
            element: d,
            audioContext: i.audioContext,
            localTrack: i
          };
          if (yield t.init(h), i.log.debug("processor initialized", i.logContext), i.processor && (yield i.internalStopProcessor()), i.kind === "unknown")
            throw TypeError("cannot set processor on track of unknown kind");
          if (Zr(i._mediaStreamTrack, d), d.muted = !0, d.play().catch((f) => {
            f instanceof DOMException && f.name === "AbortError" ? (i.log.warn("failed to play processor element, retrying", Object.assign(Object.assign({}, i.logContext), {
              error: f
            })), setTimeout(() => {
              d.play().catch((p) => {
                i.log.error("failed to play processor element", Object.assign(Object.assign({}, i.logContext), {
                  err: p
                }));
              });
            }, 100)) : i.log.error("failed to play processor element", Object.assign(Object.assign({}, i.logContext), {
              error: f
            }));
          }), i.processor = t, i.processorElement = d, i.processor.processedTrack) {
            for (const f of i.attachedElements)
              f !== i.processorElement && s && (ns(i._mediaStreamTrack, f), Zr(i.processor.processedTrack, f));
            yield (c = i.sender) === null || c === void 0 ? void 0 : c.replaceTrack(i.processor.processedTrack);
          }
          i.emit(re.TrackProcessorUpdate, i.processor);
        } finally {
          u();
        }
        yield i.onSenderTrackSwapped();
      })();
    });
  }
  getProcessor() {
    return this.processor;
  }
  /**
   * Stops the track processor
   * See https://github.com/livekit/track-processors-js for example usage
   *
   */
  stopProcessor() {
    return R(this, arguments, void 0, function() {
      var e = this;
      let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0;
      return (function* () {
        const i = yield e.trackChangeLock.lock();
        try {
          yield e.internalStopProcessor(t);
        } finally {
          i();
        }
        yield e.onSenderTrackSwapped();
      })();
    });
  }
  /**
   * @internal
   * This method assumes the caller has acquired a trackChangeLock already.
   * The public facing method for stopping the processor is `stopProcessor` and it wraps this method in the trackChangeLock.
   */
  internalStopProcessor() {
    return R(this, arguments, void 0, function() {
      var e = this;
      let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0;
      return (function* () {
        var i, s;
        e.processor && (e.log.debug("stopping processor", e.logContext), (i = e.processor.processedTrack) === null || i === void 0 || i.stop(), yield e.processor.destroy(), e.processor = void 0, t || ((s = e.processorElement) === null || s === void 0 || s.remove(), e.processorElement = void 0), yield e._mediaStreamTrack.applyConstraints(e._constraints), yield e.setMediaStreamTrack(e._mediaStreamTrack, !0), e.emit(re.TrackProcessorUpdate));
      })();
    });
  }
  /** @internal */
  startPreConnectBuffer() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 100;
    if (!yM()) {
      this.log.warn("MediaRecorder is not available, cannot start preconnect buffer", this.logContext);
      return;
    }
    if (this.localTrackRecorder) {
      this.log.warn("preconnect buffer already started");
      return;
    } else {
      let t = "audio/webm;codecs=opus";
      MediaRecorder.isTypeSupported(t) || (t = "video/mp4"), this.localTrackRecorder = new vM(this, {
        mimeType: t
      });
    }
    this.localTrackRecorder.start(e), this.autoStopPreConnectBuffer = setTimeout(() => {
      this.log.warn("preconnect buffer timed out, stopping recording automatically", this.logContext), this.stopPreConnectBuffer();
    }, TM);
  }
  /** @internal */
  stopPreConnectBuffer() {
    clearTimeout(this.autoStopPreConnectBuffer), this.localTrackRecorder && (this.localTrackRecorder.stop(), this.localTrackRecorder = void 0);
  }
  /** @internal */
  getPreConnectBuffer() {
    var e;
    return (e = this.localTrackRecorder) === null || e === void 0 ? void 0 : e.byteStream;
  }
  getPreConnectBufferMimeType() {
    var e;
    return (e = this.localTrackRecorder) === null || e === void 0 ? void 0 : e.mimeType;
  }
}
class Hl extends xS {
  /**
   * boolean indicating whether enhanced noise cancellation is currently being used on this track
   */
  get enhancedNoiseCancellation() {
    return this.isKrispNoiseFilterEnabled;
  }
  /**
   *
   * @param mediaTrack
   * @param constraints MediaTrackConstraints that are being used when restarting or reacquiring tracks
   * @param userProvidedTrack Signals to the SDK whether or not the mediaTrack should be managed (i.e. released and reacquired) internally by the SDK
   */
  constructor(e, t) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0, s = arguments.length > 3 ? arguments[3] : void 0, c = arguments.length > 4 ? arguments[4] : void 0;
    super(e, H.Kind.Audio, t, i, c), this.stopOnMute = !1, this.isKrispNoiseFilterEnabled = !1, this.monitorSender = () => R(this, void 0, void 0, function* () {
      if (!this.sender) {
        this._currentBitrate = 0;
        return;
      }
      let u;
      try {
        u = yield this.getSenderStats();
      } catch (d) {
        this.log.error("could not get audio sender stats", Object.assign(Object.assign({}, this.logContext), {
          error: d
        }));
        return;
      }
      u && this.prevStats && (this._currentBitrate = du(u, this.prevStats)), this.prevStats = u;
    }), this.handleKrispNoiseFilterEnable = () => {
      this.isKrispNoiseFilterEnabled = !0, this.log.debug("Krisp noise filter enabled", this.logContext), this.emit(re.AudioTrackFeatureUpdate, this, _t.TF_ENHANCED_NOISE_CANCELLATION, !0);
    }, this.handleKrispNoiseFilterDisable = () => {
      this.isKrispNoiseFilterEnabled = !1, this.log.debug("Krisp noise filter disabled", this.logContext), this.emit(re.AudioTrackFeatureUpdate, this, _t.TF_ENHANCED_NOISE_CANCELLATION, !1);
    }, this.audioContext = s, this.checkForSilence();
  }
  mute() {
    const e = Object.create(null, {
      mute: {
        get: () => super.mute
      }
    });
    return R(this, void 0, void 0, function* () {
      const t = yield this.muteLock.lock();
      try {
        return this.isMuted ? (this.log.debug("Track already muted", this.logContext), this) : (this.source === H.Source.Microphone && this.stopOnMute && !this.isUserProvided && (this.log.debug("stopping mic track", this.logContext), this._mediaStreamTrack.stop()), yield e.mute.call(this), this);
      } finally {
        t();
      }
    });
  }
  unmute() {
    const e = Object.create(null, {
      unmute: {
        get: () => super.unmute
      }
    });
    return R(this, void 0, void 0, function* () {
      const t = yield this.muteLock.lock();
      try {
        return this.isMuted ? (this.source === H.Source.Microphone && (this.stopOnMute || this._mediaStreamTrack.readyState === "ended" || this.pendingDeviceChange) && !this.isUserProvided && (this.log.debug("reacquiring mic track", this.logContext), yield this.restart(void 0, !0)), yield e.unmute.call(this), this) : (this.log.debug("Track already unmuted", this.logContext), this);
      } finally {
        t();
      }
    });
  }
  restartTrack(e) {
    return R(this, void 0, void 0, function* () {
      let t;
      if (e) {
        const i = gm({
          audio: e
        });
        typeof i.audio != "boolean" && (t = i.audio);
      }
      yield this.restart(t);
    });
  }
  restart(e, t) {
    const i = Object.create(null, {
      restart: {
        get: () => super.restart
      }
    });
    return R(this, void 0, void 0, function* () {
      const s = yield i.restart.call(this, e, t);
      return this.checkForSilence(), s;
    });
  }
  /* @internal */
  startMonitor() {
    an() && (this.monitorInterval || (this.monitorInterval = setInterval(() => {
      this.monitorSender();
    }, Tm)));
  }
  setProcessor(e) {
    return R(this, void 0, void 0, function* () {
      var t;
      const i = yield this.trackChangeLock.lock();
      try {
        if (!Qn() && !this.audioContext)
          throw Error("Audio context needs to be set on LocalAudioTrack in order to enable processors");
        this.processor && (yield this.internalStopProcessor());
        const s = {
          kind: this.kind,
          track: this._mediaStreamTrack,
          // RN won't have or use AudioContext
          audioContext: this.audioContext,
          localTrack: this
        };
        this.log.debug("setting up audio processor ".concat(e.name), this.logContext), yield e.init(s), this.processor = e, this.processor.processedTrack && (yield (t = this.sender) === null || t === void 0 ? void 0 : t.replaceTrack(this.processor.processedTrack), this.processor.processedTrack.addEventListener("enable-lk-krisp-noise-filter", this.handleKrispNoiseFilterEnable), this.processor.processedTrack.addEventListener("disable-lk-krisp-noise-filter", this.handleKrispNoiseFilterDisable)), this.emit(re.TrackProcessorUpdate, this.processor);
      } finally {
        i();
      }
    });
  }
  /**
   * @internal
   * @experimental
   */
  setAudioContext(e) {
    this.audioContext = e;
  }
  getSenderStats() {
    return R(this, void 0, void 0, function* () {
      var e;
      if (!(!((e = this.sender) === null || e === void 0) && e.getStats))
        return;
      const t = yield this.sender.getStats();
      let i;
      return t.forEach((s) => {
        s.type === "outbound-rtp" && (i = {
          type: "audio",
          streamId: s.id,
          packetsSent: s.packetsSent,
          packetsLost: s.packetsLost,
          bytesSent: s.bytesSent,
          timestamp: s.timestamp,
          roundTripTime: s.roundTripTime,
          jitter: s.jitter
        });
      }), i;
    });
  }
  checkForSilence() {
    return R(this, void 0, void 0, function* () {
      const e = yield SS(this);
      return e && (this.isMuted || this.log.debug("silence detected on local audio track", this.logContext), this.emit(re.AudioSilenceDetected)), e;
    });
  }
}
function SM(a, e, t) {
  switch (a.kind) {
    case "audio":
      return new Hl(a, e, !1, void 0, t);
    case "video":
      return new Gl(a, e, !1, t);
    default:
      throw new Ni("unsupported track type: ".concat(a.kind));
  }
}
const kM = Object.values(Do), CM = Object.values(Ph), EM = Object.values(pm), _M = [Do.h180, Do.h360], RM = [Ph.h180, Ph.h360], wM = (a) => [{
  scaleResolutionDownBy: 2,
  fps: a.encoding.maxFramerate
}].map((t) => {
  var i, s;
  return new Qe(Math.floor(a.width / t.scaleResolutionDownBy), Math.floor(a.height / t.scaleResolutionDownBy), Math.max(15e4, Math.floor(a.encoding.maxBitrate / (Math.pow(t.scaleResolutionDownBy, 2) * (((i = a.encoding.maxFramerate) !== null && i !== void 0 ? i : 30) / ((s = t.fps) !== null && s !== void 0 ? s : 30))))), t.fps, a.encoding.priority);
}), qh = ["q", "h", "f"];
function Vl(a, e, t, i) {
  var s, c;
  let u = i?.videoEncoding;
  a && (u = i?.screenShareEncoding);
  const d = i?.simulcast, h = i?.scalabilityMode, f = i?.videoCodec;
  if (!u && !d && !h || !e || !t)
    return [{}];
  u || (u = MM(a, e, t, f), Ee.debug("using video encoding", u));
  const p = u.maxFramerate, v = new Qe(e, t, u.maxBitrate, u.maxFramerate, u.priority);
  if (h && Vn(f)) {
    const x = new IS(h), A = [];
    if (x.spatial > 3)
      throw new Error("unsupported scalabilityMode: ".concat(h));
    const B = Kt();
    if (Po() || // Even tho RN runs M114, it does not produce SVC layers when a single encoding
    // is provided. So we'll use the legacy SVC specification for now.
    // TODO: when we upstream libwebrtc, this will need additional verification
    Qn() || B?.name === "Chrome" && Xn(B?.version, "113") < 0) {
      const ie = x.suffix == "h" ? 2 : 3, C = hw(B);
      for (let E = 0; E < x.spatial; E += 1)
        A.push({
          rid: qh[2 - E],
          maxBitrate: u.maxBitrate / Math.pow(ie, E),
          maxFramerate: v.encoding.maxFramerate,
          scaleResolutionDownBy: C ? Math.pow(2, E) : void 0
        });
      A[0].scalabilityMode = h;
    } else
      A.push({
        maxBitrate: u.maxBitrate,
        maxFramerate: v.encoding.maxFramerate,
        /* @ts-ignore */
        scalabilityMode: h
      });
    return v.encoding.priority && (A[0].priority = v.encoding.priority, A[0].networkPriority = v.encoding.priority), Ee.debug("using svc encoding", {
      encodings: A
    }), A;
  }
  if (!d)
    return [u];
  let y;
  a ? y = (s = e0(i?.screenShareSimulcastLayers)) !== null && s !== void 0 ? s : $b(a, v) : y = (c = e0(i?.videoSimulcastLayers)) !== null && c !== void 0 ? c : $b(a, v);
  let T;
  if (y.length > 0) {
    const x = y[0];
    if (y.length > 1) {
      var _ = y, k = ge(_, 2);
      T = k[1];
    }
    const A = Math.max(e, t);
    if (A >= 960 && T)
      return sh(e, t, [x, T, v], p);
    if (A >= 480)
      return sh(e, t, [x, v], p);
  }
  return sh(e, t, [v]);
}
function NS(a, e, t) {
  var i, s, c, u;
  if (!t.backupCodec || t.backupCodec === !0 || t.backupCodec.codec === t.videoCodec)
    return;
  e !== t.backupCodec.codec && Ee.warn("requested a different codec than specified as backup", {
    serverRequested: e,
    backup: t.backupCodec.codec
  }), t.videoCodec = e, t.videoEncoding = t.backupCodec.encoding;
  const d = a.mediaStreamTrack.getSettings(), h = (i = d.width) !== null && i !== void 0 ? i : (s = a.dimensions) === null || s === void 0 ? void 0 : s.width, f = (c = d.height) !== null && c !== void 0 ? c : (u = a.dimensions) === null || u === void 0 ? void 0 : u.height;
  return a.source === H.Source.ScreenShare && t.simulcast && (t.simulcast = !1), Vl(a.source === H.Source.ScreenShare, h, f, t);
}
function MM(a, e, t, i) {
  const s = AM(a, e, t);
  let c = s[0].encoding;
  const u = Math.max(e, t);
  for (let d = 0; d < s.length; d += 1) {
    const h = s[d];
    if (c = h.encoding, h.width >= u)
      break;
  }
  if (i)
    switch (i) {
      case "av1":
      case "h265":
        c = Object.assign({}, c), c.maxBitrate = c.maxBitrate * 0.7;
        break;
      case "vp9":
        c = Object.assign({}, c), c.maxBitrate = c.maxBitrate * 0.85;
        break;
    }
  return c;
}
function AM(a, e, t) {
  if (a)
    return EM;
  const i = e > t ? e / t : t / e;
  return Math.abs(i - 16 / 9) < Math.abs(i - 4 / 3) ? kM : CM;
}
function $b(a, e) {
  if (a)
    return wM(e);
  const t = e.width, i = e.height, s = t > i ? t / i : i / t;
  return Math.abs(s - 16 / 9) < Math.abs(s - 4 / 3) ? _M : RM;
}
function sh(a, e, t, i) {
  const s = [];
  if (t.forEach((c, u) => {
    if (u >= qh.length)
      return;
    const d = Math.min(a, e), f = {
      rid: qh[u],
      scaleResolutionDownBy: Math.max(1, d / Math.min(c.width, c.height)),
      maxBitrate: c.encoding.maxBitrate
    }, p = i && c.encoding.maxFramerate ? Math.min(i, c.encoding.maxFramerate) : c.encoding.maxFramerate;
    p && (f.maxFramerate = p);
    const v = Kt(), y = v?.name === "Firefox" && v.os !== "iOS" || u === 0;
    c.encoding.priority && y && (f.priority = c.encoding.priority, f.networkPriority = c.encoding.priority), s.push(f);
  }), Qn() && RS() === "ios") {
    let c;
    s.forEach((d) => {
      c ? d.maxFramerate && d.maxFramerate > c && (c = d.maxFramerate) : c = d.maxFramerate;
    });
    let u = !0;
    s.forEach((d) => {
      var h;
      d.maxFramerate != c && (u && (u = !1, Ee.info("Simulcast on iOS React-Native requires all encodings to share the same framerate.")), Ee.info('Setting framerate of encoding "'.concat((h = d.rid) !== null && h !== void 0 ? h : "", '" to ').concat(c)), d.maxFramerate = c);
    });
  }
  return s;
}
function e0(a) {
  if (a)
    return a.slice().sort((e, t) => {
      const i = e.encoding, s = t.encoding;
      return i.maxBitrate > s.maxBitrate ? 1 : i.maxBitrate < s.maxBitrate ? -1 : i.maxBitrate === s.maxBitrate && i.maxFramerate && s.maxFramerate ? i.maxFramerate > s.maxFramerate ? 1 : -1 : 0;
    });
}
class IS {
  constructor(e) {
    const t = e.match(/^L(\d)T(\d)(h|_KEY|_KEY_SHIFT){0,1}$/);
    if (!t)
      throw new Error("invalid scalability mode");
    if (this.spatial = parseInt(t[1]), this.temporal = parseInt(t[2]), t.length > 3)
      switch (t[3]) {
        case "h":
        case "_KEY":
        case "_KEY_SHIFT":
          this.suffix = t[3];
      }
  }
  toString() {
    var e;
    return "L".concat(this.spatial, "T").concat(this.temporal).concat((e = this.suffix) !== null && e !== void 0 ? e : "");
  }
}
function OM(a) {
  return a.source === H.Source.ScreenShare || a.constraints.height && tr(a.constraints.height) >= 1080 ? "maintain-resolution" : "balanced";
}
const DM = 5e3;
class Gl extends xS {
  get sender() {
    return this._sender;
  }
  set sender(e) {
    this._sender = e, this.degradationPreference && this.setDegradationPreference(this.degradationPreference);
  }
  /**
   *
   * @param mediaTrack
   * @param constraints MediaTrackConstraints that are being used when restarting or reacquiring tracks
   * @param userProvidedTrack Signals to the SDK whether or not the mediaTrack should be managed (i.e. released and reacquired) internally by the SDK
   */
  constructor(e, t) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0, s = arguments.length > 3 ? arguments[3] : void 0;
    super(e, H.Kind.Video, t, i, s), this.simulcastCodecs = /* @__PURE__ */ new Map(), this.degradationPreference = "balanced", this.isCpuConstrained = !1, this.optimizeForPerformance = !1, this.monitorSender = () => R(this, void 0, void 0, function* () {
      if (!this.sender) {
        this._currentBitrate = 0;
        return;
      }
      let c;
      try {
        c = yield this.getSenderStats();
      } catch (h) {
        this.log.error("could not get video sender stats", Object.assign(Object.assign({}, this.logContext), {
          error: h
        }));
        return;
      }
      const u = new Map(c.map((h) => [h.rid, h])), d = c.some((h) => h.qualityLimitationReason === "cpu");
      if (d !== this.isCpuConstrained && (this.isCpuConstrained = d, this.isCpuConstrained && this.emit(re.CpuConstrained)), this.prevStats) {
        let h = 0;
        u.forEach((f, p) => {
          var v;
          const y = (v = this.prevStats) === null || v === void 0 ? void 0 : v.get(p);
          h += du(f, y);
        }), this._currentBitrate = h;
      }
      this.prevStats = u;
    }), this.senderLock = new Lt();
  }
  get isSimulcast() {
    return !!(this.sender && this.sender.getParameters().encodings.length > 1);
  }
  /* @internal */
  startMonitor(e) {
    var t;
    if (this.signalClient = e, !an())
      return;
    const i = (t = this.sender) === null || t === void 0 ? void 0 : t.getParameters();
    i && (this.encodings = i.encodings), !this.monitorInterval && (this.monitorInterval = setInterval(() => {
      this.monitorSender();
    }, Tm));
  }
  stop() {
    this._mediaStreamTrack.getConstraints(), this.simulcastCodecs.forEach((e) => {
      e.mediaStreamTrack.stop();
    }), super.stop();
  }
  pauseUpstream() {
    const e = Object.create(null, {
      pauseUpstream: {
        get: () => super.pauseUpstream
      }
    });
    return R(this, void 0, void 0, function* () {
      var t, i, s, c, u;
      yield e.pauseUpstream.call(this);
      try {
        for (var d = !0, h = Hn(this.simulcastCodecs.values()), f; f = yield h.next(), t = f.done, !t; d = !0)
          c = f.value, d = !1, yield (u = c.sender) === null || u === void 0 ? void 0 : u.replaceTrack(null);
      } catch (p) {
        i = {
          error: p
        };
      } finally {
        try {
          !d && !t && (s = h.return) && (yield s.call(h));
        } finally {
          if (i) throw i.error;
        }
      }
    });
  }
  resumeUpstream() {
    const e = Object.create(null, {
      resumeUpstream: {
        get: () => super.resumeUpstream
      }
    });
    return R(this, void 0, void 0, function* () {
      var t, i, s, c, u;
      yield e.resumeUpstream.call(this);
      try {
        for (var d = !0, h = Hn(this.simulcastCodecs.values()), f; f = yield h.next(), t = f.done, !t; d = !0) {
          c = f.value, d = !1;
          const p = c;
          yield (u = p.sender) === null || u === void 0 ? void 0 : u.replaceTrack(p.mediaStreamTrack);
        }
      } catch (p) {
        i = {
          error: p
        };
      } finally {
        try {
          !d && !t && (s = h.return) && (yield s.call(h));
        } finally {
          if (i) throw i.error;
        }
      }
    });
  }
  mute() {
    const e = Object.create(null, {
      mute: {
        get: () => super.mute
      }
    });
    return R(this, void 0, void 0, function* () {
      const t = yield this.muteLock.lock();
      try {
        return this.isMuted ? (this.log.debug("Track already muted", this.logContext), this) : (this.source === H.Source.Camera && !this.isUserProvided && (this.log.debug("stopping camera track", this.logContext), this._mediaStreamTrack.stop()), yield e.mute.call(this), this);
      } finally {
        t();
      }
    });
  }
  unmute() {
    const e = Object.create(null, {
      unmute: {
        get: () => super.unmute
      }
    });
    return R(this, void 0, void 0, function* () {
      const t = yield this.muteLock.lock();
      try {
        return this.isMuted ? (this.source === H.Source.Camera && !this.isUserProvided && (this.log.debug("reacquiring camera track", this.logContext), yield this.restart(void 0, !0)), yield e.unmute.call(this), this) : (this.log.debug("Track already unmuted", this.logContext), this);
      } finally {
        t();
      }
    });
  }
  setTrackMuted(e) {
    super.setTrackMuted(e);
    for (const t of this.simulcastCodecs.values())
      t.mediaStreamTrack.enabled = !e;
  }
  getSenderStats() {
    return R(this, void 0, void 0, function* () {
      var e;
      if (!(!((e = this.sender) === null || e === void 0) && e.getStats))
        return [];
      const t = [], i = yield this.sender.getStats();
      return i.forEach((s) => {
        var c;
        if (s.type === "outbound-rtp") {
          const u = {
            type: "video",
            streamId: s.id,
            frameHeight: s.frameHeight,
            frameWidth: s.frameWidth,
            framesPerSecond: s.framesPerSecond,
            framesSent: s.framesSent,
            firCount: s.firCount,
            pliCount: s.pliCount,
            nackCount: s.nackCount,
            packetsSent: s.packetsSent,
            bytesSent: s.bytesSent,
            qualityLimitationReason: s.qualityLimitationReason,
            qualityLimitationDurations: s.qualityLimitationDurations,
            qualityLimitationResolutionChanges: s.qualityLimitationResolutionChanges,
            rid: (c = s.rid) !== null && c !== void 0 ? c : s.id,
            retransmittedPacketsSent: s.retransmittedPacketsSent,
            targetBitrate: s.targetBitrate,
            timestamp: s.timestamp
          }, d = i.get(s.remoteId);
          d && (u.jitter = d.jitter, u.packetsLost = d.packetsLost, u.roundTripTime = d.roundTripTime), t.push(u);
        }
      }), t.sort((s, c) => {
        var u, d;
        return ((u = c.frameWidth) !== null && u !== void 0 ? u : 0) - ((d = s.frameWidth) !== null && d !== void 0 ? d : 0);
      }), t;
    });
  }
  setPublishingQuality(e) {
    const t = [];
    for (let i = yn.LOW; i <= yn.HIGH; i += 1)
      t.push(new um({
        quality: i,
        enabled: i <= e
      }));
    this.log.debug("setting publishing quality. max quality ".concat(e), this.logContext), this.setPublishingLayers(Vn(this.codec), t);
  }
  restartTrack(e) {
    return R(this, void 0, void 0, function* () {
      var t, i, s, c, u;
      let d;
      if (e) {
        const v = gm({
          video: e
        });
        typeof v.video != "boolean" && (d = v.video);
      }
      yield this.restart(d), this.isCpuConstrained = !1;
      try {
        for (var h = !0, f = Hn(this.simulcastCodecs.values()), p; p = yield f.next(), t = p.done, !t; h = !0) {
          c = p.value, h = !1;
          const v = c;
          v.sender && ((u = v.sender.transport) === null || u === void 0 ? void 0 : u.state) !== "closed" && (v.mediaStreamTrack = this.mediaStreamTrack.clone(), yield v.sender.replaceTrack(v.mediaStreamTrack));
        }
      } catch (v) {
        i = {
          error: v
        };
      } finally {
        try {
          !h && !t && (s = f.return) && (yield s.call(f));
        } finally {
          if (i) throw i.error;
        }
      }
      yield this.onSenderTrackSwapped();
    });
  }
  onSenderTrackSwapped() {
    return R(this, void 0, void 0, function* () {
      yield this.refreshSenderEncodings();
    });
  }
  /**
   * Recomputes encoding parameters for this track's senders based on the current
   * MediaStreamTrack dimensions and reapplies them via setParameters. This is a no-op
   * if the track hasn't been published yet or if the track is in performance-optimized
   * mode (which manages its own encodings).
   */
  refreshSenderEncodings() {
    return R(this, void 0, void 0, function* () {
      var e;
      if (!this.sender || !this.publishOptions || this.optimizeForPerformance)
        return;
      const t = yield this.senderLock.lock();
      try {
        let s;
        try {
          s = yield this.waitForDimensions();
        } catch (d) {
          this.log.warn("could not determine new track dimensions, skipping encoding recompute", Object.assign(Object.assign({}, this.logContext), {
            error: d
          }));
          return;
        }
        if (this.lastEncodedDimensions && this.lastEncodedDimensions.width === s.width && this.lastEncodedDimensions.height === s.height)
          return;
        const c = this.source === H.Source.ScreenShare, u = Vl(c, s.width, s.height, Object.assign({}, this.publishOptions));
        yield this.applyEncodingsToSender(this.sender, u), this.encodings = u, this.lastEncodedDimensions = s;
        for (const d of this.simulcastCodecs) {
          var i = ge(d, 2);
          const h = i[0], f = i[1];
          if (!f.sender || ((e = f.sender.transport) === null || e === void 0 ? void 0 : e.state) === "closed" || !bS(h))
            continue;
          const p = Object.assign({}, this.publishOptions), v = NS(this, h, p);
          v && (yield this.applyEncodingsToSender(f.sender, v), f.encodings = v);
        }
      } catch (s) {
        this.log.warn("failed to apply recomputed encodings", Object.assign(Object.assign({}, this.logContext), {
          error: s
        }));
      } finally {
        t();
      }
    });
  }
  applyEncodingsToSender(e, t) {
    return R(this, void 0, void 0, function* () {
      const i = e.getParameters();
      !i.encodings || i.encodings.length !== t.length || (i.encodings.forEach((s, c) => {
        if (s.active === !1)
          return;
        const u = t[c];
        u.scaleResolutionDownBy !== void 0 && (s.scaleResolutionDownBy = u.scaleResolutionDownBy), u.maxBitrate !== void 0 && (s.maxBitrate = u.maxBitrate), u.maxFramerate !== void 0 && (s.maxFramerate = u.maxFramerate), u.priority !== void 0 && (s.priority = u.priority, s.networkPriority = u.priority);
      }), this.log.debug("updating sender encodings after track restart", Object.assign(Object.assign({}, this.logContext), {
        encodings: i.encodings
      })), yield e.setParameters(i));
    });
  }
  setProcessor(e) {
    const t = Object.create(null, {
      setProcessor: {
        get: () => super.setProcessor
      }
    });
    return R(this, arguments, void 0, function(i) {
      var s = this;
      let c = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0;
      return (function* () {
        var u, d, h, f, p, v;
        if (yield t.setProcessor.call(s, i, c), !((p = s.processor) === null || p === void 0) && p.processedTrack)
          try {
            for (var y = !0, T = Hn(s.simulcastCodecs.values()), _; _ = yield T.next(), u = _.done, !u; y = !0)
              f = _.value, y = !1, yield (v = f.sender) === null || v === void 0 ? void 0 : v.replaceTrack(s.processor.processedTrack);
          } catch (k) {
            d = {
              error: k
            };
          } finally {
            try {
              !y && !u && (h = T.return) && (yield h.call(T));
            } finally {
              if (d) throw d.error;
            }
          }
      })();
    });
  }
  setDegradationPreference(e) {
    return R(this, void 0, void 0, function* () {
      if (this.degradationPreference = e, this.sender)
        try {
          this.log.debug("setting degradationPreference to ".concat(e), this.logContext);
          const t = this.sender.getParameters();
          t.degradationPreference = e, this.sender.setParameters(t);
        } catch (t) {
          this.log.warn("failed to set degradationPreference", Object.assign({
            error: t
          }, this.logContext));
        }
    });
  }
  addSimulcastTrack(e, t) {
    if (this.simulcastCodecs.has(e)) {
      this.log.error("".concat(e, " already added, skipping adding simulcast codec"), this.logContext);
      return;
    }
    const i = {
      codec: e,
      mediaStreamTrack: this.mediaStreamTrack.clone(),
      sender: void 0,
      encodings: t
    };
    return this.simulcastCodecs.set(e, i), i;
  }
  setSimulcastTrackSender(e, t) {
    const i = this.simulcastCodecs.get(e);
    i && (i.sender = t, setTimeout(() => {
      this.subscribedCodecs && this.setPublishingCodecs(this.subscribedCodecs);
    }, DM));
  }
  /**
   * @internal
   * Sets codecs that should be publishing, returns new codecs that have not yet
   * been published
   */
  setPublishingCodecs(e) {
    return R(this, void 0, void 0, function* () {
      var t, i, s, c, u, d, h;
      if (this.log.debug("setting publishing codecs", Object.assign(Object.assign({}, this.logContext), {
        codecs: e,
        currentCodec: this.codec
      })), !this.codec && e.length > 0)
        return yield this.setPublishingLayers(Vn(e[0].codec), e[0].qualities), [];
      this.subscribedCodecs = e;
      const f = [];
      try {
        for (t = !0, i = Hn(e); s = yield i.next(), c = s.done, !c; t = !0) {
          h = s.value, t = !1;
          const p = h;
          if (!this.codec || this.codec === p.codec)
            yield this.setPublishingLayers(Vn(p.codec), p.qualities);
          else {
            const v = this.simulcastCodecs.get(p.codec);
            if (this.log.debug("try setPublishingCodec for ".concat(p.codec), Object.assign(Object.assign({}, this.logContext), {
              simulcastCodecInfo: v
            })), !v || !v.sender) {
              for (const y of p.qualities)
                if (y.enabled) {
                  f.push(p.codec);
                  break;
                }
            } else v.encodings && (this.log.debug("try setPublishingLayersForSender ".concat(p.codec), this.logContext), yield t0(v.sender, v.encodings, p.qualities, this.senderLock, Vn(p.codec), this.log, this.logContext));
          }
        }
      } catch (p) {
        u = {
          error: p
        };
      } finally {
        try {
          !t && !c && (d = i.return) && (yield d.call(i));
        } finally {
          if (u) throw u.error;
        }
      }
      return f;
    });
  }
  /**
   * @internal
   * Sets layers that should be publishing
   */
  setPublishingLayers(e, t) {
    return R(this, void 0, void 0, function* () {
      if (this.optimizeForPerformance) {
        this.log.info("skipping setPublishingLayers due to optimized publishing performance", Object.assign(Object.assign({}, this.logContext), {
          qualities: t
        }));
        return;
      }
      this.log.debug("setting publishing layers", Object.assign(Object.assign({}, this.logContext), {
        qualities: t
      })), !(!this.sender || !this.encodings) && (yield t0(this.sender, this.encodings, t, this.senderLock, e, this.log, this.logContext));
    });
  }
  /**
   * Designed for lower powered devices, reduces video publishing quality and disables simulcast.
   * @experimental
   */
  prioritizePerformance() {
    return R(this, void 0, void 0, function* () {
      if (!this.sender)
        throw new Error("sender not found");
      const e = yield this.senderLock.lock();
      try {
        this.optimizeForPerformance = !0;
        const t = this.sender.getParameters();
        t.encodings = t.encodings.map((i, s) => {
          var c;
          return Object.assign(Object.assign({}, i), {
            active: s === 0,
            scaleResolutionDownBy: Math.max(1, Math.ceil(((c = this.mediaStreamTrack.getSettings().height) !== null && c !== void 0 ? c : 360) / 360)),
            scalabilityMode: s === 0 && Vn(this.codec) ? "L1T3" : void 0,
            maxFramerate: s === 0 ? 15 : 0,
            maxBitrate: s === 0 ? i.maxBitrate : 0
          });
        }), this.log.debug("setting performance optimised encodings", Object.assign(Object.assign({}, this.logContext), {
          encodings: t.encodings
        })), this.encodings = t.encodings, yield this.sender.setParameters(t);
      } catch (t) {
        this.log.error("failed to set performance optimised encodings", Object.assign(Object.assign({}, this.logContext), {
          error: t
        })), this.optimizeForPerformance = !1;
      } finally {
        e();
      }
    });
  }
  handleAppVisibilityChanged() {
    const e = Object.create(null, {
      handleAppVisibilityChanged: {
        get: () => super.handleAppVisibilityChanged
      }
    });
    return R(this, void 0, void 0, function* () {
      yield e.handleAppVisibilityChanged.call(this), ES() && this.isInBackground && this.source === H.Source.Camera && (this._mediaStreamTrack.enabled = !1);
    });
  }
}
function t0(a, e, t, i, s, c, u) {
  return R(this, void 0, void 0, function* () {
    const d = yield i.lock();
    c.debug("setPublishingLayersForSender", Object.assign(Object.assign({}, u), {
      sender: a,
      qualities: t,
      senderEncodings: e
    }));
    try {
      const h = a.getParameters(), f = h.encodings;
      if (!f)
        return;
      if (f.length !== e.length) {
        c.warn("cannot set publishing layers, encodings mismatch", Object.assign(Object.assign({}, u), {
          encodings: f,
          senderEncodings: e
        }));
        return;
      }
      let p = !1;
      !1 && f[0].scalabilityMode || (s && t.some((T) => T.enabled) && t.forEach((T) => T.enabled = !0), f.forEach((y, T) => {
        var _;
        let k = (_ = y.rid) !== null && _ !== void 0 ? _ : "";
        k === "" && (k = "q");
        const x = LS(k), A = t.find((B) => B.quality === x);
        A && y.active !== A.enabled && (p = !0, y.active = A.enabled, c.debug("setting layer ".concat(A.quality, " to ").concat(y.active ? "enabled" : "disabled"), u), rr() && (A.enabled ? (y.scaleResolutionDownBy = e[T].scaleResolutionDownBy, y.maxBitrate = e[T].maxBitrate, y.maxFrameRate = e[T].maxFrameRate) : (y.scaleResolutionDownBy = 4, y.maxBitrate = 10, y.maxFrameRate = 2)));
      })), p && (h.encodings = f, c.debug("setting encodings", Object.assign(Object.assign({}, u), {
        encodings: h.encodings
      })), yield a.setParameters(h));
    } finally {
      d();
    }
  });
}
function LS(a) {
  switch (a) {
    case "f":
      return yn.HIGH;
    case "h":
      return yn.MEDIUM;
    case "q":
      return yn.LOW;
    default:
      return yn.HIGH;
  }
}
function n0(a, e, t, i) {
  if (!t)
    return [new ya({
      quality: yn.HIGH,
      width: a,
      height: e,
      bitrate: 0,
      ssrc: 0
    })];
  if (i) {
    const s = t[0].scalabilityMode, c = new IS(s), u = [], d = c.suffix == "h" ? 1.5 : 2, h = c.suffix == "h" ? 2 : 3;
    for (let f = 0; f < c.spatial; f += 1)
      u.push(new ya({
        quality: Math.min(yn.HIGH, c.spatial - 1) - f,
        width: Math.ceil(a / Math.pow(d, f)),
        height: Math.ceil(e / Math.pow(d, f)),
        bitrate: t[0].maxBitrate ? Math.ceil(t[0].maxBitrate / Math.pow(h, f)) : 0,
        ssrc: 0
      }));
    return u;
  }
  return t.map((s) => {
    var c, u, d;
    const h = (c = s.scaleResolutionDownBy) !== null && c !== void 0 ? c : 1;
    let f = LS((u = s.rid) !== null && u !== void 0 ? u : "");
    return new ya({
      quality: f,
      width: Math.ceil(a / h),
      height: Math.ceil(e / h),
      bitrate: (d = s.maxBitrate) !== null && d !== void 0 ? d : 0,
      ssrc: 0
    });
  });
}
const i0 = "_lossy", a0 = "_reliable", r0 = "_data_track", PM = 2 * 1e3, oh = "leave-reconnect", xM = 3e4, NM = 8 * 1024, IM = 256 * 1024, LM = 3, UM = 3;
var On;
(function(a) {
  a[a.New = 0] = "New", a[a.Connected = 1] = "Connected", a[a.Disconnected = 2] = "Disconnected", a[a.Reconnecting = 3] = "Reconnecting", a[a.Closed = 4] = "Closed";
})(On || (On = {}));
var _e;
(function(a) {
  a[a.RELIABLE = 0] = "RELIABLE", a[a.LOSSY = 1] = "LOSSY", a[a.DATA_TRACK_LOSSY = 2] = "DATA_TRACK_LOSSY";
})(_e || (_e = {}));
const s0 = 64e3;
class BM extends sn.EventEmitter {
  get isClosed() {
    return this._isClosed;
  }
  get isNewlyCreated() {
    return this._isNewlyCreated;
  }
  get pendingReconnect() {
    return !!this.reconnectTimeout;
  }
  constructor(e) {
    var t;
    super(), this.options = e, this.rtcConfig = {}, this.peerConnectionTimeout = Rm.peerConnectionTimeout, this.fullReconnectOnNext = !1, this.latestRemoteOfferId = 0, this.subscriberPrimary = !1, this.pcState = On.New, this._isClosed = !0, this._isNewlyCreated = !0, this.pendingTrackResolvers = {}, this.reconnectAttempts = 0, this.reconnectStart = 0, this.attemptingReconnect = !1, this.joinAttempts = 0, this.maxJoinAttempts = 1, this.shouldFailNext = !1, this.shouldFailOnV1Path = !1, this.log = Ee, this.reliableDataSequence = 1, this.reliableMessageBuffer = new Vb(), this.reliableReceivedState = new rM(xM), this.lossyDataStatCurrentBytes = 0, this.lossyDataStatByterate = 0, this.lossyDataDropCount = 0, this.midToTrackId = {}, this.isWaitingForNetworkReconnect = !1, this.bufferStatusLowClosingFuture = new wt(), this.handleDataChannel = (i) => R(this, [i], void 0, function(s) {
      var c = this;
      let u = s.channel;
      return (function* () {
        if (!u)
          return;
        let d;
        if (u.label === a0)
          c.reliableDCSub = u, d = c.handleDataMessage;
        else if (u.label === i0)
          c.lossyDCSub = u, d = c.handleDataMessage;
        else if (u.label === r0)
          c.dataTrackDCSub = u, d = c.handleDataTrackMessage;
        else
          return;
        c.log.debug("on data channel ".concat(u.id, ", ").concat(u.label)), u.onmessage = d;
      })();
    }), this.handleDataMessage = (i) => R(this, void 0, void 0, function* () {
      var s, c, u, d, h;
      const f = yield this.dataProcessLock.lock();
      try {
        let p;
        if (i.data instanceof ArrayBuffer)
          p = i.data;
        else if (i.data instanceof Blob)
          p = yield i.data.arrayBuffer();
        else {
          this.log.error("unsupported data type", {
            data: i.data
          });
          return;
        }
        const v = Bt.fromBinary(new Uint8Array(p));
        if (v.sequence > 0 && v.participantSid !== "") {
          const y = this.reliableReceivedState.get(v.participantSid);
          if (y && v.sequence <= y)
            return;
          this.reliableReceivedState.set(v.participantSid, v.sequence);
        }
        if (((s = v.value) === null || s === void 0 ? void 0 : s.case) === "speaker")
          this.emit(ae.ActiveSpeakersUpdate, v.value.value.speakers);
        else if (((c = v.value) === null || c === void 0 ? void 0 : c.case) === "encryptedPacket") {
          if (!this.e2eeManager) {
            this.log.error("Received encrypted packet but E2EE not set up");
            return;
          }
          const y = yield (u = this.e2eeManager) === null || u === void 0 ? void 0 : u.handleEncryptedData(v.value.value.encryptedValue, v.value.value.iv, v.participantIdentity, v.value.value.keyIndex), T = lT.fromBinary(y.payload), _ = new Bt({
            value: T.value,
            participantIdentity: v.participantIdentity,
            participantSid: v.participantSid
          });
          ((d = _.value) === null || d === void 0 ? void 0 : d.case) === "user" && o0(_, _.value.value), this.emit(ae.DataPacketReceived, _, v.value.value.encryptionType);
        } else
          ((h = v.value) === null || h === void 0 ? void 0 : h.case) === "user" && o0(v, v.value.value), this.emit(ae.DataPacketReceived, v, $e.NONE);
      } finally {
        f();
      }
    }), this.handleDataTrackMessage = (i) => R(this, void 0, void 0, function* () {
      let s;
      if (i.data instanceof ArrayBuffer)
        s = i.data;
      else if (i.data instanceof Blob)
        s = yield i.data.arrayBuffer();
      else {
        this.log.error("unsupported data type", {
          data: i.data
        });
        return;
      }
      this.emit("dataTrackPacketReceived", new Uint8Array(s));
    }), this.handleDataError = (i) => {
      if (this._isClosed)
        return;
      const c = i.currentTarget.maxRetransmits === 0 ? "lossy" : "reliable";
      if (typeof RTCErrorEvent < "u" && i instanceof RTCErrorEvent && i.error) {
        const u = i.error;
        this.log.error("DataChannel error on ".concat(c, ": ").concat(u.message), {
          error: u,
          errorDetail: u.errorDetail,
          sctpCauseCode: u.sctpCauseCode
        });
      } else
        this.log.error("Unknown DataChannel error on ".concat(c), {
          event: i
        });
    }, this.handleDataChannelClose = (i) => () => {
      var s;
      !this._isClosed && ((s = this.pcManager) === null || s === void 0 ? void 0 : s.publisher.getConnectionState()) === "connected" && this.log.error("publisher data channel '".concat(_e[i], "' closed unexpectedly"), this.logContext);
    }, this.handleBufferedAmountLow = (i) => {
      this.updateAndEmitDCBufferStatus(i);
    }, this.handleDisconnect = (i, s) => {
      if (this._isClosed)
        return;
      this.log.warn("".concat(i, " disconnected")), this.reconnectAttempts === 0 && (this.reconnectStart = Date.now());
      const c = (h) => {
        this.log.warn("could not recover connection after ".concat(this.reconnectAttempts, " attempts, ").concat(h, "ms. giving up")), this.emit(ae.Disconnected), this.close();
      }, u = Date.now() - this.reconnectStart;
      let d = this.getNextRetryDelay({
        elapsedMs: u,
        retryCount: this.reconnectAttempts
      });
      if (d === null) {
        c(u);
        return;
      }
      i === oh && (d = 0), this.log.debug("reconnecting in ".concat(d, "ms")), this.clearReconnectTimeout(), this.token && this.emit(ae.TokenRefreshed, this.token), this.reconnectTimeout = bt.setTimeout(() => this.attemptReconnect(s).finally(() => this.reconnectTimeout = void 0), d);
    }, this.waitForRestarted = () => new Promise((i, s) => {
      this.pcState === On.Connected && i();
      const c = () => {
        this.off(ae.Disconnected, u), i();
      }, u = () => {
        this.off(ae.Restarted, c), s();
      };
      this.once(ae.Restarted, c), this.once(ae.Disconnected, u);
    }), this.updateAndEmitDCBufferStatus = (i) => {
      if (i === _e.RELIABLE) {
        const c = this.dataChannelForKind(i);
        c && this.reliableMessageBuffer.alignBufferedAmount(c.bufferedAmount);
      }
      const s = this.isBufferStatusLow(i);
      typeof s < "u" && s !== this.dcBufferStatus.get(i) && (this.dcBufferStatus.set(i, s), this.emit(ae.DCBufferStatusChanged, s, i));
    }, this.isBufferStatusLow = (i) => {
      const s = this.dataChannelForKind(i);
      if (s)
        return s.bufferedAmount <= s.bufferedAmountLowThreshold;
    }, this.onRtpMapAvailable = (i) => {
      const s = /* @__PURE__ */ new Map();
      i.forEach((c) => {
        const u = c.codec.toLowerCase();
        yw(u) && s.set(c.payload, u);
      }), this.emit(ae.RTPVideoMapUpdate, s);
    }, this.handleBrowserOnLine = () => R(this, void 0, void 0, function* () {
      !this.url || !(yield fetch(xo(this.url), {
        method: "HEAD"
      }).then((s) => s.ok).catch(() => !1)) || (this.log.info("detected network reconnected"), // in case the engine is currently reconnecting, attempt a reconnect immediately after the browser state has changed to 'onLine'
      (this.client.currentState === Ze.RECONNECTING || // also if the browser went offline before and the engine still thinks it's in a connected state, treat it as a network interruption that we haven't noticed yet
      this.isWaitingForNetworkReconnect && this.client.currentState === Ze.CONNECTED) && (this.clearReconnectTimeout(), this.attemptReconnect(Ka.RR_SIGNAL_DISCONNECTED), this.isWaitingForNetworkReconnect = !1));
    }), this.handleBrowserOffline = () => R(this, void 0, void 0, function* () {
      if (this.url)
        try {
          yield Promise.race([
            fetch(xo(this.url), {
              method: "HEAD"
            }),
            // if there's no internet connection the fetch rejects immediately, so we only use a short timeout here
            zt(4e3).then(() => Promise.reject())
          ]);
        } catch {
          window.navigator.onLine === !1 && (this.log.info("detected network interruption"), this.isWaitingForNetworkReconnect = !0);
        }
    }), this.log = rn((t = e.loggerName) !== null && t !== void 0 ? t : Mt.Engine, () => this.logContext), this.loggerOptions = {
      loggerName: e.loggerName,
      loggerContextCb: () => this.logContext
    }, this.client = new Em(void 0, this.loggerOptions), this.client.signalLatency = this.options.expSignalLatency, this.reconnectPolicy = this.options.reconnectPolicy, this.closingLock = new Lt(), this.dataProcessLock = new Lt(), this.dcBufferStatus = /* @__PURE__ */ new Map([[_e.RELIABLE, !0], [_e.LOSSY, !0], [_e.DATA_TRACK_LOSSY, !0]]), this.client.onParticipantUpdate = (i) => this.emit(ae.ParticipantUpdate, i), this.client.onConnectionQuality = (i) => this.emit(ae.ConnectionQualityUpdate, i), this.client.onRoomUpdate = (i) => this.emit(ae.RoomUpdate, i), this.client.onSubscriptionError = (i) => this.emit(ae.SubscriptionError, i), this.client.onSubscriptionPermissionUpdate = (i) => this.emit(ae.SubscriptionPermissionUpdate, i), this.client.onSpeakersChanged = (i) => this.emit(ae.SpeakersChanged, i), this.client.onStreamStateUpdate = (i) => this.emit(ae.StreamStateChanged, i), this.client.onRequestResponse = (i) => this.emit(ae.SignalRequestResponse, i), this.client.onParticipantUpdate = (i) => this.emit(ae.ParticipantUpdate, i), this.client.onJoined = (i) => this.emit(ae.Joined, i), this.on(ae.Closing, () => {
      var i, s;
      (s = (i = this.bufferStatusLowClosingFuture).reject) === null || s === void 0 || s.call(i, new rt("engine closed"));
    }), this.bufferStatusLowClosingFuture.promise.catch(() => {
    });
  }
  /** @internal */
  get logContext() {
    var e, t, i, s, c, u;
    return {
      room: (t = (e = this.latestJoinResponse) === null || e === void 0 ? void 0 : e.room) === null || t === void 0 ? void 0 : t.name,
      roomID: (s = (i = this.latestJoinResponse) === null || i === void 0 ? void 0 : i.room) === null || s === void 0 ? void 0 : s.sid,
      participant: (u = (c = this.latestJoinResponse) === null || c === void 0 ? void 0 : c.participant) === null || u === void 0 ? void 0 : u.identity,
      participantID: this.participantSid
    };
  }
  join(e, t, i, s) {
    return R(this, arguments, void 0, function(c, u, d, h) {
      var f = this;
      let p = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : !1;
      return (function* () {
        var v, y, T;
        f._isNewlyCreated = !1, f.url = c, f.token = u, f.signalOpts = d, f.maxJoinAttempts = d.maxRetries;
        try {
          f.joinAttempts += 1, f.setupSignalClientCallbacks();
          const _ = !p && Rw();
          let k;
          if (_) {
            f.pcManager || (yield f.configure(), f.applyInitialPublisherLayout());
            const B = yield (v = f.pcManager) === null || v === void 0 ? void 0 : v.publisher.createInitialOffer();
            B && (k = Xa(B.offer, B.offerId));
          }
          if (h?.aborted)
            throw ye.cancelled("Connection aborted");
          if (!p && f.shouldFailOnV1Path)
            throw f.shouldFailOnV1Path = !1, ye.serviceNotFound("Simulated v1 path failure", "v0-rtc");
          const x = yield f.client.join(c, u, d, h, p, k);
          f._isClosed = !1, f.latestJoinResponse = x, f.participantSid = (y = x.participant) === null || y === void 0 ? void 0 : y.sid, f.subscriberPrimary = x.subscriberPrimary, _ ? (T = f.pcManager) === null || T === void 0 || T.updateConfiguration(f.makeRTCConfiguration(x)) : (f.pcManager || (yield f.configure(x, !p), p || f.applyInitialPublisherLayout()), (!f.subscriberPrimary || x.fastPublish) && f.negotiate().catch((B) => {
            f.log.error(B);
          })), f.registerOnLineListener(), f.clientConfiguration = x.clientConfiguration, f.emit(ae.SignalConnected, x);
          let A = x.serverInfo;
          return A || (A = {
            version: x.serverVersion,
            region: x.serverRegion
          }), f.log.info("connected to Livekit Server ".concat(Object.entries(A).map((B) => {
            let ie = ge(B, 2), C = ie[0], E = ie[1];
            return "".concat(C, ": ").concat(E);
          }).join(", "))), {
            joinResponse: x,
            serverInfo: A
          };
        } catch (_) {
          if (_ instanceof ye) {
            if (_.reason === We.ServerUnreachable) {
              if (f.log.warn("Couldn't connect to server, attempt ".concat(f.joinAttempts, " of ").concat(f.maxJoinAttempts)), f.joinAttempts < f.maxJoinAttempts)
                return f.join(c, u, d, h, p);
            } else if (_.reason === We.ServiceNotFound)
              return f.log.warn("Initial connection failed: ".concat(_.message, " – Retrying")), f.pcManager && (f.pcManager.onStateChange = void 0, yield f.cleanupPeerConnections()), f.join(c, u, d, h, !0);
          }
          throw _;
        }
      })();
    });
  }
  close() {
    return R(this, void 0, void 0, function* () {
      const e = yield this.closingLock.lock();
      if (this.isClosed) {
        e();
        return;
      }
      try {
        this._isClosed = !0, this.joinAttempts = 0, this.emit(ae.Closing), this.removeAllListeners(), this.deregisterOnLineListener(), this.clearPendingReconnect(), this.cleanupLossyDataStats(), yield this.cleanupPeerConnections(), yield this.cleanupClient();
      } finally {
        e();
      }
    });
  }
  cleanupPeerConnections() {
    return R(this, void 0, void 0, function* () {
      var e;
      const t = (i) => {
        i && (i.onbufferedamountlow = null, i.onclose = null, i.onclosing = null, i.onerror = null, i.onmessage = null, i.onopen = null, i.close());
      };
      t(this.lossyDC), t(this.lossyDCSub), t(this.reliableDC), t(this.reliableDCSub), t(this.dataTrackDC), t(this.dataTrackDCSub), yield (e = this.pcManager) === null || e === void 0 ? void 0 : e.close(), this.pcManager = void 0, this.lossyDC = void 0, this.lossyDCSub = void 0, this.reliableDC = void 0, this.reliableDCSub = void 0, this.dataTrackDC = void 0, this.dataTrackDCSub = void 0, this.reliableMessageBuffer = new Vb(), this.reliableDataSequence = 1, this.reliableReceivedState.clear();
    });
  }
  cleanupLossyDataStats() {
    this.lossyDataStatByterate = 0, this.lossyDataStatCurrentBytes = 0, this.lossyDataStatInterval && (clearInterval(this.lossyDataStatInterval), this.lossyDataStatInterval = void 0), this.lossyDataDropCount = 0;
  }
  cleanupClient() {
    return R(this, void 0, void 0, function* () {
      yield this.client.close(), this.client.resetCallbacks();
      for (const e of Object.keys(this.pendingTrackResolvers))
        this.pendingTrackResolvers[e].reject();
      this.pendingTrackResolvers = {};
    });
  }
  addTrack(e) {
    if (this.pendingTrackResolvers[e.cid])
      throw new Ni("a track with the same ID has already been published");
    return new Promise((t, i) => {
      const s = setTimeout(() => {
        delete this.pendingTrackResolvers[e.cid], i(ye.timeout("publication of local track timed out, no response from server"));
      }, 1e4);
      this.pendingTrackResolvers[e.cid] = {
        resolve: (c) => {
          clearTimeout(s), t(c);
        },
        reject: () => {
          clearTimeout(s), i(new Error("Cancelled publication by calling unpublish"));
        }
      }, this.client.sendAddTrack(e);
    });
  }
  /**
   * Removes sender from PeerConnection, returning true if it was removed successfully
   * and a negotiation is necessary
   * @param sender
   * @returns
   */
  removeTrack(e) {
    if (e.track && this.pendingTrackResolvers[e.track.id]) {
      const t = this.pendingTrackResolvers[e.track.id].reject;
      t && t(), delete this.pendingTrackResolvers[e.track.id];
    }
    try {
      return this.pcManager.removeTrack(e), !0;
    } catch (t) {
      this.log.warn("failed to remove track", {
        error: t
      });
    }
    return !1;
  }
  updateMuteStatus(e, t) {
    this.client.sendMuteTrack(e, t);
  }
  get dataSubscriberReadyState() {
    var e;
    return (e = this.reliableDCSub) === null || e === void 0 ? void 0 : e.readyState;
  }
  getConnectedServerAddress() {
    return R(this, void 0, void 0, function* () {
      var e;
      return (e = this.pcManager) === null || e === void 0 ? void 0 : e.getConnectedAddress();
    });
  }
  /* @internal */
  setRegionStrategy(e) {
    this.regionStrategy = e;
  }
  configure(e, t) {
    return R(this, void 0, void 0, function* () {
      var i;
      if (!(this.pcManager && this.pcManager.currentState !== st.NEW)) {
        if (e) {
          this.participantSid = (i = e.participant) === null || i === void 0 ? void 0 : i.sid;
          const s = this.makeRTCConfiguration(e);
          this.pcManager = new Wb(t ? "publisher-only" : e.subscriberPrimary ? "subscriber-primary" : "publisher-primary", this.loggerOptions, s);
        } else {
          const s = this.makeRTCConfiguration();
          this.pcManager = new Wb("publisher-only", this.loggerOptions, s);
        }
        this.emit(ae.TransportsCreated, this.pcManager.publisher, this.pcManager.subscriber), this.pcManager.onIceCandidate = (s, c) => {
          this.client.sendIceCandidate(s, c);
        }, this.pcManager.onPublisherOffer = (s, c) => {
          this.client.sendOffer(s, c);
        }, this.pcManager.onDataChannel = this.handleDataChannel, this.pcManager.onStateChange = (s, c, u) => R(this, void 0, void 0, function* () {
          if (this.log.debug("primary PC state changed ".concat(s)), ["closed", "disconnected", "failed"].includes(c) && (this.publisherConnectionPromise = void 0), s === st.CONNECTED) {
            const f = this.pcState === On.New;
            this.pcState = On.Connected, f && this.emit(ae.Connected, this.latestJoinResponse);
          } else s === st.FAILED && (this.pcState === On.Connected || this.pcState === On.Reconnecting) && (this.pcState = On.Disconnected, this.handleDisconnect("peerconnection failed", u === "failed" ? Ka.RR_SUBSCRIBER_FAILED : Ka.RR_PUBLISHER_FAILED));
          const d = this.client.isDisconnected || this.client.currentState === Ze.RECONNECTING, h = [st.FAILED, st.CLOSING, st.CLOSED].includes(s);
          d && h && !this._isClosed && this.emit(ae.Offline);
        }), this.pcManager.onTrack = (s) => {
          s.streams.length !== 0 && this.emit(ae.MediaTrackAdded, s.track, s.streams[0], s.receiver);
        };
      }
    });
  }
  setupSignalClientCallbacks() {
    this.client.onAnswer = (e, t, i) => R(this, void 0, void 0, function* () {
      this.pcManager && (this.log.debug("received server answer", {
        RTCSdpType: e.type,
        sdp: e.sdp,
        midToTrackId: i
      }), this.midToTrackId = i, yield this.pcManager.setPublisherAnswer(e, t));
    }), this.client.onTrickle = (e, t) => {
      this.pcManager && (this.log.debug("got ICE candidate from peer", {
        candidate: e,
        target: t
      }), this.pcManager.addIceCandidate(e, t));
    }, this.client.onOffer = (e, t, i) => R(this, void 0, void 0, function* () {
      if (this.latestRemoteOfferId = t, !this.pcManager)
        return;
      this.midToTrackId = i;
      const s = yield this.pcManager.createSubscriberAnswerFromOffer(e, t);
      s && this.client.sendAnswer(s, t);
    }), this.client.onLocalTrackPublished = (e) => {
      var t;
      if (this.log.debug("received trackPublishedResponse", {
        cid: e.cid,
        track: (t = e.track) === null || t === void 0 ? void 0 : t.sid
      }), !this.pendingTrackResolvers[e.cid]) {
        this.log.error("missing track resolver for ".concat(e.cid), {
          cid: e.cid
        });
        return;
      }
      const i = this.pendingTrackResolvers[e.cid].resolve;
      delete this.pendingTrackResolvers[e.cid], i(e.track);
    }, this.client.onLocalTrackUnpublished = (e) => {
      this.emit(ae.LocalTrackUnpublished, e);
    }, this.client.onLocalTrackSubscribed = (e) => {
      this.emit(ae.LocalTrackSubscribed, e);
    }, this.client.onTokenRefresh = (e) => {
      this.token = e, this.emit(ae.TokenRefreshed, e);
    }, this.client.onRemoteMuteChanged = (e, t) => {
      this.emit(ae.RemoteMute, e, t);
    }, this.client.onSubscribedQualityUpdate = (e) => {
      this.emit(ae.SubscribedQualityUpdate, e);
    }, this.client.onRoomMoved = (e) => {
      var t;
      this.participantSid = (t = e.participant) === null || t === void 0 ? void 0 : t.sid, this.latestJoinResponse && (this.latestJoinResponse.room = e.room), this.emit(ae.RoomMoved, e);
    }, this.client.onMediaSectionsRequirement = (e) => {
      this.addMediaSections(e.numAudios, e.numVideos), this.negotiate();
    }, this.client.onPublishDataTrackResponse = (e) => {
      this.emit(ae.PublishDataTrackResponse, e);
    }, this.client.onUnPublishDataTrackResponse = (e) => {
      this.emit(ae.UnPublishDataTrackResponse, e);
    }, this.client.onDataTrackSubscriberHandles = (e) => {
      this.emit(ae.DataTrackSubscriberHandles, e);
    }, this.client.onClose = () => {
      this.handleDisconnect("signal", Ka.RR_SIGNAL_DISCONNECTED);
    }, this.client.onLeave = (e) => {
      switch (this.log.info("client leave request received (action=".concat(e?.action, ")"), {
        reason: e?.reason
      }), e.regions && (this.log.debug("updating regions"), this.emit(ae.ServerRegionsReported, e.regions)), e.action) {
        case es.DISCONNECT:
          this.emit(ae.Disconnected, e?.reason), this.close();
          break;
        case es.RECONNECT:
          this.fullReconnectOnNext = !0, this.handleDisconnect(oh);
          break;
        case es.RESUME:
          this.handleDisconnect(oh);
      }
    };
  }
  makeRTCConfiguration(e) {
    var t;
    const i = Object.assign({}, this.rtcConfig);
    if ((((t = this.signalOpts) === null || t === void 0 ? void 0 : t.e2eeEnabled) || this.frameMetadataWorker && !No()) && ym() && (this.log.debug("E2EE - setting up transports with insertable streams"), i.encodedInsertableStreams = !0), i.sdpSemantics = "unified-plan", i.continualGatheringPolicy = "gather_continually", !e)
      return i;
    if (e.iceServers && !i.iceServers) {
      const c = [];
      e.iceServers.forEach((u) => {
        const d = {
          urls: u.urls
        };
        u.username && (d.username = u.username), u.credential && (d.credential = u.credential), c.push(d);
      }), i.iceServers = c;
    }
    return e.clientConfiguration && e.clientConfiguration.forceRelay === Ao.ENABLED && (i.iceTransportPolicy = "relay"), i;
  }
  /**
   * Populate the publisher PC so its first offer carries the data channels + recvonly media
   * sections. Required for every V1 connection: Firefox only binds receive decoders for media
   * present in that first offer, and the offer-with-join path needs the sections to
   * build a meaningful initial offer. Must be called on a configured pcManager.
   */
  applyInitialPublisherLayout() {
    this.createDataChannels(), Qn() || this.addMediaSections(LM, UM);
  }
  addMediaSections(e, t) {
    var i, s;
    const c = {
      direction: "recvonly"
    };
    for (let u = 0; u < e; u++)
      (i = this.pcManager) === null || i === void 0 || i.addPublisherTransceiverOfKind("audio", c);
    for (let u = 0; u < t; u++)
      (s = this.pcManager) === null || s === void 0 || s.addPublisherTransceiverOfKind("video", c);
  }
  createDataChannels() {
    this.pcManager && (this.lossyDC && (this.lossyDC.onmessage = null, this.lossyDC.onerror = null, this.lossyDC.onclose = null), this.reliableDC && (this.reliableDC.onmessage = null, this.reliableDC.onerror = null, this.reliableDC.onclose = null), this.dataTrackDC && (this.dataTrackDC.onmessage = null, this.dataTrackDC.onerror = null, this.dataTrackDC.onclose = null), this.lossyDC = this.pcManager.createPublisherDataChannel(i0, {
      ordered: !1,
      maxRetransmits: 0
    }), this.reliableDC = this.pcManager.createPublisherDataChannel(a0, {
      ordered: !0
    }), this.dataTrackDC = this.pcManager.createPublisherDataChannel(r0, {
      ordered: !1,
      maxRetransmits: 0
    }), this.lossyDC.onmessage = this.handleDataMessage, this.reliableDC.onmessage = this.handleDataMessage, this.dataTrackDC.onmessage = this.handleDataTrackMessage, this.lossyDC.onerror = this.handleDataError, this.reliableDC.onerror = this.handleDataError, this.dataTrackDC.onerror = this.handleDataError, this.lossyDC.onclose = this.handleDataChannelClose(_e.LOSSY), this.reliableDC.onclose = this.handleDataChannelClose(_e.RELIABLE), this.dataTrackDC.onclose = this.handleDataChannelClose(_e.DATA_TRACK_LOSSY), this.lossyDC.bufferedAmountLowThreshold = 65535, this.reliableDC.bufferedAmountLowThreshold = 65535, this.dataTrackDC.bufferedAmountLowThreshold = 65535, this.lossyDC.onbufferedamountlow = () => this.handleBufferedAmountLow(_e.LOSSY), this.reliableDC.onbufferedamountlow = () => this.handleBufferedAmountLow(_e.RELIABLE), this.dataTrackDC.onbufferedamountlow = () => this.handleBufferedAmountLow(_e.DATA_TRACK_LOSSY), this.cleanupLossyDataStats(), this.lossyDataStatInterval = setInterval(() => {
      this.lossyDataStatByterate = this.lossyDataStatCurrentBytes, this.lossyDataStatCurrentBytes = 0;
      const e = this.dataChannelForKind(_e.LOSSY);
      if (e) {
        const t = this.lossyDataStatByterate / 10;
        e.bufferedAmountLowThreshold = Math.min(Math.max(t, NM), IM);
      }
    }, 1e3));
  }
  createSender(e, t, i) {
    return R(this, void 0, void 0, function* () {
      let s;
      if (Ih())
        s = yield this.createTransceiverRTCRtpSender(e, t, i);
      else if (Lh())
        this.log.warn("using add-track fallback"), s = yield this.createRTCRtpSender(e.mediaStreamTrack);
      else
        throw new rt("Required webRTC APIs not supported on this device");
      return this.setupFrameMetadataSender(s, t), s;
    });
  }
  createSimulcastSender(e, t, i, s) {
    return R(this, void 0, void 0, function* () {
      let c;
      if (Ih())
        c = yield this.createSimulcastTransceiverSender(e, t, i, s);
      else if (Lh())
        this.log.debug("using add-track fallback"), c = yield this.createRTCRtpSender(e.mediaStreamTrack);
      else
        throw new rt("Cannot stream on this device");
      return c && this.setupFrameMetadataSender(c, i), c;
    });
  }
  get frameMetadataWorker() {
    var e, t;
    return (t = (e = this.options.frameMetadata) !== null && e !== void 0 ? e : this.options.packetTrailer) === null || t === void 0 ? void 0 : t.worker;
  }
  setupFrameMetadataSender(e) {
    let t = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    var i, s, c;
    const u = this.frameMetadataWorker;
    if (!u || !((i = this.signalOpts) === null || i === void 0) && i.e2eeEnabled)
      return;
    const d = (s = t.frameMetadata) !== null && s !== void 0 ? s : t.packetTrailer, h = jl(d);
    if (No()) {
      h && (e.transform = new RTCRtpScriptTransform(u, {
        kind: "encode",
        packetTrailer: d
      }));
      return;
    }
    if (!Io((c = this.options.frameMetadata) !== null && c !== void 0 ? c : this.options.packetTrailer) || !("createEncodedStreams" in e)) {
      h && this.log.warn("frame metadata transform not supported; skipping write", this.logContext);
      return;
    }
    const f = e.createEncodedStreams(), p = f.readable, v = f.writable;
    h ? u.postMessage({
      kind: "encode",
      data: {
        readableStream: p,
        writableStream: v,
        packetTrailer: d
      }
    }, [p, v]) : p.pipeTo(v);
  }
  createTransceiverRTCRtpSender(e, t, i) {
    return R(this, void 0, void 0, function* () {
      if (!this.pcManager)
        throw new rt("publisher is closed");
      const s = [];
      e.mediaStream && s.push(e.mediaStream), Li(e) && (e.codec = t.videoCodec);
      const c = {
        direction: "sendonly",
        streams: s
      };
      return i && (c.sendEncodings = i), (yield this.pcManager.addPublisherTransceiver(e.mediaStreamTrack, c)).sender;
    });
  }
  createSimulcastTransceiverSender(e, t, i, s) {
    return R(this, void 0, void 0, function* () {
      if (!this.pcManager)
        throw new rt("publisher is closed");
      const c = {
        direction: "sendonly"
      };
      s && (c.sendEncodings = s);
      const u = yield this.pcManager.addPublisherTransceiver(t.mediaStreamTrack, c);
      if (i.videoCodec)
        return e.setSimulcastTrackSender(i.videoCodec, u.sender), u.sender;
    });
  }
  createRTCRtpSender(e) {
    return R(this, void 0, void 0, function* () {
      if (!this.pcManager)
        throw new rt("publisher is closed");
      return this.pcManager.addPublisherTrack(e);
    });
  }
  attemptReconnect(e) {
    return R(this, void 0, void 0, function* () {
      var t, i, s;
      if (!this._isClosed) {
        if (this.attemptingReconnect) {
          this.log.warn("already attempting reconnect, returning early");
          return;
        }
        (((t = this.clientConfiguration) === null || t === void 0 ? void 0 : t.resumeConnection) === Ao.DISABLED || // signaling state could change to closed due to hardware sleep
        // those connections cannot be resumed
        ((s = (i = this.pcManager) === null || i === void 0 ? void 0 : i.currentState) !== null && s !== void 0 ? s : st.NEW) === st.NEW) && (this.fullReconnectOnNext = !0);
        try {
          this.attemptingReconnect = !0, this.fullReconnectOnNext ? yield this.restartConnection() : yield this.resumeConnection(e), this.clearPendingReconnect(), this.fullReconnectOnNext = !1;
        } catch (c) {
          this.reconnectAttempts += 1;
          let u = !0;
          c instanceof rt ? (this.log.debug("received unrecoverable error", {
            error: c
          }), u = !1) : c instanceof Jr || (this.fullReconnectOnNext = !0), u ? this.handleDisconnect("reconnect", Ka.RR_UNKNOWN) : (this.log.info("could not recover connection after ".concat(this.reconnectAttempts, " attempts, ").concat(Date.now() - this.reconnectStart, "ms. giving up")), this.emit(ae.Disconnected), yield this.close());
        } finally {
          this.attemptingReconnect = !1;
        }
      }
    });
  }
  getNextRetryDelay(e) {
    try {
      return this.reconnectPolicy.nextRetryDelayInMs(e);
    } catch (t) {
      this.log.warn("encountered error in reconnect policy", {
        error: t
      });
    }
    return null;
  }
  restartConnection(e) {
    return R(this, void 0, void 0, function* () {
      var t, i, s;
      try {
        if (!this.url || !this.token)
          throw new rt("could not reconnect, url or token not saved");
        this.log.info("reconnecting, attempt: ".concat(this.reconnectAttempts)), this.emit(ae.Restarting), this.client.isDisconnected || (yield this.client.sendLeave()), yield this.cleanupPeerConnections(), yield this.cleanupClient();
        let c;
        try {
          if (!this.signalOpts)
            throw this.log.warn("attempted connection restart, without signal options present"), new Jr();
          c = (yield this.join(e ?? this.url, this.token, this.signalOpts, void 0, !this.options.singlePeerConnection)).joinResponse;
        } catch (u) {
          throw u instanceof ye && u.reason === We.NotAllowed ? new rt("could not reconnect, token might be expired") : new Jr();
        }
        if (this.shouldFailNext)
          throw this.shouldFailNext = !1, new Error("simulated failure");
        if (this.client.setReconnected(), this.emit(ae.SignalRestarted, c), yield this.waitForPCReconnected(), this.client.currentState !== Ze.CONNECTED)
          throw new Jr("Signal connection got severed during reconnect");
        (t = this.regionStrategy) === null || t === void 0 || t.resetAttempts(), this.emit(ae.Restarted);
      } catch (c) {
        const u = yield (i = this.regionStrategy) === null || i === void 0 ? void 0 : i.getNextUrl();
        if (u) {
          yield this.restartConnection(u);
          return;
        } else
          throw (s = this.regionStrategy) === null || s === void 0 || s.resetAttempts(), c;
      }
    });
  }
  resumeConnection(e) {
    return R(this, void 0, void 0, function* () {
      var t;
      if (!this.url || !this.token)
        throw new rt("could not reconnect, url or token not saved");
      if (!this.pcManager)
        throw new rt("publisher and subscriber connections unset");
      this.log.info("resuming signal connection, attempt ".concat(this.reconnectAttempts)), this.emit(ae.Resuming);
      let i;
      try {
        this.setupSignalClientCallbacks(), i = yield this.client.reconnect(this.url, this.token, this.participantSid, e);
      } catch (s) {
        let c = "";
        throw s instanceof Error && (c = s.message, this.log.error(s.message, {
          error: s
        })), s instanceof ye && s.reason === We.NotAllowed ? new rt("could not reconnect, token might be expired") : s instanceof ye && s.reason === We.LeaveRequest ? s : new Jr(c);
      }
      if (this.emit(ae.SignalResumed), i) {
        const s = this.makeRTCConfiguration(i);
        this.pcManager.updateConfiguration(s), this.latestJoinResponse && (this.latestJoinResponse.serverInfo = i.serverInfo);
      } else
        this.log.warn("Did not receive reconnect response");
      if (this.shouldFailNext)
        throw this.shouldFailNext = !1, new Error("simulated failure");
      if (yield this.pcManager.triggerIceRestart(), yield this.waitForPCReconnected(), this.client.currentState !== Ze.CONNECTED)
        throw new Jr("Signal connection got severed during reconnect");
      this.client.setReconnected(), ((t = this.reliableDC) === null || t === void 0 ? void 0 : t.readyState) === "open" && this.reliableDC.id === null && this.createDataChannels(), i?.lastMessageSeq && this.resendReliableMessagesForResume(i.lastMessageSeq), this.emit(ae.Resumed);
    });
  }
  waitForPCInitialConnection(e, t) {
    return R(this, void 0, void 0, function* () {
      if (!this.pcManager)
        throw new rt("PC manager is closed");
      yield this.pcManager.ensurePCTransportConnection(t, e);
    });
  }
  waitForPCReconnected() {
    return R(this, void 0, void 0, function* () {
      this.pcState = On.Reconnecting, this.log.debug("waiting for peer connection to reconnect");
      try {
        if (yield zt(PM), !this.pcManager)
          throw new rt("PC manager is closed");
        yield this.pcManager.ensurePCTransportConnection(void 0, this.peerConnectionTimeout), this.pcState = On.Connected;
      } catch (e) {
        throw this.pcState = On.Disconnected, ye.internal("could not establish PC connection, ".concat(e.message));
      }
    });
  }
  /** @internal */
  publishRpcAck(e, t) {
    return R(this, void 0, void 0, function* () {
      const i = new Bt({
        destinationIdentities: [e],
        kind: ba.RELIABLE,
        value: {
          case: "rpcAck",
          value: new tu({
            requestId: t
          })
        }
      });
      yield this.sendDataPacket(i, _e.RELIABLE);
    });
  }
  /* @internal */
  sendDataPacket(e, t) {
    return R(this, void 0, void 0, function* () {
      var i, s;
      if (yield this.ensurePublisherConnected(t), this.e2eeManager && this.e2eeManager.isDataChannelEncryptionEnabled) {
        const d = Bw(e);
        if (d) {
          const h = yield this.e2eeManager.encryptData(d.toBinary());
          e.value = {
            case: "encryptedPacket",
            value: new cT({
              encryptedValue: h.payload,
              iv: h.iv,
              keyIndex: h.keyIndex
            })
          };
        }
      }
      t === _e.RELIABLE && (e.sequence = this.reliableDataSequence, this.reliableDataSequence += 1);
      const c = e.toBinary(), u = Math.min((s = (i = this.pcManager) === null || i === void 0 ? void 0 : i.getMaxPublisherMessageSize()) !== null && s !== void 0 ? s : s0, s0);
      if (typeof u < "u" && u !== 0 && c.byteLength > u)
        throw new Y1("cannot publish data packet larger than ".concat(u, " bytes (got ").concat(c.byteLength, ")"));
      switch (t) {
        case _e.LOSSY:
        case _e.DATA_TRACK_LOSSY:
          return this.sendLossyBytes(c, t);
        case _e.RELIABLE:
          const d = this.dataChannelForKind(t);
          if (d) {
            if (yield this.waitForBufferStatusLow(t), this.reliableMessageBuffer.push({
              data: c,
              sequence: e.sequence
            }), this.attemptingReconnect)
              return;
            d.send(c);
          }
          this.updateAndEmitDCBufferStatus(t);
          break;
      }
    });
  }
  /* @internal */
  sendLossyBytes(e, t) {
    return R(this, arguments, void 0, function(i, s) {
      var c = this;
      let u = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "drop";
      return (function* () {
        yield c.ensurePublisherConnected(s);
        const d = c.dataChannelForKind(s);
        if (d) {
          if (!c.isBufferStatusLow(s))
            switch (u) {
              case "wait":
                yield c.waitForBufferStatusLow(s);
                break;
              case "drop":
                c.lossyDataDropCount += 1, c.lossyDataDropCount % 100 === 0 && c.log.warn("dropping lossy data channel messages, total dropped: ".concat(c.lossyDataDropCount));
                return;
            }
          if (c.lossyDataStatCurrentBytes += i.byteLength, c.attemptingReconnect)
            return;
          d.send(i);
        }
        c.updateAndEmitDCBufferStatus(s);
      })();
    });
  }
  resendReliableMessagesForResume(e) {
    return R(this, void 0, void 0, function* () {
      yield this.ensurePublisherConnected(_e.RELIABLE);
      const t = this.dataChannelForKind(_e.RELIABLE);
      t && (this.reliableMessageBuffer.popToSequence(e), this.reliableMessageBuffer.getAll().forEach((i) => {
        t.send(i.data);
      })), this.updateAndEmitDCBufferStatus(_e.RELIABLE);
    });
  }
  waitForBufferStatusLow(e) {
    return R(this, void 0, void 0, function* () {
      return new Gt((t, i) => R(this, void 0, void 0, function* () {
        if (this.isClosed && i(new rt("engine closed")), this.isBufferStatusLow(e))
          t();
        else {
          const s = this.dataChannelForKind(e);
          if (!s) {
            i(new rt("DataChannel not found, kind: ".concat(e)));
            return;
          }
          this.bufferStatusLowClosingFuture.promise.catch((c) => i(c)), s.addEventListener("bufferedamountlow", () => t(), {
            once: !0
          });
        }
      }));
    });
  }
  /**
   * @internal
   */
  ensureDataTransportConnected(e) {
    return R(this, arguments, void 0, function(t) {
      var i = this;
      let s = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : this.subscriberPrimary;
      return (function* () {
        var c;
        if (!i.pcManager)
          throw new rt("PC manager is closed");
        const u = s ? i.pcManager.subscriber : i.pcManager.publisher, d = s ? "Subscriber" : "Publisher";
        if (!u)
          throw ye.internal("".concat(d, " connection not set"));
        let h = !1;
        !s && !i.dataChannelForKind(t, s) && (i.createDataChannels(), h = !0), !h && !s && !i.pcManager.publisher.isICEConnected && i.pcManager.publisher.getICEConnectionState() !== "checking" && (h = !0), h && i.negotiate().catch((v) => {
          i.log.error(v);
        });
        const f = i.dataChannelForKind(t, s);
        if (f?.readyState === "open")
          return;
        const p = (/* @__PURE__ */ new Date()).getTime() + i.peerConnectionTimeout;
        for (; (/* @__PURE__ */ new Date()).getTime() < p; ) {
          if (u.isICEConnected && ((c = i.dataChannelForKind(t, s)) === null || c === void 0 ? void 0 : c.readyState) === "open")
            return;
          yield zt(50);
        }
        throw ye.internal("could not establish ".concat(d, " connection, state: ").concat(u.getICEConnectionState()));
      })();
    });
  }
  ensurePublisherConnected(e) {
    return R(this, void 0, void 0, function* () {
      this.publisherConnectionPromise || (this.publisherConnectionPromise = this.ensureDataTransportConnected(e, !1)), yield this.publisherConnectionPromise;
    });
  }
  /* @internal */
  verifyTransport() {
    return !(!this.pcManager || ![st.CONNECTING, st.CONNECTED].includes(this.pcManager.currentState) || !this.client.ws || this.client.ws.readyState === WebSocket.CLOSED);
  }
  /** @internal */
  negotiate() {
    return R(this, void 0, void 0, function* () {
      return new Gt((e, t) => R(this, void 0, void 0, function* () {
        if (!this.pcManager) {
          t(new er("PC manager is closed"));
          return;
        }
        this.pcManager.requirePublisher(), this.pcManager.publisher.getTransceivers().length == 0 && !this.lossyDC && !this.reliableDC && !this.dataTrackDC && this.createDataChannels();
        const i = new AbortController(), s = () => {
          i.abort(), this.log.debug("engine disconnected while negotiation was ongoing"), e();
        };
        this.isClosed && t(new er("cannot negotiate on closed engine")), this.on(ae.Closing, s), this.on(ae.Restarting, s), this.pcManager.publisher.off(ga.RTPVideoPayloadTypes, this.onRtpMapAvailable), this.pcManager.publisher.once(ga.RTPVideoPayloadTypes, this.onRtpMapAvailable);
        try {
          yield this.pcManager.negotiate(i), e();
        } catch (c) {
          if (i.signal.aborted) {
            e();
            return;
          }
          c instanceof er && (this.fullReconnectOnNext = !0), this.handleDisconnect("negotiation", Ka.RR_UNKNOWN), c instanceof Error ? t(c) : t(new Error(String(c)));
        } finally {
          this.off(ae.Closing, s), this.off(ae.Restarting, s);
        }
      }));
    });
  }
  dataChannelForKind(e, t) {
    switch (e) {
      case _e.RELIABLE:
        return t ? this.reliableDCSub : this.reliableDC;
      case _e.LOSSY:
        return t ? this.lossyDCSub : this.lossyDC;
      case _e.DATA_TRACK_LOSSY:
        return t ? this.dataTrackDCSub : this.dataTrackDC;
    }
  }
  /** @internal */
  sendSyncState(e, t, i) {
    var s, c, u, d;
    if (!this.pcManager) {
      this.log.warn("sync state cannot be sent without peer connection setup");
      return;
    }
    const h = this.pcManager.publisher.getLocalDescription(), f = this.pcManager.publisher.getRemoteDescription(), p = (s = this.pcManager.subscriber) === null || s === void 0 ? void 0 : s.getRemoteDescription(), v = (c = this.pcManager.subscriber) === null || c === void 0 ? void 0 : c.getLocalDescription(), y = (d = (u = this.signalOpts) === null || u === void 0 ? void 0 : u.autoSubscribe) !== null && d !== void 0 ? d : !0, T = new Array(), _ = new Array();
    e.forEach((k) => {
      k.isDesired !== y && T.push(k.trackSid), k.isEnabled || _.push(k.trackSid);
    }), this.client.sendSyncState(new dm({
      answer: this.pcManager.mode === "publisher-only" ? f ? Xa({
        sdp: f.sdp,
        type: f.type
      }) : void 0 : v ? Xa({
        sdp: v.sdp,
        type: v.type
      }) : void 0,
      offer: this.pcManager.mode === "publisher-only" ? h ? Xa({
        sdp: h.sdp,
        type: h.type
      }) : void 0 : p ? Xa({
        sdp: p.sdp,
        type: p.type
      }) : void 0,
      subscription: new cu({
        trackSids: T,
        subscribe: !y,
        participantTracks: []
      }),
      publishTracks: ew(t),
      dataChannels: this.dataChannelsInfo(),
      trackSidsDisabled: _,
      datachannelReceiveStates: this.reliableReceivedState.map((k, x) => new NT({
        publisherSid: x,
        lastSeq: k
      })),
      publishDataTracks: i.map((k) => new rm({
        info: ql.toProtobuf(k)
      }))
    }));
  }
  /* @internal */
  failNext() {
    this.shouldFailNext = !0;
  }
  /* @internal */
  failNextV1Path() {
    this.shouldFailOnV1Path = !0;
  }
  dataChannelsInfo() {
    const e = [], t = (i, s) => {
      i?.id !== void 0 && i.id !== null && e.push(new IT({
        label: i.label,
        id: i.id,
        target: s
      }));
    };
    return t(this.dataChannelForKind(_e.LOSSY), qn.PUBLISHER), t(this.dataChannelForKind(_e.RELIABLE), qn.PUBLISHER), t(this.dataChannelForKind(_e.LOSSY, !0), qn.SUBSCRIBER), t(this.dataChannelForKind(_e.RELIABLE, !0), qn.SUBSCRIBER), e;
  }
  clearReconnectTimeout() {
    this.reconnectTimeout && bt.clearTimeout(this.reconnectTimeout);
  }
  clearPendingReconnect() {
    this.clearReconnectTimeout(), this.reconnectAttempts = 0;
  }
  registerOnLineListener() {
    an() && (window.addEventListener("online", this.handleBrowserOnLine), window.addEventListener("offline", this.handleBrowserOffline));
  }
  deregisterOnLineListener() {
    an() && (window.removeEventListener("online", this.handleBrowserOnLine), window.removeEventListener("offline", this.handleBrowserOffline));
  }
  getTrackIdForReceiver(e) {
    var t;
    const i = (t = this.pcManager) === null || t === void 0 ? void 0 : t.getMidForReceiver(e);
    if (i) {
      const s = Object.entries(this.midToTrackId).find((c) => ge(c, 1)[0] === i);
      if (s)
        return s[1];
    }
  }
}
function o0(a, e) {
  const t = a.participantIdentity ? a.participantIdentity : e.participantIdentity;
  a.participantIdentity = t, e.participantIdentity = t;
  const i = a.destinationIdentities.length !== 0 ? a.destinationIdentities : e.destinationIdentities;
  a.destinationIdentities = i, e.destinationIdentities = i;
}
const Hh = 5e3, zM = 3e4;
class Ue {
  static fetchRegionSettings(e, t, i) {
    return R(this, void 0, void 0, function* () {
      const s = yield Ue.fetchLock.lock();
      try {
        const c = yield fetch("".concat(FM(e), "/regions"), {
          headers: {
            authorization: "Bearer ".concat(t)
          },
          signal: i
        });
        if (c.ok) {
          const u = _w(c.headers), d = u ? u * 1e3 : Hh;
          return {
            regionSettings: yield c.json(),
            updatedAtInMs: Date.now(),
            maxAgeInMs: d
          };
        } else
          throw c.status === 401 ? ye.notAllowed("Could not fetch region settings: ".concat(c.statusText), c.status) : ye.internal("Could not fetch region settings: ".concat(c.statusText));
      } catch (c) {
        throw c instanceof ye ? c : i?.aborted ? ye.cancelled("Region fetching was aborted") : ye.serverUnreachable("Could not fetch region settings, ".concat(c instanceof Error ? "".concat(c.name, ": ").concat(c.message) : c));
      } finally {
        s();
      }
    });
  }
  static scheduleRefetch(e, t, i) {
    return R(this, void 0, void 0, function* () {
      const s = Ue.settingsTimeouts.get(e.hostname);
      clearTimeout(s), Ue.settingsTimeouts.set(e.hostname, setTimeout(() => R(this, void 0, void 0, function* () {
        try {
          const c = yield Ue.fetchRegionSettings(e, t);
          Ue.updateCachedRegionSettings(e, t, c);
        } catch (c) {
          if (c instanceof ye && c.reason === We.NotAllowed) {
            Ee.debug("token is not valid, cancelling auto region refresh");
            return;
          }
          Ee.debug("auto refetching of region settings failed", {
            error: c
          }), Ue.scheduleRefetch(e, t, i);
        }
      }), i));
    });
  }
  static updateCachedRegionSettings(e, t, i) {
    Ue.cache.set(e.hostname, i), Ue.scheduleRefetch(e, t, i.maxAgeInMs);
  }
  static stopRefetch(e) {
    const t = Ue.settingsTimeouts.get(e);
    t && (clearTimeout(t), Ue.settingsTimeouts.delete(e));
  }
  static scheduleCleanup(e) {
    let t = Ue.connectionTrackers.get(e);
    t && (t.cleanupTimeout && clearTimeout(t.cleanupTimeout), t.cleanupTimeout = setTimeout(() => {
      const i = Ue.connectionTrackers.get(e);
      i && i.connectionCount === 0 && (Ee.debug("stopping region refetch after disconnect delay", {
        hostname: e
      }), Ue.stopRefetch(e)), i && (i.cleanupTimeout = void 0);
    }, zM));
  }
  static cancelCleanup(e) {
    const t = Ue.connectionTrackers.get(e);
    t?.cleanupTimeout && (clearTimeout(t.cleanupTimeout), t.cleanupTimeout = void 0);
  }
  notifyConnected() {
    const e = this.serverUrl.hostname;
    let t = Ue.connectionTrackers.get(e);
    t || (t = {
      connectionCount: 0
    }, Ue.connectionTrackers.set(e, t)), t.connectionCount++, Ue.cancelCleanup(e);
  }
  notifyDisconnected() {
    const e = this.serverUrl.hostname, t = Ue.connectionTrackers.get(e);
    t && (t.connectionCount = Math.max(0, t.connectionCount - 1), t.connectionCount === 0 && Ue.scheduleCleanup(e));
  }
  constructor(e, t) {
    this.attemptedRegions = [], this.serverUrl = new URL(e), this.token = t;
  }
  updateToken(e) {
    var t;
    this.token = e;
    const i = this.getServerUrl(), s = Ue.cache.get(i.hostname);
    Ue.scheduleRefetch(this.serverUrl, this.token, (t = s?.maxAgeInMs) !== null && t !== void 0 ? t : Hh);
  }
  isCloud() {
    return fs(this.serverUrl);
  }
  getServerUrl() {
    return this.serverUrl;
  }
  /** @internal */
  fetchRegionSettings(e) {
    return R(this, void 0, void 0, function* () {
      return Ue.fetchRegionSettings(this.serverUrl, this.token, e);
    });
  }
  getNextBestRegionUrl(e) {
    return R(this, void 0, void 0, function* () {
      if (!this.isCloud())
        throw Error("region availability is only supported for LiveKit Cloud domains");
      let t = Ue.cache.get(this.serverUrl.hostname);
      (!t || Date.now() - t.updatedAtInMs > t.maxAgeInMs) && (t = yield this.fetchRegionSettings(e), Ue.updateCachedRegionSettings(this.serverUrl, this.token, t));
      const i = t.regionSettings.regions.filter((s) => !this.attemptedRegions.find((c) => c.url === s.url));
      if (i.length > 0) {
        const s = i[0];
        return this.attemptedRegions.push(s), Ee.debug("next region: ".concat(s.region)), s.url;
      } else
        return null;
    });
  }
  resetAttempts() {
    this.attemptedRegions = [];
  }
  setServerReportedRegions(e) {
    Ue.updateCachedRegionSettings(this.serverUrl, this.token, e);
  }
}
Ue.cache = /* @__PURE__ */ new Map();
Ue.settingsTimeouts = /* @__PURE__ */ new Map();
Ue.connectionTrackers = /* @__PURE__ */ new Map();
Ue.fetchLock = new Lt();
function FM(a) {
  return "".concat(a.protocol.replace("ws", "http"), "//").concat(a.host, "/settings");
}
class US {
  get info() {
    return this._info;
  }
  /** @internal */
  validateBytesReceived() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !1;
    if (!(typeof this.totalByteSize != "number" || this.totalByteSize === 0)) {
      if (e && this.bytesReceived < this.totalByteSize)
        throw new Dn("Not enough chunk(s) received - expected ".concat(this.totalByteSize, " bytes of data total, only received ").concat(this.bytesReceived, " bytes"), Ht.Incomplete);
      if (this.bytesReceived > this.totalByteSize)
        throw new Dn("Extra chunk(s) received - expected ".concat(this.totalByteSize, " bytes of data total, received ").concat(this.bytesReceived, " bytes"), Ht.LengthExceeded);
    }
  }
  constructor(e, t, i) {
    this.reader = t, this.totalByteSize = i, this._info = e, this.bytesReceived = 0;
  }
}
class jM extends US {
  handleChunkReceived(e) {
    var t;
    this.bytesReceived += e.content.byteLength, this.validateBytesReceived();
    const i = this.totalByteSize ? this.bytesReceived / this.totalByteSize : void 0;
    (t = this.onProgress) === null || t === void 0 || t.call(this, i);
  }
  [Symbol.asyncIterator]() {
    const e = this.reader.getReader();
    e.closed.catch(() => {
    });
    const t = () => {
      e.releaseLock(), this.signal = void 0;
    };
    return {
      next: () => R(this, void 0, void 0, function* () {
        try {
          const i = this.signal;
          if (i?.aborted)
            throw i.reason;
          const s = yield new Promise((c, u) => {
            if (i) {
              const d = () => u(i.reason);
              i.addEventListener("abort", d, {
                once: !0
              }), e.read().then(c, u).finally(() => {
                i.removeEventListener("abort", d);
              });
            } else
              e.read().then(c, u);
          });
          return s.done ? (this.validateBytesReceived(!0), {
            done: !0,
            value: void 0
          }) : (this.handleChunkReceived(s.value), {
            done: !1,
            value: s.value.content
          });
        } catch (i) {
          throw t(), i;
        }
      }),
      // note: `return` runs only for premature exits, see:
      // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols#errors_during_iteration
      return() {
        return R(this, void 0, void 0, function* () {
          return t(), {
            done: !0,
            value: void 0
          };
        });
      }
    };
  }
  /**
   * Injects an AbortSignal, which if aborted, will terminate the currently active
   * stream iteration operation.
   *
   * Note that when using AbortSignal.timeout(...), the timeout applies across
   * the whole iteration operation, not just one individual chunk read.
   */
  withAbortSignal(e) {
    return this.signal = e, this;
  }
  readAll() {
    return R(this, arguments, void 0, function() {
      var e = this;
      let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
      return (function* () {
        var i, s, c, u;
        let d = /* @__PURE__ */ new Set();
        const h = t.signal ? e.withAbortSignal(t.signal) : e;
        try {
          for (var f = !0, p = Hn(h), v; v = yield p.next(), i = v.done, !i; f = !0) {
            u = v.value, f = !1;
            const y = u;
            d.add(y);
          }
        } catch (y) {
          s = {
            error: y
          };
        } finally {
          try {
            !f && !i && (c = p.return) && (yield c.call(p));
          } finally {
            if (s) throw s.error;
          }
        }
        return Array.from(d);
      })();
    });
  }
}
class qM extends US {
  /**
   * A TextStreamReader instance can be used as an AsyncIterator that returns the entire string
   * that has been received up to the current point in time.
   */
  constructor(e, t, i) {
    super(e, t, i), this.receivedChunks = /* @__PURE__ */ new Map();
  }
  handleChunkReceived(e) {
    var t;
    const i = Al(e.chunkIndex), s = this.receivedChunks.get(i);
    if (s && s.version > e.version)
      return;
    this.receivedChunks.set(i, e), this.bytesReceived += e.content.byteLength, this.validateBytesReceived();
    const c = this.totalByteSize ? this.bytesReceived / this.totalByteSize : void 0;
    (t = this.onProgress) === null || t === void 0 || t.call(this, c);
  }
  /**
   * Async iterator implementation to allow usage of `for await...of` syntax.
   * Yields structured chunks from the stream.
   *
   */
  [Symbol.asyncIterator]() {
    const e = this.reader.getReader();
    e.closed.catch(() => {
    });
    const t = new TextDecoder("utf-8", {
      fatal: !0
    }), i = this.signal, s = () => {
      e.releaseLock(), this.signal = void 0;
    };
    return {
      next: () => R(this, void 0, void 0, function* () {
        try {
          if (i?.aborted)
            throw i.reason;
          const c = yield new Promise((u, d) => {
            if (i) {
              const h = () => d(i.reason);
              i.addEventListener("abort", h, {
                once: !0
              }), e.read().then(u, d).finally(() => {
                i.removeEventListener("abort", h);
              });
            } else
              e.read().then(u, d);
          });
          if (c.done)
            return this.validateBytesReceived(!0), {
              done: !0,
              value: void 0
            };
          {
            this.handleChunkReceived(c.value);
            let u;
            try {
              u = t.decode(c.value.content);
            } catch (d) {
              throw new Dn("Cannot decode datastream chunk ".concat(c.value.chunkIndex, " as text: ").concat(d), Ht.DecodeFailed);
            }
            return {
              done: !1,
              value: u
            };
          }
        } catch (c) {
          throw s(), c;
        }
      }),
      // note: `return` runs only for premature exits, see:
      // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols#errors_during_iteration
      return() {
        return R(this, void 0, void 0, function* () {
          return s(), {
            done: !0,
            value: void 0
          };
        });
      }
    };
  }
  /**
   * Injects an AbortSignal, which if aborted, will terminate the currently active
   * stream iteration operation.
   *
   * Note that when using AbortSignal.timeout(...), the timeout applies across
   * the whole iteration operation, not just one individual chunk read.
   */
  withAbortSignal(e) {
    return this.signal = e, this;
  }
  readAll() {
    return R(this, arguments, void 0, function() {
      var e = this;
      let t = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
      return (function* () {
        var i, s, c, u;
        let d = "";
        const h = t.signal ? e.withAbortSignal(t.signal) : e;
        try {
          for (var f = !0, p = Hn(h), v; v = yield p.next(), i = v.done, !i; f = !0)
            u = v.value, f = !1, d += u;
        } catch (y) {
          s = {
            error: y
          };
        } finally {
          try {
            !f && !i && (c = p.return) && (yield c.call(p));
          } finally {
            if (s) throw s.error;
          }
        }
        return d;
      })();
    });
  }
}
class HM {
  constructor() {
    this.log = Ee, this.byteStreamControllers = /* @__PURE__ */ new Map(), this.textStreamControllers = /* @__PURE__ */ new Map(), this.byteStreamHandlers = /* @__PURE__ */ new Map(), this.textStreamHandlers = /* @__PURE__ */ new Map(), this.isConnected = !1, this.bufferedPackets = [];
  }
  setConnected(e) {
    this.isConnected = e, e && this.flushBufferedPackets();
  }
  flushBufferedPackets() {
    const e = this.bufferedPackets;
    this.bufferedPackets = [];
    for (const t of e) {
      const i = t.packet, s = t.encryptionType;
      this.handleDataStreamPacket(i, s);
    }
  }
  registerTextStreamHandler(e, t) {
    if (this.textStreamHandlers.has(e))
      throw new Dn('A text stream handler for topic "'.concat(e, '" has already been set.'), Ht.HandlerAlreadyRegistered);
    this.textStreamHandlers.set(e, t);
  }
  unregisterTextStreamHandler(e) {
    this.textStreamHandlers.delete(e);
  }
  registerByteStreamHandler(e, t) {
    if (this.byteStreamHandlers.has(e))
      throw new Dn('A byte stream handler for topic "'.concat(e, '" has already been set.'), Ht.HandlerAlreadyRegistered);
    this.byteStreamHandlers.set(e, t);
  }
  unregisterByteStreamHandler(e) {
    this.byteStreamHandlers.delete(e);
  }
  clearControllers() {
    this.byteStreamControllers.clear(), this.textStreamControllers.clear(), this.bufferedPackets = [];
  }
  validateParticipantHasNoActiveDataStreams(e) {
    const t = Array.from(this.textStreamControllers.entries()).filter((u) => u[1].sendingParticipantIdentity === e), i = Array.from(this.byteStreamControllers.entries()).filter((u) => u[1].sendingParticipantIdentity === e);
    if (t.length > 0 || i.length > 0) {
      const u = new Dn("Participant ".concat(e, " unexpectedly disconnected in the middle of sending data"), Ht.AbnormalEnd);
      for (const d of i) {
        var s = ge(d, 2);
        const h = s[0];
        s[1].controller.error(u), this.byteStreamControllers.delete(h);
      }
      for (const d of t) {
        var c = ge(d, 2);
        const h = c[0];
        c[1].controller.error(u), this.textStreamControllers.delete(h);
      }
    }
  }
  handleDataStreamPacket(e, t) {
    if (!this.isConnected) {
      this.bufferedPackets.push({
        packet: e,
        encryptionType: t
      });
      return;
    }
    switch (e.value.case) {
      case "streamHeader":
        return this.handleStreamHeader(e.value.value, e.participantIdentity, t);
      case "streamChunk":
        return this.handleStreamChunk(e.value.value, t);
      case "streamTrailer":
        return this.handleStreamTrailer(e.value.value, t);
      default:
        throw new Error('DataPacket of value "'.concat(e.value.case, '" is not data stream related!'));
    }
  }
  handleStreamHeader(e, t, i) {
    var s;
    if (e.contentHeader.case === "byteHeader") {
      const c = this.byteStreamHandlers.get(e.topic);
      if (!c) {
        this.log.debug("ignoring incoming byte stream due to no handler for topic", e.topic);
        return;
      }
      let u;
      const d = {
        id: e.streamId,
        name: (s = e.contentHeader.value.name) !== null && s !== void 0 ? s : "unknown",
        mimeType: e.mimeType,
        size: e.totalLength ? Number(e.totalLength) : void 0,
        topic: e.topic,
        timestamp: Al(e.timestamp),
        attributes: e.attributes,
        encryptionType: i
      }, h = new ReadableStream({
        start: (f) => {
          if (u = f, this.textStreamControllers.has(e.streamId))
            throw new Dn("A data stream read is already in progress for a stream with id ".concat(e.streamId, "."), Ht.AlreadyOpened);
          this.byteStreamControllers.set(e.streamId, {
            info: d,
            controller: u,
            startTime: Date.now(),
            sendingParticipantIdentity: t
          });
        }
      });
      c(new jM(d, h, Al(e.totalLength)), {
        identity: t
      });
    } else if (e.contentHeader.case === "textHeader") {
      const c = this.textStreamHandlers.get(e.topic);
      if (!c) {
        this.log.debug("ignoring incoming text stream due to no handler for topic", e.topic);
        return;
      }
      let u;
      const d = {
        id: e.streamId,
        mimeType: e.mimeType,
        size: e.totalLength ? Number(e.totalLength) : void 0,
        topic: e.topic,
        timestamp: Number(e.timestamp),
        attributes: e.attributes,
        encryptionType: i,
        attachedStreamIds: e.contentHeader.value.attachedStreamIds
      }, h = new ReadableStream({
        start: (f) => {
          if (u = f, this.textStreamControllers.has(e.streamId))
            throw new Dn("A data stream read is already in progress for a stream with id ".concat(e.streamId, "."), Ht.AlreadyOpened);
          this.textStreamControllers.set(e.streamId, {
            info: d,
            controller: u,
            startTime: Date.now(),
            sendingParticipantIdentity: t
          });
        }
      });
      c(new qM(d, h, Al(e.totalLength)), {
        identity: t
      });
    }
  }
  handleStreamChunk(e, t) {
    const i = this.byteStreamControllers.get(e.streamId);
    i && (i.info.encryptionType !== t ? (i.controller.error(new Dn("Encryption type mismatch for stream ".concat(e.streamId, ". Expected ").concat(t, ", got ").concat(i.info.encryptionType), Ht.EncryptionTypeMismatch)), this.byteStreamControllers.delete(e.streamId)) : e.content.length > 0 && i.controller.enqueue(e));
    const s = this.textStreamControllers.get(e.streamId);
    s && (s.info.encryptionType !== t ? (s.controller.error(new Dn("Encryption type mismatch for stream ".concat(e.streamId, ". Expected ").concat(t, ", got ").concat(s.info.encryptionType), Ht.EncryptionTypeMismatch)), this.textStreamControllers.delete(e.streamId)) : e.content.length > 0 && s.controller.enqueue(e));
  }
  handleStreamTrailer(e, t) {
    const i = this.textStreamControllers.get(e.streamId);
    i && (i.info.encryptionType !== t ? i.controller.error(new Dn("Encryption type mismatch for stream ".concat(e.streamId, ". Expected ").concat(t, ", got ").concat(i.info.encryptionType), Ht.EncryptionTypeMismatch)) : (i.info.attributes = Object.assign(Object.assign({}, i.info.attributes), e.attributes), i.controller.close(), this.textStreamControllers.delete(e.streamId)));
    const s = this.byteStreamControllers.get(e.streamId);
    s && (s.info.encryptionType !== t ? s.controller.error(new Dn("Encryption type mismatch for stream ".concat(e.streamId, ". Expected ").concat(t, ", got ").concat(s.info.encryptionType), Ht.EncryptionTypeMismatch)) : (s.info.attributes = Object.assign(Object.assign({}, s.info.attributes), e.attributes), s.controller.close()), this.byteStreamControllers.delete(e.streamId));
  }
}
class BS {
  constructor(e, t, i) {
    this.writableStream = e, this.defaultWriter = e.getWriter(), this.onClose = i, this.info = t;
  }
  write(e) {
    return this.defaultWriter.write(e);
  }
  close() {
    return R(this, void 0, void 0, function* () {
      var e;
      yield this.defaultWriter.close(), this.defaultWriter.releaseLock(), (e = this.onClose) === null || e === void 0 || e.call(this);
    });
  }
}
class VM extends BS {
}
class GM extends BS {
}
const c0 = 15e3;
class KM {
  constructor(e, t) {
    this.engine = e, this.log = t;
  }
  setupEngine(e) {
    this.engine = e;
  }
  /** {@inheritDoc LocalParticipant.sendText} */
  sendText(e, t) {
    return R(this, void 0, void 0, function* () {
      var i;
      const s = crypto.randomUUID(), u = new TextEncoder().encode(e).byteLength, d = (i = t?.attachments) === null || i === void 0 ? void 0 : i.map(() => crypto.randomUUID()), h = new Array(d ? d.length + 1 : 1).fill(0), f = (v, y) => {
        var T;
        h[y] = v;
        const _ = h.reduce((k, x) => k + x, 0);
        (T = t?.onProgress) === null || T === void 0 || T.call(t, _);
      }, p = yield this.streamText({
        streamId: s,
        totalSize: u,
        destinationIdentities: t?.destinationIdentities,
        topic: t?.topic,
        attachedStreamIds: d,
        attributes: t?.attributes
      });
      return yield p.write(e), f(1, 0), yield p.close(), t?.attachments && d && (yield Promise.all(t.attachments.map((v, y) => R(this, void 0, void 0, function* () {
        return this._sendFile(d[y], v, {
          topic: t.topic,
          mimeType: v.type,
          onProgress: (T) => {
            f(T, y + 1);
          }
        });
      })))), p.info;
    });
  }
  /**
   * @internal
   */
  streamText(e) {
    return R(this, void 0, void 0, function* () {
      var t, i, s;
      const c = (t = e?.streamId) !== null && t !== void 0 ? t : crypto.randomUUID(), u = {
        id: c,
        mimeType: "text/plain",
        timestamp: Date.now(),
        topic: (i = e?.topic) !== null && i !== void 0 ? i : "",
        size: e?.totalSize,
        attributes: e?.attributes,
        encryptionType: !((s = this.engine.e2eeManager) === null || s === void 0) && s.isDataChannelEncryptionEnabled ? $e.GCM : $e.NONE,
        attachedStreamIds: e?.attachedStreamIds
      }, d = new Ll({
        streamId: c,
        mimeType: u.mimeType,
        topic: u.topic,
        timestamp: Ya(u.timestamp),
        totalLength: Ya(u.size),
        attributes: u.attributes,
        contentHeader: {
          case: "textHeader",
          value: new ST({
            version: e?.version,
            attachedStreamIds: u.attachedStreamIds,
            replyToStreamId: e?.replyToStreamId,
            operationType: e?.type === "update" ? vh.UPDATE : vh.CREATE
          })
        }
      }), h = e?.destinationIdentities, f = new Bt({
        destinationIdentities: h,
        value: {
          case: "streamHeader",
          value: d
        }
      });
      yield this.engine.sendDataPacket(f, _e.RELIABLE);
      let p = 0;
      const v = this.engine, y = new WritableStream({
        // Implement the sink
        write(k) {
          return R(this, void 0, void 0, function* () {
            for (const x of Ew(k, c0)) {
              const A = new Ul({
                content: x,
                streamId: c,
                chunkIndex: Ya(p)
              }), B = new Bt({
                destinationIdentities: h,
                value: {
                  case: "streamChunk",
                  value: A
                }
              });
              yield v.sendDataPacket(B, _e.RELIABLE), p += 1;
            }
          });
        },
        close() {
          return R(this, void 0, void 0, function* () {
            const k = new Bl({
              streamId: c
            }), x = new Bt({
              destinationIdentities: h,
              value: {
                case: "streamTrailer",
                value: k
              }
            });
            yield v.sendDataPacket(x, _e.RELIABLE);
          });
        },
        abort(k) {
          console.log("Sink error:", k);
        }
      });
      let T = () => R(this, void 0, void 0, function* () {
        yield _.close();
      });
      v.once(ae.Closing, T);
      const _ = new VM(y, u, () => this.engine.off(ae.Closing, T));
      return _;
    });
  }
  sendFile(e, t) {
    return R(this, void 0, void 0, function* () {
      const i = crypto.randomUUID();
      return yield this._sendFile(i, e, t), {
        id: i
      };
    });
  }
  _sendFile(e, t, i) {
    return R(this, void 0, void 0, function* () {
      var s;
      const c = yield this.streamBytes({
        streamId: e,
        totalSize: t.size,
        name: t.name,
        mimeType: (s = i?.mimeType) !== null && s !== void 0 ? s : t.type,
        topic: i?.topic,
        destinationIdentities: i?.destinationIdentities
      }), u = t.stream().getReader();
      for (; ; ) {
        const d = yield u.read(), h = d.done, f = d.value;
        if (h)
          break;
        yield c.write(f);
      }
      return yield c.close(), c.info;
    });
  }
  streamBytes(e) {
    return R(this, void 0, void 0, function* () {
      var t, i, s, c, u;
      const d = (t = e?.streamId) !== null && t !== void 0 ? t : crypto.randomUUID(), h = e?.destinationIdentities, f = {
        id: d,
        mimeType: (i = e?.mimeType) !== null && i !== void 0 ? i : "application/octet-stream",
        topic: (s = e?.topic) !== null && s !== void 0 ? s : "",
        timestamp: Date.now(),
        attributes: e?.attributes,
        size: e?.totalSize,
        name: (c = e?.name) !== null && c !== void 0 ? c : "unknown",
        encryptionType: !((u = this.engine.e2eeManager) === null || u === void 0) && u.isDataChannelEncryptionEnabled ? $e.GCM : $e.NONE
      }, p = new Ll({
        totalLength: Ya(f.size),
        mimeType: f.mimeType,
        streamId: d,
        topic: f.topic,
        timestamp: Ya(Date.now()),
        attributes: f.attributes,
        contentHeader: {
          case: "byteHeader",
          value: new kT({
            name: f.name
          })
        }
      }), v = new Bt({
        destinationIdentities: h,
        value: {
          case: "streamHeader",
          value: p
        }
      });
      yield this.engine.sendDataPacket(v, _e.RELIABLE);
      let y = 0;
      const T = new Lt(), _ = this.engine, k = this.log, x = new WritableStream({
        write(B) {
          return R(this, void 0, void 0, function* () {
            const ie = yield T.lock();
            let C = 0;
            try {
              for (; C < B.byteLength; ) {
                const E = B.slice(C, C + c0), M = new Bt({
                  destinationIdentities: h,
                  value: {
                    case: "streamChunk",
                    value: new Ul({
                      content: E,
                      streamId: d,
                      chunkIndex: Ya(y)
                    })
                  }
                });
                yield _.sendDataPacket(M, _e.RELIABLE), y += 1, C += E.byteLength;
              }
            } finally {
              ie();
            }
          });
        },
        close() {
          return R(this, void 0, void 0, function* () {
            const B = new Bl({
              streamId: d
            }), ie = new Bt({
              destinationIdentities: h,
              value: {
                case: "streamTrailer",
                value: B
              }
            });
            yield _.sendDataPacket(ie, _e.RELIABLE);
          });
        },
        abort(B) {
          k.error("Sink error:", B);
        }
      });
      return new GM(x, f);
    });
  }
}
function zS(a) {
  if (a.length === 0)
    return new AbortController().signal;
  if (a.length === 1)
    return a[0];
  for (const s of a)
    if (s.aborted)
      return s;
  const e = new AbortController(), t = Array(a.length), i = () => {
    for (const s of t)
      s();
  };
  return a.forEach((s, c) => {
    const u = () => {
      e.abort(s.reason), i();
    };
    s.addEventListener("abort", u), t[c] = () => s.removeEventListener("abort", u);
  }), e.signal;
}
function FS(a) {
  const e = new AbortController();
  return setTimeout(() => {
    e.abort(new DOMException("signal timed out after ".concat(a, " ms"), "TimeoutError"));
  }, a), e.signal;
}
const ut = 1, Mi = 2, l0 = 4, YM = 8, u0 = 0, d0 = 12, f0 = 5, JM = 7, h0 = 3, QM = 3, m0 = 2, p0 = 1, g0 = 0, v0 = 3, dl = 2, y0 = 2, XM = 1, ZM = 0;
var Oi;
(function(a) {
  a[a.TooShort = 0] = "TooShort", a[a.HeaderOverrun = 1] = "HeaderOverrun", a[a.MissingExtWords = 2] = "MissingExtWords", a[a.UnsupportedVersion = 3] = "UnsupportedVersion", a[a.InvalidHandle = 4] = "InvalidHandle", a[a.MalformedExt = 5] = "MalformedExt";
})(Oi || (Oi = {}));
class Vt extends Nn {
  constructor(e, t, i) {
    super(19, e, i), this.name = "DataTrackDeserializeError", this.reason = t, this.reasonName = Oi[t];
  }
  static tooShort() {
    return new Vt("Too short to contain a valid header", Oi.TooShort);
  }
  static headerOverrun() {
    return new Vt("Header exceeds total packet length", Oi.HeaderOverrun);
  }
  static missingExtWords() {
    return new Vt("Extension word indicator is missing", Oi.MissingExtWords);
  }
  static unsupportedVersion(e) {
    return new Vt("Unsupported version ".concat(e), Oi.UnsupportedVersion);
  }
  static invalidHandle(e) {
    return new Vt("invalid track handle: ".concat(e.message), Oi.InvalidHandle, {
      cause: e
    });
  }
  static malformedExt(e) {
    return new Vt("Extension with tag ".concat(e, " is malformed"), Oi.MalformedExt);
  }
}
var Ro;
(function(a) {
  a[a.TooSmallForHeader = 0] = "TooSmallForHeader", a[a.TooSmallForPayload = 1] = "TooSmallForPayload";
})(Ro || (Ro = {}));
class Lo extends Nn {
  constructor(e, t, i) {
    super(19, e, i), this.name = "DataTrackSerializeError", this.reason = t, this.reasonName = Ro[t];
  }
  static tooSmallForHeader() {
    return new Lo("Buffer cannot fit header", Ro.TooSmallForHeader);
  }
  static tooSmallForPayload() {
    return new Lo("Buffer cannot fit payload", Ro.TooSmallForPayload);
  }
}
class fu {
  /** Encodes the instance as binary and returns the data as a Uint8Array. */
  toBinary() {
    const e = this.toBinaryLengthBytes(), t = new ArrayBuffer(e), i = new DataView(t), s = this.toBinaryInto(i);
    if (e !== s)
      throw new Error("".concat(this.constructor.name, ".toBinary: written bytes (").concat(s, " bytes) not equal to allocated array buffer length (").concat(e, " bytes)."));
    return new Uint8Array(t);
  }
}
var ms;
(function(a) {
  a[a.UserTimestamp = 2] = "UserTimestamp", a[a.E2ee = 1] = "E2ee";
})(ms || (ms = {}));
class jS extends fu {
}
class Gn extends jS {
  constructor(e) {
    super(), this.timestamp = e;
  }
  toBinaryLengthBytes() {
    return ut + ut + Gn.lengthBytes;
  }
  toBinaryInto(e) {
    let t = 0;
    e.setUint8(t, Gn.tag), t += ut, e.setUint8(t, Gn.lengthBytes), t += ut, e.setBigUint64(t, this.timestamp), t += YM;
    const i = this.toBinaryLengthBytes();
    if (t !== i)
      throw new Error("DataTrackUserTimestampExtension.toBinaryInto: Wrote ".concat(t, " bytes but expected length was ").concat(i, " bytes"));
    return t;
  }
  toJSON() {
    return {
      tag: Gn.tag,
      lengthBytes: Gn.lengthBytes,
      timestamp: this.timestamp
    };
  }
}
Gn.tag = ms.UserTimestamp;
Gn.lengthBytes = 8;
class Kn extends jS {
  constructor(e, t) {
    super(), this.keyIndex = e, this.iv = t;
  }
  toBinaryLengthBytes() {
    return ut + ut + Kn.lengthBytes;
  }
  toBinaryInto(e) {
    let t = 0;
    e.setUint8(t, Kn.tag), t += ut, e.setUint8(t, Kn.lengthBytes), t += ut, e.setUint8(t, this.keyIndex), t += ut;
    for (let s = 0; s < this.iv.length; s += 1)
      e.setUint8(t, this.iv[s]), t += ut;
    const i = this.toBinaryLengthBytes();
    if (t !== i)
      throw new Error("DataTrackE2eeExtension.toBinaryInto: Wrote ".concat(t, " bytes but expected length was ").concat(i, " bytes"));
    return t;
  }
  toJSON() {
    return {
      tag: Kn.tag,
      lengthBytes: Kn.lengthBytes,
      keyIndex: this.keyIndex,
      iv: this.iv
    };
  }
}
Kn.tag = ms.E2ee;
Kn.lengthBytes = 13;
class ss extends fu {
  constructor() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    super(), this.userTimestamp = e.userTimestamp, this.e2ee = e.e2ee;
  }
  toBinaryLengthBytes() {
    let e = 0;
    return this.userTimestamp && (e += this.userTimestamp.toBinaryLengthBytes()), this.e2ee && (e += this.e2ee.toBinaryLengthBytes()), e;
  }
  toBinaryInto(e) {
    let t = 0;
    if (this.e2ee) {
      const s = this.e2ee.toBinaryInto(e);
      t += s;
    }
    if (this.userTimestamp) {
      const s = this.userTimestamp.toBinaryInto(new DataView(e.buffer, e.byteOffset + t));
      t += s;
    }
    const i = this.toBinaryLengthBytes();
    if (t !== i)
      throw new Error("DataTrackExtensions.toBinaryInto: Wrote ".concat(t, " bytes but expected length was ").concat(i, " bytes"));
    return t;
  }
  static fromBinary(e) {
    const t = Cm(e);
    let i, s, c = 0;
    for (; t.byteLength - c >= ut + ut; ) {
      const u = t.getUint8(c);
      c += ut;
      const d = t.getUint8(c);
      if (c += ut, u !== ZM)
        switch (u) {
          case ms.UserTimestamp:
            if (t.byteLength - c < Gn.lengthBytes)
              throw Vt.malformedExt(u);
            i = new Gn(t.getBigUint64(c)), c += d;
            break;
          case ms.E2ee:
            if (t.byteLength - c < Kn.lengthBytes)
              throw Vt.malformedExt(u);
            const h = t.getUint8(c), f = new Uint8Array(12);
            for (let p = 0; p < f.length; p += 1) {
              let v = c;
              v += ut, v += p * ut, f[p] = t.getUint8(v);
            }
            s = new Kn(h, f), c += d;
            break;
          default:
            if (t.byteLength - c < d)
              throw Vt.malformedExt(u);
            c += d;
            break;
        }
    }
    return [new ss({
      userTimestamp: i,
      e2ee: s
    }), t.byteLength];
  }
  toJSON() {
    var e, t, i, s;
    return {
      userTimestamp: (t = (e = this.userTimestamp) === null || e === void 0 ? void 0 : e.toJSON()) !== null && t !== void 0 ? t : null,
      e2ee: (s = (i = this.e2ee) === null || i === void 0 ? void 0 : i.toJSON()) !== null && s !== void 0 ? s : null
    };
  }
}
const qS = {
  from(a) {
    return {
      payload: a.payload,
      extensions: new ss({
        userTimestamp: a.userTimestamp ? new Gn(a.userTimestamp) : void 0
      })
    };
  },
  /** Converts from a DataTrackFrameInternal -> DataTrackFrame. Some internal information is
   * discarded like e2ee encrption extension data. */
  lossyIntoFrame(a) {
    var e;
    return {
      payload: a.payload,
      userTimestamp: (e = a.extensions.userTimestamp) === null || e === void 0 ? void 0 : e.timestamp
    };
  }
}, HS = Symbol.for("lk.track"), VS = Symbol.for("lk.data-track");
class GS {
  /** @internal */
  constructor(e, t, i) {
    this.trackSymbol = HS, this.isLocal = !1, this.typeSymbol = VS, this.info = e, this.manager = t, this.publisherIdentity = i.publisherIdentity;
  }
  /** Subscribes to the data track to receive frames.
   *
   * # Returns
   *
   * A stream that yields {@link DataTrackFrame}s as they arrive.
   *
   * # Multiple Subscriptions
   *
   * An application may call `subscribe` more than once to process frames in
   * multiple places. For example, one async task might plot values on a graph
   * while another writes them to a file.
   *
   * Internally, only the first call to `subscribe` communicates with the SFU and
   * allocates the resources required to receive frames. Additional subscriptions
   * reuse the same underlying pipeline and do not trigger additional signaling.
   *
   * Note that newly created subscriptions only receive frames published after
   * the initial subscription is established.
   */
  subscribe(e) {
    try {
      const t = this.manager.openSubscriptionStream(this.info.sid, e?.signal, e?.bufferSize), i = ge(t, 2), s = i[0];
      return i[1].catch(() => {
      }), s;
    } catch (t) {
      throw t;
    }
  }
  /** Configure how incoming frames for this track are processed before they are handed out to
   * subscribers (the "pipeline"). These options apply to all current and future subscriptions
   * of this track, and may be set at any time. */
  setPipelineOptions(e) {
    this.manager.setPipelineOptions(this.info.sid, e);
  }
}
class Uo extends fu {
  constructor(e) {
    var t;
    super(), this.marker = e.marker, this.trackHandle = e.trackHandle, this.sequence = e.sequence, this.frameNumber = e.frameNumber, this.timestamp = e.timestamp, this.extensions = (t = e.extensions) !== null && t !== void 0 ? t : new ss();
  }
  extensionsMetrics() {
    const e = this.extensions.toBinaryLengthBytes(), t = Math.ceil((dl + e) / 4), i = t * 4 - dl - e;
    return {
      lengthBytes: e,
      lengthWords: t,
      paddingLengthBytes: i
    };
  }
  toBinaryLengthBytes() {
    const e = this.extensionsMetrics(), t = e.lengthBytes, i = e.paddingLengthBytes;
    let s = d0;
    return t > 0 && (s += dl + t + i), s;
  }
  toBinaryInto(e) {
    if (e.byteLength < this.toBinaryLengthBytes())
      throw Lo.tooSmallForHeader();
    let t = u0 << f0, i;
    switch (this.marker) {
      case it.Inter:
        i = g0;
        break;
      case it.Final:
        i = p0;
        break;
      case it.Start:
        i = m0;
        break;
      case it.Single:
        i = v0;
        break;
    }
    t |= i << h0;
    const s = this.extensionsMetrics(), c = s.lengthBytes, u = s.lengthWords, d = s.paddingLengthBytes;
    c > 0 && (t |= 1 << y0);
    let h = 0;
    if (e.setUint8(h, t), h += ut, e.setUint8(h, 0), h += ut, e.setUint16(h, this.trackHandle), h += Mi, e.setUint16(h, this.sequence.value), h += Mi, e.setUint16(h, this.frameNumber.value), h += Mi, e.setUint32(h, this.timestamp.asTicks()), h += l0, c > 0) {
      const p = u - 1;
      e.setUint16(h, p), h += Mi;
      const v = this.extensions.toBinaryInto(new DataView(e.buffer, e.byteOffset + h));
      h += v;
      for (let y = 0; y < d; y += 1)
        e.setUint8(h, 0), h += ut;
    }
    const f = this.toBinaryLengthBytes();
    if (h !== f)
      throw new Error("DataTrackPacketHeader.toBinaryInto: Wrote ".concat(h, " bytes but expected length was ").concat(f, " bytes"));
    return f;
  }
  static fromBinary(e) {
    const t = Cm(e);
    if (t.byteLength < d0)
      throw Vt.tooShort();
    let i = 0;
    const s = t.getUint8(i);
    i += ut;
    const c = s >> f0 & JM;
    if (c > u0)
      throw Vt.unsupportedVersion(c);
    let u;
    switch (s >> h0 & QM) {
      case m0:
        u = it.Start;
        break;
      case p0:
        u = it.Final;
        break;
      case v0:
        u = it.Single;
        break;
      case g0:
      default:
        u = it.Inter;
        break;
    }
    const d = (s >> y0 & XM) > 0;
    i += ut;
    let h;
    try {
      h = Xw.fromNumber(t.getUint16(i));
    } catch (T) {
      throw T instanceof hs && (T.isReason(nr.Reserved) || T.isReason(nr.TooLarge)) ? Vt.invalidHandle(T) : T;
    }
    i += Mi;
    const f = Jn.u16(t.getUint16(i));
    i += Mi;
    const p = Jn.u16(t.getUint16(i));
    i += Mi;
    const v = rs.fromRtpTicks(t.getUint32(i));
    i += l0;
    let y = new ss();
    if (d) {
      if (t.byteLength - i < Mi)
        throw Vt.missingExtWords();
      let T = t.getUint16(i);
      i += Mi;
      let k = 4 * (T + 1) - dl;
      if (i + k > t.byteLength)
        throw Vt.headerOverrun();
      let x = new DataView(t.buffer, t.byteOffset + i, k);
      const A = ss.fromBinary(x), B = ge(A, 2), ie = B[0], C = B[1];
      y = ie, i += C;
    }
    return [new Uo({
      marker: u,
      trackHandle: h,
      sequence: f,
      frameNumber: p,
      timestamp: v,
      extensions: y
    }), i];
  }
  toJSON() {
    return {
      marker: this.marker,
      trackHandle: this.trackHandle,
      sequence: this.sequence.value,
      frameNumber: this.frameNumber.value,
      timestamp: this.timestamp.asTicks(),
      extensions: this.extensions.toJSON()
    };
  }
}
var it;
(function(a) {
  a[a.Start = 0] = "Start", a[a.Inter = 1] = "Inter", a[a.Final = 2] = "Final", a[a.Single = 3] = "Single";
})(it || (it = {}));
class hu extends fu {
  constructor(e, t) {
    super(), this.header = e, this.payload = t;
  }
  toBinaryLengthBytes() {
    return this.header.toBinaryLengthBytes() + this.payload.byteLength;
  }
  toBinaryInto(e) {
    let t = 0;
    const i = this.header.toBinaryInto(e);
    if (t += i, e.byteLength - t < this.payload.byteLength)
      throw Lo.tooSmallForPayload();
    for (let c = 0; c < this.payload.length; c += 1)
      e.setUint8(t, this.payload[c]), t += ut;
    const s = this.toBinaryLengthBytes();
    if (t !== s)
      throw new Error("DataTrackPacket.toBinaryInto: Wrote ".concat(t, " bytes but expected length was ").concat(s, " bytes"));
    return s;
  }
  static fromBinary(e) {
    const t = Cm(e), i = Uo.fromBinary(t), s = ge(i, 2), c = s[0], u = s[1], d = t.buffer.slice(t.byteOffset + u, t.byteOffset + t.byteLength);
    return [new hu(c, new Uint8Array(d)), t.byteLength];
  }
  toJSON() {
    return {
      header: this.header.toJSON(),
      payload: this.payload
    };
  }
}
const ch = rn(Mt.DataTracks);
class oi extends Nn {
  constructor(e, t, i, s) {
    super(19, "Frame ".concat(i, " dropped: ").concat(e), s), this.name = "DataTrackDepacketizerDropError", this.reason = t, this.reasonName = Za[t], this.frameNumber = i;
  }
  static interrupted(e, t) {
    return new oi("Interrupted by the start of a new frame ".concat(t), Za.Interrupted, e);
  }
  static unknownFrame(e) {
    return new oi("Initial packet was never received.", Za.UnknownFrame, e);
  }
  static bufferFull(e) {
    return new oi("Reorder buffer is full.", Za.BufferFull, e);
  }
  static incomplete(e, t, i) {
    return new oi("Not all packets received before final packet. Received ".concat(t, " packets, expected ").concat(i, " packets."), Za.Incomplete, e);
  }
}
var Za;
(function(a) {
  a[a.Interrupted = 0] = "Interrupted", a[a.UnknownFrame = 1] = "UnknownFrame", a[a.BufferFull = 2] = "BufferFull", a[a.Incomplete = 3] = "Incomplete";
})(Za || (Za = {}));
class mu {
  constructor() {
    this.partials = /* @__PURE__ */ new Map();
  }
  /** Should be repeatedly called with received {@link DataTrackPacket}s - intermediate calls
   * aggregate the packet's state internally, and return null.
   *
   * Once this method is called with the final packet to form a frame, a new {@link DataTrackFrameInternal}
   * is returned.*/
  push(e, t) {
    switch (e.header.marker) {
      case it.Single:
        return this.frameFromSingle(e, t);
      case it.Start:
        return this.beginPartial(e, t);
      case it.Inter:
      case it.Final:
        return this.pushToPartial(e);
    }
  }
  reset() {
    this.partials.clear();
  }
  peekOldestPartialFrameNumber() {
    const e = this.partials.keys().next();
    return e.done ? null : e.value;
  }
  frameFromSingle(e, t) {
    var i;
    if (e.header.marker !== it.Single)
      throw new Error("Depacketizer.frameFromSingle: packet.header.marker was not FrameMarker.Single, found ".concat(e.header.marker, "."));
    const s = (i = t?.maxPartialFrames) !== null && i !== void 0 ? i : 1;
    if (this.partials.size >= s) {
      const c = this.peekOldestPartialFrameNumber();
      if (typeof c != "number")
        throw new Error("Depacketizer.frameFromSingle: no oldest frame number found, but partials.size is ".concat(this.partials.size, "."));
      if (this.partials.delete(c), t?.throwOnInterruption)
        throw oi.interrupted(c, e.header.frameNumber.value);
      ch.warn("Data track frame ".concat(c, " was interrupted by single-packet frame ").concat(e.header.frameNumber.value, ", dropping."));
    }
    return {
      payload: e.payload,
      extensions: e.header.extensions
    };
  }
  /** Begin assembling a new packet. */
  beginPartial(e, t) {
    var i;
    if (e.header.marker !== it.Start)
      throw new Error("Depacketizer.beginPartial: packet.header.marker was not FrameMarker.Start, found ".concat(e.header.marker, "."));
    const s = e.header.sequence, c = e.header.frameNumber.value, u = {
      startSequence: s,
      extensions: e.header.extensions,
      payloads: /* @__PURE__ */ new Map([[s.value, e.payload]])
    }, d = (i = t?.maxPartialFrames) !== null && i !== void 0 ? i : 1;
    for (; this.partials.size >= d; ) {
      const h = this.peekOldestPartialFrameNumber();
      if (typeof h != "number")
        break;
      if (this.partials.delete(h), t?.throwOnInterruption)
        throw oi.interrupted(h, c);
      ch.warn("Data track partials full (max ".concat(d, "), evicted oldest frame ").concat(h, " to make room for new frame ").concat(c, "."));
    }
    return this.partials.set(c, u), null;
  }
  /** Push to the existing partial frame. */
  pushToPartial(e) {
    if (e.header.marker !== it.Inter && e.header.marker !== it.Final)
      throw new Error("Depacketizer.pushToPartial: packet.header.marker was not FrameMarker.Inter or FrameMarker.Final, found ".concat(e.header.marker, "."));
    const t = e.header.frameNumber.value, i = this.partials.get(t);
    if (!i)
      throw this.partials.delete(t), oi.unknownFrame(t);
    if (i.payloads.size >= mu.MAX_BUFFER_PACKETS)
      throw this.partials.delete(t), oi.bufferFull(t);
    return i.payloads.has(e.header.sequence.value) && ch.warn("Data track frame ".concat(t, " received duplicate packet for sequence ").concat(e.header.sequence.value, ", so replacing with newly received packet.")), i.payloads.set(e.header.sequence.value, e.payload), e.header.marker === it.Final ? this.finalize(t, i, e.header.sequence.value) : null;
  }
  /** Try to reassemble the complete frame. */
  finalize(e, t, i) {
    const s = t.payloads.size;
    let c = 0;
    for (const f of t.payloads.values())
      c += f.length;
    const u = new Uint8Array(c);
    let d = t.startSequence.clone(), h = 0;
    for (; ; ) {
      const f = t.payloads.get(d.value);
      if (!f)
        break;
      t.payloads.delete(d.value);
      const p = u.length - h;
      if (f.length > p)
        throw new Error("Depacketizer.finalize: Expected at least ".concat(f.length, " more bytes left in the payload buffer, only got ").concat(p, " bytes."));
      if (u.set(f, h), h += f.length, d.value != i) {
        d.increment();
        continue;
      }
      return this.partials.delete(e), {
        payload: u,
        extensions: t.extensions
      };
    }
    throw this.partials.delete(e), oi.incomplete(e, s, i - t.startSequence.value + 1);
  }
}
mu.MAX_BUFFER_PACKETS = 128;
var Wa;
(function(a) {
  a[a.Unpublished = 0] = "Unpublished", a[a.Timeout = 1] = "Timeout", a[a.Disconnected = 2] = "Disconnected", a[a.Cancelled = 4] = "Cancelled";
})(Wa || (Wa = {}));
class tn extends Nn {
  constructor(e, t, i) {
    super(22, e, i), this.name = "DataTrackSubscribeError", this.reason = t, this.reasonName = Wa[t];
  }
  static unpublished() {
    return new tn("The track has been unpublished and is no longer available", Wa.Unpublished);
  }
  static timeout() {
    return new tn("Request to subscribe to data track timed-out", Wa.Timeout);
  }
  static disconnected() {
    return new tn("Cannot subscribe to data track when disconnected", Wa.Disconnected);
  }
  // NOTE: this was introduced by web / there isn't a corresponding case in the rust version.
  static cancelled() {
    return new tn("Subscription to data track cancelled by caller", Wa.Cancelled);
  }
}
const lh = rn(Mt.DataTracks);
class WM {
  /**
   * Creates a new pipeline with the given options.
   */
  constructor(e) {
    var t, i;
    const s = e.e2eeManager !== null;
    if (e.info.usesE2ee !== s)
      throw new Error("IncomingDataTrackPipeline: DataTrackInfo.usesE2ee must match presence of decryptionProvider");
    const c = new mu();
    this.publisherIdentity = e.publisherIdentity, this.e2eeManager = (t = e.e2eeManager) !== null && t !== void 0 ? t : null, this.depacketizer = c, this.options = (i = e.pipelineOptions) !== null && i !== void 0 ? i : {};
  }
  updateE2eeManager(e) {
    this.e2eeManager = e;
  }
  setOptions(e) {
    this.options = e;
  }
  processPacket(e) {
    return R(this, void 0, void 0, function* () {
      const t = this.depacketize(e);
      if (!t)
        return null;
      const i = yield this.decryptIfNeeded(t);
      return i || null;
    });
  }
  /**
   * Depacketize the given frame, log if a drop occurs.
   */
  depacketize(e) {
    let t;
    try {
      t = this.depacketizer.push(e, {
        throwOnInterruption: !1,
        maxPartialFrames: this.options.maxPartialFrames
      });
    } catch (i) {
      return lh.warn("Data frame depacketize error: ".concat(i)), null;
    }
    return t;
  }
  /**
   * Decrypt the frame's payload if E2EE is enabled for this track.
   */
  decryptIfNeeded(e) {
    return R(this, void 0, void 0, function* () {
      var t, i;
      const s = this.e2eeManager;
      if (!s)
        return e;
      const c = (i = (t = e.extensions) === null || t === void 0 ? void 0 : t.e2ee) !== null && i !== void 0 ? i : null;
      if (!c)
        return lh.error("Missing E2EE meta"), null;
      let u;
      try {
        u = yield s.handleEncryptedData(e.payload, c.iv, this.publisherIdentity, c.keyIndex);
      } catch (d) {
        return lh.error("Error decrypting packet: ".concat(d)), null;
      }
      return e.payload = u.payload, e;
    });
  }
}
const Nt = rn(Mt.DataTracks), $M = 1e4, eA = 16;
class tA extends sn.EventEmitter {
  constructor(e) {
    var t;
    super(), this.descriptors = /* @__PURE__ */ new Map(), this.subscriptionHandles = /* @__PURE__ */ new Map(), this.e2eeManager = (t = e?.e2eeManager) !== null && t !== void 0 ? t : null;
  }
  /** @internal */
  updateE2eeManager(e) {
    this.e2eeManager = e;
    for (const t of this.descriptors.values())
      t.subscription.type === "active" && t.subscription.pipeline.updateE2eeManager(e);
  }
  /** @internal */
  setPipelineOptions(e, t) {
    const i = this.descriptors.get(e);
    if (!i) {
      Nt.warn("Unknown track ".concat(e, ", cannot set pipeline options."));
      return;
    }
    i.pipelineOptions = t, i.subscription.type === "active" && i.subscription.pipeline.setOptions(t);
  }
  /** Allocates a ReadableStream which emits when a new {@link DataTrackFrame} is received from the
   * SFU. The SFU subscription is initiated lazily when the stream is created.
   *
   * @returns A tuple of the ReadableStream and a Promise that resolves once the SFU subscription
   * is fully established / the stream is ready to receive frames.
   *
   * @internal
   **/
  openSubscriptionStream(e, t) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : eA, s = null;
    const c = new wt(), u = () => {
      t?.removeEventListener("abort", h);
    }, d = () => {
      if (u(), !s) {
        Nt.warn("ReadableStream subscribed to ".concat(e, " was not started."));
        return;
      }
      const p = this.descriptors.get(e);
      if (!p) {
        Nt.warn("Unknown track ".concat(e, ", skipping cancel..."));
        return;
      }
      if (p.subscription.type !== "active") {
        Nt.warn("Subscription for track ".concat(e, " is not active, skipping cancel..."));
        return;
      }
      p.subscription.streamControllers.delete(s), p.subscription.streamControllers.size === 0 && this.unSubscribeRequest(p.info.sid);
    }, h = () => {
      var p;
      if (!s)
        return;
      const v = this.descriptors.get(e);
      v?.subscription.type === "active" && v.subscription.streamControllers.delete(s), s.error(tn.cancelled()), (p = c.reject) === null || p === void 0 || p.call(c, tn.cancelled()), d();
    };
    return [new ReadableStream({
      start: (p) => {
        s = p, this.subscribeRequest(e, t).then(() => R(this, void 0, void 0, function* () {
          var v, y, T;
          const _ = this.descriptors.get(e);
          if (!_) {
            Nt.error("Unknown track ".concat(e));
            const k = tn.disconnected();
            p.error(k), (v = c.reject) === null || v === void 0 || v.call(c, k);
            return;
          }
          if (_.subscription.type !== "active") {
            Nt.error("Subscription for track ".concat(e, " is not active"));
            const k = tn.disconnected();
            p.error(k), (y = c.reject) === null || y === void 0 || y.call(c, k);
            return;
          }
          if (t?.aborted) {
            h();
            return;
          }
          t?.addEventListener("abort", h), _.subscription.streamControllers.set(p, u), (T = c.resolve) === null || T === void 0 || T.call(c);
        })).catch((v) => {
          var y;
          p.error(v), (y = c.reject) === null || y === void 0 || y.call(c, v);
        });
      },
      cancel: () => {
        d();
      }
    }, new CountQueuingStrategy({
      highWaterMark: i
    })), c.promise];
  }
  /** Client requested to subscribe to a data track.
   *
   * This is sent when the user calls {@link RemoteDataTrack.subscribe}.
   *
   * Only the first request to subscribe to a given track incurs meaningful overhead; subsequent
   * requests simply attach an additional receiver to the broadcast channel, allowing them to consume
   * frames from the existing subscription pipeline.
   */
  subscribeRequest(e, t) {
    return R(this, void 0, void 0, function* () {
      const i = this.descriptors.get(e);
      if (!i)
        throw new Error("Cannot subscribe to unknown track");
      const s = (c, u, d) => R(this, void 0, void 0, function* () {
        if (c.subscription.type === "active")
          return;
        if (c.subscription.type !== "pending")
          throw new Error("Descriptor for track ".concat(e, " is not pending, found ").concat(c.subscription.type));
        const h = zS([u, d].filter((v) => typeof v < "u")), f = new wt();
        c.subscription.completionFuture.promise.then(() => {
          var v;
          return (v = f.resolve) === null || v === void 0 ? void 0 : v.call(f);
        }).catch((v) => {
          var y;
          return (y = f.reject) === null || y === void 0 ? void 0 : y.call(f, v);
        });
        const p = () => {
          var v;
          if (c.subscription.type === "pending") {
            if (c.subscription.pendingRequestCount -= 1, d?.aborted) {
              c.subscription.cancel();
              return;
            }
            if (c.subscription.pendingRequestCount <= 0) {
              c.subscription.cancel();
              return;
            }
            (v = f.reject) === null || v === void 0 || v.call(f, tn.cancelled());
          }
        };
        h.aborted && p(), h.addEventListener("abort", p), yield f.promise, h.removeEventListener("abort", p);
      });
      switch (i.subscription.type) {
        case "none": {
          i.subscription = {
            type: "pending",
            completionFuture: new wt(),
            pendingRequestCount: 1,
            cancel: () => {
              var u, d;
              const h = i.subscription;
              i.subscription = {
                type: "none"
              }, this.emit("sfuUpdateSubscription", {
                sid: e,
                subscribe: !1
              }), h.type === "pending" && ((d = (u = h.completionFuture).reject) === null || d === void 0 || d.call(u, c.aborted ? tn.timeout() : (
                // NOTE: the below cancelled case was introduced by web / there isn't a corresponding case in the rust version.
                tn.cancelled()
              )));
            }
          }, this.emit("sfuUpdateSubscription", {
            sid: e,
            subscribe: !0
          });
          const c = FS($M);
          yield s(i, t, c);
          return;
        }
        case "pending": {
          i.subscription.pendingRequestCount += 1, yield s(i, t);
          return;
        }
        case "active":
          return;
      }
    });
  }
  /**
   * Get information about all currently subscribed tracks.
   * @internal */
  querySubscribed() {
    return R(this, void 0, void 0, function* () {
      return Array.from(this.descriptors.values()).filter((t) => t.subscription.type === "active").map((t) => [t.info, t.publisherIdentity]);
    });
  }
  /** Client requested to unsubscribe from a data track. */
  unSubscribeRequest(e) {
    var t;
    const i = this.descriptors.get(e);
    if (!i)
      throw new Error("Cannot subscribe to unknown track");
    if (i.subscription.type !== "active") {
      Nt.warn("Unexpected descriptor state in unSubscribeRequest, expected active, found ".concat((t = i.subscription) === null || t === void 0 ? void 0 : t.type));
      return;
    }
    this.closeStreamControllers(i.subscription.streamControllers, e);
    const s = i.subscription;
    i.subscription = {
      type: "none"
    }, this.subscriptionHandles.delete(s.subcriptionHandle), this.emit("sfuUpdateSubscription", {
      sid: e,
      subscribe: !1
    });
  }
  /** Detach abort-signal listeners and close all downstream stream controllers for an active
   * subscription. Used when the subscription is being torn down by the manager (unsubscribe,
   * unpublish, or shutdown). */
  closeStreamControllers(e, t) {
    for (const s of e) {
      var i = ge(s, 2);
      const c = i[0], u = i[1];
      u();
      try {
        c.close();
      } catch (d) {
        Nt.warn("Failed to close readable stream for track ".concat(t, ": ").concat(d));
      }
    }
  }
  /** SFU notification that track publications have changed.
   *
   * This event is produced from both {@link JoinResponse} and {@link ParticipantUpdate}
   * to provide a complete view of remote participants' track publications:
   *
   * - From a `JoinResponse`, it captures the initial set of tracks published when a participant joins.
   * - From a `ParticipantUpdate`, it captures subsequent changes (i.e., new tracks being
   *   published and existing tracks unpublished).
   */
  receiveSfuPublicationUpdates(e) {
    return R(this, void 0, void 0, function* () {
      if (e.size === 0)
        return;
      const t = /* @__PURE__ */ new Map();
      for (const c of e.entries()) {
        var i = ge(c, 2);
        const u = i[0], d = i[1], h = /* @__PURE__ */ new Set();
        for (const f of d)
          h.add(f.sid), !this.descriptors.has(f.sid) && (yield this.handleTrackPublished(u, f));
        t.set(u, h);
      }
      for (const c of t.entries()) {
        var s = ge(c, 2);
        const u = s[0], d = s[1];
        let f = Array.from(this.descriptors.entries()).filter((p) => {
          let v = ge(p, 2);
          return v[0], v[1].publisherIdentity === u;
        }).map((p) => ge(p, 1)[0]).filter((p) => !d.has(p));
        for (const p of f)
          this.handleTrackUnpublished(p);
      }
    });
  }
  /**
   * Get information about all currently remotely published tracks which could be subscribed to.
   * @internal */
  queryPublications() {
    return R(this, void 0, void 0, function* () {
      return Array.from(this.descriptors.values()).map((e) => e.info);
    });
  }
  handleTrackPublished(e, t) {
    return R(this, void 0, void 0, function* () {
      if (this.descriptors.has(t.sid)) {
        Nt.error("Existing descriptor for track ".concat(t.sid));
        return;
      }
      let i = {
        info: t,
        publisherIdentity: e,
        subscription: {
          type: "none"
        },
        pipelineOptions: {}
      };
      this.descriptors.set(i.info.sid, i);
      const s = new GS(i.info, this, {
        publisherIdentity: e
      });
      this.emit("trackPublished", {
        track: s
      });
    });
  }
  handleTrackUnpublished(e) {
    const t = this.descriptors.get(e);
    if (!t) {
      Nt.error("Unknown track ".concat(e));
      return;
    }
    this.descriptors.delete(e), t.subscription.type === "active" && (this.closeStreamControllers(t.subscription.streamControllers, e), this.subscriptionHandles.delete(t.subscription.subcriptionHandle)), this.emit("trackUnpublished", {
      sid: e,
      publisherIdentity: t.publisherIdentity
    });
  }
  /** SFU notification that handles have been assigned for requested subscriptions. */
  receivedSfuSubscriberHandles(e) {
    for (const i of e.entries()) {
      var t = ge(i, 2);
      const s = t[0], c = t[1];
      this.registerSubscriberHandle(s, c);
    }
  }
  registerSubscriberHandle(e, t) {
    var i, s;
    const c = this.descriptors.get(t);
    if (!c) {
      Nt.error("Unknown track ".concat(t));
      return;
    }
    switch (c.subscription.type) {
      case "none": {
        Nt.warn("No subscription for ".concat(t));
        return;
      }
      case "active": {
        c.subscription.subcriptionHandle = e, this.subscriptionHandles.set(e, t);
        return;
      }
      case "pending": {
        const u = new WM({
          info: c.info,
          publisherIdentity: c.publisherIdentity,
          e2eeManager: this.e2eeManager,
          pipelineOptions: c.pipelineOptions
        }), d = c.subscription;
        c.subscription = {
          type: "active",
          subcriptionHandle: e,
          pipeline: u,
          streamControllers: /* @__PURE__ */ new Map()
        }, this.subscriptionHandles.set(e, t), (s = (i = d.completionFuture).resolve) === null || s === void 0 || s.call(i);
      }
    }
  }
  /** Packet has been received over the transport. */
  packetReceived(e) {
    return R(this, void 0, void 0, function* () {
      let t;
      try {
        var i = hu.fromBinary(e), s = ge(i, 1);
        t = s[0];
      } catch (h) {
        Nt.error("Failed to deserialize packet: ".concat(h));
        return;
      }
      const c = this.subscriptionHandles.get(t.header.trackHandle);
      if (!c) {
        Nt.warn("Unknown subscriber handle ".concat(t.header.trackHandle));
        return;
      }
      const u = this.descriptors.get(c);
      if (!u) {
        Nt.error("Missing descriptor for track ".concat(c));
        return;
      }
      if (u.subscription.type !== "active") {
        Nt.warn("Received packet for track ".concat(c, " without active subscription"));
        return;
      }
      const d = yield u.subscription.pipeline.processPacket(t);
      if (d)
        for (const h of u.subscription.streamControllers.keys()) {
          if (h.desiredSize !== null && h.desiredSize <= 0) {
            Nt.warn("Cannot send frame to subscribers: readable stream is full (desiredSize is ".concat(h.desiredSize, "). To increase this threshold, set a higher 'options.highWaterMark' when calling .subscribe()."));
            continue;
          }
          const f = qS.lossyIntoFrame(d);
          h.enqueue(f);
        }
    });
  }
  /** Resend all subscription updates.
   *
   * This must be sent after a full reconnect to ensure the SFU knows which
   * tracks are subscribed to locally.
   */
  resendSubscriptionUpdates() {
    for (const t of this.descriptors) {
      var e = ge(t, 2);
      const i = e[0];
      e[1].subscription.type !== "none" && this.emit("sfuUpdateSubscription", {
        sid: i,
        subscribe: !0
      });
    }
  }
  /** Called when a remote participant is disconnected so that any pending data tracks can be
   * cancelled. */
  handleRemoteParticipantDisconnected(e) {
    var t, i;
    for (const s of this.descriptors.values())
      if (s.publisherIdentity === e)
        switch (s.subscription.type) {
          case "none":
            break;
          case "pending":
            (i = (t = s.subscription.completionFuture).reject) === null || i === void 0 || i.call(t, tn.disconnected());
            break;
          case "active":
            this.unSubscribeRequest(s.info.sid);
            break;
        }
  }
  /** Resets the manager, ending any subscriptions, and getting it ready for the next room
   * connection.  */
  reset() {
    var e, t;
    for (const i of this.descriptors.values())
      this.emit("trackUnpublished", {
        sid: i.info.sid,
        publisherIdentity: i.publisherIdentity
      }), i.subscription.type === "pending" && ((t = (e = i.subscription.completionFuture).reject) === null || t === void 0 || t.call(e, tn.disconnected())), i.subscription.type === "active" && this.closeStreamControllers(i.subscription.streamControllers, i.info.sid);
    this.descriptors.clear(), this.subscriptionHandles.clear();
  }
}
class pu extends Nn {
  constructor(e, t, i) {
    super(19, e, i), this.name = "DataTrackPacketizerError", this.reason = t, this.reasonName = Kl[t];
  }
  static mtuTooShort() {
    return new pu("MTU is too short to send frame", Kl.MtuTooShort);
  }
}
var Kl;
(function(a) {
  a[a.MtuTooShort = 0] = "MtuTooShort";
})(Kl || (Kl = {}));
class Mm {
  constructor(e, t) {
    this.sequence = Jn.u16(0), this.frameNumber = Jn.u16(0), this.clock = Qa.rtpStartingNow(rs.rtpRandom()), this.handle = e, this.mtuSizeBytes = t;
  }
  /** @internal */
  static computeFrameMarker(e, t) {
    return t <= 1 ? it.Single : e === 0 ? it.Start : e === t - 1 ? it.Final : it.Inter;
  }
  /** Generates a series of packets for the specified {@link DataTrackFrameInternal}.
   *
   * NOTE: The return value of this function is a generator, so it can be lazily ran if desired,
   * or converted to an array with {@link Array.from}.
   */
  *packetize(e, t) {
    var i;
    const s = this.frameNumber.getThenIncrement(), c = {
      marker: it.Inter,
      trackHandle: this.handle,
      sequence: Jn.u16(0),
      frameNumber: s,
      timestamp: (i = t?.now) !== null && i !== void 0 ? i : this.clock.now(),
      extensions: e.extensions
    }, u = new Uo(c).toBinaryLengthBytes();
    if (u >= this.mtuSizeBytes)
      throw pu.mtuTooShort();
    const d = this.mtuSizeBytes - u, h = Math.ceil(e.payload.byteLength / d);
    for (let p = 0, v = 0; v < e.payload.byteLength; f = [p + 1, v + d], p = f[0], v = f[1], f) {
      var f;
      const y = this.sequence.getThenIncrement(), T = new Uo(Object.assign(Object.assign({}, c), {
        marker: Mm.computeFrameMarker(p, h),
        sequence: y
      })), _ = Math.min(
        // All but the last packet will be max length ...
        d,
        // ... and the last packet will be as long as it needs to be to finish out the buffer.
        e.payload.byteLength - v
      ), k = new Uint8Array(e.payload.buffer, e.payload.byteOffset + v, _);
      yield new hu(T, k);
    }
  }
}
var zn;
(function(a) {
  a[a.NotAllowed = 0] = "NotAllowed", a[a.DuplicateName = 1] = "DuplicateName", a[a.Timeout = 2] = "Timeout", a[a.LimitReached = 3] = "LimitReached", a[a.Disconnected = 4] = "Disconnected", a[a.Cancelled = 5] = "Cancelled", a[a.InvalidName = 6] = "InvalidName", a[a.Unknown = 7] = "Unknown";
})(zn || (zn = {}));
class kt extends Nn {
  constructor(e, t, i) {
    super(21, e, i), this.name = "DataTrackPublishError", this.reason = t, this.reasonName = zn[t], this.rawMessage = i?.rawMessage;
  }
  static notAllowed(e) {
    return new kt("Data track publishing unauthorized", zn.NotAllowed, {
      rawMessage: e
    });
  }
  static duplicateName(e) {
    return new kt("Track name already taken", zn.DuplicateName, {
      rawMessage: e
    });
  }
  static invalidName(e) {
    return new kt("Track name is invalid", zn.InvalidName, {
      rawMessage: e
    });
  }
  static timeout() {
    return new kt("Publish data track timed-out. Does the LiveKit server support data tracks?", zn.Timeout);
  }
  static limitReached(e) {
    return new kt("Data track publication limit reached", zn.LimitReached, {
      rawMessage: e
    });
  }
  static unknown(e, t) {
    return new kt("Received RequestResponse for publishDataTrack, but reason was unrecognised (".concat(e, ", ").concat(t, ")"), zn.Unknown);
  }
  static disconnected() {
    return new kt("Room disconnected", zn.Disconnected);
  }
  // NOTE: this was introduced by web / there isn't a corresponding case in the rust version.
  static cancelled() {
    return new kt("Publish data track cancelled by caller", zn.Cancelled);
  }
}
var wo;
(function(a) {
  a[a.TrackUnpublished = 0] = "TrackUnpublished", a[a.Dropped = 1] = "Dropped";
})(wo || (wo = {}));
class va extends Nn {
  constructor(e, t, i) {
    super(22, e, i), this.name = "DataTrackPushFrameError", this.reason = t, this.reasonName = wo[t];
  }
  static trackUnpublished() {
    return new va("Track is no longer published", wo.TrackUnpublished);
  }
  static dropped(e) {
    return new va("Frame was dropped", wo.Dropped, {
      cause: e
    });
  }
}
var Mo;
(function(a) {
  a[a.Packetizer = 0] = "Packetizer", a[a.Encryption = 1] = "Encryption";
})(Mo || (Mo = {}));
class Bo extends Nn {
  constructor(e, t, i) {
    super(21, e, i), this.name = "DataTrackOutgoingPipelineError", this.reason = t, this.reasonName = Mo[t];
  }
  static packetizer(e) {
    return new Bo("Error packetizing frame", Mo.Packetizer, {
      cause: e
    });
  }
  static encryption(e) {
    return new Bo("Error encrypting frame", Mo.Encryption, {
      cause: e
    });
  }
}
class gu {
  /** @internal */
  constructor(e, t) {
    this.trackSymbol = HS, this.isLocal = !0, this.typeSymbol = VS, this.handle = null, this.log = Ee, this.flushedFuture = new wt(), this.isFlushed = !0, this.handleManagerReset = () => {
      var i, s;
      (s = (i = this.flushedFuture).resolve) === null || s === void 0 || s.call(i), this.manager.off("packetsFlushedChange", this.handleManagerPacketsFlushedChange), this.manager.off("reset", this.handleManagerReset);
    }, this.handleManagerPacketsFlushedChange = (i) => {
      var s, c;
      this.isFlushed = i.isFlushed, i.isFlushed && ((c = (s = this.flushedFuture).resolve) === null || c === void 0 || c.call(s), this.flushedFuture = new wt());
    }, this.options = e, this.manager = t, this.log = rn(Mt.DataTracks), this.manager.on("packetsFlushedChange", this.handleManagerPacketsFlushedChange), this.manager.on("reset", this.handleManagerReset);
  }
  /** @internal */
  static withExplicitHandle(e, t, i) {
    const s = new gu(e, t);
    return s.handle = i, s;
  }
  /** Metrics about the data track publication. */
  get info() {
    const e = this.descriptor;
    if (e?.type === "active")
      return e.info;
  }
  /** The raw descriptor from the manager containing the internal state for this local track. */
  get descriptor() {
    return this.handle ? this.manager.getDescriptor(this.handle) : null;
  }
  /**
   * Publish the track to the SFU. This must be done before calling {@link tryPush} for the first time.
   * @internal
   * */
  publish(e) {
    return R(this, void 0, void 0, function* () {
      try {
        this.handle = yield this.manager.publishRequest(this.options, e);
      } catch (t) {
        throw t;
      }
    });
  }
  isPublished() {
    var e;
    return ((e = this.descriptor) === null || e === void 0 ? void 0 : e.type) === "active" && this.descriptor.publishState !== "unpublished";
  }
  /** Try pushing a frame to subscribers of the track.
   *
   * Pushing a frame can fail for several reasons:
   *
   * - The track has been unpublished by the local participant or SFU
   * - The room is no longer connected
   */
  tryPush(e) {
    if (!this.handle)
      throw va.trackUnpublished();
    const t = qS.from(e);
    try {
      return this.manager.tryProcessAndSend(this.handle, t);
    } catch (i) {
      throw i;
    }
  }
  /**
   * When called, waits for all in flight packets to be sent before resolving.
   *
   * Use this to:
   *
   * 1. Send frames exactly in order:
   * ```ts
   * await track.tryPush(/* ... *\/);
   * await track.flush();
   * await track.tryPush(/* ... *\/);
   * await track.flush();
   * // ... etc ...
   * ```
   *
   * 2. Wait for frames to all be delivered before unpublishing a local data track:
   *
   * ```ts
   * await track.tryPush(/* ... *\/);
   * await track.tryPush(/* ... *\/);
   * await track.tryPush(/* ... *\/);
   * // ... etc ...
   * await track.flush();
   * await track.unpublish();
   * ```
   **/
  flush() {
    return R(this, void 0, void 0, function* () {
      if (!this.isFlushed)
        return this.flushedFuture.promise;
    });
  }
  /**
   * Unpublish the track from the SFU. Once this is called, any further calls to {@link tryPush}
   * will fail.
   * */
  unpublish() {
    return R(this, void 0, void 0, function* () {
      if (!this.handle) {
        Ee.warn('Data track "'.concat(this.options.name, '" is not published, so unpublishing has no effect.'));
        return;
      }
      try {
        yield this.manager.unpublishRequest(this.handle);
      } catch (e) {
        throw e;
      }
    });
  }
}
class vu {
  constructor(e) {
    this.e2eeManager = e.e2eeManager, this.packetizer = new Mm(e.info.pubHandle, vu.TRANSPORT_MTU_BYTES);
  }
  updateE2eeManager(e) {
    this.e2eeManager = e;
  }
  processFrame(e) {
    return A1(this, arguments, function* () {
      const i = yield ds(this.encryptIfNeeded(e));
      try {
        yield ds(yield* O1(Hn(this.packetizer.packetize(i))));
      } catch (s) {
        throw s instanceof pu ? Bo.packetizer(s) : s;
      }
    });
  }
  encryptIfNeeded(e) {
    return R(this, void 0, void 0, function* () {
      if (!this.e2eeManager)
        return e;
      let t;
      try {
        t = yield this.e2eeManager.encryptData(e.payload);
      } catch (i) {
        throw Bo.encryption(i);
      }
      return e.payload = t.payload, e.extensions.e2ee = new Kn(t.keyIndex, t.iv), e;
    });
  }
}
vu.TRANSPORT_MTU_BYTES = 16e3;
const ai = rn(Mt.DataTracks), b0 = {
  pending() {
    return {
      type: "pending",
      completionFuture: new wt()
    };
  },
  active(a, e) {
    return {
      type: "active",
      info: a,
      publishState: "published",
      pipeline: new vu({
        info: a,
        e2eeManager: e
      }),
      unpublishingFuture: new wt()
    };
  }
}, nA = 1e4;
class Am extends sn.EventEmitter {
  constructor(e) {
    var t;
    super(), this.handleAllocator = new Zw(), this.descriptors = /* @__PURE__ */ new Map(), this.inFlightPacketCounter = /* @__PURE__ */ new Map(), this.e2eeManager = (t = e?.e2eeManager) !== null && t !== void 0 ? t : null;
  }
  static withDescriptors(e) {
    const t = new Am();
    return t.descriptors = e, t;
  }
  /** @internal */
  updateE2eeManager(e) {
    this.e2eeManager = e;
    for (const t of this.descriptors.values())
      t.type === "active" && t.pipeline.updateE2eeManager(e);
  }
  /**
   * Used by attached {@link LocalDataTrack} instances to query their associated descriptor info.
   * @internal
   */
  getDescriptor(e) {
    var t;
    return (t = this.descriptors.get(e)) !== null && t !== void 0 ? t : null;
  }
  /** Used by attached {@link LocalDataTrack} instances to broadcast data track packets to other
   * subscribers.
   * @internal
   */
  tryProcessAndSend(e, t) {
    return R(this, void 0, void 0, function* () {
      var i, s, c, u, d;
      const h = this.getDescriptor(e);
      if (h?.type !== "active" || h.publishState === "unpublished")
        throw va.trackUnpublished();
      if (h.publishState === "republishing")
        throw va.dropped("Data track republishing");
      try {
        try {
          for (var f = !0, p = Hn(h.pipeline.processFrame(t)), v; v = yield p.next(), i = v.done, !i; f = !0) {
            u = v.value, f = !1;
            const y = u, T = (d = this.inFlightPacketCounter.get(e)) !== null && d !== void 0 ? d : 0;
            this.inFlightPacketCounter.set(e, T + 1), T === 0 && this.emit("packetsFlushedChange", {
              handle: e,
              isFlushed: !1
            }), this.emit("packetAvailable", {
              handle: e,
              bytes: y.toBinary()
            });
          }
        } catch (y) {
          s = {
            error: y
          };
        } finally {
          try {
            !f && !i && (c = p.return) && (yield c.call(p));
          } finally {
            if (s) throw s.error;
          }
        }
      } catch (y) {
        throw va.dropped(y);
      }
    });
  }
  /** The client has sent a packet over the rtc data channel. This signal is used for determining
   * once all packets are sent and a data track has been "flushed".
   *
   * @internal */
  handlePacketSendComplete(e) {
    var t;
    let s = ((t = this.inFlightPacketCounter.get(e)) !== null && t !== void 0 ? t : 0) - 1;
    s < 0 && (ai.warn("OutgoingDataTrackManager.handlePacketSendComplete: inFlightPacketCounter was decremented below 0 (got ".concat(this.inFlightPacketCounter, " - resetting to 0. Were more packets send than were emitted?")), s = 0), this.inFlightPacketCounter.set(e, s), s === 0 && this.emit("packetsFlushedChange", {
      handle: e,
      isFlushed: !0
    });
  }
  /**
   * Client requested to publish a track.
   *
   * If the LiveKit server is too old and doesn't support data tracks, a
   * {@link DataTrackPublishError#timeout} will be thrown.
   *
   * @internal
   **/
  publishRequest(e, t) {
    return R(this, void 0, void 0, function* () {
      const i = this.handleAllocator.get();
      if (!i)
        throw kt.limitReached();
      const s = FS(nA), c = t ? zS([t, s]) : s;
      if (this.descriptors.has(i))
        throw new Error("Descriptor for handle already exists");
      const u = b0.pending();
      this.descriptors.set(i, u);
      const d = () => {
        var h, f;
        const p = this.descriptors.get(i);
        if (!p) {
          ai.warn("No descriptor for ".concat(i));
          return;
        }
        this.descriptors.delete(i), this.emit("sfuUnpublishRequest", {
          handle: i
        }), p.type === "pending" && ((f = (h = p.completionFuture).reject) === null || f === void 0 || f.call(h, s.aborted ? kt.timeout() : (
          // NOTE: the below cancelled case was introduced by web / there isn't a corresponding case in the rust version.
          kt.cancelled()
        )));
      };
      return c.aborted ? (d(), u.completionFuture.promise.then(
        () => i
        /* no-op, makes typescript happy */
      )) : (c.addEventListener("abort", d), this.emit("sfuPublishRequest", {
        handle: i,
        name: e.name,
        usesE2ee: this.e2eeManager !== null
      }), yield u.completionFuture.promise, c.removeEventListener("abort", d), this.emit("trackPublished", {
        track: gu.withExplicitHandle(e, this, i)
      }), i);
    });
  }
  /**
   * Get information about all currently published tracks.
   * @internal
   **/
  queryPublished() {
    return Array.from(this.descriptors.values()).filter((t) => t.type === "active").map((t) => t.info);
  }
  /**
   * Client request to unpublish a track.
   * @internal
   **/
  unpublishRequest(e) {
    return R(this, void 0, void 0, function* () {
      const t = this.descriptors.get(e);
      if (!t) {
        ai.warn("No descriptor for ".concat(e));
        return;
      }
      if (t.type !== "active") {
        ai.warn("Track ".concat(e, " not active"));
        return;
      }
      this.emit("sfuUnpublishRequest", {
        handle: e
      }), yield t.unpublishingFuture.promise, this.inFlightPacketCounter.delete(e), this.emit("trackUnpublished", {
        sid: t.info.sid
      });
    });
  }
  /**
   * SFU responded to a request to publish a data track.
   * @internal
   **/
  receivedSfuPublishResponse(e, t) {
    var i, s, c, u;
    const d = this.descriptors.get(e);
    if (!d) {
      ai.warn("No descriptor for ".concat(e));
      return;
    }
    switch (this.descriptors.delete(e), d.type) {
      case "pending": {
        if (t.type === "ok") {
          const h = t.data, f = h.usesE2ee ? this.e2eeManager : null;
          this.descriptors.set(h.pubHandle, b0.active(h, f)), (s = (i = d.completionFuture).resolve) === null || s === void 0 || s.call(i);
        } else
          (u = (c = d.completionFuture).reject) === null || u === void 0 || u.call(c, t.error);
        return;
      }
      case "active": {
        if (d.publishState !== "republishing") {
          ai.warn("Track ".concat(e, " already active"));
          return;
        }
        if (t.type === "error") {
          ai.warn("Republish failed for track ".concat(e));
          return;
        }
        ai.debug("Track ".concat(e, " republished")), d.info.sid = t.data.sid, d.publishState = "published", this.descriptors.set(d.info.pubHandle, d);
      }
    }
  }
  /**
   * SFU notification that a track has been unpublished.
   * @internal
   **/
  receivedSfuUnpublishResponse(e) {
    var t, i;
    const s = this.descriptors.get(e);
    if (!s) {
      ai.warn("No descriptor for ".concat(e));
      return;
    }
    if (this.descriptors.delete(e), s.type !== "active") {
      ai.warn("Track ".concat(e, " not active"));
      return;
    }
    s.publishState = "unpublished", (i = (t = s.unpublishingFuture).resolve) === null || i === void 0 || i.call(t);
  }
  /** Republish all tracks.
   *
   * This must be sent after a full reconnect in order for existing publications
   * to be recognized by the SFU. Each republished track will be assigned a new SID.
   * @internal
   */
  sfuWillRepublishTracks() {
    var e, t;
    for (const s of this.descriptors.entries()) {
      var i = ge(s, 2);
      const c = i[0], u = i[1];
      switch (u.type) {
        case "pending":
          this.descriptors.delete(c), (t = (e = u.completionFuture).reject) === null || t === void 0 || t.call(e, kt.disconnected());
          break;
        case "active":
          u.publishState = "republishing", this.emit("sfuPublishRequest", {
            handle: u.info.pubHandle,
            name: u.info.name,
            usesE2ee: u.info.usesE2ee
          });
      }
    }
  }
  /**
   * Reset's the state of the manager and all associated tracks. Run on room disconnect to get
   * the manager ready for the next room connection.
   * @internal
   **/
  reset() {
    return R(this, void 0, void 0, function* () {
      var e, t, i, s;
      this.handleAllocator.reset();
      for (const c of this.descriptors.values())
        switch (c.type) {
          case "pending":
            (t = (e = c.completionFuture).reject) === null || t === void 0 || t.call(e, kt.disconnected());
            break;
          case "active":
            (s = (i = c.unpublishingFuture).resolve) === null || s === void 0 || s.call(i), yield this.unpublishRequest(c.info.pubHandle);
            break;
        }
      this.descriptors.clear(), this.inFlightPacketCounter.clear(), this.emit("reset");
    });
  }
}
class Ge extends Error {
  /**
   * Creates an error object with the given code and message, plus an optional data payload.
   *
   * If thrown in an RPC method handler, the error will be sent back to the caller.
   *
   * Error codes 1001-1999 are reserved for built-in errors (see RpcError.ErrorCode for their meanings).
   */
  constructor(e, t, i, s) {
    super(t), this.code = e, this.message = T0(t, Ge.MAX_MESSAGE_BYTES), this.data = i ? T0(i, Ge.MAX_DATA_BYTES) : void 0, typeof s?.cause < "u" && (this.cause = s?.cause);
  }
  /**
   * @internal
   */
  static fromProto(e) {
    return new Ge(e.code, e.message, e.data);
  }
  /**
   * @internal
   */
  toProto() {
    return new fT({
      code: this.code,
      message: this.message,
      data: this.data
    });
  }
  /**
   * Creates an error object from the code, with an auto-populated message.
   *
   * @internal
   */
  static builtIn(e, t, i) {
    return new Ge(Ge.ErrorCode[e], Ge.ErrorMessage[e], t, i);
  }
}
Ge.MAX_MESSAGE_BYTES = 256;
Ge.MAX_DATA_BYTES = 15360;
Ge.ErrorCode = {
  APPLICATION_ERROR: 1500,
  CONNECTION_TIMEOUT: 1501,
  RESPONSE_TIMEOUT: 1502,
  RECIPIENT_DISCONNECTED: 1503,
  RESPONSE_PAYLOAD_TOO_LARGE: 1504,
  SEND_FAILED: 1505,
  UNSUPPORTED_METHOD: 1400,
  RECIPIENT_NOT_FOUND: 1401,
  REQUEST_PAYLOAD_TOO_LARGE: 1402,
  UNSUPPORTED_SERVER: 1403,
  UNSUPPORTED_VERSION: 1404
};
Ge.ErrorMessage = {
  APPLICATION_ERROR: "Application error in method handler",
  CONNECTION_TIMEOUT: "Connection timeout",
  RESPONSE_TIMEOUT: "Response timeout",
  RECIPIENT_DISCONNECTED: "Recipient disconnected",
  RESPONSE_PAYLOAD_TOO_LARGE: "Response payload too large",
  SEND_FAILED: "Failed to send",
  UNSUPPORTED_METHOD: "Method not supported at destination",
  RECIPIENT_NOT_FOUND: "Recipient not found",
  REQUEST_PAYLOAD_TOO_LARGE: "Request payload too large",
  UNSUPPORTED_SERVER: "RPC not supported by server",
  UNSUPPORTED_VERSION: "Unsupported RPC version"
};
const KS = 15360, YS = "lk.rpc_request", JS = "lk.rpc_response";
var It;
(function(a) {
  a.RPC_REQUEST_ID = "lk.rpc_request_id", a.RPC_REQUEST_METHOD = "lk.rpc_request_method", a.RPC_REQUEST_RESPONSE_TIMEOUT_MS = "lk.rpc_request_response_timeout_ms", a.RPC_REQUEST_VERSION = "lk.rpc_request_version";
})(It || (It = {}));
const iA = 1, QS = 2;
function Om(a) {
  return new TextEncoder().encode(a).length;
}
function T0(a, e) {
  if (Om(a) <= e)
    return a;
  let t = 0, i = a.length;
  const s = new TextEncoder();
  for (; t < i; ) {
    const c = Math.floor((t + i + 1) / 2);
    s.encode(a.slice(0, c)).length <= e ? t = c : i = c - 1;
  }
  return a.slice(0, t);
}
class aA extends sn.EventEmitter {
  constructor(e, t, i, s) {
    super(), this.pendingAcks = /* @__PURE__ */ new Map(), this.pendingResponses = /* @__PURE__ */ new Map(), this.log = e, this.outgoingDataStreamManager = t, this.getRemoteParticipantClientProtocol = i, this.getServerVersion = s;
  }
  performRpc(e) {
    return R(this, arguments, void 0, function(t) {
      var i = this;
      let s = t.destinationIdentity, c = t.method, u = t.payload, d = t.responseTimeout, h = d === void 0 ? 15e3 : d;
      return (function* () {
        const v = i.getRemoteParticipantClientProtocol(s);
        if (Om(u) > KS && v < zl)
          throw Ge.builtIn("REQUEST_PAYLOAD_TOO_LARGE");
        const T = i.getServerVersion();
        if (T && Xn(T, "1.8.0") < 0)
          throw Ge.builtIn("UNSUPPORTED_SERVER");
        const _ = Math.max(h, 8e3), k = crypto.randomUUID(), x = new wt();
        let A = null;
        const B = setTimeout(() => {
          var C;
          i.pendingAcks.delete(k), (C = x.reject) === null || C === void 0 || C.call(x, Ge.builtIn("CONNECTION_TIMEOUT")), i.pendingResponses.delete(k), A !== null && clearTimeout(A);
        }, 7e3);
        i.pendingAcks.set(k, {
          resolve: () => {
            clearTimeout(B);
          },
          participantIdentity: s
        }), i.pendingResponses.set(k, {
          completionFuture: x,
          participantIdentity: s
        }), yield i.publishRpcRequest(s, k, c, u, _, v), A = setTimeout(() => {
          var C;
          i.pendingResponses.delete(k), (C = x.reject) === null || C === void 0 || C.call(x, Ge.builtIn("RESPONSE_TIMEOUT"));
        }, h);
        const ie = x.promise.finally(() => {
          clearTimeout(A), i.pendingAcks.has(k) && (i.log.warn("RPC response received before ack", k), i.pendingAcks.delete(k), clearTimeout(B));
        });
        return [k, ie];
      })();
    });
  }
  publishRpcRequest(e, t, i, s, c, u) {
    return R(this, void 0, void 0, function* () {
      if (u >= zl) {
        const d = yield this.outgoingDataStreamManager.streamText({
          topic: YS,
          destinationIdentities: [e],
          attributes: {
            [It.RPC_REQUEST_ID]: t,
            [It.RPC_REQUEST_METHOD]: i,
            [It.RPC_REQUEST_RESPONSE_TIMEOUT_MS]: "".concat(c),
            [It.RPC_REQUEST_VERSION]: "".concat(QS)
          }
        });
        yield d.write(s), yield d.close();
        return;
      }
      this.emit("sendDataPacket", {
        packet: new Bt({
          destinationIdentities: [e],
          kind: ba.RELIABLE,
          value: {
            case: "rpcRequest",
            value: new nm({
              id: t,
              method: i,
              payload: s,
              responseTimeoutMs: c,
              version: iA
            })
          }
        })
      });
    });
  }
  /**
   * Handle an incoming data stream containing an RPC response payload.
   * @internal
   */
  handleIncomingDataStream(e, t, i) {
    return R(this, void 0, void 0, function* () {
      const s = i[It.RPC_REQUEST_ID];
      if (!s) {
        this.log.warn("RPC data stream malformed: ".concat(It.RPC_REQUEST_ID, " not set."));
        return;
      }
      const c = this.pendingResponses.get(s);
      if (c && c.participantIdentity !== t) {
        this.log.warn("RPC response stream for ".concat(s, " arrived from unexpected sender ").concat(t, ", expected ").concat(c.participantIdentity, ". Ignoring."));
        return;
      }
      let u;
      try {
        u = yield e.readAll();
      } catch (d) {
        this.log.warn("Error reading RPC response payload: ".concat(d)), this.handleIncomingRpcResponseFailure(s, Ge.builtIn("APPLICATION_ERROR", "Error reading RPC response payload", {
          cause: d
        }));
        return;
      }
      this.handleIncomingRpcResponseSuccess(s, u);
    });
  }
  /** @internal */
  handleIncomingRpcResponseSuccess(e, t) {
    var i, s;
    const c = this.pendingResponses.get(e);
    c ? ((s = (i = c.completionFuture).resolve) === null || s === void 0 || s.call(i, t), this.pendingResponses.delete(e)) : this.log.error("Response received for unexpected RPC request", e);
  }
  /** @internal */
  handleIncomingRpcResponseFailure(e, t) {
    var i, s;
    const c = this.pendingResponses.get(e);
    c ? ((s = (i = c.completionFuture).reject) === null || s === void 0 || s.call(i, t), this.pendingResponses.delete(e)) : this.log.error("Response received for unexpected RPC request", e);
  }
  /** @internal */
  handleIncomingRpcAck(e) {
    const t = this.pendingAcks.get(e);
    t ? (t.resolve(), this.pendingAcks.delete(e)) : this.log.error("Ack received for unexpected RPC request: ".concat(e));
  }
  /** @internal */
  handleParticipantDisconnected(e) {
    var t;
    for (const u of this.pendingAcks) {
      var i = ge(u, 2);
      const d = i[0];
      i[1].participantIdentity === e && this.pendingAcks.delete(d);
    }
    for (const u of this.pendingResponses) {
      var s = ge(u, 2);
      const d = s[0];
      var c = s[1];
      const h = c.participantIdentity, f = c.completionFuture;
      h === e && ((t = f.reject) === null || t === void 0 || t.call(f, Ge.builtIn("RECIPIENT_DISCONNECTED")), this.pendingResponses.delete(d));
    }
  }
}
class rA extends sn.EventEmitter {
  constructor(e, t, i) {
    super(), this.rpcHandlers = /* @__PURE__ */ new Map(), this.log = e, this.outgoingDataStreamManager = t, this.getRemoteParticipantClientProtocol = i;
  }
  registerRpcMethod(e, t) {
    if (this.rpcHandlers.has(e))
      throw Error("RPC handler already registered for method ".concat(e, ", unregisterRpcMethod before trying to register again"));
    this.rpcHandlers.set(e, t);
  }
  unregisterRpcMethod(e) {
    this.rpcHandlers.delete(e);
  }
  /**
   * Handle an incoming RPCRequest message containing a payload.
   * This handles "version 1" of rpc requests.
   * @internal
   */
  handleIncomingRpcRequest(e, t) {
    return R(this, void 0, void 0, function* () {
      var i;
      if (this.publishRpcAck(e, t.id), t.version !== 1) {
        this.publishRpcResponsePacket(e, t.id, null, Ge.builtIn("UNSUPPORTED_VERSION"));
        return;
      }
      const s = this.rpcHandlers.get(t.method);
      if (!s) {
        this.publishRpcResponsePacket(e, t.id, null, Ge.builtIn("UNSUPPORTED_METHOD"));
        return;
      }
      let c;
      try {
        c = yield s({
          requestId: t.id,
          callerIdentity: e,
          payload: t.payload,
          responseTimeout: t.responseTimeoutMs
        });
      } catch (u) {
        let d;
        u instanceof Ge ? d = u : (this.log.warn("Uncaught error returned by RPC handler for ".concat(t.method, ". Returning APPLICATION_ERROR instead."), u), d = Ge.builtIn("APPLICATION_ERROR", "Uncaught error: ".concat((i = u?.message) !== null && i !== void 0 ? i : u), {
          cause: u
        })), this.publishRpcResponsePacket(e, t.id, null, d);
        return;
      }
      yield this.publishRpcResponse(e, t.id, c ?? "");
    });
  }
  /**
   * Handle an incoming data stream containing a RPC request payload.
   * This handles "version 2" of rpc requests.
   * @internal
   */
  handleIncomingDataStream(e, t, i) {
    return R(this, void 0, void 0, function* () {
      const s = i[It.RPC_REQUEST_ID], c = i[It.RPC_REQUEST_METHOD], u = parseInt(i[It.RPC_REQUEST_RESPONSE_TIMEOUT_MS], 10), d = parseInt(i[It.RPC_REQUEST_VERSION], 10);
      if (!s || !c || Number.isNaN(u) || Number.isNaN(d)) {
        this.log.warn("RPC data stream malformed: ".concat(It.RPC_REQUEST_ID, " / ").concat(It.RPC_REQUEST_METHOD, " / ").concat(It.RPC_REQUEST_RESPONSE_TIMEOUT_MS, " / ").concat(It.RPC_REQUEST_VERSION, " not set.")), this.publishRpcResponsePacket(t, s, null, Ge.builtIn("APPLICATION_ERROR", "RPC data stream malformed"));
        return;
      }
      if (this.publishRpcAck(t, s), d !== QS) {
        this.publishRpcResponsePacket(t, s, null, Ge.builtIn("UNSUPPORTED_VERSION"));
        return;
      }
      let h;
      try {
        h = yield e.readAll();
      } catch (v) {
        this.log.warn("Error reading RPC request payload: ".concat(v)), this.publishRpcResponsePacket(t, s, null, Ge.builtIn("APPLICATION_ERROR", "Error reading RPC request payload", {
          cause: v
        }));
        return;
      }
      const f = this.rpcHandlers.get(c);
      if (!f) {
        this.publishRpcResponsePacket(t, s, null, Ge.builtIn("UNSUPPORTED_METHOD"));
        return;
      }
      let p;
      try {
        p = yield f({
          requestId: s,
          callerIdentity: t,
          payload: h,
          responseTimeout: u
        });
      } catch (v) {
        let y;
        v instanceof Ge ? y = v : (this.log.warn("Uncaught error returned by RPC handler for ".concat(c, ". Returning APPLICATION_ERROR instead."), v), y = Ge.builtIn("APPLICATION_ERROR")), this.publishRpcResponsePacket(t, s, null, y);
        return;
      }
      yield this.publishRpcResponse(t, s, p ?? "");
    });
  }
  publishRpcAck(e, t) {
    this.emit("sendDataPacket", {
      packet: new Bt({
        destinationIdentities: [e],
        kind: ba.RELIABLE,
        value: {
          case: "rpcAck",
          value: new tu({
            requestId: t
          })
        }
      })
    });
  }
  publishRpcResponsePacket(e, t, i, s) {
    this.emit("sendDataPacket", {
      packet: new Bt({
        destinationIdentities: [e],
        kind: ba.RELIABLE,
        value: {
          case: "rpcResponse",
          value: new im({
            requestId: t,
            value: s ? {
              case: "error",
              value: s.toProto()
            } : {
              case: "payload",
              value: i ?? ""
            }
          })
        }
      })
    });
  }
  /**
   * Send a successful RPC response payload, choosing the transport based on
   * the caller's client protocol version.
   */
  publishRpcResponse(e, t, i) {
    return R(this, void 0, void 0, function* () {
      if (this.getRemoteParticipantClientProtocol(e) >= zl) {
        const u = yield this.outgoingDataStreamManager.streamText({
          topic: JS,
          destinationIdentities: [e],
          attributes: {
            [It.RPC_REQUEST_ID]: t
          }
        });
        yield u.write(i), yield u.close();
        return;
      }
      if (Om(i) > KS) {
        this.log.warn("RPC Response payload too large for request ".concat(t, ". To send larger responses, consider updating the sending client.")), this.publishRpcResponsePacket(e, t, null, Ge.builtIn("RESPONSE_PAYLOAD_TOO_LARGE"));
        return;
      }
      this.publishRpcResponsePacket(e, t, i, null);
    });
  }
}
class XS extends AS {
  constructor(e, t, i, s, c, u) {
    super(e, t, H.Kind.Audio, i, u), this.monitorReceiver = () => R(this, void 0, void 0, function* () {
      if (!this.receiver) {
        this._currentBitrate = 0;
        return;
      }
      const d = yield this.getReceiverStats();
      d && this.prevStats && this.receiver && (this._currentBitrate = du(d, this.prevStats)), this.prevStats = d;
    }), this.audioContext = s, this.webAudioPluginNodes = [], c && (this.sinkId = c.deviceId);
  }
  /**
   * sets the volume for all attached audio elements
   */
  setVolume(e) {
    var t;
    for (const i of this.attachedElements)
      this.audioContext ? (t = this.gainNode) === null || t === void 0 || t.gain.setTargetAtTime(e, 0, 0.1) : i.volume = e;
    Qn() && this._mediaStreamTrack._setVolume(e), this.elementVolume = e;
  }
  /**
   * gets the volume of attached audio elements (loudest)
   */
  getVolume() {
    if (this.elementVolume)
      return this.elementVolume;
    if (Qn())
      return 1;
    let e = 0;
    return this.attachedElements.forEach((t) => {
      t.volume > e && (e = t.volume);
    }), e;
  }
  /**
   * calls setSinkId on all attached elements, if supported
   * @param deviceId audio output device
   */
  setSinkId(e) {
    return R(this, void 0, void 0, function* () {
      this.sinkId = e, yield Promise.all(this.attachedElements.map((t) => {
        if (Uh(t))
          return t.setSinkId(e);
      }));
    });
  }
  attach(e) {
    const t = this.attachedElements.length === 0;
    return e ? super.attach(e) : e = super.attach(), this.sinkId && Uh(e) && e.setSinkId(this.sinkId).catch((i) => {
      this.log.error("Failed to set sink id on remote audio track", i, this.logContext);
    }), this.audioContext && t && (this.log.debug("using audio context mapping", this.logContext), this.connectWebAudio(this.audioContext, e), e.volume = 0, e.muted = !0), this.elementVolume && this.setVolume(this.elementVolume), e;
  }
  detach(e) {
    let t;
    return e ? (t = super.detach(e), this.audioContext && (this.attachedElements.length > 0 ? this.connectWebAudio(this.audioContext, this.attachedElements[0]) : this.disconnectWebAudio())) : (t = super.detach(), this.disconnectWebAudio()), t;
  }
  /**
   * @internal
   * @experimental
   */
  setAudioContext(e) {
    this.audioContext = e, e && this.attachedElements.length > 0 ? this.connectWebAudio(e, this.attachedElements[0]) : e || this.disconnectWebAudio();
  }
  /**
   * @internal
   * @experimental
   * @param {AudioNode[]} nodes - An array of WebAudio nodes. These nodes should not be connected to each other when passed, as the sdk will take care of connecting them in the order of the array.
   */
  setWebAudioPlugins(e) {
    this.webAudioPluginNodes = e, this.attachedElements.length > 0 && this.audioContext && this.connectWebAudio(this.audioContext, this.attachedElements[0]);
  }
  connectWebAudio(e, t) {
    this.disconnectWebAudio(), this.sourceNode = e.createMediaStreamSource(t.srcObject);
    let i = this.sourceNode;
    this.webAudioPluginNodes.forEach((s) => {
      i.connect(s), i = s;
    }), this.gainNode = e.createGain(), i.connect(this.gainNode), this.gainNode.connect(e.destination), this.elementVolume && this.gainNode.gain.setTargetAtTime(this.elementVolume, 0, 0.1), e.state !== "running" && e.resume().then(() => {
      e.state !== "running" && this.emit(re.AudioPlaybackFailed, new Error("Audio Context couldn't be started automatically"));
    }).catch((s) => {
      this.emit(re.AudioPlaybackFailed, s);
    });
  }
  disconnectWebAudio() {
    var e, t;
    (e = this.gainNode) === null || e === void 0 || e.disconnect(), (t = this.sourceNode) === null || t === void 0 || t.disconnect(), this.gainNode = void 0, this.sourceNode = void 0;
  }
  getReceiverStats() {
    return R(this, void 0, void 0, function* () {
      if (!this.receiver || !this.receiver.getStats)
        return;
      const e = yield this.receiver.getStats();
      let t;
      return e.forEach((i) => {
        i.type === "inbound-rtp" && (t = {
          type: "audio",
          streamId: i.id,
          timestamp: i.timestamp,
          jitter: i.jitter,
          bytesReceived: i.bytesReceived,
          concealedSamples: i.concealedSamples,
          concealmentEvents: i.concealmentEvents,
          silentConcealedSamples: i.silentConcealedSamples,
          silentConcealmentEvents: i.silentConcealmentEvents,
          totalAudioEnergy: i.totalAudioEnergy,
          totalSamplesDuration: i.totalSamplesDuration
        });
      }), t;
    });
  }
}
class Di extends sn.EventEmitter {
  constructor(e, t, i, s) {
    var c;
    super(), this.metadataMuted = !1, this.encryption = $e.NONE, this.log = Ee, this.handleMuted = () => {
      this.emit(re.Muted);
    }, this.handleUnmuted = () => {
      this.emit(re.Unmuted);
    }, this.log = rn((c = s?.loggerName) !== null && c !== void 0 ? c : Mt.Publication), this.loggerContextCb = this.loggerContextCb, this.setMaxListeners(100), this.kind = e, this.trackSid = t, this.trackName = i, this.source = H.Source.Unknown;
  }
  /** @internal */
  setTrack(e) {
    this.track && (this.track.off(re.Muted, this.handleMuted), this.track.off(re.Unmuted, this.handleUnmuted)), this.track = e, e && (e.on(re.Muted, this.handleMuted), e.on(re.Unmuted, this.handleUnmuted));
  }
  get logContext() {
    var e;
    return Object.assign(Object.assign({}, (e = this.loggerContextCb) === null || e === void 0 ? void 0 : e.call(this)), Oe(this));
  }
  get isMuted() {
    return this.metadataMuted;
  }
  get isEnabled() {
    return !0;
  }
  get isSubscribed() {
    return this.track !== void 0;
  }
  get isEncrypted() {
    return this.encryption !== $e.NONE;
  }
  /**
   * an [AudioTrack] if this publication holds an audio track
   */
  get audioTrack() {
    if (ci(this.track))
      return this.track;
  }
  /**
   * an [VideoTrack] if this publication holds a video track
   */
  get videoTrack() {
    if (Li(this.track))
      return this.track;
  }
  /** @internal */
  updateInfo(e) {
    this.trackSid = e.sid, this.trackName = e.name, this.source = H.sourceFromProto(e.source), this.mimeType = e.mimeType, this.kind === H.Kind.Video && e.width > 0 && (this.dimensions = {
      width: e.width,
      height: e.height
    }, this.simulcasted = e.simulcast), this.encryption = e.encryption, this.trackInfo = e, this.log.debug("update publication info", Object.assign(Object.assign({}, this.logContext), {
      info: e
    }));
  }
}
(function(a) {
  (function(e) {
    e.Desired = "desired", e.Subscribed = "subscribed", e.Unsubscribed = "unsubscribed";
  })(a.SubscriptionStatus || (a.SubscriptionStatus = {})), (function(e) {
    e.Allowed = "allowed", e.NotAllowed = "not_allowed";
  })(a.PermissionStatus || (a.PermissionStatus = {}));
})(Di || (Di = {}));
class Vh extends Di {
  get isUpstreamPaused() {
    var e;
    return (e = this.track) === null || e === void 0 ? void 0 : e.isUpstreamPaused;
  }
  constructor(e, t, i, s) {
    super(e, t.sid, t.name, s), this.track = void 0, this.handleTrackEnded = () => {
      this.emit(re.Ended);
    }, this.handleCpuConstrained = () => {
      this.track && Li(this.track) && this.emit(re.CpuConstrained, this.track);
    }, this.updateInfo(t), this.setTrack(i);
  }
  setTrack(e) {
    this.track && (this.track.off(re.Ended, this.handleTrackEnded), this.track.off(re.CpuConstrained, this.handleCpuConstrained)), super.setTrack(e), e && (e.on(re.Ended, this.handleTrackEnded), e.on(re.CpuConstrained, this.handleCpuConstrained));
  }
  get isMuted() {
    return this.track ? this.track.isMuted : super.isMuted;
  }
  get audioTrack() {
    return super.audioTrack;
  }
  get videoTrack() {
    return super.videoTrack;
  }
  get isLocal() {
    return !0;
  }
  /**
   * Mute the track associated with this publication
   */
  mute() {
    return R(this, void 0, void 0, function* () {
      var e;
      return (e = this.track) === null || e === void 0 ? void 0 : e.mute();
    });
  }
  /**
   * Unmute track associated with this publication
   */
  unmute() {
    return R(this, void 0, void 0, function* () {
      var e;
      return (e = this.track) === null || e === void 0 ? void 0 : e.unmute();
    });
  }
  /**
   * Pauses the media stream track associated with this publication from being sent to the server
   * and signals "muted" event to other participants
   * Useful if you want to pause the stream without pausing the local media stream track
   */
  pauseUpstream() {
    return R(this, void 0, void 0, function* () {
      var e;
      yield (e = this.track) === null || e === void 0 ? void 0 : e.pauseUpstream();
    });
  }
  /**
   * Resumes sending the media stream track associated with this publication to the server after a call to [[pauseUpstream()]]
   * and signals "unmuted" event to other participants (unless the track is explicitly muted)
   */
  resumeUpstream() {
    return R(this, void 0, void 0, function* () {
      var e;
      yield (e = this.track) === null || e === void 0 ? void 0 : e.resumeUpstream();
    });
  }
  getTrackFeatures() {
    var e;
    if (ci(this.track)) {
      const t = this.track.getSourceTrackSettings(), i = /* @__PURE__ */ new Set();
      return t.autoGainControl && i.add(_t.TF_AUTO_GAIN_CONTROL), t.echoCancellation && i.add(_t.TF_ECHO_CANCELLATION), t.noiseSuppression && i.add(_t.TF_NOISE_SUPPRESSION), t.channelCount && t.channelCount > 1 && i.add(_t.TF_STEREO), !((e = this.options) === null || e === void 0) && e.dtx || i.add(_t.TF_NO_DTX), this.track.enhancedNoiseCancellation && i.add(_t.TF_ENHANCED_NOISE_CANCELLATION), Array.from(i.values());
    } else return [];
  }
}
function yu(a, e) {
  return R(this, void 0, void 0, function* () {
    a ?? (a = {});
    let t = !1;
    const i = CS(a), s = i.audioProcessor, c = i.videoProcessor, u = i.optionsWithoutProcessor;
    let d = u.audio, h = u.video;
    if (s && typeof u.audio == "object" && (u.audio.processor = s), c && typeof u.video == "object" && (u.video.processor = c), a.audio && typeof u.audio == "object" && typeof u.audio.deviceId == "string") {
      const y = u.audio.deviceId;
      u.audio.deviceId = {
        exact: y
      }, t = !0, d = Object.assign(Object.assign({}, u.audio), {
        deviceId: {
          ideal: y
        }
      });
    }
    if (u.video && typeof u.video == "object" && typeof u.video.deviceId == "string") {
      const y = u.video.deviceId;
      u.video.deviceId = {
        exact: y
      }, t = !0, h = Object.assign(Object.assign({}, u.video), {
        deviceId: {
          ideal: y
        }
      });
    }
    u.audio === !0 ? u.audio = {
      deviceId: "default"
    } : typeof u.audio == "object" && u.audio !== null && (u.audio = Object.assign(Object.assign({}, u.audio), {
      deviceId: u.audio.deviceId || "default"
    })), u.video === !0 ? u.video = {
      deviceId: "default"
    } : typeof u.video == "object" && !u.video.deviceId && (u.video.deviceId = "default");
    const f = TS(u, DS, PS), p = gm(f), v = navigator.mediaDevices.getUserMedia(p);
    u.audio && (Rt.userMediaPromiseMap.set("audioinput", v), v.catch(() => Rt.userMediaPromiseMap.delete("audioinput"))), u.video && (Rt.userMediaPromiseMap.set("videoinput", v), v.catch(() => Rt.userMediaPromiseMap.delete("videoinput")));
    try {
      const y = yield v;
      return yield Promise.all(y.getTracks().map((T) => R(this, void 0, void 0, function* () {
        const _ = T.kind === "audio";
        let k;
        const x = _ ? p.audio : p.video;
        typeof x != "boolean" && (k = x);
        const A = T.getSettings().deviceId;
        k?.deviceId && tr(k.deviceId) !== A ? k.deviceId = A : k || (k = {
          deviceId: A
        });
        const B = SM(T, k, e);
        return B.kind === H.Kind.Video ? B.source = H.Source.Camera : B.kind === H.Kind.Audio && (B.source = H.Source.Microphone), B.mediaStream = y, ci(B) && s ? yield B.setProcessor(s) : Li(B) && c && (yield B.setProcessor(c)), B;
      })));
    } catch (y) {
      if (!t)
        throw y;
      return yu(Object.assign(Object.assign({}, a), {
        audio: d,
        video: h
      }), e);
    }
  });
}
function sA(a) {
  return R(this, void 0, void 0, function* () {
    return (yield yu({
      audio: !1,
      video: !0
    }))[0];
  });
}
function oA(a) {
  return R(this, void 0, void 0, function* () {
    return (yield yu({
      audio: !0,
      video: !1
    }))[0];
  });
}
var ma;
(function(a) {
  a.Excellent = "excellent", a.Good = "good", a.Poor = "poor", a.Lost = "lost", a.Unknown = "unknown";
})(ma || (ma = {}));
function cA(a) {
  switch (a) {
    case bo.EXCELLENT:
      return ma.Excellent;
    case bo.GOOD:
      return ma.Good;
    case bo.POOR:
      return ma.Poor;
    case bo.LOST:
      return ma.Lost;
    default:
      return ma.Unknown;
  }
}
class ZS extends sn.EventEmitter {
  get logContext() {
    var e, t;
    return Object.assign({}, (t = (e = this.loggerOptions) === null || e === void 0 ? void 0 : e.loggerContextCb) === null || t === void 0 ? void 0 : t.call(e));
  }
  get isEncrypted() {
    return this.trackPublications.size > 0 && Array.from(this.trackPublications.values()).every((e) => e.isEncrypted);
  }
  get isAgent() {
    var e;
    return ((e = this.permissions) === null || e === void 0 ? void 0 : e.agent) || this.kind === ka.AGENT;
  }
  get isActive() {
    var e;
    return ((e = this.participantInfo) === null || e === void 0 ? void 0 : e.state) === $r.ACTIVE;
  }
  get kind() {
    return this._kind;
  }
  /** participant attributes, similar to metadata, but as a key/value map */
  get attributes() {
    return Object.freeze(Object.assign({}, this._attributes));
  }
  /** @internal */
  constructor(e, t, i, s, c, u) {
    let d = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : ka.STANDARD;
    var h;
    super(), this.audioLevel = 0, this.isSpeaking = !1, this._connectionQuality = ma.Unknown, this.log = Ee, this.loggerOptions = u, this.log = rn((h = u?.loggerName) !== null && h !== void 0 ? h : Mt.Participant, () => this.logContext), this.setMaxListeners(100), this.sid = e, this.identity = t, this.name = i, this.metadata = s, this.audioTrackPublications = /* @__PURE__ */ new Map(), this.videoTrackPublications = /* @__PURE__ */ new Map(), this.trackPublications = /* @__PURE__ */ new Map(), this._kind = d, this._attributes = c ?? {};
  }
  getTrackPublications() {
    return Array.from(this.trackPublications.values());
  }
  /**
   * Finds the first track that matches the source filter, for example, getting
   * the user's camera track with getTrackBySource(Track.Source.Camera).
   */
  getTrackPublication(e) {
    for (const i of this.trackPublications) {
      var t = ge(i, 2);
      const s = t[1];
      if (s.source === e)
        return s;
    }
  }
  /**
   * Finds the first track that matches the track's name.
   */
  getTrackPublicationByName(e) {
    for (const i of this.trackPublications) {
      var t = ge(i, 2);
      const s = t[1];
      if (s.trackName === e)
        return s;
    }
  }
  /**
   * Waits until the participant is active and ready to receive data messages
   * @returns a promise that resolves when the participant is active
   */
  waitUntilActive() {
    return this.isActive ? Promise.resolve() : this.activeFuture ? this.activeFuture.promise : (this.activeFuture = new wt(), this.once(X.Active, () => {
      var e, t;
      (t = (e = this.activeFuture) === null || e === void 0 ? void 0 : e.resolve) === null || t === void 0 || t.call(e), this.activeFuture = void 0;
    }), this.activeFuture.promise);
  }
  get connectionQuality() {
    return this._connectionQuality;
  }
  get isCameraEnabled() {
    var e;
    const t = this.getTrackPublication(H.Source.Camera);
    return !(!((e = t?.isMuted) !== null && e !== void 0) || e);
  }
  get isMicrophoneEnabled() {
    var e;
    const t = this.getTrackPublication(H.Source.Microphone);
    return !(!((e = t?.isMuted) !== null && e !== void 0) || e);
  }
  get isScreenShareEnabled() {
    return !!this.getTrackPublication(H.Source.ScreenShare);
  }
  get isLocal() {
    return !1;
  }
  /** when participant joined the room */
  get joinedAt() {
    return this.participantInfo ? new Date(Number.parseInt(this.participantInfo.joinedAt.toString()) * 1e3) : /* @__PURE__ */ new Date();
  }
  /** @internal */
  updateInfo(e) {
    var t;
    return this.participantInfo && this.participantInfo.sid === e.sid && this.participantInfo.version > e.version ? !1 : (this.identity = e.identity, this.sid = e.sid, this._setName(e.name), this._setMetadata(e.metadata), this._setAttributes(e.attributes), e.state === $r.ACTIVE && ((t = this.participantInfo) === null || t === void 0 ? void 0 : t.state) !== $r.ACTIVE && this.emit(X.Active), e.permission && this.setPermissions(e.permission), this.participantInfo = e, !0);
  }
  /**
   * Updates metadata from server
   **/
  _setMetadata(e) {
    const t = this.metadata !== e, i = this.metadata;
    this.metadata = e, t && this.emit(X.ParticipantMetadataChanged, i);
  }
  _setName(e) {
    const t = this.name !== e;
    this.name = e, t && this.emit(X.ParticipantNameChanged, e);
  }
  /**
   * Updates metadata from server
   **/
  _setAttributes(e) {
    const t = nw(this.attributes, e);
    this._attributes = e, Object.keys(t).length > 0 && this.emit(X.AttributesChanged, t);
  }
  /** @internal */
  setPermissions(e) {
    var t, i, s, c, u, d;
    const h = this.permissions, f = e.canPublish !== ((t = this.permissions) === null || t === void 0 ? void 0 : t.canPublish) || e.canSubscribe !== ((i = this.permissions) === null || i === void 0 ? void 0 : i.canSubscribe) || e.canPublishData !== ((s = this.permissions) === null || s === void 0 ? void 0 : s.canPublishData) || e.hidden !== ((c = this.permissions) === null || c === void 0 ? void 0 : c.hidden) || e.recorder !== ((u = this.permissions) === null || u === void 0 ? void 0 : u.recorder) || e.canPublishSources.length !== this.permissions.canPublishSources.length || e.canPublishSources.some((p, v) => {
      var y;
      return p !== ((y = this.permissions) === null || y === void 0 ? void 0 : y.canPublishSources[v]);
    }) || e.canSubscribeMetrics !== ((d = this.permissions) === null || d === void 0 ? void 0 : d.canSubscribeMetrics);
    return this.permissions = e, f && this.emit(X.ParticipantPermissionsChanged, h), f;
  }
  /** @internal */
  setIsSpeaking(e) {
    e !== this.isSpeaking && (this.isSpeaking = e, e && (this.lastSpokeAt = /* @__PURE__ */ new Date()), this.emit(X.IsSpeakingChanged, e));
  }
  /** @internal */
  setConnectionQuality(e) {
    const t = this._connectionQuality;
    this._connectionQuality = cA(e), t !== this._connectionQuality && this.emit(X.ConnectionQualityChanged, this._connectionQuality);
  }
  /**
   * @internal
   */
  setDisconnected() {
    var e, t;
    this.activeFuture && ((t = (e = this.activeFuture).reject) === null || t === void 0 || t.call(e, new Error("Participant disconnected")), this.activeFuture = void 0);
  }
  /**
   * @internal
   */
  setAudioContext(e) {
    this.audioContext = e, this.audioTrackPublications.forEach((t) => ci(t.track) && t.track.setAudioContext(e));
  }
  addTrackPublication(e) {
    e.on(re.Muted, () => {
      this.emit(X.TrackMuted, e);
    }), e.on(re.Unmuted, () => {
      this.emit(X.TrackUnmuted, e);
    });
    const t = e;
    switch (t.track && (t.track.sid = e.trackSid), this.trackPublications.set(e.trackSid, e), e.kind) {
      case H.Kind.Audio:
        this.audioTrackPublications.set(e.trackSid, e);
        break;
      case H.Kind.Video:
        this.videoTrackPublications.set(e.trackSid, e);
        break;
    }
  }
}
function lA(a) {
  var e, t, i;
  if (!a.participantSid && !a.participantIdentity)
    throw new Error("Invalid track permission, must provide at least one of participantIdentity and participantSid");
  return new PT({
    participantIdentity: (e = a.participantIdentity) !== null && e !== void 0 ? e : "",
    participantSid: (t = a.participantSid) !== null && t !== void 0 ? t : "",
    allTracks: (i = a.allowAll) !== null && i !== void 0 ? i : !1,
    trackSids: a.allowedTrackSids || []
  });
}
class uA extends ZS {
  /** @internal */
  constructor(e, t, i, s, c, u, d, h) {
    super(e, t, void 0, void 0, void 0, {
      loggerName: s.loggerName,
      loggerContextCb: () => this.engine.logContext
    }), this.pendingPublishing = /* @__PURE__ */ new Set(), this.pendingPublishPromises = /* @__PURE__ */ new Map(), this.participantTrackPermissions = [], this.allParticipantsAllowedToSubscribe = !0, this.encryptionType = $e.NONE, this.e2eeStateMutex = new Lt(), this.enabledPublishVideoCodecs = [], this.handleReconnecting = () => {
      this.reconnectFuture || (this.reconnectFuture = new wt());
    }, this.handleReconnected = () => {
      var f, p;
      (p = (f = this.reconnectFuture) === null || f === void 0 ? void 0 : f.resolve) === null || p === void 0 || p.call(f), this.reconnectFuture = void 0, this.updateTrackSubscriptionPermissions();
    }, this.handleClosing = () => {
      var f, p, v, y, T, _;
      this.reconnectFuture && (this.reconnectFuture.promise.catch((k) => this.log.warn(k.message)), (p = (f = this.reconnectFuture) === null || f === void 0 ? void 0 : f.reject) === null || p === void 0 || p.call(f, new Error("Got disconnected during reconnection attempt")), this.reconnectFuture = void 0), this.signalConnectedFuture && ((y = (v = this.signalConnectedFuture).reject) === null || y === void 0 || y.call(v, new Error("Got disconnected without signal connected")), this.signalConnectedFuture = void 0), (_ = (T = this.activeAgentFuture) === null || T === void 0 ? void 0 : T.reject) === null || _ === void 0 || _.call(T, new Error("Got disconnected without active agent present")), this.activeAgentFuture = void 0, this.firstActiveAgent = void 0;
    }, this.handleSignalConnected = (f) => {
      var p, v;
      f.participant && this.updateInfo(f.participant), this.signalConnectedFuture || (this.signalConnectedFuture = new wt()), (v = (p = this.signalConnectedFuture).resolve) === null || v === void 0 || v.call(p);
    }, this.handleSignalRequestResponse = (f) => {
      const p = f.requestId, v = f.reason, y = f.message, T = this.pendingSignalRequests.get(p);
      switch (T && (v !== Ja.OK && T.reject(new Ob(y, v)), this.pendingSignalRequests.delete(p)), f.request.case) {
        case "publishDataTrack": {
          let _;
          switch (f.reason) {
            case Ja.NOT_ALLOWED:
              _ = kt.notAllowed(f.message);
              break;
            case Ja.DUPLICATE_NAME:
              _ = kt.duplicateName(f.message);
              break;
            case Ja.INVALID_NAME:
              _ = kt.invalidName(f.message);
              break;
            case Ja.LIMIT_EXCEEDED:
              _ = kt.limitReached(f.message);
              break;
            default:
              _ = kt.unknown(f.reason, f.message);
              break;
          }
          this.roomOutgoingDataTrackManager.receivedSfuPublishResponse(f.request.value.pubHandle, {
            type: "error",
            error: _
          });
          break;
        }
      }
    }, this.updateTrackSubscriptionPermissions = () => {
      this.log.debug("updating track subscription permissions", {
        allParticipantsAllowed: this.allParticipantsAllowedToSubscribe,
        participantTrackPermissions: this.participantTrackPermissions
      }), this.engine.client.sendUpdateSubscriptionPermissions(this.allParticipantsAllowedToSubscribe, this.participantTrackPermissions.map((f) => lA(f)));
    }, this.onTrackUnmuted = (f) => {
      this.onTrackMuted(f, f.isUpstreamPaused);
    }, this.onTrackMuted = (f, p) => {
      if (p === void 0 && (p = !0), !f.sid) {
        this.log.error("could not update mute status for unpublished track", Oe(f));
        return;
      }
      this.engine.updateMuteStatus(f.sid, p);
    }, this.onTrackUpstreamPaused = (f) => {
      this.log.debug("upstream paused", Oe(f)), this.onTrackMuted(f, !0);
    }, this.onTrackUpstreamResumed = (f) => {
      this.log.debug("upstream resumed", Oe(f)), this.onTrackMuted(f, f.isMuted);
    }, this.onTrackFeatureUpdate = (f) => {
      const p = this.audioTrackPublications.get(f.sid);
      if (!p) {
        this.log.warn("Could not update local audio track settings, missing publication for track ".concat(f.sid));
        return;
      }
      this.engine.client.sendUpdateLocalAudioTrack(p.trackSid, p.getTrackFeatures());
    }, this.onTrackCpuConstrained = (f, p) => {
      this.log.debug("track cpu constrained", Oe(p)), this.emit(X.LocalTrackCpuConstrained, f, p);
    }, this.handleSubscribedQualityUpdate = (f) => R(this, void 0, void 0, function* () {
      var p, v, y, T, _;
      if (!(!((_ = this.roomOptions) === null || _ === void 0) && _.dynacast))
        return;
      const k = this.videoTrackPublications.get(f.trackSid);
      if (!k) {
        this.log.warn("received subscribed quality update for unknown track", {
          trackSid: f.trackSid
        });
        return;
      }
      if (!k.videoTrack)
        return;
      const x = yield k.videoTrack.setPublishingCodecs(f.subscribedCodecs);
      try {
        for (var A = !0, B = Hn(x), ie; ie = yield B.next(), p = ie.done, !p; A = !0) {
          T = ie.value, A = !1;
          const C = T;
          Z1(C) && (this.log.debug("publish ".concat(C, " for ").concat(k.videoTrack.sid), Oe(k)), yield this.publishAdditionalCodecForTrack(k.videoTrack, C, k.options));
        }
      } catch (C) {
        v = {
          error: C
        };
      } finally {
        try {
          !A && !p && (y = B.return) && (yield y.call(B));
        } finally {
          if (v) throw v.error;
        }
      }
    }), this.handleLocalTrackUnpublished = (f) => {
      const p = this.trackPublications.get(f.trackSid);
      if (!p) {
        this.log.warn("received unpublished event for unknown track", {
          trackSid: f.trackSid
        });
        return;
      }
      this.unpublishTrack(p.track);
    }, this.handleTrackEnded = (f) => R(this, void 0, void 0, function* () {
      if (f.source === H.Source.ScreenShare || f.source === H.Source.ScreenShareAudio)
        this.log.debug("unpublishing local track due to TrackEnded", Oe(f)), this.unpublishTrack(f);
      else if (f.isUserProvided)
        yield f.mute();
      else if (Ai(f) || ii(f))
        try {
          if (an())
            try {
              const p = yield navigator?.permissions.query({
                // the permission query for camera and microphone currently not supported in Safari and Firefox
                // @ts-ignore
                name: f.source === H.Source.Camera ? "camera" : "microphone"
              });
              if (p && p.state === "denied")
                throw this.log.warn("user has revoked access to ".concat(f.source), Oe(f)), p.onchange = () => {
                  p.state !== "denied" && (f.isMuted || f.restartTrack(), p.onchange = null);
                }, new Error("GetUserMedia Permission denied");
            } catch {
            }
          f.isMuted || (this.log.debug("track ended, attempting to use a different device", Oe(f)), Ai(f) ? yield f.restartTrack({
            deviceId: "default"
          }) : yield f.restartTrack());
        } catch {
          this.log.warn("could not restart track, muting instead", Oe(f)), yield f.mute();
        }
    }), this.audioTrackPublications = /* @__PURE__ */ new Map(), this.videoTrackPublications = /* @__PURE__ */ new Map(), this.trackPublications = /* @__PURE__ */ new Map(), this.engine = i, this.roomOptions = s, this.setupEngine(i), this.activeDeviceMap = /* @__PURE__ */ new Map([["audioinput", "default"], ["videoinput", "default"], ["audiooutput", "default"]]), this.pendingSignalRequests = /* @__PURE__ */ new Map(), this.roomOutgoingDataStreamManager = c, this.roomOutgoingDataTrackManager = u, this.rpcClientManager = d, this.rpcServerManager = h;
  }
  get lastCameraError() {
    return this.cameraError;
  }
  get lastMicrophoneError() {
    return this.microphoneError;
  }
  get isE2EEEnabled() {
    return this.encryptionType !== $e.NONE;
  }
  getTrackPublication(e) {
    const t = super.getTrackPublication(e);
    if (t)
      return t;
  }
  getTrackPublicationByName(e) {
    const t = super.getTrackPublicationByName(e);
    if (t)
      return t;
  }
  /**
   * @internal
   */
  setupEngine(e) {
    var t;
    this.engine = e, this.engine.on(ae.RemoteMute, (i, s) => {
      const c = this.trackPublications.get(i);
      !c || !c.track || (s ? c.mute() : c.unmute());
    }), !((t = this.signalConnectedFuture) === null || t === void 0) && t.isResolved && (this.signalConnectedFuture = void 0), this.engine.on(ae.Connected, this.handleReconnected).on(ae.SignalConnected, this.handleSignalConnected).on(ae.SignalRestarted, this.handleReconnected).on(ae.SignalResumed, this.handleReconnected).on(ae.Restarting, this.handleReconnecting).on(ae.Resuming, this.handleReconnecting).on(ae.LocalTrackUnpublished, this.handleLocalTrackUnpublished).on(ae.SubscribedQualityUpdate, this.handleSubscribedQualityUpdate).on(ae.Closing, this.handleClosing).on(ae.SignalRequestResponse, this.handleSignalRequestResponse);
  }
  /**
   * Sets and updates the metadata of the local participant.
   * Note: this requires `canUpdateOwnMetadata` permission.
   * method will throw if the user doesn't have the required permissions
   * @param metadata
   */
  setMetadata(e) {
    return R(this, void 0, void 0, function* () {
      yield this.requestMetadataUpdate({
        metadata: e
      });
    });
  }
  /**
   * Sets and updates the name of the local participant.
   * Note: this requires `canUpdateOwnMetadata` permission.
   * method will throw if the user doesn't have the required permissions
   * @param metadata
   */
  setName(e) {
    return R(this, void 0, void 0, function* () {
      yield this.requestMetadataUpdate({
        name: e
      });
    });
  }
  /**
   * Set or update participant attributes. It will make updates only to keys that
   * are present in `attributes`, and will not override others.
   * Note: this requires `canUpdateOwnMetadata` permission.
   * @param attributes attributes to update
   */
  setAttributes(e) {
    return R(this, void 0, void 0, function* () {
      yield this.requestMetadataUpdate({
        attributes: e
      });
    });
  }
  requestMetadataUpdate(e) {
    return R(this, arguments, void 0, function(t) {
      var i = this;
      let s = t.metadata, c = t.name, u = t.attributes;
      return (function* () {
        return new Gt((d, h) => R(i, void 0, void 0, function* () {
          var f, p;
          try {
            let v = !1;
            const y = yield this.engine.client.sendUpdateLocalMetadata((f = s ?? this.metadata) !== null && f !== void 0 ? f : "", (p = c ?? this.name) !== null && p !== void 0 ? p : "", u), T = performance.now();
            for (this.pendingSignalRequests.set(y, {
              resolve: d,
              reject: (_) => {
                h(_), v = !0;
              },
              values: {
                name: c,
                metadata: s,
                attributes: u
              }
            }); performance.now() - T < 5e3 && !v; ) {
              if ((!c || this.name === c) && (!s || this.metadata === s) && (!u || Object.entries(u).every((_) => {
                let k = ge(_, 2), x = k[0], A = k[1];
                return this.attributes[x] === A || A === "" && !this.attributes[x];
              }))) {
                this.pendingSignalRequests.delete(y), d();
                return;
              }
              yield zt(50);
            }
            h(new Ob("Request to update local metadata timed out", "TimeoutError"));
          } catch (v) {
            v instanceof Error ? h(v) : h(new Error(String(v)));
          }
        }));
      })();
    });
  }
  /**
   * Enable or disable a participant's camera track.
   *
   * If a track has already published, it'll mute or unmute the track.
   * Resolves with a `LocalTrackPublication` instance if successful and `undefined` otherwise
   */
  setCameraEnabled(e, t, i) {
    return this.setTrackEnabled(H.Source.Camera, e, t, i);
  }
  /**
   * Enable or disable a participant's microphone track.
   *
   * If a track has already published, it'll mute or unmute the track.
   * Resolves with a `LocalTrackPublication` instance if successful and `undefined` otherwise
   */
  setMicrophoneEnabled(e, t, i) {
    return this.setTrackEnabled(H.Source.Microphone, e, t, i);
  }
  /**
   * Start or stop sharing a participant's screen
   * Resolves with a `LocalTrackPublication` instance if successful and `undefined` otherwise
   */
  setScreenShareEnabled(e, t, i) {
    return this.setTrackEnabled(H.Source.ScreenShare, e, t, i);
  }
  /** @internal */
  setE2EEEnabled(e) {
    return R(this, void 0, void 0, function* () {
      const t = yield this.e2eeStateMutex.lock();
      try {
        if (this.encryptionType = e ? $e.GCM : $e.NONE, yield Promise.all(this.pendingPublishPromises.values()), this.trackPublications.size === 0 || Array.from(this.trackPublications.values()).every((i) => i.isEncrypted === e))
          return;
        yield this.republishAllTracks(void 0, !1);
      } finally {
        t();
      }
    });
  }
  setTrackEnabled(e, t, i, s) {
    return R(this, void 0, void 0, function* () {
      var c, u;
      this.log.debug("setTrackEnabled", {
        source: e,
        enabled: t
      }), this.republishPromise && (yield this.republishPromise);
      let d = this.getTrackPublication(e);
      if (t)
        if (d)
          yield d.unmute();
        else {
          let v;
          if (this.pendingPublishing.has(e)) {
            const y = yield this.waitForPendingPublicationOfSource(e);
            return y || this.log.info("waiting for pending publication promise timed out", {
              source: e
            }), yield y?.unmute(), y;
          }
          this.pendingPublishing.add(e);
          try {
            switch (e) {
              case H.Source.Camera:
                v = yield this.createTracks({
                  video: (c = i) !== null && c !== void 0 ? c : !0
                });
                break;
              case H.Source.Microphone:
                v = yield this.createTracks({
                  audio: (u = i) !== null && u !== void 0 ? u : !0
                });
                break;
              case H.Source.ScreenShare:
                v = yield this.createScreenTracks(Object.assign({}, i));
                break;
              default:
                throw new Ni(e);
            }
          } catch (y) {
            throw v?.forEach((T) => {
              T.stop();
            }), y instanceof Error && this.emit(X.MediaDevicesError, y, Nh(e)), this.pendingPublishing.delete(e), y;
          }
          for (const y of v) {
            const T = Object.assign(Object.assign({}, this.roomOptions.publishDefaults), i);
            e === H.Source.Microphone && ci(y) && T.preConnectBuffer && (this.log.info("starting preconnect buffer for microphone"), y.startPreConnectBuffer());
          }
          try {
            const y = [];
            for (const _ of v)
              this.log.info("publishing track", Oe(_)), y.push(this.publishTrack(_, s));
            const T = yield Promise.all(y);
            var h = ge(T, 1);
            d = h[0];
          } catch (y) {
            throw v?.forEach((T) => {
              T.stop();
            }), y;
          } finally {
            this.pendingPublishing.delete(e);
          }
        }
      else if (!d?.track && this.pendingPublishing.has(e) && (d = yield this.waitForPendingPublicationOfSource(e), d || this.log.info("waiting for pending publication promise timed out", {
        source: e
      })), d && d.track)
        if (e === H.Source.ScreenShare) {
          const v = [this.unpublishTrack(d.track)], y = this.getTrackPublication(H.Source.ScreenShareAudio);
          y && y.track && v.push(this.unpublishTrack(y.track));
          var f = yield Promise.all(v), p = ge(f, 1);
          d = p[0];
        } else
          yield d.mute();
      return d;
    });
  }
  /**
   * Publish both camera and microphone at the same time. This is useful for
   * displaying a single Permission Dialog box to the end user.
   */
  enableCameraAndMicrophone() {
    return R(this, void 0, void 0, function* () {
      if (!(this.pendingPublishing.has(H.Source.Camera) || this.pendingPublishing.has(H.Source.Microphone))) {
        this.pendingPublishing.add(H.Source.Camera), this.pendingPublishing.add(H.Source.Microphone);
        try {
          const e = yield this.createTracks({
            audio: !0,
            video: !0
          });
          yield Promise.all(e.map((t) => this.publishTrack(t)));
        } finally {
          this.pendingPublishing.delete(H.Source.Camera), this.pendingPublishing.delete(H.Source.Microphone);
        }
      }
    });
  }
  /**
   * Create local camera and/or microphone tracks
   * @param options
   * @returns
   */
  createTracks(e) {
    return R(this, void 0, void 0, function* () {
      var t, i;
      e ?? (e = {});
      const s = TS(e, (t = this.roomOptions) === null || t === void 0 ? void 0 : t.audioCaptureDefaults, (i = this.roomOptions) === null || i === void 0 ? void 0 : i.videoCaptureDefaults);
      try {
        return (yield yu(s, {
          loggerName: this.roomOptions.loggerName,
          loggerContextCb: () => this.logContext
        })).map((d) => (ci(d) && (this.microphoneError = void 0, d.setAudioContext(this.audioContext), d.source = H.Source.Microphone, this.emit(X.AudioStreamAcquired)), Li(d) && (this.cameraError = void 0, d.source = H.Source.Camera), d));
      } catch (c) {
        throw c instanceof Error && (e.audio && (this.microphoneError = c), e.video && (this.cameraError = c)), c;
      }
    });
  }
  /**
   * Creates a screen capture tracks with getDisplayMedia().
   * A LocalVideoTrack is always created and returned.
   * If { audio: true }, and the browser supports audio capture, a LocalAudioTrack is also created.
   */
  createScreenTracks(e) {
    return R(this, void 0, void 0, function* () {
      if (e === void 0 && (e = {}), navigator.mediaDevices.getDisplayMedia === void 0)
        throw new mm("getDisplayMedia not supported");
      e.resolution === void 0 && !fw() && (e.resolution = pm.h1080fps30.resolution);
      const t = $1(e), i = yield navigator.mediaDevices.getDisplayMedia(t), s = i.getVideoTracks();
      if (s.length === 0)
        throw new Ni("no video track found");
      const c = new Gl(s[0], void 0, !1, {
        loggerName: this.roomOptions.loggerName,
        loggerContextCb: () => this.logContext
      });
      c.source = H.Source.ScreenShare, e.contentHint && (c.mediaStreamTrack.contentHint = e.contentHint);
      const u = [c];
      if (i.getAudioTracks().length > 0) {
        this.emit(X.AudioStreamAcquired);
        const d = new Hl(i.getAudioTracks()[0], void 0, !1, this.audioContext, {
          loggerName: this.roomOptions.loggerName,
          loggerContextCb: () => this.logContext
        });
        d.source = H.Source.ScreenShareAudio, u.push(d);
      }
      return u;
    });
  }
  /**
   * Publish a new track to the room
   * @param track
   * @param options
   */
  publishTrack(e, t) {
    return R(this, void 0, void 0, function* () {
      return this.publishOrRepublishTrack(e, t);
    });
  }
  /**
   * Waits for the engine's next `Restarted` event. Unlike `engine.waitForRestarted`, this does
   * not short-circuit when `pcState === Connected` — at the point this is called (right after a
   * `NegotiationError`) the PC transport is still connected, but `fullReconnectOnNext` has been
   * set and `attemptReconnect` is queued via setTimeout. We need to wait for that restart to
   * actually complete (which clears `pendingTrackResolvers` via `cleanupClient`) before retrying.
   */
  waitForNextEngineRestart() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 15e3;
    return new Promise((t, i) => {
      const s = () => {
        clearTimeout(d), this.engine.off(ae.Restarted, c), this.engine.off(ae.Closing, u);
      }, c = () => {
        s(), t();
      }, u = () => {
        s(), i(new Error("engine closed before restart completed"));
      }, d = setTimeout(() => {
        s(), i(new Error("timed out waiting for engine restart"));
      }, e);
      this.engine.once(ae.Restarted, c), this.engine.once(ae.Closing, u);
    });
  }
  publishOrRepublishTrack(e, t) {
    return R(this, arguments, void 0, function(i, s) {
      var c = this;
      let u = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !1, d = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : !1;
      return (function* () {
        var h, f, p, v;
        Ai(i) && i.setAudioContext(c.audioContext), yield (h = c.reconnectFuture) === null || h === void 0 ? void 0 : h.promise, c.republishPromise && !u && (yield c.republishPromise), is(i) && c.pendingPublishPromises.has(i) && (yield c.pendingPublishPromises.get(i));
        let y;
        if (i instanceof MediaStreamTrack)
          y = i.getConstraints();
        else {
          y = i.constraints;
          let B;
          switch (i.source) {
            case H.Source.Microphone:
              B = "audioinput";
              break;
            case H.Source.Camera:
              B = "videoinput";
          }
          B && c.activeDeviceMap.has(B) && (y = Object.assign(Object.assign({}, y), {
            deviceId: c.activeDeviceMap.get(B)
          }));
        }
        if (i instanceof MediaStreamTrack)
          switch (i.kind) {
            case "audio":
              i = new Hl(i, y, !0, c.audioContext, {
                loggerName: c.roomOptions.loggerName,
                loggerContextCb: () => c.logContext
              });
              break;
            case "video":
              i = new Gl(i, y, !0, {
                loggerName: c.roomOptions.loggerName,
                loggerContextCb: () => c.logContext
              });
              break;
            default:
              throw new Ni("unsupported MediaStreamTrack kind ".concat(i.kind));
          }
        else
          i.updateLoggerOptions({
            loggerName: c.roomOptions.loggerName,
            loggerContextCb: () => c.logContext
          });
        let T;
        if (c.trackPublications.forEach((B) => {
          B.track && B.track === i && (T = B);
        }), T)
          return c.log.warn("track has already been published, skipping", Oe(T)), T;
        const _ = Object.assign(Object.assign({}, c.roomOptions.publishDefaults), s), k = "channelCount" in i.mediaStreamTrack.getSettings() && // @ts-ignore `channelCount` on getSettings() is currently only available for Safari, but is generally the best way to determine a stereo track https://developer.mozilla.org/en-US/docs/Web/API/MediaTrackSettings/channelCount
        i.mediaStreamTrack.getSettings().channelCount === 2 || i.mediaStreamTrack.getConstraints().channelCount === 2, x = (f = _.forceStereo) !== null && f !== void 0 ? f : k;
        x && (_.dtx === void 0 && c.log.debug("Opus DTX will be disabled for stereo tracks by default. Enable them explicitly to make it work.", Oe(i)), _.red === void 0 && c.log.debug("Opus RED will be disabled for stereo tracks by default. Enable them explicitly to make it work."), (p = _.dtx) !== null && p !== void 0 || (_.dtx = !1), (v = _.red) !== null && v !== void 0 || (_.red = !1)), !mw() && c.roomOptions.e2ee && (c.log.info("End-to-end encryption is set up, simulcast publishing will be disabled on Safari versions and iOS browsers running iOS < v17.2"), _.simulcast = !1), _.source && (i.source = _.source);
        const A = new Promise((B, ie) => R(c, void 0, void 0, function* () {
          try {
            if (this.engine.client.currentState !== Ze.CONNECTED) {
              this.log.debug("deferring track publication until signal is connected", {
                track: Oe(i)
              });
              let C = !1;
              const E = setTimeout(() => {
                C = !0, i.stop(), ie(new Ab("publishing rejected as engine not connected within timeout", 408));
              }, 15e3);
              if (yield this.waitUntilEngineConnected(), clearTimeout(E), C)
                return;
              const M = yield this.publish(i, _, x);
              B(M);
            } else
              try {
                const C = yield this.publish(i, _, x);
                B(C);
              } catch (C) {
                ie(C);
              }
          } catch (C) {
            ie(C);
          }
        }));
        c.pendingPublishPromises.set(i, A);
        try {
          return yield A;
        } catch (B) {
          if (!d && B instanceof er)
            return c.log.warn("negotiation due to track publish failed, retrying after reconnect", {
              error: B
            }), c.pendingPublishPromises.delete(i), yield c.waitForNextEngineRestart(), yield c.publishOrRepublishTrack(i, s, u, !0);
          throw B;
        } finally {
          c.pendingPublishPromises.delete(i);
        }
      })();
    });
  }
  waitUntilEngineConnected() {
    return this.signalConnectedFuture || (this.signalConnectedFuture = new wt()), this.signalConnectedFuture.promise;
  }
  hasPermissionsToPublish(e) {
    if (!this.permissions)
      return this.log.warn("no permissions present for publishing track", Oe(e)), !1;
    const t = this.permissions, i = t.canPublish, s = t.canPublishSources;
    return i && (s.length === 0 || s.map((c) => iw(c)).includes(e.source)) ? !0 : (this.log.warn("insufficient permissions to publish", Oe(e)), !1);
  }
  publish(e, t, i) {
    return R(this, void 0, void 0, function* () {
      var s, c, u, d, h, f, p, v, y, T;
      if (!this.hasPermissionsToPublish(e))
        throw new Ab("failed to publish track, insufficient permissions", 403);
      Array.from(this.trackPublications.values()).find((Y) => is(e) && Y.source === e.source) && e.source !== H.Source.Unknown && this.log.info("publishing a second track with the same source: ".concat(e.source), Oe(e)), t.stopMicTrackOnMute && ci(e) && (e.stopOnMute = !0), e.source === H.Source.ScreenShare && rr() && (t.simulcast = !1), t.videoCodec === "av1" && !cw() && (t.videoCodec = void 0), t.videoCodec === "vp9" && !lw() && (t.videoCodec = void 0), t.videoCodec === void 0 && (t.videoCodec = jh), this.enabledPublishVideoCodecs.length > 0 && (this.enabledPublishVideoCodecs.some((Y) => t.videoCodec === Co(Y.mime)) || (t.videoCodec = Co(this.enabledPublishVideoCodecs[0].mime)));
      const k = t.videoCodec;
      e.on(re.Muted, this.onTrackMuted), e.on(re.Unmuted, this.onTrackUnmuted), e.on(re.Ended, this.handleTrackEnded), e.on(re.UpstreamPaused, this.onTrackUpstreamPaused), e.on(re.UpstreamResumed, this.onTrackUpstreamResumed), e.on(re.AudioTrackFeatureUpdate, this.onTrackFeatureUpdate);
      const x = [], A = !(!((s = t.dtx) !== null && s !== void 0) || s), B = e.getSourceTrackSettings();
      B.autoGainControl && x.push(_t.TF_AUTO_GAIN_CONTROL), B.echoCancellation && x.push(_t.TF_ECHO_CANCELLATION), B.noiseSuppression && x.push(_t.TF_NOISE_SUPPRESSION), B.channelCount && B.channelCount > 1 && x.push(_t.TF_STEREO), A && x.push(_t.TF_NO_DTX), Ai(e) && e.hasPreConnectBuffer && x.push(_t.TF_PRECONNECT_BUFFER);
      const ie = this.normalizeRequestedFrameMetadataOptions(e, t), C = new Oo({
        // get local track id for use during publishing
        cid: e.mediaStreamTrack.id,
        name: t.name,
        type: H.kindToProto(e.kind),
        muted: e.isMuted,
        source: H.sourceToProto(e.source),
        disableDtx: A,
        encryption: this.encryptionType,
        stereo: i,
        disableRed: this.isE2EEEnabled || !(!((c = t.red) !== null && c !== void 0) || c),
        stream: t?.stream,
        backupCodecPolicy: t?.backupCodecPolicy,
        audioFeatures: x,
        packetTrailerFeatures: ie
      });
      let E;
      if (e.kind === H.Kind.Video) {
        let Y;
        try {
          Y = yield e.waitForDimensions();
        } catch {
          const ne = (d = (u = this.roomOptions.videoCaptureDefaults) === null || u === void 0 ? void 0 : u.resolution) !== null && d !== void 0 ? d : Do.h720.resolution;
          Y = {
            width: ne.width,
            height: ne.height
          }, this.log.error("could not determine track dimensions, using defaults", Object.assign(Object.assign({}, Oe(e)), {
            dims: Y
          }));
        }
        C.width = Y.width, C.height = Y.height, ii(e) && (Vn(k) && (e.source === H.Source.ScreenShare && (t.scalabilityMode = "L1T3", "contentHint" in e.mediaStreamTrack && (e.mediaStreamTrack.contentHint = "motion", this.log.debug("forcing contentHint to motion for screenshare with SVC codecs", Oe(e)))), t.scalabilityMode = (h = t.scalabilityMode) !== null && h !== void 0 ? h : "L3T3_KEY"), C.simulcastCodecs = [new Sh({
          codec: k,
          cid: e.mediaStreamTrack.id
        })], t.backupCodec === !0 && (t.backupCodec = {
          codec: jh
        }), t.backupCodec && k !== t.backupCodec.codec && // TODO remove this once e2ee is supported for backup codecs
        C.encryption === $e.NONE && (this.roomOptions.dynacast || (this.roomOptions.dynacast = !0), C.simulcastCodecs.push(new Sh({
          codec: t.backupCodec.codec,
          cid: ""
        })))), E = Vl(e.source === H.Source.ScreenShare, C.width, C.height, t), C.layers = n0(C.width, C.height, E, Vn(t.videoCodec));
      } else e.kind === H.Kind.Audio && (E = [{
        maxBitrate: (f = t.audioPreset) === null || f === void 0 ? void 0 : f.maxBitrate,
        priority: (v = (p = t.audioPreset) === null || p === void 0 ? void 0 : p.priority) !== null && v !== void 0 ? v : "high",
        networkPriority: (T = (y = t.audioPreset) === null || y === void 0 ? void 0 : y.priority) !== null && T !== void 0 ? T : "high"
      }]);
      if (!this.engine || this.engine.isClosed)
        throw new rt("cannot publish track when not connected");
      const M = () => R(this, void 0, void 0, function* () {
        var Y, J, ne;
        if (!this.engine.pcManager)
          throw new rt("pcManager is not ready");
        if (e.sender = yield this.engine.createSender(e, t, E), ii(e) && (e.publishOptions = t), this.emit(X.LocalSenderCreated, e.sender, e), ii(e) && ((Y = t.degradationPreference) !== null && Y !== void 0 || (t.degradationPreference = OM(e)), e.setDegradationPreference(t.degradationPreference)), E)
          if (rr() && e.kind === H.Kind.Audio) {
            let de;
            for (const fe of this.engine.pcManager.publisher.getTransceivers())
              if (fe.sender === e.sender) {
                de = fe;
                break;
              }
            de && this.engine.pcManager.publisher.setTrackCodecBitrate({
              transceiver: de,
              codec: "opus",
              maxbr: !((J = E[0]) === null || J === void 0) && J.maxBitrate ? E[0].maxBitrate / 1e3 : 0
            });
          } else e.codec && Vn(e.codec) && (!((ne = E[0]) === null || ne === void 0) && ne.maxBitrate) && this.engine.pcManager.publisher.setTrackCodecBitrate({
            cid: C.cid,
            codec: e.codec,
            maxbr: E[0].maxBitrate / 1e3
          });
        yield this.engine.negotiate();
      });
      let N;
      const G = new Promise((Y, J) => R(this, void 0, void 0, function* () {
        var ne;
        try {
          N = yield this.engine.addTrack(C), Y(N);
        } catch (de) {
          if (e.sender && (!((ne = this.engine.pcManager) === null || ne === void 0) && ne.publisher)) {
            try {
              this.engine.pcManager.publisher.removeTrack(e.sender);
            } catch (fe) {
              this.log.error(fe);
            }
            yield this.engine.negotiate().catch((fe) => {
              this.log.error("failed to negotiate after removing track due to failed add track request", Object.assign(Object.assign({}, Oe(e)), {
                error: fe
              }));
            });
          }
          J(de);
        }
      }));
      if (this.enabledPublishVideoCodecs.length > 0 && ie.length === 0)
        N = (yield Promise.all([G, M()]))[0];
      else {
        N = yield G;
        let Y;
        if (N.codecs.forEach((J) => {
          Y === void 0 && (Y = J.mimeType);
        }), Y && e.kind === H.Kind.Video) {
          const J = Co(Y);
          J !== k && (this.log.debug("falling back to server selected codec", Object.assign(Object.assign({}, Oe(e)), {
            codec: J
          })), t.videoCodec = J, E = Vl(e.source === H.Source.ScreenShare, C.width, C.height, t));
        }
        yield M();
      }
      const W = new Vh(e.kind, N, e, {
        loggerName: this.roomOptions.loggerName,
        loggerContextCb: () => this.logContext
      });
      if (W.on(re.CpuConstrained, (Y) => this.onTrackCpuConstrained(Y, W)), W.options = t, e.sid = N.sid, ii(e) && (e.publishOptions = t, C.width && C.height && (e.lastEncodedDimensions = {
        width: C.width,
        height: C.height
      })), this.log.debug("publishing ".concat(e.kind, " with encodings"), {
        encodings: E,
        trackInfo: N
      }), ii(e) ? e.startMonitor(this.engine.client) : Ai(e) && e.startMonitor(), this.addTrackPublication(W), this.emit(X.LocalTrackPublished, W), Ai(e) && N.audioFeatures.includes(_t.TF_PRECONNECT_BUFFER)) {
        const Y = e.getPreConnectBuffer(), J = e.getPreConnectBufferMimeType();
        this.on(X.LocalTrackSubscribed, (ne) => {
          if (ne.trackSid === N.sid) {
            if (!e.hasPreConnectBuffer) {
              this.log.warn("subscribe event came to late, buffer already closed");
              return;
            }
            this.log.debug("finished recording preconnect buffer", Oe(e)), e.stopPreConnectBuffer();
          }
        }), Y && new Promise((de, fe) => R(this, void 0, void 0, function* () {
          var ke, q, te, le, Me, Ae;
          try {
            this.log.debug("waiting for agent", Oe(e));
            const ce = setTimeout(() => {
              fe(new Error("agent not active within 10 seconds"));
            }, 1e4), pe = yield this.waitUntilActiveAgentPresent();
            clearTimeout(ce), this.log.debug("sending preconnect buffer", Oe(e));
            const oe = yield this.streamBytes({
              name: "preconnect-buffer",
              mimeType: J,
              topic: "lk.agent.pre-connect-audio-buffer",
              destinationIdentities: [pe.identity],
              attributes: {
                trackId: W.trackSid,
                sampleRate: String((Me = B.sampleRate) !== null && Me !== void 0 ? Me : "48000"),
                channels: String((Ae = B.channelCount) !== null && Ae !== void 0 ? Ae : "1")
              }
            });
            try {
              for (var w = !0, K = Hn(Y), se; se = yield K.next(), ke = se.done, !ke; w = !0) {
                le = se.value, w = !1;
                const ue = le;
                yield oe.write(ue);
              }
            } catch (ue) {
              q = {
                error: ue
              };
            } finally {
              try {
                !w && !ke && (te = K.return) && (yield te.call(K));
              } finally {
                if (q) throw q.error;
              }
            }
            yield oe.close(), de();
          } catch (ce) {
            fe(ce);
          }
        })).then(() => {
          this.log.debug("preconnect buffer sent successfully", Oe(e));
        }).catch((de) => {
          this.log.error("error sending preconnect buffer", Object.assign(Object.assign({}, Oe(e)), {
            error: de
          }));
        });
      }
      return W;
    });
  }
  canPublishFrameMetadata() {
    var e;
    return !!(this.roomOptions.e2ee || this.roomOptions.encryption || Io((e = this.roomOptions.frameMetadata) !== null && e !== void 0 ? e : this.roomOptions.packetTrailer));
  }
  normalizeRequestedFrameMetadataOptions(e, t) {
    var i;
    const s = (i = t.frameMetadata) !== null && i !== void 0 ? i : t.packetTrailer;
    if (e.kind !== H.Kind.Video || !jl(s))
      return t.frameMetadata = void 0, t.packetTrailer = void 0, [];
    if (!this.canPublishFrameMetadata())
      return this.log.warn("frame metadata transform not supported; not advertising features", Object.assign(Object.assign({}, this.logContext), Oe(e))), t.frameMetadata = void 0, t.packetTrailer = void 0, [];
    const c = jw(s), u = qw(c);
    return t.frameMetadata = u, t.packetTrailer = u, c;
  }
  get isLocal() {
    return !0;
  }
  /** @internal
   * publish additional codec to existing track
   */
  publishAdditionalCodecForTrack(e, t, i) {
    return R(this, void 0, void 0, function* () {
      var s;
      if (this.encryptionType !== $e.NONE)
        return;
      let c;
      if (this.trackPublications.forEach((_) => {
        _.track && _.track === e && (c = _);
      }), !c)
        throw new Ni("track is not published");
      if (!ii(e))
        throw new Ni("track is not a video track");
      const u = Object.assign(Object.assign({}, (s = this.roomOptions) === null || s === void 0 ? void 0 : s.publishDefaults), i), d = NS(e, t, u);
      if (!d) {
        this.log.info("backup codec has been disabled, ignoring request to add additional codec for track", Oe(e));
        return;
      }
      const h = e.addSimulcastTrack(t, d);
      if (!h)
        return;
      const f = this.normalizeRequestedFrameMetadataOptions(e, u), p = new Oo({
        cid: h.mediaStreamTrack.id,
        type: H.kindToProto(e.kind),
        muted: e.isMuted,
        source: H.sourceToProto(e.source),
        sid: e.sid,
        packetTrailerFeatures: f,
        simulcastCodecs: [{
          codec: u.videoCodec,
          cid: h.mediaStreamTrack.id
        }]
      });
      if (p.layers = n0(p.width, p.height, d), !this.engine || this.engine.isClosed)
        throw new rt("cannot publish track when not connected");
      const v = () => R(this, void 0, void 0, function* () {
        yield this.engine.createSimulcastSender(e, h, u, d), yield this.engine.negotiate();
      }), T = (yield Promise.all([this.engine.addTrack(p), v()]))[0];
      this.log.debug("published ".concat(t, " for track ").concat(e.sid), {
        encodings: d,
        trackInfo: T
      });
    });
  }
  unpublishTrack(e, t) {
    return R(this, void 0, void 0, function* () {
      var i, s;
      if (is(e)) {
        const p = this.pendingPublishPromises.get(e);
        p && (this.log.debug("awaiting publish promise before attempting to unpublish", Oe(e)), yield p);
      }
      const c = this.getPublicationForTrack(e), u = c ? Oe(c) : void 0;
      if (this.log.info("unpublishing track", u), !c || !c.track) {
        this.log.warn("track was not unpublished because no publication was found", u);
        return;
      }
      e = c.track, e.off(re.Muted, this.onTrackMuted), e.off(re.Unmuted, this.onTrackUnmuted), e.off(re.Ended, this.handleTrackEnded), e.off(re.UpstreamPaused, this.onTrackUpstreamPaused), e.off(re.UpstreamResumed, this.onTrackUpstreamResumed), e.off(re.AudioTrackFeatureUpdate, this.onTrackFeatureUpdate), t === void 0 && (t = (s = (i = this.roomOptions) === null || i === void 0 ? void 0 : i.stopLocalTrackOnUnpublish) !== null && s !== void 0 ? s : !0), t ? e.stop() : e.stopMonitor();
      let d = !1;
      const h = e.sender;
      if (e.sender = void 0, this.engine.pcManager && this.engine.pcManager.currentState < st.FAILED && h)
        try {
          for (const p of this.engine.pcManager.publisher.getTransceivers())
            p.sender === h && (p.direction = "inactive", d = !0);
          try {
            d = this.engine.removeTrack(h);
          } catch (p) {
            this.log.warn(p), d = !0;
          }
          if (ii(e)) {
            for (const p of e.simulcastCodecs) {
              var f = ge(p, 2);
              const v = f[1];
              if (v.sender) {
                try {
                  d = this.engine.removeTrack(v.sender);
                } catch (y) {
                  this.log.warn(y), d = !0;
                }
                v.sender = void 0;
              }
            }
            e.simulcastCodecs.clear();
          }
        } catch (p) {
          this.log.warn("failed to unpublish track", Object.assign(Object.assign({}, u), {
            error: p
          }));
        }
      switch (this.trackPublications.delete(c.trackSid), c.kind) {
        case H.Kind.Audio:
          this.audioTrackPublications.delete(c.trackSid);
          break;
        case H.Kind.Video:
          this.videoTrackPublications.delete(c.trackSid);
          break;
      }
      return this.emit(X.LocalTrackUnpublished, c), c.setTrack(void 0), d && (yield this.engine.negotiate()), c;
    });
  }
  unpublishTracks(e) {
    return R(this, void 0, void 0, function* () {
      return (yield Promise.all(e.map((i) => this.unpublishTrack(i)))).filter((i) => !!i);
    });
  }
  republishAllTracks(e) {
    return R(this, arguments, void 0, function(t) {
      var i = this;
      let s = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0;
      return (function* () {
        i.republishPromise && (yield i.republishPromise), i.republishPromise = new Gt((c, u) => R(i, void 0, void 0, function* () {
          try {
            const d = [];
            this.trackPublications.forEach((h) => {
              h.track && (t && (h.options = Object.assign(Object.assign({}, h.options), t)), d.push(h));
            }), yield Promise.all(d.map((h) => R(this, void 0, void 0, function* () {
              const f = h.track;
              yield this.unpublishTrack(f, !1), s && !f.isMuted && f.source !== H.Source.ScreenShare && f.source !== H.Source.ScreenShareAudio && (Ai(f) || ii(f)) && !f.isUserProvided && (this.log.debug("restarting existing track", {
                track: h.trackSid
              }), yield f.restartTrack()), yield this.publishOrRepublishTrack(f, h.options, !0);
            }))), c();
          } catch (d) {
            d instanceof Error ? u(d) : u(new Error(String(d)));
          } finally {
            this.republishPromise = void 0;
          }
        })), yield i.republishPromise;
      })();
    });
  }
  /**
   * Publish a new data payload to the room. Data will be forwarded to each
   * participant in the room if the destination field in publishOptions is empty
   *
   * @param data Uint8Array of the payload. To send string data, use TextEncoder.encode
   * @param options optionally specify a `reliable`, `topic` and `destination`
   */
  publishData(e) {
    return R(this, arguments, void 0, function(t) {
      var i = this;
      let s = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
      return (function* () {
        const c = s.reliable ? _e.RELIABLE : _e.LOSSY, u = s.reliable ? ba.RELIABLE : ba.LOSSY, d = s.destinationIdentities, h = s.topic;
        let f = new tm({
          participantIdentity: i.identity,
          payload: t,
          destinationIdentities: d,
          topic: h
        });
        const p = new Bt({
          kind: u,
          value: {
            case: "user",
            value: f
          }
        });
        yield i.engine.sendDataPacket(p, c);
      })();
    });
  }
  /**
   * Publish SIP DTMF message to the room.
   *
   * @param code DTMF code
   * @param digit DTMF digit
   */
  publishDtmf(e, t) {
    return R(this, void 0, void 0, function* () {
      const i = new Bt({
        kind: ba.RELIABLE,
        value: {
          case: "sipDtmf",
          value: new dT({
            code: e,
            digit: t
          })
        }
      });
      yield this.engine.sendDataPacket(i, _e.RELIABLE);
    });
  }
  /** @deprecated Consider migrating to {@link sendText} */
  sendChatMessage(e, t) {
    return R(this, void 0, void 0, function* () {
      const i = {
        id: crypto.randomUUID(),
        message: e,
        timestamp: Date.now(),
        attachedFiles: t?.attachments
      }, s = new Bt({
        value: {
          case: "chatMessage",
          value: new Il(Object.assign(Object.assign({}, i), {
            timestamp: at.parse(i.timestamp)
          }))
        }
      });
      return yield this.engine.sendDataPacket(s, _e.RELIABLE), this.emit(X.ChatMessage, i), i;
    });
  }
  /** @deprecated Consider migrating to {@link sendText} */
  editChatMessage(e, t) {
    return R(this, void 0, void 0, function* () {
      const i = Object.assign(Object.assign({}, t), {
        message: e,
        editTimestamp: Date.now()
      }), s = new Bt({
        value: {
          case: "chatMessage",
          value: new Il(Object.assign(Object.assign({}, i), {
            timestamp: at.parse(i.timestamp),
            editTimestamp: at.parse(i.editTimestamp)
          }))
        }
      });
      return yield this.engine.sendDataPacket(s, _e.RELIABLE), this.emit(X.ChatMessage, i), i;
    });
  }
  /**
   * Sends the given string to participants in the room via the data channel.
   * For longer messages, consider using {@link streamText} instead.
   *
   * @param text The text payload
   * @param options.topic Topic identifier used to route the stream to appropriate handlers.
   */
  sendText(e, t) {
    return R(this, void 0, void 0, function* () {
      return this.roomOutgoingDataStreamManager.sendText(e, t);
    });
  }
  /**
   * Creates a new TextStreamWriter which can be used to stream text incrementally
   * to participants in the room via the data channel.
   *
   * @param options.topic Topic identifier used to route the stream to appropriate handlers.
   *
   * @internal
   * @experimental CAUTION, might get removed in a minor release
   */
  streamText(e) {
    return R(this, void 0, void 0, function* () {
      return this.roomOutgoingDataStreamManager.streamText(e);
    });
  }
  /** Send a File to all participants in the room via the data channel.
   * @param file The File object payload
   * @param options.topic Topic identifier used to route the stream to appropriate handlers.
   * @param options.onProgress A callback function used to monitor the upload progress percentage.
   */
  sendFile(e, t) {
    return R(this, void 0, void 0, function* () {
      return this.roomOutgoingDataStreamManager.sendFile(e, t);
    });
  }
  /**
   * Stream bytes incrementally to participants in the room via the data channel.
   * For sending files, consider using {@link sendFile} instead.
   *
   * @param options.topic Topic identifier used to route the stream to appropriate handlers.
   */
  streamBytes(e) {
    return R(this, void 0, void 0, function* () {
      return this.roomOutgoingDataStreamManager.streamBytes(e);
    });
  }
  /**
   * Initiate an RPC call to a remote participant
   * @param params - Parameters for initiating the RPC call, see {@link PerformRpcParams}
   * @returns A promise that resolves with the response payload or rejects with an error.
   * @throws Error on failure. Details in `message`.
   */
  performRpc(e) {
    return this.rpcClientManager.performRpc(e).then((t) => {
      let i = ge(t, 2);
      return i[0], i[1];
    });
  }
  /**
   * @deprecated use `room.registerRpcMethod` instead
   */
  registerRpcMethod(e, t) {
    this.rpcServerManager.registerRpcMethod(e, t);
  }
  /**
   * @deprecated use `room.unregisterRpcMethod` instead
   */
  unregisterRpcMethod(e) {
    this.rpcServerManager.unregisterRpcMethod(e);
  }
  /**
   * Control who can subscribe to LocalParticipant's published tracks.
   *
   * By default, all participants can subscribe. This allows fine-grained control over
   * who is able to subscribe at a participant and track level.
   *
   * Note: if access is given at a track-level (i.e. both [allParticipantsAllowed] and
   * [ParticipantTrackPermission.allTracksAllowed] are false), any newer published tracks
   * will not grant permissions to any participants and will require a subsequent
   * permissions update to allow subscription.
   *
   * @param allParticipantsAllowed Allows all participants to subscribe all tracks.
   *  Takes precedence over [[participantTrackPermissions]] if set to true.
   *  By default this is set to true.
   * @param participantTrackPermissions Full list of individual permissions per
   *  participant/track. Any omitted participants will not receive any permissions.
   */
  setTrackSubscriptionPermissions(e) {
    let t = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : [];
    this.participantTrackPermissions = t, this.allParticipantsAllowedToSubscribe = e, this.engine.client.isDisconnected || this.updateTrackSubscriptionPermissions();
  }
  /** @internal */
  setEnabledPublishCodecs(e) {
    this.enabledPublishVideoCodecs = e.filter((t) => t.mime.split("/")[0].toLowerCase() === "video");
  }
  /** @internal */
  updateInfo(e) {
    return super.updateInfo(e) ? (e.tracks.forEach((t) => {
      var i, s;
      const c = this.trackPublications.get(t.sid);
      if (c) {
        const u = c.isMuted || ((s = (i = c.track) === null || i === void 0 ? void 0 : i.isUpstreamPaused) !== null && s !== void 0 ? s : !1);
        u !== t.muted && (this.log.debug("updating server mute state after reconcile", Object.assign(Object.assign({}, Oe(c)), {
          mutedOnServer: u
        })), this.engine.client.sendMuteTrack(t.sid, u));
      }
    }), !0) : !1;
  }
  /** @internal */
  setActiveAgent(e) {
    var t, i, s, c;
    this.firstActiveAgent = e, e && !this.firstActiveAgent && (this.firstActiveAgent = e), e ? (i = (t = this.activeAgentFuture) === null || t === void 0 ? void 0 : t.resolve) === null || i === void 0 || i.call(t, e) : (c = (s = this.activeAgentFuture) === null || s === void 0 ? void 0 : s.reject) === null || c === void 0 || c.call(s, new Error("Agent disconnected")), this.activeAgentFuture = void 0;
  }
  waitUntilActiveAgentPresent() {
    return this.firstActiveAgent ? Promise.resolve(this.firstActiveAgent) : (this.activeAgentFuture || (this.activeAgentFuture = new wt()), this.activeAgentFuture.promise);
  }
  getPublicationForTrack(e) {
    let t;
    return this.trackPublications.forEach((i) => {
      const s = i.track;
      s && (e instanceof MediaStreamTrack ? (Ai(s) || ii(s)) && s.mediaStreamTrack === e && (t = i) : e === s && (t = i));
    }), t;
  }
  waitForPendingPublicationOfSource(e) {
    return R(this, void 0, void 0, function* () {
      const i = Date.now();
      for (; Date.now() < i + 1e4; ) {
        const s = Array.from(this.pendingPublishPromises.entries()).find((c) => ge(c, 1)[0].source === e);
        if (s)
          return s[1];
        yield zt(20);
      }
    });
  }
  /** Publishes a data track.
   *
   * Returns the published data track if successful. Use {@link LocalDataTrack#tryPush}
   * to send data frames on the track.
   */
  publishDataTrack(e) {
    return R(this, void 0, void 0, function* () {
      const t = new gu(e, this.roomOutgoingDataTrackManager);
      return yield t.publish(), t;
    });
  }
}
class S0 extends DOMException {
  constructor(e, t) {
    super(e, "AbortError"), this.reason = t;
  }
}
class dA extends Map {
  constructor() {
    super(...arguments), this.pending = /* @__PURE__ */ new Map();
  }
  set(e, t) {
    var i, s;
    super.set(e, t);
    const c = (i = this.pending) === null || i === void 0 ? void 0 : i.get(e);
    if (c) {
      for (const u of c)
        u.isResolved || (s = u.resolve) === null || s === void 0 || s.call(u, t);
      this.pending.delete(e);
    }
    return this;
  }
  get [Symbol.toStringTag]() {
    return "DeferrableMap";
  }
  getDeferred(e, t) {
    return R(this, void 0, void 0, function* () {
      const i = this.get(e);
      if (typeof i < "u")
        return i;
      if (t?.aborted)
        throw new S0("The operation was aborted.", t.reason);
      const s = new wt(void 0, () => {
        const u = this.pending.get(e);
        if (!u)
          return;
        const d = u.indexOf(s);
        d !== -1 && u.splice(d, 1), u.length === 0 && this.pending.delete(e);
      }), c = this.pending.get(e);
      if (c ? c.push(s) : this.pending.set(e, [s]), t) {
        const u = () => {
          var d;
          s.isResolved || (d = s.reject) === null || d === void 0 || d.call(s, new S0("The operation was aborted.", t.reason));
        };
        t.addEventListener("abort", u, {
          once: !0
        }), s.promise.finally(() => {
          t.removeEventListener("abort", u);
        });
      }
      return s.promise;
    });
  }
}
class WS extends Di {
  constructor(e, t, i, s) {
    super(e, t.sid, t.name, s), this.track = void 0, this.allowed = !0, this.requestedDisabled = void 0, this.visible = !0, this.handleEnded = (c) => {
      this.setTrack(void 0), this.emit(re.Ended, c);
    }, this.handleVisibilityChange = (c) => {
      this.log.debug("adaptivestream video visibility ".concat(this.trackSid, ", visible=").concat(c), this.logContext), this.visible = c, this.emitTrackUpdate();
    }, this.handleVideoDimensionsChange = (c) => {
      this.log.debug("adaptivestream video dimensions ".concat(c.width, "x").concat(c.height), this.logContext), this.videoDimensionsAdaptiveStream = c, this.emitTrackUpdate();
    }, this.subscribed = i, this.updateInfo(t);
  }
  /**
   * Subscribe or unsubscribe to this remote track
   * @param subscribed true to subscribe to a track, false to unsubscribe
   */
  setSubscribed(e) {
    const t = this.subscriptionStatus, i = this.permissionStatus;
    this.subscribed = e, e && (this.allowed = !0);
    const s = new cu({
      trackSids: [this.trackSid],
      subscribe: this.subscribed,
      participantTracks: [new hT({
        // sending an empty participant id since TrackPublication doesn't keep it
        // this is filled in by the participant that receives this message
        participantSid: "",
        trackSids: [this.trackSid]
      })]
    });
    this.emit(re.UpdateSubscription, s), this.emitSubscriptionUpdateIfChanged(t), this.emitPermissionUpdateIfChanged(i);
  }
  get subscriptionStatus() {
    return this.subscribed === !1 ? Di.SubscriptionStatus.Unsubscribed : super.isSubscribed ? Di.SubscriptionStatus.Subscribed : Di.SubscriptionStatus.Desired;
  }
  get permissionStatus() {
    return this.allowed ? Di.PermissionStatus.Allowed : Di.PermissionStatus.NotAllowed;
  }
  /**
   * Returns true if track is subscribed, and ready for playback
   */
  get isSubscribed() {
    return this.subscribed === !1 ? !1 : super.isSubscribed;
  }
  // returns client's desire to subscribe to a track, also true if autoSubscribe is enabled
  get isDesired() {
    return this.subscribed !== !1;
  }
  get isEnabled() {
    return this.requestedDisabled !== void 0 ? !this.requestedDisabled : this.isAdaptiveStream ? this.visible : !0;
  }
  get isLocal() {
    return !1;
  }
  /**
   * disable server from sending down data for this track. this is useful when
   * the participant is off screen, you may disable streaming down their video
   * to reduce bandwidth requirements
   * @param enabled
   */
  setEnabled(e) {
    !this.isManualOperationAllowed() || this.requestedDisabled === !e || (this.requestedDisabled = !e, this.emitTrackUpdate());
  }
  /**
   * for tracks that support simulcasting, adjust subscribed quality
   *
   * This indicates the highest quality the client can accept. if network
   * bandwidth does not allow, server will automatically reduce quality to
   * optimize for uninterrupted video
   */
  setVideoQuality(e) {
    !this.isManualOperationAllowed() || this.requestedMaxQuality === e || (this.requestedMaxQuality = e, this.requestedVideoDimensions = void 0, this.emitTrackUpdate());
  }
  /**
   * Explicitly set the video dimensions for this track.
   *
   * This will take precedence over adaptive stream dimensions.
   *
   * @param dimensions The video dimensions to set.
   */
  setVideoDimensions(e) {
    var t, i;
    this.isManualOperationAllowed() && (((t = this.requestedVideoDimensions) === null || t === void 0 ? void 0 : t.width) === e.width && ((i = this.requestedVideoDimensions) === null || i === void 0 ? void 0 : i.height) === e.height || (eh(this.track) && (this.requestedVideoDimensions = e), this.requestedMaxQuality = void 0, this.emitTrackUpdate()));
  }
  setVideoFPS(e) {
    this.isManualOperationAllowed() && eh(this.track) && this.fps !== e && (this.fps = e, this.emitTrackUpdate());
  }
  get videoQuality() {
    var e;
    return (e = this.requestedMaxQuality) !== null && e !== void 0 ? e : yn.HIGH;
  }
  /** @internal */
  setTrack(e) {
    const t = this.subscriptionStatus, i = this.permissionStatus, s = this.track;
    s !== e && (s && (s.off(re.VideoDimensionsChanged, this.handleVideoDimensionsChange), s.off(re.VisibilityChanged, this.handleVisibilityChange), s.off(re.Ended, this.handleEnded), s.detach(), s.stopMonitor(), this.emit(re.Unsubscribed, s)), super.setTrack(e), e && (e.sid = this.trackSid, e.on(re.VideoDimensionsChanged, this.handleVideoDimensionsChange), e.on(re.VisibilityChanged, this.handleVisibilityChange), e.on(re.Ended, this.handleEnded), this.emit(re.Subscribed, e)), this.emitPermissionUpdateIfChanged(i), this.emitSubscriptionUpdateIfChanged(t));
  }
  /** @internal */
  setAllowed(e) {
    const t = this.subscriptionStatus, i = this.permissionStatus;
    this.allowed = e, this.emitPermissionUpdateIfChanged(i), this.emitSubscriptionUpdateIfChanged(t);
  }
  /** @internal */
  setSubscriptionError(e) {
    this.emit(re.SubscriptionFailed, e);
  }
  /** @internal */
  updateInfo(e) {
    super.updateInfo(e);
    const t = this.metadataMuted;
    this.metadataMuted = e.muted, this.track ? this.track.setMuted(e.muted) : t !== e.muted && this.emit(e.muted ? re.Muted : re.Unmuted);
  }
  emitSubscriptionUpdateIfChanged(e) {
    const t = this.subscriptionStatus;
    e !== t && this.emit(re.SubscriptionStatusChanged, t, e);
  }
  emitPermissionUpdateIfChanged(e) {
    this.permissionStatus !== e && this.emit(re.SubscriptionPermissionChanged, this.permissionStatus, e);
  }
  isManualOperationAllowed() {
    return this.isDesired ? !0 : (this.log.warn("cannot update track settings when not subscribed", this.logContext), !1);
  }
  get isAdaptiveStream() {
    return eh(this.track) && this.track.isAdaptiveStream;
  }
  /* @internal */
  emitTrackUpdate() {
    const e = new MT({
      trackSids: [this.trackSid],
      disabled: !this.isEnabled,
      fps: this.fps
    });
    if (this.kind === H.Kind.Video) {
      let t = this.requestedVideoDimensions;
      if (this.videoDimensionsAdaptiveStream !== void 0)
        if (t)
          Pb(this.videoDimensionsAdaptiveStream, t) && (this.log.debug("using adaptive stream dimensions instead of requested", Object.assign(Object.assign({}, this.logContext), this.videoDimensionsAdaptiveStream)), t = this.videoDimensionsAdaptiveStream);
        else if (this.requestedMaxQuality !== void 0 && this.trackInfo) {
          const i = aw(this.trackInfo, this.requestedMaxQuality);
          i && Pb(this.videoDimensionsAdaptiveStream, i) && (this.log.debug("using adaptive stream dimensions instead of max quality layer", Object.assign(Object.assign({}, this.logContext), this.videoDimensionsAdaptiveStream)), t = this.videoDimensionsAdaptiveStream);
        } else
          this.log.debug("using adaptive stream dimensions", Object.assign(Object.assign({}, this.logContext), this.videoDimensionsAdaptiveStream)), t = this.videoDimensionsAdaptiveStream;
      t ? (e.width = Math.ceil(t.width), e.height = Math.ceil(t.height)) : this.requestedMaxQuality !== void 0 ? (this.log.debug("using requested max quality", Object.assign(Object.assign({}, this.logContext), {
        quality: this.requestedMaxQuality
      })), e.quality = this.requestedMaxQuality) : (this.log.debug("using default quality", Object.assign(Object.assign({}, this.logContext), {
        quality: yn.HIGH
      })), e.quality = yn.HIGH);
    }
    this.emit(re.UpdateSettings, e);
  }
}
class Yl extends ZS {
  /** @internal */
  static fromParticipantInfo(e, t, i, s) {
    return new Yl(e, t.sid, t.identity, t.name, t.metadata, t.attributes, i, t.kind, t.dataTracks.map((c) => {
      const u = ql.from(c);
      return new GS(u, s, {
        publisherIdentity: t.identity
      });
    }), t.clientProtocol);
  }
  get logContext() {
    return Object.assign(Object.assign({}, super.logContext), {
      remoteParticipantID: this.sid,
      remoteParticipant: this.identity
    });
  }
  /** @internal */
  constructor(e, t, i, s, c, u, d) {
    let h = arguments.length > 7 && arguments[7] !== void 0 ? arguments[7] : ka.STANDARD, f = arguments.length > 8 && arguments[8] !== void 0 ? arguments[8] : [], p = arguments.length > 9 && arguments[9] !== void 0 ? arguments[9] : yS;
    super(t, i || "", s, c, u, d, h), this.signalClient = e, this.trackPublications = /* @__PURE__ */ new Map(), this.audioTrackPublications = /* @__PURE__ */ new Map(), this.videoTrackPublications = /* @__PURE__ */ new Map(), this.dataTracks = new dA(f.map((v) => [v.info.name, v])), this.volumeMap = /* @__PURE__ */ new Map(), this.clientProtocol = p;
  }
  addTrackPublication(e) {
    super.addTrackPublication(e), e.on(re.UpdateSettings, (t) => {
      this.log.debug("send update settings", Object.assign(Object.assign(Object.assign({}, this.logContext), Oe(e)), {
        settings: t
      })), this.signalClient.sendUpdateTrackSettings(t);
    }), e.on(re.UpdateSubscription, (t) => {
      t.participantTracks.forEach((i) => {
        i.participantSid = this.sid;
      }), this.signalClient.sendUpdateSubscription(t);
    }), e.on(re.SubscriptionPermissionChanged, (t) => {
      this.emit(X.TrackSubscriptionPermissionChanged, e, t);
    }), e.on(re.SubscriptionStatusChanged, (t) => {
      this.emit(X.TrackSubscriptionStatusChanged, e, t);
    }), e.on(re.Subscribed, (t) => {
      this.emit(X.TrackSubscribed, t, e);
    }), e.on(re.Unsubscribed, (t) => {
      this.emit(X.TrackUnsubscribed, t, e);
    }), e.on(re.SubscriptionFailed, (t) => {
      this.emit(X.TrackSubscriptionFailed, e.trackSid, t);
    });
  }
  getTrackPublication(e) {
    const t = super.getTrackPublication(e);
    if (t)
      return t;
  }
  getTrackPublicationByName(e) {
    const t = super.getTrackPublicationByName(e);
    if (t)
      return t;
  }
  /**
   * sets the volume on the participant's audio track
   * by default, this affects the microphone publication
   * a different source can be passed in as a second argument
   * if no track exists the volume will be applied when the microphone track is added
   */
  setVolume(e) {
    let t = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : H.Source.Microphone;
    this.volumeMap.set(t, e);
    const i = this.getTrackPublication(t);
    i && i.track && i.track.setVolume(e);
  }
  /**
   * gets the volume on the participant's microphone track
   */
  getVolume() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : H.Source.Microphone;
    const t = this.getTrackPublication(e);
    return t && t.track ? t.track.getVolume() : this.volumeMap.get(e);
  }
  /** @internal */
  addSubscribedMediaTrack(e, t, i, s, c, u) {
    let d = this.getTrackPublicationBySid(t);
    if (d || t.startsWith("TR") || this.trackPublications.forEach((p) => {
      !d && e.kind === p.kind.toString() && (d = p);
    }), !d) {
      if (u === 0) {
        this.log.error("could not find published track", Object.assign(Object.assign({}, this.logContext), {
          trackSid: t
        })), this.emit(X.TrackSubscriptionFailed, t);
        return;
      }
      u === void 0 && (u = 20), setTimeout(() => {
        this.addSubscribedMediaTrack(e, t, i, s, c, u - 1);
      }, 150);
      return;
    }
    if (e.readyState === "ended") {
      this.log.error("unable to subscribe because MediaStreamTrack is ended. Do not call MediaStreamTrack.stop()", Object.assign(Object.assign({}, this.logContext), Oe(d))), this.emit(X.TrackSubscriptionFailed, t);
      return;
    }
    const h = e.kind === "video";
    let f;
    return h ? f = new Sm(e, t, s, c) : f = new XS(e, t, s, this.audioContext, this.audioOutput), f.source = d.source, f.isMuted = d.isMuted, f.setMediaStream(i), f.start(), d.setTrack(f), this.volumeMap.has(d.source) && zh(f) && ci(f) && f.setVolume(this.volumeMap.get(d.source)), d;
  }
  /** @internal */
  get hasMetadata() {
    return !!this.participantInfo;
  }
  /**
   * @internal
   */
  getTrackPublicationBySid(e) {
    return this.trackPublications.get(e);
  }
  /** @internal */
  updateInfo(e) {
    if (!super.updateInfo(e))
      return !1;
    const t = /* @__PURE__ */ new Map(), i = /* @__PURE__ */ new Map();
    return e.tracks.forEach((s) => {
      var c, u;
      let d = this.getTrackPublicationBySid(s.sid);
      if (d)
        d.updateInfo(s);
      else {
        const h = H.kindFromProto(s.type);
        if (!h)
          return;
        d = new WS(h, s, (c = this.signalClient.connectOptions) === null || c === void 0 ? void 0 : c.autoSubscribe, {
          loggerContextCb: () => this.logContext,
          loggerName: (u = this.loggerOptions) === null || u === void 0 ? void 0 : u.loggerName
        }), d.updateInfo(s), i.set(s.sid, d);
        const f = Array.from(this.trackPublications.values()).find((p) => p.source === d?.source);
        f && d.source !== H.Source.Unknown && this.log.debug("received a second track publication for ".concat(this.identity, " with the same source: ").concat(d.source), Object.assign(Object.assign({}, this.logContext), {
          oldTrack: Oe(f),
          newTrack: Oe(d)
        })), this.addTrackPublication(d);
      }
      t.set(s.sid, d);
    }), this.trackPublications.forEach((s) => {
      t.has(s.trackSid) || (this.log.trace("detected removed track on remote participant, unpublishing", Object.assign(Object.assign({}, this.logContext), Oe(s))), this.unpublishTrack(s.trackSid, !0));
    }), i.forEach((s) => {
      this.emit(X.TrackPublished, s);
    }), !0;
  }
  /** @internal */
  unpublishTrack(e, t) {
    const i = this.trackPublications.get(e);
    if (!i)
      return;
    const s = i.track;
    switch (s && (s.stop(), i.setTrack(void 0)), this.trackPublications.delete(e), i.kind) {
      case H.Kind.Audio:
        this.audioTrackPublications.delete(e);
        break;
      case H.Kind.Video:
        this.videoTrackPublications.delete(e);
        break;
    }
    t && this.emit(X.TrackUnpublished, i);
  }
  /**
   * @internal
   */
  setAudioOutput(e) {
    return R(this, void 0, void 0, function* () {
      this.audioOutput = e;
      const t = [];
      this.audioTrackPublications.forEach((i) => {
        var s;
        ci(i.track) && zh(i.track) && t.push(i.track.setSinkId((s = e.deviceId) !== null && s !== void 0 ? s : "default"));
      }), yield Promise.all(t);
    });
  }
  /** @internal */
  addRemoteDataTrack(e) {
    this.dataTracks.set(e.info.name, e);
  }
  /** @internal */
  removeRemoteDataTrack(e) {
    for (const i of this.dataTracks.entries()) {
      var t = ge(i, 2);
      const s = t[0], c = t[1];
      e === c.info.sid && this.dataTracks.delete(s);
    }
  }
  /** @internal */
  emit(e) {
    for (var t = arguments.length, i = new Array(t > 1 ? t - 1 : 0), s = 1; s < t; s++)
      i[s - 1] = arguments[s];
    return this.log.trace("participant event", Object.assign(Object.assign({}, this.logContext), {
      event: e,
      args: i
    })), super.emit(e, ...i);
  }
}
var Te;
(function(a) {
  a.Disconnected = "disconnected", a.Connecting = "connecting", a.Connected = "connected", a.Reconnecting = "reconnecting", a.SignalReconnecting = "signalReconnecting";
})(Te || (Te = {}));
const fA = 4 * 1e3;
class ps extends sn.EventEmitter {
  get hasE2EESetup() {
    return this.e2eeManager !== void 0;
  }
  /**
   * Creates a new Room, the primary construct for a LiveKit session.
   * @param options
   */
  constructor(e) {
    var t, i, s, c, u, d, h;
    if (super(), t = this, this.state = Te.Disconnected, this.activeSpeakers = [], this.isE2EEEnabled = !1, this.audioEnabled = !0, this.e2eeStateMutex = new Lt(), this.isVideoPlaybackBlocked = !1, this.log = Ee, this.bufferedEvents = [], this.isResuming = !1, this.connect = (f, p, v) => R(this, void 0, void 0, function* () {
      var y;
      if (!uw())
        throw Qn() ? Error("WebRTC isn't detected, have you called registerGlobals?") : Error("LiveKit doesn't seem to be supported on this browser. Try to update your browser and make sure no browser extensions are disabling webRTC.");
      const T = yield this.disconnectLock.lock();
      if (this.state === Te.Connected)
        return this.log.info("already connected to room ".concat(this.name)), T(), Promise.resolve();
      if (this.connectFuture)
        return T(), this.connectFuture.promise;
      this.setAndEmitConnectionState(Te.Connecting), ((y = this.regionUrlProvider) === null || y === void 0 ? void 0 : y.getServerUrl().toString()) !== MS(f) && (this.regionUrl = void 0, this.regionUrlProvider = void 0), fs(new URL(f)) && (this.regionUrlProvider === void 0 ? this.regionUrlProvider = new Ue(f, p) : this.regionUrlProvider.updateToken(p), this.regionUrlProvider.fetchRegionSettings().then((x) => {
        var A;
        (A = this.regionUrlProvider) === null || A === void 0 || A.setServerReportedRegions(x);
      }).catch((x) => {
        this.log.warn("could not fetch region settings", {
          error: x
        });
      }));
      const _ = (x, A, B) => R(this, void 0, void 0, function* () {
        var ie, C;
        this.abortController && this.abortController.abort();
        const E = new AbortController();
        this.abortController = E, T?.();
        try {
          if (yield as.getInstance().getBackOffPromise(f), E.signal.aborted)
            throw ye.cancelled("Connection attempt aborted");
          yield this.attemptConnection(B ?? f, p, v, E), this.abortController = void 0, x();
        } catch (M) {
          if (this.regionUrlProvider && M instanceof ye && M.reason !== We.Cancelled && M.reason !== We.NotAllowed) {
            let N = null;
            try {
              this.log.debug("Fetching next region"), N = yield this.regionUrlProvider.getNextBestRegionUrl((ie = this.abortController) === null || ie === void 0 ? void 0 : ie.signal);
            } catch (G) {
              if (G instanceof ye && (G.status === 401 || G.reason === We.Cancelled)) {
                this.handleDisconnect(this.options.stopLocalTrackOnUnpublish), A(G);
                return;
              }
            }
            // making sure we only register failed attempts on things we actually care about
            [We.InternalError, We.ServerUnreachable, We.Timeout].includes(M.reason) && (this.log.debug("Adding failed connection attempt to back off"), as.getInstance().addFailedConnectionAttempt(f)), N && !(!((C = this.abortController) === null || C === void 0) && C.signal.aborted) ? (this.log.info("Initial connection failed with ConnectionError: ".concat(M.message, ". Retrying with another region: ").concat(N)), this.recreateEngine(!0), yield _(x, A, N)) : (this.handleDisconnect(this.options.stopLocalTrackOnUnpublish, Bb(M)), A(M));
          } else {
            let N = Yn.UNKNOWN_REASON;
            M instanceof ye && (N = Bb(M)), this.handleDisconnect(this.options.stopLocalTrackOnUnpublish, N), A(M);
          }
        }
      }), k = this.regionUrl;
      return this.regionUrl = void 0, this.connectFuture = new wt((x, A) => {
        _(x, A, k);
      }, () => {
        this.clearConnectionFutures();
      }), this.connectFuture.promise;
    }), this.connectSignal = (f, p, v, y, T, _) => R(this, void 0, void 0, function* () {
      var k;
      const x = yield v.join(f, p, {
        autoSubscribe: y.autoSubscribe,
        adaptiveStream: typeof T.adaptiveStream == "object" ? !0 : T.adaptiveStream,
        clientInfoCapabilities: Io((k = T.frameMetadata) !== null && k !== void 0 ? k : T.packetTrailer) || this.e2eeManager ? [yT.CAP_PACKET_TRAILER] : void 0,
        maxRetries: y.maxRetries,
        e2eeEnabled: !!this.e2eeManager,
        websocketTimeout: y.websocketTimeout
      }, _.signal, !T.singlePeerConnection), A = x.joinResponse, B = x.serverInfo;
      if (this.serverInfo = B, !B.version)
        throw new K1("unknown server version");
      return B.version === "0.15.1" && this.options.dynacast && (this.log.debug("disabling dynacast due to server version"), T.dynacast = !1), A;
    }), this.applyJoinResponse = (f) => {
      const p = f.participant;
      if (this.localParticipant.sid = p.sid, this.localParticipant.identity = p.identity, this.localParticipant.setEnabledPublishCodecs(f.enabledPublishCodecs), this.e2eeManager)
        try {
          this.e2eeManager.setSifTrailer(f.sifTrailer);
        } catch (v) {
          this.log.error(v instanceof Error ? v.message : "Could not set SifTrailer", {
            error: v
          });
        }
      this.handleParticipantUpdates([p, ...f.otherParticipants]), f.room && this.handleRoomUpdate(f.room);
    }, this.attemptConnection = (f, p, v, y) => R(this, void 0, void 0, function* () {
      var T, _;
      this.state === Te.Reconnecting || this.isResuming || !((T = this.engine) === null || T === void 0) && T.pendingReconnect ? (this.log.info("Reconnection attempt replaced by new connection attempt"), this.recreateEngine(!0)) : this.maybeCreateEngine(), !((_ = this.regionUrlProvider) === null || _ === void 0) && _.isCloud() && this.engine.setRegionStrategy(this.createRegionStrategy()), this.acquireAudioContext(), this.connOptions = Object.assign(Object.assign({}, Rm), v), this.connOptions.rtcConfig && (this.engine.rtcConfig = this.connOptions.rtcConfig), this.connOptions.peerConnectionTimeout && (this.engine.peerConnectionTimeout = this.connOptions.peerConnectionTimeout);
      try {
        const k = yield this.connectSignal(f, p, this.engine, this.connOptions, this.options, y);
        this.applyJoinResponse(k), this.setupLocalParticipantEvents(), this.emit(j.SignalConnected);
      } catch (k) {
        yield this.engine.close(), this.recreateEngine();
        const x = y.signal.aborted ? ye.cancelled("Signal connection aborted") : ye.serverUnreachable("could not establish signal connection");
        throw k instanceof Error && (x.message = "".concat(x.message, ": ").concat(k.message)), k instanceof ye && (x.reason = k.reason, x.status = k.status), this.log.debug("error trying to establish signal connection", {
          error: k
        }), x;
      }
      if (y.signal.aborted)
        throw yield this.engine.close(), this.recreateEngine(), ye.cancelled("Connection attempt aborted");
      try {
        yield this.engine.waitForPCInitialConnection(this.connOptions.peerConnectionTimeout, y);
      } catch (k) {
        throw yield this.engine.close(), this.recreateEngine(), k;
      }
      an() && this.options.disconnectOnPageLeave && (window.addEventListener("pagehide", this.onPageLeave), window.addEventListener("beforeunload", this.onPageLeave)), an() && window.addEventListener("freeze", this.onPageLeave), this.setAndEmitConnectionState(Te.Connected), this.emit(j.Connected), as.getInstance().resetFailedConnectionAttempts(f), this.registerConnectionReconcile(), this.regionUrlProvider && this.regionUrlProvider.notifyConnected();
    }), this.disconnect = function() {
      for (var f = arguments.length, p = new Array(f), v = 0; v < f; v++)
        p[v] = arguments[v];
      return R(t, [...p], void 0, function() {
        var y = this;
        let T = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0;
        return (function* () {
          var _, k, x;
          const A = yield y.disconnectLock.lock();
          try {
            if (y.state === Te.Disconnected) {
              y.log.debug("already disconnected");
              return;
            }
            if (y.log.info("disconnect from room"), y.state === Te.Connecting || y.state === Te.Reconnecting || y.isResuming) {
              const B = "Abort connection attempt due to user initiated disconnect";
              y.log.warn(B), (_ = y.abortController) === null || _ === void 0 || _.abort(B), (x = (k = y.connectFuture) === null || k === void 0 ? void 0 : k.reject) === null || x === void 0 || x.call(k, ye.cancelled("Client initiated disconnect")), y.connectFuture = void 0;
            }
            y.engine && (y.engine.client.isDisconnected || (yield y.engine.client.sendLeave()), yield y.engine.close()), y.handleDisconnect(T, Yn.CLIENT_INITIATED), y.engine = void 0;
          } finally {
            A();
          }
        })();
      });
    }, this.onPageLeave = () => R(this, void 0, void 0, function* () {
      this.log.info("Page leave detected, disconnecting"), yield this.disconnect();
    }), this.startAudio = () => R(this, void 0, void 0, function* () {
      const f = [], p = Kt();
      if (p && p.os === "iOS") {
        const v = "livekit-dummy-audio-el";
        let y = document.getElementById(v);
        if (!y) {
          y = document.createElement("audio"), y.id = v, y.autoplay = !0, y.hidden = !0;
          const T = $f();
          T.enabled = !0;
          const _ = new MediaStream([T]);
          y.srcObject = _, document.addEventListener("visibilitychange", () => {
            y && (y.srcObject = document.hidden ? null : _, document.hidden || (this.log.debug("page visible again, triggering startAudio to resume playback and update playback status"), this.startAudio()));
          }), document.body.append(y), this.once(j.Disconnected, () => {
            y?.remove(), y = null;
          });
        }
        f.push(y);
      }
      this.remoteParticipants.forEach((v) => {
        v.audioTrackPublications.forEach((y) => {
          y.track && y.track.attachedElements.forEach((T) => {
            f.push(T);
          });
        });
      });
      try {
        yield Promise.all([this.acquireAudioContext(), ...f.map((v) => (v.muted = !1, v.play()))]), this.handleAudioPlaybackStarted();
      } catch (v) {
        throw this.handleAudioPlaybackFailed(v), v;
      }
    }), this.startVideo = () => R(this, void 0, void 0, function* () {
      const f = [];
      for (const p of this.remoteParticipants.values())
        p.videoTrackPublications.forEach((v) => {
          var y;
          (y = v.track) === null || y === void 0 || y.attachedElements.forEach((T) => {
            f.includes(T) || f.push(T);
          });
        });
      yield Promise.all(f.map((p) => p.play())).then(() => {
        this.handleVideoPlaybackStarted();
      }).catch((p) => {
        p.name === "NotAllowedError" ? this.handleVideoPlaybackFailed() : this.log.warn("Resuming video playback failed, make sure you call `startVideo` directly in a user gesture handler");
      });
    }), this.handleRestarting = () => {
      this.clearConnectionReconcile(), this.isResuming = !1;
      for (const f of this.remoteParticipants.values())
        this.handleParticipantDisconnected(f.identity, f);
      this.setAndEmitConnectionState(Te.Reconnecting) && this.emit(j.Reconnecting);
    }, this.handleRestarted = () => {
      this.outgoingDataTrackManager.sfuWillRepublishTracks(), this.incomingDataTrackManager.resendSubscriptionUpdates();
    }, this.handleSignalRestarted = (f) => R(this, void 0, void 0, function* () {
      this.log.debug("signal reconnected to server, region ".concat(f.serverRegion), {
        region: f.serverRegion
      }), this.bufferedEvents = [], this.applyJoinResponse(f);
      try {
        yield this.localParticipant.republishAllTracks(void 0, !0);
      } catch (p) {
        this.log.error("error trying to re-publish tracks after reconnection", {
          error: p
        });
      }
      try {
        yield this.engine.waitForRestarted(), this.log.debug("fully reconnected to server", {
          region: f.serverRegion
        });
      } catch {
        return;
      }
      this.setAndEmitConnectionState(Te.Connected), this.emit(j.Reconnected), this.registerConnectionReconcile(), this.emitBufferedEvents();
    }), this.handleParticipantUpdates = (f) => {
      var p;
      for (const y of f) {
        if (y.identity === this.localParticipant.identity) {
          this.localParticipant.updateInfo(y);
          continue;
        }
        y.identity === "" && (y.identity = (p = this.sidToIdentity.get(y.sid)) !== null && p !== void 0 ? p : "");
        let T = this.remoteParticipants.get(y.identity);
        y.state === $r.DISCONNECTED ? this.handleParticipantDisconnected(y.identity, T) : this.getOrCreateParticipant(y.identity, y);
      }
      const v = new Map(f.filter((y) => y.identity !== this.localParticipant.identity).map((y) => [y.identity, y.dataTracks.map((T) => ql.from(T))]));
      this.incomingDataTrackManager.receiveSfuPublicationUpdates(v);
    }, this.handleActiveSpeakersUpdate = (f) => {
      const p = [], v = {};
      f.forEach((y) => {
        if (v[y.sid] = !0, y.sid === this.localParticipant.sid)
          this.localParticipant.audioLevel = y.level, this.localParticipant.setIsSpeaking(!0), p.push(this.localParticipant);
        else {
          const T = this.getRemoteParticipantBySid(y.sid);
          T && (T.audioLevel = y.level, T.setIsSpeaking(!0), p.push(T));
        }
      }), v[this.localParticipant.sid] || (this.localParticipant.audioLevel = 0, this.localParticipant.setIsSpeaking(!1)), this.remoteParticipants.forEach((y) => {
        v[y.sid] || (y.audioLevel = 0, y.setIsSpeaking(!1));
      }), this.activeSpeakers = p, this.emitWhenConnected(j.ActiveSpeakersChanged, p);
    }, this.handleSpeakersChanged = (f) => {
      const p = /* @__PURE__ */ new Map();
      this.activeSpeakers.forEach((y) => {
        const T = this.remoteParticipants.get(y.identity);
        T && T.sid !== y.sid || p.set(y.sid, y);
      }), f.forEach((y) => {
        let T = this.getRemoteParticipantBySid(y.sid);
        y.sid === this.localParticipant.sid && (T = this.localParticipant), T && (T.audioLevel = y.level, T.setIsSpeaking(y.active), y.active ? p.set(y.sid, T) : p.delete(y.sid));
      });
      const v = Array.from(p.values());
      v.sort((y, T) => T.audioLevel - y.audioLevel), this.activeSpeakers = v, this.emitWhenConnected(j.ActiveSpeakersChanged, v);
    }, this.handleStreamStateUpdate = (f) => {
      f.streamStates.forEach((p) => {
        const v = this.getRemoteParticipantBySid(p.participantSid);
        if (!v)
          return;
        const y = v.getTrackPublicationBySid(p.trackSid);
        if (!y || !y.track)
          return;
        const T = H.streamStateFromProto(p.state);
        y.track.setStreamState(T), T !== y.track.streamState && (v.emit(X.TrackStreamStateChanged, y, y.track.streamState), this.emitWhenConnected(j.TrackStreamStateChanged, y, y.track.streamState, v));
      });
    }, this.handleSubscriptionPermissionUpdate = (f) => {
      const p = this.getRemoteParticipantBySid(f.participantSid);
      if (!p)
        return;
      const v = p.getTrackPublicationBySid(f.trackSid);
      v && v.setAllowed(f.allowed);
    }, this.handleSubscriptionError = (f) => {
      const p = Array.from(this.remoteParticipants.values()).find((y) => y.trackPublications.has(f.trackSid));
      if (!p)
        return;
      const v = p.getTrackPublicationBySid(f.trackSid);
      v && v.setSubscriptionError(f.err);
    }, this.handleDataPacket = (f, p) => {
      const v = this.remoteParticipants.get(f.participantIdentity);
      if (f.value.case === "user")
        this.handleUserPacket(v, f.value.value, f.kind, p);
      else if (f.value.case === "transcription")
        this.handleTranscription(v, f.value.value);
      else if (f.value.case === "sipDtmf")
        this.handleSipDtmf(v, f.value.value);
      else if (f.value.case === "chatMessage")
        this.handleChatMessage(v, f.value.value);
      else if (f.value.case === "metrics")
        this.handleMetrics(f.value.value, v);
      else if (f.value.case === "streamHeader" || f.value.case === "streamChunk" || f.value.case === "streamTrailer")
        this.handleDataStream(f, p);
      else if (f.value.case === "rpcRequest") {
        const y = f.value.value;
        this.rpcServerManager.handleIncomingRpcRequest(f.participantIdentity, y);
      } else if (f.value.case === "rpcResponse") {
        const y = f.value.value;
        switch (y.value.case) {
          case "payload":
            this.rpcClientManager.handleIncomingRpcResponseSuccess(y.requestId, y.value.value);
            break;
          case "error":
            this.rpcClientManager.handleIncomingRpcResponseFailure(y.requestId, Ge.fromProto(y.value.value));
            break;
          default:
            this.log.warn("Unknown rpcResponse.value.case: ".concat(y.value.case), this.logContext);
            break;
        }
      } else f.value.case === "rpcAck" && this.rpcClientManager.handleIncomingRpcAck(f.value.value.requestId);
    }, this.handleUserPacket = (f, p, v, y) => {
      this.emit(j.DataReceived, p.payload, f, v, p.topic, y), f?.emit(X.DataReceived, p.payload, v, y);
    }, this.handleSipDtmf = (f, p) => {
      this.emit(j.SipDTMFReceived, p, f), f?.emit(X.SipDTMFReceived, p);
    }, this.handleTranscription = (f, p) => {
      const v = p.transcribedParticipantIdentity === this.localParticipant.identity ? this.localParticipant : this.getParticipantByIdentity(p.transcribedParticipantIdentity), y = v?.trackPublications.get(p.trackId), T = Tw(p, this.transcriptionReceivedTimes);
      y?.emit(re.TranscriptionReceived, T), v?.emit(X.TranscriptionReceived, T, y), this.emit(j.TranscriptionReceived, T, v, y);
    }, this.handleChatMessage = (f, p) => {
      const v = Sw(p);
      this.emit(j.ChatMessage, v, f);
    }, this.handleMetrics = (f, p) => {
      this.emit(j.MetricsReceived, f, p);
    }, this.handleDataStream = (f, p) => {
      this.incomingDataStreamManager.handleDataStreamPacket(f, p);
    }, this.bufferedSegments = /* @__PURE__ */ new Map(), this.handleAudioPlaybackStarted = () => {
      this.canPlaybackAudio || (this.audioEnabled = !0, this.emit(j.AudioPlaybackStatusChanged, !0));
    }, this.handleAudioPlaybackFailed = (f) => {
      this.log.warn("could not playback audio", {
        error: f
      }), this.canPlaybackAudio && (this.audioEnabled = !1, this.emit(j.AudioPlaybackStatusChanged, !1));
    }, this.handleVideoPlaybackStarted = () => {
      this.isVideoPlaybackBlocked && (this.isVideoPlaybackBlocked = !1, this.emit(j.VideoPlaybackStatusChanged, !0));
    }, this.handleVideoPlaybackFailed = () => {
      this.isVideoPlaybackBlocked || (this.isVideoPlaybackBlocked = !0, this.emit(j.VideoPlaybackStatusChanged, !1));
    }, this.handleDeviceChange = () => R(this, void 0, void 0, function* () {
      var f;
      ((f = Kt()) === null || f === void 0 ? void 0 : f.os) !== "iOS" && (yield this.selectDefaultDevices()), this.emit(j.MediaDevicesChanged);
    }), this.handleRoomUpdate = (f) => {
      const p = this.roomInfo;
      this.roomInfo = f, p && p.metadata !== f.metadata && this.emitWhenConnected(j.RoomMetadataChanged, f.metadata), p?.activeRecording !== f.activeRecording && this.emitWhenConnected(j.RecordingStatusChanged, f.activeRecording);
    }, this.handleConnectionQualityUpdate = (f) => {
      f.updates.forEach((p) => {
        if (p.participantSid === this.localParticipant.sid) {
          this.localParticipant.setConnectionQuality(p.quality);
          return;
        }
        const v = this.getRemoteParticipantBySid(p.participantSid);
        v && v.setConnectionQuality(p.quality);
      });
    }, this.getRemoteParticipantClientProtocol = (f) => {
      var p, v;
      return (v = (p = this.remoteParticipants.get(f)) === null || p === void 0 ? void 0 : p.clientProtocol) !== null && v !== void 0 ? v : yS;
    }, this.onLocalParticipantMetadataChanged = (f) => {
      this.emit(j.ParticipantMetadataChanged, f, this.localParticipant);
    }, this.onLocalParticipantNameChanged = (f) => {
      this.emit(j.ParticipantNameChanged, f, this.localParticipant);
    }, this.onLocalAttributesChanged = (f) => {
      this.emit(j.ParticipantAttributesChanged, f, this.localParticipant);
    }, this.onLocalTrackMuted = (f) => {
      this.emit(j.TrackMuted, f, this.localParticipant);
    }, this.onLocalTrackUnmuted = (f) => {
      this.emit(j.TrackUnmuted, f, this.localParticipant);
    }, this.onTrackProcessorUpdate = (f) => {
      var p;
      (p = f?.onPublish) === null || p === void 0 || p.call(f, this);
    }, this.onLocalTrackPublished = (f) => R(this, void 0, void 0, function* () {
      var p, v, y, T, _, k;
      (p = f.track) === null || p === void 0 || p.on(re.TrackProcessorUpdate, this.onTrackProcessorUpdate), (v = f.track) === null || v === void 0 || v.on(re.Restarted, this.onLocalTrackRestarted), (_ = (T = (y = f.track) === null || y === void 0 ? void 0 : y.getProcessor()) === null || T === void 0 ? void 0 : T.onPublish) === null || _ === void 0 || _.call(T, this), this.emit(j.LocalTrackPublished, f, this.localParticipant), Ai(f.track) && (yield f.track.checkForSilence()) && this.emit(j.LocalAudioSilenceDetected, f);
      const x = yield (k = f.track) === null || k === void 0 ? void 0 : k.getDeviceId(!1), A = Nh(f.source);
      A && x && x !== this.localParticipant.activeDeviceMap.get(A) && (this.localParticipant.activeDeviceMap.set(A, x), this.emit(j.ActiveDeviceChanged, A, x));
    }), this.onLocalTrackUnpublished = (f) => {
      var p, v;
      (p = f.track) === null || p === void 0 || p.off(re.TrackProcessorUpdate, this.onTrackProcessorUpdate), (v = f.track) === null || v === void 0 || v.off(re.Restarted, this.onLocalTrackRestarted), this.emit(j.LocalTrackUnpublished, f, this.localParticipant);
    }, this.onLocalTrackRestarted = (f) => R(this, void 0, void 0, function* () {
      const p = yield f.getDeviceId(!1), v = Nh(f.source);
      v && p && p !== this.localParticipant.activeDeviceMap.get(v) && (this.log.debug("local track restarted, setting ".concat(v, " ").concat(p, " active")), this.localParticipant.activeDeviceMap.set(v, p), this.emit(j.ActiveDeviceChanged, v, p));
    }), this.onLocalConnectionQualityChanged = (f) => {
      this.emit(j.ConnectionQualityChanged, f, this.localParticipant);
    }, this.onMediaDevicesError = (f, p) => {
      this.emit(j.MediaDevicesError, f, p);
    }, this.onLocalParticipantPermissionsChanged = (f) => {
      this.emit(j.ParticipantPermissionsChanged, f, this.localParticipant);
    }, this.onLocalChatMessageSent = (f) => {
      this.emit(j.ChatMessage, f, this.localParticipant);
    }, this.setMaxListeners(100), this.remoteParticipants = /* @__PURE__ */ new Map(), this.sidToIdentity = /* @__PURE__ */ new Map(), this.options = Object.assign(Object.assign({}, mM), e), this.log = rn((i = this.options.loggerName) !== null && i !== void 0 ? i : Mt.Room, () => this.logContext), this.transcriptionReceivedTimes = /* @__PURE__ */ new Map(), this.options.audioCaptureDefaults = Object.assign(Object.assign({}, DS), e?.audioCaptureDefaults), this.options.videoCaptureDefaults = Object.assign(Object.assign({}, PS), e?.videoCaptureDefaults), this.options.publishDefaults = Object.assign(Object.assign({}, hM), e?.publishDefaults), this.maybeCreateEngine(), this.incomingDataStreamManager = new HM(), this.outgoingDataStreamManager = new KM(this.engine, this.log), this.incomingDataTrackManager = new tA({
      e2eeManager: this.e2eeManager
    }), this.incomingDataTrackManager.on("sfuUpdateSubscription", (f) => {
      this.engine.client.sendUpdateDataSubscription(f.sid, f.subscribe);
    }).on("trackPublished", (f) => {
      var p;
      f.track.publisherIdentity !== this.localParticipant.identity && (this.emit(j.DataTrackPublished, f.track), (p = this.remoteParticipants.get(f.track.publisherIdentity)) === null || p === void 0 || p.addRemoteDataTrack(f.track));
    }).on("trackUnpublished", (f) => {
      var p;
      f.publisherIdentity !== this.localParticipant.identity && (this.emit(j.DataTrackUnpublished, f.sid), (p = this.remoteParticipants.get(f.publisherIdentity)) === null || p === void 0 || p.removeRemoteDataTrack(f.sid));
    }), this.outgoingDataTrackManager = new Am({
      e2eeManager: this.e2eeManager
    }), this.outgoingDataTrackManager.on("sfuPublishRequest", (f) => {
      this.engine.client.sendPublishDataTrackRequest(f.handle, f.name, f.usesE2ee);
    }).on("sfuUnpublishRequest", (f) => {
      this.engine.client.sendUnPublishDataTrackRequest(f.handle);
    }).on("trackPublished", (f) => {
      this.emit(j.LocalDataTrackPublished, f.track);
    }).on("trackUnpublished", (f) => {
      this.emit(j.LocalDataTrackUnpublished, f.sid);
    }).on("packetAvailable", (f) => {
      let p = f.handle, v = f.bytes;
      this.engine.sendLossyBytes(v, _e.DATA_TRACK_LOSSY, "wait").finally(() => this.outgoingDataTrackManager.handlePacketSendComplete(p));
    }), this.registerRpcDataStreamHandler(), this.rpcClientManager = new aA(this.log, this.outgoingDataStreamManager, this.getRemoteParticipantClientProtocol, () => {
      var f, p;
      return (p = (f = this.engine.latestJoinResponse) === null || f === void 0 ? void 0 : f.serverInfo) === null || p === void 0 ? void 0 : p.version;
    }), this.rpcClientManager.on("sendDataPacket", (f) => {
      let p = f.packet;
      var v;
      (v = this.engine) === null || v === void 0 || v.sendDataPacket(p, _e.RELIABLE);
    }), this.rpcServerManager = new rA(this.log, this.outgoingDataStreamManager, this.getRemoteParticipantClientProtocol), this.rpcServerManager.on("sendDataPacket", (f) => {
      let p = f.packet;
      var v;
      (v = this.engine) === null || v === void 0 || v.sendDataPacket(p, _e.RELIABLE);
    }), this.disconnectLock = new Lt(), this.localParticipant = new uA("", "", this.engine, this.options, this.outgoingDataStreamManager, this.outgoingDataTrackManager, this.rpcClientManager, this.rpcServerManager), this.setupFrameMetadata(), (this.options.e2ee || this.options.encryption) && this.setupE2EE(), this.engine.e2eeManager = this.e2eeManager, this.incomingDataTrackManager.updateE2eeManager((s = this.e2eeManager) !== null && s !== void 0 ? s : null), this.outgoingDataTrackManager.updateE2eeManager((c = this.e2eeManager) !== null && c !== void 0 ? c : null), this.options.videoCaptureDefaults.deviceId && this.localParticipant.activeDeviceMap.set("videoinput", tr(this.options.videoCaptureDefaults.deviceId)), this.options.audioCaptureDefaults.deviceId && this.localParticipant.activeDeviceMap.set("audioinput", tr(this.options.audioCaptureDefaults.deviceId)), !((u = this.options.audioOutput) === null || u === void 0) && u.deviceId && this.switchActiveDevice("audiooutput", tr(this.options.audioOutput.deviceId)).catch((f) => this.log.warn("Could not set audio output: ".concat(f.message))), an()) {
      const f = new AbortController();
      let p;
      if (ps.cleanupRegistry) {
        const v = new WeakRef(this);
        p = () => {
          const y = v.deref();
          y && y.handleDeviceChange();
        }, ps.cleanupRegistry.register(this, () => {
          f.abort();
        });
      } else
        p = this.handleDeviceChange;
      (h = (d = navigator.mediaDevices) === null || d === void 0 ? void 0 : d.addEventListener) === null || h === void 0 || h.call(d, "devicechange", p, {
        signal: f.signal
      });
    }
  }
  registerTextStreamHandler(e, t) {
    return this.incomingDataStreamManager.registerTextStreamHandler(e, t);
  }
  unregisterTextStreamHandler(e) {
    return this.incomingDataStreamManager.unregisterTextStreamHandler(e);
  }
  registerByteStreamHandler(e, t) {
    return this.incomingDataStreamManager.registerByteStreamHandler(e, t);
  }
  unregisterByteStreamHandler(e) {
    return this.incomingDataStreamManager.unregisterByteStreamHandler(e);
  }
  /**
   * Establishes the participant as a receiver for calls of the specified RPC method.
   *
   * @param method - The name of the indicated RPC method
   * @param handler - Will be invoked when an RPC request for this method is received
   * @returns A promise that resolves when the method is successfully registered
   * @throws {Error} If a handler for this method is already registered (must call unregisterRpcMethod first)
   *
   * @example
   * ```typescript
   * room.localParticipant?.registerRpcMethod(
   *   'greet',
   *   async (data: RpcInvocationData) => {
   *     console.log(`Received greeting from ${data.callerIdentity}: ${data.payload}`);
   *     return `Hello, ${data.callerIdentity}!`;
   *   }
   * );
   * ```
   *
   * The handler should return a Promise that resolves to a string.
   * If unable to respond within `responseTimeout`, the request will result in an error on the caller's side.
   *
   * You may throw errors of type `RpcError` with a string `message` in the handler,
   * and they will be received on the caller's side with the message intact.
   * Other errors thrown in your handler will not be transmitted as-is, and will instead arrive to the caller as `1500` ("Application Error").
   */
  registerRpcMethod(e, t) {
    this.rpcServerManager.registerRpcMethod(e, t);
  }
  /**
   * Unregisters a previously registered RPC method.
   *
   * @param method - The name of the RPC method to unregister
   */
  unregisterRpcMethod(e) {
    this.rpcServerManager.unregisterRpcMethod(e);
  }
  /**
   * @experimental
   */
  setE2EEEnabled(e) {
    return R(this, void 0, void 0, function* () {
      const t = yield this.e2eeStateMutex.lock();
      try {
        if (this.e2eeManager)
          this.isE2EEEnabled !== e && (yield this.localParticipant.setE2EEEnabled(e), this.localParticipant.identity !== "" && this.e2eeManager.setParticipantCryptorEnabled(e, this.localParticipant.identity));
        else
          throw Error("e2ee not configured, please set e2ee settings within the room options");
      } finally {
        t();
      }
    });
  }
  setupE2EE() {
    var e, t;
    const i = !!this.options.encryption, s = this.options.encryption || this.options.e2ee;
    s && ("e2eeManager" in s ? (this.e2eeManager = s.e2eeManager, this.e2eeManager.isDataChannelEncryptionEnabled = i) : this.e2eeManager = new Vw(s, i), this.e2eeManager.on(pa.ParticipantEncryptionStatusChanged, (c, u) => {
      Cw(u) && (this.isE2EEEnabled = c), this.emit(j.ParticipantEncryptionStatusChanged, c, u);
    }), this.e2eeManager.on(pa.EncryptionError, (c, u) => {
      const d = u ? this.getParticipantByIdentity(u) : void 0;
      this.emit(j.EncryptionError, c, d);
    }), (e = this.e2eeManager) === null || e === void 0 || e.setup(this), (t = this.e2eeManager) === null || t === void 0 || t.setupEngine(this.engine));
  }
  setupFrameMetadata() {
    var e;
    const t = (e = this.options.frameMetadata) !== null && e !== void 0 ? e : this.options.packetTrailer;
    this.frameMetadataManager = new Yw(t), this.frameMetadataManager.setup(this);
  }
  get logContext() {
    var e, t, i;
    return {
      room: this.name,
      roomID: (e = this.roomInfo) === null || e === void 0 ? void 0 : e.sid,
      participant: (t = this.localParticipant) === null || t === void 0 ? void 0 : t.identity,
      participantID: (i = this.localParticipant) === null || i === void 0 ? void 0 : i.sid
    };
  }
  /**
   * if the current room has a participant with `recorder: true` in its JWT grant
   **/
  get isRecording() {
    var e, t;
    return (t = (e = this.roomInfo) === null || e === void 0 ? void 0 : e.activeRecording) !== null && t !== void 0 ? t : !1;
  }
  /**
   * server assigned unique room id.
   * returns once a sid has been issued by the server.
   */
  getSid() {
    return this.state === Te.Disconnected ? Gt.resolve("") : this.roomInfo && this.roomInfo.sid !== "" ? Gt.resolve(this.roomInfo.sid) : new Gt((e, t) => {
      const i = (s) => {
        s.sid !== "" && (this.engine.off(ae.RoomUpdate, i), e(s.sid));
      };
      this.engine.on(ae.RoomUpdate, i), this.once(j.Disconnected, () => {
        this.engine.off(ae.RoomUpdate, i), t(new rt("Room disconnected before room server id was available"));
      });
    });
  }
  /** user assigned name, derived from JWT token */
  get name() {
    var e, t;
    return (t = (e = this.roomInfo) === null || e === void 0 ? void 0 : e.name) !== null && t !== void 0 ? t : "";
  }
  /** room metadata */
  get metadata() {
    var e;
    return (e = this.roomInfo) === null || e === void 0 ? void 0 : e.metadata;
  }
  get numParticipants() {
    var e, t;
    return (t = (e = this.roomInfo) === null || e === void 0 ? void 0 : e.numParticipants) !== null && t !== void 0 ? t : 0;
  }
  get numPublishers() {
    var e, t;
    return (t = (e = this.roomInfo) === null || e === void 0 ? void 0 : e.numPublishers) !== null && t !== void 0 ? t : 0;
  }
  maybeCreateEngine() {
    this.engine && (this.engine.isNewlyCreated || !this.engine.isClosed) || (this.engine = new BM(this.options), this.engine.e2eeManager = this.e2eeManager, this.engine.on(ae.ParticipantUpdate, this.handleParticipantUpdates).on(ae.RoomUpdate, this.handleRoomUpdate).on(ae.SpeakersChanged, this.handleSpeakersChanged).on(ae.StreamStateChanged, this.handleStreamStateUpdate).on(ae.ConnectionQualityUpdate, this.handleConnectionQualityUpdate).on(ae.SubscriptionError, this.handleSubscriptionError).on(ae.SubscriptionPermissionUpdate, this.handleSubscriptionPermissionUpdate).on(ae.MediaTrackAdded, (e, t, i) => {
      this.onTrackAdded(e, t, i);
    }).on(ae.Disconnected, (e) => {
      this.handleDisconnect(this.options.stopLocalTrackOnUnpublish, e);
    }).on(ae.ActiveSpeakersUpdate, this.handleActiveSpeakersUpdate).on(ae.DataPacketReceived, this.handleDataPacket).on(ae.Resuming, () => {
      this.clearConnectionReconcile(), this.isResuming = !0, this.log.debug("Resuming signal connection"), this.setAndEmitConnectionState(Te.SignalReconnecting) && this.emit(j.SignalReconnecting);
    }).on(ae.Resumed, () => {
      this.registerConnectionReconcile(), this.isResuming = !1, this.log.debug("Resumed signal connection"), this.updateSubscriptions(), this.emitBufferedEvents(), this.setAndEmitConnectionState(Te.Connected) && this.emit(j.Reconnected);
    }).on(ae.SignalResumed, () => {
      this.bufferedEvents = [], (this.state === Te.Reconnecting || this.isResuming) && this.sendSyncState();
    }).on(ae.Restarting, this.handleRestarting).on(ae.Restarted, this.handleRestarted).on(ae.SignalRestarted, this.handleSignalRestarted).on(ae.Offline, () => {
      this.setAndEmitConnectionState(Te.Reconnecting) && this.emit(j.Reconnecting);
    }).on(ae.DCBufferStatusChanged, (e, t) => {
      this.emit(j.DCBufferStatusChanged, e, t);
    }).on(ae.LocalTrackSubscribed, (e) => {
      this.handleLocalTrackSubscribed(e);
    }).on(ae.RoomMoved, (e) => {
      this.log.debug("room moved", e), e.room && this.handleRoomUpdate(e.room), this.remoteParticipants.forEach((t, i) => {
        this.handleParticipantDisconnected(i, t);
      }), this.emit(j.Moved, e.room.name), e.participant ? this.handleParticipantUpdates([e.participant, ...e.otherParticipants]) : this.handleParticipantUpdates(e.otherParticipants);
    }).on(ae.PublishDataTrackResponse, (e) => {
      if (!e.info) {
        this.log.warn("received PublishDataTrackResponse, but event.info was ".concat(e.info, ", so skipping."));
        return;
      }
      this.outgoingDataTrackManager.receivedSfuPublishResponse(e.info.pubHandle, {
        type: "ok",
        data: {
          sid: e.info.sid,
          pubHandle: e.info.pubHandle,
          name: e.info.name,
          usesE2ee: e.info.encryption !== $e.NONE
        }
      });
    }).on(ae.UnPublishDataTrackResponse, (e) => {
      if (!e.info) {
        this.log.warn("received UnPublishDataTrackResponse, but event.info was ".concat(e.info, ", so skipping."));
        return;
      }
      this.outgoingDataTrackManager.receivedSfuUnpublishResponse(e.info.pubHandle);
    }).on(ae.DataTrackSubscriberHandles, (e) => {
      const t = new Map(Object.entries(e.subHandles).map((i) => {
        let s = ge(i, 2), c = s[0], u = s[1];
        return [parseInt(c, 10), u.trackSid];
      }));
      this.incomingDataTrackManager.receivedSfuSubscriberHandles(t);
    }).on(ae.DataTrackPacketReceived, (e) => {
      try {
        this.incomingDataTrackManager.packetReceived(e);
      } catch (t) {
        throw t;
      }
    }).on(ae.Joined, (e) => {
      const t = new Map(e.otherParticipants.map((i) => [i.identity, i.dataTracks.map((s) => ql.from(s))]));
      this.incomingDataTrackManager.receiveSfuPublicationUpdates(t);
    }).on(ae.TokenRefreshed, (e) => {
      var t;
      (t = this.regionUrlProvider) === null || t === void 0 || t.updateToken(e);
    }).on(ae.ServerRegionsReported, (e) => {
      var t;
      (t = this.regionUrlProvider) === null || t === void 0 || t.setServerReportedRegions({
        regionSettings: e,
        updatedAtInMs: Date.now(),
        maxAgeInMs: Hh
      });
    }), this.localParticipant && this.localParticipant.setupEngine(this.engine), this.e2eeManager && this.e2eeManager.setupEngine(this.engine), this.outgoingDataStreamManager && this.outgoingDataStreamManager.setupEngine(this.engine));
  }
  createRegionStrategy() {
    return {
      getNextUrl: (e) => R(this, void 0, void 0, function* () {
        return this.regionUrlProvider ? this.regionUrlProvider.getNextBestRegionUrl(e) : null;
      }),
      resetAttempts: () => {
        var e;
        return (e = this.regionUrlProvider) === null || e === void 0 ? void 0 : e.resetAttempts();
      }
    };
  }
  /**
   * getLocalDevices abstracts navigator.mediaDevices.enumerateDevices.
   * In particular, it requests device permissions by default if needed
   * and makes sure the returned device does not consist of dummy devices
   * @param kind
   * @returns a list of available local devices
   */
  static getLocalDevices(e) {
    let t = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : !0;
    return Rt.getInstance().getDevices(e, t);
  }
  /**
   * prepareConnection should be called as soon as the page is loaded, in order
   * to speed up the connection attempt. This function will
   * - perform DNS resolution and pre-warm the DNS cache
   * - establish TLS connection and cache TLS keys
   *
   * With LiveKit Cloud, it will also determine the best edge data center for
   * the current client to connect to if a token is provided.
   */
  prepareConnection(e, t) {
    return R(this, void 0, void 0, function* () {
      if (this.state === Te.Disconnected) {
        this.log.debug("prepareConnection to ".concat(e));
        try {
          if (fs(new URL(e)) && t) {
            this.regionUrlProvider = new Ue(e, t);
            const i = yield this.regionUrlProvider.getNextBestRegionUrl();
            i && this.state === Te.Disconnected && (this.regionUrl = i, yield fetch(xo(i), {
              method: "HEAD"
            }), this.log.debug("prepared connection to ".concat(i)));
          } else
            yield fetch(xo(e), {
              method: "HEAD"
            });
        } catch (i) {
          this.log.warn("could not prepare connection", {
            error: i
          });
        }
      }
    });
  }
  /**
   * retrieves a participant by identity
   * @param identity
   * @returns
   */
  getParticipantByIdentity(e) {
    return this.localParticipant.identity === e ? this.localParticipant : this.remoteParticipants.get(e);
  }
  clearConnectionFutures() {
    this.connectFuture = void 0;
  }
  /**
   * @internal for testing
   */
  simulateScenario(e, t) {
    return R(this, void 0, void 0, function* () {
      let i = () => R(this, void 0, void 0, function* () {
      }), s;
      switch (e) {
        case "signal-reconnect":
          yield this.engine.client.handleOnClose("simulate disconnect");
          break;
        case "fail-on-v1-path":
          this.engine.failNextV1Path();
          break;
        case "speaker":
          s = new ri({
            scenario: {
              case: "speakerUpdate",
              value: 3
            }
          });
          break;
        case "node-failure":
          s = new ri({
            scenario: {
              case: "nodeFailure",
              value: !0
            }
          });
          break;
        case "server-leave":
          s = new ri({
            scenario: {
              case: "serverLeave",
              value: !0
            }
          });
          break;
        case "migration":
          s = new ri({
            scenario: {
              case: "migration",
              value: !0
            }
          });
          break;
        case "resume-reconnect":
          this.engine.failNext(), yield this.engine.client.handleOnClose("simulate resume-disconnect");
          break;
        case "disconnect-signal-on-resume":
          i = () => R(this, void 0, void 0, function* () {
            yield this.engine.client.handleOnClose("simulate resume-disconnect");
          }), s = new ri({
            scenario: {
              case: "disconnectSignalOnResume",
              value: !0
            }
          });
          break;
        case "disconnect-signal-on-resume-no-messages":
          i = () => R(this, void 0, void 0, function* () {
            yield this.engine.client.handleOnClose("simulate resume-disconnect");
          }), s = new ri({
            scenario: {
              case: "disconnectSignalOnResumeNoMessages",
              value: !0
            }
          });
          break;
        case "full-reconnect":
          this.engine.fullReconnectOnNext = !0, yield this.engine.client.handleOnClose("simulate full-reconnect");
          break;
        case "force-tcp":
        case "force-tls":
          s = new ri({
            scenario: {
              case: "switchCandidateProtocol",
              value: e === "force-tls" ? 2 : 1
            }
          }), i = () => R(this, void 0, void 0, function* () {
            const c = this.engine.client.onLeave;
            c && c(new lu({
              reason: Yn.CLIENT_INITIATED,
              action: es.RECONNECT
            }));
          });
          break;
        case "subscriber-bandwidth":
          if (t === void 0 || typeof t != "number")
            throw new Error("subscriber-bandwidth requires a number as argument");
          s = new ri({
            scenario: {
              case: "subscriberBandwidth",
              value: Ya(t)
            }
          });
          break;
        case "leave-full-reconnect":
          s = new ri({
            scenario: {
              case: "leaveRequestFullReconnect",
              value: !0
            }
          });
      }
      s && (yield this.engine.client.sendSimulateScenario(s), yield i());
    });
  }
  /**
   * Returns true if audio playback is enabled
   */
  get canPlaybackAudio() {
    return this.audioEnabled;
  }
  /**
   * Returns true if video playback is enabled
   */
  get canPlaybackVideo() {
    return !this.isVideoPlaybackBlocked;
  }
  getActiveDevice(e) {
    return this.localParticipant.activeDeviceMap.get(e);
  }
  /**
   * Switches all active devices used in this room to the given device.
   *
   * Note: setting AudioOutput is not supported on some browsers. See [setSinkId](https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/setSinkId#browser_compatibility)
   *
   * @param kind use `videoinput` for camera track,
   *  `audioinput` for microphone track,
   *  `audiooutput` to set speaker for all incoming audio tracks
   * @param deviceId
   */
  switchActiveDevice(e, t) {
    return R(this, arguments, void 0, function(i, s) {
      var c = this;
      let u = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : !0;
      return (function* () {
        var d, h, f, p, v, y, T;
        let _ = !0, k = !1;
        const x = u ? {
          exact: s
        } : s;
        if (i === "audioinput") {
          k = c.localParticipant.audioTrackPublications.size === 0;
          const A = (d = c.getActiveDevice(i)) !== null && d !== void 0 ? d : c.options.audioCaptureDefaults.deviceId;
          c.options.audioCaptureDefaults.deviceId = x;
          const B = Array.from(c.localParticipant.audioTrackPublications.values()).filter((C) => C.source === H.Source.Microphone);
          try {
            _ = (yield Promise.all(B.map((C) => {
              var E;
              return (E = C.audioTrack) === null || E === void 0 ? void 0 : E.setDeviceId(x);
            }))).every((C) => C === !0);
          } catch (C) {
            throw c.options.audioCaptureDefaults.deviceId = A, C;
          }
          const ie = B.some((C) => {
            var E, M;
            return (M = (E = C.track) === null || E === void 0 ? void 0 : E.isMuted) !== null && M !== void 0 ? M : !1;
          });
          _ && ie && (k = !0);
        } else if (i === "videoinput") {
          k = c.localParticipant.videoTrackPublications.size === 0;
          const A = (h = c.getActiveDevice(i)) !== null && h !== void 0 ? h : c.options.videoCaptureDefaults.deviceId;
          c.options.videoCaptureDefaults.deviceId = x;
          const B = Array.from(c.localParticipant.videoTrackPublications.values()).filter((C) => C.source === H.Source.Camera);
          try {
            _ = (yield Promise.all(B.map((C) => {
              var E;
              return (E = C.videoTrack) === null || E === void 0 ? void 0 : E.setDeviceId(x);
            }))).every((C) => C === !0);
          } catch (C) {
            throw c.options.videoCaptureDefaults.deviceId = A, C;
          }
          const ie = B.some((C) => {
            var E, M;
            return (M = (E = C.track) === null || E === void 0 ? void 0 : E.isMuted) !== null && M !== void 0 ? M : !1;
          });
          _ && ie && (k = !0);
        } else if (i === "audiooutput") {
          if (k = !0, !Uh() && !c.options.webAudioMix || c.options.webAudioMix && c.audioContext && !("setSinkId" in c.audioContext))
            throw new Error("cannot switch audio output, the current browser does not support it");
          c.options.webAudioMix && (s = (f = yield Rt.getInstance().normalizeDeviceId("audiooutput", s)) !== null && f !== void 0 ? f : ""), (p = (T = c.options).audioOutput) !== null && p !== void 0 || (T.audioOutput = {});
          const A = (v = c.getActiveDevice(i)) !== null && v !== void 0 ? v : c.options.audioOutput.deviceId;
          c.options.audioOutput.deviceId = s;
          try {
            c.options.webAudioMix && ((y = c.audioContext) === null || y === void 0 || y.setSinkId(s)), yield Promise.all(Array.from(c.remoteParticipants.values()).map((B) => B.setAudioOutput({
              deviceId: s
            })));
          } catch (B) {
            throw c.options.audioOutput.deviceId = A, B;
          }
        }
        return k && (c.localParticipant.activeDeviceMap.set(i, s), c.emit(j.ActiveDeviceChanged, i, s)), _;
      })();
    });
  }
  setupLocalParticipantEvents() {
    this.localParticipant.on(X.ParticipantMetadataChanged, this.onLocalParticipantMetadataChanged).on(X.ParticipantNameChanged, this.onLocalParticipantNameChanged).on(X.AttributesChanged, this.onLocalAttributesChanged).on(X.TrackMuted, this.onLocalTrackMuted).on(X.TrackUnmuted, this.onLocalTrackUnmuted).on(X.LocalTrackPublished, this.onLocalTrackPublished).on(X.LocalTrackUnpublished, this.onLocalTrackUnpublished).on(X.ConnectionQualityChanged, this.onLocalConnectionQualityChanged).on(X.MediaDevicesError, this.onMediaDevicesError).on(X.AudioStreamAcquired, this.startAudio).on(X.ChatMessage, this.onLocalChatMessageSent).on(X.ParticipantPermissionsChanged, this.onLocalParticipantPermissionsChanged);
  }
  recreateEngine(e) {
    const t = this.engine;
    e && t && !t.client.isDisconnected ? t.client.sendLeave().finally(() => t.close()) : t?.close(), this.engine = void 0, this.isResuming = !1, this.remoteParticipants.clear(), this.sidToIdentity.clear(), this.bufferedEvents = [], this.maybeCreateEngine();
  }
  onTrackAdded(e, t, i) {
    if (this.state === Te.Connecting || this.state === Te.Reconnecting) {
      const v = () => {
        this.log.debug("deferring on track for later", {
          mediaTrackId: e.id,
          mediaStreamId: t.id,
          tracksInStream: t.getTracks().map((T) => T.id)
        }), this.onTrackAdded(e, t, i), y();
      }, y = () => {
        this.off(j.Reconnected, v), this.off(j.Connected, v), this.off(j.Disconnected, y);
      };
      this.once(j.Reconnected, v), this.once(j.Connected, v), this.once(j.Disconnected, y);
      return;
    }
    if (this.state === Te.Disconnected) {
      this.log.warn("skipping incoming track after Room disconnected");
      return;
    }
    if (e.readyState === "ended") {
      this.log.debug("skipping incoming track as it already ended");
      return;
    }
    const s = ow(t.id), c = s[0];
    let u = s[1], d = e.id;
    if (u && u.startsWith("TR") && (d = u), c === this.localParticipant.sid) {
      this.log.warn("tried to create RemoteParticipant for local participant");
      return;
    }
    const h = Array.from(this.remoteParticipants.values()).find((v) => v.sid === c);
    if (!h) {
      c.startsWith("PA") && this.log.error("Tried to add a track for a participant, that's not present. Sid: ".concat(c));
      return;
    }
    if (!d.startsWith("TR")) {
      const v = this.engine.getTrackIdForReceiver(i);
      if (!v) {
        this.log.error("Tried to add a track whose 'sid' could not be found for a participant, that's not present. Sid: ".concat(c));
        return;
      }
      d = v;
    }
    d.startsWith("TR") || this.log.warn("Tried to add a track whose 'sid' could not be determined for a participant, that's not present. Sid: ".concat(c, ", streamId: ").concat(u, ", trackId: ").concat(d), {
      remoteParticipantID: c,
      streamId: u,
      trackId: d
    });
    let f;
    this.options.adaptiveStream && (typeof this.options.adaptiveStream == "object" ? f = this.options.adaptiveStream : f = {});
    const p = h.addSubscribedMediaTrack(e, d, t, i, f);
    p?.isEncrypted && !this.e2eeManager && this.emit(j.EncryptionError, new Error("Encrypted ".concat(p.source, " track received from participant ").concat(h.sid, ", but room does not have encryption enabled!")));
  }
  handleLocalTrackSubscribed(e) {
    const t = () => this.localParticipant.getTrackPublications().find((h) => h.trackSid === e), i = t();
    if (i) {
      this.emitLocalTrackSubscribed(i);
      return;
    }
    this.log.debug("deferring LocalTrackSubscribed, publication not yet available", {
      subscribedSid: e
    });
    const s = 1e4;
    let c;
    const u = (h) => {
      h.trackSid === e && (d(), this.emitLocalTrackSubscribed(h));
    }, d = () => {
      clearTimeout(c), this.localParticipant.off(X.LocalTrackPublished, u), this.off(j.Disconnected, d);
    };
    this.localParticipant.on(X.LocalTrackPublished, u), this.once(j.Disconnected, d), c = setTimeout(() => {
      d();
      const h = t();
      h ? this.emitLocalTrackSubscribed(h) : this.log.warn("could not find local track publication for LocalTrackSubscribed event after timeout", {
        subscribedSid: e
      });
    }, s);
  }
  emitLocalTrackSubscribed(e) {
    this.localParticipant.emit(X.LocalTrackSubscribed, e), this.emitWhenConnected(j.LocalTrackSubscribed, e, this.localParticipant);
  }
  handleDisconnect() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : !0, t = arguments.length > 1 ? arguments[1] : void 0;
    var i, s;
    if (this.clearConnectionReconcile(), this.isResuming = !1, this.bufferedEvents = [], this.transcriptionReceivedTimes.clear(), this.incomingDataStreamManager.clearControllers(), this.incomingDataTrackManager.reset(), this.outgoingDataTrackManager.reset(), this.state !== Te.Disconnected) {
      this.regionUrl = void 0, this.regionUrlProvider && this.regionUrlProvider.notifyDisconnected();
      try {
        this.remoteParticipants.forEach((c) => {
          c.trackPublications.forEach((u) => {
            c.unpublishTrack(u.trackSid);
          });
        }), this.localParticipant.trackPublications.forEach((c) => {
          var u, d, h;
          c.track && this.localParticipant.unpublishTrack(c.track, e), e ? ((u = c.track) === null || u === void 0 || u.detach(), (d = c.track) === null || d === void 0 || d.stop()) : (h = c.track) === null || h === void 0 || h.stopMonitor();
        }), this.localParticipant.off(X.ParticipantMetadataChanged, this.onLocalParticipantMetadataChanged).off(X.ParticipantNameChanged, this.onLocalParticipantNameChanged).off(X.AttributesChanged, this.onLocalAttributesChanged).off(X.TrackMuted, this.onLocalTrackMuted).off(X.TrackUnmuted, this.onLocalTrackUnmuted).off(X.LocalTrackPublished, this.onLocalTrackPublished).off(X.LocalTrackUnpublished, this.onLocalTrackUnpublished).off(X.ConnectionQualityChanged, this.onLocalConnectionQualityChanged).off(X.MediaDevicesError, this.onMediaDevicesError).off(X.AudioStreamAcquired, this.startAudio).off(X.ChatMessage, this.onLocalChatMessageSent).off(X.ParticipantPermissionsChanged, this.onLocalParticipantPermissionsChanged), this.localParticipant.trackPublications.clear(), this.localParticipant.videoTrackPublications.clear(), this.localParticipant.audioTrackPublications.clear(), this.remoteParticipants.clear(), this.sidToIdentity.clear(), this.activeSpeakers = [], this.audioContext && typeof this.options.webAudioMix == "boolean" && (this.audioContext.close(), this.audioContext = void 0), an() && (window.removeEventListener("beforeunload", this.onPageLeave), window.removeEventListener("pagehide", this.onPageLeave), window.removeEventListener("freeze", this.onPageLeave), (s = (i = navigator.mediaDevices) === null || i === void 0 ? void 0 : i.removeEventListener) === null || s === void 0 || s.call(i, "devicechange", this.handleDeviceChange));
      } finally {
        this.setAndEmitConnectionState(Te.Disconnected), this.emit(j.Disconnected, t);
      }
    }
  }
  handleParticipantDisconnected(e, t) {
    this.remoteParticipants.delete(e), t && (this.incomingDataStreamManager.validateParticipantHasNoActiveDataStreams(e), this.incomingDataTrackManager.handleRemoteParticipantDisconnected(e), t.trackPublications.forEach((i) => {
      t.unpublishTrack(i.trackSid, !0);
    }), this.emit(j.ParticipantDisconnected, t), t.setDisconnected(), this.rpcClientManager.handleParticipantDisconnected(t.identity));
  }
  /**
   * attempt to select the default devices if the previously selected devices are no longer available after a device change event
   */
  selectDefaultDevices() {
    return R(this, void 0, void 0, function* () {
      var e, t, i;
      const s = Rt.getInstance().previousDevices, c = yield Rt.getInstance().getDevices(void 0, !1), u = Kt();
      if (u?.name === "Chrome" && u.os !== "iOS")
        for (let h of c) {
          const f = s.find((p) => p.deviceId === h.deviceId);
          f && f.label !== "" && f.kind === h.kind && f.label !== h.label && this.getActiveDevice(h.kind) === "default" && this.emit(j.ActiveDeviceChanged, h.kind, h.deviceId);
        }
      const d = ["audiooutput", "audioinput", "videoinput"];
      for (let h of d) {
        const f = W1(h), p = this.localParticipant.getTrackPublication(f);
        if (p && (!((e = p.track) === null || e === void 0) && e.isUserProvided))
          continue;
        const v = c.filter((T) => T.kind === h), y = this.getActiveDevice(h);
        if (y === ((t = s.filter((T) => T.kind === h)[0]) === null || t === void 0 ? void 0 : t.deviceId) && v.length > 0 && ((i = v[0]) === null || i === void 0 ? void 0 : i.deviceId) !== y) {
          yield this.switchActiveDevice(h, v[0].deviceId);
          continue;
        }
        h === "audioinput" && !Po() || h === "videoinput" || v.length > 0 && !v.find((T) => T.deviceId === this.getActiveDevice(h)) && // avoid switching audio output on safari without explicit user action as it leads to slowed down audio playback
        (h !== "audiooutput" || !Po()) && (yield this.switchActiveDevice(h, v[0].deviceId));
      }
    });
  }
  acquireAudioContext() {
    return R(this, void 0, void 0, function* () {
      var e, t;
      if (typeof this.options.webAudioMix != "boolean" && this.options.webAudioMix.audioContext ? this.audioContext = this.options.webAudioMix.audioContext : (!this.audioContext || this.audioContext.state === "closed") && (this.audioContext = (e = kS()) !== null && e !== void 0 ? e : void 0), this.options.webAudioMix && this.remoteParticipants.forEach((s) => s.setAudioContext(this.audioContext)), this.localParticipant.setAudioContext(this.audioContext), this.audioContext && this.audioContext.state === "suspended")
        try {
          yield Promise.race([this.audioContext.resume(), zt(200)]);
        } catch (s) {
          this.log.warn("Could not resume audio context", {
            error: s
          });
        }
      const i = ((t = this.audioContext) === null || t === void 0 ? void 0 : t.state) === "running";
      i !== this.canPlaybackAudio && (this.audioEnabled = i, this.emit(j.AudioPlaybackStatusChanged, i));
    });
  }
  createParticipant(e, t) {
    var i;
    let s;
    return t ? s = Yl.fromParticipantInfo(this.engine.client, t, {
      loggerContextCb: () => this.logContext,
      loggerName: this.options.loggerName
    }, this.incomingDataTrackManager) : s = new Yl(this.engine.client, "", e, void 0, void 0, void 0, {
      loggerContextCb: () => this.logContext,
      loggerName: this.options.loggerName
    }), this.options.webAudioMix && s.setAudioContext(this.audioContext), !((i = this.options.audioOutput) === null || i === void 0) && i.deviceId && s.setAudioOutput(this.options.audioOutput).catch((c) => this.log.warn("Could not set audio output: ".concat(c.message))), s;
  }
  getOrCreateParticipant(e, t) {
    if (this.remoteParticipants.has(e)) {
      const s = this.remoteParticipants.get(e);
      return t && s.updateInfo(t) && this.sidToIdentity.set(t.sid, t.identity), s;
    }
    const i = this.createParticipant(e, t);
    return this.remoteParticipants.set(e, i), this.sidToIdentity.set(t.sid, t.identity), this.emitWhenConnected(j.ParticipantConnected, i), i.on(X.TrackPublished, (s) => {
      this.emitWhenConnected(j.TrackPublished, s, i);
    }).on(X.TrackSubscribed, (s, c) => {
      s.kind === H.Kind.Audio ? (s.on(re.AudioPlaybackStarted, this.handleAudioPlaybackStarted), s.on(re.AudioPlaybackFailed, this.handleAudioPlaybackFailed)) : s.kind === H.Kind.Video && (s.on(re.VideoPlaybackFailed, this.handleVideoPlaybackFailed), s.on(re.VideoPlaybackStarted, this.handleVideoPlaybackStarted)), this.emitWhenConnected(j.TrackSubscribed, s, c, i);
    }).on(X.TrackUnpublished, (s) => {
      this.emit(j.TrackUnpublished, s, i);
    }).on(X.TrackUnsubscribed, (s, c) => {
      this.emit(j.TrackUnsubscribed, s, c, i);
    }).on(X.TrackMuted, (s) => {
      this.emitWhenConnected(j.TrackMuted, s, i);
    }).on(X.TrackUnmuted, (s) => {
      this.emitWhenConnected(j.TrackUnmuted, s, i);
    }).on(X.ParticipantMetadataChanged, (s) => {
      this.emitWhenConnected(j.ParticipantMetadataChanged, s, i);
    }).on(X.ParticipantNameChanged, (s) => {
      this.emitWhenConnected(j.ParticipantNameChanged, s, i);
    }).on(X.AttributesChanged, (s) => {
      this.emitWhenConnected(j.ParticipantAttributesChanged, s, i);
    }).on(X.ConnectionQualityChanged, (s) => {
      this.emitWhenConnected(j.ConnectionQualityChanged, s, i);
    }).on(X.ParticipantPermissionsChanged, (s) => {
      this.emitWhenConnected(j.ParticipantPermissionsChanged, s, i);
    }).on(X.TrackSubscriptionStatusChanged, (s, c) => {
      this.emitWhenConnected(j.TrackSubscriptionStatusChanged, s, c, i);
    }).on(X.TrackSubscriptionFailed, (s, c) => {
      this.emit(j.TrackSubscriptionFailed, s, i, c);
    }).on(X.TrackSubscriptionPermissionChanged, (s, c) => {
      this.emitWhenConnected(j.TrackSubscriptionPermissionChanged, s, c, i);
    }).on(X.Active, () => {
      this.emitWhenConnected(j.ParticipantActive, i), i.kind === ka.AGENT && this.localParticipant.setActiveAgent(i);
    }), t && i.updateInfo(t), i;
  }
  sendSyncState() {
    const e = Array.from(this.remoteParticipants.values()).reduce((s, c) => (s.push(...c.getTrackPublications()), s), []), t = this.localParticipant.getTrackPublications(), i = this.outgoingDataTrackManager.queryPublished();
    this.engine.sendSyncState(e, t, i);
  }
  /**
   * After resuming, we'll need to notify the server of the current
   * subscription settings.
   */
  updateSubscriptions() {
    for (const e of this.remoteParticipants.values())
      for (const t of e.videoTrackPublications.values())
        t.isSubscribed && kw(t) && t.emitTrackUpdate();
  }
  getRemoteParticipantBySid(e) {
    const t = this.sidToIdentity.get(e);
    if (t)
      return this.remoteParticipants.get(t);
  }
  registerRpcDataStreamHandler() {
    this.incomingDataStreamManager.registerTextStreamHandler(YS, (e, t) => R(this, [e, t], void 0, function(i, s) {
      var c = this;
      let u = s.identity;
      return (function* () {
        var d;
        const h = (d = i.info.attributes) !== null && d !== void 0 ? d : {};
        yield c.rpcServerManager.handleIncomingDataStream(i, u, h);
      })();
    })), this.incomingDataStreamManager.registerTextStreamHandler(JS, (e, t) => R(this, [e, t], void 0, function(i, s) {
      var c = this;
      let u = s.identity;
      return (function* () {
        var d;
        const h = (d = i.info.attributes) !== null && d !== void 0 ? d : {};
        yield c.rpcClientManager.handleIncomingDataStream(i, u, h);
      })();
    }));
  }
  registerConnectionReconcile() {
    this.clearConnectionReconcile();
    let e = 0;
    this.connectionReconcileInterval = bt.setInterval(() => {
      // ensure we didn't tear it down
      !this.engine || // engine detected close, but Room missed it
      this.engine.isClosed || // transports failed without notifying engine
      !this.engine.verifyTransport() ? (e++, this.log.warn("detected connection state mismatch", {
        numFailures: e,
        engine: this.engine ? {
          closed: this.engine.isClosed,
          transportsConnectedOrConnecting: this.engine.verifyTransport()
        } : void 0
      }), e >= 3 && (this.recreateEngine(), this.handleDisconnect(this.options.stopLocalTrackOnUnpublish, Yn.STATE_MISMATCH))) : e = 0;
    }, fA);
  }
  clearConnectionReconcile() {
    this.connectionReconcileInterval && bt.clearInterval(this.connectionReconcileInterval);
  }
  setAndEmitConnectionState(e) {
    return e === this.state ? !1 : (this.log.info("connection state changed: ".concat(this.state, " -> ").concat(e)), this.state = e, this.incomingDataStreamManager.setConnected(e === Te.Connected), this.emit(j.ConnectionStateChanged, this.state), !0);
  }
  emitBufferedEvents() {
    this.bufferedEvents.forEach((e) => {
      let t = ge(e, 2), i = t[0], s = t[1];
      this.emit(i, ...s);
    }), this.bufferedEvents = [];
  }
  emitWhenConnected(e) {
    for (var t = arguments.length, i = new Array(t > 1 ? t - 1 : 0), s = 1; s < t; s++)
      i[s - 1] = arguments[s];
    if (this.state === Te.Reconnecting || this.isResuming || !this.engine || this.engine.pendingReconnect)
      this.bufferedEvents.push([e, i]);
    else if (this.state === Te.Connected)
      return this.emit(e, ...i);
    return !1;
  }
  /**
   * Allows to populate a room with simulated participants.
   * No actual connection to a server will be established, all state is
   * @experimental
   */
  simulateParticipants(e) {
    return R(this, void 0, void 0, function* () {
      var t, i, s, c;
      const u = Object.assign({
        audio: !0,
        video: !0,
        useRealTracks: !1
      }, e.publish), d = Object.assign({
        count: 9,
        audio: !1,
        video: !0,
        aspectRatios: [1.66, 1.7, 1.3]
      }, e.participants);
      if (this.handleDisconnect(), this.roomInfo = new $l({
        sid: "RM_SIMULATED",
        name: "simulated-room",
        emptyTimeout: 0,
        maxParticipants: 0,
        creationTime: at.parse((/* @__PURE__ */ new Date()).getTime()),
        metadata: "",
        numParticipants: 1,
        numPublishers: 1,
        turnPassword: "",
        enabledCodecs: [],
        activeRecording: !1
      }), this.localParticipant.updateInfo(new ar({
        identity: "simulated-local",
        name: "local-name"
      })), this.setupLocalParticipantEvents(), this.emit(j.SignalConnected), this.emit(j.Connected), this.setAndEmitConnectionState(Te.Connected), u.video) {
        const h = new Vh(H.Kind.Video, new Xr({
          source: yt.CAMERA,
          sid: Math.floor(Math.random() * 1e4).toString(),
          type: jn.AUDIO,
          name: "video-dummy"
        }), new Gl(u.useRealTracks && (!((t = window.navigator.mediaDevices) === null || t === void 0) && t.getUserMedia) ? (yield window.navigator.mediaDevices.getUserMedia({
          video: !0
        })).getVideoTracks()[0] : Ub(160 * ((i = d.aspectRatios[0]) !== null && i !== void 0 ? i : 1), 160, !0, !0), void 0, !1, {
          loggerName: this.options.loggerName,
          loggerContextCb: () => this.logContext
        }), {
          loggerName: this.options.loggerName,
          loggerContextCb: () => this.logContext
        });
        this.localParticipant.addTrackPublication(h), this.localParticipant.emit(X.LocalTrackPublished, h);
      }
      if (u.audio) {
        const h = new Vh(H.Kind.Audio, new Xr({
          source: yt.MICROPHONE,
          sid: Math.floor(Math.random() * 1e4).toString(),
          type: jn.AUDIO
        }), new Hl(u.useRealTracks && (!((s = navigator.mediaDevices) === null || s === void 0) && s.getUserMedia) ? (yield navigator.mediaDevices.getUserMedia({
          audio: !0
        })).getAudioTracks()[0] : $f(), void 0, !1, this.audioContext, {
          loggerName: this.options.loggerName,
          loggerContextCb: () => this.logContext
        }), {
          loggerName: this.options.loggerName,
          loggerContextCb: () => this.logContext
        });
        this.localParticipant.addTrackPublication(h), this.localParticipant.emit(X.LocalTrackPublished, h);
      }
      for (let h = 0; h < d.count - 1; h += 1) {
        let f = new ar({
          sid: Math.floor(Math.random() * 1e4).toString(),
          identity: "simulated-".concat(h),
          state: $r.ACTIVE,
          tracks: [],
          joinedAt: at.parse(Date.now())
        });
        const p = this.getOrCreateParticipant(f.identity, f);
        if (d.video) {
          const v = Ub(160 * ((c = d.aspectRatios[h % d.aspectRatios.length]) !== null && c !== void 0 ? c : 1), 160, !1, !0), y = new Xr({
            source: yt.CAMERA,
            sid: Math.floor(Math.random() * 1e4).toString(),
            type: jn.AUDIO
          });
          p.addSubscribedMediaTrack(v, y.sid, new MediaStream([v]), new RTCRtpReceiver()), f.tracks = [...f.tracks, y];
        }
        if (d.audio) {
          const v = $f(), y = new Xr({
            source: yt.MICROPHONE,
            sid: Math.floor(Math.random() * 1e4).toString(),
            type: jn.AUDIO
          });
          p.addSubscribedMediaTrack(v, y.sid, new MediaStream([v]), new RTCRtpReceiver()), f.tracks = [...f.tracks, y];
        }
        p.updateInfo(f);
      }
    });
  }
  // /** @internal */
  emit(e) {
    for (var t = arguments.length, i = new Array(t > 1 ? t - 1 : 0), s = 1; s < t; s++)
      i[s - 1] = arguments[s];
    if (e !== j.ActiveSpeakersChanged && e !== j.TranscriptionReceived) {
      const c = $S(i).filter((u) => u !== void 0);
      (e === j.TrackSubscribed || e === j.TrackUnsubscribed) && this.log.trace("subscribe trace: ".concat(e), {
        event: e,
        args: c
      }), this.log.debug("room event ".concat(e), {
        event: e,
        args: c
      });
    }
    return super.emit(e, ...i);
  }
}
ps.cleanupRegistry = typeof FinalizationRegistry < "u" && typeof WeakRef < "u" && new FinalizationRegistry((a) => {
  a();
});
function $S(a) {
  return a.map((e) => {
    if (e)
      return Array.isArray(e) ? $S(e) : typeof e == "object" ? "logContext" in e ? e.logContext : void 0 : e;
  });
}
var Fn;
(function(a) {
  a[a.IDLE = 0] = "IDLE", a[a.RUNNING = 1] = "RUNNING", a[a.SKIPPED = 2] = "SKIPPED", a[a.SUCCESS = 3] = "SUCCESS", a[a.FAILED = 4] = "FAILED";
})(Fn || (Fn = {}));
class Ra extends sn.EventEmitter {
  constructor(e, t) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
    super(), this.status = Fn.IDLE, this.logs = [], this.options = {}, this.url = e, this.token = t, this.name = this.constructor.name, this.room = new ps(i.roomOptions), this.connectOptions = i.connectOptions, this.options = i;
  }
  run(e) {
    return R(this, void 0, void 0, function* () {
      if (this.status !== Fn.IDLE)
        throw Error("check is running already");
      this.setStatus(Fn.RUNNING);
      try {
        yield this.perform();
      } catch (t) {
        t instanceof Error && (this.options.errorsAsWarnings ? this.appendWarning(t.message) : this.appendError(t.message));
      }
      return yield this.disconnect(), yield new Promise((t) => setTimeout(t, 500)), this.status !== Fn.SKIPPED && this.setStatus(this.isSuccess() ? Fn.SUCCESS : Fn.FAILED), e && e(), this.getInfo();
    });
  }
  isSuccess() {
    return !this.logs.some((e) => e.level === "error");
  }
  connect(e) {
    return R(this, void 0, void 0, function* () {
      return this.room.state === Te.Connected ? this.room : (e || (e = this.url), yield this.room.connect(e, this.token, this.connectOptions), this.room);
    });
  }
  disconnect() {
    return R(this, void 0, void 0, function* () {
      this.room && this.room.state !== Te.Disconnected && (yield this.room.disconnect(), yield new Promise((e) => setTimeout(e, 500)));
    });
  }
  skip() {
    this.setStatus(Fn.SKIPPED);
  }
  switchProtocol(e) {
    return R(this, void 0, void 0, function* () {
      let t = !1, i = !1;
      if (this.room.on(j.Reconnecting, () => {
        t = !0;
      }), this.room.once(j.Reconnected, () => {
        i = !0;
      }), this.room.simulateScenario("force-".concat(e)), yield new Promise((c) => setTimeout(c, 1e3)), !t)
        return;
      const s = Date.now() + 1e4;
      for (; Date.now() < s; ) {
        if (i)
          return;
        yield zt(100);
      }
      throw new Error("Could not reconnect using ".concat(e, " protocol after 10 seconds"));
    });
  }
  appendMessage(e) {
    this.logs.push({
      level: "info",
      message: e
    }), this.emit("update", this.getInfo());
  }
  appendWarning(e) {
    this.logs.push({
      level: "warning",
      message: e
    }), this.emit("update", this.getInfo());
  }
  appendError(e) {
    this.logs.push({
      level: "error",
      message: e
    }), this.emit("update", this.getInfo());
  }
  setStatus(e) {
    this.status = e, this.emit("update", this.getInfo());
  }
  get engine() {
    var e;
    return (e = this.room) === null || e === void 0 ? void 0 : e.engine;
  }
  getInfo() {
    return {
      logs: this.logs,
      name: this.name,
      status: this.status,
      description: this.description
    };
  }
}
class hA extends Ra {
  get description() {
    return "Cloud regions";
  }
  perform() {
    return R(this, void 0, void 0, function* () {
      const e = new Ue(this.url, this.token);
      if (!e.isCloud()) {
        this.skip();
        return;
      }
      const t = [], i = /* @__PURE__ */ new Set();
      for (let c = 0; c < 3; c++) {
        const u = yield e.getNextBestRegionUrl();
        if (!u)
          break;
        if (i.has(u))
          continue;
        i.add(u);
        const d = yield this.checkCloudRegion(u);
        this.appendMessage("".concat(d.region, " RTT: ").concat(d.rtt, "ms, duration: ").concat(d.duration, "ms")), t.push(d);
      }
      t.sort((c, u) => (c.duration - u.duration) * 0.5 + (c.rtt - u.rtt) * 0.5);
      const s = t[0];
      this.bestStats = s, this.appendMessage("best Cloud region: ".concat(s.region));
    });
  }
  getInfo() {
    const e = super.getInfo();
    return e.data = this.bestStats, e;
  }
  checkCloudRegion(e) {
    return R(this, void 0, void 0, function* () {
      var t, i;
      yield this.connect(e), this.options.protocol === "tcp" && (yield this.switchProtocol("tcp"));
      const s = (t = this.room.serverInfo) === null || t === void 0 ? void 0 : t.region;
      if (!s)
        throw new Error("Region not found");
      const c = yield this.room.localParticipant.streamText({
        topic: "test"
      }), u = 1e3, h = 1e6 / u, f = "A".repeat(u), p = Date.now();
      for (let _ = 0; _ < h; _++)
        yield c.write(f);
      yield c.close();
      const v = Date.now(), y = yield (i = this.room.engine.pcManager) === null || i === void 0 ? void 0 : i.publisher.getStats(), T = {
        region: s,
        rtt: 1e4,
        duration: v - p
      };
      return y?.forEach((_) => {
        _.type === "candidate-pair" && _.nominated && (T.rtt = _.currentRoundTripTime * 1e3);
      }), yield this.disconnect(), T;
    });
  }
}
const uh = 1e4;
class mA extends Ra {
  get description() {
    return "Connection via UDP vs TCP";
  }
  perform() {
    return R(this, void 0, void 0, function* () {
      const e = yield this.checkConnectionProtocol("udp"), t = yield this.checkConnectionProtocol("tcp");
      this.bestStats = e, e.qualityLimitationDurations.bandwidth - t.qualityLimitationDurations.bandwidth > 0.5 || (e.packetsLost - t.packetsLost) / e.packetsSent > 0.01 ? (this.appendMessage("best connection quality via tcp"), this.bestStats = t) : this.appendMessage("best connection quality via udp");
      const i = this.bestStats;
      this.appendMessage("upstream bitrate: ".concat((i.bitrateTotal / i.count / 1e3 / 1e3).toFixed(2), " mbps")), this.appendMessage("RTT: ".concat((i.rttTotal / i.count * 1e3).toFixed(2), " ms")), this.appendMessage("jitter: ".concat((i.jitterTotal / i.count * 1e3).toFixed(2), " ms")), i.packetsLost > 0 && this.appendWarning("packets lost: ".concat((i.packetsLost / i.packetsSent * 100).toFixed(2), "%")), i.qualityLimitationDurations.bandwidth > 1 && this.appendWarning("bandwidth limited ".concat((i.qualityLimitationDurations.bandwidth / (uh / 1e3) * 100).toFixed(2), "%")), i.qualityLimitationDurations.cpu > 0 && this.appendWarning("cpu limited ".concat((i.qualityLimitationDurations.cpu / (uh / 1e3) * 100).toFixed(2), "%"));
    });
  }
  getInfo() {
    const e = super.getInfo();
    return e.data = this.bestStats, e;
  }
  checkConnectionProtocol(e) {
    return R(this, void 0, void 0, function* () {
      yield this.connect(), e === "tcp" ? yield this.switchProtocol("tcp") : yield this.switchProtocol("udp");
      const t = document.createElement("canvas");
      t.width = 1280, t.height = 720;
      const i = t.getContext("2d");
      if (!i)
        throw new Error("Could not get canvas context");
      let s = 0;
      const c = () => {
        s = (s + 1) % 360, i.fillStyle = "hsl(".concat(s, ", 100%, 50%)"), i.fillRect(0, 0, t.width, t.height), requestAnimationFrame(c);
      };
      c();
      const d = t.captureStream(30).getVideoTracks()[0], f = (yield this.room.localParticipant.publishTrack(d, {
        simulcast: !1,
        degradationPreference: "maintain-resolution",
        videoEncoding: {
          maxBitrate: 2e6
        }
      })).track, p = {
        protocol: e,
        packetsLost: 0,
        packetsSent: 0,
        qualityLimitationDurations: {},
        rttTotal: 0,
        jitterTotal: 0,
        bitrateTotal: 0,
        count: 0
      }, v = setInterval(() => R(this, void 0, void 0, function* () {
        const y = yield f.getRTCStatsReport();
        y?.forEach((T) => {
          T.type === "outbound-rtp" ? (p.packetsSent = T.packetsSent, p.qualityLimitationDurations = T.qualityLimitationDurations, p.bitrateTotal += T.targetBitrate, p.count++) : T.type === "remote-inbound-rtp" && (p.packetsLost = T.packetsLost, p.rttTotal += T.roundTripTime, p.jitterTotal += T.jitter);
        });
      }), 1e3);
      return yield new Promise((y) => setTimeout(y, uh)), clearInterval(v), d.stop(), t.remove(), yield this.disconnect(), p;
    });
  }
}
class pA extends Ra {
  get description() {
    return "Can publish audio";
  }
  perform() {
    return R(this, void 0, void 0, function* () {
      var e;
      const t = yield this.connect(), i = yield oA();
      if (yield SS(i, 1e3))
        throw new Error("unable to detect audio from microphone");
      this.appendMessage("detected audio from microphone"), t.localParticipant.publishTrack(i), yield new Promise((d) => setTimeout(d, 3e3));
      const c = yield (e = i.sender) === null || e === void 0 ? void 0 : e.getStats();
      if (!c)
        throw new Error("Could not get RTCStats");
      let u = 0;
      if (c.forEach((d) => {
        d.type === "outbound-rtp" && (d.kind === "audio" || !d.kind && d.mediaType === "audio") && (u = d.packetsSent);
      }), u === 0)
        throw new Error("Could not determine packets are sent");
      this.appendMessage("published ".concat(u, " audio packets"));
    });
  }
}
class gA extends Ra {
  get description() {
    return "Can publish video";
  }
  perform() {
    return R(this, void 0, void 0, function* () {
      var e;
      const t = yield this.connect(), i = yield sA();
      yield this.checkForVideo(i.mediaStreamTrack), t.localParticipant.publishTrack(i), yield new Promise((u) => setTimeout(u, 5e3));
      const s = yield (e = i.sender) === null || e === void 0 ? void 0 : e.getStats();
      if (!s)
        throw new Error("Could not get RTCStats");
      let c = 0;
      if (s.forEach((u) => {
        u.type === "outbound-rtp" && (u.kind === "video" || !u.kind && u.mediaType === "video") && (c += u.packetsSent);
      }), c === 0)
        throw new Error("Could not determine packets are sent");
      this.appendMessage("published ".concat(c, " video packets"));
    });
  }
  checkForVideo(e) {
    return R(this, void 0, void 0, function* () {
      const t = new MediaStream();
      t.addTrack(e.clone());
      const i = document.createElement("video");
      i.srcObject = t, i.muted = !0, i.autoplay = !0, i.playsInline = !0, i.setAttribute("playsinline", "true"), document.body.appendChild(i), yield new Promise((s) => {
        i.onplay = () => {
          setTimeout(() => {
            var c, u, d, h;
            const f = document.createElement("canvas"), p = e.getSettings(), v = (u = (c = p.width) !== null && c !== void 0 ? c : i.videoWidth) !== null && u !== void 0 ? u : 1280, y = (h = (d = p.height) !== null && d !== void 0 ? d : i.videoHeight) !== null && h !== void 0 ? h : 720;
            f.width = v, f.height = y;
            const T = f.getContext("2d");
            T.drawImage(i, 0, 0);
            const k = T.getImageData(0, 0, f.width, f.height).data;
            let x = !0;
            for (let A = 0; A < k.length; A += 4)
              if (k[A] !== 0 || k[A + 1] !== 0 || k[A + 2] !== 0) {
                x = !1;
                break;
              }
            x ? this.appendError("camera appears to be producing only black frames") : this.appendMessage("received video frames"), s();
          }, 1e3);
        }, i.play();
      }), t.getTracks().forEach((s) => s.stop()), i.remove();
    });
  }
}
class vA extends Ra {
  get description() {
    return "Resuming connection after interruption";
  }
  perform() {
    return R(this, void 0, void 0, function* () {
      var e;
      const t = yield this.connect();
      let i = !1, s = !1, c;
      const u = new Promise((f) => {
        setTimeout(f, 5e3), c = f;
      }), d = () => {
        i = !0;
      };
      t.on(j.SignalReconnecting, d).on(j.Reconnecting, d).on(j.Reconnected, () => {
        s = !0, c(!0);
      }), (e = t.engine.client.ws) === null || e === void 0 || e.close();
      const h = t.engine.client.onClose;
      if (h && h(""), yield u, i) {
        if (!s || t.state !== Te.Connected)
          throw this.appendWarning("reconnection is only possible in Redis-based configurations"), new Error("Not able to reconnect");
      } else throw new Error("Did not attempt to reconnect");
    });
  }
}
class yA extends Ra {
  get description() {
    return "Can connect via TURN";
  }
  perform() {
    return R(this, void 0, void 0, function* () {
      var e, t, i;
      fs(new URL(this.url)) && (this.appendMessage("Using region specific url"), this.url = (e = yield new Ue(this.url, this.token).getNextBestRegionUrl()) !== null && e !== void 0 ? e : this.url);
      const s = new Em(), c = yield s.join(this.url, this.token, {
        autoSubscribe: !0,
        maxRetries: 0,
        e2eeEnabled: !1,
        websocketTimeout: 15e3
      }, void 0, !0);
      let u = !1, d = !1, h = !1;
      for (let f of c.iceServers)
        for (let p of f.urls)
          p.startsWith("turn:") ? (d = !0, h = !0) : p.startsWith("turns:") && (d = !0, h = !0, u = !0), p.startsWith("stun:") && (h = !0);
      h ? d && !u && this.appendWarning("TURN is configured server side, but TURN/TLS is unavailable.") : this.appendWarning("No STUN servers configured on server side."), yield s.close(), !((i = (t = this.connectOptions) === null || t === void 0 ? void 0 : t.rtcConfig) === null || i === void 0) && i.iceServers || d ? yield this.room.connect(this.url, this.token, {
        rtcConfig: {
          iceTransportPolicy: "relay"
        }
      }) : (this.appendWarning("No TURN servers configured."), this.skip(), yield new Promise((f) => setTimeout(f, 0)));
    });
  }
}
class bA extends Ra {
  get description() {
    return "Establishing WebRTC connection";
  }
  perform() {
    return R(this, void 0, void 0, function* () {
      let e = !1, t = !1;
      this.room.on(j.SignalConnected, () => {
        var i;
        const s = this.room.engine.client.onTrickle;
        this.room.engine.client.onTrickle = (c, u) => {
          if (c.candidate) {
            const d = new RTCIceCandidate(c);
            let h = "".concat(d.protocol, " ").concat(d.address, ":").concat(d.port, " ").concat(d.type);
            d.address && (TA(d.address) ? h += " (private)" : d.protocol === "tcp" && d.tcpType === "passive" ? (e = !0, h += " (passive)") : d.protocol === "udp" && (t = !0)), this.appendMessage(h);
          }
          s && s(c, u);
        }, !((i = this.room.engine.pcManager) === null || i === void 0) && i.subscriber && (this.room.engine.pcManager.subscriber.onIceCandidateError = (c) => {
          c instanceof RTCPeerConnectionIceErrorEvent && this.appendWarning("error with ICE candidate: ".concat(c.errorCode, " ").concat(c.errorText, " ").concat(c.url));
        });
      });
      try {
        yield this.connect(), Ee.info("now the room is connected");
      } catch (i) {
        throw this.appendWarning("ports need to be open on firewall in order to connect."), i;
      }
      e || this.appendWarning("Server is not configured for ICE/TCP"), t || this.appendWarning("No public IPv4 UDP candidates were found. Your server is likely not configured correctly");
    });
  }
}
function TA(a) {
  const e = a.split(".");
  if (e.length === 4) {
    if (e[0] === "10")
      return !0;
    if (e[0] === "192" && e[1] === "168")
      return !0;
    if (e[0] === "172") {
      const t = parseInt(e[1], 10);
      if (t >= 16 && t <= 31)
        return !0;
    }
  }
  return !1;
}
class SA extends Ra {
  get description() {
    return "Connecting to signal connection via WebSocket";
  }
  perform() {
    return R(this, void 0, void 0, function* () {
      var e, t, i;
      (this.url.startsWith("ws:") || this.url.startsWith("http:")) && this.appendWarning("Server is insecure, clients may block connections to it");
      let s = new Em(), c;
      try {
        c = yield s.join(this.url, this.token, {
          autoSubscribe: !0,
          maxRetries: 0,
          e2eeEnabled: !1,
          websocketTimeout: 15e3
        }, void 0, !0);
      } catch (u) {
        if (fs(new URL(this.url))) {
          this.appendMessage("Initial connection failed with error ".concat(u.message, ". Retrying with region fallback"));
          const h = yield new Ue(this.url, this.token).getNextBestRegionUrl();
          h && (c = yield s.join(h, this.token, {
            autoSubscribe: !0,
            maxRetries: 0,
            e2eeEnabled: !1,
            websocketTimeout: 15e3
          }, void 0, !0), this.appendMessage("Fallback to region worked. To avoid initial connections failing, ensure you're calling room.prepareConnection() ahead of time"));
        }
      }
      c ? (this.appendMessage("Connected to server, version ".concat(c.serverVersion, ".")), ((e = c.serverInfo) === null || e === void 0 ? void 0 : e.edition) === pT.Cloud && (!((t = c.serverInfo) === null || t === void 0) && t.region) && this.appendMessage("LiveKit Cloud: ".concat((i = c.serverInfo) === null || i === void 0 ? void 0 : i.region))) : this.appendError("Websocket connection could not be established"), yield s.close();
    });
  }
}
class EP extends sn.EventEmitter {
  constructor(e, t) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : {};
    super(), this.options = {}, this.checkResults = /* @__PURE__ */ new Map(), this.url = e, this.token = t, this.options = i;
  }
  getNextCheckId() {
    const e = this.checkResults.size;
    return this.checkResults.set(e, {
      logs: [],
      status: Fn.IDLE,
      name: "",
      description: ""
    }), e;
  }
  updateCheck(e, t) {
    this.checkResults.set(e, t), this.emit("checkUpdate", e, t);
  }
  isSuccess() {
    return Array.from(this.checkResults.values()).every((e) => e.status !== Fn.FAILED);
  }
  getResults() {
    return Array.from(this.checkResults.values());
  }
  createAndRunCheck(e) {
    return R(this, void 0, void 0, function* () {
      const t = this.getNextCheckId(), i = new e(this.url, this.token, this.options), s = (u) => {
        this.updateCheck(t, u);
      };
      i.on("update", s);
      const c = yield i.run();
      return i.off("update", s), c;
    });
  }
  checkWebsocket() {
    return R(this, void 0, void 0, function* () {
      return this.createAndRunCheck(SA);
    });
  }
  checkWebRTC() {
    return R(this, void 0, void 0, function* () {
      return this.createAndRunCheck(bA);
    });
  }
  checkTURN() {
    return R(this, void 0, void 0, function* () {
      return this.createAndRunCheck(yA);
    });
  }
  checkReconnect() {
    return R(this, void 0, void 0, function* () {
      return this.createAndRunCheck(vA);
    });
  }
  checkPublishAudio() {
    return R(this, void 0, void 0, function* () {
      return this.createAndRunCheck(pA);
    });
  }
  checkPublishVideo() {
    return R(this, void 0, void 0, function* () {
      return this.createAndRunCheck(gA);
    });
  }
  checkConnectionProtocol() {
    return R(this, void 0, void 0, function* () {
      const e = yield this.createAndRunCheck(mA);
      if (e.data && "protocol" in e.data) {
        const t = e.data;
        this.options.protocol = t.protocol;
      }
      return e;
    });
  }
  checkCloudRegion() {
    return R(this, void 0, void 0, function* () {
      return this.createAndRunCheck(hA);
    });
  }
}
class ek {
}
new TextEncoder();
const Gh = new TextDecoder();
function kA(a) {
  if (Uint8Array.fromBase64)
    return Uint8Array.fromBase64(a);
  const e = atob(a), t = new Uint8Array(e.length);
  for (let i = 0; i < e.length; i++)
    t[i] = e.charCodeAt(i);
  return t;
}
function CA(a) {
  if (Uint8Array.fromBase64)
    return Uint8Array.fromBase64(typeof a == "string" ? a : Gh.decode(a), {
      alphabet: "base64url"
    });
  let e = a;
  e instanceof Uint8Array && (e = Gh.decode(e)), e = e.replace(/-/g, "+").replace(/_/g, "/");
  try {
    return kA(e);
  } catch {
    throw new TypeError("The input to be decoded is not correctly encoded.");
  }
}
class Yt extends Error {
  constructor(e, t) {
    var i;
    super(e, t), xe(this, "code", "ERR_JOSE_GENERIC"), this.name = this.constructor.name, (i = Error.captureStackTrace) === null || i === void 0 || i.call(Error, this, this.constructor);
  }
}
xe(Yt, "code", "ERR_JOSE_GENERIC");
class EA extends Yt {
  constructor(e, t) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "unspecified", s = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : "unspecified";
    super(e, {
      cause: {
        claim: i,
        reason: s,
        payload: t
      }
    }), xe(this, "code", "ERR_JWT_CLAIM_VALIDATION_FAILED"), xe(this, "claim", void 0), xe(this, "reason", void 0), xe(this, "payload", void 0), this.claim = i, this.reason = s, this.payload = t;
  }
}
xe(EA, "code", "ERR_JWT_CLAIM_VALIDATION_FAILED");
class _A extends Yt {
  constructor(e, t) {
    let i = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "unspecified", s = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : "unspecified";
    super(e, {
      cause: {
        claim: i,
        reason: s,
        payload: t
      }
    }), xe(this, "code", "ERR_JWT_EXPIRED"), xe(this, "claim", void 0), xe(this, "reason", void 0), xe(this, "payload", void 0), this.claim = i, this.reason = s, this.payload = t;
  }
}
xe(_A, "code", "ERR_JWT_EXPIRED");
class RA extends Yt {
  constructor() {
    super(...arguments), xe(this, "code", "ERR_JOSE_ALG_NOT_ALLOWED");
  }
}
xe(RA, "code", "ERR_JOSE_ALG_NOT_ALLOWED");
class wA extends Yt {
  constructor() {
    super(...arguments), xe(this, "code", "ERR_JOSE_NOT_SUPPORTED");
  }
}
xe(wA, "code", "ERR_JOSE_NOT_SUPPORTED");
class MA extends Yt {
  constructor() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "decryption operation failed", t = arguments.length > 1 ? arguments[1] : void 0;
    super(e, t), xe(this, "code", "ERR_JWE_DECRYPTION_FAILED");
  }
}
xe(MA, "code", "ERR_JWE_DECRYPTION_FAILED");
class AA extends Yt {
  constructor() {
    super(...arguments), xe(this, "code", "ERR_JWE_INVALID");
  }
}
xe(AA, "code", "ERR_JWE_INVALID");
class OA extends Yt {
  constructor() {
    super(...arguments), xe(this, "code", "ERR_JWS_INVALID");
  }
}
xe(OA, "code", "ERR_JWS_INVALID");
class ha extends Yt {
  constructor() {
    super(...arguments), xe(this, "code", "ERR_JWT_INVALID");
  }
}
xe(ha, "code", "ERR_JWT_INVALID");
class DA extends Yt {
  constructor() {
    super(...arguments), xe(this, "code", "ERR_JWK_INVALID");
  }
}
xe(DA, "code", "ERR_JWK_INVALID");
class PA extends Yt {
  constructor() {
    super(...arguments), xe(this, "code", "ERR_JWKS_INVALID");
  }
}
xe(PA, "code", "ERR_JWKS_INVALID");
class xA extends Yt {
  constructor() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "no applicable key found in the JSON Web Key Set", t = arguments.length > 1 ? arguments[1] : void 0;
    super(e, t), xe(this, "code", "ERR_JWKS_NO_MATCHING_KEY");
  }
}
xe(xA, "code", "ERR_JWKS_NO_MATCHING_KEY");
class NA extends Yt {
  constructor() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "multiple matching keys found in the JSON Web Key Set", t = arguments.length > 1 ? arguments[1] : void 0;
    super(e, t), xe(this, Symbol.asyncIterator, void 0), xe(this, "code", "ERR_JWKS_MULTIPLE_MATCHING_KEYS");
  }
}
xe(NA, "code", "ERR_JWKS_MULTIPLE_MATCHING_KEYS");
class IA extends Yt {
  constructor() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "request timed out", t = arguments.length > 1 ? arguments[1] : void 0;
    super(e, t), xe(this, "code", "ERR_JWKS_TIMEOUT");
  }
}
xe(IA, "code", "ERR_JWKS_TIMEOUT");
class LA extends Yt {
  constructor() {
    let e = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "signature verification failed", t = arguments.length > 1 ? arguments[1] : void 0;
    super(e, t), xe(this, "code", "ERR_JWS_SIGNATURE_VERIFICATION_FAILED");
  }
}
xe(LA, "code", "ERR_JWS_SIGNATURE_VERIFICATION_FAILED");
const UA = (a) => typeof a == "object" && a !== null;
function BA(a) {
  if (!UA(a) || Object.prototype.toString.call(a) !== "[object Object]")
    return !1;
  if (Object.getPrototypeOf(a) === null)
    return !0;
  let e = a;
  for (; Object.getPrototypeOf(e) !== null; )
    e = Object.getPrototypeOf(e);
  return Object.getPrototypeOf(a) === e;
}
function zA(a) {
  if (typeof a != "string") throw new ha("JWTs must use Compact JWS serialization, JWT must be a string");
  const e = a.split("."), t = e[1], i = e.length;
  if (i === 5) throw new ha("Only JWTs using Compact JWS serialization can be decoded");
  if (i !== 3) throw new ha("Invalid JWT");
  if (!t) throw new ha("JWTs must contain a payload");
  let s;
  try {
    s = CA(t);
  } catch {
    throw new ha("Failed to base64url decode the payload");
  }
  let c;
  try {
    c = JSON.parse(Gh.decode(s));
  } catch {
    throw new ha("Failed to parse the decoded payload as JSON");
  }
  if (!BA(c)) throw new ha("Invalid JWT Claims Set");
  return c;
}
function FA(a) {
  const e = zA(a);
  e.roomConfig;
  const t = BT(e, ["roomConfig"]);
  return Object.assign(Object.assign({}, t), {
    roomConfig: e.roomConfig ? KR.fromJson(e.roomConfig, {
      ignoreUnknownFields: !0
    }) : void 0
  });
}
var fl = typeof globalThis < "u" ? globalThis : typeof window < "u" ? window : typeof global < "u" ? global : typeof self < "u" ? self : {};
function jA(a) {
  return a && a.__esModule && Object.prototype.hasOwnProperty.call(a, "default") ? a.default : a;
}
var Kh = { exports: {} }, qA = Kh.exports, k0;
function HA() {
  return k0 || (k0 = 1, (function(a) {
    (function(e, t) {
      a.exports ? a.exports = t() : e.log = t();
    })(qA, function() {
      var e = function() {
      }, t = "undefined", i = typeof window !== t && typeof window.navigator !== t && /Trident\/|MSIE /.test(window.navigator.userAgent), s = [
        "trace",
        "debug",
        "info",
        "warn",
        "error"
      ], c = {}, u = null;
      function d(k, x) {
        var A = k[x];
        if (typeof A.bind == "function")
          return A.bind(k);
        try {
          return Function.prototype.bind.call(A, k);
        } catch {
          return function() {
            return Function.prototype.apply.apply(A, [k, arguments]);
          };
        }
      }
      function h() {
        console.log && (console.log.apply ? console.log.apply(console, arguments) : Function.prototype.apply.apply(console.log, [console, arguments])), console.trace && console.trace();
      }
      function f(k) {
        return k === "debug" && (k = "log"), typeof console === t ? !1 : k === "trace" && i ? h : console[k] !== void 0 ? d(console, k) : console.log !== void 0 ? d(console, "log") : e;
      }
      function p() {
        for (var k = this.getLevel(), x = 0; x < s.length; x++) {
          var A = s[x];
          this[A] = x < k ? e : this.methodFactory(A, k, this.name);
        }
        if (this.log = this.debug, typeof console === t && k < this.levels.SILENT)
          return "No console available for logging";
      }
      function v(k) {
        return function() {
          typeof console !== t && (p.call(this), this[k].apply(this, arguments));
        };
      }
      function y(k, x, A) {
        return f(k) || v.apply(this, arguments);
      }
      function T(k, x) {
        var A = this, B, ie, C, E = "loglevel";
        typeof k == "string" ? E += ":" + k : typeof k == "symbol" && (E = void 0);
        function M(J) {
          var ne = (s[J] || "silent").toUpperCase();
          if (!(typeof window === t || !E)) {
            try {
              window.localStorage[E] = ne;
              return;
            } catch {
            }
            try {
              window.document.cookie = encodeURIComponent(E) + "=" + ne + ";";
            } catch {
            }
          }
        }
        function N() {
          var J;
          if (!(typeof window === t || !E)) {
            try {
              J = window.localStorage[E];
            } catch {
            }
            if (typeof J === t)
              try {
                var ne = window.document.cookie, de = encodeURIComponent(E), fe = ne.indexOf(de + "=");
                fe !== -1 && (J = /^([^;]+)/.exec(
                  ne.slice(fe + de.length + 1)
                )[1]);
              } catch {
              }
            return A.levels[J] === void 0 && (J = void 0), J;
          }
        }
        function G() {
          if (!(typeof window === t || !E)) {
            try {
              window.localStorage.removeItem(E);
            } catch {
            }
            try {
              window.document.cookie = encodeURIComponent(E) + "=; expires=Thu, 01 Jan 1970 00:00:00 UTC";
            } catch {
            }
          }
        }
        function W(J) {
          var ne = J;
          if (typeof ne == "string" && A.levels[ne.toUpperCase()] !== void 0 && (ne = A.levels[ne.toUpperCase()]), typeof ne == "number" && ne >= 0 && ne <= A.levels.SILENT)
            return ne;
          throw new TypeError("log.setLevel() called with invalid level: " + J);
        }
        A.name = k, A.levels = {
          TRACE: 0,
          DEBUG: 1,
          INFO: 2,
          WARN: 3,
          ERROR: 4,
          SILENT: 5
        }, A.methodFactory = x || y, A.getLevel = function() {
          return C ?? ie ?? B;
        }, A.setLevel = function(J, ne) {
          return C = W(J), ne !== !1 && M(C), p.call(A);
        }, A.setDefaultLevel = function(J) {
          ie = W(J), N() || A.setLevel(J, !1);
        }, A.resetLevel = function() {
          C = null, G(), p.call(A);
        }, A.enableAll = function(J) {
          A.setLevel(A.levels.TRACE, J);
        }, A.disableAll = function(J) {
          A.setLevel(A.levels.SILENT, J);
        }, A.rebuild = function() {
          if (u !== A && (B = W(u.getLevel())), p.call(A), u === A)
            for (var J in c)
              c[J].rebuild();
        }, B = W(
          u ? u.getLevel() : "WARN"
        );
        var Y = N();
        Y != null && (C = W(Y)), p.call(A);
      }
      u = new T(), u.getLogger = function(k) {
        if (typeof k != "symbol" && typeof k != "string" || k === "")
          throw new TypeError("You must supply a name when creating a logger.");
        var x = c[k];
        return x || (x = c[k] = new T(
          k,
          u.methodFactory
        )), x;
      };
      var _ = typeof window !== t ? window.log : void 0;
      return u.noConflict = function() {
        return typeof window !== t && window.log === u && (window.log = _), u;
      }, u.getLoggers = function() {
        return c;
      }, u.default = u, u;
    });
  })(Kh)), Kh.exports;
}
var VA = HA();
const GA = /* @__PURE__ */ jA(VA);
var Yh = function(a, e) {
  return Yh = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(t, i) {
    t.__proto__ = i;
  } || function(t, i) {
    for (var s in i) Object.prototype.hasOwnProperty.call(i, s) && (t[s] = i[s]);
  }, Yh(a, e);
};
function zi(a, e) {
  if (typeof e != "function" && e !== null)
    throw new TypeError("Class extends value " + String(e) + " is not a constructor or null");
  Yh(a, e);
  function t() {
    this.constructor = a;
  }
  a.prototype = e === null ? Object.create(e) : (t.prototype = e.prototype, new t());
}
function KA(a, e, t, i) {
  function s(c) {
    return c instanceof t ? c : new t(function(u) {
      u(c);
    });
  }
  return new (t || (t = Promise))(function(c, u) {
    function d(p) {
      try {
        f(i.next(p));
      } catch (v) {
        u(v);
      }
    }
    function h(p) {
      try {
        f(i.throw(p));
      } catch (v) {
        u(v);
      }
    }
    function f(p) {
      p.done ? c(p.value) : s(p.value).then(d, h);
    }
    f((i = i.apply(a, [])).next());
  });
}
function tk(a, e) {
  var t = { label: 0, sent: function() {
    if (c[0] & 1) throw c[1];
    return c[1];
  }, trys: [], ops: [] }, i, s, c, u = Object.create((typeof Iterator == "function" ? Iterator : Object).prototype);
  return u.next = d(0), u.throw = d(1), u.return = d(2), typeof Symbol == "function" && (u[Symbol.iterator] = function() {
    return this;
  }), u;
  function d(f) {
    return function(p) {
      return h([f, p]);
    };
  }
  function h(f) {
    if (i) throw new TypeError("Generator is already executing.");
    for (; u && (u = 0, f[0] && (t = 0)), t; ) try {
      if (i = 1, s && (c = f[0] & 2 ? s.return : f[0] ? s.throw || ((c = s.return) && c.call(s), 0) : s.next) && !(c = c.call(s, f[1])).done) return c;
      switch (s = 0, c && (f = [f[0] & 2, c.value]), f[0]) {
        case 0:
        case 1:
          c = f;
          break;
        case 4:
          return t.label++, { value: f[1], done: !1 };
        case 5:
          t.label++, s = f[1], f = [0];
          continue;
        case 7:
          f = t.ops.pop(), t.trys.pop();
          continue;
        default:
          if (c = t.trys, !(c = c.length > 0 && c[c.length - 1]) && (f[0] === 6 || f[0] === 2)) {
            t = 0;
            continue;
          }
          if (f[0] === 3 && (!c || f[1] > c[0] && f[1] < c[3])) {
            t.label = f[1];
            break;
          }
          if (f[0] === 6 && t.label < c[1]) {
            t.label = c[1], c = f;
            break;
          }
          if (c && t.label < c[2]) {
            t.label = c[2], t.ops.push(f);
            break;
          }
          c[2] && t.ops.pop(), t.trys.pop();
          continue;
      }
      f = e.call(a, t);
    } catch (p) {
      f = [6, p], s = 0;
    } finally {
      i = c = 0;
    }
    if (f[0] & 5) throw f[1];
    return { value: f[0] ? f[1] : void 0, done: !0 };
  }
}
function gs(a) {
  var e = typeof Symbol == "function" && Symbol.iterator, t = e && a[e], i = 0;
  if (t) return t.call(a);
  if (a && typeof a.length == "number") return {
    next: function() {
      return a && i >= a.length && (a = void 0), { value: a && a[i++], done: !a };
    }
  };
  throw new TypeError(e ? "Object is not iterable." : "Symbol.iterator is not defined.");
}
function zo(a, e) {
  var t = typeof Symbol == "function" && a[Symbol.iterator];
  if (!t) return a;
  var i = t.call(a), s, c = [], u;
  try {
    for (; (e === void 0 || e-- > 0) && !(s = i.next()).done; ) c.push(s.value);
  } catch (d) {
    u = { error: d };
  } finally {
    try {
      s && !s.done && (t = i.return) && t.call(i);
    } finally {
      if (u) throw u.error;
    }
  }
  return c;
}
function Fo(a, e, t) {
  if (arguments.length === 2) for (var i = 0, s = e.length, c; i < s; i++)
    (c || !(i in e)) && (c || (c = Array.prototype.slice.call(e, 0, i)), c[i] = e[i]);
  return a.concat(c || Array.prototype.slice.call(e));
}
function os(a) {
  return this instanceof os ? (this.v = a, this) : new os(a);
}
function YA(a, e, t) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var i = t.apply(a, e || []), s, c = [];
  return s = Object.create((typeof AsyncIterator == "function" ? AsyncIterator : Object).prototype), d("next"), d("throw"), d("return", u), s[Symbol.asyncIterator] = function() {
    return this;
  }, s;
  function u(T) {
    return function(_) {
      return Promise.resolve(_).then(T, v);
    };
  }
  function d(T, _) {
    i[T] && (s[T] = function(k) {
      return new Promise(function(x, A) {
        c.push([T, k, x, A]) > 1 || h(T, k);
      });
    }, _ && (s[T] = _(s[T])));
  }
  function h(T, _) {
    try {
      f(i[T](_));
    } catch (k) {
      y(c[0][3], k);
    }
  }
  function f(T) {
    T.value instanceof os ? Promise.resolve(T.value.v).then(p, v) : y(c[0][2], T);
  }
  function p(T) {
    h("next", T);
  }
  function v(T) {
    h("throw", T);
  }
  function y(T, _) {
    T(_), c.shift(), c.length && h(c[0][0], c[0][1]);
  }
}
function JA(a) {
  if (!Symbol.asyncIterator) throw new TypeError("Symbol.asyncIterator is not defined.");
  var e = a[Symbol.asyncIterator], t;
  return e ? e.call(a) : (a = typeof gs == "function" ? gs(a) : a[Symbol.iterator](), t = {}, i("next"), i("throw"), i("return"), t[Symbol.asyncIterator] = function() {
    return this;
  }, t);
  function i(c) {
    t[c] = a[c] && function(u) {
      return new Promise(function(d, h) {
        u = a[c](u), s(d, h, u.done, u.value);
      });
    };
  }
  function s(c, u, d, h) {
    Promise.resolve(h).then(function(f) {
      c({ value: f, done: d });
    }, u);
  }
}
function Tt(a) {
  return typeof a == "function";
}
function Dm(a) {
  var e = function(i) {
    Error.call(i), i.stack = new Error().stack;
  }, t = a(e);
  return t.prototype = Object.create(Error.prototype), t.prototype.constructor = t, t;
}
var dh = Dm(function(a) {
  return function(e) {
    a(this), this.message = e ? e.length + ` errors occurred during unsubscription:
` + e.map(function(t, i) {
      return i + 1 + ") " + t.toString();
    }).join(`
  `) : "", this.name = "UnsubscriptionError", this.errors = e;
  };
});
function Jl(a, e) {
  if (a) {
    var t = a.indexOf(e);
    0 <= t && a.splice(t, 1);
  }
}
var qo = (function() {
  function a(e) {
    this.initialTeardown = e, this.closed = !1, this._parentage = null, this._finalizers = null;
  }
  return a.prototype.unsubscribe = function() {
    var e, t, i, s, c;
    if (!this.closed) {
      this.closed = !0;
      var u = this._parentage;
      if (u)
        if (this._parentage = null, Array.isArray(u))
          try {
            for (var d = gs(u), h = d.next(); !h.done; h = d.next()) {
              var f = h.value;
              f.remove(this);
            }
          } catch (k) {
            e = { error: k };
          } finally {
            try {
              h && !h.done && (t = d.return) && t.call(d);
            } finally {
              if (e) throw e.error;
            }
          }
        else
          u.remove(this);
      var p = this.initialTeardown;
      if (Tt(p))
        try {
          p();
        } catch (k) {
          c = k instanceof dh ? k.errors : [k];
        }
      var v = this._finalizers;
      if (v) {
        this._finalizers = null;
        try {
          for (var y = gs(v), T = y.next(); !T.done; T = y.next()) {
            var _ = T.value;
            try {
              C0(_);
            } catch (k) {
              c = c ?? [], k instanceof dh ? c = Fo(Fo([], zo(c)), zo(k.errors)) : c.push(k);
            }
          }
        } catch (k) {
          i = { error: k };
        } finally {
          try {
            T && !T.done && (s = y.return) && s.call(y);
          } finally {
            if (i) throw i.error;
          }
        }
      }
      if (c)
        throw new dh(c);
    }
  }, a.prototype.add = function(e) {
    var t;
    if (e && e !== this)
      if (this.closed)
        C0(e);
      else {
        if (e instanceof a) {
          if (e.closed || e._hasParent(this))
            return;
          e._addParent(this);
        }
        (this._finalizers = (t = this._finalizers) !== null && t !== void 0 ? t : []).push(e);
      }
  }, a.prototype._hasParent = function(e) {
    var t = this._parentage;
    return t === e || Array.isArray(t) && t.includes(e);
  }, a.prototype._addParent = function(e) {
    var t = this._parentage;
    this._parentage = Array.isArray(t) ? (t.push(e), t) : t ? [t, e] : e;
  }, a.prototype._removeParent = function(e) {
    var t = this._parentage;
    t === e ? this._parentage = null : Array.isArray(t) && Jl(t, e);
  }, a.prototype.remove = function(e) {
    var t = this._finalizers;
    t && Jl(t, e), e instanceof a && e._removeParent(this);
  }, a.EMPTY = (function() {
    var e = new a();
    return e.closed = !0, e;
  })(), a;
})(), nk = qo.EMPTY;
function ik(a) {
  return a instanceof qo || a && "closed" in a && Tt(a.remove) && Tt(a.add) && Tt(a.unsubscribe);
}
function C0(a) {
  Tt(a) ? a() : a.unsubscribe();
}
var QA = {
  Promise: void 0
}, XA = {
  setTimeout: function(a, e) {
    for (var t = [], i = 2; i < arguments.length; i++)
      t[i - 2] = arguments[i];
    return setTimeout.apply(void 0, Fo([a, e], zo(t)));
  },
  clearTimeout: function(a) {
    return clearTimeout(a);
  },
  delegate: void 0
};
function ak(a) {
  XA.setTimeout(function() {
    throw a;
  });
}
function Jh() {
}
function Ol(a) {
  a();
}
var Pm = (function(a) {
  zi(e, a);
  function e(t) {
    var i = a.call(this) || this;
    return i.isStopped = !1, t ? (i.destination = t, ik(t) && t.add(i)) : i.destination = $A, i;
  }
  return e.create = function(t, i, s) {
    return new jo(t, i, s);
  }, e.prototype.next = function(t) {
    this.isStopped || this._next(t);
  }, e.prototype.error = function(t) {
    this.isStopped || (this.isStopped = !0, this._error(t));
  }, e.prototype.complete = function() {
    this.isStopped || (this.isStopped = !0, this._complete());
  }, e.prototype.unsubscribe = function() {
    this.closed || (this.isStopped = !0, a.prototype.unsubscribe.call(this), this.destination = null);
  }, e.prototype._next = function(t) {
    this.destination.next(t);
  }, e.prototype._error = function(t) {
    try {
      this.destination.error(t);
    } finally {
      this.unsubscribe();
    }
  }, e.prototype._complete = function() {
    try {
      this.destination.complete();
    } finally {
      this.unsubscribe();
    }
  }, e;
})(qo), ZA = (function() {
  function a(e) {
    this.partialObserver = e;
  }
  return a.prototype.next = function(e) {
    var t = this.partialObserver;
    if (t.next)
      try {
        t.next(e);
      } catch (i) {
        hl(i);
      }
  }, a.prototype.error = function(e) {
    var t = this.partialObserver;
    if (t.error)
      try {
        t.error(e);
      } catch (i) {
        hl(i);
      }
    else
      hl(e);
  }, a.prototype.complete = function() {
    var e = this.partialObserver;
    if (e.complete)
      try {
        e.complete();
      } catch (t) {
        hl(t);
      }
  }, a;
})(), jo = (function(a) {
  zi(e, a);
  function e(t, i, s) {
    var c = a.call(this) || this, u;
    return Tt(t) || !t ? u = {
      next: t ?? void 0,
      error: i ?? void 0,
      complete: s ?? void 0
    } : u = t, c.destination = new ZA(u), c;
  }
  return e;
})(Pm);
function hl(a) {
  ak(a);
}
function WA(a) {
  throw a;
}
var $A = {
  closed: !0,
  next: Jh,
  error: WA,
  complete: Jh
}, xm = (function() {
  return typeof Symbol == "function" && Symbol.observable || "@@observable";
})();
function Nm(a) {
  return a;
}
function eO(a) {
  return a.length === 0 ? Nm : a.length === 1 ? a[0] : function(e) {
    return a.reduce(function(t, i) {
      return i(t);
    }, e);
  };
}
var Ft = (function() {
  function a(e) {
    e && (this._subscribe = e);
  }
  return a.prototype.lift = function(e) {
    var t = new a();
    return t.source = this, t.operator = e, t;
  }, a.prototype.subscribe = function(e, t, i) {
    var s = this, c = nO(e) ? e : new jo(e, t, i);
    return Ol(function() {
      var u = s, d = u.operator, h = u.source;
      c.add(d ? d.call(c, h) : h ? s._subscribe(c) : s._trySubscribe(c));
    }), c;
  }, a.prototype._trySubscribe = function(e) {
    try {
      return this._subscribe(e);
    } catch (t) {
      e.error(t);
    }
  }, a.prototype.forEach = function(e, t) {
    var i = this;
    return t = E0(t), new t(function(s, c) {
      var u = new jo({
        next: function(d) {
          try {
            e(d);
          } catch (h) {
            c(h), u.unsubscribe();
          }
        },
        error: c,
        complete: s
      });
      i.subscribe(u);
    });
  }, a.prototype._subscribe = function(e) {
    var t;
    return (t = this.source) === null || t === void 0 ? void 0 : t.subscribe(e);
  }, a.prototype[xm] = function() {
    return this;
  }, a.prototype.pipe = function() {
    for (var e = [], t = 0; t < arguments.length; t++)
      e[t] = arguments[t];
    return eO(e)(this);
  }, a.prototype.toPromise = function(e) {
    var t = this;
    return e = E0(e), new e(function(i, s) {
      var c;
      t.subscribe(function(u) {
        return c = u;
      }, function(u) {
        return s(u);
      }, function() {
        return i(c);
      });
    });
  }, a.create = function(e) {
    return new a(e);
  }, a;
})();
function E0(a) {
  var e;
  return (e = a ?? QA.Promise) !== null && e !== void 0 ? e : Promise;
}
function tO(a) {
  return a && Tt(a.next) && Tt(a.error) && Tt(a.complete);
}
function nO(a) {
  return a && a instanceof Pm || tO(a) && ik(a);
}
function iO(a) {
  return Tt(a?.lift);
}
function Zn(a) {
  return function(e) {
    if (iO(e))
      return e.lift(function(t) {
        try {
          return a(t, this);
        } catch (i) {
          this.error(i);
        }
      });
    throw new TypeError("Unable to lift unknown Observable type");
  };
}
function Ea(a, e, t, i, s) {
  return new aO(a, e, t, i, s);
}
var aO = (function(a) {
  zi(e, a);
  function e(t, i, s, c, u, d) {
    var h = a.call(this, t) || this;
    return h.onFinalize = u, h.shouldUnsubscribe = d, h._next = i ? function(f) {
      try {
        i(f);
      } catch (p) {
        t.error(p);
      }
    } : a.prototype._next, h._error = c ? function(f) {
      try {
        c(f);
      } catch (p) {
        t.error(p);
      } finally {
        this.unsubscribe();
      }
    } : a.prototype._error, h._complete = s ? function() {
      try {
        s();
      } catch (f) {
        t.error(f);
      } finally {
        this.unsubscribe();
      }
    } : a.prototype._complete, h;
  }
  return e.prototype.unsubscribe = function() {
    var t;
    if (!this.shouldUnsubscribe || this.shouldUnsubscribe()) {
      var i = this.closed;
      a.prototype.unsubscribe.call(this), !i && ((t = this.onFinalize) === null || t === void 0 || t.call(this));
    }
  }, e;
})(Pm), rO = Dm(function(a) {
  return function() {
    a(this), this.name = "ObjectUnsubscribedError", this.message = "object unsubscribed";
  };
}), Ui = (function(a) {
  zi(e, a);
  function e() {
    var t = a.call(this) || this;
    return t.closed = !1, t.currentObservers = null, t.observers = [], t.isStopped = !1, t.hasError = !1, t.thrownError = null, t;
  }
  return e.prototype.lift = function(t) {
    var i = new _0(this, this);
    return i.operator = t, i;
  }, e.prototype._throwIfClosed = function() {
    if (this.closed)
      throw new rO();
  }, e.prototype.next = function(t) {
    var i = this;
    Ol(function() {
      var s, c;
      if (i._throwIfClosed(), !i.isStopped) {
        i.currentObservers || (i.currentObservers = Array.from(i.observers));
        try {
          for (var u = gs(i.currentObservers), d = u.next(); !d.done; d = u.next()) {
            var h = d.value;
            h.next(t);
          }
        } catch (f) {
          s = { error: f };
        } finally {
          try {
            d && !d.done && (c = u.return) && c.call(u);
          } finally {
            if (s) throw s.error;
          }
        }
      }
    });
  }, e.prototype.error = function(t) {
    var i = this;
    Ol(function() {
      if (i._throwIfClosed(), !i.isStopped) {
        i.hasError = i.isStopped = !0, i.thrownError = t;
        for (var s = i.observers; s.length; )
          s.shift().error(t);
      }
    });
  }, e.prototype.complete = function() {
    var t = this;
    Ol(function() {
      if (t._throwIfClosed(), !t.isStopped) {
        t.isStopped = !0;
        for (var i = t.observers; i.length; )
          i.shift().complete();
      }
    });
  }, e.prototype.unsubscribe = function() {
    this.isStopped = this.closed = !0, this.observers = this.currentObservers = null;
  }, Object.defineProperty(e.prototype, "observed", {
    get: function() {
      var t;
      return ((t = this.observers) === null || t === void 0 ? void 0 : t.length) > 0;
    },
    enumerable: !1,
    configurable: !0
  }), e.prototype._trySubscribe = function(t) {
    return this._throwIfClosed(), a.prototype._trySubscribe.call(this, t);
  }, e.prototype._subscribe = function(t) {
    return this._throwIfClosed(), this._checkFinalizedStatuses(t), this._innerSubscribe(t);
  }, e.prototype._innerSubscribe = function(t) {
    var i = this, s = this, c = s.hasError, u = s.isStopped, d = s.observers;
    return c || u ? nk : (this.currentObservers = null, d.push(t), new qo(function() {
      i.currentObservers = null, Jl(d, t);
    }));
  }, e.prototype._checkFinalizedStatuses = function(t) {
    var i = this, s = i.hasError, c = i.thrownError, u = i.isStopped;
    s ? t.error(c) : u && t.complete();
  }, e.prototype.asObservable = function() {
    var t = new Ft();
    return t.source = this, t;
  }, e.create = function(t, i) {
    return new _0(t, i);
  }, e;
})(Ft), _0 = (function(a) {
  zi(e, a);
  function e(t, i) {
    var s = a.call(this) || this;
    return s.destination = t, s.source = i, s;
  }
  return e.prototype.next = function(t) {
    var i, s;
    (s = (i = this.destination) === null || i === void 0 ? void 0 : i.next) === null || s === void 0 || s.call(i, t);
  }, e.prototype.error = function(t) {
    var i, s;
    (s = (i = this.destination) === null || i === void 0 ? void 0 : i.error) === null || s === void 0 || s.call(i, t);
  }, e.prototype.complete = function() {
    var t, i;
    (i = (t = this.destination) === null || t === void 0 ? void 0 : t.complete) === null || i === void 0 || i.call(t);
  }, e.prototype._subscribe = function(t) {
    var i, s;
    return (s = (i = this.source) === null || i === void 0 ? void 0 : i.subscribe(t)) !== null && s !== void 0 ? s : nk;
  }, e;
})(Ui), sO = (function(a) {
  zi(e, a);
  function e(t) {
    var i = a.call(this) || this;
    return i._value = t, i;
  }
  return Object.defineProperty(e.prototype, "value", {
    get: function() {
      return this.getValue();
    },
    enumerable: !1,
    configurable: !0
  }), e.prototype._subscribe = function(t) {
    var i = a.prototype._subscribe.call(this, t);
    return !i.closed && t.next(this._value), i;
  }, e.prototype.getValue = function() {
    var t = this, i = t.hasError, s = t.thrownError, c = t._value;
    if (i)
      throw s;
    return this._throwIfClosed(), c;
  }, e.prototype.next = function(t) {
    a.prototype.next.call(this, this._value = t);
  }, e;
})(Ui), oO = {
  now: function() {
    return Date.now();
  }
}, cO = (function(a) {
  zi(e, a);
  function e(t, i) {
    return a.call(this) || this;
  }
  return e.prototype.schedule = function(t, i) {
    return this;
  }, e;
})(qo), R0 = {
  setInterval: function(a, e) {
    for (var t = [], i = 2; i < arguments.length; i++)
      t[i - 2] = arguments[i];
    return setInterval.apply(void 0, Fo([a, e], zo(t)));
  },
  clearInterval: function(a) {
    return clearInterval(a);
  },
  delegate: void 0
}, lO = (function(a) {
  zi(e, a);
  function e(t, i) {
    var s = a.call(this, t, i) || this;
    return s.scheduler = t, s.work = i, s.pending = !1, s;
  }
  return e.prototype.schedule = function(t, i) {
    var s;
    if (i === void 0 && (i = 0), this.closed)
      return this;
    this.state = t;
    var c = this.id, u = this.scheduler;
    return c != null && (this.id = this.recycleAsyncId(u, c, i)), this.pending = !0, this.delay = i, this.id = (s = this.id) !== null && s !== void 0 ? s : this.requestAsyncId(u, this.id, i), this;
  }, e.prototype.requestAsyncId = function(t, i, s) {
    return s === void 0 && (s = 0), R0.setInterval(t.flush.bind(t, this), s);
  }, e.prototype.recycleAsyncId = function(t, i, s) {
    if (s === void 0 && (s = 0), s != null && this.delay === s && this.pending === !1)
      return i;
    i != null && R0.clearInterval(i);
  }, e.prototype.execute = function(t, i) {
    if (this.closed)
      return new Error("executing a cancelled action");
    this.pending = !1;
    var s = this._execute(t, i);
    if (s)
      return s;
    this.pending === !1 && this.id != null && (this.id = this.recycleAsyncId(this.scheduler, this.id, null));
  }, e.prototype._execute = function(t, i) {
    var s = !1, c;
    try {
      this.work(t);
    } catch (u) {
      s = !0, c = u || new Error("Scheduled action threw falsy error");
    }
    if (s)
      return this.unsubscribe(), c;
  }, e.prototype.unsubscribe = function() {
    if (!this.closed) {
      var t = this, i = t.id, s = t.scheduler, c = s.actions;
      this.work = this.state = this.scheduler = null, this.pending = !1, Jl(c, this), i != null && (this.id = this.recycleAsyncId(s, i, null)), this.delay = null, a.prototype.unsubscribe.call(this);
    }
  }, e;
})(cO), w0 = (function() {
  function a(e, t) {
    t === void 0 && (t = a.now), this.schedulerActionCtor = e, this.now = t;
  }
  return a.prototype.schedule = function(e, t, i) {
    return t === void 0 && (t = 0), new this.schedulerActionCtor(this, e).schedule(i, t);
  }, a.now = oO.now, a;
})(), uO = (function(a) {
  zi(e, a);
  function e(t, i) {
    i === void 0 && (i = w0.now);
    var s = a.call(this, t, i) || this;
    return s.actions = [], s._active = !1, s;
  }
  return e.prototype.flush = function(t) {
    var i = this.actions;
    if (this._active) {
      i.push(t);
      return;
    }
    var s;
    this._active = !0;
    do
      if (s = t.execute(t.state, t.delay))
        break;
    while (t = i.shift());
    if (this._active = !1, s) {
      for (; t = i.shift(); )
        t.unsubscribe();
      throw s;
    }
  }, e;
})(w0);
new uO(lO);
function dO(a) {
  return a && Tt(a.schedule);
}
function fO(a) {
  return a[a.length - 1];
}
function Im(a) {
  return dO(fO(a)) ? a.pop() : void 0;
}
var rk = (function(a) {
  return a && typeof a.length == "number" && typeof a != "function";
});
function sk(a) {
  return Tt(a?.then);
}
function ok(a) {
  return Tt(a[xm]);
}
function ck(a) {
  return Symbol.asyncIterator && Tt(a?.[Symbol.asyncIterator]);
}
function lk(a) {
  return new TypeError("You provided " + (a !== null && typeof a == "object" ? "an invalid object" : "'" + a + "'") + " where a stream was expected. You can provide an Observable, Promise, ReadableStream, Array, AsyncIterable, or Iterable.");
}
function hO() {
  return typeof Symbol != "function" || !Symbol.iterator ? "@@iterator" : Symbol.iterator;
}
var uk = hO();
function dk(a) {
  return Tt(a?.[uk]);
}
function fk(a) {
  return YA(this, arguments, function() {
    var e, t, i, s;
    return tk(this, function(c) {
      switch (c.label) {
        case 0:
          e = a.getReader(), c.label = 1;
        case 1:
          c.trys.push([1, , 9, 10]), c.label = 2;
        case 2:
          return [4, os(e.read())];
        case 3:
          return t = c.sent(), i = t.value, s = t.done, s ? [4, os(void 0)] : [3, 5];
        case 4:
          return [2, c.sent()];
        case 5:
          return [4, os(i)];
        case 6:
          return [4, c.sent()];
        case 7:
          return c.sent(), [3, 2];
        case 8:
          return [3, 10];
        case 9:
          return e.releaseLock(), [7];
        case 10:
          return [2];
      }
    });
  });
}
function hk(a) {
  return Tt(a?.getReader);
}
function wa(a) {
  if (a instanceof Ft)
    return a;
  if (a != null) {
    if (ok(a))
      return mO(a);
    if (rk(a))
      return pO(a);
    if (sk(a))
      return gO(a);
    if (ck(a))
      return mk(a);
    if (dk(a))
      return vO(a);
    if (hk(a))
      return yO(a);
  }
  throw lk(a);
}
function mO(a) {
  return new Ft(function(e) {
    var t = a[xm]();
    if (Tt(t.subscribe))
      return t.subscribe(e);
    throw new TypeError("Provided object does not correctly implement Symbol.observable");
  });
}
function pO(a) {
  return new Ft(function(e) {
    for (var t = 0; t < a.length && !e.closed; t++)
      e.next(a[t]);
    e.complete();
  });
}
function gO(a) {
  return new Ft(function(e) {
    a.then(function(t) {
      e.closed || (e.next(t), e.complete());
    }, function(t) {
      return e.error(t);
    }).then(null, ak);
  });
}
function vO(a) {
  return new Ft(function(e) {
    var t, i;
    try {
      for (var s = gs(a), c = s.next(); !c.done; c = s.next()) {
        var u = c.value;
        if (e.next(u), e.closed)
          return;
      }
    } catch (d) {
      t = { error: d };
    } finally {
      try {
        c && !c.done && (i = s.return) && i.call(s);
      } finally {
        if (t) throw t.error;
      }
    }
    e.complete();
  });
}
function mk(a) {
  return new Ft(function(e) {
    bO(a, e).catch(function(t) {
      return e.error(t);
    });
  });
}
function yO(a) {
  return mk(fk(a));
}
function bO(a, e) {
  var t, i, s, c;
  return KA(this, void 0, void 0, function() {
    var u, d;
    return tk(this, function(h) {
      switch (h.label) {
        case 0:
          h.trys.push([0, 5, 6, 11]), t = JA(a), h.label = 1;
        case 1:
          return [4, t.next()];
        case 2:
          if (i = h.sent(), !!i.done) return [3, 4];
          if (u = i.value, e.next(u), e.closed)
            return [2];
          h.label = 3;
        case 3:
          return [3, 1];
        case 4:
          return [3, 11];
        case 5:
          return d = h.sent(), s = { error: d }, [3, 11];
        case 6:
          return h.trys.push([6, , 9, 10]), i && !i.done && (c = t.return) ? [4, c.call(t)] : [3, 8];
        case 7:
          h.sent(), h.label = 8;
        case 8:
          return [3, 10];
        case 9:
          if (s) throw s.error;
          return [7];
        case 10:
          return [7];
        case 11:
          return e.complete(), [2];
      }
    });
  });
}
function ir(a, e, t, i, s) {
  i === void 0 && (i = 0), s === void 0 && (s = !1);
  var c = e.schedule(function() {
    t(), s ? a.add(this.schedule(null, i)) : this.unsubscribe();
  }, i);
  if (a.add(c), !s)
    return c;
}
function pk(a, e) {
  return e === void 0 && (e = 0), Zn(function(t, i) {
    t.subscribe(Ea(i, function(s) {
      return ir(i, a, function() {
        return i.next(s);
      }, e);
    }, function() {
      return ir(i, a, function() {
        return i.complete();
      }, e);
    }, function(s) {
      return ir(i, a, function() {
        return i.error(s);
      }, e);
    }));
  });
}
function gk(a, e) {
  return e === void 0 && (e = 0), Zn(function(t, i) {
    i.add(a.schedule(function() {
      return t.subscribe(i);
    }, e));
  });
}
function TO(a, e) {
  return wa(a).pipe(gk(e), pk(e));
}
function SO(a, e) {
  return wa(a).pipe(gk(e), pk(e));
}
function kO(a, e) {
  return new Ft(function(t) {
    var i = 0;
    return e.schedule(function() {
      i === a.length ? t.complete() : (t.next(a[i++]), t.closed || this.schedule());
    });
  });
}
function CO(a, e) {
  return new Ft(function(t) {
    var i;
    return ir(t, e, function() {
      i = a[uk](), ir(t, e, function() {
        var s, c, u;
        try {
          s = i.next(), c = s.value, u = s.done;
        } catch (d) {
          t.error(d);
          return;
        }
        u ? t.complete() : t.next(c);
      }, 0, !0);
    }), function() {
      return Tt(i?.return) && i.return();
    };
  });
}
function vk(a, e) {
  if (!a)
    throw new Error("Iterable cannot be null");
  return new Ft(function(t) {
    ir(t, e, function() {
      var i = a[Symbol.asyncIterator]();
      ir(t, e, function() {
        i.next().then(function(s) {
          s.done ? t.complete() : t.next(s.value);
        });
      }, 0, !0);
    });
  });
}
function EO(a, e) {
  return vk(fk(a), e);
}
function _O(a, e) {
  if (a != null) {
    if (ok(a))
      return TO(a, e);
    if (rk(a))
      return kO(a, e);
    if (sk(a))
      return SO(a, e);
    if (ck(a))
      return vk(a, e);
    if (dk(a))
      return CO(a, e);
    if (hk(a))
      return EO(a, e);
  }
  throw lk(a);
}
function cs(a, e) {
  return e ? _O(a, e) : wa(a);
}
function RO() {
  for (var a = [], e = 0; e < arguments.length; e++)
    a[e] = arguments[e];
  var t = Im(a);
  return cs(a, t);
}
Dm(function(a) {
  return function(e) {
    e === void 0 && (e = null), a(this), this.message = "Timeout has occurred", this.name = "TimeoutError", this.info = e;
  };
});
function Pn(a, e) {
  return Zn(function(t, i) {
    var s = 0;
    t.subscribe(Ea(i, function(c) {
      i.next(a.call(e, c, s++));
    }));
  });
}
function wO(a, e, t, i, s, c, u, d) {
  var h = [], f = 0, p = 0, v = !1, y = function() {
    v && !h.length && !f && e.complete();
  }, T = function(k) {
    return f < i ? _(k) : h.push(k);
  }, _ = function(k) {
    f++;
    var x = !1;
    wa(t(k, p++)).subscribe(Ea(e, function(A) {
      e.next(A);
    }, function() {
      x = !0;
    }, void 0, function() {
      if (x)
        try {
          f--;
          for (var A = function() {
            var B = h.shift();
            u || _(B);
          }; h.length && f < i; )
            A();
          y();
        } catch (B) {
          e.error(B);
        }
    }));
  };
  return a.subscribe(Ea(e, T, function() {
    v = !0, y();
  })), function() {
  };
}
function Ql(a, e, t) {
  return t === void 0 && (t = 1 / 0), Tt(e) ? Ql(function(i, s) {
    return Pn(function(c, u) {
      return e(i, c, s, u);
    })(wa(a(i, s)));
  }, t) : (typeof e == "number" && (t = e), Zn(function(i, s) {
    return wO(i, s, a, t);
  }));
}
function MO(a) {
  return Ql(Nm, a);
}
function AO() {
  return MO(1);
}
function M0() {
  for (var a = [], e = 0; e < arguments.length; e++)
    a[e] = arguments[e];
  return AO()(cs(a, Im(a)));
}
function yk(a, e) {
  return Zn(function(t, i) {
    var s = 0;
    t.subscribe(Ea(i, function(c) {
      return a.call(e, c, s++) && i.next(c);
    }));
  });
}
function OO(a, e, t, i, s) {
  return function(c, u) {
    var d = t, h = e, f = 0;
    c.subscribe(Ea(u, function(p) {
      var v = f++;
      h = d ? a(h, p, v) : (d = !0, p), u.next(h);
    }, s));
  };
}
function DO(a) {
  return Zn(function(e, t) {
    try {
      e.subscribe(t);
    } finally {
      t.add(a);
    }
  });
}
function Dl(a, e) {
  return Zn(OO(a, e, arguments.length >= 2, !0));
}
function PO(a) {
  a === void 0 && (a = {});
  var e = a.connector, t = e === void 0 ? function() {
    return new Ui();
  } : e, i = a.resetOnError, s = i === void 0 ? !0 : i, c = a.resetOnComplete, u = c === void 0 ? !0 : c, d = a.resetOnRefCountZero, h = d === void 0 ? !0 : d;
  return function(f) {
    var p, v, y, T = 0, _ = !1, k = !1, x = function() {
      v?.unsubscribe(), v = void 0;
    }, A = function() {
      x(), p = y = void 0, _ = k = !1;
    }, B = function() {
      var ie = p;
      A(), ie?.unsubscribe();
    };
    return Zn(function(ie, C) {
      T++, !k && !_ && x();
      var E = y = y ?? t();
      C.add(function() {
        T--, T === 0 && !k && !_ && (v = fh(B, h));
      }), E.subscribe(C), !p && T > 0 && (p = new jo({
        next: function(M) {
          return E.next(M);
        },
        error: function(M) {
          k = !0, x(), v = fh(A, s, M), E.error(M);
        },
        complete: function() {
          _ = !0, x(), v = fh(A, u), E.complete();
        }
      }), wa(ie).subscribe(p));
    })(f);
  };
}
function fh(a, e) {
  for (var t = [], i = 2; i < arguments.length; i++)
    t[i - 2] = arguments[i];
  if (e === !0) {
    a();
    return;
  }
  if (e !== !1) {
    var s = new jo({
      next: function() {
        s.unsubscribe(), a();
      }
    });
    return wa(e.apply(void 0, Fo([], zo(t)))).subscribe(s);
  }
}
function Fi() {
  for (var a = [], e = 0; e < arguments.length; e++)
    a[e] = arguments[e];
  var t = Im(a);
  return Zn(function(i, s) {
    (t ? M0(a, i, t) : M0(a, i)).subscribe(s);
  });
}
function A0(a) {
  return Zn(function(e, t) {
    wa(a).subscribe(Ea(t, function() {
      return t.complete();
    }, Jh)), !t.closed && e.subscribe(t);
  });
}
function xO(a, e, t) {
  var i = Tt(a) || e || t ? { next: a, error: e, complete: t } : a;
  return i ? Zn(function(s, c) {
    var u;
    (u = i.subscribe) === null || u === void 0 || u.call(i);
    var d = !0;
    s.subscribe(Ea(c, function(h) {
      var f;
      (f = i.next) === null || f === void 0 || f.call(i, h), c.next(h);
    }, function() {
      var h;
      d = !1, (h = i.complete) === null || h === void 0 || h.call(i), c.complete();
    }, function(h) {
      var f;
      d = !1, (f = i.error) === null || f === void 0 || f.call(i, h), c.error(h);
    }, function() {
      var h, f;
      d && ((h = i.unsubscribe) === null || h === void 0 || h.call(i)), (f = i.finalize) === null || f === void 0 || f.call(i);
    }));
  }) : Nm;
}
var NO = Object.defineProperty, IO = Object.defineProperties, LO = Object.getOwnPropertyDescriptors, O0 = Object.getOwnPropertySymbols, UO = Object.prototype.hasOwnProperty, BO = Object.prototype.propertyIsEnumerable, D0 = (a, e) => (e = Symbol[a]) ? e : /* @__PURE__ */ Symbol.for("Symbol." + a), P0 = (a, e, t) => e in a ? NO(a, e, { enumerable: !0, configurable: !0, writable: !0, value: t }) : a[e] = t, $a = (a, e) => {
  for (var t in e || (e = {}))
    UO.call(e, t) && P0(a, t, e[t]);
  if (O0)
    for (var t of O0(e))
      BO.call(e, t) && P0(a, t, e[t]);
  return a;
}, Qr = (a, e) => IO(a, LO(e)), Ii = (a, e, t) => new Promise((i, s) => {
  var c = (h) => {
    try {
      d(t.next(h));
    } catch (f) {
      s(f);
    }
  }, u = (h) => {
    try {
      d(t.throw(h));
    } catch (f) {
      s(f);
    }
  }, d = (h) => h.done ? i(h.value) : Promise.resolve(h.value).then(c, u);
  d((t = t.apply(a, e)).next());
}), zO = (a, e, t) => (e = a[D0("asyncIterator")]) ? e.call(a) : (a = a[D0("iterator")](), e = {}, t = (i, s) => (s = a[i]) && (e[i] = (c) => new Promise((u, d, h) => (c = s.call(a, c), h = c.done, Promise.resolve(c.value).then((f) => u({ value: f, done: h }), d)))), t("next"), t("return"), e), FO = "lk";
function Lm(a) {
  return typeof a > "u" ? !1 : jO(a) || qO(a);
}
function jO(a) {
  var e;
  return a ? a.hasOwnProperty("participant") && a.hasOwnProperty("source") && a.hasOwnProperty("track") && typeof ((e = a.publication) == null ? void 0 : e.track) < "u" : !1;
}
function qO(a) {
  return a ? a.hasOwnProperty("participant") && a.hasOwnProperty("source") && a.hasOwnProperty("publication") && typeof a.publication < "u" : !1;
}
function HO(a) {
  return a ? a.hasOwnProperty("participant") && a.hasOwnProperty("source") && typeof a.publication > "u" : !1;
}
function VO(a) {
  if (typeof a == "string" || typeof a == "number")
    return `${a}`;
  if (HO(a))
    return `${a.participant.identity}_${a.source}_placeholder`;
  if (Lm(a))
    return `${a.participant.identity}_${a.publication.source}_${a.publication.trackSid}`;
  throw new Error(`Can't generate a id for the given track reference: ${a}`);
}
var GO = [
  j.ConnectionStateChanged,
  j.RoomMetadataChanged,
  j.ActiveSpeakersChanged,
  j.ConnectionQualityChanged,
  j.ParticipantConnected,
  j.ParticipantDisconnected,
  j.ParticipantPermissionsChanged,
  j.ParticipantMetadataChanged,
  j.ParticipantNameChanged,
  j.ParticipantAttributesChanged,
  j.TrackMuted,
  j.TrackUnmuted,
  j.TrackPublished,
  j.TrackUnpublished,
  j.TrackStreamStateChanged,
  j.TrackSubscriptionFailed,
  j.TrackSubscriptionPermissionChanged,
  j.TrackSubscriptionStatusChanged
], bk = [
  ...GO,
  j.LocalTrackPublished,
  j.LocalTrackUnpublished
], KO = [
  X.TrackPublished,
  X.TrackUnpublished,
  X.TrackMuted,
  X.TrackUnmuted,
  X.TrackStreamStateChanged,
  X.TrackSubscribed,
  X.TrackUnsubscribed,
  X.TrackSubscriptionPermissionChanged,
  X.TrackSubscriptionFailed,
  X.LocalTrackPublished,
  X.LocalTrackUnpublished
], YO = [
  X.ConnectionQualityChanged,
  X.IsSpeakingChanged,
  X.ParticipantMetadataChanged,
  X.ParticipantPermissionsChanged,
  X.TrackMuted,
  X.TrackUnmuted,
  X.TrackPublished,
  X.TrackUnpublished,
  X.TrackStreamStateChanged,
  X.TrackSubscriptionFailed,
  X.TrackSubscriptionPermissionChanged,
  X.TrackSubscriptionStatusChanged
];
[
  ...YO,
  X.LocalTrackPublished,
  X.LocalTrackUnpublished
];
var xn = GA.getLogger("lk-components-js");
xn.setDefaultLevel("WARN");
var Wr = /* @__PURE__ */ ((a) => (a.AgentState = "lk.agent.state", a.PublishOnBehalf = "lk.publish_on_behalf", a.TranscriptionFinal = "lk.transcription_final", a.TranscriptionSegmentId = "lk.segment_id", a.TranscribedTrackId = "lk.transcribed_track_id", a))(Wr || {});
function Tk(a) {
  return typeof a == "object";
}
function Sk(a) {
  return Array.isArray(a) && a.filter(Tk).length > 0;
}
function Um(a) {
  return `${FO}-${a}`;
}
function JO(a) {
  const e = Qh(a), t = zm(a.participant).pipe(
    Pn(() => Qh(a)),
    Fi(e)
  );
  return { className: Um(
    a.source === H.Source.Camera || a.source === H.Source.ScreenShare ? "participant-media-video" : "participant-media-audio"
  ), trackObserver: t };
}
function Qh(a) {
  if (Lm(a))
    return a.publication;
  {
    const { source: e, name: t, participant: i } = a;
    if (e && t)
      return i.getTrackPublications().find((s) => s.source === e && s.trackName === t);
    if (t)
      return i.getTrackPublicationByName(t);
    if (e)
      return i.getTrackPublication(e);
    throw new Error("At least one of source and name needs to be defined");
  }
}
function kk(a, ...e) {
  return new Ft((t) => {
    const i = () => {
      t.next(a);
    };
    return e.forEach((s) => {
      a.on(s, i);
    }), () => {
      e.forEach((s) => {
        a.off(s, i);
      });
    };
  }).pipe(Fi(a));
}
function Ck(a, e) {
  return new Ft((t) => {
    const i = (...s) => {
      t.next(s);
    };
    return a.on(e, i), () => {
      a.off(e, i);
    };
  });
}
function QO(a) {
  return Ck(a, j.ConnectionStateChanged).pipe(
    Pn(([e]) => e),
    Fi(a.state)
  );
}
function XO(a) {
  return Ck(a, j.DataReceived);
}
function Bm(a, ...e) {
  return new Ft((t) => {
    const i = () => {
      t.next(a);
    };
    return e.forEach((s) => {
      a.on(s, i);
    }), () => {
      e.forEach((s) => {
        a.off(s, i);
      });
    };
  }).pipe(Fi(a));
}
function zm(a) {
  return Bm(
    a,
    X.TrackMuted,
    X.TrackUnmuted,
    X.ParticipantPermissionsChanged,
    // ParticipantEvent.IsSpeakingChanged,
    X.TrackPublished,
    X.TrackUnpublished,
    X.LocalTrackPublished,
    X.LocalTrackUnpublished,
    X.MediaDevicesError,
    X.TrackSubscriptionStatusChanged
    // ParticipantEvent.ConnectionQualityChanged,
  ).pipe(
    Pn((e) => {
      const { isMicrophoneEnabled: t, isCameraEnabled: i, isScreenShareEnabled: s } = e, c = e.getTrackPublication(H.Source.Microphone), u = e.getTrackPublication(H.Source.Camera);
      return {
        isCameraEnabled: i,
        isMicrophoneEnabled: t,
        isScreenShareEnabled: s,
        cameraTrack: u,
        microphoneTrack: c,
        participant: e
      };
    })
  );
}
function ZO(a) {
  return a ? Bm(
    a,
    X.ParticipantMetadataChanged,
    X.ParticipantNameChanged
  ).pipe(
    Pn(({ name: e, identity: t, metadata: i }) => ({
      name: e,
      identity: t,
      metadata: i
    })),
    Fi({
      name: a.name,
      identity: a.identity,
      metadata: a.metadata
    })
  ) : void 0;
}
function WO(a, e = {}) {
  var t;
  let i;
  const s = new Ft((h) => (i = h, () => d.unsubscribe())).pipe(Fi(Array.from(a.remoteParticipants.values()))), c = (t = e.additionalRoomEvents) != null ? t : bk, u = Array.from(
    /* @__PURE__ */ new Set([
      j.ParticipantConnected,
      j.ParticipantDisconnected,
      j.ConnectionStateChanged,
      ...c
    ])
  ), d = kk(a, ...u).subscribe(
    (h) => i?.next(Array.from(h.remoteParticipants.values()))
  );
  return a.remoteParticipants.size > 0 && i?.next(Array.from(a.remoteParticipants.values())), s;
}
function $O(a, e, t, i, s) {
  const { localParticipant: c } = e, u = (p, v) => {
    let y = !1;
    switch (p) {
      case H.Source.Camera:
        y = v.isCameraEnabled;
        break;
      case H.Source.Microphone:
        y = v.isMicrophoneEnabled;
        break;
      case H.Source.ScreenShare:
        y = v.isScreenShareEnabled;
        break;
    }
    return y;
  }, d = zm(c).pipe(
    Pn((p) => u(a, p.participant)),
    Fi(u(a, c))
  ), h = new Ui(), f = (p, v) => Ii(null, null, function* () {
    try {
      switch (v ?? (v = t), h.next(!0), a) {
        case H.Source.Camera:
          return yield c.setCameraEnabled(
            p ?? !c.isCameraEnabled,
            v,
            i
          ), c.isCameraEnabled;
        case H.Source.Microphone:
          return yield c.setMicrophoneEnabled(
            p ?? !c.isMicrophoneEnabled,
            v,
            i
          ), c.isMicrophoneEnabled;
        case H.Source.ScreenShare:
          return yield c.setScreenShareEnabled(
            p ?? !c.isScreenShareEnabled,
            v,
            i
          ), c.isScreenShareEnabled;
        default:
          throw new TypeError("Tried to toggle unsupported source");
      }
    } catch (y) {
      if (s && y instanceof Error) {
        s?.(y);
        return;
      } else
        throw y;
    } finally {
      h.next(!1);
    }
  });
  return {
    className: Um("button"),
    toggle: f,
    enabledObserver: d,
    pendingObserver: h.asObservable()
  };
}
function eD() {
  let a = !1;
  const e = new Ui(), t = new Ui(), i = (s) => Ii(null, null, function* () {
    t.next(!0), a = s ?? !a, e.next(a), t.next(!1);
  });
  return {
    className: Um("button"),
    toggle: i,
    enabledObserver: e.asObservable(),
    pendingObserver: t.asObservable()
  };
}
var Ek = {
  CHAT: "lk.chat",
  TRANSCRIPTION: "lk.transcription"
}, tD = {
  CHAT: "lk-chat-topic"
};
function _k(a, e) {
  return Ii(this, arguments, function* (t, i, s = {}) {
    const { reliable: c, destinationIdentities: u, topic: d } = s;
    yield t.publishData(i, {
      destinationIdentities: u,
      topic: d,
      reliable: c
    });
  });
}
function nD(a, e, t) {
  const i = Array.isArray(e) ? e : [e], s = XO(a).pipe(
    yk(
      ([, , , d]) => e === void 0 || d !== void 0 && i.includes(d)
    ),
    Pn(([d, h, , f]) => ({
      payload: d,
      topic: f,
      from: h
    }))
  );
  let c;
  const u = new Ft((d) => {
    c = d;
  });
  return { messageObservable: s, isSendingObservable: u, send: (d, ...h) => Ii(null, [d, ...h], function* (f, p = {}) {
    c.next(!0);
    try {
      yield _k(a.localParticipant, f, $a({ topic: i[0] }, p));
    } finally {
      c.next(!1);
    }
  }) };
}
var iD = class {
  constructor(a, e) {
    this._isResolved = !1, this.onFinally = e, this.promise = new Promise((t, i) => Ii(this, null, function* () {
      this.resolve = t, this.reject = i, a && (yield a(t, i));
    })).finally(() => {
      var t;
      this._isResolved = !0, (t = this.onFinally) == null || t.call(this);
    });
  }
  get isResolved() {
    return this._isResolved;
  }
}, ml = /* @__PURE__ */ new WeakMap(), pl = /* @__PURE__ */ new Map();
function aD(a) {
  return a.ignoreLegacy == !0;
}
var rD = (a) => JSON.parse(new TextDecoder().decode(a)), sD = (a) => new TextEncoder().encode(JSON.stringify(a));
function oD(a, e) {
  var t, i, s, c, u, d;
  const h = () => {
    var E, M, N;
    return ((E = a.serverInfo) == null ? void 0 : E.edition) === 1 || !!((M = a.serverInfo) != null && M.version) && Xn((N = a.serverInfo) == null ? void 0 : N.version, "1.8.2") > 0;
  }, f = new Ui(), p = (t = e?.channelTopic) != null ? t : Ek.CHAT, v = (i = e?.channelTopic) != null ? i : tD.CHAT;
  let y = !1;
  ml.has(a) || (y = !0);
  const T = (s = ml.get(a)) != null ? s : /* @__PURE__ */ new Map(), _ = (c = T.get(p)) != null ? c : new Ui();
  T.set(p, _), ml.set(a, T);
  const k = (u = e?.messageDecoder) != null ? u : rD;
  if (y) {
    a.registerTextStreamHandler(p, (M, N) => Ii(null, null, function* () {
      const { id: G, timestamp: W, attributes: Y, attachedStreamIds: J } = M.info, ne = new Map(
        (J ?? []).map((de) => [
          de,
          new iD()
        ])
      );
      pl.set(G, ne), cs(M).pipe(
        Dl((de, fe) => de + fe),
        Ql((de) => ne.size === 0 ? RO({ chunk: de, attachedFiles: [] }) : cs(ne.values()).pipe(
          Ql((fe) => cs(fe.promise)),
          Dl(
            (fe, ke) => [...fe, new File(ke.buffer, ke.fileName)],
            []
          ),
          Pn((fe) => ({ chunk: de, attachedFiles: fe }))
        )),
        Pn(({ chunk: de, attachedFiles: fe }) => ({
          id: G,
          timestamp: W,
          message: de,
          from: a.getParticipantByIdentity(N.identity),
          type: "chatMessage",
          attributes: Y,
          attachedFiles: fe
          // editTimestamp: type === 'update' ? timestamp : undefined,
        })),
        DO(() => pl.delete(G))
      ).subscribe({
        next: (de) => _.next(de)
      });
    })), a.registerByteStreamHandler(p, (M) => Ii(null, null, function* () {
      var N, G;
      const { id: W } = M.info, Y = Array.from(pl).find(
        ([, le]) => le.has(W)
      );
      if (!Y)
        return;
      const J = Y[0], ne = [];
      try {
        for (var de = zO(M), fe, ke, q; fe = !(ke = yield de.next()).done; fe = !1) {
          const le = ke.value;
          ne.push(le);
        }
      } catch (le) {
        q = [le];
      } finally {
        try {
          fe && (ke = de.return) && (yield ke.call(de));
        } finally {
          if (q)
            throw q[0];
        }
      }
      const te = (N = pl.get(J)) == null ? void 0 : N.get(W);
      te && ((G = te.resolve) == null || G.call(te, {
        fileName: M.info.name,
        buffer: ne
      }));
    }));
    const { messageObservable: E } = nD(a, [v]);
    E.pipe(
      Pn((M) => {
        const N = k(M.payload);
        return aD(N) ? void 0 : Qr($a({}, N), {
          type: "chatMessage",
          from: M.from
        });
      }),
      yk((M) => !!M),
      A0(f)
    ).subscribe(_);
  }
  const x = _.pipe(
    Dl((E, M) => {
      if ("id" in M && E.find((N) => {
        var G, W;
        return ((G = N.from) == null ? void 0 : G.identity) === ((W = M.from) == null ? void 0 : W.identity) && N.id === M.id;
      })) {
        const N = E.findIndex((G) => G.id === M.id);
        if (N > -1) {
          const G = E[N];
          E[N] = Qr($a({}, M), {
            timestamp: G.timestamp,
            editTimestamp: M.timestamp
          });
        }
        return [...E];
      }
      return [...E, M];
    }, []),
    A0(f)
  ), A = new sO(!1), B = (d = e?.messageEncoder) != null ? d : sD, ie = (E, M) => Ii(null, null, function* () {
    M || (M = {}), M.topic != null || (M.topic = p), A.next(!0);
    try {
      const N = {
        id: (yield a.localParticipant.sendText(E, M)).id,
        timestamp: Date.now(),
        message: E
      }, G = Qr($a({}, N), {
        attachedFiles: M.attachments
      }), W = Qr($a({}, G), {
        type: "chatMessage",
        from: a.localParticipant,
        attributes: M.attributes
      });
      _.next(W);
      const Y = B(Qr($a({}, N), {
        ignoreLegacy: h()
      }));
      try {
        yield _k(a.localParticipant, Y, {
          reliable: !0,
          topic: v
        });
      } catch (J) {
        xn.info("could not send message in legacy chat format", J);
      }
      return W;
    } finally {
      A.next(!1);
    }
  });
  function C() {
    f.next(), f.complete(), _.complete(), ml.delete(a), a.unregisterTextStreamHandler(p), a.unregisterByteStreamHandler(p);
  }
  return a.once(j.Disconnected, C), {
    messageObservable: x,
    isSendingObservable: A,
    send: ie
  };
}
function x0(a, e, t = !0) {
  const i = [a.localParticipant, ...Array.from(a.remoteParticipants.values())], s = [];
  return i.forEach((c) => {
    e.forEach((u) => {
      const d = Array.from(
        c.trackPublications.values()
      ).filter(
        (h) => h.source === u && // either return all or only the ones that are subscribed
        (!t || h.track)
      ).map((h) => ({
        participant: c,
        publication: h,
        source: h.source
      }));
      s.push(...d);
    });
  }), { trackReferences: s, participants: i };
}
function N0(a, e, t = !1) {
  const { sources: i, kind: s, name: c } = e;
  return Array.from(a.trackPublications.values()).filter(
    (u) => (!i || i.includes(u.source)) && (!s || u.kind === s) && (!c || u.trackName === c) && // either return all or only the ones that are subscribed
    (!t || u.track)
  ).map((u) => ({
    participant: a,
    publication: u,
    source: u.source
  }));
}
function cD(a, e, t) {
  var i, s;
  const c = (i = t.additionalRoomEvents) != null ? i : bk, u = (s = t.onlySubscribed) != null ? s : !0, d = Array.from(
    (/* @__PURE__ */ new Set([
      j.ParticipantConnected,
      j.ParticipantDisconnected,
      j.ConnectionStateChanged,
      j.LocalTrackPublished,
      j.LocalTrackUnpublished,
      j.TrackPublished,
      j.TrackUnpublished,
      j.TrackSubscriptionStatusChanged,
      ...c
    ])).values()
  );
  return kk(a, ...d).pipe(
    Pn((h) => {
      const f = x0(h, e, u);
      return xn.debug(`TrackReference[] was updated. (length ${f.trackReferences.length})`, f), f;
    }),
    Fi(x0(a, e, u))
  );
}
function lD(a, e) {
  return Bm(a, ...KO).pipe(
    Pn((t) => {
      const i = N0(t, e);
      return xn.debug(`TrackReference[] was updated. (length ${i.length})`, i), i;
    }),
    Fi(N0(a, e))
  );
}
var hh = null, mh = null, uD = 0;
function I0() {
  return hh || (hh = /* @__PURE__ */ new Map()), hh;
}
function dD() {
  return mh || (mh = /* @__PURE__ */ new WeakMap()), mh;
}
function fD(a, e) {
  const t = dD();
  let i = t.get(a);
  return i || (i = `room_${uD++}`, t.set(a, i)), `${i}:${e}`;
}
function hD(a, e) {
  const t = fD(a, e), i = I0(), s = i.get(t);
  if (s)
    return s;
  const c = new Ui();
  let u = [];
  const d = "lk.segment_id", h = c.pipe(
    xO({
      subscribe: () => {
        a.registerTextStreamHandler(e, (f, p) => Ii(null, null, function* () {
          var v;
          const y = cs(f).pipe(
            Dl((_, k) => _ + k, "")
          ), T = !!((v = f.info.attributes) != null && v[d]);
          y.subscribe((_) => {
            const k = u.findIndex(
              (x) => {
                var A, B;
                return x.streamInfo.id === f.info.id || T && ((A = x.streamInfo.attributes) == null ? void 0 : A[d]) === ((B = f.info.attributes) == null ? void 0 : B[d]);
              }
            );
            k !== -1 ? (u[k] = Qr($a({}, u[k]), {
              text: _,
              // Carry the latest streamInfo forward. Transcription updates for a
              // segment arrive as separate streams sharing the same lk.segment_id;
              // keeping the original streamInfo would freeze attributes that change
              // over the segment's lifetime — notably lk.transcription_final flipping
              // "false" -> "true" on the final user STT result.
              streamInfo: f.info
            }), c.next([...u])) : (u.push({
              text: _,
              participantInfo: p,
              streamInfo: f.info
            }), c.next([...u]));
          });
        }));
      },
      finalize: () => {
        a.unregisterTextStreamHandler(e);
      }
    }),
    PO()
  );
  return i.set(t, h), a.on(j.Disconnected, () => {
    I0().delete(t), u = [], c.next([]);
  }), h;
}
L.createContext(void 0);
const mD = L.createContext(
  void 0
);
function pD() {
  return L.useContext(mD);
}
function gD(a) {
  const e = pD(), t = a ?? e;
  if (!t)
    throw new Error(
      "No TrackRef, make sure you are inside a TrackRefContext or pass the TrackRef explicitly"
    );
  return t;
}
const vD = L.createContext(void 0);
function Rk() {
  return L.useContext(vD);
}
const wk = L.createContext(void 0);
function Fm() {
  return L.useContext(wk);
}
function vs(a) {
  const e = Fm(), t = a ?? e;
  if (!t)
    throw new Error(
      "No room provided, make sure you are inside a Room context or pass the room explicitly"
    );
  return t;
}
const jm = L.createContext(void 0);
function Ho() {
  const a = L.useContext(jm);
  if (!a)
    throw Error("tried to access session context outside of SessionProvider component");
  return a;
}
function Mk() {
  return L.useContext(jm);
}
function yD(a) {
  const e = Mk(), t = a ?? e;
  if (!t)
    throw new Error(
      "No session provided, make sure you are inside a Session context or pass the session explicitly"
    );
  return t;
}
L.createContext(void 0);
function Ak(a) {
  var e, t, i = "";
  if (typeof a == "string" || typeof a == "number") i += a;
  else if (typeof a == "object") if (Array.isArray(a)) {
    var s = a.length;
    for (e = 0; e < s; e++) a[e] && (t = Ak(a[e])) && (i && (i += " "), i += t);
  } else for (t in a) a[t] && (i && (i += " "), i += t);
  return i;
}
function bD() {
  for (var a, e, t = 0, i = "", s = arguments.length; t < s; t++) (a = arguments[t]) && (e = Ak(a)) && (i && (i += " "), i += e);
  return i;
}
function TD(...a) {
  return (...e) => {
    for (const t of a)
      if (typeof t == "function")
        try {
          t(...e);
        } catch (i) {
          console.error(i);
        }
  };
}
function Ok(...a) {
  const e = { ...a[0] };
  for (let t = 1; t < a.length; t++) {
    const i = a[t];
    for (const s in i) {
      const c = e[s], u = i[s];
      typeof c == "function" && typeof u == "function" && // This is a lot faster than a regex.
      s[0] === "o" && s[1] === "n" && s.charCodeAt(2) >= /* 'A' */
      65 && s.charCodeAt(2) <= /* 'Z' */
      90 ? e[s] = TD(c, u) : (s === "className" || s === "UNSAFE_className") && typeof c == "string" && typeof u == "string" ? e[s] = bD(c, u) : e[s] = u !== void 0 ? u : c;
    }
  }
  return e;
}
function SD(a) {
  return a !== void 0;
}
function kD(...a) {
  return Ok(...a.filter(SD));
}
function _a(a, e, t = !0) {
  const [i, s] = L.useState(e);
  return L.useEffect(() => {
    if (t && s(e), typeof window > "u" || !a) return;
    const c = a.subscribe(s);
    return () => c.unsubscribe();
  }, [a, t]), i;
}
function Dk(a) {
  const e = vs(a), t = L.useMemo(() => QO(e), [e]);
  return _a(t, e.state);
}
function Pk(a = {}) {
  const e = vs(a.room), [t, i] = L.useState(e.localParticipant), [s, c] = L.useState(
    t.isMicrophoneEnabled
  ), [u, d] = L.useState(t.isCameraEnabled), [h, f] = L.useState(
    t.isScreenShareEnabled
  ), [p, v] = L.useState(
    t.lastMicrophoneError
  ), [y, T] = L.useState(t.lastCameraError), [_, k] = L.useState(
    void 0
  ), [x, A] = L.useState(void 0), B = (ie) => {
    d(ie.isCameraEnabled), c(ie.isMicrophoneEnabled), f(ie.isScreenShareEnabled), A(ie.cameraTrack), k(ie.microphoneTrack), v(ie.participant.lastMicrophoneError), T(ie.participant.lastCameraError), i(ie.participant);
  };
  return L.useEffect(() => {
    const ie = zm(e.localParticipant).subscribe(B);
    return () => ie.unsubscribe();
  }, [e]), {
    isMicrophoneEnabled: s,
    isScreenShareEnabled: h,
    isCameraEnabled: u,
    microphoneTrack: _,
    cameraTrack: x,
    lastMicrophoneError: p,
    lastCameraError: y,
    localParticipant: t
  };
}
function CD(a = {}) {
  let e = Rk();
  a.participant && (e = a.participant);
  const t = L.useMemo(() => ZO(e), [e]), { identity: i, name: s, metadata: c } = _a(t, {
    name: e?.name,
    identity: e?.identity,
    metadata: e?.metadata
  });
  return { identity: i, name: s, metadata: c };
}
function xk(a = {}) {
  const e = vs(a.room), [t, i] = L.useState([]);
  return L.useEffect(() => {
    const s = WO(e, {
      additionalRoomEvents: a.updateOnlyOn
    }).subscribe(i);
    return () => s.unsubscribe();
  }, [e, JSON.stringify(a.updateOnlyOn)]), t;
}
function ED(a = {}) {
  const e = xk(a), { localParticipant: t } = Pk(a);
  return L.useMemo(
    () => [t, ...e],
    [t, e]
  );
}
function _D({
  source: a,
  onChange: e,
  initialState: t,
  captureOptions: i,
  publishOptions: s,
  onDeviceError: c,
  room: u,
  ...d
}) {
  const h = Fm(), f = L.useMemo(() => u ?? h, [u, h]), p = f?.localParticipant?.getTrackPublication(a), v = L.useRef(!1), { toggle: y, className: T, pendingObserver: _, enabledObserver: k } = L.useMemo(
    () => f ? $O(a, f, i, s, c) : eD(),
    [f, a, JSON.stringify(i), s]
  ), x = _a(_, !1), A = _a(k, t ?? !!p?.isEnabled);
  L.useEffect(() => {
    e?.(A, v.current), v.current = !1;
  }, [A, e]), L.useEffect(() => {
    t !== void 0 && (xn.debug("forcing initial toggle state", a, t), y(t));
  }, []);
  const B = L.useMemo(() => Ok(d, { className: T }), [d, T]), ie = L.useCallback(
    (C) => {
      v.current = !0, y().catch(() => v.current = !1), d.onClick?.(C);
    },
    [d, y]
  );
  return {
    toggle: y,
    enabled: A,
    pending: x,
    track: p,
    buttonProps: {
      ...B,
      "aria-pressed": A,
      "data-lk-source": a,
      "data-lk-enabled": A,
      disabled: x,
      onClick: ie
    }
  };
}
function RD(a = [
  H.Source.Camera,
  H.Source.Microphone,
  H.Source.ScreenShare,
  H.Source.ScreenShareAudio,
  H.Source.Unknown
], e = {}) {
  const t = vs(e.room), [i, s] = L.useState([]), [c, u] = L.useState([]), d = L.useMemo(() => a.map((h) => Tk(h) ? h.source : h), [JSON.stringify(a)]);
  return L.useEffect(() => {
    const h = cD(t, d, {
      additionalRoomEvents: e.updateOnlyOn,
      onlySubscribed: e.onlySubscribed
    }).subscribe(({ trackReferences: f, participants: p }) => {
      xn.debug("setting track bundles", f, p), s(f), u(p);
    });
    return () => h.unsubscribe();
  }, [
    t,
    JSON.stringify(e.onlySubscribed),
    JSON.stringify(e.updateOnlyOn),
    JSON.stringify(a)
  ]), L.useMemo(() => {
    if (Sk(a)) {
      const h = MD(a, c), f = Array.from(i);
      return c.forEach((p) => {
        h.has(p.identity) && (h.get(p.identity) ?? []).forEach((v) => {
          if (i.find(
            ({ participant: T, publication: _ }) => p.identity === T.identity && _.source === v
          ))
            return;
          xn.debug(
            `Add ${v} placeholder for participant ${p.identity}.`
          );
          const y = {
            participant: p,
            source: v
          };
          f.push(y);
        });
      }), f;
    } else
      return i;
  }, [i, c, a]);
}
function wD(a, e) {
  const t = new Set(a);
  for (const i of e)
    t.delete(i);
  return t;
}
function MD(a, e) {
  const t = /* @__PURE__ */ new Map();
  if (Sk(a)) {
    const i = a.filter((s) => s.withPlaceholder).map((s) => s.source);
    e.forEach((s) => {
      const c = s.getTrackPublications().map((d) => d.track?.source).filter((d) => d !== void 0), u = Array.from(
        wD(new Set(i), new Set(c))
      );
      u.length > 0 && t.set(s.identity, u);
    });
  }
  return t;
}
function Nk(a) {
  const e = vs(a?.room), t = Dk(e), i = L.useMemo(
    () => t === Te.Disconnected,
    [t]
  ), s = L.useMemo(
    () => oD(e, a),
    [e, a, i]
  ), c = _a(s.isSendingObservable, !1), u = _a(s.messageObservable, []);
  return { send: s.send, chatMessages: u, isSending: c };
}
function L0(a, e = {}) {
  let t, i;
  typeof e == "string" ? t = e : (t = e?.participantIdentity, i = e?.room);
  const s = Rk(), c = ED({ room: i, updateOnlyOn: [] }), u = L.useMemo(() => t ? c.find((h) => h.identity === t) : s, [t, c, s]), d = L.useMemo(() => {
    if (u)
      return lD(u, { sources: a });
  }, [u, JSON.stringify(a)]);
  return _a(d, []);
}
function AD(a, e) {
  const t = vs(e?.room), i = Dk(t) === Te.Disconnected, s = L.useMemo(() => hD(t, a), [t, a]);
  return { textStreams: _a(i ? void 0 : s, []) };
}
function OD(a) {
  const { participantIdentities: e, trackSids: t } = a ?? {}, { textStreams: i } = AD(Ek.TRANSCRIPTION, { room: a?.room });
  return L.useMemo(
    () => i.filter(
      (s) => e ? e.includes(s.participantInfo.identity) : !0
    ).filter(
      (s) => t ? t.includes(
        s.streamInfo.attributes?.[Wr.TranscribedTrackId] ?? ""
      ) : !0
    ),
    [i, e, t]
  );
}
var gl = { exports: {} }, U0;
function DD() {
  if (U0) return gl.exports;
  U0 = 1;
  var a = typeof Reflect == "object" ? Reflect : null, e = a && typeof a.apply == "function" ? a.apply : function(C, E, M) {
    return Function.prototype.apply.call(C, E, M);
  }, t;
  a && typeof a.ownKeys == "function" ? t = a.ownKeys : Object.getOwnPropertySymbols ? t = function(C) {
    return Object.getOwnPropertyNames(C).concat(Object.getOwnPropertySymbols(C));
  } : t = function(C) {
    return Object.getOwnPropertyNames(C);
  };
  function i(C) {
    console && console.warn && console.warn(C);
  }
  var s = Number.isNaN || function(C) {
    return C !== C;
  };
  function c() {
    c.init.call(this);
  }
  gl.exports = c, gl.exports.once = A, c.EventEmitter = c, c.prototype._events = void 0, c.prototype._eventsCount = 0, c.prototype._maxListeners = void 0;
  var u = 10;
  function d(C) {
    if (typeof C != "function")
      throw new TypeError('The "listener" argument must be of type Function. Received type ' + typeof C);
  }
  Object.defineProperty(c, "defaultMaxListeners", {
    enumerable: !0,
    get: function() {
      return u;
    },
    set: function(C) {
      if (typeof C != "number" || C < 0 || s(C))
        throw new RangeError('The value of "defaultMaxListeners" is out of range. It must be a non-negative number. Received ' + C + ".");
      u = C;
    }
  }), c.init = function() {
    (this._events === void 0 || this._events === Object.getPrototypeOf(this)._events) && (this._events = /* @__PURE__ */ Object.create(null), this._eventsCount = 0), this._maxListeners = this._maxListeners || void 0;
  }, c.prototype.setMaxListeners = function(C) {
    if (typeof C != "number" || C < 0 || s(C))
      throw new RangeError('The value of "n" is out of range. It must be a non-negative number. Received ' + C + ".");
    return this._maxListeners = C, this;
  };
  function h(C) {
    return C._maxListeners === void 0 ? c.defaultMaxListeners : C._maxListeners;
  }
  c.prototype.getMaxListeners = function() {
    return h(this);
  }, c.prototype.emit = function(C) {
    for (var E = [], M = 1; M < arguments.length; M++) E.push(arguments[M]);
    var N = C === "error", G = this._events;
    if (G !== void 0)
      N = N && G.error === void 0;
    else if (!N)
      return !1;
    if (N) {
      var W;
      if (E.length > 0 && (W = E[0]), W instanceof Error)
        throw W;
      var Y = new Error("Unhandled error." + (W ? " (" + W.message + ")" : ""));
      throw Y.context = W, Y;
    }
    var J = G[C];
    if (J === void 0)
      return !1;
    if (typeof J == "function")
      e(J, this, E);
    else
      for (var ne = J.length, de = _(J, ne), M = 0; M < ne; ++M)
        e(de[M], this, E);
    return !0;
  };
  function f(C, E, M, N) {
    var G, W, Y;
    if (d(M), W = C._events, W === void 0 ? (W = C._events = /* @__PURE__ */ Object.create(null), C._eventsCount = 0) : (W.newListener !== void 0 && (C.emit(
      "newListener",
      E,
      M.listener ? M.listener : M
    ), W = C._events), Y = W[E]), Y === void 0)
      Y = W[E] = M, ++C._eventsCount;
    else if (typeof Y == "function" ? Y = W[E] = N ? [M, Y] : [Y, M] : N ? Y.unshift(M) : Y.push(M), G = h(C), G > 0 && Y.length > G && !Y.warned) {
      Y.warned = !0;
      var J = new Error("Possible EventEmitter memory leak detected. " + Y.length + " " + String(E) + " listeners added. Use emitter.setMaxListeners() to increase limit");
      J.name = "MaxListenersExceededWarning", J.emitter = C, J.type = E, J.count = Y.length, i(J);
    }
    return C;
  }
  c.prototype.addListener = function(C, E) {
    return f(this, C, E, !1);
  }, c.prototype.on = c.prototype.addListener, c.prototype.prependListener = function(C, E) {
    return f(this, C, E, !0);
  };
  function p() {
    if (!this.fired)
      return this.target.removeListener(this.type, this.wrapFn), this.fired = !0, arguments.length === 0 ? this.listener.call(this.target) : this.listener.apply(this.target, arguments);
  }
  function v(C, E, M) {
    var N = { fired: !1, wrapFn: void 0, target: C, type: E, listener: M }, G = p.bind(N);
    return G.listener = M, N.wrapFn = G, G;
  }
  c.prototype.once = function(C, E) {
    return d(E), this.on(C, v(this, C, E)), this;
  }, c.prototype.prependOnceListener = function(C, E) {
    return d(E), this.prependListener(C, v(this, C, E)), this;
  }, c.prototype.removeListener = function(C, E) {
    var M, N, G, W, Y;
    if (d(E), N = this._events, N === void 0)
      return this;
    if (M = N[C], M === void 0)
      return this;
    if (M === E || M.listener === E)
      --this._eventsCount === 0 ? this._events = /* @__PURE__ */ Object.create(null) : (delete N[C], N.removeListener && this.emit("removeListener", C, M.listener || E));
    else if (typeof M != "function") {
      for (G = -1, W = M.length - 1; W >= 0; W--)
        if (M[W] === E || M[W].listener === E) {
          Y = M[W].listener, G = W;
          break;
        }
      if (G < 0)
        return this;
      G === 0 ? M.shift() : k(M, G), M.length === 1 && (N[C] = M[0]), N.removeListener !== void 0 && this.emit("removeListener", C, Y || E);
    }
    return this;
  }, c.prototype.off = c.prototype.removeListener, c.prototype.removeAllListeners = function(C) {
    var E, M, N;
    if (M = this._events, M === void 0)
      return this;
    if (M.removeListener === void 0)
      return arguments.length === 0 ? (this._events = /* @__PURE__ */ Object.create(null), this._eventsCount = 0) : M[C] !== void 0 && (--this._eventsCount === 0 ? this._events = /* @__PURE__ */ Object.create(null) : delete M[C]), this;
    if (arguments.length === 0) {
      var G = Object.keys(M), W;
      for (N = 0; N < G.length; ++N)
        W = G[N], W !== "removeListener" && this.removeAllListeners(W);
      return this.removeAllListeners("removeListener"), this._events = /* @__PURE__ */ Object.create(null), this._eventsCount = 0, this;
    }
    if (E = M[C], typeof E == "function")
      this.removeListener(C, E);
    else if (E !== void 0)
      for (N = E.length - 1; N >= 0; N--)
        this.removeListener(C, E[N]);
    return this;
  };
  function y(C, E, M) {
    var N = C._events;
    if (N === void 0)
      return [];
    var G = N[E];
    return G === void 0 ? [] : typeof G == "function" ? M ? [G.listener || G] : [G] : M ? x(G) : _(G, G.length);
  }
  c.prototype.listeners = function(C) {
    return y(this, C, !0);
  }, c.prototype.rawListeners = function(C) {
    return y(this, C, !1);
  }, c.listenerCount = function(C, E) {
    return typeof C.listenerCount == "function" ? C.listenerCount(E) : T.call(C, E);
  }, c.prototype.listenerCount = T;
  function T(C) {
    var E = this._events;
    if (E !== void 0) {
      var M = E[C];
      if (typeof M == "function")
        return 1;
      if (M !== void 0)
        return M.length;
    }
    return 0;
  }
  c.prototype.eventNames = function() {
    return this._eventsCount > 0 ? t(this._events) : [];
  };
  function _(C, E) {
    for (var M = new Array(E), N = 0; N < E; ++N)
      M[N] = C[N];
    return M;
  }
  function k(C, E) {
    for (; E + 1 < C.length; E++)
      C[E] = C[E + 1];
    C.pop();
  }
  function x(C) {
    for (var E = new Array(C.length), M = 0; M < E.length; ++M)
      E[M] = C[M].listener || C[M];
    return E;
  }
  function A(C, E) {
    return new Promise(function(M, N) {
      function G(Y) {
        C.removeListener(E, W), N(Y);
      }
      function W() {
        typeof C.removeListener == "function" && C.removeListener("error", G), M([].slice.call(arguments));
      }
      ie(C, E, W, { once: !0 }), E !== "error" && B(C, G, { once: !0 });
    });
  }
  function B(C, E, M) {
    typeof C.on == "function" && ie(C, "error", E, M);
  }
  function ie(C, E, M, N) {
    if (typeof C.on == "function")
      N.once ? C.once(E, M) : C.on(E, M);
    else if (typeof C.addEventListener == "function")
      C.addEventListener(E, function G(W) {
        N.once && C.removeEventListener(E, G), M(W);
      });
    else
      throw new TypeError('The "emitter" argument must be of type EventEmitter. Received type ' + typeof C);
  }
  return gl.exports;
}
var qm = DD();
const PD = 2e4, vn = (a) => ({
  isConnected: a === "listening" || a === "thinking" || a === "speaking",
  canListen: a === "pre-connect-buffering" || a === "listening" || a === "thinking" || a === "speaking",
  isFinished: a === "disconnected" || a === "failed",
  isPending: a === "connecting" || a === "initializing" || a === "idle"
}), xD = () => {
  const [a, e] = L.useState(
    null
  ), [t, i] = L.useState(
    null
  ), s = L.useRef("connecting"), c = L.useRef(!1), u = (d) => setTimeout(() => {
    if (!c.current) {
      e("Agent did not join the room.");
      return;
    }
    const { isConnected: h } = vn(s.current);
    if (!h) {
      e("Agent joined the room but did not complete initializing.");
      return;
    }
  }, d ?? PD);
  return {
    agentTimeoutFailureReason: a,
    startAgentTimeout: L.useCallback(
      (d) => {
        t && clearTimeout(t), e(null), i(u(d)), s.current = "connecting", c.current = !1;
      },
      [t]
    ),
    clearAgentTimeout: L.useCallback(() => {
      t && clearTimeout(t), e(null), i(null), s.current = "connecting", c.current = !1;
    }, [t]),
    clearAgentTimeoutFailureReason: L.useCallback(() => {
      e(null);
    }, []),
    updateAgentTimeoutState: L.useCallback((d) => {
      s.current = d;
    }, []),
    updateAgentTimeoutParticipantExists: L.useCallback((d) => {
      c.current = d;
    }, [])
  };
};
function ND(a, e) {
  const t = L.useRef(e);
  L.useEffect(() => {
    t.current = e;
  }, [e]);
  const i = L.useCallback(
    async (u) => {
      const { isConnected: d } = vn(t.current);
      if (!d)
        return new Promise((h, f) => {
          const p = (T) => {
            const { isConnected: _ } = vn(T);
            _ && (y(), h());
          }, v = () => {
            y(), f(new Error("useAgent(/* ... */).waitUntilConnected - signal aborted"));
          }, y = () => {
            a.off("stateChanged", p), u?.removeEventListener("abort", v);
          };
          a.on("stateChanged", p), u?.addEventListener("abort", v);
        });
    },
    [a]
  ), s = L.useCallback(
    async (u) => {
      const { canListen: d } = vn(t.current);
      if (!d)
        return new Promise((h, f) => {
          const p = (T) => {
            const { canListen: _ } = vn(T);
            _ && (y(), h());
          }, v = () => {
            y(), f(new Error("useAgent(/* ... */).waitUntilCouldBeListening - signal aborted"));
          }, y = () => {
            a.off("stateChanged", p), u?.removeEventListener("abort", v);
          };
          a.on("stateChanged", p), u?.addEventListener("abort", v);
        });
    },
    [a]
  ), c = L.useCallback(
    async (u) => {
      const { isFinished: d } = vn(t.current);
      if (!d)
        return new Promise((h, f) => {
          const p = (T) => {
            const { isFinished: _ } = vn(T);
            _ && (y(), h());
          }, v = () => {
            y(), f(new Error("useAgent(/* ... */).waitUntilFinished - signal aborted"));
          }, y = () => {
            a.off("stateChanged", p), u?.removeEventListener("abort", v);
          };
          a.on("stateChanged", p), u?.addEventListener("abort", v);
        });
    },
    [a]
  );
  return { waitUntilConnected: i, waitUntilCouldBeListening: s, waitUntilFinished: c };
}
function Hm(a) {
  const e = Mk();
  if (a = a ?? e, !a)
    throw new Error(
      "No session provided, make sure you are inside a Session context or pass the session explicitly"
    );
  const {
    room: t,
    internal: {
      agentConnectTimeoutMilliseconds: i,
      agentTimeoutFailureReason: s,
      startAgentTimeout: c,
      clearAgentTimeout: u,
      clearAgentTimeoutFailureReason: d,
      updateAgentTimeoutState: h,
      updateAgentTimeoutParticipantExists: f
    }
  } = a, p = L.useMemo(() => new qm.EventEmitter(), []), v = xk({ room: t }), y = L.useMemo(() => v.find(
    (w) => w.kind === ka.AGENT && !(Wr.PublishOnBehalf in w.attributes)
  ) ?? null, [v]), T = L.useMemo(() => y ? v.find(
    (w) => w.kind === ka.AGENT && w.attributes[Wr.PublishOnBehalf] === y.identity
  ) ?? null : null, [y, v]), _ = y?.attributes ?? {}, k = L0([H.Source.Camera, H.Source.Microphone], {
    room: t,
    participantIdentity: y?.identity
  }), x = L0([H.Source.Camera, H.Source.Microphone], {
    room: t,
    participantIdentity: T?.identity
  }), A = L.useMemo(
    () => k.find((w) => w.source === H.Source.Camera) ?? x.find((w) => w.source === H.Source.Camera),
    [k, x]
  );
  L.useEffect(() => {
    p.emit("cameraChanged", A);
  }, [p, A]);
  const B = L.useMemo(
    () => k.find((w) => w.source === H.Source.Microphone) ?? x.find((w) => w.source === H.Source.Microphone),
    [k, x]
  );
  L.useEffect(() => {
    p.emit("microphoneChanged", B);
  }, [p, B]);
  const [ie, C] = L.useState(t.state);
  L.useEffect(() => {
    const w = (K) => {
      C(K);
    };
    return t.on(j.ConnectionStateChanged, w), () => {
      t.off(j.ConnectionStateChanged, w);
    };
  }, [t]), L.useEffect(() => {
    y && d();
  }, [y]);
  const [E, M] = L.useState(null);
  L.useEffect(() => {
    if (!y)
      return;
    const w = (K) => {
      K.identity === y?.identity && M("Agent left the room unexpectedly.");
    };
    return t.on(j.ParticipantDisconnected, w), () => {
      t.off(j.ParticipantDisconnected, w);
    };
  }, [y, t]), L.useEffect(() => {
    ie === Te.Disconnected && M(null);
  }, [ie]);
  const [N, G] = L.useState(
    () => t.localParticipant.getTrackPublication(H.Source.Microphone) ?? null
  );
  L.useEffect(() => {
    const w = () => {
      G(t.localParticipant.getTrackPublication(H.Source.Microphone) ?? null);
    }, K = () => {
      G(null);
    };
    return t.localParticipant.on(
      X.LocalTrackPublished,
      w
    ), t.localParticipant.on(
      X.LocalTrackUnpublished,
      K
    ), () => {
      t.localParticipant.off(
        X.LocalTrackPublished,
        w
      ), t.localParticipant.off(
        X.LocalTrackUnpublished,
        K
      );
    };
  }, [t.localParticipant]);
  const W = L.useMemo(() => {
    const w = [];
    return s && w.push(s), E && w.push(E), w;
  }, [s, E]), Y = L.useMemo(() => {
    if (W.length > 0)
      return "failed";
    let w = "disconnected";
    return ie !== Te.Disconnected && (w = "connecting"), N && (w = "pre-connect-buffering"), y && _[Wr.AgentState] && (w = _[Wr.AgentState]), w;
  }, [
    W,
    ie,
    N,
    y,
    _
  ]);
  L.useEffect(() => {
    p.emit("stateChanged", Y), h(Y);
  }, [p, Y]), L.useEffect(() => {
    f(y !== null);
  }, [y]);
  const J = a.connectionState === "disconnected";
  L.useEffect(() => {
    if (!J)
      return c(i), () => {
        u();
      };
  }, [J, i]);
  const {
    identity: ne,
    name: de,
    metadata: fe
  } = CD({ participant: y ?? void 0 }), ke = L.useMemo(() => {
    const w = {
      attributes: _,
      internal: {
        agentParticipant: y,
        workerParticipant: T,
        emitter: p
      }
    };
    switch (Y) {
      case "disconnected":
        return {
          ...w,
          identity: void 0,
          name: void 0,
          metadata: void 0,
          state: Y,
          ...vn(Y),
          failureReasons: null,
          // Clear inner values if no longer connected
          cameraTrack: void 0,
          microphoneTrack: void 0
        };
      case "connecting":
        return {
          ...w,
          identity: void 0,
          name: void 0,
          metadata: void 0,
          state: Y,
          ...vn(Y),
          failureReasons: null,
          // Clear inner values if no longer connected
          cameraTrack: void 0,
          microphoneTrack: void 0
        };
      case "initializing":
      case "idle":
        return {
          ...w,
          identity: ne,
          name: de,
          metadata: fe,
          state: Y,
          ...vn(Y),
          failureReasons: null,
          cameraTrack: A,
          microphoneTrack: B
        };
      case "pre-connect-buffering":
        return {
          ...w,
          identity: ne,
          name: de,
          metadata: fe,
          state: Y,
          ...vn(Y),
          failureReasons: null,
          cameraTrack: A,
          microphoneTrack: B
        };
      case "listening":
      case "thinking":
      case "speaking":
        return {
          ...w,
          identity: ne,
          name: de,
          metadata: fe,
          state: Y,
          ...vn(Y),
          failureReasons: null,
          cameraTrack: A,
          microphoneTrack: B
        };
      case "failed":
        return {
          ...w,
          identity: void 0,
          name: void 0,
          metadata: void 0,
          state: "failed",
          ...vn("failed"),
          failureReasons: W,
          // Clear inner values if no longer connected
          cameraTrack: void 0,
          microphoneTrack: void 0
        };
    }
  }, [
    ne,
    de,
    fe,
    _,
    p,
    y,
    Y,
    A,
    B
  ]), { waitUntilConnected: q, waitUntilCouldBeListening: te, waitUntilFinished: le } = ND(p, Y), Me = L.useCallback(
    (w) => new Promise((K, se) => {
      const ce = (ue) => {
        ue && (oe(), K(ue));
      }, pe = () => {
        oe(), se(new Error("useAgent(/* ... */).waitUntilCamera - signal aborted"));
      }, oe = () => {
        p.off("cameraChanged", ce), w?.removeEventListener("abort", pe);
      };
      p.on("cameraChanged", ce), w?.addEventListener("abort", pe);
    }),
    [p]
  ), Ae = L.useCallback(
    (w) => new Promise((K, se) => {
      const ce = (ue) => {
        ue && (oe(), K(ue));
      }, pe = () => {
        oe(), se(new Error("useAgent(/* ... */).waitUntilMicrophone - signal aborted"));
      }, oe = () => {
        p.off("microphoneChanged", ce), w?.removeEventListener("abort", pe);
      };
      p.on("microphoneChanged", ce), w?.addEventListener("abort", pe);
    }),
    [p]
  );
  return L.useMemo(() => ({
    ...ke,
    waitUntilConnected: q,
    waitUntilCouldBeListening: te,
    waitUntilFinished: le,
    waitUntilCamera: Me,
    waitUntilMicrophone: Ae
  }), [
    ke,
    q,
    te,
    le,
    Me,
    Ae
  ]);
}
function ID(a, e) {
  const t = /* @__PURE__ */ new Set([...Object.keys(a), ...Object.keys(e)]);
  for (const i of t)
    switch (i) {
      case "roomName":
      case "participantName":
      case "participantIdentity":
      case "participantMetadata":
      case "participantAttributes":
      case "agentName":
      case "agentMetadata":
        if (a[i] !== e[i])
          return !1;
        break;
      default:
        const s = i;
        throw new Error(`Options key ${s} not being checked for equality!`);
    }
  return !0;
}
function LD(a, e) {
  const t = L.useRef(e);
  return L.useEffect(() => {
    t.current = e;
  }, [e]), L.useCallback(
    async (i, s) => {
      if (t.current !== i)
        return new Promise((c, u) => {
          const d = (p) => {
            p === i && (f(), c());
          }, h = () => {
            f(), u(
              new Error(
                `useSession(/* ... */).waitUntilConnectionState(${i}, /* signal */) - signal aborted`
              )
            );
          }, f = () => {
            a.off("connectionStateChanged", d), s?.removeEventListener("abort", h);
          };
          a.on("connectionStateChanged", d), s?.addEventListener("abort", h);
        });
    },
    [a]
  );
}
function UD(a, e) {
  const t = a instanceof ek, i = L.useRef(
    t ? e : null
  );
  return L.useEffect(() => {
    if (!t) {
      i.current = null;
      return;
    }
    i.current !== null && ID(i.current, e) || (i.current = e);
  }, [t, e]), L.useCallback(
    async (s) => {
      if (t) {
        if (!i.current)
          throw new Error(
            "AgentSession - memoized token fetch options are not set, but the passed tokenSource was an instance of TokenSourceConfigurable. If you are seeing this please make a new GitHub issue!"
          );
        return a.fetch(i.current, s);
      } else
        return a.fetch();
    },
    [t, a]
  );
}
function BD(a, e = {}) {
  const {
    room: t,
    agentConnectTimeoutMilliseconds: i,
    encryption: s,
    ...c
  } = e, u = s && "e2eeManager" in s ? s.e2eeManager : null, d = s && !("e2eeManager" in s) ? s.key ?? null : null, h = s && !("e2eeManager" in s) ? s.worker ?? null : null, f = Fm(), p = L.useRef(null), v = L.useMemo(() => typeof d == "string" || d instanceof ArrayBuffer ? (p.current === null && (p.current = new Fw()), p.current.setKey(d).catch((oe) => xn.error(oe)), p.current) : d, [d]), y = L.useMemo(() => {
    const oe = f ?? t;
    if (oe)
      return oe;
    const ue = !!(v && h), Be = !!u, je = ue || Be, jt = {};
    ue ? jt.encryption = {
      keyProvider: v,
      worker: h
    } : Be ? jt.encryption = {
      e2eeManager: u
    } : s !== void 0 && xn.warn(
      "useSession options encryption was set, but neither encryption.key with encryption.worker nor encryption.e2eeManager was provided."
    );
    const li = new ps(jt);
    return je && li.setE2EEEnabled(!0), li;
  }, [f, t, v, h, u]);
  L.useEffect(() => () => {
    y.disconnect();
  }, [y]);
  const T = L.useMemo(
    () => new qm.EventEmitter(),
    []
  ), _ = L.useCallback(
    (oe) => ({
      isConnected: oe === Te.Connected || oe === Te.Reconnecting || oe === Te.SignalReconnecting
    }),
    []
  ), [k, x] = L.useState(y.state);
  L.useEffect(() => {
    const oe = (ue) => {
      x(ue);
    };
    return y.on(j.ConnectionStateChanged, oe), () => {
      y.off(j.ConnectionStateChanged, oe);
    };
  }, [y]), L.useEffect(() => {
    const oe = async (ue) => {
      T.emit("mediaDevicesError", ue);
    };
    return y.on(j.MediaDevicesError, oe), () => {
      y.off(j.MediaDevicesError, oe);
    };
  }, [y, T]), L.useEffect(() => {
    const oe = async (ue) => {
      T.emit("encryptionError", ue);
    };
    return y.on(j.EncryptionError, oe), () => {
      y.off(j.EncryptionError, oe);
    };
  }, [y, T]);
  const { localParticipant: A } = Pk({ room: y }), B = A.getTrackPublication(H.Source.Camera), ie = L.useMemo(() => {
    if (B)
      return {
        source: H.Source.Camera,
        participant: A,
        publication: B
      };
  }, [A, B]), C = A.getTrackPublication(H.Source.Microphone), E = L.useMemo(() => {
    if (C)
      return {
        source: H.Source.Microphone,
        participant: A,
        publication: C
      };
  }, [A, C]), M = A.getTrackPublication(H.Source.ScreenShare), N = L.useMemo(() => {
    if (M)
      return {
        source: H.Source.ScreenShare,
        participant: A,
        publication: M
      };
  }, [A, M]), {
    agentTimeoutFailureReason: G,
    startAgentTimeout: W,
    clearAgentTimeout: Y,
    clearAgentTimeoutFailureReason: J,
    updateAgentTimeoutState: ne,
    updateAgentTimeoutParticipantExists: de
  } = xD(), fe = L.useMemo(
    () => ({
      emitter: T,
      tokenSource: a,
      agentConnectTimeoutMilliseconds: i,
      agentTimeoutFailureReason: G,
      startAgentTimeout: W,
      clearAgentTimeout: Y,
      clearAgentTimeoutFailureReason: J,
      updateAgentTimeoutState: ne,
      updateAgentTimeoutParticipantExists: de
    }),
    [
      T,
      i,
      a,
      G,
      W,
      Y,
      J,
      ne,
      de
    ]
  ), ke = L.useMemo(() => {
    const oe = {
      room: y,
      internal: fe
    };
    switch (k) {
      case Te.Connecting:
        return {
          ...oe,
          connectionState: Te.Connecting,
          ..._(Te.Connecting),
          local: {
            cameraTrack: void 0,
            microphoneTrack: void 0,
            screenShareTrack: void 0
          }
        };
      case Te.Connected:
      case Te.Reconnecting:
      case Te.SignalReconnecting:
        return {
          ...oe,
          connectionState: k,
          ..._(k),
          local: {
            cameraTrack: ie,
            microphoneTrack: E,
            screenShareTrack: N
          }
        };
      case Te.Disconnected:
        return {
          ...oe,
          connectionState: Te.Disconnected,
          ..._(Te.Disconnected),
          local: {
            cameraTrack: void 0,
            microphoneTrack: void 0,
            screenShareTrack: void 0
          }
        };
    }
  }, [
    fe,
    y,
    k,
    ie,
    E,
    _
  ]);
  L.useEffect(() => {
    T.emit("connectionStateChanged", ke.connectionState);
  }, [T, ke.connectionState]);
  const q = LD(
    T,
    ke.connectionState
  ), te = L.useCallback(
    async (oe) => q(
      Te.Connected,
      oe
    ),
    [q]
  ), le = L.useCallback(
    async (oe) => q(Te.Disconnected, oe),
    [q]
  ), Me = L.useCallback(
    async (oe) => y.setE2EEEnabled(oe),
    [y]
  ), Ae = Hm(
    L.useMemo(
      () => ({
        connectionState: ke.connectionState,
        room: y,
        internal: fe
      }),
      [ke, y, fe]
    )
  ), w = UD(a, c), K = L.useRef(!1), se = L.useCallback(
    async (oe = {}) => {
      const {
        signal: ue,
        tracks: Be = { microphone: { enabled: !0, publishOptions: { preConnectBuffer: !0 } } },
        roomConnectOptions: je
      } = oe;
      await le(ue), K.current = !1;
      const jt = () => {
        y.disconnect();
      };
      ue?.addEventListener("abort", jt);
      const li = () => {
        K.current || w(!0);
      };
      y.once(j.Disconnected, li);
      let cr = !1;
      await Promise.all([
        w().then(({ serverUrl: Vo, participantToken: Wn }) => (cr = (FA(Wn).roomConfig?.agents?.length ?? 0) > 0, y.connect(Vo, Wn, je))),
        // Start microphone (with preconnect buffer) by default
        Be.microphone?.enabled ? y.localParticipant.setMicrophoneEnabled(
          !0,
          void 0,
          Be.microphone?.publishOptions ?? {}
        ) : Promise.resolve(),
        Be.camera?.enabled ? y.localParticipant.setCameraEnabled(
          !0,
          void 0,
          Be.camera?.publishOptions ?? {}
        ) : Promise.resolve(),
        Be.screenShare?.enabled ? y.localParticipant.setScreenShareEnabled(
          !0,
          void 0,
          Be.screenShare?.publishOptions ?? {}
        ) : Promise.resolve()
      ]), await te(ue), cr && await Ae.waitUntilConnected(ue), ue?.removeEventListener("abort", jt);
    },
    [y, le, w, te, Ae.waitUntilConnected]
  ), ce = L.useCallback(async () => {
    K.current = !0, w(!0), await y.disconnect();
  }, [y, w]), pe = L.useCallback(async () => {
    const oe = await w();
    await y.prepareConnection(oe.serverUrl, oe.participantToken);
  }, [w, y]);
  return L.useEffect(
    () => {
      pe().catch((oe) => {
        console.warn("WARNING: Room.prepareConnection failed:", oe);
      });
    },
    [
      /* note: no prepareConnection here, this effect should only ever run once! */
    ]
  ), L.useMemo(
    () => ({
      ...ke,
      waitUntilConnected: te,
      waitUntilDisconnected: le,
      prepareConnection: pe,
      start: se,
      end: ce,
      setEncryptionEnabled: Me
    }),
    [
      ke,
      te,
      le,
      pe,
      se,
      ce,
      Me
    ]
  );
}
function zD(a) {
  const { room: e } = yD(a), t = L.useMemo(
    () => new qm.EventEmitter(),
    []
  ), i = Hm(a), s = OD({ room: e }), c = L.useMemo(() => ({ room: e }), [e]), u = Nk(c), d = L.useMemo(() => s.map((y) => {
    switch (y.participantInfo.identity) {
      case e.localParticipant.identity:
        return {
          type: "userTranscript",
          message: y.text,
          id: y.streamInfo.id,
          timestamp: y.streamInfo.timestamp,
          from: e.localParticipant
        };
      case i.internal.agentParticipant?.identity:
      case i.internal.workerParticipant?.identity:
        return {
          type: "agentTranscript",
          message: y.text,
          id: y.streamInfo.id,
          timestamp: y.streamInfo.timestamp,
          from: i.internal.agentParticipant?.identity === y.participantInfo.identity ? i.internal.agentParticipant : i.internal.workerParticipant
        };
      default:
        return {
          type: "agentTranscript",
          message: y.text,
          id: y.streamInfo.id,
          timestamp: y.streamInfo.timestamp,
          from: Array.from(e.remoteParticipants.values()).find(
            (T) => T.identity === y.participantInfo.identity
          )
        };
    }
  }), [s, e]), h = L.useMemo(() => [...d, ...u.chatMessages], [d, u.chatMessages]), f = L.useRef(/* @__PURE__ */ new Map()), p = L.useMemo(() => {
    const y = /* @__PURE__ */ new Date();
    for (const T of h)
      f.current.has(T.id) || f.current.set(T.id, y);
    return h.sort((T, _) => {
      const k = f.current.get(T.id), x = f.current.get(_.id);
      return typeof k > "u" || typeof x > "u" ? 0 : k.getTime() - x.getTime();
    });
  }, [h]), v = L.useRef(/* @__PURE__ */ new Set());
  return L.useEffect(() => {
    for (const y of p)
      v.current.has(y.id) || (v.current.add(y.id), t.emit("messageReceived", y));
  }, [p]), L.useMemo(
    () => ({
      messages: p,
      send: u.send,
      isSending: u.isSending,
      internal: { emitter: t }
    }),
    [p, u.send, u.isSending]
  );
}
function FD(a, e = {}) {
  const [t, i] = L.useState(Qh(a)), [s, c] = L.useState(t?.isMuted), [u, d] = L.useState(t?.isSubscribed), [h, f] = L.useState(t?.track), [p, v] = L.useState("landscape"), y = L.useRef(), { className: T, trackObserver: _ } = L.useMemo(() => JO(a), [
    a.participant.sid ?? a.participant.identity,
    a.source,
    Lm(a) && a.publication.trackSid
  ]);
  return L.useEffect(() => {
    const k = _.subscribe((x) => {
      xn.debug("update track", x), i(x), c(x?.isMuted), d(x?.isSubscribed), f(x?.track);
    });
    return () => k?.unsubscribe();
  }, [_]), L.useEffect(() => (h && (y.current && h.detach(y.current), e.element?.current && !(a.participant.isLocal && h?.kind === "audio") && h.attach(e.element.current)), y.current = e.element?.current, () => {
    y.current && h?.detach(y.current);
  }), [h, e.element]), L.useEffect(() => {
    if (typeof t?.dimensions?.width == "number" && typeof t?.dimensions?.height == "number") {
      const k = t.dimensions.width > t.dimensions.height ? "landscape" : "portrait";
      v(k);
    }
  }, [t]), {
    publication: t,
    isMuted: s,
    isSubscribed: u,
    track: h,
    elementProps: kD(e.props, {
      className: T,
      "data-lk-local-participant": a.participant.isLocal,
      "data-lk-source": t?.source,
      ...t?.kind === "video" && { "data-lk-orientation": p }
    })
  };
}
var ph, B0;
function jD() {
  if (B0) return ph;
  B0 = 1;
  var a = "Expected a function", e = NaN, t = "[object Symbol]", i = /^\s+|\s+$/g, s = /^[-+]0x[0-9a-f]+$/i, c = /^0b[01]+$/i, u = /^0o[0-7]+$/i, d = parseInt, h = typeof fl == "object" && fl && fl.Object === Object && fl, f = typeof self == "object" && self && self.Object === Object && self, p = h || f || Function("return this")(), v = Object.prototype, y = v.toString, T = Math.max, _ = Math.min, k = function() {
    return p.Date.now();
  };
  function x(E, M, N) {
    var G, W, Y, J, ne, de, fe = 0, ke = !1, q = !1, te = !0;
    if (typeof E != "function")
      throw new TypeError(a);
    M = C(M) || 0, A(N) && (ke = !!N.leading, q = "maxWait" in N, Y = q ? T(C(N.maxWait) || 0, M) : Y, te = "trailing" in N ? !!N.trailing : te);
    function le(ue) {
      var Be = G, je = W;
      return G = W = void 0, fe = ue, J = E.apply(je, Be), J;
    }
    function Me(ue) {
      return fe = ue, ne = setTimeout(K, M), ke ? le(ue) : J;
    }
    function Ae(ue) {
      var Be = ue - de, je = ue - fe, jt = M - Be;
      return q ? _(jt, Y - je) : jt;
    }
    function w(ue) {
      var Be = ue - de, je = ue - fe;
      return de === void 0 || Be >= M || Be < 0 || q && je >= Y;
    }
    function K() {
      var ue = k();
      if (w(ue))
        return se(ue);
      ne = setTimeout(K, Ae(ue));
    }
    function se(ue) {
      return ne = void 0, te && G ? le(ue) : (G = W = void 0, J);
    }
    function ce() {
      ne !== void 0 && clearTimeout(ne), fe = 0, G = de = W = ne = void 0;
    }
    function pe() {
      return ne === void 0 ? J : se(k());
    }
    function oe() {
      var ue = k(), Be = w(ue);
      if (G = arguments, W = this, de = ue, Be) {
        if (ne === void 0)
          return Me(de);
        if (q)
          return ne = setTimeout(K, M), le(de);
      }
      return ne === void 0 && (ne = setTimeout(K, M)), J;
    }
    return oe.cancel = ce, oe.flush = pe, oe;
  }
  function A(E) {
    var M = typeof E;
    return !!E && (M == "object" || M == "function");
  }
  function B(E) {
    return !!E && typeof E == "object";
  }
  function ie(E) {
    return typeof E == "symbol" || B(E) && y.call(E) == t;
  }
  function C(E) {
    if (typeof E == "number")
      return E;
    if (ie(E))
      return e;
    if (A(E)) {
      var M = typeof E.valueOf == "function" ? E.valueOf() : E;
      E = A(M) ? M + "" : M;
    }
    if (typeof E != "string")
      return E === 0 ? E : +E;
    E = E.replace(i, "");
    var N = c.test(E);
    return N || u.test(E) ? d(E.slice(2), N ? 2 : 8) : s.test(E) ? e : +E;
  }
  return ph = x, ph;
}
jD();
const qD = /* @__PURE__ */ L.forwardRef(
  function({ trackRef: a, onSubscriptionStatusChanged: e, volume: t, ...i }, s) {
    const c = gD(a), u = L.useRef(null);
    L.useImperativeHandle(s, () => u.current);
    const {
      elementProps: d,
      isSubscribed: h,
      track: f,
      publication: p
    } = FD(c, {
      element: u,
      props: i
    });
    return L.useEffect(() => {
      e?.(!!h);
    }, [h, e]), L.useEffect(() => {
      f === void 0 || t === void 0 || (f instanceof XS ? f.setVolume(t) : xn.warn("Volume can only be set on remote audio tracks."));
    }, [t, f]), L.useEffect(() => {
      p === void 0 || i.muted === void 0 || (p instanceof WS ? p.setEnabled(!i.muted) : xn.warn("Can only call setEnabled on remote track publications."));
    }, [i.muted, p, f]), /* @__PURE__ */ L.createElement("audio", { ref: u, ...d });
  }
);
function HD({ room: a, volume: e, muted: t }) {
  const i = RD(
    [H.Source.Microphone, H.Source.ScreenShareAudio, H.Source.Unknown],
    {
      updateOnlyOn: [],
      onlySubscribed: !0,
      room: a
    }
  ).filter((s) => !s.participant.isLocal && s.publication.kind === H.Kind.Audio);
  return /* @__PURE__ */ L.createElement("div", { style: { display: "none" } }, i.map((s) => /* @__PURE__ */ L.createElement(
    qD,
    {
      key: VO(s),
      trackRef: s,
      volume: e,
      muted: t
    }
  )));
}
function VD(a) {
  return /* @__PURE__ */ L.createElement(jm.Provider, { value: a.session }, /* @__PURE__ */ L.createElement(wk.Provider, { value: a.session.room }, a.children));
}
function GD() {
  const { send: a } = Nk(), [e, t] = L.useState(""), [i, s] = L.useState(!1), c = i || e.trim().length === 0, u = async () => {
    if (!c) {
      s(!0);
      try {
        await a(e.trim()), t("");
      } catch (d) {
        console.error("chat send failed", d);
      } finally {
        s(!1);
      }
    }
  };
  return /* @__PURE__ */ Se.jsxs("div", { className: "lk-input", children: [
    /* @__PURE__ */ Se.jsx(
      "textarea",
      {
        value: e,
        placeholder: "Type a message…",
        onChange: (d) => t(d.target.value),
        onKeyDown: (d) => {
          d.key === "Enter" && !d.shiftKey && (d.preventDefault(), u());
        }
      }
    ),
    /* @__PURE__ */ Se.jsx("button", { className: "lk-btn", disabled: c, onClick: u, children: i ? "…" : "Send" })
  ] });
}
const Ik = L.createContext(null), KD = Ik.Provider;
function bu() {
  const a = L.useContext(Ik);
  if (!a) throw new Error("HassStore not provided");
  return a;
}
function Lk() {
  const a = bu();
  return L.useSyncExternalStore(a.subscribe, a.getSnapshot).hass;
}
function Vm() {
  const a = bu();
  return L.useSyncExternalStore(a.subscribe, a.getSnapshot).config;
}
function YD(a, e) {
  a?.dispatchEvent(
    new CustomEvent("hass-more-info", {
      detail: { entityId: e },
      bubbles: !0,
      composed: !0
    })
  );
}
function JD(a) {
  if (!a) return null;
  for (const e of a.remoteParticipants.values())
    if (e.kind === ka.AGENT || e.isAgent) return e.identity;
  return null;
}
function QD() {
  const a = Vm(), e = Ho(), t = a.input_mode === "push_to_talk";
  return /* @__PURE__ */ Se.jsxs("div", { className: "lk-controls", children: [
    t ? /* @__PURE__ */ Se.jsx(ZD, {}) : /* @__PURE__ */ Se.jsx(XD, {}),
    /* @__PURE__ */ Se.jsx("button", { className: "lk-icon-btn lk-danger", title: "End", onClick: () => e.end?.(), children: /* @__PURE__ */ Se.jsx("ha-icon", { icon: "mdi:phone-hangup" }) })
  ] });
}
function XD() {
  const { enabled: a, toggle: e } = _D({ source: H.Source.Microphone });
  return /* @__PURE__ */ Se.jsx(
    "button",
    {
      className: "lk-icon-btn",
      "data-on": a ? "1" : "0",
      title: a ? "Mute microphone" : "Unmute microphone",
      onClick: () => e(),
      style: { flex: 1, borderRadius: 999 },
      children: /* @__PURE__ */ Se.jsx("ha-icon", { icon: a ? "mdi:microphone" : "mdi:microphone-off" })
    }
  );
}
function ZD() {
  const a = Ho(), e = L.useRef(!1), t = async (c) => {
    const u = a.room, d = JD(u);
    if (!(!u || !d))
      try {
        await u.localParticipant.performRpc({ destinationIdentity: d, method: c, payload: "" });
      } catch (h) {
        console.error(`${c} failed`, h);
      }
  }, i = async (c) => {
    c.preventDefault(), !e.current && (e.current = !0, c.currentTarget.dataset.holding = "1", c.currentTarget.setPointerCapture?.(c.pointerId), await a.room?.localParticipant.setMicrophoneEnabled(!0), t("start_turn"));
  }, s = async (c) => {
    e.current && (e.current = !1, c.currentTarget.dataset.holding = "0", await t("end_turn"), await a.room?.localParticipant.setMicrophoneEnabled(!1));
  };
  return /* @__PURE__ */ Se.jsx(
    "button",
    {
      className: "lk-btn lk-talk",
      style: { flex: 1 },
      onPointerDown: i,
      onPointerUp: s,
      onPointerCancel: s,
      onPointerLeave: s,
      children: "Hold to talk"
    }
  );
}
function Xl(a) {
  return a.split(".")[0];
}
function Zl(a, e) {
  return a.states[e]?.attributes?.friendly_name || e;
}
const WD = /* @__PURE__ */ new Set([
  "off",
  "unavailable",
  "unknown",
  "idle",
  "closed",
  "locked",
  "standby",
  "not_home",
  ""
]);
function Xh(a) {
  if (!a) return !1;
  const e = (a.state ?? "").toString().toLowerCase();
  if (WD.has(e)) return !1;
  const t = Number(e);
  return !(e !== "" && !Number.isNaN(t));
}
function $D(a) {
  const e = Xl(a);
  return ["light", "switch", "fan", "input_boolean", "humidifier", "siren", "automation", "cover"].includes(e) ? { domain: e, service: "toggle" } : e === "scene" ? { domain: "scene", service: "turn_on" } : e === "script" ? { domain: "script", service: "turn_on" } : null;
}
const eP = {
  light: "mdi:lightbulb",
  switch: "mdi:toggle-switch-variant",
  fan: "mdi:fan",
  climate: "mdi:thermostat",
  cover: "mdi:window-shutter",
  lock: "mdi:lock",
  media_player: "mdi:cast",
  sensor: "mdi:gauge",
  binary_sensor: "mdi:checkbox-marked-circle-outline",
  scene: "mdi:palette",
  script: "mdi:script-text",
  vacuum: "mdi:robot-vacuum",
  person: "mdi:account",
  automation: "mdi:robot",
  humidifier: "mdi:air-humidifier",
  camera: "mdi:cctv"
};
function tP(a, e) {
  const t = a.states[e];
  return t?.attributes?.icon ? t.attributes.icon : eP[Xl(e)] || "mdi:help-circle-outline";
}
function nP(a) {
  const e = (a.state ?? "").toString(), t = e.toLowerCase();
  if (t === "" || t === "unknown") return "Unknown";
  if (t === "unavailable") return "N/A";
  if (t === "on") return "On";
  if (t === "off") return "Off";
  const i = a.attributes?.unit_of_measurement, s = Number(e);
  if (e !== "" && !Number.isNaN(s)) {
    const c = Number.isInteger(s) ? s : Math.round(s * 10) / 10;
    if (i) {
      const u = i === "%" || i.startsWith("°");
      return `${c}${u ? "" : " "}${i}`;
    }
    return String(c);
  }
  return e.charAt(0).toUpperCase() + e.slice(1);
}
function iP(a, e) {
  const t = new Set(e.map((s) => s.toLowerCase().trim())), i = /* @__PURE__ */ new Set();
  for (const [s, c] of Object.entries(a.areas ?? {})) {
    const u = c?.name?.toLowerCase?.() ?? "";
    (t.has(s.toLowerCase()) || t.has(u)) && i.add(s);
  }
  return i;
}
function z0(a, e) {
  if (!e.length || !a.entities) return [];
  const t = iP(a, e);
  if (!t.size) return [];
  const i = [];
  for (const [s, c] of Object.entries(a.entities)) {
    if (c?.hidden || c?.disabled_by) continue;
    let u = c?.area_id;
    if (!u) {
      const d = c?.device_id;
      d && (u = a.devices?.[d]?.area_id);
    }
    u && t.has(u) && a.states[s] && i.push(s);
  }
  return i;
}
function aP(a, e, t) {
  const i = [], s = /* @__PURE__ */ new Set(), c = (u) => {
    !s.has(u) && a.states[u] && (s.add(u), i.push(u));
  };
  return (e.entities ?? []).forEach(c), z0(a, e.areas ?? []).forEach(c), e.follow_agent !== !1 && z0(a, t).forEach(c), i;
}
const F0 = {
  light: 0,
  climate: 1,
  media_player: 2,
  fan: 3,
  cover: 4,
  lock: 5,
  switch: 6,
  vacuum: 7,
  scene: 9,
  script: 10,
  automation: 10,
  binary_sensor: 20,
  sensor: 21
};
function rP(a, e) {
  return [...e].sort((t, i) => {
    const s = F0[Xl(t)] ?? 15, c = F0[Xl(i)] ?? 15;
    if (s !== c) return s - c;
    const u = Number(Xh(a.states[i])) - Number(Xh(a.states[t]));
    return u !== 0 ? u : Zl(a, t).localeCompare(Zl(a, i));
  });
}
function sP({ agentAreas: a }) {
  const e = Lk(), t = Vm(), i = bu().host;
  if (!e) return null;
  const s = rP(e, aP(e, t, a));
  return s.length ? /* @__PURE__ */ Se.jsx("div", { className: "lk-tiles", children: s.map((c) => /* @__PURE__ */ Se.jsx(oP, { entityId: c, hass: e, host: i }, c)) }) : null;
}
function oP({ entityId: a, hass: e, host: t }) {
  const i = e.states[a], s = L.useRef(!1), c = L.useRef(void 0);
  if (!i) return null;
  const u = Xh(i), d = () => YD(t, a), h = () => {
    if (s.current) {
      s.current = !1;
      return;
    }
    const v = $D(a);
    v ? e.callService(v.domain, v.service, { entity_id: a }) : d();
  }, f = () => {
    s.current = !1, c.current = window.setTimeout(() => {
      s.current = !0, d();
    }, 500);
  }, p = () => window.clearTimeout(c.current);
  return /* @__PURE__ */ Se.jsxs(
    "button",
    {
      className: "lk-tile",
      "data-active": u ? "1" : "0",
      title: Zl(e, a),
      onClick: h,
      onPointerDown: f,
      onPointerUp: p,
      onPointerLeave: p,
      onContextMenu: (v) => {
        v.preventDefault(), d();
      },
      children: [
        /* @__PURE__ */ Se.jsx("div", { className: "lk-tile-top", children: /* @__PURE__ */ Se.jsx("span", { className: "lk-tile-icon", children: /* @__PURE__ */ Se.jsx("ha-icon", { icon: tP(e, a) }) }) }),
        /* @__PURE__ */ Se.jsxs("div", { style: { minWidth: 0 }, children: [
          /* @__PURE__ */ Se.jsx("div", { className: "lk-tile-state", children: nP(i) }),
          /* @__PURE__ */ Se.jsx("div", { className: "lk-tile-name", children: Zl(e, a) })
        ] })
      ]
    }
  );
}
function cP(a) {
  const e = a.replace(/^Hass/, "").replace(/([a-z0-9])([A-Z])/g, "$1 $2").replace(/[_-]+/g, " ").trim();
  return e ? e.charAt(0).toUpperCase() + e.slice(1) : a;
}
function lP(a) {
  return a ? typeof a == "string" ? a : Object.entries(a).map(([e, t]) => `${e}: ${typeof t == "object" ? JSON.stringify(t) : String(t)}`).join(", ") : "";
}
function uP({ toolCalls: a }) {
  if (!a.length) return null;
  const e = a.slice(-4);
  return /* @__PURE__ */ Se.jsx("div", { className: "lk-tools", children: e.map((t) => /* @__PURE__ */ Se.jsxs("div", { className: "lk-tool", children: [
    /* @__PURE__ */ Se.jsx("span", { className: "lk-tool-name", children: cP(t.name) }),
    /* @__PURE__ */ Se.jsx("span", { className: "lk-tool-args", children: lP(t.args) }),
    /* @__PURE__ */ Se.jsx("span", { className: "lk-tool-badge", "data-s": t.status, children: t.status })
  ] }, t.callId)) });
}
function dP() {
  const a = Ho(), { messages: e } = zD(a), t = L.useRef(null);
  return L.useEffect(() => {
    const i = t.current;
    i && (i.scrollTop = i.scrollHeight);
  }, [e]), e?.length ? /* @__PURE__ */ Se.jsx("div", { className: "lk-transcript", ref: t, children: e.map((i) => {
    const s = i.from?.isLocal;
    return /* @__PURE__ */ Se.jsxs("div", { className: "lk-line", "data-role": s ? "user" : "agent", children: [
      /* @__PURE__ */ Se.jsx("span", { className: "lk-who", children: s ? "You" : "AI" }),
      i.message
    ] }, i.id);
  }) }) : /* @__PURE__ */ Se.jsx("div", { className: "lk-hint", children: "Ask about your home — e.g. “turn on the study light”." });
}
class fP extends ek {
  constructor(e) {
    super(), this.store = e;
  }
  async fetch() {
    const { hass: e, config: t } = this.store.getSnapshot();
    if (!e) throw new Error("Home Assistant not ready");
    const i = t.input_mode === "auto" ? "auto" : "push_to_talk";
    return e.callApi("POST", "livekit_voice/token", { input_mode: i });
  }
}
const hP = "ha.tool_call";
function mP(a) {
  if (!a || typeof a == "string") return [];
  const e = a.area;
  return typeof e == "string" ? [e] : Array.isArray(e) ? e.map(String) : [];
}
function pP() {
  const a = Ho(), e = a.room, [t, i] = L.useState({});
  return L.useEffect(() => {
    if (!e) return;
    const s = new TextDecoder(), c = (u, d, h, f) => {
      if (f !== hP) return;
      let p;
      try {
        p = JSON.parse(s.decode(u));
      } catch {
        return;
      }
      const v = p.update;
      if (!v) return;
      const y = typeof p.created_at == "number" ? p.created_at * 1e3 : Date.now();
      if (v.type === "tool_call_started") {
        const T = v.function_call ?? {}, _ = String(T.call_id);
        let k = null;
        if (typeof T.arguments == "string" && T.arguments)
          try {
            k = JSON.parse(T.arguments);
          } catch {
            k = T.arguments;
          }
        i((x) => ({
          ...x,
          [_]: { callId: _, name: String(T.name), args: k, status: "running", startedAt: y }
        }));
      } else if (v.type === "tool_call_ended") {
        const T = String(v.call_id);
        i((_) => {
          const k = _[T] ?? { callId: T, name: "tool", args: null, status: "running", startedAt: y };
          return {
            ..._,
            [T]: {
              ...k,
              status: v.status ?? "done",
              output: v.message ?? k.output ?? null,
              endedAt: y
            }
          };
        });
      } else if (v.type === "tool_call_updated") {
        const T = String(v.call_id);
        i((_) => {
          const k = _[T];
          return k ? { ..._, [T]: { ...k, output: v.message ?? k.output } } : _;
        });
      }
    };
    return e.on(j.DataReceived, c), () => {
      e.off(j.DataReceived, c);
    };
  }, [e]), L.useEffect(() => {
    a.isConnected || i({});
  }, [a.isConnected]), L.useMemo(() => {
    const s = Object.values(t).sort((u, d) => u.startedAt - d.startedAt), c = /* @__PURE__ */ new Set();
    for (const u of s) for (const d of mP(u.args)) c.add(d);
    return { toolCalls: s, agentAreas: [...c] };
  }, [t]);
}
const gP = /* @__PURE__ */ new Set(["listening", "thinking", "speaking"]);
function vP({ store: a }) {
  return /* @__PURE__ */ Se.jsx(KD, { value: a, children: /* @__PURE__ */ Se.jsx(yP, {}) });
}
function yP() {
  const a = bu(), e = L.useMemo(() => new fP(a), [a]), t = BD(e, {});
  return /* @__PURE__ */ Se.jsxs(VD, { session: t, children: [
    /* @__PURE__ */ Se.jsx(HD, {}),
    /* @__PURE__ */ Se.jsx(bP, {})
  ] });
}
function bP() {
  const a = Lk(), e = Vm(), t = Ho(), { state: i } = Hm(), { toolCalls: s, agentAreas: c } = pP(), u = e.input_mode !== "push_to_talk", d = t.room?.localParticipant;
  L.useEffect(() => {
    !t.isConnected || !d || d.setMicrophoneEnabled(u);
  }, [u, t.isConnected, d]);
  const h = t.isConnected, f = gP.has(i ?? ""), p = h ? i || "ready" : "offline";
  return /* @__PURE__ */ Se.jsxs("ha-card", { children: [
    /* @__PURE__ */ Se.jsxs("div", { className: "lk-header", children: [
      /* @__PURE__ */ Se.jsx("span", { className: "lk-title", children: e.title || "Voice Assistant" }),
      /* @__PURE__ */ Se.jsxs("span", { className: "lk-state", children: [
        /* @__PURE__ */ Se.jsx("span", { className: "lk-dot", "data-active": h ? "1" : "0", "data-pulse": f ? "1" : "0" }),
        p
      ] })
    ] }),
    /* @__PURE__ */ Se.jsx(sP, { agentAreas: c }),
    h ? /* @__PURE__ */ Se.jsxs(Se.Fragment, { children: [
      /* @__PURE__ */ Se.jsx(dP, {}),
      /* @__PURE__ */ Se.jsx(uP, { toolCalls: s }),
      /* @__PURE__ */ Se.jsx(GD, {}),
      /* @__PURE__ */ Se.jsx(QD, {})
    ] }) : /* @__PURE__ */ Se.jsx("button", { className: "lk-btn", onClick: () => t.start?.(), disabled: !a, children: "Start voice" })
  ] });
}
const j0 = { type: "livekit-voice-card" };
class TP {
  constructor() {
    this._snapshot = { hass: null, config: j0 }, this._host = null, this._listeners = /* @__PURE__ */ new Set(), this.subscribe = (e) => (this._listeners.add(e), () => {
      this._listeners.delete(e);
    }), this.getSnapshot = () => this._snapshot;
  }
  setHass(e) {
    this._snapshot = { ...this._snapshot, hass: e }, this._emit();
  }
  setConfig(e) {
    this._snapshot = { ...this._snapshot, config: e || j0 }, this._emit();
  }
  setHost(e) {
    this._host = e;
  }
  get host() {
    return this._host;
  }
  _emit() {
    for (const e of this._listeners) e();
  }
}
const SP = `
  :host { display: block; }
  * { box-sizing: border-box; }

  ha-card {
    padding: 12px 14px 14px;
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .lk-header { display: flex; align-items: center; gap: 8px; }
  .lk-title { font-size: 1rem; font-weight: 600; color: var(--primary-text-color); }
  .lk-state {
    margin-left: auto; display: inline-flex; align-items: center; gap: 6px;
    font-size: 0.8rem; color: var(--secondary-text-color); text-transform: capitalize;
  }
  .lk-dot { width: 8px; height: 8px; border-radius: 50%; background: var(--disabled-text-color, #9e9e9e); }
  .lk-dot[data-active="1"] { background: var(--primary-color); }
  .lk-dot[data-pulse="1"] { animation: lk-pulse 1.4s ease-in-out infinite; }
  @keyframes lk-pulse { 0%,100% { opacity: 1; } 50% { opacity: 0.35; } }

  /* Device tiles */
  .lk-tiles { display: grid; grid-template-columns: repeat(auto-fill, minmax(96px, 1fr)); gap: 8px; }
  .lk-tile {
    font: inherit; text-align: left; cursor: pointer;
    display: flex; flex-direction: column; gap: 8px;
    border: 1px solid var(--divider-color); border-radius: 14px; padding: 10px;
    background: var(--card-background-color); color: var(--primary-text-color);
    transition: border-color .15s ease, background-color .15s ease, transform .05s ease;
    min-width: 0;
  }
  .lk-tile:active { transform: scale(0.97); }
  .lk-tile[data-active="1"] {
    border-color: transparent;
    box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--primary-color) 45%, transparent);
    background: color-mix(in srgb, var(--primary-color) 10%, var(--card-background-color));
  }
  .lk-tile[data-touched="1"] { outline: 2px solid color-mix(in srgb, var(--primary-color) 55%, transparent); outline-offset: 1px; }
  .lk-tile-top { display: flex; align-items: center; justify-content: space-between; }
  .lk-tile-icon {
    --mdc-icon-size: 20px; width: 34px; height: 34px; border-radius: 10px;
    display: inline-flex; align-items: center; justify-content: center;
    background: var(--secondary-background-color); color: var(--secondary-text-color);
  }
  .lk-tile[data-active="1"] .lk-tile-icon { background: var(--primary-color); color: var(--text-primary-color, #fff); }
  .lk-tile-state { font-size: 1.05rem; font-weight: 600; line-height: 1.1; }
  .lk-tile-name {
    font-size: 0.75rem; color: var(--secondary-text-color);
    white-space: nowrap; overflow: hidden; text-overflow: ellipsis;
  }

  /* Transcript */
  .lk-transcript {
    display: flex; flex-direction: column; gap: 6px;
    max-height: 220px; overflow-y: auto; font-size: 0.9rem; line-height: 1.35;
  }
  .lk-line { color: var(--primary-text-color); }
  .lk-line[data-role="user"] { color: var(--secondary-text-color); }
  .lk-who {
    font-size: 0.68rem; text-transform: uppercase; letter-spacing: .04em;
    color: var(--secondary-text-color); margin-right: 6px;
  }

  /* Tool cards */
  .lk-tools { display: flex; flex-direction: column; gap: 6px; }
  .lk-tool {
    border: 1px solid var(--divider-color); border-radius: 10px; padding: 8px 10px;
    font-size: 0.82rem; display: flex; align-items: center; gap: 8px;
  }
  .lk-tool-name { font-weight: 600; color: var(--primary-text-color); }
  .lk-tool-args { color: var(--secondary-text-color); overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .lk-tool-badge { margin-left: auto; font-size: 0.7rem; padding: 2px 8px; border-radius: 999px; }
  .lk-tool-badge[data-s="running"] { background: color-mix(in srgb, var(--primary-color) 18%, transparent); color: var(--primary-color); }
  .lk-tool-badge[data-s="done"] { background: color-mix(in srgb, var(--success-color, #2e7d32) 18%, transparent); color: var(--success-color, #2e7d32); }
  .lk-tool-badge[data-s="error"], .lk-tool-badge[data-s="cancelled"] { background: color-mix(in srgb, var(--error-color, #db4437) 18%, transparent); color: var(--error-color, #db4437); }

  /* Controls / input */
  .lk-hint { color: var(--secondary-text-color); font-size: 0.85rem; text-align: center; padding: 4px 0; }
  .lk-controls { display: flex; align-items: center; gap: 8px; }
  .lk-input { display: flex; align-items: flex-end; gap: 8px; flex: 1; }
  .lk-input textarea {
    font: inherit; flex: 1; resize: none; min-height: 38px; max-height: 96px;
    border: 1px solid var(--divider-color); border-radius: 10px; padding: 8px 10px;
    background: var(--card-background-color); color: var(--primary-text-color);
  }
  .lk-input textarea:focus { outline: none; border-color: var(--primary-color); }

  button.lk-btn {
    font: inherit; cursor: pointer; border: none; border-radius: 999px;
    padding: 10px 18px; font-weight: 600;
    color: var(--text-primary-color, #fff); background: var(--primary-color);
    transition: opacity .2s ease, transform .05s ease; -webkit-user-select: none; user-select: none;
  }
  button.lk-btn:active { transform: scale(0.98); }
  button.lk-btn:disabled { opacity: .5; cursor: default; }
  button.lk-icon-btn {
    font: inherit; cursor: pointer; width: 40px; height: 40px; border-radius: 999px;
    display: inline-flex; align-items: center; justify-content: center;
    border: 1px solid var(--divider-color); background: var(--card-background-color); color: var(--primary-text-color);
  }
  button.lk-icon-btn[data-on="1"] { background: var(--primary-color); color: var(--text-primary-color, #fff); border-color: transparent; }
  button.lk-secondary { background: transparent; color: var(--secondary-text-color); border: 1px solid var(--divider-color); }
  button.lk-danger { background: color-mix(in srgb, var(--error-color, #db4437) 14%, transparent); color: var(--error-color, #db4437); }
  button.lk-talk[data-holding="1"] { background: var(--error-color, #db4437); }

  .lk-error { color: var(--error-color, #db4437); font-size: 0.85rem; text-align: center; }
`;
class kP extends HTMLElement {
  constructor() {
    super(...arguments), this._store = new TP(), this._root = null, this._mount = null;
  }
  setConfig(e) {
    this._store.setConfig(e), this._render();
  }
  set hass(e) {
    this._store.setHass(e), this._render();
  }
  getCardSize() {
    return 8;
  }
  connectedCallback() {
    this._render();
  }
  _render() {
    if (!this._mount) {
      const e = this.shadowRoot ?? this.attachShadow({ mode: "open" }), t = document.createElement("style");
      t.textContent = SP;
      const i = document.createElement("div");
      e.append(t, i), this._store.setHost(this), this._mount = i;
    }
    this._root || (this._root = p_.createRoot(this._mount)), this._root.render(/* @__PURE__ */ Se.jsx(vP, { store: this._store }));
  }
}
customElements.define("livekit-voice-card", kP);
window.customCards = window.customCards || [];
window.customCards.push({
  type: "livekit-voice-card",
  name: "LiveKit Voice Assistant",
  description: "Talk to your Home Assistant voice agent and control devices from the dashboard.",
  preview: !1
});
